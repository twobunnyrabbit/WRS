% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{rm.marg.esCI}
\alias{rm.marg.esCI}
\title{Bootstrap Confidence Interval for Robust Marginal Effect Size}
\usage{
rm.marg.esCI(
  x,
  y = NULL,
  tr = 0.2,
  nboot = 1000,
  SEED = TRUE,
  alpha = 0.05,
  null.val = 0,
  MC = FALSE,
  ...
)
}
\arguments{
\item{x}{Either a two-column matrix/data frame or a vector (if y is specified).}

\item{y}{Optional second variable. If NULL, x must be a two-column matrix.}

\item{tr}{Trimming proportion for robust estimators (default: .2).}

\item{nboot}{Number of bootstrap samples (default: 1000).}

\item{SEED}{Logical; if \code{TRUE} (default), set random seed to 2 for reproducibility.}

\item{alpha}{Nominal alpha level for the confidence interval (default: .05).}

\item{null.val}{Null value for computing p-value (default: 0).}

\item{MC}{Logical; if \code{TRUE}, use parallel processing via \code{mclapply} (default: FALSE).}

\item{...}{Additional arguments (currently not used).}
}
\value{
A list with components:
\item{n}{Sample size (after removing missing values)}
\item{effect.size}{Estimated robust marginal effect size}
\item{ci}{Bootstrap confidence interval for the effect size}
\item{p.value}{Two-sided p-value for testing H0: effect size = null.val}
}
\description{
Computes a bootstrap confidence interval for the robust marginal effect size
for two dependent groups. This version accounts for heteroscedasticity and
association using marginal distributions rather than difference scores.
}
\details{
This function provides inference for the robust marginal effect size computed by
\code{\link{rm.marg.es}}. The bootstrap percentile method is used to construct
confidence intervals.

For robust estimators with dependent data, effect sizes based on marginal
distributions (as computed here) generally differ from those based on difference
scores, as they account for the association structure differently.

The p-value tests H0: effect size = null.val (default: 0) using a two-sided test.

Missing values are automatically removed.
}
\seealso{
\code{\link{rm.marg.es}}, \code{\link{rm.margOM.es}},
\code{\link{D.akp.effect.ci}}
}
\keyword{htest}
