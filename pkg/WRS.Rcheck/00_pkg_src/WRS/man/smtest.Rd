% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{smtest}
\alias{smtest}
\title{Test for Equal Location Measures Across Quantile-Defined Groups}
\usage{
smtest(
  x,
  y,
  IV = 1,
  Qsplit = 0.5,
  nboot = 1000,
  est = tmean,
  tr = 0.2,
  PB = FALSE,
  xout = FALSE,
  outfun = outpro,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{x}{Matrix of independent variables (predictors).}

\item{y}{Vector of dependent variable (response).}

\item{IV}{Integer specifying which independent variable to split on (column index).
Default is 1 (first column).}

\item{Qsplit}{Vector of quantiles for splitting the independent variable.
Default is 0.5 (median split). Example: \code{c(.25, .5, .75)} splits at quartiles.}

\item{nboot}{Number of bootstrap samples when using percentile bootstrap.
Default is 1000.}

\item{est}{Measure of location. Default is \code{\link{tmean}} (20\% trimmed mean).}

\item{tr}{Amount of trimming when using non-bootstrap method. Default is 0.2.}

\item{PB}{Logical. If \code{TRUE}, use percentile bootstrap method. Default is \code{FALSE}.
Automatically set to \code{TRUE} if \code{est} is \code{median} or \code{hd}.}

\item{xout}{Logical. If \code{TRUE}, remove multivariate outliers from predictors.
Default is \code{FALSE}.}

\item{outfun}{Outlier detection function. Default is \code{\link{outpro}}.}

\item{SEED}{Logical. If \code{TRUE} (default), set random seed for reproducibility.}

\item{...}{Additional arguments passed to \code{est} or other functions.}
}
\value{
List with components:
\item{Indep.var.summary.for.each.group}{Summary statistics for the independent
variable within each group}
\item{output}{Results from \code{\link{lincon}} or \code{\link{linconpb}}}
}
\description{
Tests the hypothesis of equal measures of location across groups defined by
quantile splits of an independent variable in multivariate data.
}
\details{
The function:
\enumerate{
\item Splits data into groups based on quantiles of the specified independent variable
\item Extracts the dependent variable values for each group
\item Tests for equal location measures using either:
\itemize{
\item \code{\link{lincon}} for trimmed means (when \code{PB=FALSE})
\item \code{\link{linconpb}} for percentile bootstrap (when \code{PB=TRUE})
}
}

Example: \code{Qsplit=c(.25, .5, .75)} creates 4 groups (below Q1, Q1-Q2, Q2-Q3, above Q3).
}
\seealso{
\code{\link{lincon}}, \code{\link{linconpb}}, \code{\link{binmat}},
\code{\link{smgrid}}
}
\keyword{htest}
\keyword{robust}
