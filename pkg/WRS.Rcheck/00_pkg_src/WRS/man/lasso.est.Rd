% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classification.R
\name{lasso.est}
\alias{lasso.est}
\title{LASSO Regression with Outlier Detection}
\usage{
lasso.est(
  x,
  y,
  xout = FALSE,
  STAND = TRUE,
  outfun = outpro,
  regout = FALSE,
  lam = NULL,
  ...
)
}
\arguments{
\item{x}{Predictor matrix (n x p). Can be a matrix or data frame.}

\item{y}{Response vector of length n.}

\item{xout}{Logical. If TRUE, eliminate leverage points using \code{outfun} before
fitting (default: FALSE).}

\item{STAND}{Logical. If TRUE (default), standardize predictors before fitting.}

\item{outfun}{Function for detecting outliers/leverage points (default: \code{outpro}).
Only used if \code{xout=TRUE} or \code{regout=TRUE}.}

\item{regout}{Logical. If TRUE, eliminate regression outliers using the \code{elo}
function before fitting (default: FALSE). This option removes both leverage points
and regression outliers.}

\item{lam}{Optional vector of lambda values for cross-validation. If NULL, glmnet
chooses the lambda sequence automatically.}

\item{...}{Additional arguments passed to the outlier detection function \code{outfun}.}
}
\value{
A list with components:
\item{coef}{Vector of estimated coefficients (including intercept). Length is
p+1 where the first element is the intercept.}
\item{lambda.min.used}{The lambda value that minimizes cross-validation error.}
}
\description{
Performs LASSO (Least Absolute Shrinkage and Selection Operator) regression
via the glmnet package, with options for eliminating leverage points and
regression outliers before fitting.
}
\details{
This function is a convenience wrapper for \code{cv.glmnet} from the glmnet package,
providing simplified output (coefficients only) and built-in outlier removal options.

\strong{LASSO regression} performs variable selection and regularization by adding an L1
penalty to the least squares objective. This shrinks some coefficients exactly to zero,
effectively performing automatic variable selection.

\strong{Outlier removal options:}
\itemize{
\item \code{xout=TRUE}: Remove leverage points (outliers in predictor space) using \code{outfun}
\item \code{regout=TRUE}: Remove both leverage points AND regression outliers (outliers in
residual space) using the \code{elo} function
\item If \code{regout=TRUE}, the \code{xout} parameter is ignored (elo handles both types)
}

Missing values are automatically removed before analysis.
}
\note{
\itemize{
\item This function requires the \code{glmnet} package to be installed
\item LASSO estimates can vary slightly across runs due to random aspects of cross-validation.
Use \code{\link{lasso.rep}} for more stable estimates via repeated fitting.
\item For more complete output and options, use \code{cv.glmnet} directly from glmnet package
}
}
\examples{
\dontrun{
# Generate data
set.seed(123)
x <- matrix(rnorm(100 * 5), ncol = 5)
y <- x[,1] + 2*x[,2] + rnorm(100)

# Basic LASSO
result <- lasso.est(x, y)
result$coef  # Coefficients (intercept + 5 slopes)

# With leverage point removal
lasso.est(x, y, xout = TRUE)

# With regression outlier removal
lasso.est(x, y, regout = TRUE)

# Without standardization
lasso.est(x, y, STAND = FALSE)
}

}
\seealso{
\code{\link{lasso.rep}} for repeated LASSO to identify stable coefficients,
\code{\link{ridge.test}} for ridge regression, \code{\link{elo}} for regression
outlier detection
}
