% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression-advanced.R
\name{regbtci}
\alias{regbtci}
\title{Bootstrap-t Confidence Intervals for Regression Parameters}
\usage{
regbtci(
  x,
  y,
  regfun = qreg,
  alpha = 0.05,
  nboot = 300,
  xout = FALSE,
  outfun = outpro,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{x}{A numeric matrix or vector containing predictor values.}

\item{y}{A numeric vector containing the response variable.}

\item{regfun}{Regression function to use (default: \code{qreg} for quantile regression).
Can be any regression function that returns a \code{coef} component.}

\item{alpha}{Significance level for confidence intervals (default: 0.05 for
95\% confidence intervals).}

\item{nboot}{Number of bootstrap samples for estimating standard errors
(default: 300).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
A list with components:
\item{output}{Matrix with rows for each parameter (intercept, slopes) and
columns: \code{ci.low} (lower CI bound), \code{ci.up} (upper CI bound), \code{Estimate}
(point estimate), \code{S.E.} (standard error), \code{p-value} (two-sided test of
H0: parameter = 0)}
\item{n}{Original sample size before outlier removal}
\item{n.keep}{Sample size after outlier removal (if \code{xout = TRUE})}
}
\description{
Computes bootstrap-t confidence intervals and hypothesis tests for regression
parameters (intercept and slopes). Works with any regression estimator.
}
\details{
This function uses the bootstrap-t method for inference on regression
parameters:
\enumerate{
\item Estimates standard errors via bootstrap (\code{regse()})
\item Constructs confidence intervals using normal approximation:
estimate ± z(1-α/2) × SE
\item Computes p-values for testing each parameter = 0 using z-statistics
}

The bootstrap-t approach provides better coverage than percentile bootstrap
in many situations, especially with smaller samples.

If \code{xout = TRUE}, outliers are removed from the predictor space before
analysis using the function specified by \code{outfun}.
}
\examples{
\dontrun{
# Bootstrap-t CIs for quantile regression
set.seed(123)
x <- matrix(rnorm(100), ncol=2)
y <- 2 + 3*x[,1] - x[,2] + rnorm(50)
regbtci(x, y)

# Use with Theil-Sen regression
regbtci(x, y, regfun=tsreg)
}
}
\references{
Wilcox, R.R. (2022). Introduction to Robust Estimation and Hypothesis Testing
(5th ed.). Academic Press.
}
\seealso{
\code{\link{regse}}, \code{\link{regci}}, \code{\link{tsreg}}
}
