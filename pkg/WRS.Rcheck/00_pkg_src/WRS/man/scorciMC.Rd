% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{scorciMC}
\alias{scorciMC}
\title{Skipped Correlation CI with Parallel Processing}
\usage{
scorciMC(
  x,
  y,
  nboot = 1000,
  alpha = 0.05,
  V2 = TRUE,
  SEED = TRUE,
  plotit = TRUE,
  STAND = TRUE,
  corfun = pcor,
  pr = TRUE,
  cop = 3,
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{V2}{Logical. If \code{TRUE} (default), uses improved version for n < 120.}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{STAND}{Logical. If \code{TRUE}, standardizes data before analysis
(default: \code{TRUE}).}

\item{corfun}{Correlation function to use. Common options: \code{pbcor} (percentage
bend), \code{pcor} (Pearson), \code{wincor} (winsorized), \code{spear} (Spearman).}

\item{pr}{Logical. If \code{TRUE}, prints progress messages (default: \code{TRUE}).}

\item{cop}{Copula type for skipped correlation (default: 3). Options: 1-5.}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
List with components:
\item{cor.ci}{Bootstrap confidence interval.}
\item{p.value}{Two-sided p-value.}
\item{cor.est}{Skipped correlation estimate.}
}
\description{
Computes bootstrap confidence interval for skipped correlation using parallel
processing via \code{mclapply()}.
}
\details{
This is the parallel version of \code{scorci()}, using \code{mclapply()} for faster
computation on multi-core systems. See \code{scorci()} for detailed methodology.

\strong{Note}: \code{mclapply()} does not work on Windows. Use \code{scorci()} on Windows.
}
\examples{
\dontrun{
# Fast skipped correlation bootstrap (not Windows)
set.seed(123)
x <- c(rnorm(80), 5, -5)
y <- c(0.7 * rnorm(80), -5, 5)
scorciMC(x, y, nboot = 2000)  # Much faster than scorci()
}
}
\seealso{
\code{\link{scorci}}, \code{\link{scor}}, \code{\link{corbMC}}
}
