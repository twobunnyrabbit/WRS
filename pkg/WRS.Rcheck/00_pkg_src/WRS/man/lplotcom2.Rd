% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{lplotcom2}
\alias{lplotcom2}
\title{Compare Predictor Importance with LOESS (Method 1)}
\usage{
lplotcom2(
  x,
  y,
  xout = FALSE,
  pts1 = NULL,
  pts2 = NULL,
  outfun = outpro,
  span = 2/3,
  npts = 10,
  tr = 0.2,
  ...
)
}
\arguments{
\item{x}{Matrix with 2 columns (two predictor variables).}

\item{y}{Response variable vector.}

\item{xout}{Logical; if TRUE, removes multivariate outliers from predictors (default: FALSE).}

\item{pts1}{Points at which to evaluate predictor 1 (default: computed from data).}

\item{pts2}{Points at which to evaluate predictor 2 (default: computed from data).}

\item{outfun}{Outlier detection function (default: \code{outpro}).}

\item{span}{Span parameter for LOESS (default: 2/3).}

\item{npts}{Number of evaluation points if pts1/pts2 not specified (default: 10).}

\item{tr}{Trimming proportion for Winsorized SD (default: 0.2).}

\item{...}{Additional arguments passed to outlier detection function.}
}
\value{
A list with components:
\item{str1}{Strength of predictor 1 at each point in pts2 (conditional SD ratio).}
\item{str2}{Strength of predictor 2 at each point in pts1 (conditional SD ratio).}
\item{p}{Proportion of pairwise comparisons where str1 > str2.}
\item{mean.str1}{Mean strength for predictor 1.}
\item{mean.str2}{Mean strength for predictor 2.}
}
\description{
For two predictors, estimates their relative importance when using LOESS regression.
Compares the conditional variability of predictions when one predictor is held fixed.
}
\details{
This function assesses \strong{predictor importance} by examining conditional variability.

For each predictor:
\enumerate{
\item Hold the other predictor fixed at several values
\item Compute LOESS predictions varying only the focal predictor
\item Measure the standard deviation of these predictions
\item Normalize by the overall SD of y
}

\strong{Strength} = (SD of conditional predictions) / (SD of y)

Higher strength indicates greater importance. The comparison p-value indicates
the proportion of comparisons where predictor 1 shows greater strength than predictor 2.

If pts1/pts2 are not specified, evaluation points are chosen as:
\deqn{Median \pm 1.5 \times MAD}

Uses \strong{Winsorized SD} for robust dispersion estimation.
}
\examples{
# Compare importance of two predictors
x1 <- rnorm(100)
x2 <- rnorm(100)
y <- 2*x1 + 0.5*x2 + rnorm(100)  # x1 more important
X <- cbind(x1, x2)
result <- lplotcom2(X, y, npts = 5)
result$mean.str1  # Strength for x1
result$mean.str2  # Strength for x2
result$p  # Proportion where x1 > x2
}
\seealso{
\code{\link{lplotcom2v2}}, \code{\link{lplotcomBCI}}, \code{\link{lplot}}
}
