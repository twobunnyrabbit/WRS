% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{anc.ES.sum}
\alias{anc.ES.sum}
\title{ANCOVA Effect Size Summary}
\usage{
anc.ES.sum(
  x1,
  y1,
  x2,
  y2,
  fr1 = 1,
  fr2 = 1,
  tr = 0.2,
  alpha = 0.05,
  pts = NA,
  SEED = TRUE,
  nboot = 1000,
  pr = TRUE,
  xout = FALSE,
  outfun = out,
  nmin = 12,
  NULL.V = c(0, 0, 0.5, 0.5, 0.5, 0),
  REL.M = NULL,
  n.est = 1e+06,
  ...
)
}
\arguments{
\item{x1}{Covariate values for group 1.}

\item{y1}{Response values for group 1.}

\item{x2}{Covariate values for group 2.}

\item{y2}{Response values for group 2.}

\item{fr1}{Span for running interval smoother for group 1 (default: 1).}

\item{fr2}{Span for running interval smoother for group 2 (default: 1).}

\item{tr}{Trimming proportion (default: 0.2). Proportion of observations to
trim from each tail when computing winsorized correlation.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{pts}{Design points. If NA, uses 5 empirically determined points.}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{pr}{Logical. If \code{TRUE}, prints progress messages (default: \code{TRUE}).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{nmin}{Minimum sample size at each design point (default: 12).}

\item{NULL.V}{Null values for effect size measures (default: c(0,0,0.5,0.5,0.5,0)).}

\item{REL.M}{Relative magnitude parameter for ES.summary.CI (default: NULL).}

\item{n.est}{Number of estimates for simulation (default: 1e6).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
List with components:
\item{n1}{Sample sizes at each design point for group 1}
\item{n2}{Sample sizes at each design point for group 2}
\item{pts}{Design points used}
\item{ES.4.Each.pt}{List of effect size results for each design point}
}
\description{
Computes multiple effect size measures at design points where regression lines
are compared. Uses ES.summary.CI to provide comprehensive effect size information
at each comparison point.
}
\note{
Only one covariate is allowed.
}
\examples{
\dontrun{
x1 <- rnorm(50)
y1 <- x1 + rnorm(50)
x2 <- rnorm(50)
y2 <- x2 + 0.5 + rnorm(50)
anc.ES.sum(x1, y1, x2, y2, nboot=500)
}
}
