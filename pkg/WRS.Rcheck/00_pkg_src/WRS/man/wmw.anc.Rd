% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{wmw.anc}
\alias{wmw.anc}
\title{Estimate P(Y1 < Y2) Conditional on Covariate Values}
\usage{
wmw.anc(x1, y1, x2, y2, pts, xout = FALSE, outfun = outpro)
}
\arguments{
\item{x1}{Matrix or vector of covariate(s) for group 1.}

\item{y1}{Vector of response variable for group 1.}

\item{x2}{Matrix or vector of covariate(s) for group 2.}

\item{y2}{Vector of response variable for group 2.}

\item{pts}{Matrix of covariate values at which to estimate P(Y1 < Y2).}

\item{xout}{Logical. If \code{TRUE}, removes outliers in covariates (default: \code{FALSE}).}

\item{outfun}{Function for outlier detection (default: \code{outpro}).}
}
\value{
Vector of P(Y1 < Y2) estimates, one for each row of \code{pts}.
}
\description{
Estimates P(Y1 < Y2) given that the covariate X equals specific values.
Used as the core computation for WMW-based ANCOVA methods.
}
\details{
For each specified covariate value in \code{pts}, this function estimates the
probability that a randomly selected observation from group 1 is less than
a randomly selected observation from group 2, conditional on the covariate
value.

The estimation uses regression-based conditional distributions via
\code{reg.con.dist} to approximate the conditional distributions at each
covariate value, then computes the probability empirically.
}
\seealso{
\code{\link{wmw.ancbse}}, \code{\link{reg.con.dist}}, \code{\link{outpro}}
}
\keyword{internal}
