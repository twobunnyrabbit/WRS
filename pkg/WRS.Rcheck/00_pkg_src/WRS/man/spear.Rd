% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{spear}
\alias{spear}
\title{Spearman's Rank Correlation}
\usage{
spear(x, y = NULL)
}
\arguments{
\item{x}{Numeric vector or matrix. If vector and \code{y} is provided, used as
first variable. If matrix, computes all pairwise correlations.}

\item{y}{Numeric vector or \code{NULL}. If provided with vector \code{x}, used as
second variable. If \code{NULL}, \code{x} must be a matrix. Default is \code{NULL}.}
}
\value{
List with components:
\item{cor}{Spearman correlation coefficient (or matrix for multivariate data)}
\item{p.value}{Two-tailed p-value(s) for test of H₀: ρ = 0}
}
\description{
Computes Spearman's rank correlation coefficient ρ and tests for association.
Works for bivariate or multivariate data.
}
\details{
Spearman's ρ is computed as the Pearson correlation of the ranks:
ρ = cor(rank(x), rank(y))

The test statistic is: t = ρ√((n-2)/(1-ρ²))

Under H₀: ρ = 0, this follows a t-distribution with n-2 degrees of freedom.

For bivariate data, returns a single correlation and p-value. For multivariate
data (matrix input with \code{y=NULL}), returns correlation matrix and matrix
of p-values.
}
\seealso{
\code{\link{spearci}}, \code{\link{pcor}}, \code{\link{wincor}}
}
\keyword{htest}
