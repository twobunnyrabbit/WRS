% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression-advanced.R
\name{regYci.sum}
\alias{regYci.sum}
\title{Summarize Confidence Intervals for Regression Predictions}
\usage{
regYci.sum(
  x,
  y,
  regfun = tsreg,
  pts = x,
  nboot = 100,
  xout = FALSE,
  outfun = out,
  SEED = TRUE,
  alpha = 0.05,
  crit = NULL,
  null.value = 0,
  plotPV = FALSE,
  ADJ = FALSE,
  MC = FALSE,
  scale = FALSE,
  span = 0.75,
  xlab = "X",
  xlab1 = "X1",
  xlab2 = "X2",
  ylab = "p-values",
  theta = 50,
  phi = 25,
  pch = "*",
  ...
)
}
\arguments{
\item{x}{A numeric vector or matrix containing predictor values (can be
multivariate).}

\item{y}{A numeric vector containing the response variable.}

\item{regfun}{Regression function to use (default: \code{tsreg} for Theil-Sen).
Can be any regression function that returns coefficients.}

\item{pts}{Points at which to compute confidence intervals (default: uses
all values in \code{x}).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{crit}{Optional user-specified critical value. If \code{NULL}, computed from
bootstrap (default: \code{NULL}).}

\item{null.value}{Null hypothesis value for testing (default: 0 for most
tests, indicating no difference or no correlation).}

\item{plotPV}{Logical. If \code{TRUE}, plots p-values vs. predictor values
(default: \code{FALSE}).}

\item{ADJ}{Logical. If \code{TRUE}, adjusts critical values so that the simultaneous
probability coverage is 1-alpha (i.e., creates a confidence band). If \code{FALSE},
computes pointwise confidence intervals (default: \code{FALSE}).}

\item{MC}{Logical. If \code{TRUE}, uses parallel processing via \code{mclapply}
(default: \code{FALSE}).}

\item{scale, span, theta, phi, pch}{Parameters for plotting.}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
A matrix with columns:
\itemize{
\item X: Predictor values (sorted in ascending order)
\item Pred. Y: Predicted Y value at each x
\item Lower.ci: Lower confidence limit
\item Upper.ci: Upper confidence limit
\item p.value: p-value for H0: predicted Y = null.value
}
}
\description{
Provides a more readable summary of confidence intervals for predicted Y
values from \code{regYci()}. Designed for single predictor models, organizing
results by x-values in ascending order.
}
\details{
This is a wrapper for \code{regYci()} that:
\itemize{
\item Restricts usage to single predictor models (p=1)
\item Sorts output by predictor values for easier interpretation
\item Returns results in matrix format with descriptive column names
}

All other functionality (bootstrap confidence intervals, outlier removal,
adjustment for simultaneous inference) is identical to \code{regYci()}.
}
\note{
This function will stop with an error if called with more than one
independent variable.
}
\examples{
\dontrun{
# Confidence intervals at each x value
set.seed(123)
x <- rnorm(50)
y <- 2*x + rnorm(50)
regYci.sum(x, y)

# At specific points
regYci.sum(x, y, pts=seq(-2, 2, by=0.5))
}
}
\references{
Wilcox, R.R. (2022). Introduction to Robust Estimation and Hypothesis Testing
(5th ed.). Academic Press.
}
\seealso{
\code{\link{regYci}}, \code{\link{regYband}}
}
