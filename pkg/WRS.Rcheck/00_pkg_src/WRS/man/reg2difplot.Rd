% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{reg2difplot}
\alias{reg2difplot}
\title{3D Plot of Regression Surface Differences Between Two Groups}
\usage{
reg2difplot(
  x1,
  y1,
  x2,
  y2,
  regfun = tsreg,
  pts = x1,
  xlab = "VAR 1",
  ylab = "VAR 2",
  zlab = "Group 2 minus Group 1",
  xout = FALSE,
  outfun = out,
  ALL = TRUE,
  pts.out = FALSE,
  SCAT = FALSE,
  theta = 50,
  phi = 25,
  ticktype = "simple",
  pr = TRUE,
  ...
)
}
\arguments{
\item{x1}{Matrix with 2 columns of predictor values for group 1.}

\item{y1}{Numeric vector of response values for group 1.}

\item{x2}{Matrix with 2 columns of predictor values for group 2.}

\item{y2}{Numeric vector of response values for group 2.}

\item{regfun}{Regression function (default: \code{tsreg} for Theil-Sen).}

\item{pts}{Matrix with 2 columns defining points for prediction.
Default: \code{x1}.}

\item{xlab}{Label for x-axis (default: "VAR 1").}

\item{ylab}{Label for y-axis (default: "VAR 2").}

\item{zlab}{Label for z-axis (default: "Group 2 minus Group 1").}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{ALL}{Logical; if \code{TRUE} (default), uses all points from both groups
for prediction (overrides \code{pts}).}

\item{pts.out}{Logical; if \code{TRUE}, removes leverage points from \code{pts}
using \code{outfun} (default: \code{FALSE}).}

\item{SCAT}{Logical; if \code{TRUE}, uses \code{scatterplot3d} package for plotting.
If \code{FALSE} (default), uses \code{rplot()}.}

\item{theta}{Rotation angle for 3D plot (default: 50).}

\item{phi}{Colatitude angle for 3D plot (default: 25).}

\item{ticktype}{Tick mark type (default: "simple").}

\item{pr}{Logical; if \code{TRUE} (default), prints messages.}

\item{...}{Additional arguments passed to \code{regfun} or \code{outfun}.}
}
\value{
Invisibly returns \code{NULL}. Creates 3D plot as side effect.
}
\description{
Fits regression models to two groups with two predictors each, then
plots the difference in predicted values as a 3D surface. Useful for
visualizing how group differences vary across the predictor space.
}
\details{
The function:
\enumerate{
\item Fits regression models to both groups
\item Computes predicted values at points in \code{pts}
\item Computes difference: prediction(group2) - prediction(group1)
\item Creates 3D surface plot of differences
}

Useful for assessing whether group differences are constant across
the predictor space (parallel surfaces) or vary (interaction effects).
}
\examples{
# Two groups with different regression relationships
x1 <- matrix(rnorm(100), ncol=2)
y1 <- x1[,1] + x1[,2] + rnorm(50)
x2 <- matrix(rnorm(100), ncol=2)
y2 <- 2*x2[,1] + x2[,2] + rnorm(50)
reg2difplot(x1, y1, x2, y2)
}
\seealso{
\code{\link{reg2plot}}, \code{\link{regYhat}}, \code{\link{rplot}}
}
