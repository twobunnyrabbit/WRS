% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{qsmcobs}
\alias{qsmcobs}
\title{Quantile Regression Smoothing Using COBS}
\usage{
qsmcobs(
  x,
  y,
  qval = 0.5,
  xlab = "X",
  ylab = "Y",
  FIT = TRUE,
  pc = ".",
  plotit = TRUE,
  xout = FALSE,
  outfun = out,
  q = NULL,
  lambda = 0,
  ...
)
}
\arguments{
\item{x}{Numeric vector. Predictor variable.}

\item{y}{Numeric vector. Response variable.}

\item{qval}{Numeric. Quantile value(s) to estimate (default: 0.5).
Can be a vector to plot multiple quantiles.}

\item{xlab}{Label for x-axis (default: "X").}

\item{ylab}{Label for y-axis (default: "Y").}

\item{FIT}{Logical. If \code{TRUE}, use values from \code{predict()};
if \code{FALSE}, use fitted values directly (default: \code{TRUE}).}

\item{pc}{Plotting character for scatter plot (default: ".").}

\item{plotit}{Logical. If \code{TRUE}, produce plot (default: \code{TRUE}).}

\item{xout}{Logical. If \code{TRUE}, remove outliers based on x (default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{\link{out}}).}

\item{q}{Alternative name for \code{qval} parameter (overrides \code{qval} if specified).}

\item{lambda}{Smoothing parameter for COBS (default: 0). Larger values = smoother fits.}

\item{...}{Additional arguments passed to \code{outfun}.}
}
\value{
A list with component \code{yhat}:
\itemize{
\item If one quantile and \code{FIT=FALSE}: vector of fitted values
\item Otherwise: NULL
}
}
\description{
Plots smooth quantile regression lines using the constrained B-splines (COBS)
method from the \pkg{cobs} package. Supports multiple quantiles simultaneously.
}
\details{
Uses constrained B-splines (\code{\link[cobs]{cobs}}) for quantile regression smoothing.
For each quantile in \code{qval}:
\enumerate{
\item Fits COBS quantile regression with specified \code{lambda}
\item Plots the smooth curve
\item If only one quantile specified and \code{FIT=FALSE}, returns fitted values
}

COBS provides smooth, shape-preserving quantile regression curves and is
particularly useful for non-linear relationships.
}
\note{
Requires the \pkg{cobs} package to be installed.
}
\seealso{
\code{\link{qsm}}, \code{\link[cobs]{cobs}}, \code{\link{runmq}}
}
\keyword{regression}
\keyword{smooth}
