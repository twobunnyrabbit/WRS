% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{rplotCV}
\alias{rplotCV}
\title{Cross-Validation Prediction Error for Running Interval Smoother}
\usage{
rplotCV(
  x,
  y,
  fr = NA,
  varfun = pbvar,
  est = tmean,
  xout = FALSE,
  outfun = out,
  eout = FALSE,
  corfun = pbvar,
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix of predictor values.}

\item{y}{Numeric vector of response values.}

\item{fr}{Span for running interval smoother. If \code{NA} (default), uses 0.8
for univariate and 1.0 for multivariate predictors.}

\item{varfun}{Function to measure variation in predicted Y values
(default: \code{pbvar} for percentage bend variance).}

\item{est}{Measure of location used by the smoother (default: \code{tmean}
for trimmed mean).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{eout}{Logical value. If \code{TRUE}, outliers are removed based on
eliminating points with large residuals or leverage (default: \code{FALSE}).}

\item{corfun}{Correlation function (currently unused, for future extension).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
List with component:
\item{VAR.Y.HAT}{Measure of variation in cross-validated residuals.}
}
\description{
Estimates prediction error for a running interval smoother using
leave-one-out cross-validation. Useful for assessing model fit quality
and comparing different smoothing parameters.
}
\details{
For each observation, the function:
\enumerate{
\item Removes that observation
\item Fits a running interval smoother to the remaining data
\item Predicts the removed observation
\item Computes residuals (observed - predicted)
\item Measures variation in residuals using \code{varfun}
}

Lower values indicate better predictive performance. Can be used with
multivariate predictors via \code{rung3hat()}.
}
\examples{
# Assess prediction error for different smoothing spans
x <- rnorm(100)
y <- sin(x) + rnorm(100, sd=0.2)
rplotCV(x, y, fr=0.5)
rplotCV(x, y, fr=0.8)
}
\seealso{
\code{\link{rplot}}, \code{\link{runhat}}, \code{\link{rung3hat}}
}
