% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{wmw.bcapv.v2}
\alias{wmw.bcapv.v2}
\title{BCA P-value for P(X<Y) Using Iteration}
\usage{
wmw.bcapv.v2(x, y, nboot = 500, iter = 100, SEED = TRUE, ...)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{iter}{Number of iterations for averaging p-values (default: 100).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{...}{Additional arguments passed to \code{wmw.bcapv}.}
}
\value{
A list with components:
\item{n1}{Sample size of group 1}
\item{n2}{Sample size of group 2}
\item{phat}{Estimated probability P(X<Y)}
\item{p.value}{Average p-value across iterations}
}
\description{
Computes a BCA bootstrap p-value for testing H0: P(X<Y) = 0.5 using
an iterative resampling approach for improved stability.
}
\details{
This function provides a more stable p-value estimate by computing
multiple bootstrap p-values (using \code{wmw.bcapv}) on subsamples of
size min(n1, n2) and averaging the results.

Each iteration randomly samples min(n1, n2) observations from both
groups and computes a p-value. The final p-value is the average
across all iterations.

Requires the \code{bcaboot} package.
}
\examples{
x <- rnorm(40, mean=100, sd=15)
y <- rnorm(35, mean=105, sd=15)
wmw.bcapv.v2(x, y, nboot=500, iter=50)
}
\seealso{
\code{\link{wmw.bcapv}}, \code{\link{wmw.bcav2}}
}
