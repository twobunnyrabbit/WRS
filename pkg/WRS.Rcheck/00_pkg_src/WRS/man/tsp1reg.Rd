% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression.R
\name{tsp1reg}
\alias{tsp1reg}
\title{Single Predictor Theil-Sen Regression}
\usage{
tsp1reg(x, y, plotit = FALSE, HD = FALSE, OPT = TRUE, tr = FALSE)
}
\arguments{
\item{x}{A numeric vector containing the predictor variable.}

\item{y}{A numeric vector containing the response variable (must have same
length as \code{x}).}

\item{plotit}{Logical. If \code{TRUE}, creates a scatterplot with the fitted
regression line (default: \code{FALSE}).}

\item{HD}{Logical. If \code{TRUE}, uses Harrell-Davis estimator for computing
the slope and intercept; if \code{FALSE}, uses sample median (default: \code{FALSE}).}

\item{OPT}{Logical. If \code{TRUE}, computes intercept as \code{median(y) - slope*median(X)};
if \code{FALSE}, computes intercept as \code{median(y - slope*X)} (default: \code{TRUE}).}

\item{tr}{Logical or numeric. Trimming parameter for Harrell-Davis estimator
when \code{HD=TRUE} (default: \code{FALSE} for no trimming).}
}
\value{
A list with components:
\item{coef}{Numeric vector of length 2 containing intercept and slope}
\item{residuals}{Vector of residuals (observed - fitted values)}
}
\description{
Computes the Theil-Sen robust regression estimator for a single predictor.
This function is used internally by \code{\link{tsreg}} and \code{\link{tshdreg}}
but can also be called directly for simple linear regression.
}
\details{
For a single predictor, the Theil-Sen slope is computed as the median of all
pairwise slopes (y\link{j} - y\link{i}) / (x\link{j} - x\link{i}) for all pairs where x\link{j} > x\link{i}.
When \code{HD=TRUE}, the Harrell-Davis estimator (a weighted average of order statistics)
is used instead of the sample median, which can provide improved efficiency.

The intercept can be computed in two ways controlled by \code{OPT}:
\itemize{
\item \code{OPT=TRUE}: intercept = median(y) - slope * median(x)
\item \code{OPT=FALSE}: intercept = median(y - slope * x)
}

Missing values are automatically removed before computation.
}
\examples{
# Simple linear regression
x <- rnorm(50)
y <- 2 + 3*x + rnorm(50)
result <- tsp1reg(x, y)
result$coef  # Intercept and slope

# With plot
tsp1reg(x, y, plotit=TRUE)

# Using Harrell-Davis estimator
tsp1reg(x, y, HD=TRUE)

# Different intercept computation
tsp1reg(x, y, OPT=FALSE)

}
\references{
Theil, H. (1950). A rank-invariant method of linear and polynomial regression
analysis. \emph{Indagationes Mathematicae}, 12, 85-91.

Sen, P. K. (1968). Estimates of the regression coefficient based on Kendall's tau.
\emph{Journal of the American Statistical Association}, 63, 1379-1389.
}
\seealso{
\code{\link{tsreg}} for multiple predictor version,
\code{\link{tshdreg}} for Harrell-Davis variant, \code{\link{hd}}
for Harrell-Davis estimator
}
