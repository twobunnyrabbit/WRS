% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{hc4test}
\alias{hc4test}
\title{Omnibus Test for OLS Slopes Using HC4 Estimator}
\usage{
hc4test(
  x,
  y,
  pval = c(1:ncol(x)),
  xout = FALSE,
  outfun = outpro,
  pr = TRUE,
  plotit = FALSE,
  xlab = "X",
  ylab = "Y",
  ...
)
}
\arguments{
\item{x}{Predictor matrix (n x p) or vector (for simple regression).}

\item{y}{Response vector (length n).}

\item{pval}{Vector of indices of coefficients to test (default: all slopes).}

\item{xout}{Logical. If \code{TRUE}, removes outliers from predictors using
\code{outfun} before fitting.}

\item{outfun}{Outlier detection function (default: \code{\link{outpro}}).}

\item{pr}{Logical. If \code{TRUE}, prints warning when p > 1.}

\item{plotit}{Logical. If \code{TRUE} and p = 1, plots data with regression line.}

\item{xlab}{Label for x-axis in plot (default: "X").}

\item{ylab}{Label for y-axis in plot (default: "Y").}

\item{...}{Additional arguments passed to \code{outfun}.}
}
\value{
A list with components:
\item{n}{Original sample size}
\item{n.keep}{Sample size after outlier removal (if \code{xout = TRUE})}
\item{test}{Chi-squared test statistic}
\item{p.value}{P-value from chi-squared distribution}
\item{coef}{OLS coefficient estimates}
}
\description{
Performs an omnibus test of the hypothesis that all slope parameters equal
zero in OLS regression, using the HC4 heteroscedasticity-consistent covariance
estimator. Recommended by Cribari-Neto (2004).
}
\details{
This function tests \eqn{H_0: \beta_1 = \beta_2 = ... = \beta_p = 0} using
a Wald-type test statistic with the HC4 covariance estimator.

\strong{HC4 Estimator}: More robust to heteroscedasticity and high leverage points
than HC0-HC3 estimators. Recommended for small to moderate sample sizes.

\strong{Performance Notes}:
\itemize{
\item Works well with p = 1 (simple regression)
\item Can be unsatisfactory with p > 4 predictors
\item Required sample size for p > 1 is not well established
\item For p > 1, consider \code{\link{olstest}} instead
}

The test statistic follows an approximate chi-squared distribution with
degrees of freedom equal to the number of slopes being tested.
}
\examples{
\dontrun{
# Simple regression with heteroscedastic errors
x <- rnorm(50)
y <- 2*x + rnorm(50, sd = 1 + abs(x))

# Test if slope is zero
hc4test(x, y)
}

}
\references{
Cribari-Neto, F. (2004). Asymptotic inference under heteroskedasticity of
unknown form. Computational Statistics & Data Analysis, 45, 215-233.
}
\seealso{
\code{\link{hc4qtest}}, \code{\link{hc4wtest}}, \code{\link{olshc4}},
\code{\link{olstest}}
}
\keyword{htest}
\keyword{regression}
