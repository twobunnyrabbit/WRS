% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{mulwmw}
\alias{mulwmw}
\title{Multivariate WMW Test Using Projection onto Line Connecting Centers}
\usage{
mulwmw(
  m1,
  m2,
  plotit = TRUE,
  cop = 3,
  alpha = 0.05,
  nboot = 1000,
  pop = 4,
  fr = 0.8,
  pr = FALSE,
  SEED = TRUE,
  tr = 0.5,
  NC = TRUE
)
}
\arguments{
\item{m1}{Matrix or data frame for group 1 (must have 2+ columns).}

\item{m2}{Matrix or data frame for group 2 (must have 2+ columns).}

\item{plotit}{Logical. If \code{TRUE}, creates a plot of projected data (default: \code{TRUE}).}

\item{cop}{Method for computing the center of each group (default: 3):
\itemize{
\item 1 = Donoho-Gasko (Tukey) median
\item 2 = MCD (Minimum Covariance Determinant) center
\item 3 = Median of marginal distributions
\item 4 = Modified M-estimator (smean)
}}

\item{alpha}{Significance level for confidence interval (default: 0.05).}

\item{nboot}{Number of bootstrap samples (default: 1000).}

\item{pop}{Plot type when \code{plotit = TRUE} (default: 4):
\itemize{
\item 1 = Two dotplots of projected distances
\item 2 = Boxplots
\item 3 = Expected frequency curve
\item 4 = Adaptive kernel density
}}

\item{fr}{Fraction parameter for span in plots (default: 0.8).}

\item{pr}{Logical. If \code{TRUE}, prints detailed output (default: \code{FALSE}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducibility (default: \code{TRUE}).}

\item{tr}{Trimming proportion for center computation when \code{cop = 3} (default: 0.5).}

\item{NC}{Logical. If \code{FALSE}, critical values not computed (default: \code{TRUE}).}
}
\value{
A list with components:
\itemize{
\item \code{p.value}: P-value for testing H0: P(X < Y) = 0.5
\item \code{ci}: Bootstrap confidence interval for P(X < Y)
\item \code{p.hat}: Estimated probability that group 1 < group 2
\item \code{n1}, \code{n2}: Sample sizes
\item \code{proj1}, \code{proj2}: Projected distances for each group
}
}
\description{
Compares two multivariate groups by projecting data onto the line connecting
the group centers, then estimating P(X < Y) using the projected distances.
This provides a distribution-free multivariate two-sample test.
}
\details{
This function reduces the multivariate two-sample problem to a univariate one
by projecting both groups onto the line connecting their centers. The projection
approach is:
\enumerate{
\item Compute the center of each group using the method specified by \code{cop}
\item Project all observations onto the line connecting these two centers
\item Use the WMW test on the projected distances
\item Estimate P(projected distance from group 1 < projected distance from group 2)
}

The choice of center (\code{cop}) affects robustness:
\itemize{
\item \strong{cop = 1}: Donoho-Gasko median (most robust, marked with +)
\item \strong{cop = 2}: MCD center (robust, good for elliptical data)
\item \strong{cop = 3}: Componentwise median (simple, fairly robust)
\item \strong{cop = 4}: Modified M-estimator (balance of efficiency and robustness)
}

Bootstrap confidence intervals are computed for the probability estimate.
}
\note{
Data must have at least 2 columns. For univariate data, use \code{wmw.ref.dif}
or related functions. Missing values are automatically removed.
}
\examples{
\dontrun{
# Generate bivariate data
set.seed(123)
m1 <- cbind(rnorm(50), rnorm(50))
m2 <- cbind(rnorm(50, mean = 0.5), rnorm(50, mean = 0.3))

# Compare groups with default settings (median of marginals)
result <- mulwmw(m1, m2)

# Use Donoho-Gasko median
mulwmw(m1, m2, cop = 1)

# Use MCD center with boxplots
mulwmw(m1, m2, cop = 2, pop = 2)

# Increase bootstrap samples
mulwmw(m1, m2, nboot = 2000)
}
}
\seealso{
\code{\link{mwmw}} for related multivariate WMW approach,
\code{\link{wmw.ref.dif}} for univariate WMW,
\code{\link{smean}} for modified M-estimator
}
