% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{qregsm}
\alias{qregsm}
\title{Quantile Regression Smoother}
\usage{
qregsm(
  x,
  y,
  est = hd,
  qval = 0.5,
  sm = TRUE,
  plotit = TRUE,
  pyhat = FALSE,
  fr = 0.8,
  nboot = 40,
  xlab = "X",
  ylab = "Y",
  xout = FALSE,
  outfun = outpro,
  STAND = TRUE,
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix of predictor variable(s)}

\item{y}{Numeric vector of response variable}

\item{est}{Function for quantile estimation (must be \code{\link{hd}} in current version)}

\item{qval}{Numeric value or vector specifying quantile(s) of interest (default: 0.5)}

\item{sm}{Logical; if TRUE, use bagging (bootstrap aggregating) (default: TRUE)}

\item{plotit}{Logical; if TRUE, produce plot (default: TRUE)}

\item{pyhat}{Logical; if TRUE, return predicted values (default: FALSE)}

\item{fr}{Numeric value for span parameter in smoothing (default: 0.8)}

\item{nboot}{Integer specifying number of bootstrap samples for bagging (default: 40)}

\item{xlab}{Character string for x-axis label (default: "X")}

\item{ylab}{Character string for y-axis label (default: "Y")}

\item{xout}{Logical; if TRUE, remove outliers in predictors (default: FALSE)}

\item{outfun}{Function for outlier detection (default: \code{\link{outpro}})}

\item{STAND}{Logical; if TRUE, standardize variables (default: TRUE)}

\item{...}{Additional arguments passed to \code{outfun}}
}
\value{
If pyhat=TRUE, returns matrix of predicted quantile values. Otherwise,
produces plot and returns NULL invisibly.
}
\description{
Performs smoothing of x versus the quantiles of y using bagging (bootstrap
aggregating). Creates smooth curves for one or more quantiles of the
conditional distribution of Y given X.
}
\details{
This function creates smooth curves for conditional quantiles using the
Harrell-Davis estimator. Multiple quantiles can be specified (e.g.,
qval=c(0.2, 0.5, 0.8) for lower quartile, median, and upper quartile).
Bagging improves stability of the smooth.
}
\seealso{
\code{\link{hd}}, \code{\link{qsm}}, \code{\link{rqsm}}
}
\keyword{regression}
\keyword{smooth}
