% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{pb2gen}
\alias{pb2gen}
\title{Percentile Bootstrap for Two-Group Comparisons (General Purpose)}
\usage{
pb2gen(
  x,
  y,
  alpha = 0.05,
  nboot = 2000,
  est = onestep,
  SEED = TRUE,
  pr = FALSE,
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{est}{Estimator function to use for computing location or other
statistics. Common choices include \code{mean}, \code{median}, \code{tmean} (trimmed mean),
\code{mom} (modified one-step M-estimator), \code{mest} (M-estimator).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{pr}{Logical. If \code{TRUE}, prints progress messages (default: \code{TRUE}).}

\item{...}{Additional arguments passed to the estimator function \code{est}.}
}
\value{
A list with components:
\itemize{
\item \code{est.1}: Estimate for group 1
\item \code{est.2}: Estimate for group 2
\item \code{est.dif}: Estimated difference (est.1 - est.2)
\item \code{ci}: Two-element vector with confidence interval bounds
\item \code{p.value}: P-value for testing H0: difference = 0
\item \code{sq.se}: Squared standard error of the bootstrap distribution
\item \code{n1}, \code{n2}: Sample sizes for groups 1 and 2
}
}
\description{
Computes a percentile bootstrap confidence interval for the difference
between any two parameters for independent groups. This is a general-purpose
two-sample bootstrap function.
}
\details{
This is a flexible function that can compare any parameter between two
independent groups using the percentile bootstrap method. By default, it
compares M-estimators of location (\code{est = onestep}).

Common choices for \code{est}:
\itemize{
\item \code{onestep} - One-step M-estimator (default)
\item \code{mean} - Arithmetic mean
\item \code{median} - Median
\item \code{tmean} - Trimmed mean (use with \code{trim = 0.2}, for example)
\item \code{mom} - Modified one-step M-estimator
\item \code{hd} - Harrell-Davis estimator (use with \code{q = 0.5} for median)
}

Missing values are automatically removed. The function uses \code{nboot} bootstrap
samples (default: 2000) to construct the confidence interval and compute the
p-value.
}
\examples{
\dontrun{
set.seed(123)
x <- rnorm(50)
y <- rnorm(60, mean = 0.5)

# Compare M-estimators (default)
pb2gen(x, y)

# Compare means
pb2gen(x, y, est = mean)

# Compare medians
pb2gen(x, y, est = median)

# Compare trimmed means
pb2gen(x, y, est = mean, trim = 0.2)

# Compare specific quantiles using Harrell-Davis
pb2gen(x, y, est = hd, q = 0.25)  # First quartile
}
}
\seealso{
\code{\link{pb2genMC}}, \code{\link{trimpb2}}, \code{\link{yuenbt}}
}
