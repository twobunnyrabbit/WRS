% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{twolsreg}
\alias{twolsreg}
\title{Bootstrap CI for Difference Between Two Regression Slopes}
\usage{
twolsreg(x1, y1, x2, y2)
}
\arguments{
\item{x1}{Covariate values for group 1.}

\item{y1}{Response values for group 1.}

\item{x2}{Covariate values for group 2.}

\item{y2}{Response values for group 2.}
}
\value{
A list with components:
\itemize{
\item \code{b1}: OLS slope estimate for group 1
\item \code{b2}: OLS slope estimate for group 2
\item \code{ci}: 95\% confidence interval for the difference (b1 - b2)
}
}
\description{
Computes a 95\% confidence interval for the difference between two OLS
regression slopes corresponding to two independent groups.
}
\details{
Uses an adjusted percentile bootstrap method that gives good results when
the error term is heteroscedastic. The function uses 599 bootstrap samples.

WARNING: If the number of bootstrap samples is altered, it is unknown how to
adjust the confidence interval when n1 + n2 < 250. The confidence interval
bounds are automatically adjusted based on the combined sample size.

Missing values are automatically removed. Only one covariate is allowed.
}
\examples{
\dontrun{
set.seed(123)
x1 <- rnorm(50)
y1 <- 2 + 3 * x1 + rnorm(50)
x2 <- rnorm(60)
y2 <- 2 + 2.5 * x2 + rnorm(60)

result <- twolsreg(x1, y1, x2, y2)
print(result)
}
}
\seealso{
\code{\link{tworegwb}}, \code{\link{reg2ci}}
}
