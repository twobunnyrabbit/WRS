% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{TWOpNOV}
\alias{TWOpNOV}
\title{Compare Two Dependent Pearson Correlations (Non-Overlapping)}
\usage{
TWOpNOV(x, y, HC4 = FALSE, alpha = 0.05, ZCI = FALSE)
}
\arguments{
\item{x}{Matrix or data frame with 2 columns.}

\item{y}{Matrix or data frame with 2 columns.}

\item{HC4}{Logical; if TRUE, uses HC4 heteroscedasticity correction. Required for
alpha not equal to 0.05.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{ZCI}{Logical; internal parameter for handling NA values in CI.}
}
\value{
A list with components:
\item{est.1}{Correlation between x\link{,1} and x\link{,2}}
\item{est.2}{Correlation between y\link{,1} and y\link{,2}}
\item{ci.lower}{Lower confidence limit for the difference}
\item{ci.upper}{Upper confidence limit for the difference}
}
\description{
Computes a confidence interval for the difference between two dependent Pearson
correlations in the non-overlapping case, where the correlations involve completely
different pairs of variables.
}
\details{
This function compares the correlation between x\link{,1} and x\link{,2} to the correlation
between y\link{,1} and y\link{,2}. This is the "non-overlapping" case because the two
correlations involve completely different pairs of variables (though the data
may be from the same subjects).

The method deals with heteroscedasticity and non-normality. For alpha != 0.05,
you must use HC4=TRUE.
}
\examples{
set.seed(123)
n <- 50
x <- cbind(rnorm(n), rnorm(n))
y <- cbind(rnorm(n), rnorm(n))

result <- TWOpNOV(x, y)
result$ci.lower
result$ci.upper
}
\references{
Wilcox, R.R. (2009). Comparing Pearson correlations: Dealing with heteroscedasticity
and non-normality. Communications in Statistics - Simulation and Computation, 38,
2220-2234.
}
\seealso{
\code{\link{TWOpov}}, \code{\link{twoDNOV}}, \code{\link{tworhobt}}
}
