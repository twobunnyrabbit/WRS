% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{ogk.pairwise}
\alias{ogk.pairwise}
\title{OGK Pairwise Robust Covariance (Internal)}
\usage{
ogk.pairwise(X, n.iter = 1, sigmamu = taulc, v = gkcov, beta = 0.9, ...)
}
\arguments{
\item{X}{Matrix (n × p). Data matrix.}

\item{n.iter}{Integer. Number of orthogonalization iterations (default: 1).}

\item{sigmamu}{Function. Robust scale/location estimator (default: \code{\link{taulc}}).}

\item{v}{Function. Pairwise robust covariance (default: \code{\link{gkcov}}).}

\item{beta}{Numeric. Coverage for hard rejection weighting (default: 0.9).}

\item{...}{Additional arguments passed to \code{sigmamu}.}
}
\value{
List with components:
\describe{
\item{center}{Numeric vector. Unweighted robust location estimate}
\item{covmat}{Matrix. Unweighted robust covariance matrix}
\item{wcenter}{Numeric vector. Reweighted robust location estimate}
\item{wcovmat}{Matrix. Reweighted robust covariance matrix}
\item{distances}{Numeric vector. Robust Mahalanobis distances}
\item{sigmamu}{Character. Name of scale estimator used}
\item{v}{Character. Name of pairwise covariance estimator used}
\item{data.name}{Character. Original data name}
\item{data}{Matrix. Original data}
}
}
\description{
Internal function implementing the pairwise OGK (Orthogonalized Gnanadesikan-Kettenring)
algorithm for robust covariance estimation. Called by \code{\link{ogk}}.
}
\details{
\strong{Algorithm} (Maronna & Zamar, 2002):

\strong{Step 1: Orthogonalization} (iterate \code{n.iter} times):
\enumerate{
\item Compute robust scales for each variable
\item Standardize data by robust scales
\item Compute pairwise robust correlations via \code{v()}
\item Eigen-decompose correlation matrix
\item Project data onto eigenvectors
}

\strong{Step 2: Robust estimation in principal components}:
\enumerate{
\item Compute robust location and scale in each PC direction
\item Compute robust Mahalanobis distances
}

\strong{Step 3: Reweighting}:
\enumerate{
\item Compute weights via hard rejection based on distances
\item Compute weighted location and covariance
}

This implementation is adapted from code by S. Konis (www.stats.ox.ac.uk/~konis/pairwise.q)
with corrections by V. Todorov.
}
\references{
Maronna, R. A. and Zamar, R. H. (2002). Robust estimates of location and
dispersion for high-dimensional datasets. Technometrics, 44, 307-317.
}
\seealso{
\code{\link{ogk}}, \code{\link{hard.rejection}}, \code{\link{gkcov}}
}
\keyword{internal}
