% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{binom.conf.pv}
\alias{binom.conf.pv}
\title{P-Value for Binomial Confidence Interval Methods}
\usage{
binom.conf.pv(
  x = sum(y),
  nn = length(y),
  y = NULL,
  method = "AC",
  AUTO = TRUE,
  pr = FALSE,
  PVSD = TRUE,
  alpha = 0.05,
  nullval = 0.5,
  NOTE = TRUE
)
}
\arguments{
\item{x}{Number of successes (computed from y if y is provided)}

\item{nn}{Sample size (computed from y if y is provided)}

\item{y}{Vector of 1s and 0s (optional)}

\item{method}{Character string specifying CI method: 'AC' (Agresti-Coull,
default), 'P' (Pratt), 'CP' (Clopper-Pearson), 'KMS' (Kulinskaya et al.),
'WIL' (Wilson), or 'SD' (Schilling-Doi)}

\item{AUTO}{Logical; if TRUE (default), automatically use 'SD' method when n < 35}

\item{pr}{Logical; if TRUE, print note about sign test (default FALSE)}

\item{PVSD}{Logical; if TRUE, compute p-value when using SD method; if FALSE,
skip p-value computation to avoid high execution time (default TRUE)}

\item{alpha}{Significance level for CI (default 0.05)}

\item{nullval}{Null hypothesis value for probability of success (default 0.5)}

\item{NOTE}{Logical; if TRUE (default), print note when PVSD=FALSE with SD method}
}
\value{
List with components:
\itemize{
\item n: Sample size
\item p.hat: Estimated proportion
\item ci: Confidence interval
\item p.value: P-value (NULL if PVSD=FALSE with SD method)
}
}
\description{
Compute p-value for testing whether the probability of success equals a
specified null value, using various binomial CI methods available in
\code{\link{binom.conf}}.
}
\details{
Computes a p-value by finding the smallest alpha level at which the
confidence interval excludes the null value. Multiple methods are available:
\itemize{
\item AC: Agresti-Coull (default, good general performance)
\item P: Pratt's method
\item CP: Clopper-Pearson (exact, conservative)
\item KMS: Kulinskaya et al.
\item WIL: Wilson
\item SD: Schilling-Doi (recommended for n < 35)
}

When AUTO=TRUE, the SD method is automatically used for small samples (n < 35).
Computing p-values with SD method can be slow; set PVSD=FALSE to skip.
}
\seealso{
\code{\link{binom.conf}}, \code{\link{binomcipv}}
}
\keyword{binomial}
\keyword{htest}
