% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{tshd}
\alias{tshd}
\title{Theil-Sen Regression with Harrell-Davis Estimator}
\usage{
tshd(
  x,
  y,
  HD = TRUE,
  plotit = FALSE,
  xlab = "X",
  ylab = "Y",
  OPT = FALSE,
  tr = FALSE
)
}
\arguments{
\item{x}{Numeric vector. Independent variable (single predictor only)}

\item{y}{Numeric vector. Dependent variable}

\item{HD}{Logical. If TRUE, use Harrell-Davis estimator; if FALSE, use median (default: TRUE)}

\item{plotit}{Logical. If TRUE, create scatter plot with regression line (default: FALSE)}

\item{xlab}{Character. X-axis label for plot (default: 'X')}

\item{ylab}{Character. Y-axis label for plot (default: 'Y')}

\item{OPT}{Logical. If TRUE, compute intercept as median(Y) - slope*median(X);
if FALSE, use median of residuals (default: FALSE)}

\item{tr}{Logical or numeric. Trimming parameter for Harrell-Davis estimator (default: FALSE)}
}
\value{
List with component:
\item{coef}{Vector of length 2: intercept and slope}
}
\description{
Computes Theil-Sen regression using the Harrell-Davis estimator for
robust slope estimation.
}
\details{
The Theil-Sen estimator is a robust alternative to OLS regression based on
the median of all pairwise slopes. This version offers two enhancements:
\itemize{
\item Using Harrell-Davis estimator instead of median for improved efficiency
\item Two methods for computing the intercept
}

The slope is estimated as the median (or HD estimate) of all pairwise slopes
(y_i - y_j)/(x_i - x_j) for i < j.

Currently limited to simple regression (single predictor).
}
\examples{
x <- rnorm(50)
y <- 2 + 3*x + rt(50, df = 3)
result <- tshd(x, y, HD = TRUE)

}
\seealso{
\code{\link{tsreg}}, \code{\link{hd}}, \code{\link{tshdreg}}
}
\keyword{regression}
\keyword{robust}
