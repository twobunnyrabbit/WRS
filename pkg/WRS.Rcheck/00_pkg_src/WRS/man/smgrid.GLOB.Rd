% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{smgrid.GLOB}
\alias{smgrid.GLOB}
\title{Global Test for Location Differences with Two-Variable Splitting}
\usage{
smgrid.GLOB(
  x,
  y,
  IV = c(1, 2),
  Qsplit1 = 0.5,
  Qsplit2 = 0.5,
  tr = 0.2,
  VAL1 = NULL,
  VAL2 = NULL,
  PB = FALSE,
  est = tmean,
  nboot = 1000,
  pr = TRUE,
  fun = ES.summary,
  xout = FALSE,
  outfun = outpro,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{x}{Matrix or data frame of predictor variables}

\item{y}{Response variable vector}

\item{IV}{Vector of length 2 specifying which columns of x to use for splitting.
Default is \code{c(1,2)} (first two columns).}

\item{Qsplit1}{Quantile(s) for splitting first independent variable. Default is 0.5.
Ignored if \code{VAL1} is specified.}

\item{Qsplit2}{Quantile(s) for splitting second independent variable. Default is 0.5.
Ignored if \code{VAL2} is specified.}

\item{tr}{Trimming proportion (0 to 0.5). Default is 0.2.}

\item{VAL1}{Optional vector of specific values for splitting first IV. If provided,
overrides \code{Qsplit1}. Default is \code{NULL}.}

\item{VAL2}{Optional vector of specific values for splitting second IV. If provided,
overrides \code{Qsplit2}. Default is \code{NULL}.}

\item{PB}{Logical. If \code{TRUE}, use percentile bootstrap. Default is \code{FALSE}.}

\item{est}{Location estimator function. Default is \code{\link{tmean}}.}

\item{nboot}{Number of bootstrap samples (if \code{PB=TRUE}). Default is 1000.}

\item{pr}{Logical. If \code{TRUE}, print warnings. Default is \code{TRUE}.}

\item{fun}{Effect size summary function. Default is \code{\link{ES.summary}}.}

\item{xout}{Logical. If \code{TRUE}, remove outliers from x. Default is \code{FALSE}.}

\item{outfun}{Outlier detection function. Default is \code{\link{outpro}}.}

\item{SEED}{Logical or numeric. Controls random seed. Default is \code{TRUE}.}

\item{...}{Additional arguments passed to estimator or outlier function.}
}
\value{
Result from \code{\link{t2way}} containing global test statistics.
}
\description{
Splits data based on quantiles or specific values of two independent variables
and performs a global test of equal location measures across all resulting groups.
Uses GLOB (Global test) methodology.
}
\details{
This function:
\enumerate{
\item Splits data into J × K groups based on two variables
\item Computes location estimates for each cell
\item Performs global test using \code{\link{t2way}} (ANOVA for trimmed means)
}

Groups with fewer than 6 observations generate warnings. Unlike \code{\link{smgrid}}
which performs pairwise comparisons, this function performs a global omnibus test.
}
\examples{
# Global test with two-way split
set.seed(123)
x <- matrix(rnorm(200), ncol=2)
y <- x[,1] + x[,2] + rnorm(100)
result <- smgrid.GLOB(x, y, Qsplit1=.5, Qsplit2=.5)
}
\seealso{
\code{\link{smgrid}} for pairwise comparisons,
\code{\link{t2way}} for two-way ANOVA with trimmed means
}
