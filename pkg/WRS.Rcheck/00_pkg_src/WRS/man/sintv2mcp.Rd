% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{sintv2mcp}
\alias{sintv2mcp}
\title{Multiple Comparisons for Medians (Dependent Groups)}
\usage{
sintv2mcp(x, con = 0, alpha = 0.05, method = "hoch")
}
\arguments{
\item{x}{Data matrix where columns represent groups/conditions, or a list
where each element is a group}

\item{con}{Contrast matrix with J rows (groups) and C columns (contrasts).
If \code{con=0} (default), performs all pairwise comparisons}

\item{alpha}{Familywise error rate (default: 0.05)}

\item{method}{P-value adjustment method (default: 'hoch' for Hochberg).
Options include methods from \code{\link{p.adjust}}: 'holm', 'hochberg',
'hommel', 'bonferroni', 'BH', 'BY', 'fdr'}
}
\value{
A list with components:
\item{num.sig}{Number of significant comparisons at alpha level}
\item{psihat}{Matrix with columns: Group, Group (or contrast number),
median difference estimate, CI lower, CI upper, unadjusted p-value,
adjusted p-value}
}
\description{
Performs multiple comparisons for medians across dependent (paired/repeated
measures) groups using the Hettmansperger-Sheather method with p-value
adjustment for multiple testing.
}
\details{
For dependent groups, this function:
\enumerate{
\item Computes difference scores for each pair or contrast
\item Uses \code{\link{sintv2}} to test if median difference = 0
\item Adjusts p-values for multiple comparisons using specified method
}

For pairwise comparisons (default), tests all J(J-1)/2 pairwise differences.
For custom contrasts, tests each specified linear combination.

The Hochberg (1988) step-up procedure is the default, which is less conservative
than Bonferroni while controlling familywise error rate.

Missing values are removed row-wise (entire row removed if any value missing).
}
\seealso{
\code{\link{sintv2}}, \code{\link{sint}}, \code{\link{p.adjust}}
}
\keyword{htest}
