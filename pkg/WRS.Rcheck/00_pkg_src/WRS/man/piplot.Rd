% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{piplot}
\alias{piplot}
\title{FY Plot with Prediction Intervals}
\usage{
piplot(x, y, alpha = 0.05)
}
\arguments{
\item{x}{Matrix or data frame of predictor variables.}

\item{y}{Numeric vector of response values.}

\item{alpha}{Significance level for prediction intervals (default: 0.05).}
}
\value{
Invisibly returns \code{NULL}. Creates a plot as a side effect.
}
\description{
Creates a fitted-versus-observed (FY) plot for OLS regression with
prediction intervals. Points outside the intervals are marked with
triangles, helping identify influential observations.
}
\details{
The function:
\enumerate{
\item Fits OLS regression of \code{y} on \code{x}
\item Computes fitted values and residuals
\item Calculates prediction intervals adjusted for:
\itemize{
\item Leverage (hat values)
\item Small sample correction: \code{(1 + 15/n) * sqrt(n/(n-p))}
}
\item Plots fitted vs observed values with:
\itemize{
\item Points: actual observations
\item Diagonal line: perfect fit
\item Triangles: upper and lower prediction limits
}
}

Observations far from their prediction intervals may be outliers or
indicate model misspecification.
}
\examples{
# OLS regression with prediction intervals
x <- matrix(rnorm(100), ncol=2)
y <- x[,1] + 2*x[,2] + rnorm(50)
piplot(x, y)
}
\seealso{
\code{\link{rplot.res}} for regression residual plots
}
