% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{comvar.mcp}
\alias{comvar.mcp}
\title{Multiple Comparisons of Variances for Independent Groups}
\usage{
comvar.mcp(x, method = "hoch", SEED = TRUE)
}
\arguments{
\item{x}{Either a matrix/data frame (each column is a group) or a list of vectors.}

\item{method}{Method for p-value adjustment (default: "hoch" for Hochberg's method).
Other options include "holm", "bonferroni", "BH", etc. See \code{\link[stats]{p.adjust}}.}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducibility (default: \code{TRUE}).}
}
\value{
A matrix with one row per comparison and columns:
\itemize{
\item \code{Var}: Index of first variable
\item \code{Var}: Index of second variable
\item \verb{SD 1}: Standard deviation of first group
\item \verb{SD 2}: Standard deviation of second group
\item \code{Dif}: Difference in standard deviations (SD1 - SD2)
\item \code{p.value}: Unadjusted p-value
\item \code{Adj.p.value}: Adjusted p-value using specified method
}
}
\description{
Performs all pairwise comparisons of variances for J independent groups
using a slight extension of the HC4 version of the Morgan-Pitman test.
Controls the familywise error rate using a specified p-value adjustment method.
}
\details{
This function compares the variances of J independent variables using all
pairwise comparisons. The Morgan-Pitman test (via \code{varcom.IND.MP}) is used
for each comparison, which is a robust heteroscedastic test for comparing
two variances.

The function performs C = J(J-1)/2 pairwise comparisons and adjusts the
p-values to control the familywise error rate.
}
\examples{
\dontrun{
# Compare variances of three groups
set.seed(123)
x1 <- rnorm(30, sd = 1)
x2 <- rnorm(30, sd = 1.5)
x3 <- rnorm(30, sd = 2)
x <- cbind(x1, x2, x3)

# Perform all pairwise variance comparisons
result <- comvar.mcp(x)
print(result)
}
}
\seealso{
\code{\link{varcom.IND.MP}}, \code{\link{comvar2}}, \code{\link[stats]{p.adjust}}
}
