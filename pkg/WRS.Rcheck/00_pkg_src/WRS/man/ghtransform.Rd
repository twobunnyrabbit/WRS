% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{ghtransform}
\alias{ghtransform}
\title{Transform Normal Data to g-and-h Distribution}
\usage{
ghtransform(x, g = 0, h = 0)
}
\arguments{
\item{x}{Numeric vector of standard normal values to transform.}

\item{g}{Numeric. Skewness parameter (default: 0).
\code{g = 0} produces symmetric distributions; larger \code{|g|} increases skewness.}

\item{h}{Numeric. Tail weight parameter (default: 0).
\code{h = 0} maintains normal tails; larger \code{h} produces heavier tails.}
}
\value{
Numeric vector of transformed values from the g-and-h distribution.
}
\description{
Applies the g-and-h transformation to standard normal data, producing
data from a distribution with specified skewness and tail weight.
}
\details{
The g-and-h transformation is defined as:

For \code{g > 0}:
\deqn{Y = \frac{e^{gX} - 1}{g} e^{hX^2/2}}

For \code{g = 0}:
\deqn{Y = X e^{hX^2/2}}

where \code{X} is standard normal input.

This transformation provides flexible control over:
\itemize{
\item Skewness via parameter \code{g}
\item Tail behavior via parameter \code{h}
}

Special cases:
\itemize{
\item \code{g = 0, h = 0}: Returns input unchanged (standard normal)
\item \code{g = 0, h > 0}: Symmetric distribution with heavy tails
\item \code{g > 0, h = 0}: Skewed distribution with normal tails
\item \code{g > 0, h > 0}: Skewed distribution with heavy tails
}
}
\examples{
# Transform standard normal data to skewed, heavy-tailed distribution
x <- rnorm(1000)
y <- ghtransform(x, g=0.5, h=0.2)
par(mfrow=c(1,2))
hist(x, main="Standard Normal", breaks=30)
hist(y, main="g-and-h (g=0.5, h=0.2)", breaks=30)

}
\seealso{
\code{\link{ghmul}}, \code{\link{ghdist}}, \code{\link{gengh}},
\code{\link{plot.ghdist}}
}
\keyword{distribution}
