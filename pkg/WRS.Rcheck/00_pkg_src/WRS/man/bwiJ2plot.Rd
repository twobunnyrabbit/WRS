% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{bwiJ2plot}
\alias{bwiJ2plot}
\title{Plot Difference Score Distributions for J-by-2 Between-Within Design}
\usage{
bwiJ2plot(
  J,
  K,
  x,
  fr = 0.8,
  aval = 0.5,
  xlab = "X",
  ylab = "",
  color = rep("black", 5),
  BOX = FALSE
)
}
\arguments{
\item{J}{Number of independent groups (maximum 5).}

\item{K}{Number of dependent measures (must be 2).}

\item{x}{Data in matrix or list format. If matrix, organized as expected
by \code{\link{bwimcp}} (columns alternate between dependent measures
within each group). If list, each element contains data for one condition.}

\item{fr}{Smoothing parameter for density plots (default: 0.8).}

\item{aval}{Alpha level for adaptive kernel density (default: 0.5).}

\item{xlab, ylab}{Axis labels (defaults: 'X', '').}

\item{color}{Vector of length 5 specifying colors for each group (default:
all black).}

\item{BOX}{Logical; if \code{TRUE}, creates boxplots instead of density plots
(default: FALSE).}
}
\value{
Invisibly returns \code{NULL}. Creates a plot as a side effect.
}
\description{
Plots distributions of difference scores for a J-by-2 mixed design (J
independent groups, 2 dependent measures). Each group's difference
distribution is overlaid for comparison.
}
\details{
For each of the J groups, the function:
\enumerate{
\item Extracts data for the two dependent measures
\item Computes difference scores (Measure 1 - Measure 2)
\item Plots all J difference distributions on the same plot
}

Uses \code{\link{g5plot}} for density plots (up to 5 groups) or
\code{boxplot} when \code{BOX = TRUE}.
}
\examples{
\dontrun{
# 3 groups, 2 time points
# Data in matrix format: columns are Time1_Grp1, Time2_Grp1, Time1_Grp2, ...
data_mat <- matrix(rnorm(200), ncol=6)
bwiJ2plot(J=3, K=2, x=data_mat)
}
}
\seealso{
\code{\link{bwimcp}}, \code{\link{g5plot}}
}
