% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{mscorciH}
\alias{mscorciH}
\title{Multiple Skipped Correlation CI with Hochberg Adjustment}
\usage{
mscorciH(
  x,
  nboot = 1000,
  alpha = 0.05,
  SEED = TRUE,
  method = "hoch",
  corfun = pcor,
  outfun = outpro,
  crit.pv = NULL,
  ALL = TRUE,
  MC = TRUE,
  pr = TRUE
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{nboot}{Number of bootstrap samples (default: 1000).}

\item{alpha}{Significance level (default: 0.05). Each CI has approximately
1-alpha coverage.}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{method}{Adjustment method (default: 'hoch' for Hochberg).}

\item{corfun}{Correlation function to use. Common options: \code{pbcor} (percentage
bend), \code{pcor} (Pearson), \code{wincor} (winsorized), \code{spear} (Spearman).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{crit.pv}{Pre-computed critical p-values (default: \code{NULL}).}

\item{ALL}{Logical. Not used for multivariate case (default: \code{TRUE}).}

\item{MC}{Logical. Use parallel processing (default: \code{TRUE}).}

\item{pr}{Logical. Print progress messages (default: \code{TRUE}).}
}
\value{
List with components:
\item{Estimates}{Matrix with columns: Est., p-value, adjusted p.value, Hoch.adjusted.p.value.}
\item{confidence.int}{Matrix with columns: Var i, Var j, ci.low, ci.up.}
}
\description{
Tests zero correlation for all pairs of variables using skipped correlation
with Hochberg adjustment to control FWE.
}
\details{
Tests H0: rho_ij = 0 for all pairs (i,j) using Hochberg adjustment of
p-values to control the familywise error rate at approximately alpha.

Each confidence interval has approximately 1-alpha probability coverage,
but the simultaneous coverage is not controlled.

Faster alternative to \code{mscorci(..., hoch = FALSE)} but may have reduced
power for small samples (n < 60) or strict alpha levels (< 0.05).
}
\examples{
\dontrun{
set.seed(123)
x <- matrix(rnorm(60 * 4), ncol = 4)
result <- mscorciH(x, nboot = 500)
result$Estimates
result$confidence.int
}
}
\seealso{
\code{\link{mscorci}}, \code{\link{mscor}}, \code{\link{scorci}}
}
