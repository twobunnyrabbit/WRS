% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{mlts}
\alias{mlts}
\title{Multivariate Least Trimmed Squares Regression}
\usage{
mlts(x, y, gamma, ns = 500, nc = 10, delta = 0.01)
}
\arguments{
\item{x}{Matrix of predictor variables (n by p).}

\item{y}{Matrix of response variables (n by q).}

\item{gamma}{Trimming proportion (fraction of observations to trim).}

\item{ns}{Number of random starts. Default is 500.}

\item{nc}{Number of concentration steps per start. Default is 10.}

\item{delta}{Proportion for reweighting step. Default is 0.01.}
}
\value{
List with components:
\itemize{
\item \code{beta} - LTS coefficient matrix (p by q)
\item \code{sigma} - LTS covariance matrix (q by q)
\item \code{dres} - Robust distances for all observations
\item \code{betaR} - Reweighted coefficient matrix
\item \code{sigmaR} - Reweighted covariance matrix
\item \code{dresR} - Robust distances for reweighted fit
}
}
\description{
Computes multivariate least trimmed squares (LTS) regression with reweighting.
This is a robust regression method for multivariate responses.
}
\details{
This function implements multivariate LTS regression, which minimizes the
determinant of the covariance matrix of the h smallest residuals, where
h = floor(n*(1-gamma)) + 1.

The algorithm:
\enumerate{
\item Generates \code{ns} random starting subsets
\item For each start, performs \code{nc} concentration steps
\item Selects the solution with smallest determinant
\item Reweights using observations with small residuals (based on \code{delta})
}

Returns both the raw LTS estimates and the reweighted estimates (betaR, sigmaR).
}
\keyword{multivariate}
\keyword{regression}
\keyword{robust}
