% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{smcorcom}
\alias{smcorcom}
\title{Smooth Correlation Comparison Between Two Groups}
\usage{
smcorcom(
  x1,
  y1,
  x2,
  y2,
  nboot = 200,
  pts = NA,
  plotit = TRUE,
  SEED = TRUE,
  varfun = pbvar,
  xout = TRUE,
  outfun = out,
  ...
)
}
\arguments{
\item{x1, y1}{Numeric vectors for group 1.}

\item{x2, y2}{Numeric vectors for group 2.}

\item{nboot}{Number of bootstrap samples (default: 200).}

\item{pts}{Points at which to evaluate smooths (default: \code{NA}).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{varfun}{Variance function for explanatory power (default: \code{pbvar}).}

\item{xout}{Logical. If \code{TRUE}, removes outliers (default: \code{TRUE}).}

\item{outfun}{Outlier detection function (default: \code{out}).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
List with components:
\item{p.value}{Bootstrap p-value for testing equal associations.}
\item{pcrit.05}{Critical p-value adjusted for sample sizes.}
}
\description{
Compares the strength of association between two independent groups using
smoothing methods and robust explanatory power.
}
\details{
This function compares the strength of association in two independent groups
using Cleveland's LOWESS smoother coupled with a robust analog of explanatory
power (R-squared).

The method generalizes the goal of comparing the usual coefficient of
determination between two independent groups, but uses nonparametric smoothing
instead of linear regression.

Reject H0 (equal associations) at the 0.05 level if \code{p.value <= pcrit.05}.
The critical value is sample-size adjusted for better small-sample performance.
}
\examples{
\dontrun{
# Compare associations in two groups
set.seed(123)
x1 <- rnorm(30)
y1 <- 0.5 * x1 + rnorm(30)
x2 <- rnorm(30)
y2 <- 0.8 * x2 + rnorm(30)
smcorcom(x1, y1, x2, y2, nboot = 500)
}
}
\seealso{
\code{\link{lplot}}, \code{\link{lplot2g}}, \code{\link{twocor}}
}
