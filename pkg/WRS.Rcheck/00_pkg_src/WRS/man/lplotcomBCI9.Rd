% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{lplotcomBCI9}
\alias{lplotcomBCI9}
\title{Compare Predictor Importance Using LOESS at Quartile Points (Bootstrap CI)}
\usage{
lplotcomBCI9(
  x,
  y,
  xout = FALSE,
  pr = TRUE,
  outfun = outpro,
  span = 2/3,
  npts = 10,
  tr = 0.2,
  nboot = 500,
  SEED = TRUE,
  plotit = TRUE,
  ticktype = "simple",
  ADJ.CI = TRUE,
  xlab = "X1",
  ylab = "X2",
  zlab = "Y",
  alpha = 0.05,
  MC = FALSE,
  ...
)
}
\arguments{
\item{x}{Matrix or data frame with 2 columns containing the predictor variables.}

\item{y}{Numeric vector of response values.}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{pr}{Logical. If \code{TRUE}, prints progress messages (default: \code{TRUE}).}

\item{outfun}{Outlier detection function (default: \code{outpro}).}

\item{span}{Span parameter for LOESS (default: 2/3).}

\item{npts}{Number of points for measuring strength (default: 10).}

\item{tr}{Trimming proportion (default: 0.2). Proportion of observations to
trim from each tail when computing winsorized correlation.}

\item{nboot}{Number of bootstrap samples (default: 500).}

\item{SEED}{Logical; if \code{TRUE}, sets random seed for reproducibility.}

\item{plotit}{Logical; if \code{TRUE}, creates a LOESS plot.}

\item{ticktype}{Tick type for 3D plot (default: 'simple').}

\item{ADJ.CI}{Logical; if \code{TRUE}, adjusts CIs using critical p-value (default: TRUE).}

\item{xlab, ylab, zlab}{Axis labels for the plot.}

\item{alpha}{Significance level (default: 0.05).}

\item{MC}{Logical; if \code{TRUE}, uses parallel processing.}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
List with components:
\item{n}{Original sample size.}
\item{n.keep}{Sample size after outlier removal.}
\item{p.crit}{Critical p-value threshold.}
\item{output}{Matrix with 9 rows (one per quartile combination) and columns:
\itemize{
\item \code{pts1}: Evaluation point for predictor 1
\item \code{pts2}: Evaluation point for predictor 2
\item \code{p-value}: Two-sided p-value for difference test
\item \code{str.x1.given.x2}: Strength of predictor 1 given predictor 2
\item \code{str.x2.given.x1}: Strength of predictor 2 given predictor 1
\item \code{ci.low}, \code{ci.hi}: Bootstrap CI for the difference
}}
}
\description{
Compares the relative importance of two predictors using LOESS smoothing,
evaluated at quartile points (25th, 50th, 75th percentiles). Tests all 9
combinations of quartile pairs and provides bootstrap confidence intervals.
}
\details{
This function evaluates predictor importance at all 9 combinations of quartile
points (Q1, Q2, Q3) for both predictors. For each combination:
\itemize{
\item Computes strength of association measures for each predictor
\item Tests whether the two predictors differ in importance
\item Provides bootstrap confidence intervals for the difference
}

The critical p-value is adjusted based on sample size when \code{ADJ.CI = TRUE}.
}
\examples{
\dontrun{
# Compare two predictors at quartile points
x1 <- rnorm(100)
x2 <- rnorm(100)
y <- x1 + 0.5*x2 + rnorm(100)
lplotcomBCI9(cbind(x1, x2), y)
}
}
\seealso{
\code{\link{lplotcom2}}, \code{\link{lplotcom2v2}}, \code{\link{lplotcomBCI}}
}
