% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression.R
\name{ltsgreg}
\alias{ltsgreg}
\title{Least Trimmed Absolute Value Regression}
\usage{
ltsgreg(x, y, tr = 0.2, h = NA, xout = FALSE, outfun = outpro, ...)
}
\arguments{
\item{x}{Numeric vector or matrix of predictor variable(s).}

\item{y}{Numeric vector of the response variable.}

\item{tr}{Trimming proportion (default: 0.2). The fraction of largest absolute
residuals to trim. Must be between 0 and 0.5.}

\item{h}{Number of observations to use in the fit (default: \code{NA}, computed
as \code{n - floor(tr*n)}). If specified, overrides \code{tr}.}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
A list with components:
\describe{
\item{coef}{Vector of regression coefficients (intercept, slopes)}
\item{residuals}{Vector of residuals from the fitted model}
}
}
\description{
Computes a robust regression estimator that minimizes the sum of the smallest
absolute residuals. This is a variant of least trimmed squares (LTS) regression
using absolute values instead of squared residuals.
}
\details{
This function minimizes the sum of the \code{h} smallest absolute residuals,
where \code{h = n - floor(tr*n)}. This provides robustness to outliers while
using absolute deviations instead of squared deviations.

\strong{Method}:
\enumerate{
\item Uses \code{ltsReg} from the \code{robustbase} package for initial estimates
\item Refines estimates using Nelder-Mead optimization
\item Minimizes: sum of h smallest |residual| values
}

\strong{Properties}:
\itemize{
\item \strong{Breakdown point}: Approximately (1-h/n) ≈ \code{tr}
\item \strong{Robustness}: High resistance to outliers
\item \strong{L1 criterion}: Uses absolute values (more robust than L2/squared)
}

\strong{When to use}:
\itemize{
\item Heavy-tailed error distributions
\item Presence of outliers in Y direction
\item Want breakdown protection with L1-type estimator
\item Alternative to \code{\link{ltsreg}} (which uses L2)
}

\strong{Trimming proportion}:
\itemize{
\item Default (0.2): 20\% trimming, good balance of robustness and efficiency
\item Larger \code{tr}: more robustness, lower efficiency
\item Smaller \code{tr}: higher efficiency, less robustness
\item Maximum \code{tr}: 0.5 (50\% breakdown point)
}
}
\examples{
\dontrun{
# Simple robust regression
set.seed(123)
x <- rnorm(50)
y <- 2 + 3*x + rnorm(50)

# Add outliers
y[1:5] <- y[1:5] + 10

# Fit with 20\% trimming
fit <- ltsgreg(x, y, tr=0.2)
print(fit$coef)

# More aggressive trimming (30\%)
fit2 <- ltsgreg(x, y, tr=0.3)
print(fit2$coef)

# Multiple predictors
x <- matrix(rnorm(100), ncol=2)
y <- 1 + 2*x[,1] - x[,2] + rt(50, df=3)
ltsgreg(x, y)
}

}
\references{
Wilcox, R.R. (2022). \emph{Introduction to Robust Estimation and Hypothesis Testing}
(5th ed.). Academic Press.
}
\seealso{
\code{\link{ltsreg}} for least trimmed squares (L2 version),
\code{\link{tsreg}} for Theil-Sen regression,
\code{\link{opreg}} for outlier-pruned regression
}
