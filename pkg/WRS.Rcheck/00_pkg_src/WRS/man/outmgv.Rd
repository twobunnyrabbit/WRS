% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outliers.R
\name{outmgv}
\alias{outmgv}
\title{MGV-Based Outlier Detection}
\usage{
outmgv(
  x,
  y = NULL,
  plotit = TRUE,
  outfun = outbox,
  se = TRUE,
  op = 1,
  ndir = 1000,
  cov.fun = rmba,
  xlab = "X",
  ylab = "Y",
  SEED = TRUE,
  STAND = FALSE,
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{se}{Logical. If TRUE, standardizes variables using median and MAD
before computing MGV (default: TRUE).}

\item{op}{Number of outliers to remove from each tail when using outlier
detection methods. Default varies by function.}

\item{ndir}{Number of random projections for computing depth when plotting
(default: 1000).}

\item{cov.fun}{Covariance function to use for computing center (default: rmba).}

\item{xlab}{Label for x-axis.}

\item{ylab}{Label for y-axis.}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{STAND}{Included for compatibility when called by other functions (not used).}

\item{...}{Additional arguments passed to \code{outfun}.}
}
\value{
A list with components:
\item{n}{Sample size.}
\item{n.out}{Number of outliers detected.}
\item{out.id}{Indices of outliers.}
\item{keep}{Indices of non-outliers.}
}
\description{
Detects outliers using the MGV (Minimum Generalized Variance) method,
which is based on detecting points that inflate the generalized variance.
}
\details{
Computes MGV values using \code{mgvar()}, then applies \code{outfun} to detect outliers.
For bivariate data with \code{plotit=TRUE}, creates a scatterplot with the median
depth contour. For p>2, uses modified boxplot rule with adjusted critical value.

\strong{Note}: Results can be affected by rounding error in \code{eigen()} if columns
are reordered.
}
\seealso{
\code{\link{mgvar}}, \code{\link{outbox}}, \code{\link{outmgvad}}
}
