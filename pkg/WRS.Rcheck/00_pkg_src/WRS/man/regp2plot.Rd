% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{regp2plot}
\alias{regp2plot}
\title{3D Plot of Regression Surface}
\usage{
regp2plot(
  x,
  y,
  xout = FALSE,
  outfun = out,
  xlab = "Var 1",
  ylab = "Var 2",
  zlab = "Var 3",
  regfun = tsreg,
  COLOR = FALSE,
  tick.marks = TRUE,
  ...
)
}
\arguments{
\item{x}{Matrix or data frame with 2 columns of predictor values.}

\item{y}{Numeric vector of response values.}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{xlab}{Label for x-axis in plots (default: "X" or "VAR 1").}

\item{ylab}{Label for y-axis in plots (default: "Y" or "VAR 2").}

\item{regfun}{Regression function to use for fitting (default: \code{tsreg}
for Theil-Sen regression). Must return a list with a \code{coef} component.}

\item{COLOR}{Logical. If \code{TRUE}, plots regression plane in blue (default: \code{FALSE}).}

\item{tick.marks}{Logical. If \code{TRUE}, adds tick marks to 3D plot (default: \code{TRUE}).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
Invisibly returns \code{NULL}. Creates a plot as a side effect.
}
\description{
Creates a 3D scatter plot with a fitted regression plane for data with
two predictor variables. Useful for visualizing multiple regression
relationships.
}
\details{
Requires the \code{scatterplot3d} package. The function removes missing values
automatically using \code{elimna()}. The x argument must have exactly 2 columns.
}
\examples{
\dontrun{
# Plot 3D regression surface
x <- matrix(rnorm(100), ncol=2)
y <- 2*x[,1] + 3*x[,2] + rnorm(50)
regp2plot(x, y)
}
}
\seealso{
\code{\link{reg2g.p2plot}}, \code{\link{regplot}}
}
