% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{ebarplot.med}
\alias{ebarplot.med}
\title{Error Bar Plot for Medians with Distribution-Free Confidence Intervals}
\usage{
ebarplot.med(
  x,
  y = NULL,
  alpha = 0.05,
  nse = 2,
  liw = uiw,
  aui = NULL,
  ali = aui,
  err = "y",
  tr = 0,
  ylim = NULL,
  sfrac = 0.01,
  gap = 0,
  add = FALSE,
  col = par("col"),
  lwd = par("lwd"),
  slty = par("lty"),
  xlab = "Group",
  ylab = NULL,
  ...
)
}
\arguments{
\item{x}{Data in matrix mode (groups as columns) or list mode (groups as elements).
If providing two vectors, see y parameter.}

\item{y}{Optional second group (vector). If provided, x should also be a vector
(default: NULL).}

\item{alpha}{Significance level for confidence intervals (default: 0.05 for 95\% CIs).}

\item{nse}{Not used for this function (included for consistency with ebarplot).}

\item{liw}{Lower inner width (passed to plotCI, default: uiw).}

\item{aui}{Upper confidence limit (default: NULL, computed from data).}

\item{ali}{Lower confidence limit (default: aui).}

\item{err}{Direction of error bars: "y" for vertical, "x" for horizontal (default: "y").}

\item{tr}{Not used for this function (included for consistency with ebarplot).}

\item{ylim}{Y-axis limits (default: NULL, automatically determined).}

\item{sfrac}{Fraction of plot width for error bar caps (default: 0.01).}

\item{gap}{Gap around points where error bars are not drawn (default: 0).}

\item{add}{Logical. Add to existing plot if TRUE (default: FALSE).}

\item{col}{Color for plotting (default: par("col")).}

\item{lwd}{Line width (default: par("lwd")).}

\item{slty}{Line type for error bars (default: par("lty")).}

\item{xlab}{Label for X-axis (default: "Group").}

\item{ylab}{Label for Y-axis (default: NULL).}

\item{...}{Additional graphical parameters passed to plotCI.}
}
\value{
None. Creates a plot.
}
\description{
Creates an error bar plot showing group medians with distribution-free confidence
intervals. Unlike \code{ebarplot}, this function uses exact nonparametric confidence
intervals for medians rather than standard errors.
}
\details{
This function is specifically designed for median-based comparisons and uses
distribution-free (nonparametric) confidence intervals computed via \code{sint}.

\strong{Advantages over ebarplot with tr = 0.5}:
\itemize{
\item Uses exact distribution-free confidence intervals
\item No normality assumptions required
\item Valid for small samples and non-normal distributions
\item Confidence level controlled by alpha parameter
}

\strong{Confidence intervals}: Computed using the sign test approach (\code{sint}),
which provides exact coverage probabilities without distributional assumptions.

\strong{Input formats}:
\itemize{
\item \strong{Matrix}: Each column represents a group
\item \strong{List}: Each element represents a group
\item \strong{Two vectors}: Provide x and y separately
}

Missing values are automatically removed from each group.

\strong{Note}: For means or trimmed means with standard error bars, use \code{ebarplot}.
}
\examples{
# Median error bar plot for three groups
set.seed(123)
x1 <- rexp(30, rate = 1)  # Skewed data
x2 <- rexp(30, rate = 0.5)
x3 <- rexp(30, rate = 0.8)
ebarplot.med(list(x1, x2, x3))

# Compare with regular error bars
par(mfrow = c(1, 2))
ebarplot(list(x1, x2, x3), main = "Mean ± 2SE")
ebarplot.med(list(x1, x2, x3), main = "Median with 95\% CI")
}
\seealso{
\code{\link{ebarplot}}, \code{\link{sint}}, \code{\link{plotCI}}
}
