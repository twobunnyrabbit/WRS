% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{longreg.plot}
\alias{longreg.plot}
\title{Plot Individual Regression Lines for Longitudinal Data}
\usage{
longreg.plot(
  x,
  x.col,
  y.col,
  s.id,
  regfun = tsreg,
  scat = TRUE,
  xlab = "X",
  ylab = "Y"
)
}
\arguments{
\item{x}{Data frame or matrix containing longitudinal data in long format.}

\item{x.col}{Column index indicating the predictor variable (typically time).}

\item{y.col}{Column index indicating the response variable.}

\item{s.id}{Column index indicating the subject ID.}

\item{regfun}{Regression function to use for fitting lines (default: \code{tsreg}
for Theil-Sen regression). Can be any regression function that returns
coefficients.}

\item{scat}{Logical; if \code{TRUE}, plots scatter points; if \code{FALSE}, plots only
regression lines (default: TRUE).}

\item{xlab, ylab}{Axis labels (defaults: "X", "Y").}
}
\value{
Invisibly returns \code{NULL}. Creates a plot as a side effect showing
individual regression trajectories.
}
\description{
Plots regression lines for longitudinal (repeated measures) data, with one
regression line fitted for each subject. Useful for visualizing individual
trajectories over time.
}
\details{
The function expects data in \strong{long format} where each row represents one
observation, similar to the \code{Orthodont} dataset in the \code{nlme} package.

For each subject:
\enumerate{
\item Extracts their observations from the data
\item Fits a regression line using \code{regfun}
\item Plots the fitted line
}

\strong{Note}: Currently only supports a single predictor (typically time or age).
The function will stop with an error if multiple predictors are specified.
}
\examples{
\dontrun{
# Example with Orthodont data (requires nlme package)
library(nlme)
# Plot individual growth curves
# Column 2 is age, column 4 is distance, column 1 is subject ID
longreg.plot(Orthodont, x.col=2, y.col=4, s.id=1)
}
}
\seealso{
\code{\link{tsreg}}, \code{\link{long2mat}}, \code{\link{longcov2mat}}
}
