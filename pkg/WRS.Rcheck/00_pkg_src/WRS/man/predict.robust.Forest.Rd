% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{predict.robust.Forest}
\alias{predict.robust.Forest}
\title{Predict Using Robust Measures of Location from Random Forest Regions}
\usage{
\method{predict}{robust.Forest}(object, newdata = NULL, what = tmean, ...)
}
\arguments{
\item{object}{Random Forest object (class 'randomForest' or 'quantregForest').}

\item{newdata}{Numeric matrix or data frame of new observations to predict, or
\code{NULL} to use out-of-bag observations.}

\item{what}{Function for location estimation (default: \code{\link{tmean}}) or
numeric vector of quantiles (e.g., c(0.25, 0.75)).}

\item{...}{Additional arguments passed to \code{what} function (e.g., \code{tr=0.1}
for trimmed mean).}
}
\value{
Numeric vector of predictions (if \code{what} returns scalar) or matrix
of predictions (if \code{what} returns vector or multiple quantiles requested).
}
\description{
Estimates robust measures of location for new data based on regions defined by
quantile Random Forest. Applies a user-specified location estimator to values
within each Random Forest leaf node.
}
\details{
For each observation in \code{newdata}, identifies which Random Forest leaf nodes
it falls into across all trees. Applies the \code{what} function to the response
values associated with each leaf. Commonly used robust location estimators include:
\itemize{
\item \code{\link{tmean}}: Trimmed mean (default, 20\\% trimming)
\item \code{\link{hd}}: Harrell-Davis median estimator
\item \code{\link{onestep}}: One-step M-estimator
\item \code{median}, \code{mean}: Standard estimators
}

Alternatively, \code{what} can be a numeric vector of quantiles to estimate (e.g.,
c(0.1, 0.5, 0.9) for 10th, 50th, and 90th percentiles).

For convenience, use \code{\link{regR.Forest}} which wraps this function.

Code based on modifications by L. Michel.
}
\examples{
\dontrun{
# Fit quantile random forest
library(quantregForest)
fit <- quantregForest(x, y)

# Predict using Harrell-Davis median
pred1 <- predict.robust.Forest(fit, newdata = xnew, what = hd)

# Predict using 10\% trimmed mean
pred2 <- predict.robust.Forest(fit, newdata = xnew, what = tmean, tr = 0.1)

# Predict multiple quantiles
pred3 <- predict.robust.Forest(fit, newdata = xnew, what = c(0.25, 0.75))
}

}
\seealso{
\code{\link{regR.Forest}}, \code{\link{tmean}}, \code{\link{hd}}
}
\keyword{regression}
\keyword{robust}
