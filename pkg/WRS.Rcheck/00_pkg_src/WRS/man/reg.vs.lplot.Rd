% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{reg.vs.lplot}
\alias{reg.vs.lplot}
\title{Compare Regression Estimates vs Lowess Estimates}
\usage{
reg.vs.lplot(
  x,
  y,
  xout = FALSE,
  Qreg.plot = TRUE,
  qv = c(0.25, 0.75),
  SMQ = FALSE,
  pch = "*",
  pr = TRUE,
  outfun = outpro,
  fr = 1,
  est = mean,
  regfun = tsreg,
  xlab = "Reg.est",
  ylab = "Lplot.est",
  span = 0.75,
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix of predictor values.}

\item{y}{Numeric vector of response values.}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{Qreg.plot}{Logical; if \code{TRUE} (default), uses quantile regression
plot. If \code{FALSE}, uses lowess plot.}

\item{qv}{Quantiles for quantile regression plot (default: c(0.25, 0.75)).}

\item{SMQ}{Logical; if \code{TRUE}, uses smoothed quantile regression
(default: \code{FALSE}).}

\item{pch}{Plotting character (default: '*').}

\item{pr}{Logical; if \code{TRUE} (default), prints update message.}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{fr}{Span for running interval in outlier detection (default: 1).}

\item{est}{Measure of location for smoother (default: \code{mean}).}

\item{regfun}{Regression function (default: \code{tsreg} for Theil-Sen).}

\item{xlab}{Label for x-axis (default: "Reg.est").}

\item{ylab}{Label for y-axis (default: "Lplot.est").}

\item{span}{Span for lowess smoother (default: 0.75).}

\item{...}{Additional arguments passed to outlier detection function.}
}
\value{
Invisibly returns \code{NULL}. Creates diagnostic plot as side effect.
}
\description{
Diagnostic plot comparing predicted values from parametric regression
against nonparametric lowess/loess smoother. Useful for assessing
linearity assumptions.
}
\details{
Similar to \code{reg.vs.rplot()} but uses lowess/loess smoother instead of
running interval smoother. If the linear model is correct, points should
cluster tightly around the y=x reference line (shown as dashed).

The function:
\enumerate{
\item Computes regression predictions using \code{regYhat()}
\item Computes lowess predictions using \code{lplot.pred()}
\item Plots one against the other with reference line y=x
}

Systematic deviations from the reference line suggest model misspecification.
}
\examples{
# Linear relationship
x <- rnorm(100)
y <- 2*x + rnorm(100)
reg.vs.lplot(x, y)
}
\seealso{
\code{\link{reg.vs.rplot}}, \code{\link{regYhat}},
\code{\link{lplot.pred}}
}
