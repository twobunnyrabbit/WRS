% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{Dancovamp}
\alias{Dancovamp}
\title{Dependent Groups ANCOVA with Multiple Covariates and Design Points}
\usage{
Dancovamp(
  x1,
  y1,
  x2 = NULL,
  y2,
  fr1 = 1,
  fr2 = 1,
  tr = 0.2,
  alpha = 0.05,
  pts = NULL,
  SEED = TRUE,
  DIF = TRUE,
  cov.fun = skipcov,
  ...
)
}
\arguments{
\item{x1}{Matrix of covariates for condition 1.}

\item{y1}{Outcome values for condition 1.}

\item{x2}{Matrix of covariates for condition 2. Default is NULL (uses x1).}

\item{y2}{Outcome values for condition 2.}

\item{tr}{Amount of trimming (0 to 0.5). Default is 0.2.}

\item{alpha}{Significance level. Default is 0.05.}

\item{pts}{Design points matrix. If NULL, chosen based on depth of x1.}

\item{SEED}{Logical. Set random seed for reproducibility. Default is TRUE.}

\item{DIF}{Logical. If TRUE, analyze difference scores; if FALSE, compare marginal estimates. Default is TRUE.}

\item{cov.fun}{Covariance function for finding nearest points. Default is skipcov.
Options: skipcov, tbscov, covogk, mgvcov, mvecov, mcdcov, wincov.}

\item{...}{Additional arguments passed to cov.fun.}
}
\value{
List with components:
\describe{
\item{pts}{Design points matrix}
\item{output}{Matrix with test results for each design point (n, estimates, test statistics, CI, p-values)}
}
}
\description{
Compare two dependent groups using nonparametric ANCOVA with multiple covariates.
Tests differences at multiple design points using running interval smoother.
}
\details{
Extension of Dancova to multiple covariates. At each design point, compares groups
using points nearest to the design point in multivariate covariate space.
If DIF=TRUE, uses trimmed mean of difference scores; if DIF=FALSE, compares
marginal trimmed means with Yuen's test.
}
\examples{
\dontrun{
  x1 <- matrix(rnorm(100), 50, 2)
  y1 <- rnorm(50)
  y2 <- y1 + 0.5 + rnorm(50)  # Paired outcomes
  result <- Dancovamp(x1, y1, x1, y2)  # x2=x1 for dependent design
}
}
\seealso{
\code{\link{Dancova}}, \code{\link{ancovamp}}
}
\keyword{ancova}
\keyword{dependent}
\keyword{multivariate}
