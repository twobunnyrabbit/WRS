% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{hotel1}
\alias{hotel1}
\title{Trimmed Analog of Hotelling's One-Sample T-Squared Test}
\usage{
hotel1(x, null.value = 0, tr = 0)
}
\arguments{
\item{x}{A numeric matrix or data frame where rows are observations and
columns are variables (p-variate data).}

\item{null.value}{Hypothesized value(s) for the population trimmed means.
Can be a single value (applied to all variables) or a vector of length p.
Default is 0.}

\item{tr}{Trimming proportion (default: 0 for no trimming). Use 0.2 for
20\% trimming.}
}
\value{
A list with components:
\item{test.statistic}{The F test statistic}
\item{degrees_of_freedom}{Numerator and denominator degrees of freedom}
\item{p.value}{P-value from F-distribution}
\item{estimate}{Vector of sample trimmed means}
\item{null.value}{Hypothesized mean vector}
}
\description{
Performs a one-sample test for multivariate location using trimmed means,
providing a robust analog to Hotelling's T-squared test.
}
\details{
This function tests the multivariate hypothesis:
\deqn{H_0: \mu_{tr,1} = \mu_1, \ldots, \mu_{tr,p} = \mu_p}
where \eqn{\mu_{tr,j}} are the population trimmed means and \eqn{\mu_j}
are the hypothesized values.

\strong{Test Statistic}:
\deqn{T^2 = n(\bar{x}_{tr} - \mu)' S^{-1} (\bar{x}_{tr} - \mu)}
where \eqn{\bar{x}_{tr}} is the vector of trimmed means and S is the
Winsorized covariance matrix.

The test statistic is transformed to an F-distribution:
\deqn{F = \frac{h - p}{p(h - 1)} T^2}
where h is the effective sample size after trimming.

\strong{Trimming}: When \code{tr > 0}, the test uses trimmed means and Winsorized
covariances, providing robustness to outliers and heavy-tailed distributions.
}
\examples{
\dontrun{
# Bivariate data
x <- matrix(rnorm(100), 50, 2)

# Test if trimmed means equal zero (no trimming)
hotel1(x, null.value = 0, tr = 0)

# Test with 20\% trimming
hotel1(x, null.value = 0, tr = 0.2)

# Test if means equal c(0.5, -0.3)
hotel1(x, null.value = c(0.5, -0.3), tr = 0.2)
}

}
\seealso{
\code{\link{hotel1.tr}}, \code{\link{winall}}
}
\keyword{htest}
\keyword{multivariate}
