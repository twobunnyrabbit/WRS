% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{rplotCIM}
\alias{rplotCIM}
\title{Running Interval Smoother with Confidence Band (Median-Based)}
\usage{
rplotCIM(
  x,
  y,
  est = hd,
  fr = 0.5,
  p.crit = NA,
  plotit = TRUE,
  scat = TRUE,
  pyhat = FALSE,
  pts = NA,
  npts = 25,
  xout = FALSE,
  xlab = "X",
  ylab = "Y",
  low.span = 2/3,
  nmin = 16,
  outfun = out,
  LP = TRUE,
  LPCI = FALSE,
  MID = TRUE,
  alpha = 0.05,
  pch = ".",
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{est}{Estimator function (currently must be \code{hd} for Harrell-Davis,
default: \code{hd}).}

\item{fr}{Span parameter controlling amount of smoothing (default: 0.5).
For strong associations, consider fr=0.2 for less smoothing.}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{pts}{Optional numeric vector of points where CIs are computed (default: NA,
automatically chosen).}

\item{npts}{Number of points where confidence intervals are computed (default: 25).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{xlab}{Label for x-axis in plots (default: "X" or "VAR 1").}

\item{ylab}{Label for y-axis in plots (default: "Y" or "VAR 2").}

\item{low.span}{Span parameter for lowess smoothing of confidence band (default: 2/3).}

\item{nmin}{Minimum sample size required in each interval (default: 16).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{LP}{Logical. If \code{TRUE}, smooths the fitted curve via lowess (default: \code{TRUE}).}

\item{LPCI}{Logical. If \code{TRUE}, smooths the confidence intervals via lowess
for smoother appearance (default: \code{FALSE}).}

\item{MID}{Logical. If \code{TRUE}, computes intervals in middle range of data
(default: \code{TRUE}).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{pch}{Plotting character for scatter plot (default: '.').}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
List with components:
\item{output}{Matrix with columns: pts, y.hat, ci.low, ci.up}
\item{str}{Strength of association measure}
\item{n}{Total sample size}
\item{n.keep}{Sample size after outlier removal}
}
\description{
Running interval smoother based on the Harrell-Davis median estimator with
simultaneous confidence band. Provides a robust alternative to \code{rplotCI} for
non-normal data.
}
\details{
This function is similar to \code{rplotCI} but uses the Harrell-Davis median
estimator instead of trimmed means, making it more robust for heavy-tailed
distributions. The confidence band has simultaneous probability coverage of
1-alpha across \code{npts} points using Bonferroni adjustment.

The current version requires \code{est=hd}. Future versions may support additional
estimators.
}
\examples{
\dontrun{
# Median-based running smoother with confidence band
x <- rnorm(100)
y <- 2*x + rt(100, df=3)  # Heavy-tailed errors
rplotCIM(x, y, fr=0.5)
}
}
\seealso{
\code{\link{rplotCI}}, \code{\link{hd}}, \code{\link{sint}}
}
