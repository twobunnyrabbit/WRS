% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{scor}
\alias{scor}
\title{Skipped Correlation}
\usage{
scor(
  x,
  y = NULL,
  corfun = pcor,
  gval = NA,
  plotit = FALSE,
  op = TRUE,
  MM = FALSE,
  cop = 3,
  xlab = "VAR 1",
  ylab = "VAR 2",
  STAND = TRUE,
  pr = TRUE,
  SEED = TRUE,
  MC = FALSE,
  RAN = FALSE
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{corfun}{Correlation function to use. Common options: \code{pbcor} (percentage
bend), \code{pcor} (Pearson), \code{wincor} (winsorized), \code{spear} (Spearman).}

\item{gval}{Numeric. Critical value for outlier detection. If \code{NA} (default),
determined automatically.}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{op}{Logical. If \code{TRUE}, uses certain options (default: \code{TRUE}).}

\item{MM}{Logical. If \code{TRUE}, uses MM-estimator (default: \code{FALSE}).}

\item{cop}{Copula type for skipped correlation (default: 3). Options: 1-5.}

\item{xlab}{Label for x-axis in plots (default: "X" or "VAR 1").}

\item{ylab}{Label for y-axis in plots (default: "Y" or "VAR 2").}

\item{STAND}{Logical. If \code{TRUE}, standardizes data before analysis
(default: \code{TRUE}).}

\item{pr}{Logical. If \code{TRUE}, prints progress messages (default: \code{TRUE}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{MC}{Logical. If \code{TRUE}, uses parallel processing via \code{mclapply}
(default: \code{FALSE}).}

\item{RAN}{Logical. Randomization flag (default: \code{FALSE}).}
}
\value{
List with components:
\item{cor}{Skipped correlation coefficient.}
\item{sig.level}{Significance level / p-value.}
\item{n.keep}{Number of observations retained after outlier removal.}
\item{outliers}{Indices of detected outliers.}
}
\description{
Computes a skipped correlation after removing bivariate outliers using
projection-based outlier detection.
}
\details{
A "skipped" correlation removes bivariate outliers before computing
the correlation. This provides robustness against influential points
while maintaining efficiency for clean data.

The function uses projection-based outlier detection (see \code{outpro()}).
For each observation, the function considers the line between it and the
center, projects all points onto this line, and checks for outliers using
a modified boxplot rule. An observation is flagged as an outlier if it is
extreme for any projection.

The \code{cop} parameter determines how the center is estimated:
\itemize{
\item cop=1: Donoho-Gasko halfspace median
\item cop=2: MCD measure of location
\item cop=3: Marginal medians (default)
\item cop=4: MVE measure of location
}

If \code{MC = TRUE}, uses the multicore version of \code{outpro()} for faster
computation on multi-core systems.
}
\examples{
# Basic skipped correlation
set.seed(123)
x <- rnorm(50)
y <- 0.6 * x + rnorm(50)
scor(x, y)

# With outliers
x2 <- c(x, 5, -5)
y2 <- c(y, -5, 5)
result <- scor(x2, y2)
result$cor  # Robust to outliers
result$outliers  # Which points were removed
result$n.keep  # How many points retained

# Using Spearman correlation after outlier removal
scor(x2, y2, corfun = spear)
}
\seealso{
\code{\link{scorci}}, \code{\link{scorciMC}}, \code{\link{mscor}},
\code{\link{pbcor}}, \code{\link{outpro}}
}
