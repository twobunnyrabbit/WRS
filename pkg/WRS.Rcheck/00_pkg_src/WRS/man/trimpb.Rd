% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{trimpb}
\alias{trimpb}
\title{Percentile Bootstrap for Trimmed Mean Comparison}
\usage{
trimpb(
  x,
  y = NULL,
  tr = 0.2,
  alpha = 0.05,
  nboot = 2000,
  WIN = FALSE,
  win = 0.1,
  plotit = FALSE,
  pop = 1,
  null.value = 0,
  pr = TRUE,
  xlab = "X",
  fr = NA,
  SEED = TRUE
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{tr}{Trimming proportion (default: 0.2). Proportion of observations to
trim from each tail when computing winsorized correlation.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{WIN}{Logical. If \code{TRUE}, uses Winsorizing (default: \code{FALSE}).}

\item{win}{Amount of Winsorizing when \code{WIN = TRUE} (default: 0.1).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{pop}{Plot type: 1=expected frequency, 2=kernel density, 3=boxplot, 4=stem-and-leaf (default: 1).}

\item{null.value}{Null hypothesis value for testing (default: 0).}

\item{pr}{Logical. If \code{TRUE}, prints progress messages (default: \code{TRUE}).}

\item{xlab}{Label for x-axis (default: "X").}

\item{fr}{Fraction parameter (default: NA).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}
}
\value{
A list with components including estimates, confidence interval, and p-value.
}
\description{
Enhanced version of trimmed mean comparison with bootstrap,  supporting both
one-sample and two-sample designs (independent or dependent groups).
}
\details{
This function compares trimmed means using percentile bootstrap. It can handle:
\itemize{
\item One-sample test when only \code{x} is provided
\item Independent two-sample test when \code{x} and \code{y} are separate vectors
\item Dependent (paired) test when \code{y = NULL} and \code{x} is a two-column matrix
}

Missing values are automatically removed.

When \code{plotit = TRUE}, creates a plot of the bootstrap distribution with type
specified by \code{pop}.
}
\examples{
\dontrun{
# Two independent groups
x <- rnorm(50)
y <- rnorm(60, mean = 0.5)
trimpb(x, y)

# Dependent groups (paired)
before <- rnorm(30)
after <- before + rnorm(30, mean = 0.3)
trimpb(cbind(before, after))
}
}
\seealso{
\code{\link{trimpb2}}, \code{\link{pb2gen}}
}
