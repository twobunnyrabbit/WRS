% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{ols.plot.inter}
\alias{ols.plot.inter}
\title{Plot OLS Regression Surface with Interaction Term}
\usage{
ols.plot.inter(
  x,
  y,
  pyhat = FALSE,
  eout = FALSE,
  xout = FALSE,
  outfun = out,
  plotit = TRUE,
  expand = 0.5,
  scale = TRUE,
  xlab = "X",
  ylab = "Y",
  zlab = "",
  theta = 50,
  phi = 25,
  family = "gaussian",
  duplicate = "error",
  ticktype = "simple",
  ...
)
}
\arguments{
\item{x}{An n-by-2 matrix of predictors (must have exactly 2 columns).}

\item{y}{Vector of response values (same length as rows in \code{x}).}

\item{pyhat}{Logical; currently not used (legacy parameter).}

\item{eout}{Logical; currently not used (legacy parameter).}

\item{xout}{Logical; if TRUE, removes outliers in the predictor space before
fitting. Default is FALSE.}

\item{outfun}{Outlier detection function to use when \code{xout=TRUE}. Default
is \code{\link{out}}.}

\item{plotit}{Logical; currently not used (legacy parameter).}

\item{expand}{Numeric expansion factor for z-axis. See
\code{\link[graphics]{persp}}. Default is 0.5.}

\item{scale}{Logical; if TRUE (default), the z-axis is scaled.}

\item{xlab}{Label for first predictor axis. Default is 'X'.}

\item{ylab}{Label for second predictor axis. Default is 'Y'.}

\item{zlab}{Label for response (z) axis. Default is empty string.}

\item{theta}{Viewing angle (azimuthal direction). Default is 50 degrees.}

\item{phi}{Viewing angle (colatitude). Default is 25 degrees.}

\item{family}{Currently not used (legacy parameter).}

\item{duplicate}{How to handle duplicate x-y locations. Default is 'error'.
See \code{\link[akima]{interp}}.}

\item{ticktype}{Type of tick marks. Default is 'simple'. See
\code{\link[graphics]{persp}}.}

\item{...}{Additional arguments passed to \code{outfun}.}
}
\value{
No return value; creates a 3D plot as a side effect.
}
\description{
Creates a 3D perspective plot of an OLS (Ordinary Least Squares) regression
surface with an interaction term (product of two predictors). This is the
least squares version of \code{\link{reg.plot.inter}}.
}
\details{
The function fits the OLS regression model: Y = b0 + b1\emph{X1 + b2}X2 + b3\emph{X1}X2
using ordinary least squares (\code{\link[stats]{lsfit}}). The interaction
term X1*X2 allows the relationship between Y and X1 to vary with X2 (and
vice versa).

The fitted surface is visualized using a 3D perspective plot via
\code{\link[graphics]{persp}} and \code{\link[akima]{interp}} for
interpolation. Duplicate points in the x-y plane are removed before plotting.

For a robust alternative that is less sensitive to outliers, see
\code{\link{reg.plot.inter}}.
}
\examples{
\dontrun{
# Create sample data with interaction
set.seed(123)
n <- 100
x1 <- runif(n, -2, 2)
x2 <- runif(n, -2, 2)
x <- cbind(x1, x2)
y <- 1 + 2*x1 + 3*x2 + 1.5*x1*x2 + rnorm(n)

# Plot OLS regression surface
ols.plot.inter(x, y)

# Different viewing angles
ols.plot.inter(x, y, theta = 30, phi = 15)

# Remove outliers before fitting
ols.plot.inter(x, y, xout = TRUE)
}

}
\seealso{
\code{\link{reg.plot.inter}}, \code{\link{regp2plot}},
\code{\link[stats]{lsfit}}, \code{\link[graphics]{persp}},
\code{\link[akima]{interp}}
}
