% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{PcorREG.best.DO}
\alias{PcorREG.best.DO}
\title{Pearson Correlation Best Predictor Decision (Decision-Oriented)}
\usage{
PcorREG.best.DO(
  x,
  y,
  neg.col = NULL,
  LARGEST = TRUE,
  xout = FALSE,
  outfun = outpro,
  ...
)
}
\arguments{
\item{x}{Matrix (n-by-p) of independent variables.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{neg.col}{Vector of column indices to negate before computing correlations
(default: \code{NULL}).}

\item{LARGEST}{Logical. Identify predictor with largest correlation if \code{TRUE}
(default), smallest if \code{FALSE}.}

\item{xout}{Logical. Remove outliers from x before analysis (default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{...}{Additional arguments passed to \code{outfun}.}
}
\value{
List with components:
\item{output}{Matrix with columns: Best.IV, IV, Est.best, Est, dif, ci.low, ci.up.}
\item{Result}{Character string: "Decide IV X is best" or "No Decision".}
}
\description{
Determines the best predictor by Pearson correlation and makes a formal
decision using pairwise comparisons via TWOpov.
}
\details{
Specifically for Pearson correlation. Identifies the predictor with the
strongest Pearson correlation, then performs pairwise comparisons between
this predictor and all others using \code{TWOpov()}. Makes a decision if all
pairwise confidence intervals exclude zero.
}
\examples{
\dontrun{
set.seed(123)
x <- matrix(rnorm(50 * 4), ncol = 4)
y <- 0.8 * x[, 1] + 0.2 * x[, 2] + rnorm(50)
PcorREG.best.DO(x, y)
}
}
\seealso{
\code{\link{corREG.DO}}, \code{\link{TWOpov}}, \code{\link{corREG.best}}
}
