% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{pcorb}
\alias{pcorb}
\title{Bootstrap Confidence Interval for Pearson Correlation}
\usage{
pcorb(x, y, SEED = TRUE)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}
}
\value{
List with components:
\item{r}{Pearson correlation coefficient.}
\item{ci}{95\% confidence interval (vector of length 2).}
}
\description{
Computes a 95\% confidence interval for Pearson's correlation coefficient
using an adjusted percentile bootstrap method.
}
\details{
This function uses an adjusted percentile bootstrap method that performs
well when the error term is heteroscedastic. The percentiles used for the
confidence interval are adjusted based on sample size to improve coverage.

Sample size adjustments:
\itemize{
\item n >= 250: uses 15th and 584th order statistics
\item 180 <= n < 250: uses 14th and 585th
\item 80 <= n < 180: uses 11th and 588th
\item 40 <= n < 80: uses 8th and 592nd
\item n < 40: uses 7th and 593rd
}

The function always uses nboot = 599 bootstrap samples.
}
\examples{
# Bootstrap CI for Pearson correlation
set.seed(123)
x <- rnorm(50)
y <- 0.7 * x + rnorm(50)
pcorb(x, y)
}
\seealso{
\code{\link{corb}}, \code{\link{pcor}}
}
