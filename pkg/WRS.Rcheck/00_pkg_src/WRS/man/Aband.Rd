% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{Aband}
\alias{Aband}
\title{Shift Function for Factor A Main Effect in 2×2 Design}
\usage{
Aband(
  x,
  alpha = 0.05,
  plotit = TRUE,
  sm = TRUE,
  SEED = TRUE,
  nboot = 500,
  grp = c(1:4),
  xlab = "X (First Factor)",
  ylab = "Delta",
  crit = NA,
  print.all = FALSE,
  plot.op = FALSE
)
}
\arguments{
\item{x}{Data in list or matrix format containing 4 groups}

\item{alpha}{Alpha level for confidence intervals (default: 0.05)}

\item{plotit}{Logical. If \code{TRUE} (default), creates a plot}

\item{sm}{Logical. If \code{TRUE} (default), uses smoothing in shift function}

\item{SEED}{Logical. If \code{TRUE} (default), sets random seed for reproducibility}

\item{nboot}{Number of bootstrap samples for critical value approximation (default: 500)}

\item{grp}{Vector indicating group assignment. Default \code{c(1:4)} means level 1 of
factor A = groups 1,2 and level 2 = groups 3,4}

\item{xlab}{Label for x-axis (default: "X (First Factor)")}

\item{ylab}{Label for y-axis (default: "Delta")}

\item{crit}{Critical value for confidence band. If \code{NA} (default), computed via bootstrap}

\item{print.all}{Logical. If \code{FALSE} (default), returns summary statistics only.
If \code{TRUE}, returns all confidence intervals}

\item{plot.op}{Logical. If \code{TRUE}, creates alternative plot format (default: \code{FALSE})}
}
\value{
If \code{print.all=FALSE} (default), returns a matrix with:
\itemize{
\item \code{number sig} - Number of confidence intervals not containing zero
\item \code{critical value} - Critical value used for confidence band
\item \code{KS test statistics} - Kolmogorov-Smirnov test statistic
}
If \code{print.all=TRUE}, returns output from \code{\link{sband}} with all confidence intervals.
}
\description{
Applies the shift function when analyzing the main effect of the first factor
in a 2×2 factorial design. Compares distributions (x1+x2)/2 vs (x3+x4)/2 where
groups 1,2 represent level 1 of factor A and groups 3,4 represent level 2.
}
\details{
This function focuses on the first factor (Factor A) in a 2×2 design by:
\enumerate{
\item Computing outer sum: (x1+x2)/2 for level 1, (x3+x4)/2 for level 2
\item Using bootstrap to approximate critical value for confidence band
\item Applying shift function via \code{\link{sband}}
\item Testing with Kolmogorov-Smirnov statistic
}
For the second factor (Factor B), use \code{\link{Bband}} instead.
}
\seealso{
\code{\link{Bband}}, \code{\link{sband}}
}
\keyword{multivariate}
