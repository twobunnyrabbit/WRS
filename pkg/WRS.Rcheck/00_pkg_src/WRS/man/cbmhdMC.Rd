% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/location.R
\name{cbmhdMC}
\alias{cbmhdMC}
\title{CI for Sum of Symmetric Quantiles (Independent Groups, Parallel)}
\usage{
cbmhdMC(
  x,
  y,
  alpha = 0.05,
  q = 0.25,
  plotit = FALSE,
  pop = 0,
  fr = 0.8,
  rval = 15,
  xlab = "",
  ylab = "",
  nboot = 600,
  SEED = TRUE
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{q}{Quantile to estimate. For example, \code{q = 0.5} estimates the median,
\code{q = 0.25} estimates the first quartile (default: 0.5).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{pop, fr, rval, xlab, ylab}{Plot parameters.}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}
}
\value{
Same as \code{\link{cbmhd}}.
}
\description{
Parallel processing version of \code{\link{cbmhd}} using \code{mclapply}.
Computes CI for sum of qth and (1-q)th quantiles of D = X - Y.
}
\details{
This function is identical to \code{\link{cbmhd}} but uses parallel
processing via \code{mclapply} for faster computation with large bootstrap
samples. May not work on all systems.
}
\examples{
\dontrun{
x <- rnorm(30)
y <- rnorm(30)
cbmhdMC(x, y, q = 0.25, nboot = 2000)
}

}
\seealso{
\code{\link{cbmhd}} for sequential version
}
