% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{ancovap2.KMS.plot}
\alias{ancovap2.KMS.plot}
\title{Plot ANCOVA KMS Effect Size as Function of Two Covariates}
\usage{
ancovap2.KMS.plot(
  x1,
  y1,
  x2,
  y2,
  pts = NULL,
  xlab = "X1",
  ylab = "X2",
  zlab = "Effect Size",
  xout = FALSE,
  outfun = outpro,
  SEED = TRUE,
  theta = 50,
  phi = 25,
  REV = FALSE
)
}
\arguments{
\item{x1}{A matrix with two columns containing the covariate values for group 1.}

\item{y1}{A numeric vector containing the outcome variable for group 1.}

\item{x2}{A matrix with two columns containing the covariate values for group 2.}

\item{y2}{A numeric vector containing the outcome variable for group 2.}

\item{pts}{A matrix with two columns specifying covariate value combinations.
If \code{NULL} (default), uses the deepest 90\% of the pooled data from x1 and x2.}

\item{xlab}{Label for first covariate axis (default: 'X1').}

\item{ylab}{Label for second covariate axis (default: 'X2').}

\item{zlab}{Label for vertical axis showing effect size (default: 'Effect Size').}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{theta}{Azimuthal viewing angle for 3D plot in degrees (default: 50).
Controls horizontal rotation of the plot.}

\item{phi}{Colatitude viewing angle for 3D plot in degrees (default: 25).
Controls vertical tilt of the plot.}

\item{REV}{Logical. If \code{FALSE} (default), uses covariates in original order.
If \code{TRUE}, reverses the order of covariates. This can affect the appearance
of the LOESS surface fitted to the effect sizes.}
}
\value{
A list with component:
\describe{
\item{Number_of_points_used_is}{The number of covariate value combinations
used in computing and plotting the effect sizes.}
}
}
\description{
Creates a 3D visualization of the KMS effect size as a function of two
covariates when comparing two independent regression lines. Automatically
removes leverage points for robust estimation.
}
\details{
The function creates a 3D plot showing how the KMS effect size varies across
the two-dimensional covariate space. The plotting method depends on the number
of points:
\itemize{
\item If N < 25: Uses \code{scatterplot3d} for a simple 3D scatter plot
\item If N ≥ 25: Uses \code{lplot} to fit a LOESS surface and create a
smoother visualization
}

Leverage points are automatically detected and removed using the function
specified by \code{outfun} (default: \code{outpro}) to ensure robust effect size
estimates.

The \code{REV} parameter can be useful when the orientation of the LOESS surface
doesn't clearly show the relationship - reversing the covariate order may
provide a clearer view.
}
\examples{
\dontrun{
# Generate example data
set.seed(123)
n1 <- 50; n2 <- 50
x1 <- matrix(rnorm(n1 * 2), ncol = 2)
y1 <- x1[,1] + x1[,2] + rnorm(n1)
x2 <- matrix(rnorm(n2 * 2), ncol = 2)
y2 <- x2[,1] + x2[,2] + 0.5 + rnorm(n2)

# Create 3D plot of effect sizes
ancovap2.KMS.plot(x1, y1, x2, y2)
}
}
\seealso{
\code{\link{ancovap2.KMS}}, \code{\link{ancovap2.KMSci}}
}
