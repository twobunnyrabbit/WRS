% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{bwmarpb}
\alias{bwmarpb}
\title{Bootstrap Multiple Comparisons for Marginal Means in Split-Plot Design}
\usage{
bwmarpb(
  J,
  K,
  x,
  est = hd,
  JK = J * K,
  grp = c(1:JK),
  nboot = 599,
  SEED = TRUE,
  na.rm = TRUE,
  ...
)
}
\arguments{
\item{J}{Number of levels for between-subjects factor (Factor A).}

\item{K}{Number of levels for within-subjects factor (Factor B).}

\item{x}{Data in matrix, data frame, or list format with J*K groups.}

\item{est}{Location estimator function (default: \code{\link{hd}} for 20\% trimming).}

\item{JK}{Total number of groups (default: J*K).}

\item{grp}{Vector specifying group order (default: 1:JK).}

\item{nboot}{Number of bootstrap samples (default: 599).}

\item{SEED}{Logical; if \code{TRUE} (default), sets random seed to 2 for reproducibility.}

\item{na.rm}{Logical; if \code{TRUE} (default), removes missing values within each
level of Factor A.}

\item{...}{Additional arguments passed to the estimator function.}
}
\value{
A list containing:
\item{FacA}{Matrix of estimates and p-values for Factor A contrasts}
\item{FacB}{Matrix of estimates and p-values for Factor B contrasts}
\item{p.FacAB}{Matrix of estimates and p-values for interaction contrasts}
\item{conA, conB, conAB}{Contrast matrices used for each set of tests}
}
\description{
Performs percentile bootstrap multiple comparisons for marginal measures of
location in a J x K between-within (split-plot) design.
}
\details{
This function computes all pairwise comparisons for:
\itemize{
\item Main effect of Factor A (between-subjects)
\item Main effect of Factor B (within-subjects)
\item Interaction contrasts (Factor A × Factor B)
}

Uses \code{\link{con2way}} to generate contrasts and \code{\link{psihat}}
to compute linear contrasts.

Data organization: If in list mode, x[\link{1}] = level (1,1), x[\link{2}] = level (1,2),
..., x[\link{K}] = level (1,K), x[\link{K+1}] = level (2,1), etc.
}
\seealso{
\code{\link{con2way}}, \code{\link{psihat}}, \code{\link{bwimcp}}
}
\keyword{bootstrap}
\keyword{htest}
