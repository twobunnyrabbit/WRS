% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression.R
\name{tstsreg}
\alias{tstsreg}
\title{Modified Theil-Sen Regression with Outlier Removal}
\usage{
tstsreg(
  x,
  y,
  sc = pbvar,
  xout = FALSE,
  outfun = outpro,
  iter = 5,
  plotit = FALSE,
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix of predictor values (n by p).}

\item{y}{Numeric vector of response values (length n).}

\item{sc}{Robust scale function for S-type estimation (default: \code{pbvar}).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{iter}{Number of iterations for S-type regression (default: 5).}

\item{plotit}{Logical; currently unused (default: FALSE).}

\item{...}{Additional arguments passed to \code{outfun}.}
}
\value{
A list with components:
\describe{
\item{coef}{Vector of regression coefficients (intercept, slopes).}
\item{residuals}{Residuals from the final Theil-Sen fit.}
}
}
\description{
Computes a modified Theil-Sen regression estimator by first using an S-type
initial estimate to identify and remove outlying residuals, then applying
standard Theil-Sen regression to the remaining data.
}
\details{
This two-stage robust regression procedure:
\enumerate{
\item \strong{Stage 1 (Outlier identification)}: Fits an S-type Theil-Sen regression
using \code{\link{stsreg}} to get initial residuals.
\item \strong{Stage 2 (Outlier removal)}: Identifies observations with standardized
residuals exceeding 2 (in MAD units: |residual - median| / MAD > 2).
\item \strong{Stage 3 (Final fit)}: Applies standard Theil-Sen regression
(\code{\link{tsreg}}) to the remaining observations.
}

This approach combines:
\itemize{
\item The efficiency of S-type estimation for initial outlier detection
\item The simplicity and robustness of standard Theil-Sen for the final fit
\item Improved performance when a small fraction of outliers is present
}

The method is particularly useful when you expect some contamination but
want to ensure the final estimates are computed on relatively clean data.
}
\examples{
\dontrun{
# Data with a few outliers
set.seed(123)
x <- rnorm(50)
y <- 2*x + rnorm(50)
y[1:3] <- y[1:3] + 10  # Add outliers

# Standard Theil-Sen (may be affected by outliers)
tsreg(x, y)

# Modified version with outlier removal
tstsreg(x, y)

# Multiple predictors
x <- matrix(rnorm(100), ncol=2)
y <- x[,1] + 2*x[,2] + rnorm(50)
y[1:5] <- y[1:5] + 8
tstsreg(x, y)
}

}
\seealso{
\code{\link{stsreg}} for S-type Theil-Sen,
\code{\link{tsreg}} for standard Theil-Sen,
\code{\link{tssnmreg}} for combining Theil-Sen with skipped regression
}
