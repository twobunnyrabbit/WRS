% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{DqdifMC}
\alias{DqdifMC}
\title{Compare Dependent Groups Using Quantiles (Parallel Processing)}
\usage{
DqdifMC(
  x,
  y = NULL,
  q = 0.25,
  nboot = 1000,
  plotit = TRUE,
  xlab = "Group 1 - Group 2",
  SEED = TRUE,
  alpha = 0.05
)
}
\arguments{
\item{x}{Numeric vector for group 1, or a vector of difference scores if \code{y=NULL}.}

\item{y}{Numeric vector for group 2 (default: NULL). If NULL, \code{x} is treated as differences.}

\item{q}{Numeric in (0, 0.5). Quantile to compare (default: 0.25 for quartiles).}

\item{nboot}{Integer. Number of bootstrap samples (default: 1000).}

\item{plotit}{Logical. If TRUE, plots kernel density of difference scores (default: TRUE).}

\item{xlab}{Character. Label for x-axis if plotting (default: "Group 1 - Group 2").}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}
}
\value{
List with components:
\describe{
\item{est.q}{Harrell-Davis estimate of qth quantile of differences}
\item{est.1.minus.q}{Harrell-Davis estimate of (1-q)th quantile of differences}
\item{conf.interval}{Bootstrap CI for sum of q and (1-q) quantiles}
\item{p.value}{Two-sided p-value testing if sum equals zero}
}
}
\description{
Tests whether two dependent groups differ by comparing the q and (1-q) quantiles
of the difference scores. Uses parallel processing via \code{mclapply} for bootstrap.
Under symmetry, the sum of these quantiles should equal zero.
}
\details{
The test is based on the principle that if difference scores are symmetric about zero
(i.e., no group difference), then q-quantile + (1-q)-quantile should equal zero.

\strong{Method}:
\enumerate{
\item Computes difference scores
\item Bootstrap resamples differences
\item For each resample, computes sum of q and (1-q) quantiles
\item Constructs CI and p-value from bootstrap distribution
}

Uses \code{\link{hd}} (Harrell-Davis estimator) for quantile estimation.
Parallel processing via \code{mclapply} speeds up bootstrap.
}
\examples{
# Compare two dependent groups at quartiles
set.seed(123)
x <- rnorm(30)
y <- x + rnorm(30, 0.5, 1)  # Slight shift
DqdifMC(x, y, q = 0.25, nboot = 500, plotit = FALSE)
}
\seealso{
\code{\link{Dqcomhd}} for independent groups version,
\code{\link{hd}} for Harrell-Davis quantile estimator,
\code{\link{qdifMC}} for related function
}
\keyword{htest}
\keyword{robust}
