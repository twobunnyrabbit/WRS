% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{anc.2gbin}
\alias{anc.2gbin}
\title{ANCOVA for Binary Outcomes - Two Groups}
\usage{
anc.2gbin(
  x1,
  y1,
  x2,
  y2,
  alpha = 0.05,
  FWE.alpha = 0.05,
  pts = NA,
  fr1 = 0.8,
  fr2 = 0.8,
  npts = 5,
  xlab = "X",
  ylab = "Est. Dif",
  xout = FALSE,
  outfun = out,
  nmin = 12,
  plotit = TRUE,
  method = "BH",
  bin.method = "KMS",
  SEED = TRUE,
  reps = 5000,
  pr = TRUE
)
}
\arguments{
\item{x1}{Covariate values for group 1.}

\item{y1}{Binary outcome (0/1) for group 1.}

\item{x2}{Covariate values for group 2.}

\item{y2}{Binary outcome (0/1) for group 2.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{FWE.alpha}{Familywise error rate for significance testing (default: 0.05).}

\item{pts}{Design points for comparison. If NA, uses npts equally spaced points.}

\item{fr1}{Span for running interval smoother for group 1 (default: 0.8).}

\item{fr2}{Span for running interval smoother for group 2 (default: 0.8).}

\item{npts}{Number of design points if pts=NA (default: 5).}

\item{xlab}{Label for x-axis.}

\item{ylab}{Label for y-axis.}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{nmin}{Minimum sample size required in each interval (default: 12).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{method}{P-value adjustment method (default: 'BH' for Benjamini-Hochberg).}

\item{bin.method}{Binomial comparison method: 'KMS', 'ECP', 'ZHZ', or 'SK'.}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{reps}{Number of replications for critical value estimation (default: 5000).}

\item{pr}{Logical. If \code{TRUE}, prints progress messages (default: \code{TRUE}).}
}
\value{
List with components:
\item{n1}{Sample size for group 1}
\item{n2}{Sample size for group 2}
\item{FWE.crit}{Critical value for familywise error rate control}
\item{output}{Matrix with results at each design point}
}
\description{
Compares probability of success for two independent groups across covariate values.
Uses a running-interval smoother coupled with binomial comparison methods.
Provides familywise error rate control through p-value adjustment.
}
\examples{
\dontrun{
x1 <- rnorm(100)
y1 <- rbinom(100, 1, plogis(x1))
x2 <- rnorm(100)
y2 <- rbinom(100, 1, plogis(x2 - 0.5))
anc.2gbin(x1, y1, x2, y2, npts=5)
}
}
