% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression-advanced.R
\name{regYband}
\alias{regYband}
\title{Plot Confidence Band for Regression Predictions}
\usage{
regYband(
  x,
  y,
  regfun = tsreg,
  npts = NULL,
  nboot = 100,
  xout = FALSE,
  outfun = outpro,
  SEED = TRUE,
  alpha = 0.05,
  crit = NULL,
  xlab = "X",
  ylab = "Y",
  SCAT = TRUE,
  ADJ = TRUE,
  pr = TRUE,
  nreps = 1000,
  MC = FALSE,
  pch = ".",
  ...
)
}
\arguments{
\item{x}{A numeric vector containing predictor values (single predictor only).}

\item{y}{A numeric vector containing the response variable.}

\item{regfun}{Regression function to use (default: \code{tsreg}).}

\item{npts}{Number of points to use for confidence intervals when \code{ADJ = FALSE}.
If \code{NULL}, uses 20 evenly-spaced points between min(x) and max(x).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{xlab, ylab}{Axis labels.}

\item{SCAT}{Logical. If \code{TRUE}, plots the scatter plot of data points
(default: \code{TRUE}).}

\item{ADJ}{Logical. If \code{TRUE}, creates simultaneous confidence band with
overall coverage 1-alpha; if \code{FALSE}, creates pointwise confidence
intervals (default: \code{TRUE}).}

\item{pr}{Logical. If \code{TRUE}, prints progress messages (default: \code{TRUE}).}

\item{nreps}{Number of replications for computing critical value (default: 1000).}

\item{MC}{Logical. If \code{TRUE}, uses parallel processing via \code{mclapply}
(default: \code{FALSE}).}

\item{pch}{Plotting character for scatter plot (default: ".").}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
A matrix with columns: X value, predicted Y, lower CI, upper CI.
The plot is created as a side effect.
}
\description{
Creates a plot with confidence bands or intervals for predicted Y values across
the range of X, with options for pointwise or simultaneous coverage.
}
\details{
This function creates a visual representation of regression uncertainty:
\itemize{
\item With \code{ADJ = FALSE}: Plots pointwise confidence intervals at \code{npts}
evenly-spaced x values. Each interval has individual coverage 1-alpha.
\item With \code{ADJ = TRUE}: Plots a simultaneous confidence band at all
unique x values with overall coverage 1-alpha. Wider than pointwise CIs.
}

The simultaneous band accounts for multiple comparisons, ensuring that the
probability that ALL intervals simultaneously contain the true values is 1-alpha.

This function is restricted to a single predictor. For multiple predictors,
use \code{regYci()} directly.
}
\examples{
\dontrun{
# Confidence band for regression
set.seed(123)
x <- rnorm(50)
y <- 2*x + rnorm(50)
# Simultaneous confidence band
regYband(x, y, ADJ = TRUE)
# Pointwise confidence intervals
regYband(x, y, ADJ = FALSE, npts = 30)
}
}
\references{
Wilcox, R.R. (2022). Introduction to Robust Estimation and Hypothesis Testing
(5th ed.). Academic Press.
}
\seealso{
\code{\link{regYci}}, \code{\link{regci}}, \code{\link{tsreg}}
}
