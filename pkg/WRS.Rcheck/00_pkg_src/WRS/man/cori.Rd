% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{cori}
\alias{cori}
\title{Correlation at Specified Value of Third Variable}
\usage{
cori(
  x,
  y,
  z,
  pt = median(z),
  fr = 0.8,
  est = onestep,
  corfun = pbcor,
  testit = FALSE,
  nboot = 599,
  sm = FALSE,
  xlab = "X",
  ylab = "Y",
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{z}{Numeric vector. The conditioning/splitting variable.}

\item{pt}{Numeric. The point at which to split z (default: \code{median(z)}).}

\item{fr}{Numeric. Fraction of data for smoothing (default: 0.8).}

\item{est}{Location estimator function (default: \code{onestep}).}

\item{corfun}{Correlation function to use. Common options: \code{pbcor} (percentage
bend), \code{pcor} (Pearson), \code{wincor} (winsorized), \code{spear} (Spearman).}

\item{testit}{Logical. If \code{TRUE}, tests equality of correlations in two groups.}

\item{nboot}{Number of bootstrap samples for testing (default: 599).}

\item{sm}{Logical. Affects smoothing in \code{runmean2g} (default: \code{FALSE}).}

\item{xlab}{Label for x-axis.}

\item{ylab}{Label for y-axis.}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
If \code{testit = FALSE}, returns "Done" (used for plotting side effect).
If \code{testit = TRUE}, returns result from \code{twocor()} testing equal correlations.
}
\description{
Computes and compares correlations between x and y for observations
split by a third variable z at a specified point.
}
\details{
This function splits the data into two groups based on whether z < pt or z >= pt,
then plots smoothed regression lines for each group using \code{runmean2g()}.

If \code{testit = TRUE}, it also tests whether the correlations differ between
the two groups.
}
\examples{
set.seed(123)
n <- 100
z <- rnorm(n)
x <- rnorm(n)
y <- ifelse(z > 0, 0.8 * x, 0.2 * x) + rnorm(n)
# Compare correlations for z < median vs z >= median
cori(x, y, z, testit = TRUE)
}
\seealso{
\code{\link{runcor}}, \code{\link{twocor}}, \code{\link{pbcor}}
}
