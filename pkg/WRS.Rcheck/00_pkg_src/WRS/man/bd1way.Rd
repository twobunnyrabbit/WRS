% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{bd1way}
\alias{bd1way}
\title{One-Way Bootstrap Test for Dependent Groups}
\usage{
bd1way(
  x,
  est = tmean,
  nboot = 599,
  alpha = 0.05,
  SEED = TRUE,
  misran = FALSE,
  na.rm = NULL,
  pr = TRUE,
  ...
)
}
\arguments{
\item{x}{Data in list mode or n×J matrix where columns represent repeated measures.}

\item{est}{Location estimator to use. Default is \code{\link{tmean}} (trimmed mean).}

\item{nboot}{Number of bootstrap samples. Default is 599.}

\item{alpha}{Significance level. Default is 0.05.}

\item{SEED}{Logical. If \code{TRUE} (default), sets random seed to 2 for reproducibility.}

\item{misran}{Logical. If \code{FALSE} (default), uses casewise deletion for missing values.
If \code{TRUE}, assumes missing values occur at random and uses all available data.}

\item{na.rm}{Optional logical override for \code{misran}. If specified, \code{!na.rm}
overrides \code{misran}. Default is \code{NULL}.}

\item{pr}{Logical. If \code{TRUE} (default), prints informational messages.}

\item{...}{Additional arguments passed to the estimator function.}
}
\value{
A list with components:
\item{test}{Observed test statistic.}
\item{estimates}{Vector of location estimates for each group.}
\item{p.value}{Bootstrap p-value.}
}
\description{
Tests the hypothesis that J measures of location are equal for dependent
(repeated measures) groups using a percentile bootstrap method.
}
\details{
This function tests \eqn{H_0: \theta_1 = \theta_2 = ... = \theta_J}{H0: θ1 = θ2 = ... = θJ}
for J dependent groups, where \eqn{\theta_j}{θj} is the location measure for occasion j.

\strong{Note}: As of October 2015, the default measure of location changed from
one-step M-estimator to trimmed mean.

The test procedure:
\enumerate{
\item Center data by subtracting location estimates
\item Generate bootstrap samples by resampling subjects (rows)
\item Compute test statistic (variance of location estimates) for each bootstrap sample
\item Compare observed test statistic to bootstrap distribution
}

\strong{Missing Value Handling}:
\itemize{
\item \code{misran=FALSE}: Casewise deletion (complete cases only)
\item \code{misran=TRUE}: Use all data (assumes MAR)
\item \code{na.rm} overrides \code{misran} if specified
}
}
\examples{
# Compare trimmed means for 3 dependent groups
x <- matrix(rnorm(60), ncol=3)
bd1way(x)

}
\seealso{
\code{\link{bd1way1}}, \code{\link{tmean}}, \code{\link{onestep}}
}
\keyword{htest}
\keyword{robust}
