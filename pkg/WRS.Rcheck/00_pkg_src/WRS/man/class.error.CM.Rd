% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classification.R
\name{class.error.CM}
\alias{class.error.CM}
\title{Classification Error Estimation with Unconditional Confusion Matrix}
\usage{
class.error.CM(
  x1 = NULL,
  x2 = NULL,
  train = NULL,
  g = NULL,
  method = "KNN",
  nboot = 100,
  EN = TRUE,
  FAST = TRUE,
  AUC = FALSE,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{x1}{Training data for group 1 (alternative to train/g specification).}

\item{x2}{Training data for group 2 (alternative to train/g specification).}

\item{train}{Training data matrix (alternative to x1/x2 specification).}

\item{g}{Group labels for training data (required if train is provided).}

\item{method}{Character string specifying classification method (default: "KNN").
See \code{\link{CLASS.fun}} for available methods.}

\item{nboot}{Number of bootstrap samples for cross-validation (default: 100).}

\item{EN}{Logical. If TRUE (default), uses equal sample sizes for test data to
address classification bias.}

\item{FAST}{Logical. If TRUE (default), excludes computationally intensive methods.}

\item{AUC}{Logical. If TRUE, computes area under ROC curve (requires ROCR package).
Default is FALSE to avoid potential errors.}

\item{SEED}{Logical. If TRUE (default), sets random seed to 2 for reproducibility.}

\item{...}{Additional arguments passed to the classification method.}
}
\value{
A list containing:
\itemize{
\item \code{C.MAT}: Unconditional confusion matrix (proportions)
\item \code{COUNTS}: Confusion matrix with raw counts
\item \code{AUC}: Area under ROC curve (if AUC=TRUE), otherwise NULL
}
}
\description{
Estimates classification error rates using cross-validation (leave-one-out) and
returns an unconditional confusion matrix.
}
\details{
This function estimates classification error rates using cross-validation with
leave-one-out sampling. It returns an \strong{unconditional} confusion matrix showing
the joint probabilities of true and predicted classes.

The confusion matrix shows:
\itemize{
\item Rows: True class membership
\item Columns: Predicted class membership
}

For a \strong{conditional} confusion matrix (showing probabilities conditional on true
class), use \code{\link{class.error.CP}} instead.
}
\note{
Requires the \code{ROCR} package if AUC=TRUE.
}
\examples{
\dontrun{
# Generate training data
set.seed(123)
x1 <- matrix(rnorm(50), ncol = 2)
x2 <- matrix(rnorm(50, mean = 2), ncol = 2)

# Estimate error with KNN
class.error.CM(x1 = x1, x2 = x2, method = "KNN", nboot = 50)

# With AUC calculation
class.error.CM(x1 = x1, x2 = x2, method = "RF", AUC = TRUE)
}

}
\seealso{
\code{\link{class.error.CP}} for conditional confusion matrix,
\code{\link{class.error.loo}} for leave-one-out error rates,
\code{\link{class.error.com}} for comparing methods, \code{\link{CLASS.fun}}
for classification methods
}
