% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outliers.R
\name{fdepthv2}
\alias{fdepthv2}
\title{Compute Halfspace Depth (Version 2)}
\usage{
fdepthv2(m, pts = NA, plotit = TRUE)
}
\arguments{
\item{m}{Data matrix (n x p).}

\item{pts}{Points for which to compute depth (default: NA, uses \code{m}).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}
}
\value{
Vector of depth values for each point in \code{pts} (or \code{m} if pts=NA).
}
\description{
Computes an approximation of halfspace depth by projecting onto lines
connecting pairs of data points. More accurate than \code{fdepth()} and handles
singular covariance matrices, but slower.
}
\details{
Projects data onto lines connecting each pair of distinct points and computes
univariate depth of projections. The final depth is the minimum across all
projections. Requires O(n^2) space but handles data with singular covariance.

For bivariate data with \code{plotit=TRUE}, creates a scatterplot with the median
depth contour. The deepest point (center) is marked with "+".
}
\seealso{
\code{\link{fdepth}}, \code{\link{depth2}}, \code{\link{prodepth}}
}
