% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{ancovampG.QS}
\alias{ancovampG.QS}
\title{ANCOVA Using Quantile Shift Effect Size (No Control Group)}
\usage{
ancovampG.QS(
  x1,
  y1,
  x2,
  y2,
  fr1 = 1,
  fr2 = 1,
  locfun = median,
  method = "hoch",
  npts = 10,
  nboot = 500,
  TUKEY = FALSE,
  alpha = 0.05,
  pts = NULL,
  SEED = TRUE,
  cov.fun = skip.cov,
  pr = FALSE,
  q = 0.5,
  plotit = FALSE,
  xlab = " X1",
  ylab = "X2 ",
  ...
)
}
\arguments{
\item{x1}{Covariate values for group 1, matrix or vector}

\item{y1}{Response values for group 1}

\item{x2}{Covariate values for group 2, matrix or vector}

\item{y2}{Response values for group 2}

\item{fr1}{Span for group 1 running interval smoother (default: 1)}

\item{fr2}{Span for group 2 running interval smoother (default: 1)}

\item{locfun}{Location estimator function (default: median)}

\item{method}{P-value adjustment method for controlling FWE (default: 'hoch')}

\item{npts}{Number of design points for comparisons (default: 10)}

\item{nboot}{Number of bootstrap samples (default: 500)}

\item{TUKEY}{Logical; if TRUE use Tukey depth, else use projection distance (default: FALSE)}

\item{alpha}{Significance level (default: 0.05)}

\item{pts}{Matrix of design points; if NULL, chosen automatically (default: NULL)}

\item{SEED}{Logical; if TRUE sets random seed for reproducibility (default: TRUE)}

\item{cov.fun}{Covariance function returning covariance matrix in \code{$cov} (default: skip.cov)}

\item{pr}{Logical; print warnings (default: FALSE)}

\item{q}{Quantile for shift function (default: 0.5)}

\item{plotit}{Logical; if TRUE creates scatterplot marking significant design points (default: FALSE)}

\item{xlab}{X-axis label for plot (default: ' X1')}

\item{ylab}{Y-axis label for plot (default: 'X2 ')}

\item{...}{Additional arguments passed to other functions}
}
\value{
A list with components:
\item{points}{Matrix of design points where comparisons were made}
\item{results}{Matrix with columns: n1, n2, p.value, Adj.p.value, Sig, Effect.size, ci.low, ci.hi}
\item{num.sig}{Number of significant comparisons}
}
\description{
Performs nonparametric ANCOVA using quantile shift measure of effect size
for two independent groups without a designated control group. Uses running
interval smoother without parametric assumptions about regression surface.
}
\details{
Generalizes ancovamp() to use quantile shift measure when there is no control group.
When a control group exists, use \code{ancovampG.QSCE()} instead.
Design points are pooled from both groups if not specified.
}
\seealso{
\code{\link{ancovampG.QSCE}}
}
