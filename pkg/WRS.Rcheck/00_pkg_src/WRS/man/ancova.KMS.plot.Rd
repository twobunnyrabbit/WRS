% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{ancova.KMS.plot}
\alias{ancova.KMS.plot}
\title{Plot ANCOVA KMS Effect Size as Function of Covariate}
\usage{
ancova.KMS.plot(
  x1,
  y1,
  x2,
  y2,
  pts = NULL,
  xlab = "X",
  ylab = "Effect Size",
  xout = FALSE,
  outfun = outpro,
  pch = "x",
  line = TRUE
)
}
\arguments{
\item{x1}{Covariate values for group 1}

\item{y1}{Response values for group 1}

\item{x2}{Covariate values for group 2}

\item{y2}{Response values for group 2}

\item{pts}{Optional vector of design points. If NULL, uses unique values from
x1 and x2 within their overlapping range}

\item{xlab}{Label for x-axis (default: 'X')}

\item{ylab}{Label for y-axis (default: 'Effect Size')}

\item{xout}{Logical; if TRUE, remove outliers before analysis (default: FALSE)}

\item{outfun}{Function for detecting outliers (default: outpro)}

\item{pch}{Point character for plotting if line=FALSE (default: 'x')}

\item{line}{Logical; if TRUE, connect points with lines (default: TRUE)}
}
\value{
NULL (creates plot as side effect)
}
\description{
Plot the robust KMS measure of effect size as a function of covariate values.
If no design points are specified, uses all unique covariate values within the
overlapping range of both groups.
}
\note{
Only one covariate is supported.
}
\examples{
# Generate data with varying effect size
set.seed(789)
x1 <- runif(60, 0, 10)
y1 <- 2 + 0.3*x1 + rnorm(60, 0, 1.5)
x2 <- runif(60, 0, 10)
y2 <- 3 + 0.4*x1 + rnorm(60, 0, 2)

# Plot effect size across covariate range
ancova.KMS.plot(x1, y1, x2, y2)

# Use specific design points
ancova.KMS.plot(x1, y1, x2, y2, pts = seq(1, 9, by = 1))

}
