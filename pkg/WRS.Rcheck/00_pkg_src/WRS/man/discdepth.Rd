% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outliers.R
\name{discdepth}
\alias{discdepth}
\title{Depth-Based Classification (Discriminant Analysis)}
\usage{
discdepth(
  train = NULL,
  test = NULL,
  g,
  x1 = NULL,
  x2 = NULL,
  depthfun = prodepth,
  ...
)
}
\arguments{
\item{train}{Training data matrix (optional if \code{x1} and \code{x2} provided).}

\item{test}{Test data matrix to classify.}

\item{x1, x2}{Training data for groups 1 and 2 (alternative to \code{train}/\code{g}).}

\item{depthfun}{Depth function to use (default: prodepth).}

\item{...}{Additional arguments passed to \code{depthfun}.}
}
\value{
Vector of predicted class labels (1 or 2) for each test observation.
}
\description{
Classifies test data into two groups based on data depth. Assigns each test
point to the group where it has greater depth.
}
\details{
For each test point, computes its depth relative to each training group.
Assigns the point to the group where it has higher depth. Can specify data
via \code{train}/\code{g} or directly via \code{x1}/\code{x2}.
}
\seealso{
\code{\link{Depth.class.bag}}, \code{\link{prodepth}}
}
