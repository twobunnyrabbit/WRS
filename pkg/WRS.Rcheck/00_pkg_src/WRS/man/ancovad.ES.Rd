% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{ancovad.ES}
\alias{ancovad.ES}
\title{Dependent Samples ANCOVA Effect Size}
\usage{
ancovad.ES(
  x1,
  y1,
  x2,
  y2,
  pts = NULL,
  plotit = TRUE,
  regfun = tsreg,
  SEED = TRUE,
  OM = "PRO",
  id = NULL,
  QM = TRUE,
  xout = FALSE,
  Nboot = 100,
  outfun = outpro,
  xlab = "X",
  ylab = "Y",
  pch = "."
)
}
\arguments{
\item{x1}{A numeric vector of covariate values at time 1 (or condition 1).}

\item{y1}{A numeric vector of outcome values at time 1.}

\item{x2}{A numeric vector of covariate values at time 2 (or condition 2),
paired with x1.}

\item{y2}{A numeric vector of outcome values at time 2, paired with y1.}

\item{pts}{A numeric vector specifying design points where effect sizes are
computed. If \code{NULL}, three points are determined based on data (see \code{QM} parameter).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{regfun}{Regression function to use (default: \code{tsreg} for Theil-Sen).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{OM}{Outlier detection method (not currently implemented in shown code).
Intended choices: 'PRO' (projection), 'BLP' (regression-based), 'MCD', 'BOX'.}

\item{id}{Column index to ignore when detecting outliers (used with 'DUM' method).}

\item{QM}{Logical. If \code{TRUE} (default) and \code{pts = NULL}, selects three points based
on quantiles. If \code{FALSE}, uses default points from \code{\link{ancova}}.}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{Nboot}{Number of bootstrap samples for estimating covariance (default: 100).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{xlab}{Label for x-axis in plot (default: 'X').}

\item{ylab}{Label for y-axis in plot (default: 'Y').}

\item{pch}{Plotting character (default: '.').}
}
\value{
A matrix with columns:
\describe{
\item{pts}{The design points where effect sizes were computed.}
\item{Effect.size}{The robust effect size at each design point, computed as
\eqn{\sqrt{2} \times (\hat{y}_1 - \hat{y}_2) / \sqrt{n \times SQE}}, where
SQE accounts for variances and covariance of the paired estimates.}
}
}
\description{
Compares two dependent (paired/repeated measures) regression lines using a robust
measure of effect size that accounts for the correlation between paired observations.
Uses bootstrap estimation of the covariance matrix to compute a marginal measure
of dispersion.
}
\details{
This function computes effect sizes for dependent (paired) regression lines.
Unlike independent samples ANCOVA, this accounts for the correlation between
paired observations through bootstrap estimation of the covariance matrix.

The effect size at each design point is:
\deqn{ES = \sqrt{2} \times \frac{\hat{y}_1 - \hat{y}_2}{\sqrt{n(Var_1 + Var_2 - 2Cov)}}}

where variances and covariance are estimated via bootstrap.

If \code{plotit = TRUE}, displays both regression lines using \code{\link{reg2plot}}.
}
\examples{
\dontrun{
# Paired data example
set.seed(123)
n <- 30
x1 <- rnorm(n)
y1 <- 2 + 3*x1 + rnorm(n)
x2 <- x1 + rnorm(n, 0, 0.5)  # Correlated covariates
y2 <- 2.5 + 3*x2 + rnorm(n)  # Paired outcomes

result <- ancovad.ES(x1, y1, x2, y2, Nboot = 500)
print(result)
}
}
\seealso{
\code{\link{ancovad.ESci}} for confidence intervals,
\code{\link{Dancova}} for dependent ANCOVA hypothesis testing
}
