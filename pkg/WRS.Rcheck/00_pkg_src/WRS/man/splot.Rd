% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{splot}
\alias{splot}
\title{Plot Relative Frequency Distribution for Discrete or Continuous Data}
\usage{
splot(
  x,
  op = TRUE,
  VL = FALSE,
  xlab = "X",
  ylab = "Rel. Freq.",
  frame.plot = TRUE,
  plotit = TRUE
)
}
\arguments{
\item{x}{Numeric vector of data values.}

\item{op}{Logical; if \code{TRUE}, draws lines connecting frequencies (default: TRUE).}

\item{VL}{Logical; if \code{TRUE}, draws vertical lines for each value instead
of connected lines (default: FALSE). Only used when \code{op = TRUE}.}

\item{xlab, ylab}{Axis labels (defaults: "X", "Rel. Freq.").}

\item{frame.plot}{Logical; if \code{TRUE}, draws a frame around the plot (default: TRUE).}

\item{plotit}{Logical; if \code{TRUE}, creates the plot (default: TRUE).}
}
\value{
List with components:
\item{obs.values}{Vector of unique observed values in \code{x}.}
\item{n}{Total sample size.}
\item{frequencies}{Count of each unique value.}
\item{rel.freq}{Relative frequency (proportion) of each unique value.}
}
\description{
Creates a frequency plot showing the relative frequency of each unique
value in the data. Useful for visualizing discrete distributions or
grouped continuous data.
}
\details{
For each unique value in \code{x}, the function:
\enumerate{
\item Computes the frequency (count) and relative frequency (proportion)
\item Plots asterisks at each unique value
\item Optionally connects points with lines or vertical bars
}

\strong{Line types} (when \code{op = TRUE}):
\itemize{
\item \code{VL = FALSE}: Connects frequencies with a line (default)
\item \code{VL = TRUE}: Draws vertical lines from 0 to each frequency
}

\strong{No lines} when \code{op = FALSE} - only asterisks are plotted.
}
\examples{
# Discrete data
x <- sample(1:5, 100, replace=TRUE)
splot(x)

# With vertical lines
splot(x, VL=TRUE)

# Return frequency information
result <- splot(x, plotit=FALSE)
print(result$rel.freq)
}
\seealso{
\code{\link{splotg5}} for plotting up to 5 groups simultaneously
}
