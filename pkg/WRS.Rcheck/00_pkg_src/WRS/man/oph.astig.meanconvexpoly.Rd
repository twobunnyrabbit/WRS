% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{oph.astig.meanconvexpoly}
\alias{oph.astig.meanconvexpoly}
\title{Compute Confidence Regions for Bivariate Means in Astigmatism Data}
\usage{
oph.astig.meanconvexpoly(
  m,
  alpha = 0.05,
  plotit = TRUE,
  xlab = "V1",
  ylab = "V2",
  nboot = 1999,
  MC = FALSE,
  SEED = TRUE
)
}
\arguments{
\item{m}{Matrix or data frame with even number of columns (J). First two columns
represent first formula, next two the second formula, etc.}

\item{plotit}{Logical; if TRUE, plot the confidence regions}

\item{xlab}{Label for x-axis (default: 'V1')}

\item{ylab}{Label for y-axis (default: 'V2')}

\item{MC}{Logical; if TRUE, use multicore processing}

\item{SEED}{Logical; if TRUE, set random seed for reproducibility}
}
\value{
List with components:
\itemize{
\item \code{centers} - List of center points (means) for each formula with sample sizes
\item \code{conf.region.points} - List of confidence region boundary points for each formula
\item \code{p.values} - List of p-values from hypothesis tests
}
}
\description{
Computes confidence regions for the mean of bivariate astigmatism prediction error
data. Creates confidence polygons for each formula pair using bootstrap methods.
}
\details{
For each formula (pair of columns), computes a 1-alpha confidence region for the
bivariate mean using bootstrap. The confidence region is represented as a convex
polygon. Optionally plots the regions.
}
\seealso{
\code{\link{meancr.cord.oph}}, \code{\link{oph.astig.datasetconvexpoly.mean}}
}
\keyword{ophthalmology}
