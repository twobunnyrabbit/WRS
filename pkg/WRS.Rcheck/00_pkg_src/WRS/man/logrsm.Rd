% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{logrsm}
\alias{logrsm}
\title{Running-Kernel Smoother for Logistic Regression (Single Predictor)}
\usage{
logrsm(
  x,
  y,
  fr = 1,
  plotit = TRUE,
  pyhat = FALSE,
  xlab = "X",
  ylab = "Y",
  STAND = TRUE,
  xout = FALSE,
  outfun = outpro,
  LP = TRUE,
  ...
)
}
\arguments{
\item{x}{Numeric vector of predictor values.}

\item{y}{Binary response vector (0/1 values).}

\item{fr}{Bandwidth parameter (span in standardized units, default: 1).}

\item{plotit}{Logical. If \code{TRUE}, produces a plot (default: \code{TRUE}).}

\item{pyhat}{Logical. If \code{TRUE}, returns predicted probabilities (default: \code{FALSE}).}

\item{xlab}{Label for x-axis (default: "X").}

\item{ylab}{Label for y-axis (default: "Y").}

\item{STAND}{Logical. If \code{TRUE}, standardizes x using MAD (default: \code{TRUE}).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before smoothing (default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{\link{outpro}}).}

\item{LP}{Logical. If \code{TRUE}, applies LOESS smooth to initial smooth (default: \code{TRUE}).}

\item{...}{Additional arguments passed to \code{outfun}.}
}
\value{
If \code{pyhat = TRUE}, returns vector of predicted probabilities.
If \code{pyhat = FALSE}, returns standardized x values (if \code{plotit = FALSE}).
}
\description{
Performs a nonparametric smooth for binary response data using a running
kernel smoother as described by Hosmer and Lemeshow. For single predictor
only.
}
\details{
The smoother uses a Gaussian kernel with bandwidth \code{fr} to compute
local weighted averages of the binary response. Steps:
\enumerate{
\item Optionally removes outliers
\item Standardizes x: (x - median(x)) / MAD(x)
\item Applies kernel weights: exp(-d²) where d is Euclidean distance
\item Optionally smooths result with LOESS
}

For multiple predictors, use \code{\link{logSM}} instead.
}
\seealso{
\code{\link{logSM}}, \code{\link{logrchk}}, \code{\link{logSMpred}}
}
