% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{hc4qtest}
\alias{hc4qtest}
\title{Test OLS Slope Using HC4 Wild Bootstrap (Quasi-t Test)}
\usage{
hc4qtest(x, y, k, nboot = 500, SEED = TRUE)
}
\arguments{
\item{x}{Predictor matrix (n x p) or vector (for simple regression).}

\item{y}{Response vector (length n).}

\item{k}{Index of the coefficient to test (1 for first predictor, 2 for
second, etc.).}

\item{nboot}{Number of bootstrap samples (default: 500).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed to 2 for reproducibility.}
}
\value{
A list with component:
\item{p.value}{Bootstrap p-value for testing \eqn{H_0: \beta_k = 0}}
}
\description{
Tests the hypothesis that a specific OLS regression slope equals zero using
the HC4 heteroscedasticity-consistent estimator with wild bootstrap and a
quasi-t test statistic.
}
\details{
This function implements a wild bootstrap test for individual regression
coefficients that is robust to heteroscedasticity. The HC4 estimator is
recommended by Cribari-Neto (2004) for small to moderate sample sizes.

\strong{Test Procedure}:
\enumerate{
\item Fit OLS regression and compute HC4 standard error for coefficient k
\item Compute quasi-t statistic: \eqn{t = \beta_k / SE_{HC4}(\beta_k)}
\item Wild bootstrap: resample residuals with random signs
\item For each bootstrap sample, recompute quasi-t statistic
\item P-value is proportion of bootstrap statistics ≥ |observed t|
}

The HC4 variance estimator adjusts for heteroscedasticity and leverage
points more aggressively than HC0, HC1, HC2, or HC3.
}
\examples{
\dontrun{
# Test second coefficient with heteroscedastic errors
x <- matrix(rnorm(100), 50, 2)
y <- x[,1] + 2*x[,2] + rnorm(50, sd = 1 + abs(x[,1]))

# Test if second slope is zero
hc4qtest(x, y, k = 2, nboot = 1000)
}

}
\seealso{
\code{\link{hc4test}}, \code{\link{hc4wtest}}, \code{\link{olshc4}}
}
\keyword{htest}
\keyword{regression}
