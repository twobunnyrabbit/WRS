% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{rm2g.tests}
\alias{rm2g.tests}
\title{Multiple Effect Size Measures for Two Dependent Groups}
\usage{
rm2g.tests(
  x,
  y = NULL,
  tr = 0.2,
  alpha = 0.05,
  REL.MAG = NULL,
  SEED = TRUE,
  nboot = 1000,
  AUTO = FALSE
)
}
\arguments{
\item{x}{Vector of observations for group 1, or difference scores if y is NULL.}

\item{y}{Optional vector of observations for group 2. If NULL, x is treated as
difference scores.}

\item{tr}{Trimming proportion for robust estimators (default: .2).}

\item{alpha}{Nominal alpha level for confidence intervals (default: .05).}

\item{REL.MAG}{Relative magnitude parameter (currently not used).}

\item{SEED}{Logical; if \code{TRUE} (default), set random seed to 2 for reproducibility.}

\item{nboot}{Number of bootstrap samples (default: 1000).}

\item{AUTO}{Logical; if \code{TRUE}, use automatic method selection for binomial CI (default: FALSE).}
}
\value{
A matrix with rows for each effect size measure and columns:
\itemize{
\item \code{Est}: Effect size estimate
\item \code{ci.low}: Lower confidence limit
\item \code{ci.up}: Upper confidence limit
\item \code{p.value}: Two-sided p-value
}
}
\description{
Computes confidence intervals and p-values for six different effect size measures
for two dependent groups based on difference scores.
}
\details{
This function computes six different effect size measures for dependent groups:
\enumerate{
\item \strong{AKP}: Robust standardized difference (analog of Cohen's d)
\item \strong{QS (median)}: Quantile shift based on median of difference scores
\item \strong{QStr}: Quantile shift based on trimmed mean of X-Y distribution
\item \strong{SIGN}: P(X<Y), probability that first observation is less than second
\item \strong{TRIM}: Difference in trimmed means
\item \strong{MEDIAN}: Difference in medians (Harrell-Davis estimator)
}

If y is not NULL, difference scores (x-y) are computed automatically. Otherwise,
x is assumed to contain the difference scores.

Each measure is reported with estimate, confidence interval, and p-value.
}
\seealso{
\code{\link{D.akp.effect.ci}}, \code{\link{depQSci}}, \code{\link{trimci}},
\code{\link{hdpb}}, \code{\link{binom.conf.pv}}
}
\keyword{htest}
