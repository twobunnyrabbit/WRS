% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{ancts}
\alias{ancts}
\title{Theil-Sen ANCOVA for Two Independent Groups}
\usage{
ancts(
  x1,
  y1,
  x2,
  y2,
  pts = NULL,
  Dpts = FALSE,
  Npts = 5,
  regfun = tsreg,
  fr1 = 1,
  fr2 = 1,
  SCAT = TRUE,
  pch1 = "*",
  pch2 = "+",
  alpha = 0.05,
  plotit = TRUE,
  xout = FALSE,
  outfun = out,
  nboot = 100,
  SEED = TRUE,
  xlab = "X",
  ylab = "Y",
  ...
)
}
\arguments{
\item{x1}{A numeric vector of covariate values for group 1.}

\item{y1}{A numeric vector of outcome values for group 1.}

\item{x2}{A numeric vector of covariate values for group 2.}

\item{y2}{A numeric vector of outcome values for group 2.}

\item{pts}{A numeric vector specifying design points where regression lines
are compared. If \code{NULL}, points are chosen automatically (see \code{Dpts} and \code{Npts}).}

\item{Dpts}{Logical. If \code{FALSE} (default), \code{Npts} covariate points are chosen
uniformly spaced between minimum and maximum observed values. If \code{TRUE},
points are chosen using the same strategy as \code{\link{ancova}}.}

\item{Npts}{Number of design points to use when \code{pts = NULL} (default: 5).}

\item{regfun}{Regression function to use (default: \code{tsreg} for Theil-Sen).
Other options include \code{tshdreg} (Theil-Sen with Harrell-Davis),
\code{opreg} (outlier-pruned), etc.}

\item{fr1}{Span for selecting observations near design points in group 1 (default: 1).}

\item{fr2}{Span for selecting observations near design points in group 2 (default: 1).}

\item{SCAT}{Logical. If \code{TRUE} (default), displays scatter plot points.}

\item{pch1}{Plotting character for group 1 points (default: '*').}

\item{pch2}{Plotting character for group 2 points (default: '+').}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{xlab}{Label for x-axis (default: "X").}

\item{ylab}{Label for y-axis (default: "Y").}

\item{...}{Additional arguments passed to the regression function.}
}
\value{
A list with components:
\describe{
\item{n}{Vector of sample sizes for the two groups.}
\item{intercept.slope.group1}{Regression coefficients (intercept, slope) for group 1.}
\item{intercept.slope.group2}{Regression coefficients (intercept, slope) for group 2.}
\item{output}{Matrix with columns: X (design point), Est1 (group 1 estimate),
Est2 (group 2 estimate), DIF (difference), TEST (test statistic), se (standard error),
ci.low (lower CI), ci.hi (upper CI), p.value, p.adjust (adjusted p-value).}
}
}
\description{
Compares regression lines of two independent groups at specified design points
using a robust regression estimator. By default, uses the Theil-Sen estimator,
which is highly resistant to outliers.
}
\details{
The function compares two regression lines using robust regression at each design point.
The Theil-Sen estimator is robust to outliers and does not require normality assumptions.

Standard errors are estimated via bootstrap. Critical values are adjusted for multiple
comparisons using Rom's method when testing at multiple design points.

If \code{plotit = TRUE}, displays both groups' data and fitted regression lines (group 1: solid,
group 2: dashed).
}
\examples{
\dontrun{
# Compare two groups with Theil-Sen regression
set.seed(123)
x1 <- rnorm(30)
y1 <- 2 + 3*x1 + rnorm(30)
x2 <- rnorm(30)
y2 <- 1 + 3*x2 + rnorm(30)

result <- ancts(x1, y1, x2, y2)
print(result$output)
}
}
\seealso{
\code{\link{anctspb}} for percentile bootstrap version,
\code{\link{anctsmp}} for multiple covariates, \code{\link{anctsmcp}} for
multiple groups
}
