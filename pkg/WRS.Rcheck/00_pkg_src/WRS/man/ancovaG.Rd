% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{ancovaG}
\alias{ancovaG}
\title{Generalized ANCOVA with Running Interval Smoother}
\usage{
ancovaG(
  x1,
  y1,
  x2,
  y2,
  fr1 = 1,
  fr2 = 1,
  tr = 0.2,
  alpha = 0.05,
  plotit = FALSE,
  pts = NULL,
  sm = FALSE,
  pr = TRUE,
  xout = FALSE,
  outfun = out,
  test = medpb2,
  ...
)
}
\arguments{
\item{x1}{Covariate values for group 1}

\item{y1}{Response values for group 1}

\item{x2}{Covariate values for group 2}

\item{y2}{Response values for group 2}

\item{fr1}{Span parameter for group 1 smoother (default: 1)}

\item{fr2}{Span parameter for group 2 smoother (default: 1)}

\item{tr}{Trimming proportion (default: 0.2). Proportion of observations to
trim from each tail when computing winsorized correlation.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{plotit}{Logical; if TRUE, plot the running means (default: FALSE)}

\item{pts}{Optional vector of design points where groups are compared. If NULL,
uses 5 equally-spaced points where both groups have at least 12 observations}

\item{sm}{Logical; if TRUE, use bootstrap bagging for smoother curves (default: FALSE)}

\item{pr}{Logical; if TRUE, print results (default: TRUE)}

\item{xout}{Logical; if TRUE, remove outliers before analysis (default: FALSE)}

\item{outfun}{Function for detecting outliers (default: out)}

\item{test}{Hypothesis testing function to use for group comparisons (default: medpb2)}

\item{...}{Additional arguments passed to the test function}
}
\value{
A list with two components:
\item{[\link{1}]}{Matrix with columns X (design point), n1 (sample size group 1),
n2 (sample size group 2)}
\item{[\link{2}]}{List of test results from the specified test function at each design point}
}
\description{
Compare two independent groups using a generalized ANCOVA approach that allows
any hypothesis testing method. Uses running interval smoothers without making
parametric assumptions about the regression line forms. Groups are compared at
specified design points along the covariate range.
}
\examples{
# Generate example data with covariate effect
set.seed(123)
x1 <- runif(50, 0, 10)
y1 <- 2 + 0.5*x1 + rnorm(50)
x2 <- runif(50, 0, 10)
y2 <- 3 + 0.5*x2 + rnorm(50)  # Different intercept

# Compare groups using default median-based test
result <- ancovaG(x1, y1, x2, y2)

# Use custom design points and trimmed mean test
result2 <- ancovaG(x1, y1, x2, y2, pts = c(2, 5, 8), test = yuen)

}
