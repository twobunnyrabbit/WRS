% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{bi2ac}
\alias{bi2ac}
\title{Compare Two Independent Binomial Proportions Using Agresti-Coull Method}
\usage{
bi2ac(
  r1 = sum(elimna(x)),
  n1 = length(elimna(x)),
  r2 = sum(elimna(y)),
  n2 = length(elimna(y)),
  x = NA,
  y = NA,
  alpha = 0.05,
  null.value = 0
)
}
\arguments{
\item{r1}{Number of successes in group 1. Default computes from \code{x}.}

\item{n1}{Sample size for group 1. Default computes from \code{x}.}

\item{r2}{Number of successes in group 2. Default computes from \code{y}.}

\item{n2}{Sample size for group 2. Default computes from \code{y}.}

\item{x}{Vector of 0s and 1s for group 1. Default is \code{NA}.}

\item{y}{Vector of 0s and 1s for group 2. Default is \code{NA}.}

\item{alpha}{Significance level. Default is 0.05.}

\item{null.value}{Null hypothesis value for the difference in proportions.
Default is 0 (no difference).}
}
\value{
List with components:
\itemize{
\item \code{n1}, \code{n2} - Sample sizes after removing missing values
\item \code{phat1}, \code{phat2} - Estimated proportions
\item \code{dif} - Difference in proportions (phat1 - phat2)
\item \code{ci} - Confidence interval for the difference
\item \code{test.stat} - z-test statistic
\item \code{p.value} - Two-sided p-value
}
}
\description{
Compares two independent binomial proportions using the Agresti-Coull method
for constructing confidence intervals and conducting hypothesis tests.
}
\details{
This function compares two binomial proportions using the Agresti-Coull
confidence interval method. It:
\enumerate{
\item Computes Agresti-Coull CI for each proportion separately via \code{\link{acbinomci}}
\item Combines the standard errors to construct a CI for the difference
\item Performs a z-test for the difference against \code{null.value}
}

The Agresti-Coull method generally has better coverage properties than the
standard Wald method for binomial proportions.

Missing values are automatically removed.
}
\references{
Agresti, A. & Coull, B. A. (1998). Approximate is better than "exact" for
interval estimation of binomial proportions. \emph{The American Statistician},
52, 119-126.
}
\seealso{
\code{\link{acbinomci}}, \code{\link{binom2g}}, \code{\link{binom2g.ZHZ}},
\code{\link{binomECP}}
}
\keyword{htest}
