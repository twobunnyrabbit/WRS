% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{tbs}
\alias{tbs}
\title{Rocke's Trimmed Biweight S-Estimator}
\usage{
tbs(x, eps = 0.001, maxiter = 20, r = 0.45, alpha = 0.05, init.est = OGK)
}
\arguments{
\item{x}{Numeric vector or matrix. Data for which to compute robust estimates}

\item{eps}{Numeric. Convergence tolerance (default: 1e-3)}

\item{maxiter}{Integer. Maximum number of iterations (default: 20)}

\item{r}{Numeric. Breakdown point (default: 0.45)}

\item{alpha}{Numeric. Asymptotic rejection probability (default: 0.05)}

\item{init.est}{Function. Initial estimator (default: \code{OGK})}
}
\value{
List with components:
\item{center}{Robust location estimate (scalar for p=1, vector for p>1)}
\item{var}{Robust variance estimate (for p=1)}
\item{cov}{Robust covariance matrix (for p>1)}
}
\description{
Computes Rocke's constrained S-estimator for robust location and scatter
estimation with high breakdown point.
}
\details{
This function implements Rocke's constrained S-estimator using biweight functions.
The estimator has high breakdown point (default 45\\%) and is designed to be
robust to outliers while maintaining efficiency under normality.

The algorithm:
\enumerate{
\item Computes initial estimates using the specified \code{init.est} function
\item Iteratively refines estimates using weighted biweight functions
\item Stops when convergence criterion is met or maximum iterations reached
}

For univariate data (p=1), the function returns location and variance.
For multivariate data (p>1), returns location vector and covariance matrix.
}
\examples{
# Univariate data
x <- c(rnorm(20), 100, 200)  # Data with outliers
result <- tbs(x)

# Bivariate data
x <- cbind(rnorm(50), rnorm(50))
result <- tbs(x)

}
\references{
Rocke, D. M. (1996). Robustness properties of S-estimators of multivariate
location and shape in high dimension. Annals of Statistics, 24, 1327-1345.
}
\seealso{
\code{\link{OGK}}, \code{\link{tauloc}}, \code{\link{cov.mve}}
}
\keyword{multivariate}
\keyword{robust}
