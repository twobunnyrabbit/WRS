% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{ltsR}
\alias{ltsR}
\title{Least Trimmed Squares Regression with Explanatory Power}
\usage{
ltsR(x, y, RES = FALSE, varfun = pbvar, corfun = pbcor)
}
\arguments{
\item{x}{Matrix or data frame of predictor variables.}

\item{y}{Vector of response values.}

\item{RES}{Logical. If \code{TRUE}, return residuals. If \code{FALSE} (default),
residuals are not returned.}

\item{varfun}{Function to compute robust variance (default: \code{\link{pbvar}}).}

\item{corfun}{Function to compute robust correlation (default: \code{\link{pbcor}}).}
}
\value{
A list with components:
\item{coef}{LTS coefficient estimates (intercept + slopes)}
\item{residuals}{Residuals (if \code{RES=TRUE}), otherwise \code{NULL}}
\item{Explanatory.Power}{Proportion of variance explained (robust R²)}
\item{Strength.Assoc}{Square root of explanatory power (robust R)}
}
\description{
Performs Least Trimmed Squares (LTS) robust regression and computes explanatory
power based on robust variance or correlation. Provides measure of association
strength between predictors and response.
}
\details{
The function computes explanatory power as:
\deqn{EP = var(yhat) / var(y)}
using robust variance estimators.

If EP ≥ 1 (can occur with robust methods), it uses robust correlation instead:
\deqn{EP = cor(yhat, y)^2}

Missing values are removed before analysis.
}
\references{
Rousseeuw, P.J. and van Driessen, K. (2006). Computing LTS regression for
large data sets. Data Mining and Knowledge Discovery, 12, 29-45.
}
\seealso{
\code{\link[robustbase]{ltsReg}}
}
\keyword{regression}
\keyword{robust}
