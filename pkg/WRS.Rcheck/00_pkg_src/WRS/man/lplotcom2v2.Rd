% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{lplotcom2v2}
\alias{lplotcom2v2}
\title{Compare Predictor Importance with LOESS (Method 2)}
\usage{
lplotcom2v2(
  x,
  y,
  xout = FALSE,
  pts1 = NULL,
  pts2 = NULL,
  outfun = outpro,
  span = 2/3,
  npts = 10,
  tr = 0.2,
  ...
)
}
\arguments{
\item{x}{Matrix with 2 columns (two predictor variables).}

\item{y}{Response variable vector.}

\item{xout}{Logical; if TRUE, removes multivariate outliers from predictors (default: FALSE).}

\item{pts1}{Points at which to evaluate predictor 1 (default: computed from data).}

\item{pts2}{Points at which to evaluate predictor 2 (default: computed from data).}

\item{outfun}{Outlier detection function (default: \code{outpro}).}

\item{span}{Span parameter for LOESS (default: 2/3).}

\item{npts}{Number of evaluation points if pts1/pts2 not specified (default: 10).}

\item{tr}{Trimming proportion for Winsorized SD (default: 0.2).}

\item{...}{Additional arguments passed to outlier detection function.}
}
\value{
A list with components:
\item{str1}{Strength of predictor 1 at each point in pts2 (conditional SD).}
\item{str2}{Strength of predictor 2 at each point in pts1 (conditional SD).}
\item{p}{Proportion of pairwise comparisons where str1 > str2.}
\item{mean.str1}{Mean strength for predictor 1.}
\item{mean.str2}{Mean strength for predictor 2.}
}
\description{
Alternative version of \code{\link{lplotcom2}} for comparing predictor importance using LOESS.
Uses absolute conditional SD instead of ratio to overall SD.
}
\details{
This function is similar to \code{\link{lplotcom2}} but uses \strong{absolute conditional SD}
instead of the ratio to overall SD of y.

For each predictor:
\enumerate{
\item Hold the other predictor fixed at several values
\item Compute LOESS predictions varying only the focal predictor
\item Measure the Winsorized SD of these predictions (not normalized)
}

\strong{Strength} = Winsorized SD of conditional predictions

This version may be more interpretable when comparing the absolute amount of variation
explained by each predictor, rather than the proportion of total variation.

Higher strength indicates the predictor produces more variability in predictions when
the other predictor is held fixed.
}
\examples{
# Compare importance using absolute SD
x1 <- rnorm(100)
x2 <- rnorm(100)
y <- 2*x1 + 0.5*x2 + rnorm(100)
X <- cbind(x1, x2)
result <- lplotcom2v2(X, y, npts = 5)
result$mean.str1  # Absolute SD for x1
result$mean.str2  # Absolute SD for x2
}
\seealso{
\code{\link{lplotcom2}}, \code{\link{lplotcomBCI}}, \code{\link{lplot}}
}
