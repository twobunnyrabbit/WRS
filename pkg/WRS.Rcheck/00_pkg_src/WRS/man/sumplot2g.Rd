% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{sumplot2g}
\alias{sumplot2g}
\title{Summary Plot Panel for Two-Group Comparison}
\usage{
sumplot2g(
  x,
  y = NULL,
  xlab = "X",
  ylab = "",
  eblabx = "Groups",
  eblaby = "",
  nse = 2
)
}
\arguments{
\item{x}{Data for group 1 (vector), or a matrix/list containing both groups.}

\item{y}{Data for group 2 (vector, optional if x is a matrix/list, default: NULL).}

\item{xlab}{Label for X-axis in density plot (default: "X").}

\item{ylab}{Label for Y-axis in density plot (default: "").}

\item{eblabx}{Label for X-axis in error bar plot (default: "Groups").}

\item{eblaby}{Label for Y-axis in error bar plot (default: "").}

\item{nse}{Number of standard errors for error bars (default: 2).}
}
\value{
None. Creates a 2×2 panel plot.
}
\description{
Creates a 2×2 panel of four diagnostic plots for comparing two independent groups:
error bar plot, boxplots, kernel density estimates, and shift function.
}
\details{
This function creates a comprehensive visual summary for two-group comparisons
by displaying four complementary plots in a 2×2 layout:

\strong{1. Error bar plot} (top-left): Shows means and standard error bars for each group.
Uses \code{ebarplot} with nse standard errors.

\strong{2. Boxplots} (top-right): Standard boxplots showing medians, quartiles, and outliers.

\strong{3. Density estimates} (bottom-left): Overlaid kernel density estimates using
\code{g2plot} with adaptive kernel estimator.

\strong{4. Shift function} (bottom-right): Shows how quantiles differ between groups
using \code{sband} (shift band plot).

\strong{Input formats}:
\itemize{
\item \strong{Two vectors}: Provide x and y separately.
\item \strong{Matrix}: Provide x as a matrix with 2 columns (y should be NULL).
\item \strong{List}: Provide x as a list with 2 elements (y should be NULL).
}

After plotting, the graphics parameters are reset to single-plot mode.
}
\examples{
# Compare two groups
set.seed(123)
x <- rnorm(100, mean = 0, sd = 1)
y <- rnorm(100, mean = 0.5, sd = 1.2)
sumplot2g(x, y)

# Using a matrix
data_mat <- cbind(x, y)
sumplot2g(data_mat)
}
\seealso{
\code{\link{ebarplot}}, \code{\link{g2plot}}, \code{\link{sband}}
}
