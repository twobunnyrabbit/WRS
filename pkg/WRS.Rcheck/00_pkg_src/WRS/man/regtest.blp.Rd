% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{regtest.blp}
\alias{regtest.blp}
\title{Test Regression Coefficients with Bad Leverage Point Removal}
\usage{
regtest.blp(
  x,
  y,
  regfun = tsreg,
  nboot = 600,
  alpha = 0.05,
  plotit = TRUE,
  grp = c(1:ncol(x)),
  nullvec = c(rep(0, length(grp))),
  SEED = TRUE,
  pr = TRUE,
  ...
)
}
\arguments{
\item{x}{Predictor matrix (n × p).}

\item{y}{Response vector.}

\item{regfun}{Regression function (default: \code{tsreg} for Theil-Sen).}

\item{nboot}{Number of bootstrap samples (default: 600).}

\item{alpha}{Significance level (default: 0.05).}

\item{plotit}{Logical; if \code{TRUE} and testing 2 coefficients, plots the
bootstrap distribution and confidence ellipsoid (default: \code{TRUE}).}

\item{grp}{Indices of coefficients to test (default: all predictors,
\code{1:ncol(x)}). The intercept is automatically excluded.}

\item{nullvec}{Null hypothesis values for coefficients being tested
(default: zeros, \code{rep(0, length(grp))}).}

\item{SEED}{Logical; if \code{TRUE}, sets random seed for reproducibility
(default: \code{TRUE}).}

\item{pr}{Logical; currently not used.}

\item{...}{Additional arguments passed to \code{regfun} and \code{reglev.gen}.}
}
\value{
A list with components:
\item{test}{Mahalanobis distance test statistic}
\item{crit}{Bootstrap critical value at level \code{1 - alpha}}
\item{p.value}{Bootstrap p-value}
\item{nullvec}{Null hypothesis values tested}
\item{est}{Estimated coefficients for predictors in \code{grp}}
\item{n}{Sample size after removing bad leverage points}
}
\description{
Tests hypotheses about regression coefficients after removing bad leverage
points using \code{\link{reglev.gen}}. Uses bootstrap-based confidence
ellipsoid with Mahalanobis distance.
}
\details{
This function:
\enumerate{
\item Identifies and removes bad leverage points via \code{\link{reglev.gen}}
\item Generates bootstrap samples from cleaned data
\item Computes bootstrap distribution of specified coefficients
\item Tests whether \code{nullvec} falls inside confidence ellipsoid
}

The test statistic is the Mahalanobis distance from the estimate to
\code{nullvec}, using the bootstrap covariance matrix. The critical value
is the \code{(1-alpha)} quantile of bootstrap Mahalanobis distances.

When testing 2 coefficients with \code{plotit=TRUE}, a plot shows the
bootstrap estimates, the null value (square), and the confidence region.
}
\seealso{
\code{\link{reglev.gen}}, \code{\link{reglev.est}}, \code{\link{regtest}}, \code{\link{tsreg}}
}
\keyword{htest}
\keyword{regression}
