% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{Scheffe}
\alias{Scheffe}
\title{Scheffe's Multiple Comparison Procedure}
\usage{
Scheffe(x, con = 0, alpha = 0.05, WARN = TRUE)
}
\arguments{
\item{x}{Data in list mode, matrix, or data frame format. For lists, each element
represents an independent group. For matrices/data frames, each column represents
a group.}

\item{con}{Matrix of contrast coefficients with J rows (groups) and d columns
(contrasts). If \code{con=0} (default), all pairwise comparisons are performed.}

\item{alpha}{Significance level (default: 0.05)}

\item{WARN}{Logical indicating whether to print warning about using \code{lincon}
instead (default: TRUE)}
}
\value{
A list with components:
\item{n}{Vector of sample sizes for each group}
\item{test}{Matrix containing test statistics, critical values, and standard
errors for each comparison or contrast}
\item{psihat}{Matrix containing contrast estimates, confidence intervals
(lower, upper), and p-values}
}
\description{
Performs Scheffe's method for multiple comparisons with simultaneous confidence
intervals, controlling familywise error rate. Supports both pairwise comparisons
and custom contrasts.
}
\details{
Scheffe's method is a conservative multiple comparison procedure that controls
the familywise error rate when comparing means across groups. The procedure
uses the F-distribution to compute simultaneous confidence intervals.

The function performs a one-way ANOVA and then computes either:
\itemize{
\item All pairwise comparisons if \code{con=0}
\item Custom linear contrasts if \code{con} is specified as a matrix
}

Missing values are automatically removed from all groups.

Note: The function suggests using \code{\link{lincon}} instead, which may offer
more flexible contrast-based testing.
}
\seealso{
\code{\link{lincon}}, \code{\link{anova1}}
}
\keyword{htest}
