% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression.R
\name{opregMC}
\alias{opregMC}
\title{Outlier-Pruned Regression (Parallel Processing)}
\usage{
opregMC(
  x,
  y,
  regfun = tsreg,
  cop = 3,
  fast = FALSE,
  pr = TRUE,
  prres = FALSE,
  STAND = TRUE,
  xout = FALSE
)
}
\arguments{
\item{x}{Numeric vector or matrix of predictor variable(s).}

\item{y}{Numeric vector of the dependent variable.}

\item{regfun}{Regression function to use (default: \code{tsreg}).
Can be any function that returns coefficients in \code{$coef}.}

\item{cop}{Critical outlier detection parameter passed to \code{outproMC}.
Larger values make detection less sensitive (default: 3).}

\item{fast}{Logical. Currently not used (default: \code{FALSE}).}

\item{pr}{Logical. If \code{TRUE} and \code{fast=TRUE}, prints coefficients
(default: \code{TRUE}).}

\item{prres}{Logical. If \code{TRUE} and \code{fast=TRUE}, prints residuals
(default: \code{FALSE}).}

\item{STAND}{Logical. If \code{TRUE}, standardizes data before outlier
detection (default: \code{TRUE}).}

\item{xout}{Logical. Included for compatibility but not used; outlier
detection is always performed (default: \code{FALSE}).}
}
\value{
A list with components:
\describe{
\item{coef}{Vector of regression coefficients (intercept, slopes)}
\item{residuals}{Vector of residuals for all observations (including outliers)}
}
}
\description{
Performs regression after removing outliers detected by the projection-based
outlier detection method \code{\link{outproMC}}. Uses parallel processing for
outlier detection.
}
\details{
This function first uses \code{\link{outproMC}} (projection-based outlier
detection with multicore processing) to identify outliers in the joint space
of predictors and response. Then it fits the regression using only the
non-outlier points.

The residuals are computed for all observations, including those flagged as
outliers, so you can see how well the robust fit predicts the outlying cases.

\strong{Outlier Detection}: Uses projection pursuit methods to detect outliers
in high-dimensional space. The \code{cop} parameter controls sensitivity:
\itemize{
\item Larger \code{cop}: fewer points flagged as outliers (more conservative)
\item Smaller \code{cop}: more points flagged as outliers (more aggressive)
\item Default (3): reasonable balance for most applications
}

\strong{When to use}:
\itemize{
\item High-dimensional regression with potential outliers
\item When you want automatic outlier removal
\item As a more robust alternative to \code{\link{opreg}}
}

This is the parallel processing version, faster than \code{\link{opreg}}
for large datasets.
}
\examples{
\dontrun{
# Regression with outliers
set.seed(123)
x <- matrix(rnorm(200), ncol=2)
y <- 1 + 2*x[,1] - x[,2] + rnorm(100)

# Add some outliers
y[1:5] <- y[1:5] + 10
x[1:5,] <- x[1:5,] + 3

# Fit with automatic outlier removal
fit <- opregMC(x, y)
print(fit$coef)

# Compare with regular tsreg (influenced by outliers)
fit.regular <- tsreg(x, y)
print(fit.regular$coef)
}

}
\references{
Wilcox, R.R. (2022). \emph{Introduction to Robust Estimation and Hypothesis Testing}
(5th ed.). Academic Press.
}
\seealso{
\code{\link{opreg}} for non-parallel version,
\code{\link{outproMC}} for the outlier detection method,
\code{\link{opregpb}} for bootstrap inference
}
