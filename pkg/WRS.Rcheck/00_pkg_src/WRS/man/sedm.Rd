% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{sedm}
\alias{sedm}
\title{Standard Error of Difference Between Quantiles (Dependent Groups)}
\usage{
sedm(x, y = NA, q = 0.5)
}
\arguments{
\item{x}{Numeric vector for first variable, or a 2-column matrix where first
column is x and second is y}

\item{y}{Numeric vector for second variable (default: NA, extracted from \code{x}
if \code{x} is a matrix)}

\item{q}{Quantile of interest (default: 0.5 for median). Determines m as
floor(q*n + 0.5)}
}
\value{
Numeric value: the estimated standard error of the difference between
the mth order statistics
}
\description{
Estimates the standard error of the difference between mth order statistics
(quantiles) for two dependent groups using an adaptive kernel method.
}
\details{
For dependent variables X and Y, this function estimates the standard error
of D = X_m - Y_m, where m is the mth order statistic corresponding to quantile
q. The estimation uses an adaptive kernel density method.

The function computes joint probabilities for various combinations of X and Y
relative to their quantiles, then uses kernel density estimates at the quantile
points to calculate the standard error.

This function is primarily used internally by \code{\link{qdtest}} for
quantile comparison tests with dependent groups.
}
\seealso{
\code{\link{qdtest}}, \code{\link{akerd}}
}
\keyword{htest}
