% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression.R
\name{corregci.sub}
\alias{corregci.sub}
\title{Bootstrap Helper for Correlations Between Predictors and Response}
\usage{
corregci.sub(isub, x, y, corfun)
}
\arguments{
\item{isub}{Integer vector of bootstrap sample indices.}

\item{x}{A numeric matrix of predictor variables (n by p).}

\item{y}{A numeric vector of the dependent variable (length n).}

\item{corfun}{Correlation function to use (e.g., \code{pbcor}, \code{wincor}).
Must return a list with component \code{$cor}.}
}
\value{
A numeric vector of length p containing the correlation between each
predictor column and y for the bootstrap sample specified by \code{isub}.
}
\description{
Internal helper function used by correlation-based regression functions to
compute correlations for a bootstrap sample. Not intended for direct use.
}
\details{
This is an internal utility function used in bootstrap procedures for
correlation-based regression methods. It:
\enumerate{
\item Extracts the bootstrap sample from x and y using indices in \code{isub}
\item Computes the correlation between each predictor and the response
\item Returns the vector of correlations
}

The function is typically called via \code{apply()} or similar over a matrix
of bootstrap sample indices.

\strong{Note}: This is an internal function not meant for end users. It is
documented for completeness and to support package development.
}
\keyword{internal}
