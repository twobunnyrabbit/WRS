% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{rm2mcp}
\alias{rm2mcp}
\title{Two-Way Within-Subjects Multiple Comparisons}
\usage{
rm2mcp(
  J,
  K,
  x,
  est = tmean,
  alpha = 0.05,
  grp = NA,
  dif = TRUE,
  nboot = NA,
  plotit = FALSE,
  BA = FALSE,
  hoch = FALSE,
  ...
)
}
\arguments{
\item{J}{Integer. Number of levels for Factor A.}

\item{K}{Integer. Number of levels for Factor B.}

\item{x}{Data in matrix/data.frame format (columns = groups) or list format.
Groups ordered as level (1,1), (1,2), ..., (J,K).}

\item{est}{Location estimator function (default: \code{\link{tmean}}).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{grp}{Optional vector to reorder groups. If specified, indicates which
columns correspond to each factor level combination.}

\item{dif}{Logical. If \code{TRUE}, use difference scores (default: \code{TRUE}).}

\item{nboot}{Number of bootstrap samples. If \code{NA}, chosen automatically.}

\item{plotit}{Logical. If \code{TRUE}, create plots (default: \code{FALSE}).}

\item{BA}{Logical. Passed to \code{\link{rmmcppb}} (default: \code{FALSE}).}

\item{hoch}{Logical. If \code{TRUE}, use Hochberg adjustment (default: \code{FALSE}).}

\item{...}{Additional arguments passed to \code{est}.}
}
\value{
A list with components:
\describe{
\item{\code{Factor.A}}{Results for Factor A main effects}
\item{\code{Factor.B}}{Results for Factor B main effects}
\item{\code{Factor.AB}}{Results for A×B interaction}
}
}
\description{
Performs multiple comparisons for dependent groups in a within-by-within
(repeated measures) two-way design. Tests main effects and interactions
using percentile bootstrap.
}
\details{
This function:
\enumerate{
\item Creates contrast matrices for main effects (A, B) and interaction (AB)
\item Calls \code{\link{rmmcppb}} for each set of contrasts
\item Returns separate results for Factor A, Factor B, and A×B interaction
}

Missing values are automatically removed.
}
\seealso{
\code{\link{rmmcppb}}, \code{\link{con2way}}, \code{\link{bwmcp}}
}
\keyword{htest}
\keyword{multivariate}
