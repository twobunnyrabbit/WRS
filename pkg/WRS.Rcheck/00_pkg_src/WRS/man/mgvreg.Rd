% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression-advanced.R
\name{mgvreg}
\alias{mgvreg}
\title{Regression with MGV Outlier Detection}
\usage{
mgvreg(
  x,
  y,
  regfun = tsreg,
  cov.fun = rmba,
  se = TRUE,
  varfun = pbvar,
  corfun = pbcor,
  SEED = TRUE
)
}
\arguments{
\item{x}{A numeric matrix or vector containing predictor values.}

\item{y}{A numeric vector containing the response variable.}

\item{regfun}{Regression function to use after outlier removal (default: \code{tsreg}).}

\item{cov.fun}{Covariance function for MGV outlier detection (default: \code{rmba}).
Determines the robust center used by \code{outmgv()}.}

\item{se}{Logical. Currently not used (kept for compatibility).}

\item{varfun}{Variance function for computing explanatory power (default: \code{pbvar}).}

\item{corfun}{Correlation function for computing explanatory power when variance
ratio >= 1 (default: \code{pbcor}).}

\item{SEED}{Logical. If \code{TRUE}, sets seed for reproducibility of \code{outmgv()}
results (default: \code{TRUE}).}
}
\value{
A list with components:
\item{coef}{Vector of regression coefficients (intercept and slopes).}
\item{residuals}{Vector of residuals from the regression on cleaned data.}
\item{Strength.Assoc}{Square root of explanatory power (similar to multiple R).}
\item{Explanatory.Power}{Proportion of variance in Y explained by predictors,
analogous to R-squared but using robust variance estimates.}
}
\description{
Performs regression on data after removing outliers identified by the MGV
(minimum generalized variance) method. Computes regression coefficients,
residuals, and strength of association.
}
\details{
This function combines robust outlier detection with regression:

\enumerate{
\item Uses \code{outmgv()} to identify outliers in the (X,Y) space using the
minimum generalized variance method
\item Removes outliers from the data
\item Fits regression using \code{regfun} (default: Theil-Sen) on cleaned data
\item Computes explanatory power as var(fitted)/var(Y) using robust estimators
}

The MGV method detects outliers based on robust covariance estimation (specified
by \code{cov.fun}). The default \code{rmba} uses the median ball algorithm.

Explanatory power is computed robustly:
\itemize{
\item If var(fitted)/var(Y) < 1: use this ratio directly
\item If ratio >= 1: use squared correlation between fitted and observed
}
}
\examples{
\dontrun{
# Regression with MGV outlier detection
set.seed(123)
x <- matrix(rnorm(100), ncol=2)
y <- 2 + 3*x[,1] - x[,2] + rnorm(50)
# Add outliers
y[1:3] <- y[1:3] + 10
result <- mgvreg(x, y)
result$coef
result$Explanatory.Power
}
}
\references{
Wilcox, R.R. (2022). Introduction to Robust Estimation and Hypothesis Testing
(5th ed.). Academic Press.
}
\seealso{
\code{\link{outmgv}}, \code{\link{tsreg}}, \code{\link{rmba}},
\code{\link{mgvfreg}}
}
