% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{lplotCI}
\alias{lplotCI}
\title{LOESS Confidence Band with Simultaneous Coverage}
\usage{
lplotCI(
  x,
  y,
  plotit = TRUE,
  xlab = "X",
  ylab = "Y",
  p.crit = NULL,
  alpha = 0.05,
  span = NULL,
  CIV = FALSE,
  xout = FALSE,
  outfun = outpro,
  pch = ".",
  SEED = TRUE,
  nboot = 100,
  pts = NULL,
  npts = 25,
  nreps = 2000,
  ...
)
}
\arguments{
\item{x}{Predictor variable (vector).}

\item{y}{Response variable (vector).}

\item{plotit}{Logical; if TRUE, creates the plot (default: TRUE).}

\item{xlab}{Label for X-axis (default: 'X').}

\item{ylab}{Label for Y-axis (default: 'Y').}

\item{p.crit}{Critical p-value for simultaneous confidence band (default: NULL, auto-determined).}

\item{alpha}{Familywise Type I error rate (default: 0.05).}

\item{span}{Span parameter for LOESS (default: NULL, auto-selected based on n).}

\item{CIV}{Logical; if TRUE, returns confidence interval values instead of plotting (default: FALSE).}

\item{xout}{Logical; if TRUE, removes outliers based on X values (default: FALSE).}

\item{outfun}{Outlier detection function (default: \code{outpro}).}

\item{pch}{Plotting character for points (default: '.').}

\item{SEED}{Logical; if TRUE, sets random seed for reproducibility (default: TRUE).}

\item{nboot}{Number of bootstrap samples for SE estimation (default: 100).}

\item{pts}{Points at which to compute confidence intervals (default: NULL, auto-selected).}

\item{npts}{Number of points for confidence band if pts=NULL (default: 25).}

\item{nreps}{Number of replications for computing p.crit if needed (default: 2000).}

\item{...}{Additional arguments passed to outfun.}
}
\value{
A list with components:
\item{p.crit}{Adjusted critical p-value used for simultaneous coverage.}
\item{Conf.Intervals}{Matrix with columns (X, Y.hat, ci.low, ci.up) if CIV=TRUE, else NULL.}
}
\description{
Creates a LOESS regression plot with simultaneous confidence band that accounts
for heteroscedasticity. The confidence intervals are adjusted so that the overall
probability coverage is approximately 1-alpha across all points.
}
\details{
This function fits a LOESS curve and constructs a simultaneous confidence band
that maintains approximately 1-alpha coverage probability across all points.

\strong{Span selection}: If span=NULL, it is chosen based on sample size:
\itemize{
\item n < 300: span = 2/3
\item 300 ≤ n < 800: span = 0.5
\item n ≥ 800: span = 0.3
}

\strong{Critical value}: For alpha=0.05 and n ≤ 2000, p.crit is determined quickly
using stored simulation results. Otherwise, it is computed via simulation
(which may take some time).

The confidence band is computed at npts points ranging from median(x) - 1.5\emph{MAD(x)
to median(x) + 1.5}MAD(x), or at user-specified pts.

Standard errors are estimated via bootstrap, and the confidence intervals
account for heteroscedasticity.
}
\examples{
# LOESS with simultaneous confidence band
set.seed(123)
x <- rnorm(100)
y <- x + 0.3 * x^2 + rnorm(100, sd = 0.5)
lplotCI(x, y, span = 0.6)

# Return confidence intervals
result <- lplotCI(x, y, CIV = TRUE, plotit = FALSE)
head(result$Conf.Intervals)
}
\seealso{
\code{\link{lplot}}, \code{\link{lplotv2}}, \code{\link{rplotCI}}
}
