% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{smeanMC}
\alias{smeanMC}
\title{Multivariate Skipped Mean Using Parallel Processing}
\usage{
smeanMC(m, cop = 6, MM = FALSE, op = 1, outfun = outogk, cov.fun = rmba, ...)
}
\arguments{
\item{m}{Numeric matrix (n×p) containing multivariate data.}

\item{cop}{Integer (1-6). Method for determining center for projection:
\itemize{
\item 1: Donoho-Gasko median
\item 2: MCD (Minimum Covariance Determinant)
\item 3: Marginal medians
\item 4: MVE (Minimum Volume Ellipsoid) center
\item 5: TBS
\item 6: rmba (Olive's median ball algorithm, default)
}}

\item{MM}{Logical. If \code{FALSE} (default), use boxplot rule for outlier detection;
if \code{TRUE}, use MAD and median-based rule.}

\item{op}{Integer (1-3). Outlier detection method:
\itemize{
\item 1: Projection method via \code{\link{outproMC}} (default)
\item 2: MGV method via \code{\link{outmgv}}
\item 3: Custom method via \code{outfun}
}}

\item{outfun}{Function for outlier detection when \code{op=3} (default: \code{\link{outogk}}).}

\item{cov.fun}{Covariance function for \code{op=2} (default: \code{\link{rmba}}).}

\item{...}{Additional arguments passed to outlier detection functions.}
}
\value{
Numeric vector of length p containing skipped multivariate means.
}
\description{
Computes a multivariate measure of location by eliminating outliers via
projection methods and then computing means of remaining observations.
Uses multicore processing for improved performance.
}
\details{
The function:
\enumerate{
\item Removes missing values from input matrix
\item Identifies outliers using specified method
\item Computes column-wise means using only non-outlier observations
}

For \code{op=1} (projection method): Projects all points onto lines between
each point and the center, detecting outliers on each projection. A point is
flagged as an outlier if it's an outlier in any projection.

Uses multicore processing via \code{mclapply} when \code{op=1}.
}
\seealso{
\code{\link{outproMC}}, \code{\link{outmgv}}, \code{\link{outogk}},
\code{\link{smean}} for non-parallel version
}
\keyword{multivariate}
\keyword{robust}
