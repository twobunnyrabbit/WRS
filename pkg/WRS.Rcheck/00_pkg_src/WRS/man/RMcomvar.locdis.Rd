% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{RMcomvar.locdis}
\alias{RMcomvar.locdis}
\title{Compare Variation in Tails for Dependent Groups After Centering}
\usage{
RMcomvar.locdis(
  x,
  y,
  loc.fun = median,
  CI = FALSE,
  plotit = TRUE,
  xlab = "First Group",
  ylab = "Est.1 - Est.2",
  ylabQCOM = "Est.2 - Est.1",
  sm = TRUE,
  QCOM = TRUE,
  q = c(0.1, 0.25, 0.75, 0.9),
  MC = FALSE,
  nboot = 2000,
  PR = TRUE,
  ...
)
}
\arguments{
\item{x}{Observations for group 1.}

\item{y}{Observations for group 2.}

\item{loc.fun}{Location function for centering (default: \code{median}).}

\item{CI}{Logical. If \code{TRUE}, computes confidence intervals (default: \code{FALSE}).}

\item{plotit}{Logical. If \code{TRUE}, creates plot (default: \code{TRUE}).}

\item{xlab}{X-axis label (default: 'First Group').}

\item{ylab}{Y-axis label (default: 'Est.1 - Est.2').}

\item{ylabQCOM}{Y-axis label for QCOM (default: 'Est.2 - Est.1').}

\item{sm}{Logical. If \code{TRUE}, applies smoothing (default: \code{TRUE}).}

\item{QCOM}{Logical. If \code{TRUE}, uses quantile comparison method (default: \code{TRUE}).}

\item{q}{Quantiles to compare (default: c(0.1, 0.25, 0.75, 0.9)).}

\item{MC}{Logical. If \code{TRUE}, uses parallel processing (default: \code{FALSE}).}

\item{nboot}{Number of bootstrap samples (default: 2000).}

\item{PR}{Logical. If \code{TRUE}, prints interpretation guide (default: \code{TRUE}).}

\item{...}{Additional arguments passed to location function.}
}
\value{
Results comparing tail variation between groups.
}
\description{
Compares the marginal distributions of two dependent groups by examining
variation in the tails after centering the data using a location measure.
}
\details{
Centers both groups using the specified location function, then compares
the distributions focusing on tail behavior. Useful for detecting differences
in variability patterns after removing location differences.
}
\examples{
\dontrun{
# Compare tail variation in paired data
set.seed(123)
x <- rt(50, df = 5)
y <- rt(50, df = 3) # Heavier tails
RMcomvar.locdis(x, y)
}
}
\seealso{
\code{\link{Dqcomhd}}, \code{\link{lband}}
}
