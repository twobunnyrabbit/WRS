% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{anclog}
\alias{anclog}
\title{ANCOVA for Logistic Regression (Binary Outcomes)}
\usage{
anclog(
  x1,
  y1,
  x2,
  y2,
  fr1 = 1,
  fr2 = 1,
  alpha = 0.05,
  pts = NULL,
  SEED = TRUE,
  DH = FALSE,
  FRAC = 0.5,
  cov.fun = skip.cov,
  pr = FALSE,
  q = 0.5,
  plotit = FALSE,
  pv = FALSE,
  theta = 50,
  xlab = " ",
  ylab = " ",
  SCAT = FALSE,
  zlab = " ",
  ...
)
}
\arguments{
\item{x1}{Matrix of covariate values for group 1 (can have multiple columns)}

\item{y1}{Response values for group 1}

\item{x2}{Matrix of covariate values for group 2}

\item{y2}{Response values for group 2}

\item{fr1}{Span parameter for group 1 smoother (default: 1)}

\item{fr2}{Span parameter for group 2 smoother (default: 1)}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{pts}{Optional matrix of design points. If NULL, uses points with deepest
depth in x1 (controlled by DH and FRAC)}

\item{SEED}{Logical; if TRUE, set seed for reproducibility with bootstrap/MVE (default: TRUE)}

\item{DH}{Logical; if TRUE, use only deepest (1-FRAC) design points (default: FALSE)}

\item{FRAC}{Fraction of deepest points to use when DH=TRUE (default: .5)}

\item{cov.fun}{Covariance function for depth computation (default: skip.cov)}

\item{pr}{Logical; if TRUE, print progress (default: FALSE)}

\item{q}{Quantile for qcomhd tests (default: .5 for median)}

\item{plotit}{Logical; if TRUE and 2 covariates, create 3D plot of p-values (default: FALSE)}

\item{theta}{Rotation angle for 3D plot (default: 50)}

\item{xlab}{Label for first covariate (default: ' X1')}

\item{ylab}{Label for second covariate (default: 'X2 ')}

\item{SCAT}{Logical; not currently used (default: FALSE)}

\item{zlab}{Label for z-axis in 3D plot (default: 'p.value ')}

\item{...}{Additional arguments passed to test function}
}
\value{
List with components: points (design points), results (test results), num.sig (number of significant tests).
}
\description{
Wrapper function for ANCOVA with binary (logistic) outcomes using ancovampG with twobinom test.
}
\keyword{ancova}
\keyword{logistic}
