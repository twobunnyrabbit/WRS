% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/power.R
\name{power.chisq.test}
\alias{power.chisq.test}
\title{Power Analysis for Chi-Square Test}
\usage{
power.chisq.test(w = NULL, N = NULL, df = NULL, sig.level = 0.05, power = NULL)
}
\arguments{
\item{w}{Effect size (Cohen's w). \code{NULL} if solving for effect size.}

\item{N}{Total sample size. \code{NULL} if solving for sample size.}

\item{df}{Degrees of freedom: \code{(r - 1) * (c - 1)} for an r × c contingency table.
\code{NULL} if solving for df.}

\item{sig.level}{Significance level (Type I error probability) (default: 0.05).
\code{NULL} if solving for significance level.}

\item{power}{Statistical power (1 - Type II error probability). \code{NULL} if
solving for power.}
}
\value{
An object of class \code{power.htest} containing the computed parameters.
}
\description{
Computes power, sample size, effect size, or significance level for a chi-square
test of association. This is a wrapper for \code{pwr.chisq.test} from the pwr package.
}
\details{
This function requires the \code{pwr} package. Exactly one of \code{w}, \code{N},
\code{df}, \code{sig.level}, and \code{power} must be \code{NULL}; that parameter
will be computed from the others.

Cohen's w effect size conventions:
\itemize{
\item Small: w = 0.1
\item Medium: w = 0.3
\item Large: w = 0.5
}
}
\examples{
# Power for a 2x3 table with w=0.3, N=100, alpha=0.05
power.chisq.test(w = 0.3, N = 100, df = (2-1)*(3-1))

# Sample size needed for power=0.80
power.chisq.test(w = 0.3, power = 0.80, df = 2, sig.level = 0.05)
}
\references{
Cohen, J. (1988). Statistical power analysis for the behavioral sciences (2nd ed.).
Hillsdale, NJ: Lawrence Erlbaum.

Wilcox, R.R. (2022). Introduction to Robust Estimation and Hypothesis Testing (5th ed.).
}
\seealso{
\code{\link[pwr]{pwr.chisq.test}}
}
