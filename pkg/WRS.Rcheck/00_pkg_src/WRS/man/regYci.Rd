% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression-advanced.R
\name{regYci}
\alias{regYci}
\title{Confidence Intervals for Predicted Y Values in Regression}
\usage{
regYci(
  x,
  y,
  regfun = tsreg,
  pts = unique(x),
  nboot = 100,
  ADJ = FALSE,
  xout = FALSE,
  outfun = out,
  SEED = TRUE,
  alpha = 0.05,
  crit = NULL,
  null.value = 0,
  plotPV = FALSE,
  scale = TRUE,
  span = 0.75,
  xlab = "X",
  xlab1 = "X1",
  xlab2 = "X2",
  ylab = "p-values",
  zlab = "p-values",
  theta = 50,
  phi = 25,
  MC = FALSE,
  nreps = 1000,
  SM = FALSE,
  pch = "*",
  ...
)
}
\arguments{
\item{x}{A numeric vector or matrix containing predictor values (can be
multivariate).}

\item{y}{A numeric vector containing the response variable.}

\item{regfun}{Regression function to use (default: \code{tsreg} for Theil-Sen).
Can be any regression function that returns coefficients.}

\item{pts}{Points at which to compute confidence intervals. Default is
\code{unique(x)} for all unique x values.}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{ADJ}{Logical. If \code{TRUE}, adjusts critical values so that the simultaneous
probability coverage is 1-alpha (i.e., creates a confidence band). If \code{FALSE},
computes pointwise confidence intervals (default: \code{FALSE}).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{crit}{Optional user-specified critical value. If \code{NULL}, computed from
bootstrap (default: \code{NULL}).}

\item{null.value}{Null hypothesis value for testing (default: 0 for most
tests, indicating no difference or no correlation).}

\item{plotPV}{Logical. If \code{TRUE}, plots p-values vs. predictor values
(default: \code{FALSE}).}

\item{scale, span, theta, phi, pch}{Parameters for plotting.}

\item{xlab, xlab1, xlab2, ylab, zlab}{Axis labels for plots.}

\item{MC}{Logical. If \code{TRUE}, uses parallel processing via \code{mclapply}
(default: \code{FALSE}).}

\item{nreps}{Number of replications for computing critical value when ADJ=TRUE
(default: 1000). Higher values give more accurate critical values.}

\item{SM}{Logical. If \code{TRUE}, applies smoothing to p-value plot (default: \code{FALSE}).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
A matrix with columns:
\itemize{
\item Predicted Y value at each point
\item Lower confidence limit
\item Upper confidence limit
\item p-value for testing H0: predicted Y = null.value
}
}
\description{
Computes bootstrap confidence intervals for predicted Y values at specified
predictor values, with optional adjustment for simultaneous inference across
multiple points (confidence band).
}
\details{
This function uses bootstrap methods to construct confidence intervals for
the predicted response at specified predictor values:
\itemize{
\item With \code{ADJ = FALSE}: Pointwise confidence intervals (each with coverage 1-alpha)
\item With \code{ADJ = TRUE}: Simultaneous confidence band (overall coverage 1-alpha)
}

The adjustment for \code{ADJ=TRUE} has been primarily studied for single predictors.
Performance with multiple predictors is less well understood.

For single predictors with \code{regfun=tsreg}, \code{ols}, or \code{qreg} and \code{alpha=0.05},
the adjustment can be computed quickly. Otherwise, \code{nreps} bootstrap samples
are used to estimate the critical value, which can be time-consuming. Use
\code{MC=TRUE} with multiple cores to reduce computation time.
}
\examples{
\dontrun{
# Confidence intervals for predicted Y values
set.seed(123)
x <- rnorm(50)
y <- 2*x + rnorm(50)
# Pointwise CIs
regYci(x, y, pts = c(-1, 0, 1))
# Simultaneous confidence band
regYci(x, y, pts = seq(-2, 2, by=0.5), ADJ = TRUE)
}
}
\references{
Wilcox, R.R. (2022). Introduction to Robust Estimation and Hypothesis Testing
(5th ed.). Academic Press.
}
\seealso{
\code{\link{regYband}}, \code{\link{regYci.sum}}, \code{\link{regci}},
\code{\link{tsreg}}
}
