% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression-advanced.R
\name{reg.break}
\alias{reg.break}
\title{Estimate Regression Breakpoint (Piecewise Linear Regression)}
\usage{
reg.break(
  x,
  y,
  int = NULL,
  xout = FALSE,
  TEST = FALSE,
  regfun = tsreg,
  outfun = outpro,
  varfun = pbvar,
  qv = 0.2,
  npts = 25,
  ...
)
}
\arguments{
\item{x}{A numeric vector containing the predictor variable (currently limited
to single predictor).}

\item{y}{A numeric vector containing the response variable.}

\item{int}{Numeric vector specifying the search interval for the breakpoint
(candidate x-values to evaluate). If \code{NULL}, uses quantiles from \code{qv} to \code{1-qv}
divided into \code{npts} evenly spaced points.}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{TEST}{Logical. Currently not used (reserved for future extension).}

\item{regfun}{Regression function to use for fitting segments (default: \code{tsreg}
for Theil-Sen).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{varfun}{Variance function used to select optimal breakpoint (default: \code{pbvar}
for percentage bend variance). The breakpoint minimizing pooled variance is selected.}

\item{qv}{Quantile value for defining default search interval (default: 0.2).
Search interval is  of x.}

\item{npts}{Number of candidate breakpoints to evaluate when \code{int=NULL} (default: 25).}

\item{...}{Additional arguments passed to \code{outfun} when \code{xout=TRUE}.}
}
\value{
A list with components:
\item{n}{Sample size after removing missing values (and outliers if \code{xout=TRUE}).}
\item{break.est}{Estimated breakpoint location (x-value where slope changes).}
\item{coef.est}{2×2 matrix of regression coefficients:
\itemize{
\item Row 1 (Lower): Intercept and slope for x ≤ breakpoint
\item Row 2 (Upper): Intercept and slope for x > breakpoint
}}
}
\description{
Detects and estimates the breakpoint in a piecewise linear regression
relationship where the slope changes abruptly. Uses a robust analog of the
Jones and Molitoris (1984) method.
}
\details{
This function implements robust piecewise linear regression:
\enumerate{
\item For each candidate breakpoint value:
\itemize{
\item Fits regression to points below breakpoint
\item Adjusts points above breakpoint to connect smoothly
\item Fits regression to adjusted upper points
\item Computes pooled variance of all residuals
}
\item Selects breakpoint minimizing pooled variance
\item Re-fits final model segments at optimal breakpoint
}

The method is a robust version of Jones & Molitoris (1984), using:
\itemize{
\item Robust regression (\code{regfun}) instead of OLS
\item Robust variance (\code{varfun}) for model selection
\item Optional outlier removal (\code{xout=TRUE})
}

\strong{Search interval control}:
\itemize{
\item Default (\code{int=NULL}): Search between \code{qv} and \code{1-qv} quantiles of x
\item Custom: Provide vector of candidate x-values via \code{int}
\item Adjust \code{qv} and \code{npts} for finer/coarser search
}
}
\note{
Currently restricted to single predictor (p=1). The argument \code{qv} must
be less than 0.5.
}
\examples{
\dontrun{
# Piecewise linear data with breakpoint at x=0
set.seed(123)
x <- seq(-2, 2, length.out=100)
y <- ifelse(x < 0, 2 - 3*x, 2 + x) + rnorm(100, sd=0.3)

# Estimate breakpoint
result <- reg.break(x, y)
result$break.est     # Should be near 0
result$coef.est      # Slopes for each segment

# Visualize
plot(x, y)
bp <- result$break.est
abline(a=result$coef.est[1,1], b=result$coef.est[1,2], col="red")  # Lower
segments(bp, result$coef.est[1,1] + result$coef.est[1,2]*bp,
         max(x), result$coef.est[2,1] + result$coef.est[2,2]*max(x),
         col="blue")  # Upper
abline(v=bp, lty=2)  # Breakpoint
}
}
\references{
Jones, B.E. & Molitoris, B.A. (1984). A Statistical Method for Determining
the Breakpoint of Two Lines. Analytical Biochemistry, 141, 287-290.

Wilcox, R.R. (2022). Introduction to Robust Estimation and Hypothesis Testing
(5th ed.). Academic Press.
}
\seealso{
\code{\link{tsreg}}, \code{\link{pbvar}}, \code{\link{outpro}}
}
