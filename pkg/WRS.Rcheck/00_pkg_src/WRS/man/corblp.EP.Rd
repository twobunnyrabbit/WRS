% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{corblp.EP}
\alias{corblp.EP}
\title{Correlation Based on Robust Regression with Bad Leverage Point Removal}
\usage{
corblp.EP(x, y, regfun = tsreg, varfun = pbvar, plotit = FALSE, ...)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{regfun}{Robust regression function (default: \code{tsreg} for Theil-Sen).}

\item{varfun}{Variance function for explanatory power (default: \code{pbvar} for
percentage bend variance).}

\item{plotit}{Logical. Create diagnostic plot (default: \code{FALSE}).}

\item{...}{Additional arguments passed to regression functions.}
}
\value{
List with components:
\item{cor}{Correlation estimate (signed square root of R-squared).}
\item{Rsq}{R-squared (explanatory power).}
}
\description{
Computes correlation as explanatory power from robust regression after
removing bad leverage points.
}
\details{
Computes correlation based on explanatory power from robust regression:
\enumerate{
\item Removes bad leverage points using \code{reglev.gen()}
\item Fits robust regression on cleaned data
\item Computes R-squared as ratio of residual variance to response variance
\item Returns signed sqrt(R-squared) as correlation
}

Sign is determined by the slope coefficient. Only supports single predictor.
}
\examples{
\dontrun{
set.seed(123)
x <- rnorm(50)
y <- 2 * x + rnorm(50)
corblp.EP(x, y)
}
}
\seealso{
\code{\link{corblp.ci}}, \code{\link{corblppb}}, \code{\link{tsreg}}, \code{\link{pbvar}}
}
