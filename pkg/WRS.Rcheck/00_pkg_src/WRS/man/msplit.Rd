% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{msplit}
\alias{msplit}
\title{Split-Plot J×K ANOVA Using Medians}
\usage{
msplit(J, K, data, grp = c(1:p), p = J * K, q = 0.5)
}
\arguments{
\item{J}{Number of levels for first factor (between-subjects)}

\item{K}{Number of levels for second factor (within-subjects/repeated)}

\item{data}{List or matrix containing data. For lists: data[\link{1}] = level (1,1),
data[\link{2}] = level (1,2), ..., data[\link{K}] = level (1,K), data[\link{K+1}] = level (2,1), etc.}

\item{grp}{Vector specifying subset of groups to analyze (default: c(1:p))}

\item{p}{Total number of groups = J×K (default: J*K)}

\item{q}{Quantile to use (default: 0.5 for median)}
}
\value{
A list with components for main effects and interaction tests,
each containing test statistics and p-values
}
\description{
Performs a J×K ANOVA using medians (or other quantiles) for split-plot designs
with repeated measures on the second factor. First factor has independent groups,
second factor is within-subjects.
}
\details{
Uses Johansen's method for testing:
\itemize{
\item Factor A (between-subjects) main effect
\item Factor B (within-subjects) main effect
\item A×B interaction
}

Covariance matrix estimated robustly using \code{covmmed}.
}
\seealso{
\code{\link{qest}}, \code{\link{covmmed}}, \code{\link{johansp}}
}
