% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{m1way}
\alias{m1way}
\title{One-Way Test for Equality of Location Measures via Percentile Bootstrap}
\usage{
m1way(x, est = hd, nboot = 599, SEED = TRUE, ...)
}
\arguments{
\item{x}{Data in list mode or matrix. If list: \code{x[[1]]} is group 1, \code{x[[2]]}
is group 2, etc. If matrix: groups stored in columns.}

\item{est}{Function to compute location measure (default: \code{\link{hd}},
Harrell-Davis median estimator). Can use \code{median}, \code{mean}, \code{tmean}, etc.}

\item{nboot}{Integer. Number of bootstrap samples (default: 599).}

\item{SEED}{Logical. If \code{TRUE} (default), set random seed for reproducibility.}

\item{...}{Additional arguments passed to \code{est} function.}
}
\value{
A list with components:
\item{test}{Test statistic (weighted sum of squared deviations)}
\item{crit.value}{Critical value from bootstrap distribution}
\item{p.value}{Bootstrap p-value}
\item{est}{Location estimates for each group}
}
\description{
Tests the hypothesis that J independent groups have equal measures of location
using the percentile bootstrap method. Default compares medians (via Harrell-Davis
estimator) but can compare other location measures.
}
\details{
The test uses a bootstrap analog of the one-way ANOVA F-statistic:
\deqn{T = \sum_j n_j (\hat{\theta}_j - \bar{\theta})^2}

where \eqn{\hat{\theta}_j} is the location estimate for group j.

Bootstrap samples are drawn from centered distributions (each observation
minus group estimate) to impose the null hypothesis.
}
\references{
Wilcox, R.R. (2017). Introduction to Robust Estimation and Hypothesis Testing,
4th Ed. Academic Press.
}
\seealso{
\code{\link{t1way}}, \code{\link{med1way}}, \code{\link{hd}}
}
\keyword{htest}
\keyword{robust}
