% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{TKmeans.grp}
\alias{TKmeans.grp}
\title{Group Data Using Trimmed K-Means Cluster Assignments}
\usage{
TKmeans.grp(x, k, y)
}
\arguments{
\item{x}{Numeric matrix. Data used for clustering (predictors)}

\item{k}{Integer. Number of clusters/groups}

\item{y}{Numeric matrix. Data to be grouped according to cluster assignments}
}
\value{
List of length k, where each element contains the rows of \code{y}
corresponding to observations assigned to that cluster
}
\description{
Creates k groups using trimmed k-means clustering on predictor data X,
then sorts response data Y into these same groups.
}
\details{
This function:
\enumerate{
\item Performs trimmed k-means clustering on \code{x}
\item Obtains cluster assignments for each observation
\item Splits the data in \code{y} into k groups based on these assignments
}

Useful when you want to group response variables according to patterns
in predictor variables, with robustness to outliers.
}
\examples{
# Create groups based on predictor patterns
x <- rbind(
  matrix(rnorm(50, mean = 0), ncol = 2),
  matrix(rnorm(50, mean = 3), ncol = 2)
)
y <- matrix(rnorm(100), ncol = 1)
groups <- TKmeans.grp(x, k = 2, y)

}
\seealso{
\code{\link{TKmeans}}, \code{\link{Kmeans}}
}
\keyword{cluster}
