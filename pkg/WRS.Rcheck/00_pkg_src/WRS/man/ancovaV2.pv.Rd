% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{ancovaV2.pv}
\alias{ancovaV2.pv}
\title{Determine Critical P-Value for ancovaV2}
\usage{
ancovaV2.pv(
  n1,
  n2,
  nreps = 2000,
  MC = FALSE,
  qpts = FALSE,
  qvals = c(0.25, 0.5, 0.75),
  nboot = 500,
  SEED = TRUE,
  est = tmean,
  alpha = 0.05
)
}
\arguments{
\item{n1}{Sample size for group 1}

\item{n2}{Sample size for group 2}

\item{nreps}{Number of simulation iterations (default: 2000)}

\item{MC}{Logical; if TRUE, use parallel processing via mclapply (default: FALSE)}

\item{qpts}{Logical; if TRUE, use quantile-based design points (default: FALSE)}

\item{qvals}{Vector of quantiles to use if qpts=TRUE (default: c(0.25, 0.5, 0.75))}

\item{nboot}{Number of bootstrap samples per iteration (default: 500)}

\item{SEED}{Logical; if TRUE, set random seed for reproducibility (default: TRUE)}

\item{est}{Estimator function to use (default: tmean)}

\item{alpha}{Significance level for determining critical value (default: 0.05)}
}
\value{
List with p.crit, the critical p-value for family-wise error control
}
\description{
Helper function to determine the critical p-value for the ancovaV2 function when
testing at specified design points. Generates data from null distribution (normal)
and computes minimum p-value across multiple iterations to establish the critical
value for controlling family-wise error rate.
}
\keyword{internal}
