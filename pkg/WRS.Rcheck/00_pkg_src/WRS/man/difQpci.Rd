% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{difQpci}
\alias{difQpci}
\title{Test Quantile Symmetry with FWE Control (Difference Quantile Plot with CI)}
\usage{
difQpci(
  x,
  y = NULL,
  q = seq(5, 40, 5)/100,
  xlab = "Quantile",
  ylab = "Group 1 minus Group 2",
  plotit = TRUE,
  alpha = 0.05,
  nboot = 1000,
  SEED = TRUE,
  LINE = FALSE
)
}
\arguments{
\item{x}{Numeric vector or matrix. If vector, tests symmetry of \code{x}. If 2-column matrix,
analyzes differences \code{x[,1] - x[,2]}. If \code{y} is provided, analyzes \code{x - y}.}

\item{y}{Numeric vector or NULL. If provided, differences \code{x - y} are analyzed (default: NULL).}

\item{q}{Numeric vector. Quantiles to test (default: seq(0.05, 0.40, 0.05)).
All values must be < 0.5.}

\item{xlab}{Character. X-axis label (default: "Quantile").}

\item{ylab}{Character. Y-axis label (default: "Group 1 minus Group 2").}

\item{plotit}{Logical. If TRUE, produces plot (default: TRUE).}

\item{alpha}{Numeric. Familywise Type I error rate (default: 0.05).}

\item{nboot}{Integer. Number of bootstrap samples (default: 1000).}

\item{SEED}{Logical. If TRUE, sets random seed to 2 for reproducibility (default: TRUE).}

\item{LINE}{Logical. If TRUE, connects estimates with lines in plot (default: FALSE).}
}
\value{
List with components:
\describe{
\item{n}{Sample size}
\item{output}{Data frame with columns:
\itemize{
\item \code{quantile}: Quantile value tested
\item \code{Est_q}: Estimate of q-th quantile
\item \code{Est_1.minus.q}: Estimate of (1-q)-th quantile
\item \code{SUM}: Sum of q and 1-q quantiles
\item \code{ci.low}: Lower confidence limit for sum
\item \code{ci.up}: Upper confidence limit for sum
\item \code{p_crit}: Hochberg-adjusted critical p-value
\item \code{p-value}: Bootstrap p-value for H0: sum = 0
\item \code{signif}: "YES" if significant after FWE control, "NO" otherwise
}}
}
}
\description{
Tests symmetry of difference scores around zero by comparing complementary quantiles
(q and 1-q) with familywise error (FWE) control via Hochberg's method. Provides a
visual and inferential assessment of distributional symmetry.
}
\details{
For each quantile q, the function:
\enumerate{
\item Computes the sum \eqn{\hat{q} + \hat{1-q}} using Harrell-Davis estimator
\item Constructs bootstrap confidence interval for the sum
\item Tests H0: q + (1-q) = 0 (symmetry)
\item Controls familywise error rate across all quantiles using Hochberg's method
}

\strong{Interpretation}:
\itemize{
\item Symmetric distribution → sums near zero, approximately horizontal plot
\item Asymmetric distribution → sums deviate from zero, non-horizontal pattern
\item If median ≠ 0, horizontal line is shifted vertically from zero
}

The plot displays:
\itemize{
\item \code{*}: Point estimates of q + (1-q) quantile sums
\item \code{+}: Confidence interval endpoints
}

Hochberg-adjusted p-values control the familywise error rate, allowing valid
simultaneous inference across all tested quantiles.
}
\examples{
# Test symmetry of difference scores
set.seed(123)
x <- rnorm(50)
y <- rnorm(50, mean = 0.3)
result <- difQpci(x, y, plotit = FALSE)
result$output
}
\seealso{
\code{\link{Dqdif}}, \code{\link{hd}}, \code{\link{qhd}}
}
\keyword{htest}
\keyword{robust}
\keyword{univar}
