% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classification.R
\name{KNNdist}
\alias{KNNdist}
\title{K-Nearest Neighbors Classification Using Data Depths}
\usage{
KNNdist(
  train = NULL,
  test = NULL,
  g,
  k = 3,
  x1 = NULL,
  x2 = NULL,
  prob = FALSE,
  plotit = FALSE,
  SEED = NULL,
  xlab = "Group 1",
  ylab = "Group 2",
  depthfun = prodepth,
  ...
)
}
\arguments{
\item{train}{Training data matrix or data frame (optional if x1 and x2 are provided).}

\item{test}{Test data matrix or data frame containing observations to classify.}

\item{g}{Group labels for training data (required if train is provided). Should be
a vector with two unique values indicating the two groups.}

\item{k}{Number of nearest neighbors to use (default: 3).}

\item{x1}{Training data for group 1 (alternative to train/g specification).}

\item{x2}{Training data for group 2 (alternative to train/g specification).}

\item{prob}{Logical. If TRUE, returns class probabilities instead of class labels
(default: FALSE).}

\item{plotit}{Logical. If TRUE, creates a DD-plot (depth-depth plot) showing the
transformation (default: FALSE).}

\item{SEED}{Seed value for reproducibility. If NULL, no seed is set. Included for
compatibility when called by other functions.}

\item{xlab}{Label for x-axis in DD-plot (default: 'Group 1').}

\item{ylab}{Label for y-axis in DD-plot (default: 'Group 2').}

\item{depthfun}{Function for computing data depth (default: \code{prodepth} for
projection depth). Must have the form \code{depthfun(x, pts)} where it computes
depth for points in \code{pts} relative to points in \code{x}.}

\item{...}{Additional arguments passed to the depth function.}
}
\value{
A numeric vector of predicted class labels for the test data (or class
probabilities if \code{prob=TRUE}). The labels correspond to the unique values in
the training group labels.
}
\description{
Performs binary classification using the K-nearest neighbors (KNN) method applied
to data depths. Points are transformed to their depth in each group (DD-plot) and
KNN is applied using the resulting depth coordinates.
}
\details{
This function implements the DD-classifier (Depth-Depth classifier) approach:
\enumerate{
\item For each point, compute its depth in group 1
\item For each point, compute its depth in group 2
\item Transform each point to 2D coordinates (depth1, depth2)
\item Apply standard KNN classification in this depth space
}

This transformation can improve classification performance by reducing the
dimensionality to 2D (regardless of original dimension) and making the decision
boundary more linear in the transformed space.

The training data can be specified in two ways:
\enumerate{
\item Using \code{train} and \code{g}: Provide all training data in \code{train} with group labels in \code{g}
\item Using \code{x1} and \code{x2}: Provide the two groups separately
}

Missing values are automatically removed from data.

Common depth functions include:
\itemize{
\item \code{prodepth}: Projection depth (default, robust)
\item \code{zonoid}: Zonoid depth
\item Other depth functions following the interface \code{depthfun(x, pts)}
}
}
\note{
This function requires the \code{class} package to be installed.
}
\examples{
\dontrun{
# Generate training data
set.seed(123)
x1 <- matrix(rnorm(50), ncol = 2)
x2 <- matrix(rnorm(50, mean = 2), ncol = 2)

# Generate test data
test <- matrix(rnorm(20, mean = 1), ncol = 2)

# KNN classification using depths
KNNdist(x1 = x1, x2 = x2, test = test, k = 5)

# With DD-plot visualization
KNNdist(x1 = x1, x2 = x2, test = test, plotit = TRUE)

# Using train/g specification
train <- rbind(x1, x2)
g <- c(rep(1, nrow(x1)), rep(2, nrow(x2)))
KNNdist(train = train, g = g, test = test, k = 5)
}

}
\references{
Li, J., Cuesta-Albertos, J. A., & Liu, R. Y. (2012). DD-classifier: Nonparametric
classification procedure based on DD-plot. Journal of the American Statistical
Association, 107(498), 737-753.
}
\seealso{
\code{\link{KNN}} for standard KNN, \code{\link{Depth.class}} for
depth-based classification, \code{\link{CLASS.fun}} for general classification
dispatcher
}
