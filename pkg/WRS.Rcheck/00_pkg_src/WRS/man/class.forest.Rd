% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classification.R
\name{class.forest}
\alias{class.forest}
\title{Random Forest Classification}
\usage{
class.forest(
  train = NULL,
  test = NULL,
  g = NULL,
  x1 = NULL,
  x2 = NULL,
  sm = FALSE,
  fr = 2,
  SEED = NULL
)
}
\arguments{
\item{train}{Training data matrix or data frame (optional if x1 and x2 are provided).}

\item{test}{Test data matrix or data frame containing observations to classify.}

\item{g}{Group labels for training data (required if train is provided). Should be
a vector with two unique values indicating the two groups.}

\item{x1}{Training data for group 1 (alternative to train/g specification).}

\item{x2}{Training data for group 2 (alternative to train/g specification).}

\item{sm}{Logical. Currently not used (included for consistency with other functions).}

\item{fr}{Numeric. Currently not used (included for consistency with other functions).}

\item{SEED}{Seed value for reproducibility. If NULL, no seed is set. Included for
compatibility when called by other functions.}
}
\value{
A numeric vector of predicted class labels (1 or 2) for the test data.
}
\description{
Performs binary classification using random forest via the randomForest package.
}
\details{
Random forests are ensemble learning methods that construct multiple decision trees
during training and output the class that is the mode of the classes from individual trees.

The training data can be specified in two ways:
\enumerate{
\item Using \code{train} and \code{g}: Provide all training data in \code{train} with group labels in \code{g}
\item Using \code{x1} and \code{x2}: Provide the two groups separately
}

Missing values are automatically removed from training and test data.
}
\note{
This function requires the \code{randomForest} package to be installed.
}
\examples{
\dontrun{
# Generate training data
set.seed(123)
x1 <- matrix(rnorm(50), ncol = 2)
x2 <- matrix(rnorm(50, mean = 2), ncol = 2)

# Generate test data
test <- matrix(rnorm(20, mean = 1), ncol = 2)

# Random forest classification
class.forest(x1 = x1, x2 = x2, test = test)

# Using train/g specification
train <- rbind(x1, x2)
g <- c(rep(1, nrow(x1)), rep(2, nrow(x2)))
class.forest(train = train, g = g, test = test)
}

}
\seealso{
\code{\link{CLASS.fun}} for general classification dispatcher,
\code{\link{CLASS.BAG}} for bagged random forest, \code{\link{class.ada}}
for AdaBoost
}
