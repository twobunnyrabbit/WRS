% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{ancdes}
\alias{ancdes}
\title{Choose Design Points for ANCOVA Analysis}
\usage{
ancdes(x, depfun = fdepth, DH = FALSE, FRAC = 0.5, ...)
}
\arguments{
\item{x}{Matrix or data frame of covariate values (n by p matrix).}

\item{depfun}{Depth function to use (default: fdepth for functional data depth).}

\item{DH}{Logical. If FALSE, returns deepest point and 0.5 depth contour points.
If TRUE, returns FRAC deepest points.}

\item{FRAC}{Fraction between 0 and 1. When DH=TRUE, returns the 1-FRAC deepest points.
For example, FRAC=0.2 returns the deepest 80\% of points.}

\item{...}{Additional arguments passed to depfun.}
}
\value{
Matrix of selected design points with same number of columns as x.
}
\description{
Selects design points for ANCOVA analysis based on data depth. Can either return
the deepest point plus points on the 0.5 depth contour, or return a specified
fraction of the deepest points.
}
\note{
Useful for selecting covariate values where group comparisons should be made.
}
\examples{
\dontrun{
x <- matrix(rnorm(100), ncol=2)
# Get deepest point and 0.5 contour
pts1 <- ancdes(x, DH=FALSE)
# Get deepest 80\% of points
pts2 <- ancdes(x, DH=TRUE, FRAC=0.2)
}
}
