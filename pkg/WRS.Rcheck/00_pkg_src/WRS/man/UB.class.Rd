% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{UB.class}
\alias{UB.class}
\title{Unbiased Classification Methods}
\usage{
UB.class(
  train = NULL,
  test = NULL,
  g = NULL,
  x1 = NULL,
  x2 = NULL,
  method = c("DIS", "DEP", "PRO"),
  depthfun = prodepth,
  ...
)
}
\arguments{
\item{train}{Training data matrix (rows = observations, columns = variables), or
can specify groups separately via \code{x1} and \code{x2}.}

\item{test}{Test data matrix for classification predictions.}

\item{g}{Group membership vector for training data (if using \code{train}).}

\item{x1}{Training data for group 1 (alternative to \code{train}/\code{g}).}

\item{x2}{Training data for group 2 (alternative to \code{train}/\code{g}).}

\item{method}{Classification method to use:
\itemize{
\item \code{'DIS'}: Classify based on data depths
\item \code{'DEP'}: Depth-based method of Makinde & Fasoranbaku (2018)
\item \code{'PRO'}: Project onto line connecting group centers, use PDF estimates
}}

\item{depthfun}{Depth function to use (default: \code{prodepth} for projection depth).}

\item{...}{Additional arguments passed to underlying classification functions.}
}
\value{
Vector of predicted class labels for test observations.
}
\description{
Collection of classification methods whose error rates are not impacted by
unequal sample sizes. Provides three depth-based and projection-based approaches
for two-group classification.
}
\details{
Unlike many classification methods (e.g., LDA, QDA), these approaches produce
error rates that are not systematically biased by unequal training sample sizes.

\strong{Methods}:
\itemize{
\item \code{DIS}: Uses \code{\link{discdepth}} - classifies points based on their
depths relative to each group
\item \code{DEP}: Uses \code{\link{Depth.class}} - implements Makinde and
Fasoranbaku's (2018, JAS) depth-based classifier
\item \code{PRO}: Uses \code{\link{pro.classPD}} - projects data onto line
connecting group centers, estimates PDFs, classifies based on likelihood
}

Bagged versions are available via \code{class.bag} functions.
}
\references{
Makinde, O. S., & Fasoranbaku, O. A. (2018). On some classification methods based
on data depth. Journal of Applied Statistics.
}
\seealso{
\code{\link{discdepth}}, \code{\link{Depth.class}}, \code{\link{pro.classPD}},
\code{\link{UB.class.error}}, \code{\link{UBROC}}
}
\keyword{classification}
\keyword{multivariate}
