% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outliers.R
\name{KNNbag}
\alias{KNNbag}
\title{Bagged K-Nearest Neighbor Classification Using Depth}
\usage{
KNNbag(
  train = NULL,
  test = NULL,
  g = NULL,
  x1 = NULL,
  x2 = NULL,
  depthfun = prodepth,
  nboot = 100,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{train}{Training data matrix (optional if \code{x1} and \code{x2} provided).}

\item{test}{Test data matrix to classify.}

\item{x1, x2}{Training data for groups 1 and 2 (alternative to \code{train}/\code{g}).}

\item{depthfun}{Depth function to use (default: \code{prodepth}).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{...}{Additional arguments.}
}
\value{
Vector of predicted class labels (1 or 2) for test observations.
}
\description{
Bootstrap aggregating version of K-nearest neighbor classification using data
depths. Uses bootstrap sampling to address bias when group sizes differ.
}
\details{
Performs KNN classification using data depths via \code{KNNdist()}. Bootstrap
bagging is used to reduce bias when group sizes are unequal (n1 != n2).
Each bootstrap sample uses balanced group sizes (min(n1,n2)).
}
\seealso{
\code{\link{KNNdist}}, \code{\link{Depth.class.bag}}
}
