% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{bptd}
\alias{bptd}
\title{Bootstrap Percentile-t for Dependent Group Contrasts}
\usage{
bptd(x, tr = 0.2, alpha = 0.05, con = 0, nboot = 599)
}
\arguments{
\item{x}{Data in matrix form (n rows × J columns) or list mode. Columns
represent dependent groups.}

\item{tr}{Numeric. Proportion of trimming (0 to 0.5). Default: 0.2 (20\% trimming).}

\item{alpha}{Numeric. Significance level for confidence intervals. Default: 0.05.}

\item{con}{J × C matrix where C is the number of contrasts and columns contain
contrast coefficients. If 0 or unspecified (default), all pairwise comparisons
are performed.}

\item{nboot}{Integer. Number of bootstrap samples. Default: 599.}
}
\value{
List with components:
\item{test}{Matrix with columns: con.num (contrast number), test (test statistic),
se (standard error)}
\item{psihat}{Matrix with columns: con.num, psihat (contrast estimate),
ci.lower, ci.upper}
\item{crit}{Critical value from bootstrap distribution}
\item{con}{Matrix of contrast coefficients used}
}
\description{
Computes bootstrap percentile-t confidence intervals and tests for linear
contrasts of trimmed means from dependent groups.
}
\details{
The percentile-t bootstrap method provides more accurate inference than the
standard percentile bootstrap, especially with trimmed means. The method:
\enumerate{
\item Centers each group by subtracting its trimmed mean
\item Generates bootstrap samples from centered data
\item Computes bootstrap test statistics
\item Uses the empirical distribution for critical values
}

Missing values are not allowed; the function will stop if any are detected.
}
\seealso{
\code{\link{bptdpsi}}, \code{\link{bptdsub}}
}
