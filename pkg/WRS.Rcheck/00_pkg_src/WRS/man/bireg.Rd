% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression.R
\name{bireg}
\alias{bireg}
\title{Biweight Midregression}
\usage{
bireg(x, y, iter = 20, bend = 1.28)
}
\arguments{
\item{x}{A numeric matrix of predictor variables (n by p).}

\item{y}{A numeric vector of the dependent variable (length n).}

\item{iter}{Maximum number of iterations for convergence (default: 20).}

\item{bend}{Bending constant for biweight estimator (default: 1.28).}
}
\value{
A list with components:
\describe{
\item{coef}{Vector of regression coefficients (intercept, slopes).}
\item{residuals}{Residuals from the final fit.}
}
}
\description{
Computes a robust regression using biweight M-estimation (midregression). This
method combines biweight covariances of predictors with the dependent variable
to produce robust slope estimates, then iteratively refines them.
}
\details{
The biweight midregression is computed by:
\enumerate{
\item Computing biweight M-estimates of location for each predictor
\item Computing biweight covariances between predictors and outcome
\item Solving for initial slopes
\item Iteratively refining estimates until convergence (change < 0.0001)
}

If convergence fails within \code{iter} iterations, a warning message is displayed.
}
\examples{
\dontrun{
# Simple biweight regression
x <- matrix(rnorm(50), ncol=1)
y <- 2*x + rnorm(50, sd=0.5)
bireg(x, y)

# Multiple regression with outliers
x <- matrix(rnorm(100), ncol=2)
y <- x[,1] + 2*x[,2] + rnorm(50, sd=0.3)
y[1:5] <- y[1:5] + 5  # add outliers
bireg(x, y)
}
}
\seealso{
Other robust regression functions: 
\code{\link{LMSreg}()},
\code{\link{MMreg}()},
\code{\link{bmreg}()},
\code{\link{chreg}()},
\code{\link{depreg}()},
\code{\link{mbmreg}()},
\code{\link{mdepreg}()},
\code{\link{mopreg}()},
\code{\link{snmreg}()},
\code{\link{tsgreg}()},
\code{\link{winreg}()},
\code{\link{wreg}()}
}
\concept{robust regression functions}
