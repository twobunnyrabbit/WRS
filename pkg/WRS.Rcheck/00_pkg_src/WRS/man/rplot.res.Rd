% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{rplot.res}
\alias{rplot.res}
\title{Regression Residual Plot with Running Interval Smoother}
\usage{
rplot.res(
  x,
  y,
  pv = 1,
  est = tmean,
  scat = TRUE,
  fr = NA,
  plotit = TRUE,
  pyhat = FALSE,
  efr = 0.5,
  theta = 50,
  phi = 25,
  scale = TRUE,
  expand = 0.5,
  SEED = TRUE,
  varfun = pbvar,
  outfun = outpro,
  STAND = TRUE,
  nmin = 0,
  xout = FALSE,
  out = FALSE,
  eout = FALSE,
  xlab = "X",
  ylab = "Y",
  zscale = FALSE,
  zlab = " ",
  pr = TRUE,
  duplicate = "error",
  ticktype = "simple",
  LP = TRUE,
  ...
)
}
\arguments{
\item{x}{Matrix of predictor values.}

\item{y}{Numeric vector of response values.}

\item{pv}{Index of predictor variable to exclude from initial fit and use
for residual plot (default: 1).}

\item{est}{Estimator function to use for computing location or other
statistics. Common choices include \code{mean}, \code{median}, \code{tmean} (trimmed mean),
\code{mom} (modified one-step M-estimator), \code{mest} (M-estimator).}

\item{fr}{Span parameter for smoothing (default: NA, automatically chosen).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{efr}{Span for error smoothing (default: 0.5).}

\item{theta}{Viewing angle for 3D plots (default: 50).}

\item{phi}{Viewing angle for 3D plots (default: 25).}

\item{scale}{Logical. If \code{TRUE}, scales variables (default: \code{TRUE}).}

\item{expand}{Expansion factor for perspective plots (default: 0.5).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{varfun}{Function for computing variance (default: \code{pbvar}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{STAND}{Logical. If \code{TRUE}, standardizes outlier detection (default: \code{TRUE}).}

\item{nmin}{Minimum sample size for smoothing (default: 0).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{out}{Deprecated parameter.}

\item{eout}{Logical value. If \code{TRUE}, outliers are removed based on
eliminating points with large residuals or leverage (default: \code{FALSE}).}

\item{xlab}{Label for x-axis in plots (default: "X" or "VAR 1").}

\item{ylab}{Label for y-axis in plots (default: "Y" or "VAR 2").}

\item{zscale}{Logical. If \code{TRUE}, scales z-axis (default: \code{FALSE}).}

\item{pr}{Logical. If \code{TRUE}, prints progress messages (default: \code{TRUE}).}

\item{duplicate}{How to handle duplicate values (default: 'error').}

\item{ticktype}{Tick mark style (default: 'simple').}

\item{LP}{Logical. If \code{TRUE}, applies lowess smoothing (default: \code{TRUE}).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
Output from \code{rplot()} applied to residuals.
}
\description{
Fits a smooth using all predictors except one, computes residuals, then
plots the smooth of residuals versus the excluded predictor. Useful for
examining the contribution of individual predictors after accounting for others.
}
\details{
The function first fits a smooth to y using all predictors except the one
specified by \code{pv}, then plots residuals from this fit against the excluded
predictor. This helps assess whether the excluded predictor contributes
additional information beyond the other predictors.
}
\examples{
\dontrun{
# Examine residuals after excluding first predictor
x <- matrix(rnorm(150), ncol=3)
y <- 2*x[,1] + 3*x[,2] + rnorm(50)
rplot.res(x, y, pv=1)  # Residuals vs X1 after fitting with X2
}
}
\seealso{
\code{\link{rplot}}, \code{\link{riplot}}
}
