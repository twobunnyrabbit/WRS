% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{nelderv2}
\alias{nelderv2}
\title{Nelder-Mead Simplex Optimization (Configurable Tolerance)}
\usage{
nelderv2(
  x,
  N,
  FN,
  START = c(rep(1, N)),
  STEP = c(rep(1, N)),
  REQMIN = 1e-04,
  XMIN = c(rep(0, N)),
  XSEC = c(rep(0, N)),
  ...
)
}
\arguments{
\item{x}{Matrix (n × p). Data matrix passed to the function being minimized.}

\item{N}{Integer. Number of parameters to optimize.}

\item{FN}{Function to minimize. Must have signature \code{FN(x, theta, ...)} where
\code{theta} is a vector of N parameter values.}

\item{START}{Numeric vector of length N. Starting parameter values (default: all 1s).}

\item{STEP}{Numeric vector of length N. Initial step sizes for simplex vertices (default: all 1s).}

\item{REQMIN}{Numeric. Convergence tolerance (default: 0.0001, less strict than \code{\link{nelder}}).}

\item{XMIN}{Numeric vector of length N. Placeholder for minimum (default: all 0s).}

\item{XSEC}{Numeric vector of length N. Placeholder for second-best (default: all 0s).}

\item{...}{Additional arguments passed to \code{FN}.}
}
\value{
Numeric vector of length N containing parameter values that minimize \code{FN}.
}
\description{
Minimizes a function of N parameters using the Nelder-Mead simplex algorithm.
Enhanced version of \code{\link{nelder}} with configurable convergence tolerance
and support for additional function arguments.
}
\details{
\strong{Differences from \code{\link{nelder}}}:
\itemize{
\item Allows user-specified convergence tolerance (\code{REQMIN})
\item Passes additional arguments to objective function via \code{...}
\item Default tolerance (0.0001) is less strict than \code{nelder} (0.0000001)
}

\strong{Algorithm}: Same Nelder-Mead simplex method as \code{\link{nelder}} with
reflection, expansion, and contraction operations. Maximum iterations: 500.

Use this version when:
\itemize{
\item The objective function requires additional parameters
\item You want to control convergence strictness
\item Faster (less precise) convergence is acceptable
}
}
\references{
Olsson, D. M. (1974). A sequential simplex program for solving minimization problems.
Journal of Quality Technology, 6, 53-57.
}
\seealso{
\code{\link{nelder}} for basic version
}
\keyword{internal}
\keyword{optimize}
