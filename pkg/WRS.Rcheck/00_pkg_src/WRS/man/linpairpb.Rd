% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{linpairpb}
\alias{linpairpb}
\title{All Pairwise Comparisons with Percentile Bootstrap}
\usage{
linpairpb(
  x,
  tr = 0.2,
  alpha = 0.05,
  nboot = NA,
  est = tmean,
  method = "hoch",
  bhop = FALSE,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{x}{Data in matrix or list format. For matrix: each column is a group.
For list: x[\link{j}] contains data for group j.}

\item{tr}{Trimming proportion (default: 0.2). Used when est = tmean.}

\item{alpha}{Significance level (default: 0.05).}

\item{nboot}{Number of bootstrap samples (default: NA, determined automatically).}

\item{est}{Estimator function (default: \code{\link{tmean}}).}

\item{method}{Method for controlling family-wise error rate (default: 'hoch' for Hochberg).
Can also use 'BH' for Benjamini-Hochberg.}

\item{bhop}{Logical. If TRUE, uses Benjamini-Hochberg method (overrides method).}

\item{SEED}{Logical. If TRUE (default), sets random seed for reproducibility.}

\item{...}{Additional arguments passed to the estimator function.}
}
\value{
List with components:
\describe{
\item{output}{Matrix with columns: Group, Group, psihat, p.value, p.crit,
ci.lower, ci.upper, Est.1, Est.2, p.adjusted}
\item{num.sig}{Number of significant pairwise comparisons}
}
}
\description{
Performs all pairwise comparisons using percentile bootstrap with control for
multiple comparisons. Returns results in a convenient format for use by other functions.
}
\details{
This is a wrapper around \code{\link{linconpb}} that:
\itemize{
\item Automatically generates all pairwise contrast coefficients
\item Calls linconpb to perform bootstrap inference
\item Reformats output to include group identifiers and individual estimates
\item Counts number of significant comparisons
}
}
\examples{
# Four groups
set.seed(123)
x <- list(rnorm(20), rnorm(20, mean = 0.5),
          rnorm(20, mean = 0.3), rnorm(20, mean = 0.8))
result <- linpairpb(x, nboot = 500)
result$num.sig
}
\seealso{
\code{\link{linconpb}}, \code{\link{con.all.pairs}}, \code{\link{linpbg}}
}
