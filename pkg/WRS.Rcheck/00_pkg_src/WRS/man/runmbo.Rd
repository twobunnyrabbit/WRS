% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{runmbo}
\alias{runmbo}
\title{Running Interval Smoother with Bootstrap Aggregating (Bagging)}
\usage{
runmbo(
  x,
  y,
  fr = 1,
  est = tmean,
  xlab = "X",
  ylab = "Y",
  pts = x,
  RNA = FALSE,
  atr = 0,
  pch = "*",
  pyhat = FALSE,
  eout = FALSE,
  outfun = out,
  plotit = TRUE,
  xout = FALSE,
  scat = TRUE,
  nboot = 40,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{x}{Predictor variable}

\item{y}{Response variable}

\item{fr}{Span parameter controlling smoothing (default: 1)}

\item{est}{Measure of location (default: tmean)}

\item{xlab}{Label for x-axis (default: "X")}

\item{ylab}{Label for y-axis (default: "Y")}

\item{pts}{Points at which to estimate y (default: x)}

\item{RNA}{Logical: remove NAs when averaging bootstrap estimates (default: FALSE)}

\item{atr}{Trimming proportion when averaging bootstrap values (default: 0)}

\item{pch}{Plotting character for data points (default: '*')}

\item{pyhat}{Logical: if TRUE, return predicted values (default: FALSE)}

\item{eout}{Logical: remove (x,y) outliers (default: FALSE)}

\item{outfun}{Outlier detection function (default: out)}

\item{plotit}{Logical: create plot (default: TRUE)}

\item{xout}{Logical: remove x outliers (default: FALSE)}

\item{scat}{Logical: show scatter plot of data points (default: TRUE)}

\item{nboot}{Number of bootstrap samples (default: 40)}

\item{SEED}{Logical: set random seed (default: TRUE)}

\item{...}{Additional arguments passed to est function}
}
\value{
"Done" if pyhat=FALSE, or vector of fitted values if pyhat=TRUE
}
\description{
Compute a running interval smoother with bootstrap aggregating (bagging)
to reduce variance and create more stable estimates.
}
\details{
Implements bootstrap aggregating (bagging) for running interval smoothers:
\enumerate{
\item Draws nboot bootstrap samples from the data
\item Computes a running interval smooth for each bootstrap sample
\item Averages the bootstrap smooths (with optional trimming via atr)
}

This approach reduces variance compared to a single smooth. Only works
with single predictor (not multivariate). Missing values are automatically
removed. Cannot have both eout=TRUE and xout=TRUE simultaneously.
}
\seealso{
\code{\link{runhat}}, \code{\link{run3bo}}, \code{\link{rungen}}
}
\keyword{bootstrap}
\keyword{nonparametric}
\keyword{smooth}
