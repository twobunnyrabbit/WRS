% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{difqci.mul}
\alias{difqci.mul}
\title{Difference Quantile Confidence Intervals for Multiple Reference Points}
\usage{
difqci.mul(
  x,
  y,
  refp = NULL,
  pts = NULL,
  q = seq(0.1, 0.4, 0.1),
  estfun = hdmq,
  center = FALSE,
  alpha = 0.05,
  nboot = 1000,
  SEED = TRUE,
  method = "BH",
  plotit = FALSE,
  xlab = "Difference",
  ylab = "Density",
  plotfun = kerSORT
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{refp}{Reference points for the probability estimates. If NULL, uses quantile
estimates based on the q parameter.}

\item{pts}{Alternative specification for reference points (synonymous with refp).}

\item{q}{Quantile values to use for automatic reference point selection
(default: seq(.1,.4,.1)).}

\item{estfun}{Estimator function for quantiles (default: hdmq, Harrell-Davis).}

\item{center}{Logical. If TRUE, centers the differences by subtracting the median.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{method}{Method for p-value adjustment (default: 'BH' for Benjamini-Hochberg).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{xlab}{Label for x-axis when plotit=TRUE.}

\item{ylab}{Label for y-axis when plotit=TRUE.}

\item{plotfun}{Plotting function to use (default: kerSORT).}
}
\value{
Matrix with columns for quantiles, reference points, probability estimates,
and confidence interval bounds.
}
\description{
Computes confidence intervals for P(X-Y < refp) at multiple reference points.
Uses the Harrell-Davis estimator by default to estimate quantiles of the
distribution of X-Y differences.
}
\examples{
\dontrun{
x <- rnorm(50, mean=5)
y <- rnorm(50, mean=3)
difqci.mul(x, y, nboot=500)
}
}
