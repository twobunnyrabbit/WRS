% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{qcomhdMC}
\alias{qcomhdMC}
\title{Compare Multiple Quantiles Between Two Groups (Multicore Version)}
\usage{
qcomhdMC(
  x,
  y,
  est = hd,
  q = c(0.1, 0.25, 0.5, 0.75, 0.9),
  nboot = 4000,
  plotit = TRUE,
  SEED = TRUE,
  xlab = "Group 1",
  ylab = "Est.1-Est.2",
  alpha = 0.05,
  ADJ.CI = TRUE
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{est}{Estimator function for quantiles (default: \code{hd}, the Harrell-Davis estimator).}

\item{q}{Vector of quantiles to compare (default: c(.1, .25, .5, .75, .9)).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{xlab}{Label for x-axis in plot (default: "Group 1").}

\item{ylab}{Label for y-axis in plot (default: "Est.1-Est.2").}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{ADJ.CI}{Logical. If \code{TRUE}, adjusts confidence intervals based on the
significance level used by the corresponding test statistic (default: \code{TRUE}).}
}
\value{
A data frame with columns:
\itemize{
\item \code{q}: The quantile being compared
\item \code{n1}, \code{n2}: Sample sizes for groups 1 and 2
\item \code{est.1}, \code{est.2}: Quantile estimates for each group
\item \code{est.1_minus_est.2}: Difference between group estimates
\item \code{ci.low}, \code{ci.up}: Confidence interval bounds for the difference
\item \code{p_crit}: Critical p-value based on Hochberg's method
\item \code{p-value}: P-value for the comparison
\item \code{signif}: "YES" if significant, "NO" otherwise
}
}
\description{
Compares quantiles between two independent groups using the Harrell-Davis
estimator with percentile bootstrap. Uses Hochberg's method to control
the familywise error rate across multiple quantile comparisons.
}
\details{
This function uses the percentile bootstrap method via \code{pb2genMC} to compare
quantiles. Tied values are allowed. When comparing lower or upper quartiles,
both power and Type I error rates compare favorably to other methods.

The function uses Hochberg's sequentially rejective procedure to control the
familywise error rate across all quantile comparisons. If \code{ADJ.CI = TRUE},
confidence intervals are adjusted to maintain consistency with the test results.

A plot is created showing the point estimates and confidence intervals for
each quantile difference.
}
\note{
This function prints a message suggesting that \code{qcomhd} with \code{MC=TRUE}
can also be used for the same analysis.
}
\examples{
\dontrun{
set.seed(123)
x <- rnorm(50, mean = 0, sd = 1)
y <- rnorm(50, mean = 0.5, sd = 1.2)

# Compare multiple quantiles
result <- qcomhdMC(x, y, nboot = 1000)
print(result)

# Compare specific quantiles only
qcomhdMC(x, y, q = c(.25, .5, .75), nboot = 1000)
}
}
\seealso{
\code{\link{qcomhd}}, \code{\link{pb2genMC}}, \code{\link{hd}}
}
