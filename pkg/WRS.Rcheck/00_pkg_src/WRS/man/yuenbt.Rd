% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{yuenbt}
\alias{yuenbt}
\title{Yuen's Bootstrap-t Test for Trimmed Means}
\usage{
yuenbt(
  x,
  y,
  tr = 0.2,
  alpha = 0.05,
  nboot = 199,
  side = FALSE,
  nullval = 0,
  pr = TRUE,
  plotit = FALSE,
  op = 1,
  SEED = TRUE
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{tr}{Trimming proportion (default: 0.2). Proportion of observations to
trim from each tail when computing winsorized correlation.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{side}{Logical. If \code{TRUE}, uses two-sided method with absolute values;
if \code{FALSE}, uses equal-tailed method (default: \code{FALSE}).}

\item{nullval}{Null hypothesis value for testing (default: 0).}

\item{pr}{Logical. If \code{TRUE}, prints progress (default: \code{TRUE}).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{op}{Plotting option when \code{plotit=TRUE}: 1 for adaptive kernel density,
2 for ridgeline plot (default: 1).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}
}
\value{
A list with components:
\item{ci}{Confidence interval for the difference in trimmed means}
\item{test.stat}{Test statistic value}
\item{p.value}{p-value for the test}
\item{est.1}{Trimmed mean of group 1}
\item{est.2}{Trimmed mean of group 2}
\item{est.dif}{Difference in trimmed means}
\item{n1}{Sample size of group 1}
\item{n2}{Sample size of group 2}
}
\description{
Computes a confidence interval for the difference between trimmed means
of two independent groups using the bootstrap-t method.
}
\details{
This function implements Yuen's method using the bootstrap-t approach
for comparing trimmed means of two independent groups. The bootstrap-t
method tends to provide better coverage than the percentile bootstrap,
especially for small samples.

The function uses centering (subtracting group trimmed means) before
bootstrap resampling to improve performance.
}
\examples{
x <- rnorm(25, mean=100, sd=15)
y <- rnorm(30, mean=110, sd=15)
yuenbt(x, y, tr=0.2, nboot=500)
}
\seealso{
\code{\link{yuen}}, \code{\link{trimse}}
}
