% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{Bband}
\alias{Bband}
\title{Shift Function for 2×2 Design Main Effects (Factor B)}
\usage{
Bband(
  x,
  alpha = 0.05,
  plotit = TRUE,
  sm = TRUE,
  SEED = TRUE,
  nboot = 500,
  grp = c(1:4),
  xlab = "X (First Level)",
  ylab = "Delta",
  crit = NA,
  print.all = FALSE,
  plot.op = FALSE
)
}
\arguments{
\item{x}{Data in list mode or matrix. Four groups representing the 2×2 design.}

\item{alpha}{Significance level. Default is 0.05.}

\item{plotit}{Logical. If \code{TRUE} (default), creates shift function plot.}

\item{sm}{Logical. If \code{TRUE} (default), uses smoothing.}

\item{SEED}{Logical. If \code{TRUE} (default), sets random seed to 2 for reproducibility.}

\item{nboot}{Number of bootstrap samples for approximating critical value. Default is 500.}

\item{grp}{Vector indicating group assignment. Default is \code{c(1:4)}.
Groups 1 and 2 form level 1 of Factor A; groups 3 and 4 form level 2.
Levels 1 and 2 of Factor B consist of groups 1,3 and 2,4, respectively.}

\item{xlab}{Label for x-axis. Default is "X (First Level)".}

\item{ylab}{Label for y-axis. Default is "Delta".}

\item{crit}{Optional pre-specified critical value. If \code{NA} (default), approximated
via bootstrap.}

\item{print.all}{Logical. If \code{FALSE} (default), returns summary statistics
(number significant, critical value, KS statistic). If \code{TRUE}, returns
all confidence intervals.}

\item{plot.op}{Logical. If \code{TRUE}, creates a g2plot instead. Default is \code{FALSE}.}
}
\value{
If \code{print.all=FALSE}, a matrix with:
\itemize{
\item \code{number sig}: Number of confidence intervals not containing zero
\item \code{critical value}: Critical value used for inference
\item \code{KS test statistics}: Kolmogorov-Smirnov test statistic
}

If \code{print.all=TRUE}, the full output from \code{\link{sband}}.
}
\description{
Applies the shift function when analyzing the main effect of Factor B in a
2×2 design. Compares distributions \eqn{d_1 = (x_1 + x_3)/2}{d1 = (x1+x3)/2}
and \eqn{d_2 = (x_2 + x_4)/2}{d2 = (x2+x4)/2}.
}
\details{
This function focuses on the main effect of Factor B in a 2×2 factorial design
by applying a shift function to averaged distributions across levels of Factor A.

For groups 1, 2, 3, 4 in a 2×2 design:
\itemize{
\item Factor A: Level 1 = groups 1,2; Level 2 = groups 3,4
\item Factor B: Level 1 = groups 1,3; Level 2 = groups 2,4
}

The function compares the distributions of averaged pairwise sums across
Factor A levels to assess the main effect of Factor B.

Data in each group are divided by 2 before analysis.
}
\seealso{
\code{\link{sband}}, \code{\link{Aband}}, \code{\link{ks.test}}
}
\keyword{design}
\keyword{htest}
