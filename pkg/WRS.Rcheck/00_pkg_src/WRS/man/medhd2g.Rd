% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/location.R
\name{medhd2g}
\alias{medhd2g}
\title{Compare Medians of Two Independent Groups Using Harrell-Davis Estimator}
\usage{
medhd2g(x, y, alpha = 0.05, nboot = 2000, SEED = TRUE, pr = TRUE, ...)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{pr}{Logical. If \code{TRUE}, prints progress messages (default: \code{TRUE}).}

\item{...}{Additional arguments passed to \code{\link{pb2gen}}.}
}
\value{
A list containing the results from \code{\link{pb2gen}}, including
confidence intervals and p-values for the comparison.
}
\description{
Compares the medians of two independent groups using the Harrell-Davis
estimator and percentile bootstrap.
}
\details{
This is a convenience wrapper for \code{\link{pb2gen}} that defaults to
using the Harrell-Davis median estimator for comparing two independent
groups.
}
\examples{
x <- rnorm(30)
y <- rnorm(30, mean = 0.5)
medhd2g(x, y, nboot = 1000)

}
\seealso{
\code{\link{pb2gen}} for the underlying implementation,
\code{\link{hd}} for Harrell-Davis estimator,
\code{\link{Dqcomhd}} for dependent groups
}
