% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{Mpca}
\alias{Mpca}
\title{Robust Principal Component Analysis Using Maronna's Method}
\usage{
Mpca(
  x,
  N1 = 3,
  N2 = 2,
  tol = 0.001,
  N2p = 10,
  Nran = 50,
  Nkeep = 10,
  SEED = TRUE,
  op.pro = 0.1,
  SCORES = FALSE,
  pval = NULL
)
}
\arguments{
\item{x}{Numeric matrix (n × m) containing the data}

\item{N1}{Number of iterations for first stage (default: 3)}

\item{N2}{Number of iterations for second stage (default: 2)}

\item{tol}{Convergence tolerance (default: 0.001)}

\item{N2p}{Number of iterations for N2p stage (default: 10)}

\item{Nran}{Number of random starts (default: 50)}

\item{Nkeep}{Number of candidates to keep at each stage (default: 10)}

\item{SEED}{Logical; if TRUE sets random seed for reproducibility (default: TRUE)}

\item{op.pro}{Maximum proportion of unexplained variance desired (default: 0.1)}

\item{SCORES}{Logical; if TRUE computes and returns principal component scores (default: FALSE)}

\item{pval}{If specified, uses exactly p=pval principal components; if NULL, determines automatically (default: NULL)}
}
\value{
A list with components:
\item{B}{Projection matrix}
\item{a}{Location estimate}
\item{var.op}{Variance of optimal projection}
\item{unexplained.pro.var}{Proportion of unexplained variance (matrix if pval=NULL, scalar if pval specified)}
\item{scores}{Principal component scores (NULL unless SCORES=TRUE and pval specified)}
\item{wt.cov}{Weighted covariance matrix (NULL unless pval specified)}
}
\description{
Performs robust PCA using Maronna's projection pursuit method (2005),
automatically determining the number of components based on proportion
of unexplained variance or computing scores for a specified number of components.
}
\details{
Implements Maronna's (2005) robust PCA method based on projection pursuit.
See Maronna, R.A. (2005), "Principal Components and Orthogonal Regression Based on
Robust Scales," Technometrics, 47, 264-273.

When pval=NULL, the function adds components sequentially until the proportion
of unexplained variance falls below \code{op.pro}.
}
\references{
Maronna, R.A. (2005). Principal Components and Orthogonal Regression Based on
Robust Scales. Technometrics, 47, 264-273.
}
\seealso{
\code{\link{marpca}}, \code{\link{spca}}
}
