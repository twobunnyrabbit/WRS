% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{ancovap2.KMSci}
\alias{ancovap2.KMSci}
\title{ANCOVA KMS Effect Size with Bootstrap Confidence Intervals (Two Covariates)}
\usage{
ancovap2.KMSci(
  x1,
  y1,
  x2,
  y2,
  pts = NULL,
  alpha = 0.05,
  nboot = 100,
  SEED = TRUE,
  npts = 20,
  SIMPLE = FALSE,
  PLOT.ADJ = FALSE,
  plotit = TRUE,
  xlab = "X1",
  ylab = "X2",
  BOTH = TRUE,
  profun = prodepth,
  xout = FALSE,
  outfun = outpro,
  method = "hoch"
)
}
\arguments{
\item{x1}{A matrix with two columns containing the covariate values for group 1.}

\item{y1}{A numeric vector containing the outcome variable for group 1.}

\item{x2}{A matrix with two columns containing the covariate values for group 2.}

\item{y2}{A numeric vector containing the outcome variable for group 2.}

\item{pts}{A matrix with two columns specifying the covariate value combinations.
If \code{NULL}, points are automatically selected (see \code{SIMPLE} parameter).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{npts}{Number of design points when \code{pts = NULL} and \code{SIMPLE = FALSE}
(default: 20). Points are evenly spaced from deepest to least deep.}

\item{SIMPLE}{Logical. When \code{pts = NULL}, if \code{TRUE}, uses quartiles of the
marginal distributions of group 1 to determine covariate points. If \code{FALSE}
(default), uses depth-based selection via \code{profun}.}

\item{PLOT.ADJ}{Logical. If \code{TRUE}, highlights points using adjusted p-values.
If \code{FALSE} (default), uses unadjusted p-values for highlighting.}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{xlab}{Label for first covariate axis in plot (default: 'X1').}

\item{ylab}{Label for second covariate axis in plot (default: 'X2').}

\item{BOTH}{Logical. If \code{TRUE} (default) and \code{SIMPLE = FALSE}, combines x1 and
x2 when selecting design points. If \code{FALSE}, uses only x1.}

\item{profun}{Function for computing depth of points when \code{SIMPLE = FALSE}.
Default is \code{prodepth} (product of marginal depths). Alternative: \code{pdepth}
(random projections).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{method}{Method for adjusting p-values for multiple comparisons. Options
include 'hoch' (Hochberg, default), 'BH' (Benjamini-Hochberg), 'bonferroni',
etc. See \code{\link{p.adjust}}.}
}
\value{
A list with components:
\describe{
\item{pts}{Matrix of covariate value combinations where effect sizes were computed.}
\item{output}{Matrix with columns: Est. (effect size estimate), Test.Stat
(test statistic), ci.low (lower CI bound), ci.up (upper CI bound),
p-value (unadjusted), p.adjusted (adjusted for multiple comparisons).}
}
}
\description{
Computes KMS effect sizes for two independent groups with two covariates,
along with bootstrap confidence intervals and hypothesis tests. Tests whether
the effect size is significantly different from zero at specified covariate
value combinations.
}
\details{
For each covariate value combination in \code{pts}, the function:
\enumerate{
\item Computes the KMS effect size using \code{\link{ancovap2.KMS}}
\item Estimates standard errors via percentile bootstrap
\item Applies a sample-size adjustment factor to improve accuracy
\item Computes confidence intervals and p-values
\item Adjusts p-values for multiple comparisons
}

The plot displays all tested points with asterisks, and circles significant
points (those with p ≤ 0.05).
}
\examples{
\dontrun{
# Generate example data
set.seed(123)
n1 <- 50; n2 <- 50
x1 <- matrix(rnorm(n1 * 2), ncol = 2)
y1 <- x1[,1] + x1[,2] + rnorm(n1)
x2 <- matrix(rnorm(n2 * 2), ncol = 2)
y2 <- x2[,1] + x2[,2] + 0.5 + rnorm(n2)

# Compute effect sizes with CIs
result <- ancovap2.KMSci(x1, y1, x2, y2, nboot = 500)
print(result$output)
}
}
\seealso{
\code{\link{ancovap2.KMS}}, \code{\link{ancovap2.KMS.plot}}
}
