% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{skip}
\alias{skip}
\title{Skipped Location and Covariance Matrix}
\usage{
skip(
  m,
  cop = 6,
  MM = FALSE,
  op = 1,
  mgv.op = 0,
  outpro.cop = 3,
  STAND = TRUE,
  pr = TRUE
)
}
\arguments{
\item{m}{Numeric matrix (n×p) containing the data.}

\item{cop}{Integer (1-6). Method for determining the center in projection method:
\itemize{
\item 1: Donoho-Gasko median
\item 2: MCD (Minimum Covariance Determinant)
\item 3: Marginal medians
\item 4: MVE (Minimum Volume Ellipsoid)
\item 5: TBS
\item 6: RMBA - Olive's median ball algorithm (default)
}}

\item{MM}{Logical. For projection method (\code{op=1}): if \code{FALSE} (default),
uses boxplot rule for outlier detection; if \code{TRUE}, uses MAD and median.}

\item{op}{Integer (1 or 2). Outlier detection method:
\itemize{
\item 1: Projection method (default) - uses \code{\link{outpro}}
\item 2: MGV method - uses \code{\link{outmgv}}
}}

\item{mgv.op}{Integer (0-2). For MGV method (\code{op=2}), determines center:
\itemize{
\item 0: All pairwise distances (default)
\item 1: MVE
\item 2: MCD
}}

\item{outpro.cop}{Integer. Value passed to \code{cop} parameter of \code{\link{outpro}}
when \code{op=1}. Default is 3 (marginal medians).}

\item{STAND}{Logical. If \code{TRUE} (default), standardizes data in projection method.}

\item{pr}{Logical. If \code{TRUE} (default), prints progress information.}
}
\value{
List with components:
\item{center}{Numeric vector of length p containing the location estimate}
\item{cov}{p×p covariance matrix estimate}
}
\description{
Computes robust location and covariance estimates by first removing outliers
using projection-based or MGV outlier detection methods, then computing classical
estimates on the remaining data.
}
\details{
The "skipped" approach first identifies and removes outliers, then computes
classical location (mean) and covariance estimates on the cleaned data. This
differs from robust estimators that downweight outliers.

\strong{Projection method} (\code{op=1}):
For each point, projects all data onto the line connecting that point to the
center, then checks for outliers along this projection. A point is flagged as
an outlier if it's detected as outlying in any projection.

\strong{MGV method} (\code{op=2}):
Uses the MGV (Minimum Generalized Variance) approach to detect outliers based
on the overall data structure.
}
\seealso{
\code{\link{outpro}}, \code{\link{outmgv}}, \code{\link{skipreg}},
\code{\link{skipSPR}}
}
\keyword{multivariate}
\keyword{robust}
