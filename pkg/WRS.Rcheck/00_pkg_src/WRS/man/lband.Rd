% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{lband}
\alias{lband}
\title{Confidence Band for Shift Function (Lombard Method)}
\usage{
lband(
  x,
  y = NULL,
  alpha = 0.05,
  plotit = TRUE,
  sm = TRUE,
  op = 1,
  ylab = "delta",
  CI = TRUE,
  xlab = "x (first group)"
)
}
\arguments{
\item{x}{Numeric vector for first group, or a matrix/list with two columns/elements.}

\item{y}{Numeric vector for second group. If \code{NULL} and x is matrix/list,
y is taken from second column/element. Default is \code{NULL}.}

\item{alpha}{Significance level for confidence band (default: 0.05).}

\item{plotit}{Logical. If \code{TRUE}, creates a plot of the shift function.
Default is \code{TRUE}.}

\item{sm}{Logical. If \code{TRUE}, smooths the plotted shift function using
lowess (if \code{op=1}) or expected frequency curve. Default is \code{TRUE}.}

\item{op}{Integer. Smoothing option: 1 uses lowess, other values use expected
frequency curve. Default is 1.}

\item{ylab}{Character string for y-axis label. Default is 'delta'.}

\item{CI}{Logical. If \code{TRUE}, includes confidence band in plot. Default is \code{TRUE}.}

\item{xlab}{Character string for x-axis label. Default is 'x (first group)'.}
}
\value{
A matrix with columns:
\item{qhat}{Quantile levels}
\item{l}{Lower confidence band}
\item{u}{Upper confidence band}
}
\description{
Computes a confidence band for the shift function when comparing two dependent
groups using the method of Lombard (2005).
}
\details{
This function implements Lombard's method for constructing simultaneous confidence
bands for the shift function (differences in quantiles) between two dependent groups.
Missing observations are removed. When plotting, the median of x is marked with +
and quartiles with o.
}
\references{
Lombard, F. (2005). Detecting change-points by Fourier analysis. Technometrics,
47, 364-369.
}
\seealso{
\code{\link{wband}}, \code{\link{sband}}, \code{\link{iband}}
}
\keyword{htest}
