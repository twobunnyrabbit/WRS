% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{ord.loc.PV}
\alias{ord.loc.PV}
\title{Test and Determine Ordering of Trimmed Means with FWE Control}
\usage{
ord.loc.PV(x, p.crit = NULL, alpha = 0.05, tr = 0.2, iter = 5000, SEED = TRUE)
}
\arguments{
\item{x}{Data. Can be a matrix, data frame, or list. Must have at least 3 groups.}

\item{p.crit}{Vector of critical p-values for each test. If \code{NULL}, determined
via simulation. Default is \code{NULL}.}

\item{alpha}{Significance level for FWE control. Default is 0.05.}

\item{tr}{Trimming proportion. Default is 0.2 (20\% trimming).}

\item{iter}{Number of simulation iterations for determining critical p-values.
Default is 5000.}

\item{SEED}{Logical. Set random seed for reproducibility. Default is \code{TRUE}.}
}
\value{
An S4 object of class 'BIN' with slots:
\item{Make.a.Decison}{'YES' if complete ordering established, 'NO' otherwise.}
\item{Decision.p.value}{Overall p-value for the ordering decision.}
\item{Estimates}{Trimmed mean estimates for all groups.}
\item{n}{Sample sizes for all groups.}
\item{output}{Matrix with detailed results for each pairwise test.}
}
\description{
Determines whether a complete ordering of population trimmed means can be
established across J groups, controlling family-wise error rate (FWE).
}
\details{
Groups are sorted by trimmed mean estimates, then adjacent pairs (j, j+1) are
tested for j=1,...,J-1. FWE is controlled using a method designed specifically
for this ordering problem. A complete ordering decision is made only if all J-1
tests reject the null hypothesis.
}
\seealso{
\code{\link{ord.loc.ex}}, \code{\link{ord.loc.crit}}, \code{\link{yuen}}
}
\keyword{htest}
\keyword{robust}
