% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{pts.to.equivalence}
\alias{pts.to.equivalence}
\title{Test Equivalence Across Range of MESOI Values}
\usage{
pts.to.equivalence(
  x,
  y,
  start.pts = 3,
  step = 1,
  max.pts = 30,
  eqbound = 0.05,
  q = 0.25,
  nboot = 1000,
  alpha = 0.05,
  SEED = TRUE,
  first.stop = "YES"
)
}
\arguments{
\item{x}{Numeric vector for group 1.}

\item{y}{Numeric vector for group 2.}

\item{start.pts}{Numeric. Starting MESOI value (default: 3).}

\item{step}{Numeric. Step size for incrementing MESOI (default: 1).}

\item{max.pts}{Numeric. Maximum MESOI value to test (default: 30).}

\item{eqbound}{Numeric. Equivalence bound for difference in probabilities (default: 0.05).}

\item{q}{Numeric. Quantile for comparison (default: 0.25).}

\item{nboot}{Numeric. Number of bootstrap samples (default: 1000).}

\item{alpha}{Numeric. Significance level (default: 0.05).}

\item{SEED}{Logical. Set random seed for reproducibility (default: \code{TRUE}).}

\item{first.stop}{Character. If "YES", stop at first equivalence detection;
otherwise test all MESOI values (default: "YES").}
}
\value{
Data frame with columns: pts, Est.dif, confidence intervals, p-values,
test results, and equivalence status for each MESOI value tested.
Also produces two ggplot visualizations.
}
\description{
Sequentially tests for equivalence between two groups across a range of Minimum
Effect Size Of Interest (MESOI) values using TOST (Two One-Sided Tests) procedure.
Creates visualizations of results.
}
\details{
For each MESOI value from \code{start.pts} to \code{max.pts}, calls
\code{\link{wmw.ref.dif.TOST}} to test equivalence. Generates two plots:
\enumerate{
\item Estimated differences with confidence intervals vs MESOI
\item S-values (surprisal values) for difference and equivalence tests
}

Stops early if equivalence is detected and \code{first.stop="YES"}.
}
\examples{
\dontrun{
# Test equivalence across MESOI from 3 to 30
result <- pts.to.equivalence(x = group1, y = group2)
}

}
\seealso{
\code{\link{wmw.ref.dif.TOST}}
}
\keyword{htest}
