% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{plotCI}
\alias{plotCI}
\title{Plot with Confidence Intervals}
\usage{
plotCI(
  x,
  y = NULL,
  uiw = NULL,
  liw = uiw,
  aui = NULL,
  ali = aui,
  err = "y",
  ylim = NULL,
  sfrac = 0.01,
  gap = 0,
  add = FALSE,
  col = par("col"),
  lwd = par("lwd"),
  slty = par("lty"),
  xlab = NULL,
  ylab = NULL,
  ...
)
}
\arguments{
\item{x}{Numeric vector of x-coordinates, or a list with components x and y.}

\item{y}{Numeric vector of y-coordinates (if x is not a list).}

\item{uiw}{Numeric vector of widths for upper intervals (relative to y or x).}

\item{liw}{Numeric vector of widths for lower intervals (default: same as \code{uiw}).}

\item{aui}{Numeric vector of absolute upper interval values.}

\item{ali}{Numeric vector of absolute lower interval values.}

\item{err}{Character. Direction of error bars: "y" (default) or "x".}

\item{ylim}{Numeric vector of length 2 specifying y-axis limits.}

\item{sfrac}{Numeric. Size fraction for error bar caps (default: 0.01).}

\item{gap}{Numeric or logical. Gap between points and error bars (default: 0).
If \code{TRUE}, uses default gap of 0.01.}

\item{add}{Logical. If \code{TRUE}, add to existing plot; if \code{FALSE}, create new plot.}

\item{col}{Color for points and error bars (default: current par("col")).}

\item{lwd}{Line width (default: current par("lwd")).}

\item{slty}{Line type for error bars (default: current par("lty")).}

\item{xlab, ylab}{Axis labels.}

\item{...}{Additional arguments passed to \code{plot} or \code{points}.}
}
\value{
Invisibly returns list with components \code{x} and \code{y}.
}
\description{
Creates a scatter plot with error bars representing confidence intervals or
other interval estimates. Originally from Bill Venables, R-list.
}
\details{
Either \code{uiw}/\code{liw} (relative intervals) or \code{aui}/\code{ali}
(absolute intervals) must be specified. Error bars are drawn using arrows with
90-degree angle caps.
}
\examples{
\dontrun{
# Plot means with standard error bars
x <- 1:5
y <- c(2.1, 3.5, 2.8, 4.2, 3.9)
se <- c(0.3, 0.4, 0.2, 0.5, 0.3)
plotCI(x, y, uiw = se)
}

}
\keyword{hplot}
