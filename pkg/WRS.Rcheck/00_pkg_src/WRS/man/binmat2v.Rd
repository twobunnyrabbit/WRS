% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{binmat2v}
\alias{binmat2v}
\title{Extract Matrix Rows Based on Binary Split Criteria}
\usage{
binmat2v(m, col = c(1, 2), int1 = c(0.5, 0.5), int2 = c(0.5, 0.5), INC = TRUE)
}
\arguments{
\item{m}{Matrix or data frame to extract rows from}

\item{col}{Vector of two column indices specifying which columns to use for
splitting criteria (default c(1,2))}

\item{int1}{Interval bounds for first variable as c(lower, upper) (default
c(.5, .5) for median split)}

\item{int2}{Interval bounds for second variable as c(lower, upper) (default
c(.5, .5) for median split)}

\item{INC}{Logical; if TRUE (default), use <= and >= comparisons; if FALSE,
use < and > comparisons}
}
\value{
Matrix or vector containing the rows of m that satisfy both criteria.
}
\description{
Extract rows from a matrix based on binary split criteria applied to two
variables. Useful for creating binary categorizations based on medians or
other cutpoints.
}
\details{
This function pulls out rows of matrix m where:
\itemize{
\item Column col\link{1} satisfies: int1\link{1} <= value <= int1\link{2} (if INC=TRUE)
\item Column col\link{2} satisfies: int2\link{1} <= value <= int2\link{2} (if INC=TRUE)
}

By default, splits at the median for both variables (0.5, 0.5). Can be used
to create 2x2 contingency tables from continuous data.
}
\keyword{binomial}
\keyword{utilities}
