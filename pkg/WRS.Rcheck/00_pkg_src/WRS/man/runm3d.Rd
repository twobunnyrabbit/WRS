% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{runm3d}
\alias{runm3d}
\title{3D Running Interval Smoother with Trimmed Means}
\usage{
runm3d(
  x,
  y,
  theta = 50,
  phi = 25,
  fr = 0.8,
  tr = 0.2,
  plotit = TRUE,
  pyhat = FALSE,
  nmin = 0,
  expand = 0.5,
  scale = FALSE,
  zscale = FALSE,
  xout = FALSE,
  outfun = out,
  eout = FALSE,
  xlab = "X",
  ylab = "Y",
  zlab = "",
  pr = TRUE,
  SEED = TRUE,
  ticktype = "simple"
)
}
\arguments{
\item{x}{An n by p matrix of predictors}

\item{y}{Vector of response values}

\item{theta}{Azimuthal viewing angle for plot (default: 50)}

\item{phi}{Colatitude viewing angle for plot (default: 25)}

\item{fr}{Span parameter controlling smoothing (default: 0.8)}

\item{tr}{Trimming proportion (default: 0.2)}

\item{plotit}{Logical: create 3D perspective plot (default: TRUE)}

\item{pyhat}{Logical: if TRUE, return predicted values (default: FALSE)}

\item{nmin}{Minimum neighbors for estimation (default: 0)}

\item{expand}{Relative length of z-axis in plot (default: 0.5)}

\item{scale}{Logical: scale the plot (default: FALSE)}

\item{zscale}{Logical: standardize variables to median 0, MAD 1 (default: FALSE)}

\item{xout}{Logical: remove x outliers (default: FALSE)}

\item{outfun}{Outlier detection function (default: out)}

\item{eout}{Logical: remove (x,y) outliers (default: FALSE)}

\item{xlab}{Label for x-axis (default: "X")}

\item{ylab}{Label for y-axis (default: "Y")}

\item{zlab}{Label for z-axis (default: "")}

\item{pr}{Logical: print advisory messages about scale parameter (default: TRUE)}

\item{SEED}{Logical: set random seed for MVE (default: TRUE)}

\item{ticktype}{Type of tick marks for plot (default: "simple")}
}
\value{
"Done" if pyhat=FALSE, or vector of predicted values if pyhat=TRUE
}
\description{
Compute a 3D running interval smoother using trimmed means. Can create
perspective plots of the smoothed surface.
}
\details{
Computes a 3D running interval smooth using trimmed means. For each point,
finds nearest neighbors based on robust Mahalanobis distance (MVE), then
computes the trimmed mean of corresponding y values.

When plotit=TRUE and p=2, creates a perspective plot. The function prints
advisory messages about the scale parameter: scale=FALSE is typically best
for independent predictors, scale=TRUE for dependent predictors.

Missing values are automatically removed. Cannot have both eout=TRUE and
xout=TRUE simultaneously.
}
\seealso{
\code{\link{rung3d}}, \code{\link{run3bo}}, \code{\link{run3hat}}
}
\keyword{nonparametric}
\keyword{smooth}
