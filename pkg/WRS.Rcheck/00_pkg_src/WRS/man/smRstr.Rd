% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{smRstr}
\alias{smRstr}
\title{Estimate Explanatory Strength of Association Using Running Interval Smoother}
\usage{
smRstr(
  x,
  y,
  fr = 1,
  est = tmean,
  nmin = 1,
  varfun = winvar,
  xout = FALSE,
  outfun = outpro,
  ...
)
}
\arguments{
\item{x}{Matrix or vector of predictor values.}

\item{y}{Vector of response values.}

\item{fr}{Span (fraction of data) for the running interval smoother. Default is 1.}

\item{est}{Measure of location to be used. Default is \code{\link{tmean}} (20\% trimmed mean).}

\item{nmin}{Minimum number of points required in running interval. Default is 1.}

\item{varfun}{Measure of variation used when estimating prediction error.
Default is \code{\link{winvar}} (winsorized variance). Example: \code{varfun=pbvar}
would use the percentage bend measure of variation.}

\item{xout}{Logical. If \code{TRUE}, remove outliers from predictor space.
Default is \code{FALSE}.}

\item{outfun}{Outlier detection function. Default is \code{\link{outpro}}.}

\item{...}{Additional arguments passed to \code{est} and other functions.}
}
\value{
List with components:
\item{str}{Explanatory strength of the association (0 to 1)}
\item{pred.error}{Prediction error based on cross-validation}
}
\description{
Estimates the explanatory strength of an association (a generalization of
Pearson's correlation) based on a running interval smoother and leave-one-out
cross-validation. Also estimates prediction error.
}
\details{
The function uses leave-one-out cross-validation to estimate:
\enumerate{
\item Prediction error based on the specified variance function
\item Explanatory strength as sqrt((variance without predictor - prediction error) /
variance without predictor)
}

The explanatory strength ranges from 0 (no association) to 1 (perfect prediction),
providing a robust alternative to R-squared that generalizes to nonlinear relationships.
}
\seealso{
\code{\link{runhat}}, \code{\link{rung3hat}}, \code{\link{smstrcom}}
}
\keyword{regression}
\keyword{robust}
