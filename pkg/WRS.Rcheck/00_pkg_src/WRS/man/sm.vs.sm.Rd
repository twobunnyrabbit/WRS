% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{sm.vs.sm}
\alias{sm.vs.sm}
\title{Compare Two Smoothing Methods Visually}
\usage{
sm.vs.sm(
  x,
  y,
  method1 = "RUN",
  method2 = "RF",
  xout = FALSE,
  outfun = outpro,
  xlab = "Est1",
  ylab = "Est2",
  pch = ".",
  pr = TRUE,
  xoutL = FALSE,
  ...
)
}
\arguments{
\item{x}{Predictor variable vector}

\item{y}{Response variable vector}

\item{method1}{First smoothing method. Default is 'RUN' (running interval smoother)}

\item{method2}{Second smoothing method. Default is 'RF' (random forest smoother)}

\item{xout}{Logical. If \code{TRUE}, remove outliers in x before smoothing.
Default is \code{FALSE}.}

\item{outfun}{Outlier detection function. Default is \code{\link{outpro}}.}

\item{xlab}{Label for x-axis (predictions from method1). Default is 'Est1'.}

\item{ylab}{Label for y-axis (predictions from method2). Default is 'Est2'.}

\item{pch}{Plotting character for points. Default is '.'.}

\item{pr}{Logical. If \code{TRUE}, print messages. Default is \code{TRUE}.}

\item{xoutL}{Logical. If \code{TRUE}, check for outliers when creating the
plot. Default is \code{FALSE}.}

\item{...}{Additional arguments passed to \code{\link{smpred}}}
}
\value{
Creates a plot comparing the two smoothers (invisible NULL).
}
\description{
Plots predictions from two different smoothing methods against each other to
assess agreement. If smoothers give similar results, points should cluster
tightly around the line y=x (slope=1, intercept=0).
}
\details{
This function:
\enumerate{
\item Computes predictions using both smoothing methods via \code{\link{smpred}}
\item Creates scatter plot of method1 vs method2 predictions
\item Adds reference line y=x (dashed) for perfect agreement
}

Agreement between smoothers suggests robust results. Systematic deviations
indicate sensitivity to smoothing method choice.
}
\examples{
set.seed(123)
x <- 1:100
y <- 2*x + 10*rnorm(100)
sm.vs.sm(x, y, method1='RUN', method2='RF')
}
\seealso{
\code{\link{smpred}} for computing smooth predictions
}
