% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outliers.R
\name{prodepth}
\alias{prodepth}
\title{Compute Projection Depth (Fast Approximation)}
\usage{
prodepth(x, pts = x, ndir = 1000, SEED = TRUE)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{pts}{Points for which to compute depth (default: \code{x}).}

\item{ndir}{Number of random projections to use (default: 1000). Higher
values give more accurate approximations but take longer.}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}
}
\value{
Vector of projection depth values.
}
\description{
Computes an approximation of projection depth using random projections via
the DepthProc package. Much faster than \code{zoudepth()} but gives approximate
results.
}
\details{
Uses \code{depthProjection()} from the DepthProc package. Running the function
twice on the same data may give slightly different results unless \code{SEED=TRUE}
(default). This is the recommended depth function for most applications due
to its speed.

Requires the DepthProc package.
}
\seealso{
\code{\link{zoudepth}}, \code{\link{pdepth}}, \code{\link[DepthProc]{depthProjection}}
}
