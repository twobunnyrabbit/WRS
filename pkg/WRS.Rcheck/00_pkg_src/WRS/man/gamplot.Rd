% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression-advanced.R
\name{gamplot}
\alias{gamplot}
\title{Plot Regression Surface Using Generalized Additive Model}
\usage{
gamplot(
  x,
  y,
  sop = TRUE,
  pyhat = FALSE,
  eout = FALSE,
  xout = FALSE,
  outfun = out,
  plotit = TRUE,
  xlab = "X",
  ylab = "",
  zlab = "",
  theta = 50,
  phi = 25,
  expand = 0.5,
  scale = TRUE,
  ticktype = "simple"
)
}
\arguments{
\item{x}{A numeric matrix containing predictor values (maximum 4 columns).}

\item{y}{A numeric vector containing the response variable.}

\item{sop}{Logical. If \code{TRUE}, uses spline smoothing (s()) in the GAM;
if \code{FALSE}, uses linear terms (default: \code{TRUE}).}

\item{pyhat}{Logical. If \code{TRUE}, returns predicted values; if \code{FALSE},
returns "Done" message (default: \code{FALSE}).}

\item{eout}{Logical value. If \code{TRUE}, outliers are removed based on
eliminating points with large residuals or leverage (default: \code{FALSE}).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{xlab, ylab, zlab}{Axis labels for plots.}

\item{theta, phi}{Viewing angles for 3D perspective plot (defaults: 50, 25).}

\item{expand}{Expansion factor for 3D plot (default: 0.5).}

\item{scale}{Logical. If \code{TRUE}, scales the 3D plot (default: \code{TRUE}).}

\item{ticktype}{Type of tick marks for 3D plot (default: "simple").}
}
\value{
If \code{pyhat = TRUE}, returns fitted values from the GAM. If
\code{pyhat = FALSE}, returns "Done". Creates plots as a side effect.
}
\description{
Fits and plots a regression surface using a generalized additive model (GAM)
with optional spline smoothing. Supports up to 4 predictors with automatic
visualization for 1D and 2D cases.
}
\details{
This function fits a generalized additive model using the \code{mgcv} package:
\itemize{
\item With \code{sop = TRUE}: Uses smoothing splines s(x) for each predictor
\item With \code{sop = FALSE}: Uses linear terms for each predictor
}

The function automatically handles visualization:
\itemize{
\item 1 predictor: Line plot of fitted values vs. x
\item 2 predictors: 3D perspective plot of fitted surface
\item 3-4 predictors: No automatic plot, but returns fitted values
}

Outlier removal options:
\itemize{
\item \code{xout = TRUE}: Remove outliers from predictor space
\item \code{eout = TRUE}: Remove outliers from combined (x,y) space
\item Cannot have both \code{xout} and \code{eout} set to \code{TRUE}
}
}
\note{
Requires the \code{mgcv} package for GAM fitting and \code{akima} package for
3D surface interpolation.
}
\examples{
\dontrun{
# GAM with single predictor
set.seed(123)
x <- seq(-3, 3, length=100)
y <- sin(x) + rnorm(100, sd=0.2)
gamplot(x, y)

# GAM with two predictors
x <- matrix(rnorm(200), ncol=2)
y <- x[,1]^2 + x[,2]^2 + rnorm(100)
gamplot(x, y)
}
}
\references{
Wood, S.N. (2017). Generalized Additive Models: An Introduction with R (2nd ed.).
Chapman and Hall/CRC.
}
\seealso{
\code{\link{gamplotv2}}, \code{\link{gamplotINT}}, \code{\link{gamindt}},
\code{\link[mgcv]{gam}}
}
