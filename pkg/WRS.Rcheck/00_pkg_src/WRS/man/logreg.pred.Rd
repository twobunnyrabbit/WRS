% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression-advanced.R
\name{logreg.pred}
\alias{logreg.pred}
\title{Predict Probabilities from Logistic Regression}
\usage{
logreg.pred(
  x,
  y,
  pts = x,
  xout = FALSE,
  outfun = outpro,
  ROB = FALSE,
  ridge = FALSE
)
}
\arguments{
\item{x}{A numeric matrix or vector containing the training predictor values.}

\item{y}{A binary numeric vector (training response).}

\item{pts}{A numeric matrix or vector of predictor values at which to predict
probabilities (default: \code{x}, predicting at training points).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{ROB}{Logical. If \code{TRUE}, uses robust weighted logistic regression via
\code{wlogreg()}; if \code{FALSE}, uses standard logistic regression (default: \code{FALSE}).}

\item{ridge}{Logical. If \code{TRUE}, uses ridge logistic regression; if \code{FALSE},
uses standard or robust logistic regression (default: \code{FALSE}).}
}
\value{
A numeric vector of predicted probabilities P(Y=1|X) at each point in
\code{pts}, with values between 0 and 1.
}
\description{
Predicts the probability of success (Y=1) at specified predictor values using
a fitted logistic regression model, with options for robust estimation and
ridge regression.
}
\details{
This function provides a unified interface for predicting probabilities from
different types of logistic regression:
\itemize{
\item \strong{Standard} (\code{ROB=FALSE}, \code{ridge=FALSE}): Uses \code{logreg()} with
optional outlier detection
\item \strong{Robust} (\code{ROB=TRUE}, \code{ridge=FALSE}): Uses \code{wlogreg()} with
iteratively reweighted least squares
\item \strong{Ridge} (\code{ridge=TRUE}): Uses \code{logistic.ridge()} with L2
regularization
}

Predictions are computed using the inverse logit transformation:
\deqn{P(Y=1|X) = \frac{exp(\beta_0 + \beta_1 X_1 + ...)}{1 + exp(\beta_0 + \beta_1 X_1 + ...)}}

If \code{xout = TRUE}, outliers in the training predictors are removed before
model fitting using the function specified by \code{outfun}.
}
\note{
For single predictor models (p=2 coefficients), \code{pts} can be a vector.
For multiple predictors, \code{pts} must be a matrix with the same number of
columns as \code{x}.
}
\examples{
\dontrun{
# Predict probabilities at new points
set.seed(123)
x <- rnorm(100)
p <- exp(1 + 2*x) / (1 + exp(1 + 2*x))
y <- rbinom(100, 1, p)

# Predict at new x values
new_x <- seq(-2, 2, by=0.5)
pred_prob <- logreg.pred(x, y, pts=new_x)
plot(new_x, pred_prob, type='l')
}
}
\references{
Wilcox, R.R. (2022). Introduction to Robust Estimation and Hypothesis Testing
(5th ed.). Academic Press.
}
\seealso{
\code{\link{logreg}}, \code{\link{logreg.P.ci}}, \code{\link{wlogreg}},
\code{\link{logistic.ridge}}
}
