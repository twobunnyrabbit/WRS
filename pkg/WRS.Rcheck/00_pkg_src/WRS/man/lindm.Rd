% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{lindm}
\alias{lindm}
\title{Bootstrap Confidence Intervals for Linear Contrasts with M-Estimators (Dependent Groups)}
\usage{
lindm(x, con = 0, est = onestep, grp = 0, alpha = 0.05, nboot = 999, ...)
}
\arguments{
\item{x}{Data in list mode or matrix format. For list: x[\link{j}] contains data for group j.
For matrix: each column represents a group.}

\item{con}{J×d matrix of contrast coefficients, where J = number of groups and d = number
of contrasts. Default (con=0) performs all pairwise comparisons. Each column specifies
one contrast.}

\item{est}{Estimator function to use (default: \code{\link{onestep}} M-estimator).
Can be any location estimator that accepts additional arguments via ...}

\item{grp}{Vector specifying which groups to compare (default: 0 uses all groups).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{nboot}{Number of bootstrap samples (default: 999, increased from 399).}

\item{...}{Additional arguments passed to the estimator function.}
}
\value{
List with components:
\describe{
\item{psihat}{Matrix with columns: con.num, psihat (contrast estimate),
ci.lower, ci.upper, se (standard error)}
\item{crit}{Critical value from bootstrap distribution}
\item{con}{Contrast coefficient matrix used}
}
}
\description{
Computes bootstrap confidence intervals for linear contrasts involving M-estimators
of marginal distributions. Designed for dependent (repeated measures) groups.
}
\details{
This function:
\itemize{
\item Uses bootstrap resampling to estimate the sampling distribution
\item Centers data by subtracting the M-estimator before bootstrapping
\item Computes covariance matrix from bootstrap samples
\item Uses Hochberg's method for simultaneous inference across contrasts
\item Sets random seed to 2 for reproducibility
}

For example, con\link{,1}=c(1,1,-1,-1,0,0) tests whether the sum of the first two
M-estimators equals the sum of the second two.
}
\examples{
# Repeated measures with 4 time points
set.seed(123)
x <- matrix(rnorm(100), ncol = 4)
x[,2] <- x[,2] + 0.5

# All pairwise comparisons
result <- lindm(x, nboot = 500)

# Custom contrast: time 1 vs mean of times 2-4
con <- matrix(c(1, -1/3, -1/3, -1/3), ncol = 1)
result2 <- lindm(x, con = con, nboot = 500)
}
\seealso{
\code{\link{lindmsub}} (helper function), \code{\link{linpbg}} (independent groups),
\code{\link{onestep}} (default estimator)
}
