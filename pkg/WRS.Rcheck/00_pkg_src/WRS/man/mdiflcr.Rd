% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{mdiflcr}
\alias{mdiflcr}
\title{Multivariate Difference Location Comparison (Two Independent Groups)}
\usage{
mdiflcr(
  m1,
  m2,
  tr = 0.5,
  nullv = rep(0, ncol(m1)),
  plotit = TRUE,
  SEED = TRUE,
  pop = 1,
  fr = 0.8,
  nboot = 600
)
}
\arguments{
\item{m1}{Data matrix for group 1 (n1 by p).}

\item{m2}{Data matrix for group 2 (n2 by p).}

\item{tr}{Trimming proportion for robust location estimate. Default is 0.5 (median).}

\item{nullv}{Null hypothesis vector. Default is zero vector.}

\item{plotit}{Logical. If \code{TRUE} and p=2, create plot. Default is \code{TRUE}.}

\item{SEED}{Logical. If \code{TRUE}, set random seed to 2. Default is \code{TRUE}.}

\item{pop}{Plotting style (if \code{plotit=TRUE}): 1=scatterplot, 2=expected frequency
curve, 3=adaptive kernel density. Default is 1.}

\item{fr}{Span parameter for plotting methods 2 and 3. Default is 0.8.}

\item{nboot}{Number of bootstrap samples. Default is 600.}
}
\value{
List with components:
\item{p.value}{Bootstrap p-value}
\item{center}{Estimated center of the difference distribution}
}
\description{
Test whether the marginal medians of differences \eqn{D = X - Y} equal zero for
two independent groups using a bootstrap procedure with Mahalanobis distance.
Multivariate analog of Wilcoxon-Mann-Whitney test.
}
\details{
For two independent groups, forms difference matrix D where each row is a difference
between a random observation from group 1 and a random observation from group 2.
Tests whether the marginal trimmed means (or medians when tr=0.5) of D equal the
null hypothesis values.

The test uses percentile bootstrap with Mahalanobis distance. Only α=0.05 is
supported for the test.

When p=2 and \code{plotit=TRUE}, creates a plot showing the distribution of
differences with the center and null hypothesis point marked.
}
\seealso{
\code{\link{lloc}}, \code{\link{mdifloc}}
}
\keyword{htest}
\keyword{multivariate}
