% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{adrunl}
\alias{adrunl}
\title{Additive Model Using Loess Smoother and Backfitting}
\usage{
adrunl(
  x,
  y,
  est = tmean,
  iter = 10,
  pyhat = FALSE,
  plotit = TRUE,
  fr = 0.8,
  xlab = "x1",
  ylab = "x2",
  zlab = "",
  theta = 50,
  phi = 25,
  expand = 0.5,
  scale = FALSE,
  zscale = TRUE,
  xout = FALSE,
  outfun = out,
  ticktype = "simple",
  ...
)
}
\arguments{
\item{x}{Matrix of predictor variables (n by p).}

\item{y}{Vector of response values.}

\item{est}{Estimator function for centering (default: \code{\link{tmean}}).}

\item{iter}{Maximum number of backfitting iterations (default: 10).}

\item{pyhat}{Logical. If \code{TRUE}, returns predicted values; if \code{FALSE}, returns "Done".}

\item{plotit}{Logical. If \code{TRUE} and p=2, creates 3D perspective plot of fitted surface.}

\item{fr}{Span parameter for loess smoother (default: 0.8). Fraction of data used in local smoothing.}

\item{xlab}{Label for first predictor axis (default: "x1").}

\item{ylab}{Label for second predictor axis (default: "x2").}

\item{zlab}{Label for response axis (default: "").}

\item{theta}{Viewing angle for perspective plot (default: 50).}

\item{phi}{Viewing angle for perspective plot (default: 25).}

\item{expand}{Expansion factor for z-axis (default: 0.5).}

\item{scale}{Logical. If \code{TRUE}, scales z-axis to same range as x-y (default: FALSE).}

\item{zscale}{Logical (not currently used).}

\item{xout}{Logical (not currently used).}

\item{outfun}{Outlier detection function (default: \code{\link{out}}, not currently used).}

\item{ticktype}{Tick mark style for 3D plot (default: "simple").}

\item{...}{Additional arguments passed to \code{est}.}
}
\value{
If \code{pyhat=TRUE}, returns vector of predicted values.
If \code{pyhat=FALSE}, returns character string "Done".
}
\description{
Fits an additive regression model using loess smoothing and the backfitting
algorithm. For models of the form \eqn{Y = f_1(X_1) + ... + f_p(X_p) + e},
estimates each function iteratively.
}
\details{
Uses the backfitting algorithm to fit additive models:
\enumerate{
\item Initialize each function \eqn{f_j} using loess smoother
\item Iterate until convergence:
\itemize{
\item For each predictor j, compute residuals from other predictors
\item Update \eqn{f_j} by smoothing residuals against \eqn{X_j}
}
\item Convergence criterion: change in normalized fitted values < 0.01
}

For bivariate predictors (p=2), optionally creates 3D perspective plot of fitted surface
using \code{\link[akima]{interp}}.
}
\references{
Hastie, T. J., & Tibshirani, R. J. (1990). Generalized Additive Models. Chapman & Hall.
}
\seealso{
\code{\link{adrun}}, \code{\link{adtest}}, \code{\link{adtestl}}, \code{\link{lplot}}
}
\keyword{regression}
\keyword{robust}
\keyword{smooth}
