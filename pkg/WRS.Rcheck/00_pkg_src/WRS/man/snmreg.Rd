% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression.R
\name{snmreg}
\alias{snmreg}
\title{Skipped Nelder-Mead Regression S-Estimator}
\usage{
snmreg(x, y, SEED = TRUE, xout = FALSE, outfun = outpro, initreg = MMreg, ...)
}
\arguments{
\item{x}{A numeric matrix of predictor variables (n by p).}

\item{y}{A numeric vector of the dependent variable (length n).}

\item{SEED}{Logical; if \code{TRUE} (default), sets random seed for reproducibility
(currently not used, retained for compatibility).}

\item{xout}{Logical. Not used in this function but included to avoid conflicts
when using with other functions like \code{regci} (default: \code{FALSE}).}

\item{initreg}{Initial regression function for starting values (default: \code{MMreg}).
Must return coefficients in \code{$coef}.}

\item{...}{Additional arguments passed to \code{outfun} if \code{xout=TRUE}.}
}
\value{
A list with components:
\describe{
\item{coef}{Vector of regression coefficients: \code{c(intercept, slopes)}.}
\item{residuals}{Residuals from the fit.}
}
}
\description{
Computes a regression S-estimator using the Nelder-Mead optimization method.
The S-estimator minimizes a robust scale measure (percentage bend midvariance)
of residuals, providing high breakdown point protection.
}
\details{
S-estimators minimize a robust scale of residuals:
\deqn{\min_{\beta} s(r_1, \ldots, r_n)}
where \eqn{r_i = y_i - x_i'\beta} are residuals and \eqn{s} is a robust
scale estimator (percentage bend midvariance).

This implementation:
\enumerate{
\item Optionally removes outliers using projection-based detection
\item Gets initial values from \code{initreg} (default: MM-regression)
\item Uses Nelder-Mead simplex method to minimize scale of residuals
\item Computes intercept as median of residuals
}

\strong{Properties}:
\itemize{
\item High breakdown point (up to 50\%)
\item Robust to outliers in both x and y
\item Affine equivariant
\item Computationally intensive due to optimization
}

The "skipped" aspect refers to optional outlier removal before fitting.
}
\examples{
\dontrun{
# Simple S-regression
set.seed(123)
x <- matrix(rnorm(50), ncol=1)
y <- 2*x + rnorm(50, sd=0.5)
snmreg(x, y)

# With outliers
y[1:5] <- y[1:5] + 10
snmreg(x, y)

# Multiple regression with outlier removal
x <- matrix(rnorm(100), ncol=2)
y <- x[,1] + 2*x[,2] + rnorm(50)
snmreg(x, y, xout=TRUE)
}
}
\references{
Rousseeuw, P.J. & Yohai, V.J. (1984). Robust regression by means of
S-estimators. \emph{Robust and Nonlinear Time Series Analysis}, 256-272.

Wilcox, R.R. (2022). \emph{Introduction to Robust Estimation and Hypothesis Testing}
(5th ed.). Academic Press.
}
\seealso{
\code{\link{MMreg}} for MM-regression, \code{\link{ltsreg}} for LTS,
\code{\link{outpro}} for outlier detection

Other robust regression functions: 
\code{\link{LMSreg}()},
\code{\link{MMreg}()},
\code{\link{bireg}()},
\code{\link{bmreg}()},
\code{\link{chreg}()},
\code{\link{depreg}()},
\code{\link{mbmreg}()},
\code{\link{mdepreg}()},
\code{\link{mopreg}()},
\code{\link{tsgreg}()},
\code{\link{winreg}()},
\code{\link{wreg}()}
}
\concept{robust regression functions}
