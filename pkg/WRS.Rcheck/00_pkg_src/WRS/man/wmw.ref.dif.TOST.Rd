% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{wmw.ref.dif.TOST}
\alias{wmw.ref.dif.TOST}
\title{Test Symmetry with TOST (Two One-Sided Tests) Extension}
\usage{
wmw.ref.dif.TOST(
  x,
  y,
  q = 0.25,
  pts = NULL,
  nboot = 1000,
  alpha = 0.05,
  SEED = TRUE,
  plotit = T,
  xlab = "Difference",
  ylab = "Density",
  estfun = hdmq,
  plotfun = function(...) NULL,
  eqbound = NULL
)
}
\arguments{
\item{x}{Observations from group 1.}

\item{y}{Observations from group 2.}

\item{q}{Quantile for reference point if pts=NULL (default: 0.25).}

\item{pts}{Reference point (default: NULL for quantile-based).}

\item{nboot}{Number of bootstrap samples (default: 1000).}

\item{alpha}{Significance level (default: 0.05).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed (default: \code{TRUE}).}

\item{plotit}{Logical. If \code{TRUE}, creates plot (default: \code{TRUE}).}

\item{xlab}{X-axis label (default: 'Difference').}

\item{ylab}{Y-axis label (default: 'Density').}

\item{estfun}{Quantile estimator (default: \code{hdmq}).}

\item{plotfun}{Plotting function (default: NULL function).}

\item{eqbound}{Equivalence bound for TOST (default: NULL for no TOST).}
}
\value{
A list with symmetry test results and optionally TOST results:
\itemize{
\item \code{L}, \code{U}, \code{Est.dif}: As in \code{wmw.ref.dif}
\item \code{ci90}, \code{ci95}: 90\% and 95\% confidence intervals
\item \code{p.value}: P-value for difference test
\item \code{TOST}: TOST equivalence test results (if eqbound specified)
}
}
\description{
Tests symmetry of X-Y distribution with TOST equivalence testing extension.
Can test both for difference from zero and equivalence to zero.
}
\details{
Extends \code{wmw.ref.dif} with TOST procedure for testing equivalence.
Provides both 90\% CIs (for TOST) and 95\% CIs (for regular inference).
}
\examples{
\dontrun{
# Test with TOST
set.seed(123)
x <- rnorm(50)
y <- rnorm(50, mean = 0.05)
wmw.ref.dif.TOST(x, y, eqbound = 0.1, nboot = 500)
}
}
\seealso{
\code{\link{wmw.ref.dif}}, \code{\link{wmw.ref.mul.TOST}}
}
