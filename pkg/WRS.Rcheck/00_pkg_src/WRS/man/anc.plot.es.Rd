% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{anc.plot.es}
\alias{anc.plot.es}
\title{Plot Effect Size as Function of Covariate}
\usage{
anc.plot.es(
  x1,
  y1,
  x2,
  y2,
  fr1 = 1,
  fr2 = 1,
  tr = 0.2,
  alpha = 0.05,
  plotit = TRUE,
  pts = x1,
  method = "QS",
  CI = FALSE,
  pr = TRUE,
  xout = FALSE,
  outfun = out,
  xlab = "X",
  ylab = "Effect.Size",
  pch = "*",
  pts.only = TRUE,
  low.span = 2/3,
  nmin = 12,
  ...
)
}
\arguments{
\item{x1, y1}{Covariate and outcome for group 1.}

\item{x2, y2}{Covariate and outcome for group 2.}

\item{fr1, fr2}{Spans for nearest neighbor selection (default: 1).}

\item{tr}{Trimming proportion (default: 0.2). Proportion of observations to
trim from each tail when computing winsorized correlation.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{pts}{Design points where effect sizes are computed (default: x1 values).}

\item{method}{Effect size measure: 'QS' (quantile shift, default), 'EP'
(explanatory power), 'AKP' (robust Cohen's d), 'WMW' (P(X<Y)), 'KMS'.}

\item{CI}{Logical. If \code{TRUE}, computes confidence intervals and p-values.}

\item{pr}{Logical. If \code{TRUE}, prints progress messages (default: \code{TRUE}).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{xlab, ylab}{Axis labels.}

\item{pch}{Plotting character (default: '*').}

\item{pts.only}{Logical. If \code{TRUE} (default), plots only the estimates.
If \code{FALSE}, adds LOESS smoother.}

\item{low.span}{Span parameter for LOESS smoother (default: 2/3).}

\item{nmin}{Minimum number of neighbors required at each design point (default: 12).}

\item{...}{Additional arguments.}
}
\value{
If \code{CI = FALSE}, returns 'Done'. If \code{CI = TRUE}, returns matrix
with columns: X, Est, ci.low, ci.hi, p.value.
}
\description{
Creates an effect size curve showing how the effect size varies across
covariate values. Uses running interval smoothing and optionally adds
confidence intervals.
}
\details{
For each design point where both groups have ≥ nmin observations nearby,
computes an effect size using the specified method. The default 'QS'
(quantile shift) method provides a robust measure of effect size.
}
\examples{
\dontrun{
x1 <- rnorm(50); y1 <- x1 + rnorm(50)
x2 <- rnorm(50); y2 <- x2 + 0.5 + rnorm(50)
anc.plot.es(x1, y1, x2, y2, method = 'QS', CI = TRUE)
}
}
