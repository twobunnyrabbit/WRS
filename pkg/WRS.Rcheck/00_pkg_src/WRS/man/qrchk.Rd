% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{qrchk}
\alias{qrchk}
\title{Test of Linear Fit Based on Quantile Regression}
\usage{
qrchk(
  x,
  y,
  qval = 0.5,
  q = NULL,
  nboot = 1000,
  com.pval = FALSE,
  SEED = TRUE,
  alpha = 0.05,
  pr = TRUE,
  xout = FALSE,
  outfun = out,
  chk.table = FALSE,
  MC = FALSE,
  ...
)
}
\arguments{
\item{x}{Numeric matrix or vector of predictor variable(s)}

\item{y}{Numeric vector of response variable}

\item{qval}{Numeric value specifying the quantile (default: 0.5)}

\item{q}{Alternative parameter for qval (overrides qval if not NULL)}

\item{nboot}{Integer specifying number of bootstrap/simulation samples (default: 1000)}

\item{com.pval}{Logical; if TRUE, compute p-value via simulation (default: FALSE)}

\item{SEED}{Logical; if TRUE, set random seed for reproducibility (default: TRUE)}

\item{alpha}{Numeric value for significance level (default: 0.05)}

\item{pr}{Logical; if TRUE, print informative messages (default: TRUE)}

\item{xout}{Logical; if TRUE, remove outliers in predictors (default: FALSE)}

\item{outfun}{Function for outlier detection (default: \code{\link{out}})}

\item{chk.table}{Logical; currently not used (default: FALSE)}

\item{MC}{Logical; if TRUE, use multicore processing for simulations (default: FALSE)}

\item{...}{Additional arguments passed to \code{outfun}}
}
\value{
List with components:
\item{test.stat}{Test statistic value}
\item{crit.value}{Critical value for the test}
\item{p.value}{P-value (NULL unless com.pval=TRUE)}
\item{Decision}{Character string: "Reject" or "Fail to reject"}
}
\description{
Tests the adequacy of a linear quantile regression fit using the method of
He and Zhu (2003, JASA, 98, 1013-1022). Uses approximate critical values for
efficiency, or simulations for p-value computation when requested.
}
\details{
For n<=400, p<=6, and qval=0.5, the function uses tabled critical values for
alpha in {0.1, 0.05, 0.025, 0.01}. Otherwise, it computes critical values via
simulation. Set com.pval=TRUE to obtain a p-value, which requires more computation.
Multicore processing (MC=TRUE) can speed up simulations substantially.
}
\references{
He, X., & Zhu, L. (2003). A lack-of-fit test for quantile regression.
Journal of the American Statistical Association, 98, 1013-1022.
}
\seealso{
\code{\link{qrchkv2}}, \code{\link{rqfit}}, \code{\link{Qregci}}
}
\keyword{htest}
\keyword{regression}
