% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{mwmw}
\alias{mwmw}
\title{Multivariate Analog of Wilcoxon-Mann-Whitney Test}
\usage{
mwmw(
  m1,
  m2,
  cop = 5,
  pr = TRUE,
  plotit = TRUE,
  pop = 1,
  fr = 0.8,
  op = 1,
  dop = 1
)
}
\arguments{
\item{m1}{Matrix or data frame for group 1 (must have 2+ columns).}

\item{m2}{Matrix or data frame for group 2 (must have 2+ columns).}

\item{cop}{Method for computing the center when approximating halfspace depth
(default: 5):
\itemize{
\item 1 = Halfspace median
\item 2 = MCD (Minimum Covariance Determinant)
\item 3 = Marginal medians
\item 4 = MVE (Minimum Volume Ellipsoid)
\item 5 = Skipped mean
}}

\item{pr}{Logical. If \code{TRUE}, prints critical values and guidance (default: \code{TRUE}).}

\item{plotit}{Logical. If \code{TRUE}, creates a plot (default: \code{TRUE}).}

\item{pop}{Plot type when \code{plotit = TRUE} (default: 1):
\itemize{
\item 1 = Scatterplot
\item 2 = Expected frequency curve
\item 3 = Adaptive kernel density
}}

\item{fr}{Fraction parameter for plotting (default: 0.8).}

\item{op}{Operation method (default: 1). Controls depth calculation approach.}

\item{dop}{Depth operation method for halfspace depth approximation (default: 1):
\itemize{
\item 1 = Method A1 approximation
\item 2 = Method A2 approximation
}}
}
\value{
A list with components:
\itemize{
\item \code{phat}: Effect size measure (relative depth of zero vector)
\item \code{center}: Center of the difference distribution
\item \code{crit.val}: Critical values for alpha = 0.1, 0.05, 0.025, 0.01
}
}
\description{
Computes a multivariate effect size measure analogous to the WMW test
using halfspace depth. Provides a distribution-free way to compare
two multivariate groups.
}
\details{
This function extends the WMW test to multivariate data using halfspace depth.
The approach:
\enumerate{
\item Computes all pairwise differences between groups: D_ij = X_i - Y_j
\item Calculates the halfspace depth of the zero vector relative to these differences
\item The effect size \code{phat} is the relative depth of zero
}

\strong{Interpretation}:
\itemize{
\item \code{phat} near 1 indicates the groups are similar (zero is central to differences)
\item \code{phat} near 0 indicates the groups differ substantially
\item Reject H0 (groups equal) if \code{phat} <= critical value
}

Critical values are provided for alpha levels 0.1, 0.05, 0.025, and 0.01,
based on the asymptotic distribution. The test statistic approximately
follows a normal distribution for large samples.

When plotting (\code{plotit = TRUE}), the function displays the distribution of
pairwise differences, with the center marked by "o" and the zero vector by "+".
}
\note{
For univariate data (single column), use \code{cid} or \code{bmp} instead.
Data must have matching numbers of columns in both groups.
}
\examples{
\dontrun{
# Generate bivariate data
set.seed(123)
m1 <- cbind(rnorm(40), rnorm(40))
m2 <- cbind(rnorm(40, mean = 0.3), rnorm(40, mean = 0.2))

# Multivariate WMW test
result <- mwmw(m1, m2)
print(result)

# Use MCD center
mwmw(m1, m2, cop = 2)

# With adaptive kernel density plot
mwmw(m1, m2, pop = 3)
}
}
\seealso{
\code{\link{mulwmw}} for projection-based multivariate WMW,
\code{\link{cid}} for univariate Cliff's analog,
\code{\link{dmean}} for Donoho-Gasko median
}
