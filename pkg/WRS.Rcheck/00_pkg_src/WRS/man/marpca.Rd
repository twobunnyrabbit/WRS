% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{marpca}
\alias{marpca}
\title{Maronna Robust Principal Component Analysis}
\usage{
marpca(
  x,
  p = ncol(x) - 1,
  N1 = 3,
  N2 = 2,
  tol = 0.001,
  N2p = 10,
  Nran = 50,
  Nkeep = 10,
  SEED = TRUE,
  LSCALE = TRUE,
  SCORES = FALSE
)
}
\arguments{
\item{x}{Data matrix (n by m).}

\item{p}{Number of principal components to retain (< m). Default is \code{ncol(x)-1}.}

\item{N1}{Number of initial C-step iterations. Default is 3.}

\item{N2}{Number of eigenvalue update iterations. Default is 2.}

\item{tol}{Convergence tolerance for proportion of unexplained variance. Default is 0.001.}

\item{N2p}{Number of refinement iterations for top candidates. Default is 10.}

\item{Nran}{Number of random starts. Default is 50.}

\item{Nkeep}{Number of top candidates to refine. Default is 10.}

\item{SEED}{Logical. If \code{TRUE}, set random seed to 2 for reproducibility. Default is \code{TRUE}.}

\item{LSCALE}{Logical. If \code{TRUE}, use L-scale estimator; if \code{FALSE}, use M-scale. Default is \code{TRUE}.}

\item{SCORES}{Logical. If \code{TRUE}, compute principal component scores. Default is \code{FALSE}.}
}
\value{
List with components:
\item{B}{(m-p) by m matrix defining orthogonal complement of principal subspace}
\item{a}{Constant vector for manifold equation}
\item{var.op}{Optimal proportion of unexplained variance}
\item{wt.cov}{Weighted covariance matrix}
\item{wt.mu}{Weighted mean vector (when \code{p=0} or \code{SCORES=TRUE})}
\item{scores}{Principal component scores matrix (n by p) if \code{SCORES=TRUE}}
}
\description{
Robust principal component analysis based on Maronna's (2005) method. Finds a
p-dimensional manifold that minimizes orthogonal distances using iterative
reweighting and random initialization.
}
\details{
This function implements Maronna's (2005) robust PCA method. The manifold is
characterized by equation \eqn{Bx = a}, where B is a (m-p) by m matrix defining
the orthogonal complement of the principal subspace, and a is a constant vector.

Algorithm overview:
\enumerate{
\item Generate \code{Nran} random initial subspaces
\item For each start, run N1+N2 iterations to find local optimum
\item Keep the \code{Nkeep} best solutions based on unexplained variance
\item Refine these candidates with \code{N2p} additional iterations
\item Return solution with minimum proportion of unexplained variance
}

The method is resistant to outliers and provides robust estimates even with
contaminated data. When \code{p=0}, returns weighted mean and covariance only.
}
\references{
Maronna, R. A. (2005). Principal components and orthogonal regression based on
robust scales. \emph{Technometrics}, \emph{47}(3), 264-273.
}
\seealso{
\code{\link{marpca.sub}}, \code{\link{Rpca}}, \code{\link{spca}}
}
\keyword{multivariate}
\keyword{robust}
