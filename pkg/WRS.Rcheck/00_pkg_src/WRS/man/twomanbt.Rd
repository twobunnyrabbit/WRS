% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{twomanbt}
\alias{twomanbt}
\title{Multivariate Two-Sample Behrens-Fisher Test Using Bootstrap}
\usage{
twomanbt(x, y, tr = 0.2, alpha = 0.05, nboot = 599)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{tr}{Trimming proportion (default: 0.2). Proportion of observations to
trim from each tail when computing winsorized correlation.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{nboot}{Number of bootstrap samples (default varies by function).}
}
\value{
A list with components:
\itemize{
\item \code{psihat}: Matrix with columns: \code{con.num} (contrast number),
\code{psihat} (difference in trimmed means), \code{ci.lower}, \code{ci.upper}
\item \code{teststat}: Matrix with test statistics and standard errors
\item \code{critical.value}: Bootstrap critical value for simultaneous inference
}
}
\description{
Compares trimmed means between two independent groups for multiple measures
(multivariate Behrens-Fisher problem). Uses the percentile-t bootstrap
method with simultaneous confidence intervals controlling familywise error rate.
}
\details{
For each of two independent groups with p measures per subject, this function
compares the trimmed means of the first measure, the second measure, and so on,
resulting in p comparisons between the two groups.

The percentile-t bootstrap method is used to compute simultaneous confidence
intervals with family-wise error control. By default, 20\% trimming is used
with 599 bootstrap samples.

Data can be provided as n-by-p matrices or in list mode. Missing values are
not allowed.
}
\examples{
\dontrun{
# Create multivariate data for two groups
set.seed(123)
g1 <- matrix(rnorm(50 * 3), ncol = 3)  # 50 subjects, 3 measures
g2 <- matrix(rnorm(60 * 3, mean = 0.3), ncol = 3)

# Compare trimmed means for all measures
result <- twomanbt(g1, g2)
print(result$psihat)
}
}
