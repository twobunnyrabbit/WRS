% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{regplot}
\alias{regplot}
\title{General Regression Plot (1D or 2D)}
\usage{
regplot(
  x,
  y,
  regfun = tsreg,
  xlab = "X",
  ylab = "Y",
  zlab = "Z",
  xout = FALSE,
  outfun = out,
  theta = 50,
  phi = 25,
  ticktype = "simple",
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix of predictor values (1 or 2 columns only).}

\item{y}{Numeric vector of response values.}

\item{regfun}{Regression function to use for fitting (default: \code{tsreg}).}

\item{xlab}{Label for x-axis in plots (default: "X" or "VAR 1").}

\item{ylab}{Label for y-axis in plots (default: "Y" or "VAR 2").}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{theta}{Viewing angle (rotation around z-axis) for 3D plots (default: 50).}

\item{phi}{Viewing angle (colatitude) for 3D plots (default: 25).}

\item{ticktype}{Tick mark style for 3D plots (default: 'simple').}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
Invisibly returns \code{NULL} (1D) or output from \code{rplot()} (2D).
}
\description{
Creates a scatter plot with fitted regression line (1 predictor) or
3D regression surface (2 predictors). Automatically adapts to number
of predictors.
}
\details{
For 1 predictor: Creates 2D scatter plot with regression line.
For 2 predictors: Creates 3D scatter plot with regression surface using \code{rplot()}.
Maximum 2 predictors allowed.
}
\examples{
# 1D regression plot
x <- rnorm(50)
y <- 2*x + rnorm(50)
regplot(x, y)
}
\seealso{
\code{\link{regp2plot}}, \code{\link{rplot}}
}
