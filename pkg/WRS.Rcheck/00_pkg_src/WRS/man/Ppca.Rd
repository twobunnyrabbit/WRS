% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{Ppca}
\alias{Ppca}
\title{Projection-Based Robust Principal Component Analysis}
\usage{
Ppca(
  x,
  p = ncol(x) - 1,
  locfun = L1medcen,
  loc.val = NULL,
  SCORES = FALSE,
  gvar.fun = cov.mba,
  pr = TRUE,
  SEED = TRUE,
  gcov = rmba,
  SCALE = TRUE,
  ...
)
}
\arguments{
\item{x}{Numeric matrix with observations in rows.}

\item{p}{Numeric. Number of principal components to extract (default: ncol(x) - 1).}

\item{locfun}{Function for robust location estimation used to center data
(default: \code{\link{L1medcen}} - spatial median).}

\item{loc.val}{Numeric vector. Pre-specified location value, or \code{NULL} to estimate.}

\item{SCORES}{Logical. If \code{TRUE}, return projected scores; if \code{FALSE}, return
only projection matrix (default: \code{FALSE}).}

\item{gvar.fun}{Function for generalized variance (kept for compatibility, not used).}

\item{pr}{Logical. Print progress messages (default: \code{TRUE}).}

\item{SEED}{Logical. Set random seed for reproducibility (default: \code{TRUE}).}

\item{gcov}{Function for robust location and scatter estimation (default: \code{\link{rmba}}).}

\item{SCALE}{Logical. If \code{TRUE}, scale variables using robust scatter estimate;
if \code{FALSE}, only center (default: \code{TRUE}).}

\item{...}{Additional arguments passed to \code{locfun}.}
}
\value{
List with components:
\itemize{
\item \code{B}: p × m projection matrix (orthonormalized)
\item \code{gen.sd}: Square root of robust generalized variance
\item \code{scores}: n × p matrix of projected scores (if \code{SCORES=TRUE}), otherwise \code{NULL}
}
}
\description{
Robust PCA that finds projection scores maximizing robust generalized variance.
Reduces data from m dimensions to p dimensions (default: m-1) using optimization
over projection directions.
}
\details{
Uses Nelder-Mead optimization (\code{\link{nelderv2}}) to find projections maximizing
robust generalized variance \code{\link{gvarg}}. Initialization uses standard robust PCA
via \code{\link{robpca}}. The projection matrix is orthonormalized using \code{\link{ortho}}.

When \code{SCALE=TRUE}, data are centered using \code{gcov} and scaled by marginal robust
standard deviations. When \code{FALSE}, only centering via \code{locfun} is applied.
}
\examples{
\dontrun{
# Robust PCA reducing to 2 dimensions
x <- matrix(rnorm(100 * 5), ncol = 5)
result <- Ppca(x, p = 2, SCORES = TRUE)
plot(result$scores)
}

}
\seealso{
\code{\link{Ppca.summary}}, \code{\link{robpca}}, \code{\link{spca}},
\code{\link{gvarg}}, \code{\link{L1medcen}}
}
\keyword{multivariate}
\keyword{robust}
