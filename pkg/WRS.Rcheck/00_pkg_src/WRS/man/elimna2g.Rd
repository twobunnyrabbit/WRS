% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{elimna2g}
\alias{elimna2g}
\title{Remove Missing Values Pairwise from Two Groups/Matrices}
\usage{
elimna2g(x, y)
}
\arguments{
\item{x}{Numeric matrix or list. First group data.}

\item{y}{Numeric matrix or list. Second group data. Must have same structure as \code{x}.}
}
\value{
List with components:
\describe{
\item{x}{Data from x with missing cases removed}
\item{y}{Data from y with corresponding cases removed}
}
}
\description{
Removes missing values pairwise from two paired matrices or lists, ensuring
corresponding observations are removed from both groups simultaneously.
}
\details{
This function performs pairwise deletion of missing values for dependent/paired data:

\strong{For matrices}:
\itemize{
\item Combines x and y columnwise
\item Removes any row containing NA in either matrix
\item Splits back into separate x and y with matching rows
}

\strong{For lists}:
\itemize{
\item Combines corresponding list elements
\item Removes observations with NA in either group
\item Returns lists with synchronized complete cases
}

This is essential for dependent samples analyses where observations must be paired
(e.g., repeated measures, matched samples). Standard \code{na.omit} would remove
cases independently from each group, breaking the pairing.
}
\examples{
# Matrix example
x <- matrix(c(1, 2, NA, 4, 5, 6), ncol = 2)
y <- matrix(c(7, 8, 9, 10, 11, NA), ncol = 2)
result <- elimna2g(x, y)
result$x  # Row 2 removed from both
result$y

# List example
x_list <- list(c(1, 2, NA), c(4, 5, 6))
y_list <- list(c(7, 8, 9), c(10, NA, 12))
result <- elimna2g(x_list, y_list)
}
\seealso{
\code{\link{elimna}}, \code{\link{na.omit}}
}
\keyword{manip}
