% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{split.mat}
\alias{split.mat}
\title{Split Data into Groups Based on Quantiles}
\usage{
\method{split}{mat}(x, y = NULL, xcol = 1, q = c(0.25, 0.5, 0.75), vals = NULL)
}
\arguments{
\item{x}{Numeric matrix or vector of predictor variable(s).}

\item{y}{Numeric vector of response variable, or \code{NULL}. If provided, will be
column-bound with \code{x}. Default is \code{NULL}.}

\item{xcol}{Integer. Which column of \code{x} to use for splitting (if \code{x} is
a matrix). Default is 1.}

\item{q}{Numeric vector of quantiles for splitting. Default is \code{c(.25, .5, .75)}
(quartiles), creating 4 groups.}

\item{vals}{Numeric vector of explicit cutpoints, or \code{NULL}. If provided, uses
these values instead of quantiles. Default is \code{NULL} (use quantiles).}
}
\value{
List of length J+1, where each element contains the response values for
observations in that group.
}
\description{
Splits response data into groups based on quantile ranges of a predictor variable.
Useful for stratified analysis or examining relationships across predictor levels.
}
\details{
Creates J+1 groups where J = length(q) (or length(vals)), with boundaries defined by:
\itemize{
\item If \code{vals=NULL}: uses quantiles of x\link{,xcol} specified by \code{q}
\item If \code{vals} provided: uses explicit cutpoints
}

Groups are formed as: [min, cutpoint₁), [cutpoint₁, cutpoint₂), ..., [cutpointⱼ, max)

Returns values from the last column of the combined data matrix (the response variable).
}
\seealso{
\code{\link{qest}}
}
\keyword{manip}
