% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{Dancovapts}
\alias{Dancovapts}
\title{Dependent Groups ANCOVA at User-Specified Design Points}
\usage{
Dancovapts(
  x1,
  y1,
  x2,
  y2,
  fr1 = 1,
  fr2 = 1,
  tr = 0.2,
  alpha = 0.05,
  plotit = TRUE,
  pts = NA,
  sm = FALSE,
  xout = FALSE,
  outfun = out,
  DIF = FALSE,
  LP = TRUE,
  ...
)
}
\arguments{
\item{pts}{Design points vector where comparisons should be made.}

\item{sm}{Logical. Use smoothed estimates via bootstrap bagging. Default is FALSE.}

\item{DIF}{Logical. If TRUE, use trimmed means of difference scores; if FALSE, compare marginal trimmed means. Default is FALSE.}

\item{LP}{Logical. Use linear programming for control. Default is TRUE.}

\item{...}{Additional arguments.}
}
\value{
Matrix with test results for each design point.
}
\description{
Compare two dependent groups using nonparametric ANCOVA at user-specified design points.
Uses running interval smoother without parametric regression assumptions.
}
\details{
User specifies exact design points where regression lines are compared.
At each point, identifies observations from both conditions nearest to that covariate value.
For those matched observations, either compares marginal trimmed means (DIF=FALSE)
or analyzes trimmed mean of difference scores (DIF=TRUE).
}
\examples{
\dontrun{
  x <- rnorm(30)
  y1 <- x + rnorm(30)
  y2 <- x + 0.5 + rnorm(30)
  result <- Dancovapts(x, y1, x, y2, pts = c(-1, 0, 1))
}
}
\seealso{
\code{\link{Dancova}}, \code{\link{ancova}}
}
\keyword{ancova}
\keyword{dependent}
