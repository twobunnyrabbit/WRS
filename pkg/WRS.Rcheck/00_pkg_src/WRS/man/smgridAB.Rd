% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{smgridAB}
\alias{smgridAB}
\title{Pairwise Comparisons of Main Effects with Two-Variable Splitting}
\usage{
smgridAB(
  x,
  y,
  IV = c(1, 2),
  Qsplit1 = 0.5,
  Qsplit2 = 0.5,
  tr = 0.2,
  VAL1 = NULL,
  VAL2 = NULL,
  PB = FALSE,
  est = tmean,
  nboot = 1000,
  pr = TRUE,
  fun = ES.summary,
  xout = FALSE,
  outfun = outpro,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{x}{Matrix or data frame of predictor variables}

\item{y}{Response variable vector}

\item{IV}{Vector of length 2 specifying which columns of x to use for splitting.
Default is \code{c(1,2)} (first two columns).}

\item{Qsplit1}{Quantile(s) for splitting first independent variable. Default is 0.5.
Ignored if \code{VAL1} is specified.}

\item{Qsplit2}{Quantile(s) for splitting second independent variable. Default is 0.5.
Ignored if \code{VAL2} is specified.}

\item{tr}{Trimming proportion (0 to 0.5). Default is 0.2.}

\item{VAL1}{Optional vector of specific values for splitting first IV. Default is \code{NULL}.}

\item{VAL2}{Optional vector of specific values for splitting second IV. Default is \code{NULL}.}

\item{PB}{Logical. If \code{TRUE}, use percentile bootstrap. Default is \code{FALSE}.}

\item{est}{Location estimator function (used when \code{PB=TRUE}). Default is
\code{\link{tmean}}.}

\item{nboot}{Number of bootstrap samples (if \code{PB=TRUE}). Default is 1000.}

\item{pr}{Logical. If \code{TRUE}, print warnings. Default is \code{TRUE}.}

\item{fun}{Effect size summary function. Default is \code{\link{ES.summary}}.}

\item{xout}{Logical. If \code{TRUE}, remove outliers from x. Default is \code{FALSE}.}

\item{outfun}{Outlier detection function. Default is \code{\link{outpro}}.}

\item{SEED}{Logical or numeric. Controls random seed. Default is \code{TRUE}.}

\item{...}{Additional arguments passed to estimator or outlier function.}
}
\value{
List with components:
\describe{
\item{est.loc.4.DV}{Matrix of location estimates for each cell}
\item{n}{Matrix of sample sizes for each cell}
\item{A}{List of pairwise comparison results for Factor A (IV1)}
\item{B}{List of pairwise comparison results for Factor B (IV2)}
\item{A.effect.sizes}{Effect sizes for Factor A comparisons}
\item{B.effect.sizes}{Effect sizes for Factor B comparisons}
}
}
\description{
Splits data based on quantiles or specific values of two independent variables
and performs all pairwise comparisons for each main effect (Factor A and Factor B).
Computes effect sizes for comparisons within each factor level.
}
\details{
This function:
\enumerate{
\item Splits data into J × K groups based on two variables
\item For Factor A: performs pairwise comparisons across rows (IV1 levels)
\item For Factor B: performs pairwise comparisons across columns (IV2 levels)
\item Computes effect sizes for all comparisons
}

Unlike \code{\link{smgrid2M}} which tests overall main effects, this function
provides detailed pairwise comparisons within each main effect.
}
\examples{
# Pairwise comparisons for main effects
set.seed(123)
x <- matrix(rnorm(200), ncol=2)
y <- x[,1] + 0.5*x[,2] + rnorm(100)
result <- smgridAB(x, y, Qsplit1=.5, Qsplit2=.5)
}
\seealso{
\code{\link{smgrid2M}} for overall main effects test,
\code{\link{smgridRC}} for row-column pairwise comparisons
}
