% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression-advanced.R
\name{regpca}
\alias{regpca}
\title{Principal Component Analysis for Regression}
\usage{
regpca(
  x,
  cor = TRUE,
  loadings = TRUE,
  SCORES = FALSE,
  pval = ncol(x),
  scree = TRUE,
  xlab = "Principal Component",
  ylab = "Proportion of Variance"
)
}
\arguments{
\item{x}{A numeric matrix or data frame containing the variables for PCA.
Rows with missing values are automatically removed.}

\item{cor}{Logical; if \code{TRUE} (default), use the correlation matrix for PCA.
If \code{FALSE}, use the covariance matrix.}

\item{loadings}{Logical; if \code{TRUE} (default), display loadings in the output.}

\item{SCORES}{Logical; if \code{TRUE}, return only the principal component scores.
If \code{FALSE} (default), return the full PCA summary.}

\item{pval}{Number of principal components to retain (default: all components).}

\item{scree}{Logical; if \code{TRUE} (default), display a scree plot showing variance
explained by each component.}

\item{xlab}{Label for x-axis of scree plot (default: "Principal Component").}

\item{ylab}{Label for y-axis of scree plot (default: "Proportion of Variance").}
}
\value{
If \code{SCORES = TRUE}, returns a matrix of principal component scores.
Otherwise, returns the output from \code{summary.princomp()}, which includes
standard deviations, loadings, and variance proportions.
}
\description{
Performs standard principal component analysis (PCA) on a data matrix, with
options for correlation or covariance-based analysis, loading display, and
scree plot visualization.
}
\details{
This function is a wrapper for \code{princomp()} that provides convenient options
for visualization and output formatting. The scree plot displays:
\itemize{
\item Individual variance proportions (solid line, asterisks)
\item Cumulative variance proportions (dashed line, dots)
}

Missing values are removed using \code{elimna()} before analysis.
}
\examples{
\dontrun{
# PCA on iris data
data(iris)
x <- iris[, 1:4]
regpca(x)  # Full PCA with scree plot

# Get only scores
scores <- regpca(x, SCORES = TRUE)

# Use covariance instead of correlation
regpca(x, cor = FALSE)
}
}
\seealso{
\code{\link[stats]{princomp}}, \code{\link{regIVstr}}
}
