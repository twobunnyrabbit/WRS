% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{qplotreg}
\alias{qplotreg}
\title{Plot Multiple Quantile Regression Lines}
\usage{
qplotreg(
  x,
  y,
  qval = c(0.2, 0.8),
  q = NULL,
  plotit = TRUE,
  xlab = "X",
  ylab = "Y",
  xout = FALSE,
  outfun = outpro,
  pch = "*",
  ...
)
}
\arguments{
\item{x}{Numeric vector of predictor values (univariate only).}

\item{y}{Numeric vector of response values.}

\item{qval}{Numeric vector of quantiles to plot (default: c(0.2, 0.8)).}

\item{q}{Alternative specification for \code{qval} (for backward compatibility).}

\item{plotit}{Logical; if \code{TRUE} (default), creates a plot.}

\item{xlab}{Label for x-axis (default: "X").}

\item{ylab}{Label for y-axis (default: "Y").}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{pch}{Plotting character (default: '*').}

\item{...}{Additional arguments passed to \code{outfun}.}
}
\value{
Matrix with columns "Inter." (intercept) and "Slope" for each
quantile, with one row per quantile in \code{qval}.
}
\description{
Computes and plots quantile regression lines for specified quantiles
of the conditional distribution. Allows visualization of how different
parts of the response distribution relate to predictors.
}
\details{
For each quantile in \code{qval}, the function:
\enumerate{
\item Fits a quantile regression line using \code{qreg()}
\item Plots the line on the scatter plot
}

Useful for examining heteroscedasticity and non-constant variance patterns.
Only one predictor is allowed.
}
\examples{
# Heteroscedastic data
x <- rnorm(100)
y <- x + (1 + 0.5*x)*rnorm(100)  # variance increases with x
qplotreg(x, y, qval=c(0.1, 0.5, 0.9))
}
\seealso{
\code{\link{qregplots}}, \code{\link{qreg}}, \code{\link{Qreg}}
}
