% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{wmwaov}
\alias{wmwaov}
\title{WMW-Based Global Test for J Independent Groups}
\usage{
wmwaov(x, est = median, nboot = 500, MC = FALSE, SEED = TRUE, MM = FALSE)
}
\arguments{
\item{x}{Either a matrix, data frame, or list where each column/element represents a group.}

\item{est}{The estimator to use (default is \code{median}). Can also use \code{hd} for
Harrell-Davis estimator when dealing with tied values.}

\item{nboot}{Number of bootstrap samples (default 500).}

\item{MC}{Logical; if TRUE, uses parallel processing via mclapply.}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{MM}{Logical; if TRUE, uses a different distance measure.}
}
\value{
The p-value for the global test.
}
\description{
Extension of the Wilcoxon-Mann-Whitney (WMW) test to J groups using a bootstrap approach.
Tests the global null hypothesis that all pairwise probabilities p_jk = P(X_j < X_k) equal
0.5 for all j < k. This provides a robust omnibus test for comparing multiple groups.
}
\details{
The function computes P(X<Y) as an effect size measure for all pairs of groups and
performs a bootstrap-based global test. When tied values are detected and the default
median estimator is used, a warning suggests using \code{est=hd} or the function
\code{cidmulv2} instead.

The test uses a projection-based distance measure to assess whether the pairwise
probabilities differ from the null hypothesis values.
}
\examples{
# Three groups
set.seed(123)
g1 <- rnorm(20, mean = 5)
g2 <- rnorm(20, mean = 5.3)
g3 <- rnorm(20, mean = 5.6)
x <- list(g1, g2, g3)

# Test for differences
wmwaov(x)

# With Harrell-Davis estimator
wmwaov(x, est = hd)
}
\seealso{
\code{\link{wmw}}, \code{\link{cidmul}}, \code{\link{cidmulv2}}, \code{\link{cid}}
}
