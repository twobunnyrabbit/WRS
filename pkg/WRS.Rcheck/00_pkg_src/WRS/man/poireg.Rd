% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression.R
\name{poireg}
\alias{poireg}
\title{Poisson Regression}
\usage{
poireg(
  x,
  y,
  xout = FALSE,
  outfun = outpro,
  plotit = FALSE,
  xlab = "X",
  ylab = "Y",
  varfun = var,
  YHAT = FALSE,
  STAND = TRUE,
  ...
)
}
\arguments{
\item{x}{A numeric matrix of predictor variables (n by p).}

\item{y}{A numeric vector of count data (typically non-negative integers).}

\item{xout}{Logical; if TRUE, remove outliers from predictors before
fitting (default: FALSE).}

\item{outfun}{Outlier detection function (default: \code{outpro} for
projection method).}

\item{plotit}{Logical; if TRUE and p=1, create a scatterplot with fitted
curve (default: FALSE).}

\item{xlab}{Label for x-axis when plotting (default: "X").}

\item{ylab}{Label for y-axis when plotting (default: "Y").}

\item{varfun}{Variance function for prediction (default: \code{var}).}

\item{YHAT}{Logical; if TRUE, return fitted values (default: FALSE).}

\item{STAND}{Logical; if TRUE, standardize predictors before outlier
detection (default: TRUE).}

\item{...}{Additional arguments passed to \code{glm}.}
}
\value{
A list with components from \code{\link[stats]{glm}}, plus:
\describe{
\item{fitted.values}{Fitted values (if \code{YHAT=TRUE})}
\item{n.keep}{Number of observations retained after outlier removal}
}
}
\description{
Performs Poisson regression for count data using generalized linear models.
Optionally removes outliers among predictors before fitting.
}
\details{
This function fits a Poisson regression model using \code{glm} with
\code{family=poisson}. The response variable \code{y} should be count
data (non-negative integers).

\strong{Outlier Removal} (\code{xout=TRUE}):
\itemize{
\item For 1 predictor: Uses MAD-median rule
\item For >1 predictor: Uses projection-based method (\code{outpro})
\item Can specify alternative method via \code{outfun} (e.g., \code{outfun=out}
for MVE method)
}
}
\examples{
\dontrun{
# Poisson regression with single predictor
set.seed(123)
x <- rnorm(100)
lambda <- exp(1 + 0.5*x)
y <- rpois(100, lambda)
poireg(x, y, plotit=TRUE)

# Multiple predictors with outlier removal
x <- matrix(rnorm(200), ncol=2)
lambda <- exp(1 + 0.5*x[,1] - 0.3*x[,2])
y <- rpois(100, lambda)
poireg(x, y, xout=TRUE)
}

}
\references{
McCullagh, P., & Nelder, J.A. (1989). \emph{Generalized Linear Models}
(2nd ed.). Chapman & Hall.

Wilcox, R.R. (2022). \emph{Introduction to Robust Estimation and Hypothesis
Testing} (5th ed.). Academic Press.
}
\seealso{
\code{\link[stats]{glm}}, \code{\link{outpro}}, \code{\link{out}}
}
