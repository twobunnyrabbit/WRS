% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classification.R
\name{Kmeans}
\alias{Kmeans}
\title{K-Means Clustering with Optional Outlier Removal}
\usage{
Kmeans(x, k, xout = FALSE, outfun = outpro)
}
\arguments{
\item{x}{Data matrix where rows are observations and columns are variables.}

\item{k}{Number of clusters.}

\item{xout}{Logical. If TRUE, outliers are removed before clustering using the
function specified in \code{outfun}. Default is FALSE.}

\item{outfun}{Function for detecting outliers (default: \code{outpro} for projection-based
outlier detection). Only used when xout=TRUE.}
}
\value{
A kmeans object containing:
\itemize{
\item \code{cluster}: A vector of integers (from 1:k) indicating the cluster to which each point is allocated
\item \code{centers}: A matrix of cluster centers
\item \code{totss}: The total sum of squares
\item \code{withinss}: Vector of within-cluster sum of squares, one component per cluster
\item \code{tot.withinss}: Total within-cluster sum of squares
\item \code{betweenss}: The between-cluster sum of squares
\item \code{size}: The number of points in each cluster
}
}
\description{
Performs K-means clustering with an option to remove outliers before clustering.
}
\details{
This function wraps the standard \code{kmeans} function with an option to remove outliers
before clustering. Removing outliers can improve clustering when the data contains
extreme values that would otherwise distort the cluster centers.

Missing values are automatically removed before analysis.
}
\examples{
\dontrun{
# Generate data with 3 clusters
set.seed(123)
x <- rbind(
  matrix(rnorm(100, mean = 0), ncol = 2),
  matrix(rnorm(100, mean = 3), ncol = 2),
  matrix(rnorm(100, mean = 6), ncol = 2)
)

# K-means clustering without outlier removal
res1 <- Kmeans(x, k = 3, xout = FALSE)
res1$centers

# K-means clustering with outlier removal
res2 <- Kmeans(x, k = 3, xout = TRUE)
res2$centers
}

}
\seealso{
\code{\link{Kmeans.grp}} for K-means with group sorting,
\code{\link{kmeans}} for the underlying clustering function
}
