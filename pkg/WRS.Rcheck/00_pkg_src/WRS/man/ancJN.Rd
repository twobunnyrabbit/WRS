% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{ancJN}
\alias{ancJN}
\title{Johnson-Neyman ANCOVA Method}
\usage{
ancJN(
  x1,
  y1,
  x2,
  y2,
  pts = NULL,
  Npts = 5,
  Dpts = FALSE,
  regfun = tsreg,
  fr1 = 1,
  fr2 = 1,
  SCAT = TRUE,
  pch1 = "*",
  pch2 = "+",
  alpha = 0.05,
  plotit = TRUE,
  xout = FALSE,
  outfun = out,
  nboot = 100,
  SEED = TRUE,
  xlab = "X",
  ylab = "Y",
  ...
)
}
\arguments{
\item{x1}{Covariate values for group 1 (must be single covariate).}

\item{y1}{Response values for group 1.}

\item{x2}{Covariate values for group 2 (must be single covariate).}

\item{y2}{Response values for group 2.}

\item{pts}{Design points where regression lines are compared. If NULL, determined automatically.}

\item{Npts}{Number of design points to use (default: 5).}

\item{Dpts}{Logical. If FALSE, points are uniformly spaced between min and max values.
If TRUE, points chosen as in ancova function.}

\item{regfun}{Regression function to use (default: tsreg for Theil-Sen).}

\item{fr1}{Span for group 1 running interval smoother (default: 1).}

\item{fr2}{Span for group 2 running interval smoother (default: 1).}

\item{SCAT}{Logical. Show scatterplot of data.}

\item{pch1, pch2}{Plot characters for groups 1 and 2.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{xout}{Logical. Remove outliers from covariates before analysis.}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{xlab, ylab}{Axis labels for plots.}

\item{...}{Additional arguments passed to regfun or outfun.}
}
\value{
Matrix with columns: X (design points), Est1, Est2 (estimates), DIF (difference),
TEST (test statistic), se (standard error), ci.low, ci.hi, p.value, p.adjust.
}
\description{
Compares two independent groups using the Johnson-Neyman technique, which identifies
regions of the covariate where group differences are statistically significant. Uses
robust regression (default: Theil-Sen) and provides simultaneous confidence intervals.
}
\note{
Only one covariate is allowed. For multiple covariates, use ancJNmp.
}
\examples{
\dontrun{
x1 <- rnorm(50); y1 <- 2*x1 + rnorm(50)
x2 <- rnorm(50); y2 <- x2 + rnorm(50, mean=1)
result <- ancJN(x1, y1, x2, y2, Npts=7)
# Check where groups differ significantly
result[result[,'p.value'] < 0.05, ]
}
}
