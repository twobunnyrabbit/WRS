% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00-utils-core.R
\name{out}
\alias{out}
\title{Outlier Detection Using Robust Mahalanobis Distance}
\usage{
out(
  x,
  cov.fun = cov.mve,
  xlab = "X",
  ylab = "Y",
  qval = 0.975,
  crit = NULL,
  KS = TRUE,
  plotit = FALSE,
  ...
)
}
\arguments{
\item{x}{Data vector or matrix (n x p).}

\item{cov.fun}{Covariance function. Options: cov.mve, cov.mcd, covmba2,
rmba, cov.roc (default: cov.mve).}

\item{xlab, ylab}{Axis labels for plots.}

\item{qval}{Quantile for default critical value (default: 0.975).}

\item{crit}{Optional critical value. If NULL, uses sqrt(qchisq(qval, p)).}

\item{KS}{Logical. If TRUE, preserves random seed (default: TRUE).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
A list with components:
\item{n}{Sample size}
\item{n.out}{Number of outliers}
\item{out.val}{Values of outliers}
\item{out.id}{Row numbers (indices) of outliers}
\item{keep}{Indices of non-outliers}
\item{dis}{Mahalanobis distances}
\item{crit}{Critical value used}
}
\description{
Identifies outliers using robust measures of location and scatter to
compute robust Mahalanobis distances.
}
\details{
For univariate data, uses median and MAD. For multivariate data, uses
robust location and scatter estimates. Points with distance > crit are
flagged as outliers.

For bivariate data with plotit=TRUE, creates a scatterplot marking outliers
with asterisks.
}
\examples{
x <- c(rnorm(50), 10, -10)  # Two outliers
out(x)

}
