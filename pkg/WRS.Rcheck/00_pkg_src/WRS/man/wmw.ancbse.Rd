% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{wmw.ancbse}
\alias{wmw.ancbse}
\title{WMW-Based ANCOVA with Bootstrap Standard Errors}
\usage{
wmw.ancbse(
  x1,
  y1,
  x2,
  y2,
  pts,
  nboot = 100,
  SEED = TRUE,
  MC = FALSE,
  null.value = 0.5,
  xout = FALSE,
  outfun = outpro,
  alpha = 0.05,
  ...
)
}
\arguments{
\item{x1}{Matrix or vector of covariate(s) for group 1.}

\item{y1}{Vector of response variable for group 1.}

\item{x2}{Matrix or vector of covariate(s) for group 2.}

\item{y2}{Vector of response variable for group 2.}

\item{pts}{Matrix of covariate values at which to compare groups. If \code{NULL},
uses values from \code{ancova.KMS}.}

\item{nboot}{Number of bootstrap samples (default: 100).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducibility (default: \code{TRUE}).}

\item{MC}{Logical. If \code{TRUE}, uses parallel processing via \code{mclapply} (default: \code{FALSE}).}

\item{null.value}{Null hypothesis value for P(Y1 < Y2) (default: 0.5).}

\item{xout}{Logical. If \code{TRUE}, removes outliers in covariates (default: \code{FALSE}).}

\item{outfun}{Function for outlier detection (default: \code{outpro}).}

\item{alpha}{Significance level (default: 0.05).}

\item{...}{Additional arguments passed to outlier detection function.}
}
\value{
A list with components:
\itemize{
\item \code{Est}: Vector of P(Y1 < Y2) estimates at each covariate value
\item \code{SE}: Bootstrap standard errors
\item \code{test.stat}: Test statistics (standardized differences)
\item \code{conf.int}: Matrix of confidence intervals
\item \code{p.value}: P-values for each comparison
}
}
\description{
Performs ANCOVA (analysis of covariance) based on a heteroscedastic analog
of the Wilcoxon-Mann-Whitney test. Estimates P(Y1 < Y2) at specified
covariate values with bootstrap standard errors.
}
\details{
This function extends the Wilcoxon-Mann-Whitney approach to ANCOVA by
estimating P(Y1 < Y2) conditional on specific covariate values. Bootstrap
resampling is used to obtain standard errors and confidence intervals.

The method allows for heteroscedasticity and does not assume linearity or
normality. It provides a robust alternative to classical ANCOVA.
}
\examples{
\dontrun{
# Generate data with covariate
set.seed(123)
n1 <- 40; n2 <- 40
x1 <- rnorm(n1)
y1 <- 2*x1 + rnorm(n1)
x2 <- rnorm(n2)
y2 <- 2*x2 + rnorm(n2, mean = 1)

# Compare at specific covariate values
pts <- matrix(c(-1, 0, 1), ncol = 1)
result <- wmw.ancbse(x1, y1, x2, y2, pts = pts, nboot = 200)
print(result)
}
}
\seealso{
\code{\link{wmw.anc}}, \code{\link{ancova.KMS}}, \code{\link{outpro}}
}
