% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{ancdetwmwQ}
\alias{ancdetwmwQ}
\title{ANCOVA Using WMW at Design Points with Simultaneous Confidence Band}
\usage{
ancdetwmwQ(
  x1,
  y1,
  x2,
  y2,
  fr1 = 1,
  fr2 = 1,
  nmin = 8,
  q = 0.05,
  alpha = 0.05,
  plotit = TRUE,
  pts = NA,
  span = 2/3,
  sm = TRUE,
  xout = FALSE,
  outfun = out,
  MC = FALSE,
  npts = 25,
  p.crit = NULL,
  SCAT = TRUE,
  xlab = "X",
  ylab = "P.hat",
  pc = ".",
  ...
)
}
\arguments{
\item{x1}{Covariate values for group 1.}

\item{y1}{Response values for group 1.}

\item{x2}{Covariate values for group 2.}

\item{y2}{Response values for group 2.}

\item{fr1}{Span for group 1 (default: 1).}

\item{fr2}{Span for group 2 (default: 1).}

\item{nmin}{Minimum sample size at each design point (default: 8).}

\item{q}{Quantile for selecting covariate range (default: 0.05).}

\item{alpha}{Significance level (default: 0.05).}

\item{plotit}{Logical. If \code{TRUE}, creates plot (default: \code{TRUE}).}

\item{pts}{Design points (default: NA for automatic selection).}

\item{span}{Span for lowess smoothing (default: 2/3).}

\item{sm}{Logical. If \code{TRUE}, applies smoothing (default: \code{TRUE}).}

\item{xout}{Logical. If \code{TRUE}, removes outliers (default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{out}).}

\item{MC}{Logical. If \code{TRUE}, uses parallel processing (default: \code{FALSE}).}

\item{npts}{Number of design points if pts not specified (default: 25).}

\item{p.crit}{Critical p-value for confidence band (default: NULL for automatic).}

\item{SCAT}{Logical. If \code{TRUE}, shows scatter plot (default: \code{TRUE}).}

\item{xlab}{X-axis label (default: 'X').}

\item{ylab}{Y-axis label (default: 'P.hat').}

\item{pc}{Plot character (default: '.').}

\item{...}{Additional arguments passed to outlier function.}
}
\value{
A list with WMW estimates and confidence bands at design points.
}
\description{
Performs ANCOVA using a Wilcoxon-Mann-Whitney analog at multiple design points
with a simultaneous confidence band. Like \code{ancdet} but uses WMW instead
of means.
}
\details{
Estimates P(Y1 < Y2 | X = x) at multiple design points with simultaneous
confidence band. Design points chosen between q and 1-q quantiles of
the covariate, requiring at least nmin observations at each point.
}
\examples{
\dontrun{
# ANCOVA with WMW
set.seed(123)
x1 <- rnorm(50); y1 <- x1 + rnorm(50)
x2 <- rnorm(50); y2 <- x2 + 0.5 + rnorm(50)
ancdetwmwQ(x1, y1, x2, y2, npts = 15)
}
}
\seealso{
\code{\link{ancovaWMW}}, \code{\link{ancdet}}
}
