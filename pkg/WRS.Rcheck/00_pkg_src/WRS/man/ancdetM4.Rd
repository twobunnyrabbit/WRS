% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{ancdetM4}
\alias{ancdetM4}
\title{ANCOVA Detection Method for Multiple Covariates}
\usage{
ancdetM4(
  x1,
  y1,
  x2,
  y2,
  fr1 = 1,
  fr2 = 1,
  tr = 0.2,
  alpha = 0.05,
  pts = NA,
  pr = TRUE,
  xout = FALSE,
  outfun = outpro,
  MC = FALSE,
  BOTH = FALSE,
  ...
)
}
\arguments{
\item{x1}{Matrix of covariate values for group 1 (n1 x p).}

\item{y1}{Dependent variable for group 1.}

\item{x2}{Matrix of covariate values for group 2 (n2 x p).}

\item{y2}{Dependent variable for group 2.}

\item{fr1}{Span for running interval smoother for group 1 (default: 1).}

\item{fr2}{Span for running interval smoother for group 2 (default: 1).}

\item{tr}{Trimming proportion (default: 0.2). Proportion of observations to
trim from each tail when computing winsorized correlation.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{pts}{Design points matrix (if NA, uses unique x1 values).}

\item{pr}{Logical. If \code{TRUE}, prints progress messages (default: \code{TRUE}).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{MC}{Logical. If \code{TRUE}, uses parallel processing via \code{mclapply}
(default: \code{FALSE}).}

\item{BOTH}{Logical. If TRUE, uses unique values from both groups as design points.}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
List with components:
\item{num.sig}{Number of significant points}
\item{p.crit}{Critical value for adjusted p-values}
\item{output}{Matrix of test results}
\item{sel.pts}{Selected design points used}
\item{sig.points}{Significant design points}
}
\description{
Extension of ancdet2C that handles more than two covariates. Performs
two-group ANCOVA comparisons with familywise error rate control for
multidimensional covariate spaces.
}
\note{
\itemize{
\item Minimum sample size of 50 per group required
\item Requires at least 12 nearest neighbors at each design point
\item Uses Hochberg adjustment for n.sel <= 25, FWE control otherwise
}
}
\examples{
\dontrun{
x1 <- matrix(rnorm(150), ncol=3)
y1 <- rowSums(x1) + rnorm(50)
x2 <- matrix(rnorm(150), ncol=3)
y2 <- rowSums(x2) + 0.5 + rnorm(50)
ancdetM4(x1, y1, x2, y2)
}
}
