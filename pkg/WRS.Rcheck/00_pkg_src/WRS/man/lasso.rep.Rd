% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classification.R
\name{lasso.rep}
\alias{lasso.rep}
\title{Repeated LASSO Regression for Stable Coefficient Identification}
\usage{
lasso.rep(
  x,
  y,
  lasso.fun = lasso.est,
  xout = FALSE,
  outfun = outpro,
  iter = 20,
  EST = FALSE,
  SEED = TRUE
)
}
\arguments{
\item{x}{Predictor matrix (n x p). Can be a matrix or data frame.}

\item{y}{Response vector of length n.}

\item{lasso.fun}{LASSO function to use (default: \code{lasso.est}). Alternative
choices include \code{H.lasso}, \code{OS.lasso}, \code{RA.lasso} (same as \code{LADlasso.Z}).}

\item{xout}{Logical. If TRUE, eliminate leverage points using \code{outfun} before
fitting (default: FALSE).}

\item{outfun}{Function for detecting outliers/leverage points (default: \code{outpro}).
Only used if \code{xout=TRUE}.}

\item{iter}{Number of times to repeat the LASSO estimation (default: 20).}

\item{EST}{Logical. If TRUE, return all \code{iter} estimates sorted by value for each
coefficient. If FALSE (default), return only the minimum absolute values.}

\item{SEED}{Logical. If TRUE (default), set random seed for reproducibility.}
}
\value{
A list with components:
\item{coef}{Vector of minimum absolute coefficient values across all iterations.
Length is p+1 where the first element is the intercept. Coefficients that
consistently shrink to zero will have values close to zero.}
\item{all.est}{Matrix of all estimates (if EST=TRUE), otherwise NULL. Each column
corresponds to a coefficient, rows are sorted estimates across iterations.}
}
\description{
Performs LASSO regression repeatedly and identifies stable coefficients by
finding the minimum absolute value across repeated fits. Helps determine which
coefficients consistently tend toward zero (indicating they should be excluded).
}
\details{
LASSO estimates can vary across runs due to:
\itemize{
\item Random aspects of cross-validation in glmnet
\item Numerical precision in optimization
}

This function addresses this variability by:
\enumerate{
\item Fitting the LASSO model \code{iter} times (default: 20)
\item For each coefficient, finding the minimum absolute value across all fits
\item Coefficients that are consistently shrunk to zero will have minimum values near zero
}

\strong{Interpretation:}
\itemize{
\item If \verb{min(|coef|)} is close to zero across iterations, the coefficient should likely
be excluded from the model
\item If \verb{min(|coef|)} is substantially different from zero, the coefficient is stable
and important for prediction
}

Missing values are automatically removed before analysis.
}
\note{
This function requires the \code{glmnet} package (or the package for the chosen \code{lasso.fun})
to be installed.
}
\examples{
\dontrun{
# Generate data with some irrelevant predictors
set.seed(123)
x <- matrix(rnorm(100 * 5), ncol = 5)
# Only x[,1] and x[,2] are important
y <- x[,1] + 2*x[,2] + rnorm(100)

# Repeated LASSO to identify stable coefficients
result <- lasso.rep(x, y, iter = 20)
result$coef  # Minimum absolute values

# Get all estimates to see variability
result2 <- lasso.rep(x, y, iter = 20, EST = TRUE)
result2$all.est  # Each column shows sorted estimates for one coefficient

# With leverage point removal
lasso.rep(x, y, xout = TRUE)
}

}
\seealso{
\code{\link{lasso.est}} for single LASSO fit, \code{\link{ridge.test}}
for ridge regression
}
