% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{dlinplot}
\alias{dlinplot}
\title{Plot Distribution of Linear Contrast for Dependent Groups}
\usage{
dlinplot(x, con, xlab = "DV", ylab = "", sym.test = FALSE)
}
\arguments{
\item{x}{Matrix or data frame where rows are subjects and columns are
groups/conditions, or a list where each element is a group's data.}

\item{con}{Numeric vector of contrast coefficients. Must sum to zero.}

\item{xlab, ylab}{Axis labels (defaults: 'DV', '').}

\item{sym.test}{Logical; if \code{TRUE}, tests whether the contrast distribution
is symmetric about zero (default: FALSE).}
}
\value{
List with components:
\item{median}{Estimate of the median contrast.}
\item{n}{Sample size.}
\item{ci.low, ci.up}{Confidence interval for the median.}
\item{p.value}{P-value for testing median = 0.}
\item{Q.effect}{Quantile shift effect size.}
\item{sym.test}{Symmetry test result (if \code{sym.test = TRUE}); NULL otherwise.}
}
\description{
For dependent (repeated measures) data, computes and plots the distribution
of a linear contrast across groups. Tests whether the contrast has median
zero and optionally tests for symmetry.
}
\details{
For each subject i, computes the contrast: Y_i = sum(c_j * X_ij), where
c_j are the contrast coefficients in \code{con}.

The function then:
\enumerate{
\item Plots the distribution using adaptive kernel density (\code{\link{akerd}})
\item Tests H0: median = 0 using \code{\link{sintv2}}
\item Computes quantile shift effect size (\code{\link{depQS}})
\item Optionally tests for symmetry using \code{\link{Dqdif}}
}
}
\examples{
# Three repeated measures
x <- matrix(rnorm(150), ncol=3)
# Test if measure 1 differs from average of measures 2 and 3
dlinplot(x, con=c(1, -0.5, -0.5))
}
\seealso{
\code{\link{linplot}}, \code{\link{sintv2}}, \code{\link{depQS}}, \code{\link{Dqdif}}
}
