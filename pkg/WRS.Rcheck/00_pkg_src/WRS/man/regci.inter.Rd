% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{regci.inter}
\alias{regci.inter}
\title{Robust Regression with Interaction Term and Confidence Intervals}
\usage{
regci.inter(
  x,
  y,
  regfun = tsreg,
  ID = c(1, 2),
  nboot = 599,
  alpha = 0.05,
  method = "PRO",
  SEED = TRUE,
  LABELS = FALSE,
  not.out = NULL,
  CI = NULL,
  MC = FALSE,
  pr = TRUE,
  xout = FALSE,
  outfun = outpro,
  ...
)
}
\arguments{
\item{x}{Matrix or data frame of predictors}

\item{y}{Response variable vector}

\item{regfun}{Regression function to use (default: \code{tsreg})}

\item{ID}{Vector of length 2 indicating which columns of x to use for
interaction term (default: c(1,2))}

\item{nboot}{Number of bootstrap samples (default: 599).}

\item{method}{Outlier detection method: 'PRO' (projection, default), 'PRO.R'
(projection random), 'BLP' (regression-based), 'DUM' (dummy variable),
'MCD' (robust Mahalanobis), or 'BOX' (boxplot, univariate only)}

\item{SEED}{Logical; if TRUE, sets random seed for reproducibility (default: TRUE).}

\item{LABELS}{Logical: use variable labels from data (default: FALSE)}

\item{not.out}{Deprecated parameter (use CI instead)}

\item{CI}{Columns to ignore when checking for outliers (required when method='DUM')}

\item{MC}{Logical: use multicore parallel processing (default: FALSE)}

\item{pr}{Logical: print messages (default: TRUE)}

\item{xout}{Logical: remove outliers before analysis (default: FALSE)}

\item{outfun}{Outlier detection function (default: \code{outpro})}

\item{...}{Additional arguments passed to regression function}
}
\value{
A list with components:
\item{output}{Matrix of results with rows for Intercept, x1, x2, x1*x2, and
any additional predictors. Columns: ci.low, ci.up, Estimate, S.E., p-value, p.adj}
\item{n}{Original sample size}
\item{n.keep}{Sample size after removing outliers (if xout=TRUE)}
}
\description{
Performs robust regression with a two-way interaction term (product of two
independent variables). Computes bootstrap confidence intervals for all
regression coefficients including the interaction effect.
}
\details{
This function:
\enumerate{
\item Rearranges data so columns specified by ID are first
\item Creates interaction term (product of first two columns)
\item Optionally removes outliers based on specified method
\item Fits robust regression model with interaction
\item Computes bootstrap confidence intervals for all coefficients
}

The model fitted is: y ~ x1 + x2 + x1*x2 + other predictors
}
\examples{
\dontrun{
x <- matrix(rnorm(100), ncol=2)
y <- x[,1] + 2*x[,2] + 0.5*x[,1]*x[,2] + rnorm(50)
regci.inter(x, y)
}

}
\seealso{
\code{\link{regci}}, \code{\link{regciMC}}, \code{\link{tsreg}},
\code{\link{out.methods}}
}
\keyword{regression}
\keyword{robust}
