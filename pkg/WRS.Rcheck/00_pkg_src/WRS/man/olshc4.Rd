% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00-utils-core.R
\name{olshc4}
\alias{olshc4}
\alias{olsci}
\title{OLS Regression with HC4 or HC3 Confidence Intervals}
\usage{
olshc4(
  x,
  y,
  alpha = 0.05,
  CN = FALSE,
  xout = FALSE,
  outfun = outpro,
  HC3 = FALSE,
  plotit = FALSE,
  xlab = "X",
  ylab = "Y",
  zlab = "Z",
  ...
)

olsci(
  x,
  y,
  alpha = 0.05,
  CN = FALSE,
  xout = FALSE,
  outfun = outpro,
  HC3 = FALSE,
  plotit = FALSE,
  xlab = "X",
  ylab = "Y",
  zlab = "Z",
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{CN}{Logical: use Cribari-Neto adjustment (default: FALSE).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{HC3}{Logical: use HC3 instead of HC4 (default: FALSE).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{xlab, ylab, zlab}{Axis labels for optional plot.}

\item{...}{Additional arguments passed to plotting functions.}
}
\value{
A list with components:
\item{n}{Sample size after outlier removal}
\item{Explanatory.Power}{R-squared value}
\item{ci}{Confidence intervals for coefficients}
\item{p.values}{P-values for coefficients}
\item{Estimates}{Coefficient estimates}
\item{res}{Residuals}
\item{Stederr}{Standard errors}
}
\description{
Performs ordinary least squares regression with heteroscedasticity-consistent
standard errors (HC4 or HC3) for confidence intervals and tests.
}
\details{
HC4 standard errors provide better small-sample properties than HC3,
particularly with high-leverage points. This function allows outlier
detection before fitting via the outfun parameter.
}
\examples{
x <- rnorm(50)
y <- 2*x + rnorm(50)
olshc4(x, y, plotit = FALSE)

}
\seealso{
\code{\link{ols}}
}
