% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{ID.sm.varPB}
\alias{ID.sm.varPB}
\title{Identify Group with Smallest Variance Using Bootstrap}
\usage{
ID.sm.varPB(
  x,
  var.fun = winvar,
  nboot = 500,
  NARM = FALSE,
  na.rm = TRUE,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{x}{Matrix or data frame where columns represent groups, or a list of
vectors}

\item{var.fun}{Variance function to use. Default is \code{\link{winvar}}
(winsorized variance)}

\item{nboot}{Number of bootstrap samples. Default is 500}

\item{NARM}{Logical. If \code{TRUE}, remove rows with any missing values.
Default is \code{FALSE}}

\item{na.rm}{Logical. If \code{TRUE}, remove NAs when computing variance.
Default is \code{TRUE}}

\item{SEED}{Logical. If \code{TRUE}, set random seed for reproducibility.
Default is \code{TRUE}}

\item{...}{Additional arguments passed to \code{var.fun}}
}
\value{
List with components:
\itemize{
\item \code{n}: Sample size
\item \code{Est}: Vector of variance estimates for each group
\item \code{p.value}: Two-tailed pseudo p-value
}
}
\description{
Bootstrap test to determine whether the group with the smallest observed
variance estimate is significantly different from other groups. Uses a
percentile bootstrap approach to compute a pseudo p-value.
}
\details{
The function identifies which group has the smallest variance estimate, then
uses bootstrap resampling to test whether this group consistently has the
smallest variance. The bootstrap procedure:
\enumerate{
\item Identifies the group with minimum variance in the original data
\item Generates \code{nboot} bootstrap samples
\item For each sample, determines which group has minimum variance
\item Computes proportion of times the original minimum group remains minimum
\item Returns a two-tailed p-value: \code{2 * min(p, 1-p)}
}
}
\seealso{
\code{\link{winvar}}, \code{\link{smvar.DO}}
}
\keyword{bootstrap}
\keyword{variance}
