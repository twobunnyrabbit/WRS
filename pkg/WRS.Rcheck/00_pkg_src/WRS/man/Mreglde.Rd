% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression-advanced.R
\name{Mreglde}
\alias{Mreglde}
\title{Multivariate Regression via Least Distance Estimator}
\usage{
Mreglde(
  x,
  y,
  xout = FALSE,
  eout = FALSE,
  outfun = outpro,
  epow.cov = mcdcov,
  RES = FALSE,
  ...
)
}
\arguments{
\item{x}{A numeric matrix containing predictor values (independent variables).}

\item{y}{A numeric matrix containing response values (dependent variables).
Must be multivariate (matrix with multiple columns).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{eout}{Logical. If \code{TRUE}, removes outliers from the entire (X,Y) space
before estimation (default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{epow.cov}{Covariance function for computing explanatory power
(default: \code{mcdcov}). Not currently used in returned output.}

\item{RES}{Logical. If \code{TRUE}, returns residuals; if \code{FALSE}, returns \code{NULL}
for residuals (default: \code{FALSE}).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
A list with components:
\item{coef}{Matrix of regression coefficients. First row contains intercepts,
subsequent rows contain slopes for each predictor. Columns correspond to
response variables.}
\item{residuals}{Matrix of residuals (Y - fitted Y) if \code{RES = TRUE},
otherwise \code{NULL}.}
}
\description{
Performs multivariate regression where parameters are estimated using the
least distance estimator (LDE), a robust method that minimizes the sum of
Mahalanobis distances between observed and fitted values.
}
\details{
The least distance estimator (LDE) minimizes:
\deqn{\sum_i d^2(y_i, x_i'\beta)}
where d() is the Mahalanobis distance based on robust covariance estimation.

This provides robustness to outliers in multivariate regression by down-weighting
observations with large Mahalanobis distances. The method is particularly useful
when both predictors and responses may contain outliers.

Predictors are standardized (centered and scaled) before optimization using
Nelder-Mead via \code{nelderv2()}. Initial values come from classical least squares.

Options for outlier detection:
\itemize{
\item \code{xout = TRUE}: Remove outliers from predictor space only
\item \code{eout = TRUE}: Remove outliers from joint (X,Y) space
\item Cannot set both \code{xout} and \code{eout} to TRUE
}
}
\examples{
\dontrun{
# Multivariate regression with 2 predictors and 2 responses
set.seed(123)
x <- matrix(rnorm(100), ncol=2)
y <- matrix(NA, nrow=50, ncol=2)
y[,1] <- 1 + 2*x[,1] - x[,2] + rnorm(50)
y[,2] <- -1 + x[,1] + 3*x[,2] + rnorm(50)
result <- Mreglde(x, y, RES = TRUE)
result$coef  # Intercepts in row 1, slopes in rows 2-3
}
}
\references{
Jhun, M., & Choi, I. (2009). Bootstrapping least distance estimator in the
multivariate regression model. Computational Statistics & Data Analysis,
53, 4221-4227.
}
\seealso{
\code{\link{mlrreg}}, \code{\link{mulgreg}}, \code{\link{COVreg}}
}
