% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{STRIPchart}
\alias{STRIPchart}
\title{Strip Chart for Matrix Data}
\usage{
STRIPchart(
  x,
  method = "overplot",
  jitter = 0.1,
  offset = 1/3,
  vertical = FALSE,
  group.names,
  add = FALSE,
  at = NULL,
  xlim = NULL,
  ylim = NULL,
  ylab = NULL,
  xlab = NULL,
  dlab = "",
  glab = "",
  log = "",
  pch = 0,
  col = par("fg"),
  cex = par("cex"),
  axes = TRUE,
  frame.plot = axes,
  ...
)
}
\arguments{
\item{x}{Data in matrix format. Each column represents a group. Will be converted
to list mode for plotting.}

\item{method}{Method for handling overlapping points (default: "overplot"):
\itemize{
\item "overplot": Points plotted on top of each other
\item "jitter": Add random noise to spread points
\item "stack": Stack overlapping points
}}

\item{jitter}{Amount of jittering when method = "jitter" (default: 0.1).}

\item{offset}{Stacking offset when method = "stack" (default: 1/3).}

\item{vertical}{Logical. Plot groups vertically if TRUE, horizontally if FALSE (default: FALSE).}

\item{group.names}{Labels for groups (optional).}

\item{add}{Logical. Add to existing plot if TRUE (default: FALSE).}

\item{at}{Numeric positions for groups (optional).}

\item{xlim}{X-axis limits (default: NULL, automatically determined).}

\item{ylim}{Y-axis limits (default: NULL, automatically determined).}

\item{ylab}{Label for Y-axis (default: NULL).}

\item{xlab}{Label for X-axis (default: NULL).}

\item{dlab}{Label for individual data axis (default: "").}

\item{glab}{Label for group axis (default: "").}

\item{log}{Specification for log scale: "", "x", "y", or "xy" (default: "").}

\item{pch}{Point character (default: 0).}

\item{col}{Color for points (default: par("fg")).}

\item{cex}{Character expansion factor for points (default: par("cex")).}

\item{axes}{Logical. Draw axes if TRUE (default: TRUE).}

\item{frame.plot}{Logical. Draw frame around plot if TRUE (default: axes).}

\item{...}{Additional graphical parameters passed to stripchart.}
}
\value{
None. Creates a plot.
}
\description{
Creates a strip chart (one-dimensional scatterplot) that accepts matrix input.
This is a wrapper for \code{stripchart} that converts matrices to list format.
}
\details{
This function extends R's built-in \code{stripchart} to accept matrix input.
The standard \code{stripchart} accepts data frames or lists but not matrices,
so this wrapper converts matrices to list format before plotting.

\strong{Use cases}: Strip charts are particularly useful for:
\itemize{
\item Small to moderate sample sizes
\item Showing all individual data points
\item Comparing distributions across groups
\item Identifying outliers and patterns
}

\strong{Input format}: Provide data as a matrix where each column represents a group.
The function automatically converts to list mode using \code{listm}.
}
\examples{
# Strip chart for three groups
set.seed(123)
x <- matrix(rnorm(150), ncol = 3)
colnames(x) <- c("Group A", "Group B", "Group C")
STRIPchart(x, method = "jitter", vertical = TRUE)

# Stack overlapping points
STRIPchart(x, method = "stack", vertical = TRUE)
}
\seealso{
\code{\link{stripchart}}, \code{\link{listm}}, \code{\link{boxplot}}
}
