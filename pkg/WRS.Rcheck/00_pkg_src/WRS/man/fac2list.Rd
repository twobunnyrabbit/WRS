% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00-utils-core.R
\name{fac2list}
\alias{fac2list}
\title{Convert Factor Design Data to List Format}
\usage{
fac2list(x, g, pr = TRUE)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{g}{Factor grouping information (vector, data frame, or matrix with
up to 4 columns).}

\item{pr}{Logical. If \code{TRUE}, prints progress messages (default: \code{TRUE}).}
}
\value{
A list where each element contains the data for one group combination.
Also prints group level information unless pr = FALSE.
}
\description{
Sorts data into groups based on factorial design levels and stores the
result in list format. Supports up to 4 factors.
}
\details{
This function is useful for converting data from wide format to the list
format required by many WRS functions. Each unique combination of factor
levels becomes a list element.
}
\examples{
x <- rnorm(20)
g <- rep(1:4, each = 5)
fac2list(x, g, pr = FALSE)

}
