% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression.R
\name{depreg}
\alias{depreg}
\title{Depth-Based Regression (Univariate)}
\usage{
depreg(x, y, xout = FALSE, outfun = out, ...)
}
\arguments{
\item{x}{A numeric vector of the predictor variable (or single-column matrix).}

\item{y}{A numeric vector of the dependent variable (length n).}

\item{xout}{Logical. Not used in this function but included to avoid conflicts
when using with other functions like \code{regci} (default: \code{FALSE}).}
}
\value{
A list with components:
\describe{
\item{coef}{Vector of regression coefficients: \code{c(intercept, slope)}.}
\item{residuals}{Residuals from the fit.}
}
}
\description{
Computes the depth regression estimator for simple linear regression (one
predictor). The method finds the regression line with maximum regression depth,
providing a robust alternative to least squares.
}
\details{
The depth regression estimator finds the line with maximum regression depth.
For each pair of points (i,j):
\enumerate{
\item Compute the line passing through both points
\item Calculate the regression depth of this line
\item Select the line(s) with maximum depth
\item Average coefficients if multiple lines achieve maximum depth
}

\strong{Regression depth} of a line measures how "central" it is relative to the
data cloud. A line with high depth is surrounded by many data points,
making it resistant to outliers.

This method provides:
\itemize{
\item High breakdown point (up to 33\%)
\item Robustness to outliers in both x and y
\item Equivariance under affine transformations
}

\strong{Note}: This function only supports simple linear regression (one predictor).
For multiple predictors, use \code{\link{mdepreg}}.
}
\examples{
\dontrun{
# Simple linear regression
set.seed(123)
x <- rnorm(50)
y <- 2*x + 1 + rnorm(50, sd=0.5)
depreg(x, y)

# With outliers
y[1:3] <- y[1:3] + 10
depreg(x, y)  # Robust to outliers

# Compare with least squares
lm(y ~ x)$coef  # Affected by outliers
}
}
\references{
Rousseeuw, P.J. & Hubert, M. (1999). Regression depth. \emph{Journal of the
American Statistical Association}, 94, 388-433.

Wilcox, R.R. (2022). \emph{Introduction to Robust Estimation and Hypothesis Testing}
(5th ed.). Academic Press.
}
\seealso{
\code{\link{mdepreg}} for multiple regression, \code{\link{tsreg}}
for Theil-Sen regression

Other robust regression functions: 
\code{\link{LMSreg}()},
\code{\link{MMreg}()},
\code{\link{bireg}()},
\code{\link{bmreg}()},
\code{\link{chreg}()},
\code{\link{mbmreg}()},
\code{\link{mdepreg}()},
\code{\link{mopreg}()},
\code{\link{snmreg}()},
\code{\link{tsgreg}()},
\code{\link{winreg}()},
\code{\link{wreg}()}
}
\concept{robust regression functions}
