% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{rm.marg.es}
\alias{rm.marg.es}
\title{Robust Marginal Effect Size for Repeated Measures}
\usage{
rm.marg.es(x, y = NULL, tr = 0.2)
}
\arguments{
\item{x}{Either a two-column matrix/data frame or a vector (if y is specified).}

\item{y}{Optional second variable. If NULL, x must be a two-column matrix.}

\item{tr}{Trimming proportion for robust estimators (default: 0.2).}
}
\value{
Numeric value: the robust marginal effect size estimate.
}
\description{
Computes a robust analog of the KMS (Algina-Keselman-Penfield) measure of effect
size for two dependent groups using trimmed means and winsorized variances based
on marginal distributions.
}
\details{
This function computes a robust standardized effect size for dependent groups
that accounts for heteroscedasticity and the correlation structure using marginal
distributions (not difference scores).

The effect size is: sqrt(2) * (TM1 - TM2) / sqrt(WV1 + WV2 - 2*WCOV)
where TM = trimmed mean, WV = winsorized variance, WCOV = winsorized covariance.

Unlike classical effect sizes based on difference scores, this approach uses
the marginal distributions directly, which generally gives different results
for robust estimators.

Missing values are automatically removed.
}
\seealso{
\code{\link{rm.marg.esCI}}, \code{\link{rm.margOM.es}},
\code{\link{D.akp.effect}}, \code{\link{winsdN}}, \code{\link{wincovN}}
}
\keyword{htest}
