% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{twoKgen}
\alias{twoKgen}
\title{Multiple Comparisons Procedure for K Independent Tests (General Function)}
\usage{
twoKgen(
  x = NULL,
  x1 = NULL,
  x2 = NULL,
  func = cidv2,
  alpha = 0.05,
  pr = TRUE,
  opt = 1
)
}
\arguments{
\item{x}{Data in matrix or list format (optional if \code{x1} and \code{x2} provided).}

\item{x1}{Data for group 1 (matrix or list, columns/elements are variables).}

\item{x2}{Data for group 2 (matrix or list, columns/elements are variables).}

\item{func}{Test function to apply to each variable pair (default: \code{cidv2}).}

\item{alpha}{Significance level (default: 0.05).}

\item{pr}{Logical. If \code{TRUE}, prints results (default: \code{TRUE}).}

\item{opt}{Integer option for data format (default: 1).}
}
\value{
A list with components:
\itemize{
\item \code{pvalues}: Vector of p-values for each test
\item \code{adj.pvalues}: Adjusted p-values using Hochberg's method
\item \code{num.sig}: Number of significant tests
\item Additional components from the test function
}
}
\description{
Performs a step-down multiple comparisons procedure for K independent tests
using the Fisher method combined with Hochberg's adjustment. Requires that
the tests be independent.
}
\details{
This function applies a specified test function to each of K variables
independently, then combines the p-values using the Fisher method and
adjusts for multiple comparisons using Hochberg's procedure.

The tests MUST be independent for this method to be valid. For dependent
tests, other adjustment methods should be used.
}
\examples{
\dontrun{
# Compare two groups on multiple independent variables
set.seed(123)
x1 <- matrix(rnorm(100), ncol = 5)
x2 <- matrix(rnorm(120, mean = 0.3), ncol = 5)

# Apply Cliff's test to each variable
result <- twoKgen(x1 = x1, x2 = x2, func = cidv2)
print(result)
}
}
\seealso{
\code{\link{twoKlin}}, \code{\link{cidv2}}
}
