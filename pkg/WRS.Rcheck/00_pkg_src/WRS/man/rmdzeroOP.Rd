% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{rmdzeroOP}
\alias{rmdzeroOP}
\title{Repeated Measures ANOVA Using OP Measure of Location}
\usage{
rmdzeroOP(
  x,
  nboot = 500,
  cop = 3,
  MC = FALSE,
  xlab = "",
  ylab = "",
  STAND = TRUE,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{x}{Data in matrix format (columns = groups) or list mode.}

\item{nboot}{Number of bootstrap samples (default: 500).}

\item{cop}{Copula type for the OP measure (default: 3).}

\item{MC}{Logical. If \code{TRUE}, use parallel processing (default: \code{FALSE}).}

\item{xlab}{Label for x-axis in plot (if applicable).}

\item{ylab}{Label for y-axis in plot (if applicable).}

\item{STAND}{Logical. If \code{TRUE}, standardize the data (default: \code{TRUE}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{...}{Additional arguments.}
}
\value{
A list with test results including p-value and OP measure estimates.
}
\description{
Forms all pairwise differences of scores then tests the hypothesis that all
differences have OP (Outlier Projection) measure of location equal to zero.
Uses bootstrap for inference.
}
\details{
This function:
\enumerate{
\item Computes all pairwise differences: D\link{j,k} = X\link{j} - X\link{k}
\item For each difference, computes the OP measure of location
\item Tests H0: all OP measures = 0 using bootstrap
}

The OP measure is robust to outliers and uses projection methods for
multivariate data.
}
\seealso{
\code{\link{rmdzero}}, \code{\link{rmdzeroG}}
}
\keyword{htest}
\keyword{robust}
