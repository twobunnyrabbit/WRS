% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{multsm}
\alias{multsm}
\title{Smoother for Multinomial Regression}
\usage{
multsm(
  x,
  y,
  pts = x,
  fr = 0.5,
  xout = FALSE,
  outfun = outpro,
  plotit = TRUE,
  pr = TRUE,
  xlab = "X",
  ylab = "Prob",
  ylab2 = "Y",
  zlab = "Prob",
  ticktype = "det",
  vplot = NULL,
  scale = TRUE,
  L = TRUE,
  ...
)
}
\arguments{
\item{x}{Vector or matrix of predictor variables}

\item{y}{Categorical response variable (vector with multiple discrete values)}

\item{pts}{Points at which to estimate probabilities (default: x values)}

\item{fr}{Span parameter for smoother (default: 0.5)}

\item{xout}{Logical; if TRUE removes outliers before smoothing (default: FALSE)}

\item{outfun}{Outlier detection function (default: \code{\link{outpro}})}

\item{plotit}{Logical; if TRUE creates plots (default: TRUE)}

\item{pr}{Logical; if TRUE prints messages (default: TRUE)}

\item{xlab}{X-axis label (default: 'X')}

\item{ylab}{Y-axis label for probabilities (default: 'Prob')}

\item{ylab2}{Y-axis label for 2D plots (default: 'Y')}

\item{zlab}{Z-axis label (default: 'Prob')}

\item{ticktype}{Tick type for 3D plots (default: 'det')}

\item{vplot}{Which y values to plot. NULL=all (1D) or smallest (2D+). Vector like c(1,3) for specific values}

\item{scale}{Logical; if TRUE scales axes in 2D plots (default: TRUE)}

\item{L}{Logical; if TRUE uses LOESS for p=2, else uses running interval smoother (default: TRUE)}

\item{...}{Additional arguments passed to outfun}
}
\value{
A list with components:
\item{estimates}{Matrix of estimated probabilities (rows=pts, cols=y values)}
\item{pts}{Standardized evaluation points}
}
\description{
Smoothing method for multinomial regression that estimates conditional probabilities
P(Y=y|X=x) for categorical outcomes using logistic smoothing.
}
\details{
Estimates P(Y=y|X=x) for each unique value of Y using logistic smoothing (\code{\link{logSMpred}}).
Data are standardized using median and MAD before smoothing.

For single predictor, plots probability curves for specified y values.
For two predictors, creates 3D surface plot using LOESS or running interval smoother.
}
\seealso{
\code{\link{logSM}}, \code{\link{logSMpred}}
}
\keyword{nonparametric}
\keyword{regression}
\keyword{smooth}
