% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{twobici}
\alias{twobici}
\title{Confidence Interval for Difference Between Two Binomial Proportions}
\usage{
twobici(
  r1 = sum(elimna(x)),
  n1 = length(elimna(x)),
  r2 = sum(elimna(y)),
  n2 = length(elimna(y)),
  x = NA,
  y = NA,
  alpha = 0.05
)
}
\arguments{
\item{r1}{Number of successes in group 1 (default: sum of non-NA values in \code{x}).}

\item{n1}{Sample size for group 1 (default: length of non-NA values in \code{x}).}

\item{r2}{Number of successes in group 2 (default: sum of non-NA values in \code{y}).}

\item{n2}{Sample size for group 2 (default: length of non-NA values in \code{y}).}

\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}
}
\value{
A list with components:
\itemize{
\item \code{ci}: Two-element vector with lower and upper confidence bounds
\item \code{p1}: Estimated proportion for group 1 (r1/n1)
\item \code{p2}: Estimated proportion for group 2 (r2/n2)
}
}
\description{
Computes a confidence interval for the difference between two independent
binomial proportions (p1 - p2) using Beal's method.
}
\details{
If vectors \code{x} and \code{y} are provided, the function treats them as binary data
(0 = failure, 1 = success) and computes \code{r1}, \code{n1}, \code{r2}, and \code{n2} automatically.
Otherwise, these values must be provided explicitly.

Beal's method provides accurate confidence intervals for the difference between
two binomial proportions without relying on large-sample approximations.
}
\examples{
\dontrun{
# Using summary statistics
twobici(r1 = 15, n1 = 50, r2 = 25, n2 = 60)

# Using binary data vectors
x <- rbinom(50, 1, 0.3)
y <- rbinom(60, 1, 0.4)
twobici(x = x, y = y)
}
}
