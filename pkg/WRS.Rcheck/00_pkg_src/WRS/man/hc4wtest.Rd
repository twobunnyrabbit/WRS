% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{hc4wtest}
\alias{hc4wtest}
\title{Test All OLS Slopes Using HC4 Wild Bootstrap (Wald Test)}
\usage{
hc4wtest(
  x,
  y,
  nboot = 500,
  SEED = TRUE,
  RAD = TRUE,
  xout = FALSE,
  outfun = outpro,
  ...
)
}
\arguments{
\item{x}{Predictor matrix (n x p) or vector (for simple regression).}

\item{y}{Response vector (length n).}

\item{nboot}{Number of bootstrap samples (default: 500).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed to 2 for reproducibility.}

\item{RAD}{Logical. If \code{TRUE} (default), uses Rademacher distribution
(discrete ±1) for wild bootstrap. If \code{FALSE}, uses continuous uniform.}

\item{xout}{Logical. If \code{TRUE}, removes outliers from predictors using
\code{outfun} before fitting.}

\item{outfun}{Outlier detection function (default: \code{\link{outpro}}).}

\item{...}{Additional arguments passed to \code{outfun}.}
}
\value{
A list with components:
\item{p.value}{Bootstrap p-value for testing \eqn{H_0: all slopes = 0}}
\item{R.squared}{R-squared from OLS regression}
}
\description{
Tests the hypothesis that all OLS regression slopes equal zero using the
HC4 heteroscedasticity-consistent estimator with wild bootstrap and a
Wald test statistic.
}
\details{
This function tests the omnibus hypothesis \eqn{H_0: \beta_1 = ... = \beta_p = 0}
using a wild bootstrap approach that is robust to heteroscedasticity.

\strong{Test Procedure}:
\enumerate{
\item Fit OLS regression and compute HC4 covariance matrix
\item Compute Wald statistic: \eqn{W = \beta' \Sigma^{-1} \beta}
\item Wild bootstrap: resample residuals with random multipliers
\item For each bootstrap sample, recompute Wald statistic
\item P-value is proportion of bootstrap statistics ≥ observed W
}

\strong{Bootstrap Multipliers}:
\itemize{
\item \code{RAD = TRUE}: Rademacher (±1 with equal probability)
\item \code{RAD = FALSE}: Standardized uniform on \link{-√3, √3}
}

The HC4 estimator is recommended by Cribari-Neto (2004) for small to
moderate sample sizes with heteroscedasticity.

This function calls \code{\link{olshc4}} and \code{lstest4} internally.
}
\examples{
\dontrun{
# Multiple regression with heteroscedastic errors
x <- matrix(rnorm(150), 50, 3)
y <- x[,1] + 2*x[,2] + rnorm(50, sd = 1 + abs(x[,1]))

# Test if all slopes are zero
hc4wtest(x, y, nboot = 1000)
}

}
\seealso{
\code{\link{hc4test}}, \code{\link{hc4qtest}}, \code{\link{olshc4}}
}
\keyword{htest}
\keyword{regression}
