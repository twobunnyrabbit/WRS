% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00-utils-core.R
\name{ancova}
\alias{ancova}
\title{ANCOVA with Running Interval Smoother}
\usage{
ancova(
  x1,
  y1,
  x2,
  y2,
  fr1 = 1,
  fr2 = 1,
  tr = 0.2,
  alpha = 0.05,
  plotit = TRUE,
  pts = NA,
  sm = FALSE,
  method = "EP",
  SEED = TRUE,
  pr = TRUE,
  xout = FALSE,
  outfun = out,
  LP = FALSE,
  SCAT = TRUE,
  xlab = "X",
  ylab = "Y",
  pch1 = "*",
  pch2 = "+",
  skip.crit = FALSE,
  nmin = 12,
  crit.val = 1.09,
  ...
)
}
\arguments{
\item{x1}{Covariate values for group 1.}

\item{y1}{Response values for group 1.}

\item{x2}{Covariate values for group 2.}

\item{y2}{Response values for group 2.}

\item{fr1}{Span for group 1 smoother (default: 1).}

\item{fr2}{Span for group 2 smoother (default: 1).}

\item{tr}{Trimming proportion (default: 0.2). Proportion of observations to
trim from each tail when computing winsorized correlation.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{pts}{Design points where regression lines are compared (default: NA uses quantiles).}

\item{sm}{Logical: use bootstrap bagging for smooths (default: FALSE).}

\item{method}{Effect size measure: "EP" (explanatory, default), "QS" (quantile shift),
"AKP" (trimmed mean analog of Cohen's d), or "WMW" (P(X<Y)).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{pr}{Logical. If \code{TRUE}, prints progress messages (default: \code{TRUE}).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{LP}{Logical: use linear programming (default: FALSE).}

\item{SCAT}{Logical: show scatter plot (default: TRUE).}

\item{xlab}{Label for x-axis in plots (default: "X" or "VAR 1").}

\item{ylab}{Label for y-axis in plots (default: "Y" or "VAR 2").}

\item{pch1, pch2}{Plot characters for groups 1 and 2.}

\item{skip.crit}{Logical: skip computational checks in ancdet.pv (default: FALSE).}

\item{nmin}{Minimum sample size (default: 12).}

\item{crit.val}{Critical value threshold (default: 1.09).}

\item{...}{Additional arguments passed to plotting functions.}
}
\value{
A list with component:
\item{output}{Matrix of test results at each design point}
}
\description{
Compares two independent groups using ANCOVA with a single covariate.
Uses a running interval smoother instead of parametric regression assumptions.
}
\details{
This function makes no parametric assumptions about the regression form.
It uses a running interval smoother to compare regression lines at specified
design points. Multiple effect size measures are available via the method argument.
}
\examples{
set.seed(123)
x1 <- rnorm(30); y1 <- 2*x1 + rnorm(30)
x2 <- rnorm(35); y2 <- 1.5*x2 + rnorm(35, sd = 1.5)
ancova(x1, y1, x2, y2, plotit = FALSE)

}
