% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{ZL.break}
\alias{ZL.break}
\title{Robust Bent Line (Breakpoint) Regression (Zhang-Li Method)}
\usage{
ZL.break(x, y, tau = median(x), xout = FALSE, outfun = outbox, ...)
}
\arguments{
\item{x}{Numeric vector of independent variable values.}

\item{y}{Numeric vector of dependent variable values.}

\item{tau}{Breakpoint location (default: median of \code{x}).}

\item{xout}{Logical. If \code{TRUE}, removes outliers from \code{x} before fitting
(default: \code{FALSE}).}

\item{outfun}{Function for detecting outliers (default: \code{\link{outbox}}).}

\item{...}{Additional arguments passed to \code{outfun}.}
}
\value{
Regression coefficients and fitted model details (structure depends on
internal implementation).
}
\description{
Fits a robust bent line regression model with a single breakpoint using the
Zhang-Li (2017) method. Estimates regression coefficients before and after
the breakpoint.
}
\details{
Implements the robust bent line regression method from:
Zhang, F. and Li, Q. (2017). Robust bent line regression.
\emph{Journal of Statistical Planning and Inference}, 185, 41-55.

The model assumes:
\deqn{y = \beta_0 + \beta_1 x + \beta_2 (x - \tau)_+ + \epsilon}
where \eqn{(x - \tau)_+} equals \eqn{x - \tau} when \eqn{x > \tau} and 0 otherwise.

This allows different slopes before and after the breakpoint \eqn{\tau}:
\itemize{
\item Before breakpoint: slope = \eqn{\beta_1}
\item After breakpoint: slope = \eqn{\beta_1 + \beta_2}
}
}
\references{
Zhang, F. and Li, Q. (2017). Robust bent line regression.
\emph{Journal of Statistical Planning and Inference}, 185, 41-55.
}
\seealso{
\code{\link{test.4.break}}, \code{\link{simp.break}}
}
\keyword{regression}
\keyword{robust}
