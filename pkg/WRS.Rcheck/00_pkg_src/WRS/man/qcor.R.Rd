% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{qcor.R}
\alias{qcor.R}
\title{Quantile Regression Correlation for Multiple Quantiles (Ratio Method)}
\usage{
qcor.R(
  x,
  y,
  q = c(0.25, 0.5, 0.75),
  alpha = 0.05,
  nboot = 1000,
  SEED = TRUE,
  xout = TRUE,
  method = "PRO",
  regfun = Qreg
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{q}{Vector of quantiles to use (default: c(0.25, 0.5, 0.75)).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{nboot}{Number of bootstrap samples (default: 1000).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{xout}{Logical. Remove outliers before analysis (default: \code{TRUE}).}

\item{method}{Outlier detection method (default: 'PRO').}

\item{regfun}{Regression function for outlier detection (default: \code{Qreg}).}
}
\value{
Matrix with columns: q, Est, ci.low, ci.up, p-value (one row per quantile).
}
\description{
Computes quantile regression correlation for multiple quantiles using the
ratio of loss functions method.
}
\details{
Computes quantile regression correlation based on ratio of loss functions
(full model vs null model) for each quantile in q. See Wilcox (2022, section 11.9).

Internally calls \code{qcorp1.ci()} for each quantile.
}
\examples{
\dontrun{
set.seed(123)
x <- rnorm(50)
y <- 2 * x + rnorm(50)
qcor.R(x, y, q = c(0.25, 0.5, 0.75), nboot = 500)
}
}
\references{
Wilcox, R. R. (2022). Introduction to Robust Estimation and Hypothesis Testing (5th ed.).
Academic Press.
}
\seealso{
\code{\link{qcorp1.ci}}, \code{\link{qcor.EP}}, \code{\link{qcor.ci}}
}
