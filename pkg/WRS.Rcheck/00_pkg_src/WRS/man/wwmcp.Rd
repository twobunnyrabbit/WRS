% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{wwmcp}
\alias{wwmcp}
\title{Within-by-Within Design Multiple Comparisons with P-value Adjustment}
\usage{
wwmcp(J, K, x, tr = 0.2, alpha = 0.05, dif = TRUE, method = "hoch")
}
\arguments{
\item{J}{Number of levels for Factor A.}

\item{K}{Number of levels for Factor B.}

\item{x}{Data matrix where each row is a participant and columns represent
the J×K conditions (structured in the appropriate order).}

\item{tr}{Trimming proportion (default: 0.2 for 20\% trimming).}

\item{alpha}{Significance level (default: 0.05).}

\item{dif}{Logical. If \code{TRUE} (default), tests differences; if
\code{FALSE}, tests individual contrasts.}

\item{method}{P-value adjustment method (default: 'hoch' for Hochberg).
Other options from \code{\link{p.adjust}}: 'holm', 'bonferroni', 'BH', 'BY', 'fdr'.}
}
\value{
A list with three components:
\item{Factor_A}{Multiple comparison results for Factor A main effects}
\item{Factor_B}{Multiple comparison results for Factor B main effects}
\item{Factor_AB}{Multiple comparison results for A×B interaction}

Each component contains test statistics, original p-values, adjusted p-values,
and standard errors.
}
\description{
Performs all pairwise multiple comparisons for a within-by-within (repeated
measures) design using trimmed means with p-value adjustment methods.
}
\details{
This function performs multiple comparisons for a two-factor within-subjects
(repeated measures) design. It tests:
\itemize{
\item \strong{Factor A main effects}: All pairwise comparisons across levels of A
\item \strong{Factor B main effects}: All pairwise comparisons across levels of B
\item \strong{A×B interaction}: All pairwise comparisons of cell means
}

The function calls \code{\link{rmmcp}} for each set of contrasts and adjusts
p-values using the specified method. The Hochberg (1988) method provides
strong control of familywise error rate with more power than Bonferroni.

Results include adjusted p-values in the fourth column of the test output.
}
\examples{
\dontrun{
# 2×3 within-within design (2 time points, 3 conditions)
# Each row is a subject, 6 columns for the 2×3 = 6 cells
data <- matrix(rnorm(60), 10, 6)

# Perform all multiple comparisons with Hochberg adjustment
result <- wwmcp(J = 2, K = 3, x = data, tr = 0.2)

# View Factor A comparisons
result$Factor_A
}

}
\seealso{
\code{\link{rmmcp}}, \code{\link{con2way}}, \code{\link{p.adjust}}
}
\keyword{htest}
\keyword{multivariate}
