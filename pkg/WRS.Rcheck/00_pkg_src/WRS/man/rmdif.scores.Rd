% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{rmdif.scores}
\alias{rmdif.scores}
\title{Compute All Pairwise Difference Scores}
\usage{
rmdif.scores(x)
}
\arguments{
\item{x}{Matrix or data frame (n-by-J).}
}
\value{
Matrix (n-by-K) where K = J(J-1)/2, containing all pairwise
differences x\link{,j} - x\link{,k} for j < k.
}
\description{
Computes difference scores for all pairs of columns in a matrix.
}
\details{
For a matrix with J columns, computes all J(J-1)/2 pairwise difference
scores. The kth column of the output contains x\link{,j} - x\link{,k} for the kth
pair (j, k) with j < k.

Useful for repeated measures designs when analyzing pairwise comparisons.
}
\examples{
set.seed(123)
x <- matrix(rnorm(30), ncol = 3)
dif <- rmdif.scores(x)
dim(dif)  # 10 rows, 3 columns (3 pairs from 3 variables)
}
\seealso{
\code{\link{smeancr.cord}}, \code{\link{rmanova}}
}
