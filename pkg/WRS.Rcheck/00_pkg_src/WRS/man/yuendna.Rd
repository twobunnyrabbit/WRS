% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{yuendna}
\alias{yuendna}
\title{Yuen's Test for Dependent Groups with Missing Values}
\usage{
yuendna(x, y = NULL, tr = 0.2, alpha = 0.05)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{tr}{Trimming proportion (default: 0.2). Proportion of observations to
trim from each tail when computing winsorized correlation.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}
}
\value{
A list with components:
\itemize{
\item \code{estimate}: Estimated difference in trimmed means
\item \code{test}: Test statistic
\item \code{se}: Standard error of the difference
}
}
\description{
Compares trimmed means of two dependent (paired) variables allowing for
missing values. Uses marginal trimmed means computed from all available data.
}
\details{
This function extends Yuen's test for dependent groups to handle missing data.
Unlike standard paired tests that delete cases with any missing values, this
function uses all available data by computing marginal trimmed means.

Pairs with both values missing are deleted. For pairs with one value missing,
the observed value contributes to its marginal trimmed mean. A weighted
combination of these estimates is used.

If \code{y} is not supplied, \code{x} is assumed to be a two-column matrix.
}
\examples{
\dontrun{
# Data with missing values
x <- c(10, 12, NA, 15, 18, 20, NA, 25)
y <- c(8, NA, 14, 13, 16, 19, 22, 23)

result <- yuendna(x, y)
print(result)
}
}
\seealso{
\code{\link{yuend}}, \code{\link{yuendv2}}
}
