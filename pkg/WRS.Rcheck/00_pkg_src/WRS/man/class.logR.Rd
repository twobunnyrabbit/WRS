% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classification.R
\name{class.logR}
\alias{class.logR}
\title{Logistic Regression or Smoother Classification}
\usage{
class.logR(
  train = NULL,
  test = NULL,
  g = NULL,
  x1 = NULL,
  x2 = NULL,
  sm = TRUE,
  fr = 2,
  rule = 0.5,
  SEED = NULL
)
}
\arguments{
\item{train}{Training data matrix or data frame (optional if x1 and x2 are provided).}

\item{test}{Test data matrix or data frame containing observations to classify.}

\item{g}{Group labels for training data (required if train is provided). Should be
a vector with two unique values indicating the two groups.}

\item{x1}{Training data for group 1 (alternative to train/g specification).}

\item{x2}{Training data for group 2 (alternative to train/g specification).}

\item{sm}{Logical. If TRUE (default), uses a smoother designed for binary outcomes.
If FALSE, uses standard logistic regression.}

\item{fr}{Numeric. Span parameter for the smoother (default: 2). Used only when sm=TRUE.}

\item{rule}{Numeric threshold for classification (default: 0.5). Predicted probabilities
greater than \code{rule} are classified as group 2, otherwise group 1.}

\item{SEED}{Seed value for reproducibility. If NULL, no seed is set. Included for
compatibility when called by other functions.}
}
\value{
A numeric vector of predicted class labels (1 or 2) for the test data.
}
\description{
Performs binary classification using either logistic regression or a smoother for
binary outcomes.
}
\details{
This function provides two classification approaches:
\itemize{
\item When \code{sm=TRUE}: Uses a smoothing method designed for binary dependent variables
\item When \code{sm=FALSE}: Uses standard logistic regression
}

The training data can be specified in two ways:
\enumerate{
\item Using \code{train} and \code{g}: Provide all training data in \code{train} with group labels in \code{g}
\item Using \code{x1} and \code{x2}: Provide the two groups separately
}

Missing values are automatically removed from training and test data.

The classification rule assigns observations to group 2 if the predicted probability
exceeds \code{rule}, and to group 1 otherwise.
}
\examples{
\dontrun{
# Generate training data
set.seed(123)
x1 <- matrix(rnorm(50), ncol = 2)
x2 <- matrix(rnorm(50, mean = 2), ncol = 2)

# Generate test data
test <- matrix(rnorm(20, mean = 1), ncol = 2)

# Classification using smoother
class.logR(x1 = x1, x2 = x2, test = test, sm = TRUE)

# Classification using logistic regression
class.logR(x1 = x1, x2 = x2, test = test, sm = FALSE)

# Using different classification threshold
class.logR(x1 = x1, x2 = x2, test = test, rule = 0.6)
}

}
\seealso{
\code{\link{CLASS.fun}} for general classification dispatcher,
\code{\link{CLASS.BAG}} for bagged version
}
