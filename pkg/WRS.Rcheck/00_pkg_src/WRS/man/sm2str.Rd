% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{sm2str}
\alias{sm2str}
\title{Compare Explanatory Power of Two Predictors Using Smoothers}
\usage{
sm2str(
  xx,
  y,
  iv = c(1, 2),
  nboot = 100,
  SEED = TRUE,
  xout = FALSE,
  outfun = outpro,
  STAND = TRUE,
  ...
)
}
\arguments{
\item{xx}{Numeric matrix with 2 or more columns containing predictor variables.}

\item{y}{Numeric vector of response variable.}

\item{iv}{Integer vector of length 2 specifying which columns of \code{xx} to compare.
Default is \code{c(1,2)} (first two columns).}

\item{nboot}{Integer. Number of bootstrap samples. Default is 100.}

\item{SEED}{Logical. If \code{TRUE} (default), sets random seed to 2 for reproducibility.}

\item{xout}{Logical. If \code{TRUE}, removes leverage points from predictors using
\code{outfun} before analysis. Default is \code{FALSE}.}

\item{outfun}{Function for outlier detection when \code{xout=TRUE}. Default is
\code{\link{outpro}}.}

\item{STAND}{Logical. Passed to \code{outfun}. Default is \code{TRUE}.}

\item{...}{Additional arguments passed to \code{outfun}.}
}
\value{
List with components:
\item{p.value}{Two-tailed p-value for difference in explanatory power}
\item{p.crit}{Suggested critical value adjusted for predictor correlation}
}
\description{
Tests whether two predictors have different explanatory power for a response
variable using running interval smoothers. Uses bootstrap to assess significance.
}
\details{
For each predictor specified in \code{iv}, the function:
\enumerate{
\item Uses \code{\link{lplot}} to compute explanatory power via smoothing
\item Generates bootstrap samples to estimate sampling distribution
\item Computes difference in explanatory power between the two predictors
\item Uses adaptive kernel CDF (\code{\link{akerdcdf}}) to get p-value
}

The critical p-value is adjusted based on the correlation between predictors:
p.crit = 0.25|r| + 0.05 + (100-n)/10000, with minimum of 0.05.
}
\seealso{
\code{\link{sm2strv7}}, \code{\link{lplot}}, \code{\link{akerdcdf}}
}
\keyword{htest}
\keyword{regression}
