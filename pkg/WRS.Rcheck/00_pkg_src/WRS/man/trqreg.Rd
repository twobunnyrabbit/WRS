% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{trqreg}
\alias{trqreg}
\title{Trimmed Quantile Regression}
\usage{
trqreg(x, y, a1 = 0.1, a2, xout = FALSE, outfun = outpro)
}
\arguments{
\item{x}{Numeric vector or matrix. Independent variable(s)}

\item{y}{Numeric vector. Dependent variable}

\item{a1}{Numeric. Lower quantile to trim (default: 0.1)}

\item{a2}{Numeric. Upper quantile to trim (required)}

\item{xout}{Logical. If TRUE, remove outliers in X before analysis (default: FALSE)}

\item{outfun}{Function. Outlier detection function (default: \code{outpro})}
}
\value{
List with component:
\item{coef}{Regression coefficients (intercept and slopes)}
}
\description{
Performs trimmed quantile regression by trimming extreme quantiles
from the conditional distribution.
}
\details{
This function implements trimmed quantile regression, which focuses on
estimating the regression function for the central portion of the conditional
distribution (excluding extreme quantiles).

The algorithm:
\enumerate{
\item Optionally removes outliers in the predictor space
\item Computes simultaneous quantile regression estimates
\item Trims quantiles below \code{a1} and above \code{a2}
\item Returns regression coefficients for the trimmed region
}

Useful when interest focuses on typical cases rather than extremes.
}
\examples{
x <- rnorm(100)
y <- 2 + 3*x + rt(100, df = 3)
result <- trqreg(x, y, a1 = 0.1, a2 = 0.9)

}
\seealso{
\code{\link{qreg}}, \code{\link{rqfit}}, \code{\link[quantreg]{rq}}
}
\keyword{regression}
\keyword{robust}
