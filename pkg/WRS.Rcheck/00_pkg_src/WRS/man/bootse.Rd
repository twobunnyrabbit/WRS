% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{bootse}
\alias{bootse}
\title{Bootstrap Standard Error Estimate}
\usage{
bootse(x, nboot = 1000, est = median, SEED = TRUE, ...)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{est}{Estimator function to use for computing location or other
statistics. Common choices include \code{mean}, \code{median}, \code{tmean} (trimmed mean),
\code{mom} (modified one-step M-estimator), \code{mest} (M-estimator).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{...}{Additional arguments passed to the estimator function \code{est}.}
}
\value{
Numeric value representing the bootstrap standard error estimate.
}
\description{
Computes a bootstrap estimate of the standard error for any estimator.
Uses simple bootstrap resampling with replacement.
}
\details{
This function generates \code{nboot} bootstrap samples by randomly sampling
with replacement from the data. The estimator \code{est} is applied to each
bootstrap sample, and the standard deviation of these bootstrap estimates
is returned as the standard error.

The default estimator is the median, but any function that takes a vector
and returns a scalar can be used.
}
\examples{
# Bootstrap SE for median
x <- rnorm(50)
bootse(x, nboot=1000, est=median)

# Bootstrap SE for trimmed mean
bootse(x, nboot=1000, est=mean, trim=0.2)

# Bootstrap SE for MAD
bootse(x, nboot=1000, est=mad)
}
