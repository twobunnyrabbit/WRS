% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{TKmeans}
\alias{TKmeans}
\title{Trimmed K-Means Clustering}
\usage{
TKmeans(
  x,
  k,
  trim = 0.1,
  scaling = FALSE,
  runs = 100,
  points = NULL,
  countmode = runs + 1,
  printcrit = FALSE,
  maxit = 2 * nrow(as.matrix(data))
)
}
\arguments{
\item{x}{Numeric matrix or data frame. Data to cluster (rows are observations)}

\item{k}{Integer. Number of clusters}

\item{trim}{Numeric. Proportion of observations to trim (default: 0.1 for 10\\%)}

\item{scaling}{Logical. If TRUE, scale variables to unit variance (default: FALSE)}

\item{runs}{Integer. Number of random initializations (default: 100)}

\item{points}{Matrix. Optional starting cluster centers}

\item{countmode}{Integer. Controls console output frequency (default: runs+1)}

\item{printcrit}{Logical. If TRUE, print optimization criterion (default: FALSE)}

\item{maxit}{Integer. Maximum iterations per run (default: 2*nrow(x))}
}
\value{
Object of class "trimkmeans" from the trimcluster package, containing
cluster assignments, centers, and trimmed observation indicators
}
\description{
Performs robust cluster analysis using the trimmed k-means method, which
automatically trims a proportion of outlying observations.
}
\details{
This function is a wrapper for \code{trimcluster::trimkmeans} with automatic
missing value removal. The trimmed k-means method robustifies standard k-means
by automatically identifying and trimming a specified proportion of the most
outlying observations.

The algorithm runs multiple times with different random starts and returns
the solution with the best optimization criterion.
}
\examples{
# Cluster analysis with 10\% trimming
x <- rbind(
  matrix(rnorm(100, mean = 0), ncol = 2),
  matrix(rnorm(100, mean = 3), ncol = 2),
  matrix(runif(20, -10, 10), ncol = 2)  # Outliers
)
result <- TKmeans(x, k = 2, trim = 0.1)

}
\references{
Garcia-Escudero, L. A., Gordaliza, A., Matrán, C., & Mayo-Iscar, A. (2008).
A general trimming approach to robust cluster analysis. Annals of Statistics,
36, 1324-1345.
}
\seealso{
\code{\link[trimcluster]{trimkmeans}}, \code{\link{TKmeans.grp}}
}
\keyword{cluster}
\keyword{robust}
