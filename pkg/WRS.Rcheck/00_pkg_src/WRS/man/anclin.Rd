% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{anclin}
\alias{anclin}
\title{ANCOVA with Adjusted Confidence Intervals for Single Covariate}
\usage{
anclin(
  x1,
  y1,
  x2,
  y2,
  regfun = tsreg,
  pts = NULL,
  ALL = FALSE,
  npts = 25,
  plotit = TRUE,
  SCAT = TRUE,
  pch1 = "*",
  pch2 = "+",
  nboot = 100,
  ADJ = TRUE,
  xout = FALSE,
  outfun = outpro,
  SEED = TRUE,
  p.crit = 0.015,
  alpha = 0.05,
  crit = NULL,
  null.value = 0,
  plotPV = FALSE,
  scale = TRUE,
  span = 0.75,
  xlab = "X",
  ylab = "p-values",
  ylab2 = "Y",
  MC = FALSE,
  nreps = 1000,
  pch = "*",
  ...
)
}
\arguments{
\item{x1}{Covariate values for group 1 (single covariate only).}

\item{y1}{Outcome values for group 1.}

\item{x2}{Covariate values for group 2.}

\item{y2}{Outcome values for group 2.}

\item{regfun}{Regression function. Default is tsreg (Theil-Sen estimator).}

\item{pts}{Covariate values where CIs are computed. If NULL, uses npts evenly-spaced points.}

\item{ALL}{Logical. If TRUE, return all results; if FALSE, only significant results. Default is FALSE.}

\item{npts}{Number of design points if pts=NULL. Default is 25.}

\item{plotit}{Logical. Plot results. Default is TRUE.}

\item{SCAT}{Logical. Show scatterplot. Default is TRUE.}

\item{pch1, pch2}{Plotting characters for groups.}

\item{nboot}{Number of bootstrap samples for SE estimation. Default is 100.}

\item{ADJ}{Logical. Adjust critical value for simultaneous coverage. Default is TRUE.}

\item{xout}{Logical. Remove outliers from covariates. Default is FALSE.}

\item{outfun}{Outlier detection function. Default is outpro.}

\item{SEED}{Logical. Set random seed. Default is TRUE.}

\item{p.crit}{Critical p-value. Default is 0.015.}

\item{alpha}{Familywise error rate. Default is 0.05.}

\item{crit}{Critical value. If NULL, computed automatically.}

\item{null.value}{Null hypothesis value. Default is 0.}

\item{plotPV}{Logical. Plot p-values. Default is FALSE.}

\item{scale, span}{Parameters for LOWESS smoothing of p-values.}

\item{xlab, ylab, ylab2}{Axis labels.}

\item{MC}{Logical. Use parallel processing for critical value computation. Default is FALSE.}

\item{nreps}{Number of replications for computing critical value. Default is 1000.}

\item{pch}{Plotting character. Default is '*'.}

\item{...}{Additional arguments passed to regression function.}
}
\value{
A list with confidence intervals, test results, and plots.
}
\description{
For two independent groups, compute confidence intervals for the difference between
typical values of Y given X, based on robust regression. Controls familywise error rate
across multiple covariate values using improved methods (better power than Hochberg/Hommel).
}
\details{
This function is identical to regYci2g. It handles larger numbers of covariate values
than ancJN. For alpha=0.05, adjustment is quick. For other alpha values or small samples,
critical value computation can be slow; use MC=TRUE to speed up.
}
\examples{
\dontrun{
  x1 <- rnorm(50); y1 <- 2*x1 + rnorm(50)
  x2 <- rnorm(50); y2 <- x2 + 1 + rnorm(50)
  result <- anclin(x1, y1, x2, y2, npts=10)
}
}
\keyword{ancova}
\keyword{regression}
