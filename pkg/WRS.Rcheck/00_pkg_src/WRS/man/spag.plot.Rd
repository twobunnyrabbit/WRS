% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{spag.plot}
\alias{spag.plot}
\title{Spaghetti Plot for Longitudinal Data}
\usage{
spag.plot(
  x,
  regfun = tsreg,
  type = c("l", "p", "b", "o", "c"),
  legend = FALSE,
  trace.label = deparse(substitute(trace.factor)),
  fixed = FALSE,
  xlab = "Time",
  ylab = "",
  xtick = FALSE,
  xaxt = par("xaxt"),
  axes = TRUE,
  fit.lin = FALSE,
  ...
)
}
\arguments{
\item{x}{An n-by-p matrix where n = number of subjects and p = number of time
points. Each row contains repeated measures for one subject.}

\item{regfun}{Regression estimator used for linear fit when \code{fit.lin=TRUE}.
Default is \code{\link{tsreg}} (Theil-Sen).}

\item{type}{Type of plot (see \code{\link[graphics]{plot.default}}): 'l' for
lines (default), 'p' for points, 'b' for both, 'o' for overplotted, 'c' for
lines part of 'b'.}

\item{legend}{Logical; if TRUE, legend is added. Default is FALSE.}

\item{trace.label}{Character label for legend. Default uses the name of the
trace factor.}

\item{fixed}{Logical; passed to \code{\link[graphics]{interaction.plot}}.}

\item{xlab}{Label for x-axis. Default is 'Time'.}

\item{ylab}{Label for y-axis. Default is empty string.}

\item{xtick}{Logical; if TRUE, x-axis tick marks are drawn. Default is FALSE.}

\item{xaxt}{Character; controls x-axis type (see \code{\link[graphics]{par}}).
Default uses current par setting.}

\item{axes}{Logical; if TRUE, axes are drawn. Default is TRUE.}

\item{fit.lin}{Logical; if TRUE, linear fit is plotted for each subject instead
of raw data. Default is FALSE.}

\item{...}{Additional plotting parameters.}
}
\value{
No return value; creates a plot as a side effect.
}
\description{
Creates a spaghetti plot for longitudinal data stored in a matrix. Each row
represents a subject, and each column contains measurements at different time
points. Optionally fits linear trends for each subject using a robust regression
estimator.
}
\details{
The function converts the matrix into long format suitable for
\code{\link[graphics]{interaction.plot}}. When \code{fit.lin=TRUE}, it fits
a regression line to each subject's data using the specified \code{regfun}
(default: Theil-Sen estimator) and plots these fitted values instead of the
raw data.
}
\examples{
\dontrun{
# Create sample longitudinal data
set.seed(123)
n <- 20  # 20 subjects
p <- 5   # 5 time points
x <- matrix(rnorm(n * p, mean = rep(1:p, each = n)), nrow = n)

# Basic spaghetti plot
spag.plot(x)

# With linear fits using Theil-Sen estimator
spag.plot(x, fit.lin = TRUE)

# With legend
spag.plot(x, legend = TRUE, trace.label = "Subject")
}

}
\seealso{
\code{\link[graphics]{interaction.plot}}, \code{\link{tsreg}}
}
