% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{KMSgrid.mcp}
\alias{KMSgrid.mcp}
\title{Grid-Based KMS Analysis: Multiple Comparisons Across Quantile Grids}
\usage{
KMSgrid.mcp(
  x,
  y,
  IV = c(1, 2),
  Qsplit1 = 0.5,
  Qsplit2 = 0.5,
  VAL1 = NULL,
  VAL2 = NULL,
  alpha = 5,
  SW = FALSE,
  nulldist = NULL,
  est = tmean,
  tr = 0.2,
  iter = 1000,
  pr = TRUE,
  method = "hoch",
  xout = FALSE,
  outfun = outpro,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{x}{Matrix or data frame containing independent variables.}

\item{y}{Numeric vector containing the dependent variable.}

\item{IV}{Integer vector of length 2 specifying columns in \code{x} to use as IVs (default: c(1,2)).}

\item{Qsplit1}{Numeric vector of quantiles for splitting first IV (default: 0.5 for median split).}

\item{Qsplit2}{Numeric vector of quantiles for splitting second IV (default: 0.5 for median split).}

\item{VAL1}{Optional numeric vector of specific values for splitting first IV (overrides \code{Qsplit1}).}

\item{VAL2}{Optional numeric vector of specific values for splitting second IV (overrides \code{Qsplit2}).}

\item{alpha}{Numeric. Significance level (default: 0.05).}

\item{SW}{Logical. If TRUE, switches row/column interpretation in analysis (default: FALSE).}

\item{nulldist}{Optional pre-computed null distribution.}

\item{est}{Estimator function for location (default: \code{tmean}).}

\item{iter}{Integer. Number of iterations for null distribution (default: 1000).}

\item{pr}{Logical. If TRUE, prints messages about eliminated groups (default: TRUE).}

\item{method}{Character. Multiple comparison adjustment method (default: 'hoch' for Hochberg).}

\item{xout}{Logical. If TRUE, removes outliers from IVs before analysis (default: FALSE).}

\item{outfun}{Function for outlier detection (default: \code{outpro}).}

\item{SEED}{Logical. If TRUE, sets random seed for reproducibility (default: TRUE).}

\item{...}{Additional arguments passed to estimator and outlier functions.}
}
\value{
Result from \code{\link{KMSinter.mcp}} containing main effects and interaction tests
for the grid-based design.
}
\description{
Performs KMS (Kolmogorov-Smirnov) effect size comparisons across groups defined
by quantile splits of two independent variables, creating a grid-based two-way
ANOVA design.
}
\details{
Creates a grid-based analysis by:
\enumerate{
\item Splitting data into bins based on quantiles of two IVs (e.g., quartiles, median splits)
\item Creating a J×K design where J = length(Qsplit1)+1 and K = length(Qsplit2)+1
\item Computing KMS effect sizes using signed (not squared) version
\item Testing main effects via \code{\link{KMSinter.mcp}}
\item Eliminating groups with n ≤ 5
}

Example splits:
\itemize{
\item \code{Qsplit1=c(.25,.5,.75), Qsplit2=.5}: Quartiles × median split
\item \code{Qsplit1=.5, Qsplit2=.5}: 2×2 median split design
}
}
\examples{
# Create 2x2 grid via median splits
set.seed(123)
x <- matrix(rnorm(200), ncol = 2)
y <- rnorm(100)
result <- KMSgrid.mcp(x, y, Qsplit1 = .5, Qsplit2 = .5)
}
\seealso{
\code{\link{KMSinter.mcp}} for interaction analysis,
\code{\link{KMSgridAB}} for alternative grid approach,
\code{\link{KMSgridRC}} for variation-based version,
\code{\link{smgrid.GLOB}} for smoothing-based grid analysis
}
