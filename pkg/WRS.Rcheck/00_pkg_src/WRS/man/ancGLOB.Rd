% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{ancGLOB}
\alias{ancGLOB}
\title{Global Test for ANCOVA - Equal Location Across Covariate Values}
\usage{
ancGLOB(
  x1,
  y1,
  x2,
  y2,
  xout = FALSE,
  outfun = outpro,
  est = tmean,
  p.crit = NULL,
  nreps = 500,
  alpha = 0.05,
  pr = TRUE,
  nboot = 500,
  SEED = TRUE,
  MC = FALSE,
  CR = FALSE,
  nmin = 12,
  pts = NULL,
  fr1 = 1,
  fr2 = 1,
  plotit = TRUE,
  SCAT = TRUE,
  pch1 = "+",
  pch2 = "o",
  xlab = "X",
  ylab = "Y",
  LP = TRUE,
  cpp = FALSE,
  ...
)
}
\arguments{
\item{x1}{Covariate values for group 1 (vector or matrix).}

\item{y1}{Response values for group 1.}

\item{x2}{Covariate values for group 2 (vector or matrix).}

\item{y2}{Response values for group 2.}

\item{xout}{Logical. If TRUE, remove outliers from covariates before analysis.}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{est}{Estimator function to use for computing location or other
statistics. Common choices include \code{mean}, \code{median}, \code{tmean} (trimmed mean),
\code{mom} (modified one-step M-estimator), \code{mest} (M-estimator).}

\item{p.crit}{Critical p-value for global test. If NULL, determined by simulation.}

\item{nreps}{Number of simulation replications for determining p.crit (default: 500).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{pr}{Logical. Print progress information.}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{MC}{Logical. If \code{TRUE}, uses parallel processing via \code{mclapply}
(default: \code{FALSE}).}

\item{CR}{Logical. If TRUE and number of points is 2 or 3, plot confidence region.}

\item{nmin}{Minimum number of values close to a design point (default: 12).}

\item{pts}{Design points for analysis. If NULL, function selects five points automatically.}

\item{fr1}{Span for group 1 running interval smoother (default: 1).}

\item{fr2}{Span for group 2 running interval smoother (default: 1).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{SCAT}{Logical. Show scatterplot of data.}

\item{pch1, pch2}{Plot characters for groups 1 and 2.}

\item{xlab, ylab}{Axis labels for plots.}

\item{LP}{Logical. Use lowess/loess smoothing for plots.}

\item{cpp}{Logical. Use C++ function for critical p-value (requires WRScpp package).}

\item{...}{Additional arguments passed to outfun or est.}
}
\value{
List with:
\item{test}{Global test statistic}
\item{p.value}{P-value for global test}
\item{p.crit}{Critical p-value used}
\item{reject}{Logical indicating whether null hypothesis is rejected}
\item{pts}{Design points used in analysis}
}
\description{
Performs a global test that measures of location are equal between two groups
across all chosen covariate values. Uses bootstrap percentile method with
familywise error rate control.
}
\note{
Works well with alpha=0.05. Performance for alpha < 0.05 is uncertain.
Reject null hypothesis if p.value < p.crit.
}
\examples{
\dontrun{
x1 <- rnorm(50); y1 <- x1 + rnorm(50)
x2 <- rnorm(50); y2 <- 0.5*x2 + rnorm(50, mean=0.5)
result <- ancGLOB(x1, y1, x2, y2, nreps=1000)
result$reject  # TRUE if groups differ
}
}
