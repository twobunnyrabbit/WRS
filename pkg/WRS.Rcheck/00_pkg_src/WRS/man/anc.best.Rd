% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{anc.best}
\alias{anc.best}
\title{Select Best Group - Multiple Group Comparisons}
\usage{
anc.best(
  x,
  p.crit = NULL,
  alpha = 0.05,
  tr = 0.2,
  iter = 5000,
  SEED = TRUE,
  NEG = FALSE
)
}
\arguments{
\item{x}{Matrix or list of data where each column (or list element) is a group.}

\item{p.crit}{Critical p-values for comparisons. If NULL, determined via simulation
to control familywise error rate at level alpha.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{tr}{Trimming proportion (default: 0.2). Proportion of observations to
trim from each tail when computing winsorized correlation.}

\item{iter}{Number of iterations for simulation to determine critical values (default: 5000).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{NEG}{Logical. If TRUE, negates all values (to find smallest instead of largest).}
}
\value{
S4 object of class 'BIN' with slots:
\item{Group.with.largest.estimate}{Index of group with largest estimate}
\item{Larger.than}{Groups that the best group is significantly larger than}
\item{n}{Sample sizes}
\item{output}{Matrix with detailed comparison results}
}
\description{
For J independent groups, identifies the group with the largest trimmed mean
and makes an inference about whether it is significantly larger than all other groups.
Controls familywise error rate through adjusted critical p-values.
}
\note{
Confidence intervals DO NOT necessarily have simultaneous probability
coverage 1-alpha using the adjusted level.
}
\examples{
\dontrun{
x <- list(rnorm(30), rnorm(30, mean=0.5), rnorm(30, mean=1))
result <- anc.best(x, alpha=0.05)
}
}
