% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{tsregF}
\alias{tsregF}
\title{Theil-Sen Regression with Gauss-Seidel Algorithm}
\usage{
tsregF(
  x,
  y,
  xout = FALSE,
  outfun = out,
  iter = 10,
  varfun = pbvar,
  corfun = pbcor,
  plotit = FALSE,
  tol = 1e-04,
  ...
)
}
\arguments{
\item{x}{Predictor variable(s). Can be a vector (single predictor) or matrix
(multiple predictors).}

\item{y}{Response variable vector.}

\item{xout}{Logical. If \code{TRUE}, removes outliers from predictors before regression.}

\item{outfun}{Outlier detection function (default: \code{out}).}

\item{iter}{Maximum number of Gauss-Seidel iterations for multiple predictors (default: 10).}

\item{varfun}{Function to compute variance for explanatory power (default: \code{pbvar}).}

\item{corfun}{Function to compute correlation for strength of association (default: \code{pbcor}).}

\item{plotit}{Logical. If \code{TRUE}, plots are generated during outlier detection.}

\item{tol}{Convergence tolerance for Gauss-Seidel algorithm (default: 0.0001).}

\item{...}{Additional arguments passed to outlier detection function.}
}
\value{
List with components:
\item{coef}{Vector of regression coefficients (intercept, then slopes).}
\item{residuals}{NULL (residuals not returned).}
\item{Strength.Assoc}{Strength of association (correlation between fitted and observed).}
\item{Explanatory.Power}{Proportion of variance explained (R-squared analog).}
}
\description{
Computes Theil-Sen regression estimator for single or multiple predictors.
For multiple predictors, uses Gauss-Seidel iterative algorithm to find
coefficient estimates.
}
\details{
The Theil-Sen estimator is a robust nonparametric regression method based on
medians of slopes. For single predictors, uses \code{\link{tsp1reg}}. For
multiple predictors, applies Gauss-Seidel iterative algorithm:
\enumerate{
\item Initialize with univariate Theil-Sen slopes
\item Iterate: update each coefficient given current values of others
\item Continue until convergence (max change < tol) or max iterations reached
}

Computes strength of association and explanatory power using variance and
correlation functions (robust by default).
}
\seealso{
\code{\link{tsp1reg}}, \code{\link{tsreg}}
}
\keyword{regression}
\keyword{robust}
