% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{ghmul}
\alias{ghmul}
\title{Generate Multivariate g-and-h Distribution Data}
\usage{
ghmul(n, g = 0, h = 0, p = 2, cmat = diag(rep(1, p)), SEED = FALSE)
}
\arguments{
\item{n}{Integer. Number of observations to generate.}

\item{g}{Numeric. Skewness parameter of the g-and-h distribution (default: 0).
\code{g = 0} gives symmetric distributions; larger \code{|g|} increases skewness.}

\item{h}{Numeric. Tail weight parameter of the g-and-h distribution (default: 0).
\code{h = 0} gives normal tails; larger \code{h} increases tail weight (heavier tails).}

\item{p}{Integer. Number of variables/dimensions (default: 2).}

\item{cmat}{Correlation matrix for the multivariate distribution. Must be a
\code{p} × \code{p} positive definite correlation matrix (default: identity matrix).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}
}
\value{
A \code{n} × \code{p} matrix containing the generated observations.
}
\description{
Generates \code{n} observations from a \code{p}-variate distribution based on the
g-and-h transformation. Creates data with specified correlation structure and
marginal g-and-h distributions.
}
\details{
The g-and-h transformation converts standard normal data to distributions with
controlled skewness (\code{g}) and tail weight (\code{h}):

\deqn{X = \frac{e^{gZ} - 1}{g} e^{hZ^2/2}}

When \code{g = 0}, the transformation simplifies to \eqn{X = Z e^{hZ^2/2}}.

The function:
\enumerate{
\item Generates multivariate normal data with correlation matrix \code{cmat}
\item Applies g-and-h transformation to each variable
\item Returns data with desired marginals and correlation structure
}

Special cases:
\itemize{
\item \code{g = 0, h = 0}: Multivariate standard normal
\item \code{g = 0, h > 0}: Symmetric with heavy tails
\item \code{g > 0, h = 0}: Skewed with normal tails
\item \code{g > 0, h > 0}: Skewed with heavy tails
}
}
\examples{
# Generate 100 observations from bivariate g-and-h distribution
# with correlation 0.5, skewness g=0.5, and heavy tails h=0.2
rho <- matrix(c(1, 0.5, 0.5, 1), 2, 2)
data <- ghmul(100, g=0.5, h=0.2, p=2, cmat=rho, SEED=TRUE)
plot(data, main="Bivariate g-and-h Distribution")

}
\seealso{
\code{\link{ghtransform}}, \code{\link{gengh}}, \code{\link{ghdist}},
\code{\link{rmulnorm}}
}
\keyword{datagen}
\keyword{distribution}
