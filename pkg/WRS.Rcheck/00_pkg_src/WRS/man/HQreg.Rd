% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{HQreg}
\alias{HQreg}
\title{Robust Elastic Net Regression (Huber/Quantile Loss)}
\usage{
HQreg(
  x,
  y,
  alpha = 1,
  xout = FALSE,
  method = "huber",
  tau = 0.5,
  outfun = outpro,
  ...
)
}
\arguments{
\item{x}{Predictor matrix (n x p).}

\item{y}{Response vector (length n).}

\item{alpha}{Elastic net mixing parameter (default: 1 for lasso). Values
between 0 and 1 mix L1 (lasso) and L2 (ridge) penalties. \code{alpha = 1}
is pure lasso; \code{alpha = 0} is pure ridge.}

\item{xout}{Logical. If \code{TRUE}, removes outliers from predictors using
\code{outfun} before fitting.}

\item{method}{Loss function. Either 'huber' for Huber loss (default) or
'quantile' for quantile regression.}

\item{tau}{Quantile level for quantile regression (default: 0.5 for median).
Only used when \code{method = 'quantile'}.}

\item{outfun}{Outlier detection function (default: \code{\link{outpro}}).}

\item{...}{Additional arguments passed to \code{outfun}.}
}
\value{
A list with component:
\item{coef}{Coefficient estimates (including intercept) at the 100th lambda value}
}
\description{
Performs robust penalized regression using elastic net with either Huber
loss or quantile regression loss. Based on the semismooth Newton coordinate
descent algorithm by Yi & Huang (2016).
}
\details{
This function fits penalized regression models that are robust to outliers:

\strong{Elastic Net Penalty}:
\deqn{\lambda[\alpha ||\beta||_1 + (1-\alpha)||\beta||_2^2/2]}
where \eqn{\lambda} is selected by cross-validation.

\strong{Loss Functions}:
\itemize{
\item \strong{Huber}: Quadratic for small residuals, linear for large (robust)
\item \strong{Quantile}: Asymmetric absolute loss for quantile regression
}

The function uses the \code{hqreg} package implementation and returns
coefficients for a specific lambda value (the 100th in the regularization path).

\strong{Reference}: Yi, C. & Huang, J. (2016). Semismooth Newton coordinate
descent algorithm for elastic-net penalized Huber loss regression and
quantile regression. Journal of Computational and Graphical Statistics.
DOI: 10.1080/10618600.2016.1256816
}
\examples{
\dontrun{
# Simulated data with outliers
set.seed(123)
n <- 100; p <- 10
x <- matrix(rnorm(n*p), n, p)
y <- x[,1] + 2*x[,2] + rnorm(n)
y[1:5] <- y[1:5] + 10  # Add outliers

# Fit robust elastic net with Huber loss
fit <- HQreg(x, y, alpha = 0.5, method = 'huber')
fit$coef

# Fit robust quantile regression (median)
fit_med <- HQreg(x, y, alpha = 1, method = 'quantile', tau = 0.5)
}

}
\seealso{
\code{\link{H.lasso}}, \code{\link{hqreg}}
}
\keyword{regression}
\keyword{robust}
