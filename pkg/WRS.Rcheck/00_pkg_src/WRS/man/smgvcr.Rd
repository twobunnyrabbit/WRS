% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{smgvcr}
\alias{smgvcr}
\title{Test MGV Location Estimator Against Null Value}
\usage{
smgvcr(
  m,
  nullv = rep(0, ncol(m)),
  SEED = TRUE,
  op = 0,
  nboot = 500,
  plotit = TRUE
)
}
\arguments{
\item{m}{Numeric matrix (n×p) containing the data.}

\item{nullv}{Numeric vector of length p specifying the null hypothesis values.
Default is a zero vector.}

\item{SEED}{Logical. If \code{TRUE} (default), sets random seed to 2 for reproducibility.}

\item{op}{Integer (0-2). Determines centering method for MGV (see \code{\link{outmgv}}):
\itemize{
\item 0: All pairwise distances (default)
\item 1: MVE
\item 2: MCD
}}

\item{nboot}{Integer. Number of bootstrap samples. Default is 500.}

\item{plotit}{Logical. If \code{TRUE} (default) and p=2, creates a scatterplot with
bootstrap cloud and convex hull confidence region.}
}
\value{
List with components:
\item{p.value}{Bootstrap p-value}
\item{crit.level}{Adjusted significance level based on sample size}
}
\description{
Tests the hypothesis that the MGV (Minimum Generalized Variance) location estimator
equals a specified null value using a bootstrap percentile method. Uses a depth-based
approach to construct confidence regions.
}
\details{
The test procedure:
\enumerate{
\item Generates \code{nboot} bootstrap samples
\item For each sample, computes MGV location estimate after outlier removal
\item Computes generalized variance (depth) for each bootstrap estimate
\item Compares depth of \code{nullv} to bootstrap depths
\item p-value = proportion of bootstrap samples with depth less than \code{nullv}
}

The test level is adjusted based on sample size n to maintain actual Type I error
rate near 0.05:
\itemize{
\item n ≤ 20: α = 0.02
\item n ≤ 30: α = 0.025
\item n ≤ 40: α = 0.03
\item ... up to α = 0.045 for n ≤ 120
}
}
\seealso{
\code{\link{outmgv}}, \code{\link{mgvar}}, \code{\link{mgvmean}}
}
\keyword{htest}
\keyword{multivariate}
