% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{ord.loc.crit}
\alias{ord.loc.crit}
\title{Determine Critical P-values for Ordering Test}
\usage{
ord.loc.crit(J, n = 30, alpha = 0.05, tr = 0.2, iter = 5000, SEED = TRUE)
}
\arguments{
\item{J}{Number of groups.}

\item{n}{Sample size per group (can be a vector for unequal sample sizes). Default is 30.}

\item{alpha}{Significance level. Default is 0.05.}

\item{tr}{Trimming proportion. Default is 0.2 (20\% trimming).}

\item{iter}{Number of simulation iterations. Default is 5000.}

\item{SEED}{Logical. Set random seed for reproducibility. Default is \code{TRUE}.}
}
\value{
A list with components containing critical p-values and the simulated
p-value distribution.
}
\description{
Determines critical p-values for testing the ordering of trimmed means using
simulation under the null hypothesis of equal population trimmed means.
}
\details{
Simulates data under the null hypothesis of equal trimmed means to determine
critical p-values that control FWE for the ordering problem. Used internally
by \code{\link{ord.loc.PV}} when critical p-values are not provided.
}
\seealso{
\code{\link{ord.loc.PV}}, \code{\link{ord.loc.ex}}
}
\keyword{htest}
