% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{man2pb}
\alias{man2pb}
\title{Multivariate Two-Sample Comparison with Percentile Bootstrap}
\usage{
man2pb(x, y, alpha = 0.05, nboot = NA, crit = NA, SEED = TRUE)
}
\arguments{
\item{x}{Data for first group. Can be n by J matrix or list with J components.}

\item{y}{Data for second group. Can be n by J matrix or list with J components.}

\item{alpha}{Alpha level for confidence intervals. Options: 0.05, 0.025, 0.01. Default is 0.05.}

\item{nboot}{Number of bootstrap samples. If \code{NA}, automatically set to 2000
(for J≤4) or 5000 (for J>4). Default is \code{NA}.}

\item{crit}{Critical p-value for familywise error control. If \code{NA}, determined
automatically based on number of variables. Default is \code{NA}.}

\item{SEED}{Logical. If \code{TRUE}, set random seed to 2 for reproducibility. Default is \code{TRUE}.}
}
\value{
List with components:
\item{output}{Matrix with columns: variable number, difference in trimmed means,
p-value, lower CI, upper CI}
\item{crit.p.value}{Critical p-value used for familywise error control}
}
\description{
Compare 20\% trimmed means between two independent groups across multiple variables
using the percentile bootstrap method. Computes confidence intervals for differences
in trimmed means for each variable with familywise error rate control.
}
\details{
This function addresses the multivariate two-sample Behrens-Fisher problem by
comparing 20\% trimmed means (fixed trimming level) across J variables simultaneously.

Critical values for familywise error control are built-in for J=2 to 8 variables
at α=0.05, 0.025, and 0.01. For J>8, critical values are approximated using the
Studentized maximum modulus distribution.

The bootstrap procedure:
\enumerate{
\item Generate bootstrap samples from each group independently
\item Compute trimmed mean differences for each bootstrap sample
\item Determine critical values accounting for multiple comparisons
\item Construct simultaneous confidence intervals
}

Missing values are automatically removed. The number of variables in both groups
must be equal.
}
\seealso{
\code{\link{bootdep}}, \code{\link{smmcrit}}, \code{\link{yuen}}
}
\keyword{multivariate}
