% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/location.R
\name{hdpb}
\alias{hdpb}
\title{Percentile Bootstrap Confidence Interval for Location Estimator}
\usage{
hdpb(x, est = hd, alpha = 0.05, nboot = 2000, SEED = TRUE, nv = 0, ...)
}
\arguments{
\item{x}{A numeric vector of data values.}

\item{est}{Estimator function to use for computing location or other
statistics. Common choices include \code{mean}, \code{median}, \code{tmean} (trimmed mean),
\code{mom} (modified one-step M-estimator), \code{mest} (M-estimator).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{nv}{Null value for computing a p-value (default: 0).}

\item{...}{Additional arguments passed to the estimator function.}
}
\value{
A list with components:
\item{ci}{A vector of length 2 containing the lower and upper confidence
limits.}
\item{n}{Sample size after removing missing values.}
\item{estimate}{The estimated value using the specified estimator.}
\item{p.value}{Two-sided p-value for testing H0: location = nv.}
}
\description{
Computes a percentile bootstrap confidence interval for a location estimator.
By default, uses the Harrell-Davis median estimator.
}
\details{
This function is similar to \code{\link{onesampb}} but defaults to using
the Harrell-Davis estimator for the median. It works well for any alpha
level and handles duplicate values better than \code{\link{hdci}}.
}
\examples{
x <- rnorm(50)
hdpb(x, nboot = 1000)
hdpb(x, est = hd, q = 0.25, nboot = 1000)

}
\seealso{
\code{\link{hd}} for Harrell-Davis estimator,
\code{\link{hdci}} for HD-specific CI with special critical values,
\code{\link{onesampb}} for general one-sample bootstrap
}
