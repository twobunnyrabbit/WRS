% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{lsqs3.sub}
\alias{lsqs3.sub}
\title{Subroutine for Liu-Singh Quality Index (Internal Use)}
\usage{
lsqs3.sub(
  x,
  y,
  plotit = FALSE,
  cop = 2,
  ap.dep = FALSE,
  v2 = FALSE,
  pv = FALSE,
  SEED = TRUE,
  nboot = 1000,
  ypch = "o",
  xpch = "+"
)
}
\arguments{
\item{x}{Matrix or data frame for first group.}

\item{y}{Matrix or data frame for second group.}

\item{plotit}{Logical. If \code{TRUE} (default) and data are bivariate (p=2),
create scatter plot with group indicators.}

\item{cop}{Integer (1, 2, or 3). Copula choice for computing depth (default: 2):
\itemize{
\item \code{cop=1}: Clayton copula
\item \code{cop=2}: Normal (Gaussian) copula
\item \code{cop=3}: t-copula
}}

\item{ap.dep}{Logical. If \code{TRUE}, use approximate depth computation.
If \code{FALSE} (default), use exact method.}

\item{v2}{Logical. Alternative depth computation method (default: \code{FALSE}).}

\item{pv}{Logical. If \code{TRUE}, compute bootstrap p-value for testing
equality of distributions (default: \code{FALSE}).}

\item{SEED}{Logical. If \code{TRUE} (default), set random seed for reproducibility
when computing p-value.}

\item{nboot}{Integer. Number of bootstrap samples for p-value (default: 1000).
Only used if \code{pv=TRUE}.}

\item{ypch}{Plot character for y group observations (default: "o").}

\item{xpch}{Plot character for x group observations (default: "+").}
}
\value{
A list with the same components as \code{\link{lsqs3}}, but typically
only the \code{test} component is used when called internally.
}
\description{
Internal function used by \code{\link{lsqs3}} to compute bootstrap p-values.
Identical to \code{lsqs3} but designed to avoid recursion when computing
p-values through bootstrap resampling.
}
\details{
This function exists to prevent \code{lsqs3} from calling itself during bootstrap
p-value computation, which would create infinite recursion. It has the same
functionality but is intended for internal use only.
}
\seealso{
\code{\link{lsqs3}}
}
\keyword{internal}
\keyword{multivariate}
