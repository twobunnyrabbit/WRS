% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{g5.cen.plot}
\alias{g5.cen.plot}
\title{Plot Centered Density Functions for Up to Five Groups}
\usage{
g5.cen.plot(
  x1,
  x2,
  x3 = NULL,
  x4 = NULL,
  x5 = NULL,
  fr = 0.8,
  aval = 0.5,
  xlab = "X",
  ylab = "",
  color = rep("black", 5),
  main = NULL,
  sub = NULL,
  loc.fun = median
)
}
\arguments{
\item{x1}{Data for group 1 (vector), or a list/matrix/data.frame containing all groups.}

\item{x2}{Data for group 2 (vector).}

\item{x3}{Data for group 3 (vector, optional, default: NULL).}

\item{x4}{Data for group 4 (vector, optional, default: NULL).}

\item{x5}{Data for group 5 (vector, optional, default: NULL).}

\item{fr}{Span parameter for adaptive kernel estimator (default: 0.8).}

\item{aval}{Scaling parameter for adaptive kernel estimator (default: 0.5).}

\item{xlab}{Label for X-axis (default: 'X').}

\item{ylab}{Label for Y-axis (default: '').}

\item{color}{Vector of colors for the 5 groups (default: rep('black', 5)).}

\item{main}{Main title for plot (default: NULL).}

\item{sub}{Subtitle for plot (default: NULL).}

\item{loc.fun}{Function for computing center (default: \code{median}).}
}
\value{
None. Creates a plot.
}
\description{
Creates overlaid density plots for up to five independent groups after centering
each group by a specified measure of location (e.g., median or mean). This allows
comparison of distributional shapes independent of location shifts.
}
\details{
This function is identical to \code{\link{g5plot}} except that each group is
centered by subtracting the value computed by loc.fun before plotting.

\strong{Centering}: Each group x_i is transformed to x_i - loc.fun(x_i) before
density estimation. This removes location differences and allows comparison
of distributional shapes (spread, skewness, etc.).

Common choices for loc.fun include:
\itemize{
\item \code{median} (default, robust)
\item \code{mean}
\item \code{tmean} (trimmed mean)
}

The plot uses the same line styles and colors as \code{g5plot}.
}
\examples{
# Compare shapes after centering by median
set.seed(123)
x1 <- rnorm(100, mean = 0, sd = 1)
x2 <- rnorm(100, mean = 5, sd = 1.5)  # Different location and spread
x3 <- rnorm(100, mean = -3, sd = 0.8)
g5.cen.plot(x1, x2, x3)

# Center by mean instead of median
g5.cen.plot(x1, x2, x3, loc.fun = mean)
}
\seealso{
\code{\link{g5plot}}, \code{\link{g2plot}}, \code{\link{akerd}}
}
