% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{pcor}
\alias{pcor}
\title{Pearson Correlation}
\usage{
pcor(x, y = NA)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}
}
\value{
List with components:
\item{cor}{Pearson correlation coefficient.}
\item{p.value}{Two-sided p-value for testing H0: rho = 0.}
}
\description{
Computes Pearson's product-moment correlation coefficient with significance
test.
}
\details{
This is a wrapper for \code{wincor()} with \code{tr = 0} (no trimming), which gives
Pearson's correlation. If \code{y = NA}, assumes \code{x} is a matrix and computes
all pairwise correlations.
}
\examples{
x <- rnorm(50)
y <- 0.7 * x + rnorm(50)
pcor(x, y)

# Matrix input
m <- cbind(x, y)
pcor(m)
}
\seealso{
\code{\link{pbcor}}, \code{\link{wincor}}, \code{\link{pcorb}}
}
