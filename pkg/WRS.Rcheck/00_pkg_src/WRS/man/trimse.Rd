% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00-utils-core.R
\name{trimse}
\alias{trimse}
\title{Standard Error of Trimmed Mean}
\usage{
trimse(x, tr = 0.2, na.rm = FALSE)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{tr}{Trimming proportion (default: 0.2). Proportion of observations to
trim from each tail when computing winsorized correlation.}

\item{na.rm}{Logical value. If \code{TRUE}, removes \code{NA} values before computation
(default: \code{TRUE} for most functions).}
}
\value{
Numeric value: the standard error of the trimmed mean.
}
\description{
Computes the standard error of the gamma trimmed mean for a sample.
}
\details{
The standard error is computed using the Winsorized variance and is
adjusted for the effective sample size after trimming. The formula is:
sqrt(winvar(x,tr)) / ((1-2*tr)*sqrt(n))
}
\examples{
x <- rnorm(100, mean = 5, sd = 2)
trimse(x, tr = 0.2)

}
\seealso{
\code{\link{trimci}}, \code{\link{winvar}}
}
