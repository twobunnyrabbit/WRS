% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outliers.R
\name{RFbag}
\alias{RFbag}
\title{Bagged Random Forest Classification}
\usage{
RFbag(
  train = NULL,
  test = NULL,
  g = NULL,
  x1 = NULL,
  x2 = NULL,
  depthfun = prodepth,
  kernel = "radial",
  nboot = 100,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{train}{Training data matrix (optional if \code{x1} and \code{x2} provided).}

\item{test}{Test data matrix to classify.}

\item{x1, x2}{Training data for groups 1 and 2 (alternative to \code{train}/\code{g}).}

\item{depthfun}{Depth function to use (default: \code{prodepth}).}

\item{kernel}{Kernel type for classification (default: 'radial').}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{...}{Additional arguments.}
}
\value{
Vector of predicted class labels (1 or 2) for test observations.
}
\description{
Bootstrap aggregating version of random forest classification. Uses bootstrap
sampling to address bias when group sizes differ.
}
\details{
Performs random forest classification via \code{class.forest()} with bootstrap
bagging. When group sizes are unequal (n1 != n2) and there is no association,
standard methods can have biased misclassification probabilities. Bootstrap
bagging with balanced samples (min(n1,n2)) addresses this bias.
}
\seealso{
\code{\link{class.forest}}, \code{\link{Depth.class.bag}}
}
