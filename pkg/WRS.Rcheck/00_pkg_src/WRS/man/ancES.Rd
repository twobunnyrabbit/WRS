% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{ancES}
\alias{ancES}
\title{Compute Effect Sizes for ANCOVA}
\usage{
ancES(
  x1,
  y1,
  x2,
  y2,
  method = "EP",
  fr1 = 1,
  fr2 = 1,
  tr = 0.2,
  pts = NA,
  SEED = TRUE,
  npts = NULL,
  nmin = 12,
  pch = "*",
  FRAC = 0.2,
  plotit = TRUE,
  xlab = "X1",
  ylab = "X2",
  zlab = "ES",
  ticktype = "det"
)

ancES(
  x1,
  y1,
  x2,
  y2,
  method = "EP",
  fr1 = 1,
  fr2 = 1,
  tr = 0.2,
  pts = NA,
  SEED = TRUE,
  npts = NULL,
  nmin = 12,
  pch = "*",
  FRAC = 0.2,
  plotit = TRUE,
  xlab = "X1",
  ylab = "X2",
  zlab = "ES",
  ticktype = "det"
)
}
\arguments{
\item{x1}{Covariate values for group 1 (vector or matrix).}

\item{y1}{Outcome values for group 1.}

\item{x2}{Covariate values for group 2 (vector or matrix).}

\item{y2}{Outcome values for group 2.}

\item{method}{Effect size method: 'EP' (explanatory power), 'AKP' (trimmed-Winsorized Cohen's d),
'QS' (quantile shift), or 'WMW' (P(Y1<Y2|X)). Default is 'EP'.}

\item{fr1}{Span for group 1 in running interval smoother. Default is 1.}

\item{fr2}{Span for group 2 in running interval smoother. Default is 1.}

\item{tr}{Trim proportion for 'AKP' method. Default is 0.2.}

\item{pts}{Covariate points at which to compute effect size. If NA (default), points are chosen automatically.}

\item{SEED}{Logical. If TRUE (default), set random seed for reproducibility.}

\item{npts}{Number of design points for single covariate. Default is NULL (uses 25).}

\item{nmin}{Minimum sample size in each group at each design point. Default is 12.}

\item{pch}{Plotting character for points. Default is '*'.}

\item{FRAC}{Fraction for selecting design points with multiple covariates. Default is 0.2.}

\item{plotit}{Logical. If TRUE (default), plot the effect size function.}

\item{xlab, ylab, zlab}{Axis labels for plots.}

\item{ylab}{Label for y-axis. Default is 'X2'.}

\item{zlab}{Label for z-axis (effect size). Default is 'ES'.}

\item{ticktype}{Type of tick marks for 3D plots. Default is 'det'.}

\item{xlab}{Label for x-axis. Default is 'X1'.}
}
\value{
Matrix with columns for design points ('pts') and effect sizes ('ES').
For single covariate: 2-column matrix. Creates plot if plotit=TRUE.

A matrix with covariate values and corresponding effect sizes.
}
\description{
Computes effect sizes at specified design points for comparing two groups
while accounting for one or more covariates. Supports multiple effect size
measures and can handle both single and multiple covariates.

Compute effect size as a function of the covariates for two independent groups.
This is an alternative implementation with additional plotting options.
}
\note{
For multiple covariates, design points are chosen based on data depth if not specified.
}
\examples{
\dontrun{
x1 <- rnorm(50); y1 <- x1 + rnorm(50)
x2 <- rnorm(50); y2 <- 0.5*x2 + rnorm(50, mean=0.5)
result <- ancES(x1, y1, x2, y2, method="EP", npts=20)
}
}
\keyword{ancova}
\keyword{effect-size}
