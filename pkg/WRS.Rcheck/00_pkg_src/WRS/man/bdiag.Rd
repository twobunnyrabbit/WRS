% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{bdiag}
\alias{bdiag}
\title{Create Block Diagonal Correlation Matrix}
\usage{
bdiag(nb, np, rho = 0)
}
\arguments{
\item{nb}{Number of blocks.}

\item{np}{Size of each block (number of variables per block).}

\item{rho}{Common correlation for off-diagonal elements within each block.
Default is 0 (independence).}
}
\value{
A \code{(nb*np) × (nb*np)} block diagonal correlation matrix.
}
\description{
Creates a block diagonal correlation matrix where each block is an \code{np × np}
correlation matrix with constant correlation \code{rho} off the diagonal.
}
\details{
Creates a \code{p × p} matrix where \code{p = nb × np}. The matrix has a block
diagonal structure with \code{nb} blocks, each of size \code{np × np}. Within
each block, the diagonal elements are 1 and off-diagonal elements are \code{rho}.
Elements outside the blocks are 0.

This is useful for simulating data with grouped variables that are correlated
within groups but independent across groups.
}
\examples{
\dontrun{
# Create 3 blocks of 4 variables each with within-block correlation 0.5
R <- bdiag(nb=3, np=4, rho=0.5)
# Result is 12×12 matrix with 3 blocks
}

}
\keyword{multivariate}
