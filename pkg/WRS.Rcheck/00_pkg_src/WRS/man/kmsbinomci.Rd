% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{kmsbinomci}
\alias{kmsbinomci}
\title{KMS Binomial Confidence Interval}
\usage{
kmsbinomci(x = sum(y), nn = length(y), y = NULL, n = NA, alpha = 0.05)
}
\arguments{
\item{x}{Integer. Number of successes (computed from y if y is provided).}

\item{nn}{Integer. Sample size (computed from y if y is provided).}

\item{y}{Numeric vector or NULL. Optional: binary data (0s and 1s); if provided,
x and nn are computed from y (default: NULL).}

\item{n}{Deprecated parameter, not used.}

\item{alpha}{Numeric. Significance level (default: 0.05).}
}
\value{
List with components:
\describe{
\item{phat}{Estimated probability of success}
\item{se}{Standard error of estimate}
\item{ci}{Confidence interval (lower, upper)}
\item{n}{Sample size}
}
}
\description{
Computes confidence interval for binomial probability of success using the
KMS (Kulinskaya-Morgenthaler-Staudte) method with arcsine transformation.
}
\details{
The KMS method uses an arcsine transformation for improved coverage:
\itemize{
\item For interior cases (x ≠ 0, 1, n-1, n): Uses arcsine transform with
adjusted estimate \eqn{\tilde{p} = (x + 3/8)/(n + 0.75)}
\item For boundary cases: Uses Clopper-Pearson exact method
}

Provides better coverage properties than simple normal approximation,
especially for small samples or extreme proportions.

Reference: Kulinskaya, E., Morgenthaler, S. & Staudte, R. (2008).
Meta Analysis: A guide to calibrating and combining statistical evidence, p. 140.
}
\examples{
# 15 successes in 30 trials
kmsbinomci(x = 15, nn = 30)

# From binary data
y <- c(rep(1, 15), rep(0, 15))
kmsbinomci(y = y)
}
\seealso{
\code{\link{acbinomci}} for Agresti-Coull method,
\code{\link{binom.conf}} for multiple binomial CI methods,
\code{\link{wilbinomci}} for Wilson method
}
