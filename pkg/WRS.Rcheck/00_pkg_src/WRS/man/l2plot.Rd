% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{l2plot}
\alias{l2plot}
\title{Plot LOESS Smoothers for Two Groups}
\usage{
l2plot(
  x1,
  y1,
  x2,
  y2,
  f = 2/3,
  SCAT = TRUE,
  xlab = "x",
  ylab = "y",
  pch = "*",
  eout = FALSE,
  xout = FALSE,
  ...
)
}
\arguments{
\item{x1}{Predictor variable for group 1 (vector).}

\item{y1}{Response variable for group 1 (vector).}

\item{x2}{Predictor variable for group 2 (vector).}

\item{y2}{Response variable for group 2 (vector).}

\item{f}{Span parameter for LOESS smoother (default: 2/3). Controls the amount
of smoothing. Larger values create smoother curves.}

\item{SCAT}{Logical. Include scatterplot if TRUE (default: TRUE). If FALSE,
only the regression lines are plotted.}

\item{xlab}{Label for X-axis (default: "x").}

\item{ylab}{Label for Y-axis (default: "y").}

\item{pch}{Point character for scatterplot (default: '*').}

\item{eout}{Logical. Currently not used (default: FALSE).}

\item{xout}{Logical. Currently not used (default: FALSE).}

\item{...}{Additional graphical parameters passed to plot.}
}
\value{
None. Creates a plot.
}
\description{
Creates a scatterplot with LOESS (locally weighted scatterplot smoothing) curves
for two independent groups, allowing visual comparison of regression trends.
}
\details{
This function compares regression trends between two groups by overlaying
their LOESS smoothers on a single plot. LOESS is a nonparametric method that
fits smooth curves through data without assuming a parametric form.

\strong{Procedure}:
\enumerate{
\item Combine data from both groups into a single scatterplot
\item Fit separate LOESS smoothers to each group using R's \code{lowess} function
\item Overlay both smoothed curves on the plot
}

The span parameter \code{f} controls smoothing:
\itemize{
\item \strong{Small f} (e.g., 0.3): Less smoothing, follows data more closely
\item \strong{Large f} (e.g., 0.9): More smoothing, smoother curve
}

Missing values are automatically removed from each group before plotting.

\strong{Note}: Uses \code{lowess} (not \code{loess}) for the smoothing algorithm.
}
\examples{
# Compare regression trends for two groups
set.seed(123)
n <- 50
x1 <- runif(n, 0, 10)
y1 <- 2 * x1 + rnorm(n, sd = 2)
x2 <- runif(n, 0, 10)
y2 <- 1.5 * x2 + 3 + rnorm(n, sd = 2)
l2plot(x1, y1, x2, y2)

# Just the smoothed lines without scatterplot
l2plot(x1, y1, x2, y2, SCAT = FALSE)
}
\seealso{
\code{\link{lplot2g}}, \code{\link{reg2plot}}, \code{\link{lowess}}
}
