% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{LAD.lasso}
\alias{LAD.lasso}
\title{Least Absolute Deviation (LAD) Lasso Regression}
\usage{
LAD.lasso(
  x,
  y,
  lam = NULL,
  WARN = FALSE,
  xout = FALSE,
  outfun = outpro,
  STAND = TRUE
)
}
\arguments{
\item{x}{Numeric matrix of predictors (independent variables). Must have at
least 2 columns.}

\item{y}{Numeric vector of response values (dependent variable).}

\item{lam}{Numeric vector of penalty parameters (lambda). If \code{NULL},
computed automatically as \code{log(n)/(n*abs(init))}. Default is \code{NULL}.}

\item{WARN}{Logical. If \code{FALSE}, suppresses warnings. Default is \code{FALSE}.}

\item{xout}{Logical. If \code{TRUE}, removes outliers from predictors using
\code{outfun}. Default is \code{FALSE}.}

\item{outfun}{Function for outlier detection (default: \code{outpro}).}

\item{STAND}{Logical. If \code{TRUE}, standardizes data before analysis.
Default is \code{TRUE}.}
}
\value{
A list with components:
\item{coef}{Estimated coefficients (intercept and slopes)}
\item{lambda}{Lambda penalty parameters used}
\item{slopes.rank}{Ranking of slopes by importance (based on lambda order)}
\item{residuals}{Regression residuals}
}
\description{
Performs LAD (least absolute deviation) regression with lasso penalty for
variable selection, optionally with outlier removal and standardization.
}
\details{
This function implements LAD regression with lasso penalty using quantile
regression. The lasso penalty encourages sparsity in the coefficient estimates.
Requires the \code{quantreg} package.
}
\references{
Wang, H., Li, G., and Jiang, G. (2007). Robust Regression Shrinkage and
Consistent Variable Selection Through the LAD-Lasso. Journal of Business
and Economic Statistics, 25, 347-355.
}
\seealso{
\code{\link{LADlasso.Z}}, \code{\link[quantreg]{rq}}
}
\keyword{regression}
\keyword{robust}
