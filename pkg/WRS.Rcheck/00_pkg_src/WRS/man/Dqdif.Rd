% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{Dqdif}
\alias{Dqdif}
\title{Compare Dependent Groups via Quantile Differences (Single Quantile Pair)}
\usage{
Dqdif(
  x,
  y = NULL,
  q = 0.25,
  nboot = 1000,
  plotit = TRUE,
  xlab = "Group 1 - Group 2",
  SEED = TRUE,
  alpha = 0.05
)
}
\arguments{
\item{x}{Numeric vector. First group, or if \code{y=NULL}, the difference scores.}

\item{y}{Numeric vector or NULL. Second group (default: NULL). If provided, analyzes
differences \code{x - y}.}

\item{q}{Numeric. Quantile to test (default: 0.25). Must be < 0.5.
Tests sum of q and (1-q) quantiles.}

\item{nboot}{Integer. Number of bootstrap samples (default: 1000).}

\item{plotit}{Logical. If TRUE, plots kernel density of difference scores (default: TRUE).}

\item{xlab}{Character. X-axis label for plot (default: "Group 1 - Group 2").}

\item{SEED}{Logical. If TRUE, sets random seed to 2 for reproducibility (default: TRUE).}

\item{alpha}{Numeric. Type I error rate for confidence interval (default: 0.05).}
}
\value{
List with components:
\describe{
\item{est.q}{Estimate of q-th quantile of differences}
\item{est.1.minus.q}{Estimate of (1-q)-th quantile of differences}
\item{conf.interval}{Bootstrap confidence interval for the sum}
\item{p.value}{Two-sided p-value for H0: sum = 0}
}
}
\description{
Compares two dependent groups by testing whether the sum of complementary quantiles
(q and 1-q) of difference scores equals zero. Tests for asymmetry in the distribution
of differences, which would indicate group differences.
}
\details{
This function tests the hypothesis that difference scores are symmetric about zero by
examining complementary quantiles:
\itemize{
\item H0: \eqn{Q_d(q) + Q_d(1-q) = 0} where \eqn{Q_d} is the quantile function of differences
\item H1: Sum ≠ 0 (asymmetric distribution)
}

\strong{Method}:
\enumerate{
\item Computes difference scores d = x - y
\item Estimates q and (1-q) quantiles using Harrell-Davis estimator
\item Bootstraps the distribution of the sum under H0
\item Tests whether observed sum is unusual
}

\strong{Interpretation}:
\itemize{
\item If distributions are symmetric around zero (no group difference), sum ≈ 0
\item If one group tends to be larger, sum will deviate from zero
\item This is more sensitive than median tests to distributional shifts
}

The bootstrap confidence interval uses percentile method. P-value uses mid-p correction
for ties.
}
\examples{
# Test paired data for symmetry
set.seed(123)
x <- rnorm(30, mean = 0)
y <- rnorm(30, mean = 0.5)
result <- Dqdif(x, y, plotit = FALSE)
result$p.value  # Test for asymmetry
}
\seealso{
\code{\link{difQpci}}, \code{\link{hd}}, \code{\link{qhd}}
}
\keyword{htest}
\keyword{robust}
