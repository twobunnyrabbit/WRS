% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{pro.class.probs}
\alias{pro.class.probs}
\title{Projection-Based Classification with Probabilities}
\usage{
pro.class.probs(
  train = NULL,
  test = NULL,
  g = NULL,
  x1 = NULL,
  x2 = NULL,
  nonpar = TRUE,
  rule = 0.5,
  fr = 2
)
}
\arguments{
\item{train}{Numeric matrix of training data, or \code{NULL} if \code{x1} and \code{x2} provided.}

\item{test}{Numeric matrix of test data to classify.}

\item{g}{Numeric vector of group labels (0 or 1) for training data.}

\item{x1}{Numeric matrix for group 1 training data (alternative to \code{train} + \code{g}).}

\item{x2}{Numeric matrix for group 2 training data (alternative to \code{train} + \code{g}).}

\item{nonpar}{Logical. If \code{TRUE}, use smoother; if \code{FALSE}, use logistic regression
(default: \code{TRUE}).}

\item{rule}{Numeric. Classification threshold (must be between 0 and 1, exclusive).}

\item{fr}{Numeric. Span parameter for smoother when \code{nonpar=TRUE} (default: 2).}
}
\value{
List with components:
\itemize{
\item \code{Decision}: Numeric vector of classifications (1 or 2)
\item \code{prob.in.second.class}: Numeric vector of estimated probabilities
for belonging to group 2
}
}
\description{
Similar to \code{\link{pro.class}}, but also returns estimated probabilities of
belonging to the second group for each test observation.
}
\details{
Projects data onto a line using robust distances, then estimates probability
of group membership. Classify as group 1 if probability < rule, otherwise group 2.
}
\examples{
\dontrun{
result <- pro.class.probs(x1 = group1, x2 = group2, test = test_data)
print(result$Decision)
print(result$prob.in.second.class)
}

}
\seealso{
\code{\link{pro.class}}, \code{\link{pro.classPD}}
}
\keyword{classif}
\keyword{multivariate}
