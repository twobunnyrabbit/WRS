% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{rmES.pro}
\alias{rmES.pro}
\title{Effect Size for Repeated Measures Using Projection Distance to Grand Mean}
\usage{
rmES.pro(x, est = tmean, PV = FALSE, SEED = TRUE, ND = NULL, iter = 2000, ...)
}
\arguments{
\item{x}{Data in matrix format (columns = groups) or list mode.}

\item{est}{Location estimator function (default: \code{\link{tmean}}).}

\item{PV}{Logical. If \code{TRUE}, compute p-value (default: \code{FALSE}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{ND}{Optional pre-computed null distribution (from \code{\link{rmESPRO.null}}).
If \code{NULL} and \code{PV=TRUE}, will be computed.}

\item{iter}{Number of iterations for null distribution (default: 2000).}

\item{...}{Additional arguments passed to \code{est}.}
}
\value{
A list with components:
\describe{
\item{\code{effect.size}}{Projection distance from location estimates to grand mean}
\item{\code{p.value}}{P-value (NA if \code{PV=FALSE})}
}
}
\description{
Computes effect size for dependent groups based on the projection distance
between location estimates and the grand mean. Optionally computes a p-value
using a null distribution.
}
\details{
Computes location estimate for each of J groups, then the projection distance
from these estimates to a vector containing the grand mean (mean of the J estimates).

If \code{PV=TRUE}, compares observed effect size to null distribution generated
by \code{\link{rmESPRO.null}}, which simulates data from multivariate normal
with mean 0 and no correlation.
}
\seealso{
\code{\link{rmES.dif.pro}} for effect size based on differences,
\code{\link{rmESPRO.null}} for null distribution,
\code{\link{pdis}} for projection distance
}
\keyword{htest}
\keyword{robust}
