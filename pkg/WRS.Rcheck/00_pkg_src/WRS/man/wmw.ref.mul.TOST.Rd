% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{wmw.ref.mul.TOST}
\alias{wmw.ref.mul.TOST}
\title{Test Symmetry at Multiple Points with TOST Extension}
\usage{
wmw.ref.mul.TOST(
  x,
  y,
  refp = NULL,
  pts = NULL,
  q = seq(0.6, 0.9, 0.1),
  center = FALSE,
  estfun = hdmq,
  alpha = 0.05,
  nboot = 1000,
  SEED = TRUE,
  method = "BH",
  plotit = FALSE,
  xlab = "Difference",
  ylab = "Density",
  plotfun = kerSORT,
  eqbound = 0.05
)
}
\arguments{
\item{x}{Observations from group 1.}

\item{y}{Observations from group 2.}

\item{refp}{Vector of reference points (default: NULL for quantile-based).}

\item{pts}{Alias for refp (default: NULL).}

\item{q}{Quantiles if refp=NULL (default: seq(0.6, 0.9, 0.1)).}

\item{center}{Logical. If \code{TRUE}, centers at median (default: \code{FALSE}).}

\item{estfun}{Quantile estimator (default: \code{hdmq}).}

\item{alpha}{Significance level (default: 0.05).}

\item{nboot}{Number of bootstrap samples (default: 1000).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed (default: \code{TRUE}).}

\item{method}{Multiple comparison adjustment (default: 'BH').}

\item{plotit}{Logical. If \code{TRUE}, plots distribution (default: \code{FALSE}).}

\item{xlab}{X-axis label (default: 'Difference').}

\item{ylab}{Y-axis label (default: 'Density').}

\item{plotfun}{Plotting function (default: \code{kerSORT}).}

\item{eqbound}{Equivalence bound for TOST (default: 0.05).}
}
\value{
Data frame with test results at each reference point including:
90\% CIs, 95\% CIs, p-values, adjusted p-values, and TOST equivalence results.
}
\description{
Tests symmetry at multiple reference points with TOST equivalence testing.
Extension of \code{wmw.ref.mul} with equivalence bounds.
}
\details{
Performs \code{wmw.ref.dif.TOST} at multiple reference points with
family-wise error control. Provides comprehensive output including
both difference testing and equivalence testing results.
}
\examples{
\dontrun{
# Multiple points with TOST
set.seed(123)
x <- rnorm(50)
y <- rnorm(50, mean = 0.05)
wmw.ref.mul.TOST(x, y, eqbound = 0.1, nboot = 500)
}
}
\seealso{
\code{\link{wmw.ref.dif.TOST}}, \code{\link{wmw.ref.mul}}
}
