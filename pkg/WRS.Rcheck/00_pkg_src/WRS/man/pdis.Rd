% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00-utils-core.R
\name{pdis}
\alias{pdis}
\title{Projection Distance}
\usage{
pdis(m, pts = m, MM = FALSE, cop = 3, dop = 1, center = NA, na.rm = TRUE)
}
\arguments{
\item{m}{Reference data matrix (n x p).}

\item{pts}{Points at which to compute projection distances (default: m).}

\item{MM}{Logical. If FALSE, scales distances using interquartile range;
if TRUE, uses MAD (default: FALSE).}

\item{cop}{Center option (1-5):
\itemize{
\item 1: Donoho-Gasko median
\item 2: MCD center
\item 3: Median of marginal distributions (default)
\item 4: MVE center
\item 5: Skipped mean
}}

\item{dop}{Depth option for cop = 1 (default: 1).}

\item{center}{Optional pre-specified center. If NA, computed based on cop.}

\item{na.rm}{Logical value. If \code{TRUE}, removes \code{NA} values before computation
(default: \code{TRUE} for most functions).}
}
\value{
A vector of projection distances for each point in pts.
}
\description{
Computes projection distances for points relative to a reference data set.
Used for outlier detection and robust multivariate analysis.
}
\details{
Projection distance is a robust measure of outlyingness based on projecting
multivariate data onto all possible one-dimensional subspaces. For each
point, the maximum standardized distance across all projections is computed.

For univariate data (ncol(m) = 1), simply computes absolute distances from
the center, scaled appropriately.
}
\examples{
# Bivariate example
m <- matrix(rnorm(100), ncol = 2)
pd <- pdis(m)
plot(m, cex = pd)  # Point size indicates projection distance

}
\seealso{
\code{\link{outpro}} for outlier detection using projection distances
}
