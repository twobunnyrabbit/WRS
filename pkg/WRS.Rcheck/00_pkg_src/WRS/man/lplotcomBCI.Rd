% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{lplotcomBCI}
\alias{lplotcomBCI}
\title{Bootstrap Confidence Interval for Comparing Predictor Importance}
\usage{
lplotcomBCI(
  x,
  y,
  xout = FALSE,
  pts1 = NULL,
  pts2 = NULL,
  p.crit = NULL,
  outfun = outpro,
  span = 2/3,
  npts = 10,
  tr = 0.2,
  nboot = 500,
  SEED = TRUE,
  SEQ = FALSE,
  MAD.OP = FALSE,
  plotit = TRUE,
  ticktype = "simple",
  xlab = "X1",
  ylab = "X2",
  zlab = "Y",
  reverse.x1 = FALSE,
  reverse.x2 = FALSE,
  pr = FALSE,
  MEDIAN = FALSE,
  Q1 = FALSE,
  Q2 = FALSE,
  alpha = 0.05,
  MC = FALSE,
  ...
)
}
\arguments{
\item{x}{Matrix with 2 columns (two predictor variables).}

\item{y}{Response variable vector.}

\item{xout}{Logical; if TRUE, removes multivariate outliers (default: FALSE).}

\item{pts1}{Evaluation points for predictor 1 (default: quartiles 0.25, 0.5, 0.75).}

\item{pts2}{Evaluation points for predictor 2 (default: quartiles 0.25, 0.5, 0.75).}

\item{p.crit}{Critical p-value for confidence intervals (default: computed from sample size).}

\item{outfun}{Outlier detection function (default: \code{outpro}).}

\item{span}{Span parameter for LOESS (default: 2/3).}

\item{npts}{Number of evaluation points if SEQ=TRUE (default: 10).}

\item{tr}{Trimming proportion for Winsorized SD (default: 0.2).}

\item{nboot}{Number of bootstrap samples (default: 500).}

\item{SEED}{Logical; if TRUE, sets random seed (default: TRUE).}

\item{SEQ}{Logical; if TRUE, uses equally spaced points instead of quartiles (default: FALSE).}

\item{MAD.OP}{Logical; if TRUE with SEQ=TRUE, uses MAD for range; otherwise uses quartiles (default: FALSE).}

\item{plotit}{Logical; if TRUE, creates LOESS plot (default: TRUE).}

\item{ticktype}{Type of tick marks for 3D plot (default: "simple").}

\item{xlab}{Label for X1 axis (default: "X1").}

\item{ylab}{Label for X2 axis (default: "X2").}

\item{zlab}{Label for Y axis (default: "Y").}

\item{reverse.x1}{Logical; if TRUE, reverses order of pts1 (default: FALSE).}

\item{reverse.x2}{Logical; if TRUE, reverses order of pts2 (default: FALSE).}

\item{pr}{Logical; if TRUE, prints warnings (default: FALSE).}

\item{MEDIAN}{Logical; if TRUE, evaluates only at median of predictors (default: FALSE).}

\item{Q1}{Logical; if TRUE, evaluates only at lower quartile (default: FALSE).}

\item{Q2}{Logical; if TRUE, evaluates only at upper quartile (default: FALSE).}

\item{alpha}{Significance level (default: 0.05).}

\item{MC}{Logical; if TRUE, uses parallel processing via mclapply (default: FALSE).}

\item{...}{Additional arguments passed to outlier detection function.}
}
\value{
A list with components:
\item{p.crit}{Critical p-value used for confidence intervals.}
\item{p.value}{P-value for testing whether predictor strengths differ.}
\item{str.x1.given.x2}{Strength of x1 conditional on x2 at each evaluation point.}
\item{str.x2.given.x1}{Strength of x2 conditional on x1 at each evaluation point.}
\item{mean.str1}{Mean strength for predictor 1.}
\item{mean.str2}{Mean strength for predictor 2.}
\item{ci.low}{Lower confidence bound for difference (mean.str1 - mean.str2).}
\item{ci.hi}{Upper confidence bound for difference.}
\item{pts.x1}{Evaluation points used for predictor 1.}
\item{pts.x2}{Evaluation points used for predictor 2.}
}
\description{
For two predictors, compares their relative importance using LOESS regression
and provides bootstrap confidence intervals for the difference in importance.
}
\details{
This function extends \code{\link{lplotcom2v2}} by adding bootstrap confidence intervals
for the difference in predictor importance.

\strong{Evaluation points}:
\itemize{
\item \strong{Default}: Uses quartiles (0.25, 0.5, 0.75) for both predictors
\item \strong{MEDIAN=TRUE}: Uses only the median
\item \strong{Q1=TRUE}: Uses only the lower quartile (0.25)
\item \strong{Q2=TRUE}: Uses only the upper quartile (0.75)
\item \strong{SEQ=TRUE}: Uses npts equally spaced points in the interquartile range (or MAD-based range if MAD.OP=TRUE)
}

\strong{Critical p-value} is automatically computed based on sample size if not specified.
This adjusts the confidence interval width to maintain proper coverage.

The \strong{p-value} tests H0: predictors have equal importance. Small values indicate
significant difference in importance.

\strong{Requires n >= 50} for reliable results.
}
\examples{
\dontrun{
# Compare two predictors with bootstrap CI
x1 <- rnorm(100)
x2 <- rnorm(100)
y <- 2*x1 + 0.5*x2 + rnorm(100)
X <- cbind(x1, x2)
result <- lplotcomBCI(X, y, nboot = 200)
result$p.value  # Test for difference
result$ci.low   # Lower bound for difference
result$ci.hi    # Upper bound
}
}
\seealso{
\code{\link{lplotcom2}}, \code{\link{lplotcom2v2}}, \code{\link{lplot}}
}
