% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{qhomt}
\alias{qhomt}
\title{Test Homoscedasticity Using Quantile Regression Slopes}
\usage{
qhomt(
  x,
  y,
  nboot = 100,
  alpha = 0.05,
  qval = c(0.2, 0.8),
  plotit = TRUE,
  SEED = TRUE,
  pch = "*",
  xlab = "X",
  ylab = "Y",
  xout = FALSE,
  outfun = outpro,
  pr = TRUE,
  WARN = FALSE,
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix with one column (predictor variable).}

\item{y}{Numeric vector (response variable).}

\item{nboot}{Integer. Number of bootstrap samples (default: 100).}

\item{alpha}{Numeric. Alpha level for confidence interval (default: 0.05).}

\item{qval}{Numeric vector of length 2 specifying which quantiles to compare
(default: c(.2,.8)). Tests difference qval\link{2} - qval\link{1}.}

\item{plotit}{Logical. If \code{TRUE}, plots the quantile regression lines
(default: \code{TRUE}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed to 2 (default: \code{TRUE}).}

\item{pch}{Plotting character (default: '*').}

\item{xlab}{Character string for x-axis label (default: "X").}

\item{ylab}{Character string for y-axis label (default: "Y").}

\item{xout}{Logical. If \code{TRUE}, removes outliers from predictors using
\code{outfun} (default: \code{FALSE}).}

\item{outfun}{Function for outlier detection (default: \code{\link{outpro}}).}

\item{pr}{Logical. If \code{TRUE}, prints progress messages (default: \code{TRUE}).}

\item{WARN}{Logical. If \code{FALSE}, suppresses warnings (default: \code{FALSE}).}

\item{...}{Additional arguments passed to \code{outfun}.}
}
\value{
List with components:
\describe{
\item{slope.bottom}{Slope estimate for lower quantile (qval\link{1})}
\item{slope.top}{Slope estimate for upper quantile (qval\link{2})}
\item{dif.est}{Estimated difference in slopes}
\item{dif.ci}{Bootstrap confidence interval for difference}
\item{p.value}{Two-sided p-value}
\item{se}{Standard error of difference}
\item{adjusted.ci}{Adjusted CI (when alpha=.05 and qval=c(.2,.8))}
}
}
\description{
Tests the hypothesis that the error term is homogeneous by comparing quantile
regression slopes. Computes a bootstrap confidence interval for the difference
between two quantile regression slopes (default: 0.8 quantile slope minus
0.2 quantile slope).
}
\details{
Uses Koenker-Bassett quantile regression. If slopes differ significantly,
suggests heteroscedasticity. Only works with one predictor; use \code{\link{qhomtv2}}
for multiple predictors.
}
\seealso{
\code{\link{qhomtv2}}, \code{\link{qplotreg}}
}
\keyword{htest}
\keyword{regression}
