% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{logIVcom}
\alias{logIVcom}
\title{Compare Strength of Association for Independent Variable Subsets in Logistic Regression}
\usage{
logIVcom(
  x,
  y,
  IV1 = 1,
  IV2 = 2,
  nboot = 500,
  xout = FALSE,
  outfun = outpro,
  SEED = TRUE,
  val = NULL,
  ...
)
}
\arguments{
\item{x}{Matrix or data frame of independent variables.}

\item{y}{Binary vector or vector with multiple discrete values. If not binary,
see \code{val} parameter.}

\item{IV1}{Integer vector specifying column indices of first subset of predictors
to compare (default: 1).}

\item{IV2}{Integer vector specifying column indices of second subset of predictors
to compare (default: 2). Example: \code{IV1=c(1,2), IV2=3} compares variables
1-2 against variable 3.}

\item{nboot}{Integer. Number of bootstrap samples (default: 500).}

\item{xout}{Logical. If \code{TRUE}, removes outliers from predictors before fitting
(default: \code{FALSE}).}

\item{outfun}{Function for multivariate outlier detection (default: \code{\link{outpro}}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{val}{Integer. If \code{y} has K > 2 unique values, specifies which value to
use as the "success" outcome. Default uses the largest value.}

\item{...}{Additional arguments passed to \code{outfun}.}
}
\value{
List with components:
\describe{
\item{n.keep}{Number of observations retained after outlier removal}
\item{est.1}{SD of predicted probabilities using IV1 subset}
\item{est.2}{SD of predicted probabilities using IV2 subset}
\item{p.value}{Two-sided p-value testing whether association strengths differ}
}
}
\description{
Compares the strength of association between two subsets of independent variables
and a binary dependent variable using logistic regression. Uses bootstrap to test
whether one set of predictors has stronger association than another.
}
\details{
\strong{Comparison Method}:
\enumerate{
\item Fit logistic regression using each subset of predictors
\item Compute predicted probabilities P(Y=1|X) for each subset
\item Measure association strength as SD of predicted probabilities
\item Bootstrap to test H0: SD(IV1 predictions) = SD(IV2 predictions)
}

Higher SD of predicted probabilities indicates stronger discriminatory power.

\strong{For non-binary Y}: If \code{y} has K unique values (e.g., 2, 4, 8), the function
focuses on predicting a specific outcome. By default, uses the largest value
(e.g., P(Y=8|X) in the example).
}
\examples{
\dontrun{
set.seed(123)
x <- matrix(rnorm(100), ncol = 5)
# Make first 2 variables associated with outcome
p <- plogis(x[,1] + 0.8*x[,2])
y <- rbinom(20, 1, p)

# Compare variables 1-2 vs variables 3-5
logIVcom(x, y, IV1 = c(1,2), IV2 = c(3,4,5), nboot = 200)
}
}
\seealso{
\code{\link{logreg.pred}} for predicted probabilities,
\code{\link{logistic.LR}} for likelihood ratio test,
\code{\link{wlogreg}} for robust logistic regression
}
\keyword{htest}
\keyword{regression}
