% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{skip.o.lap}
\alias{skip.o.lap}
\title{Skipped Overlapping Correlation Difference}
\usage{
skip.o.lap(m, MC = FALSE, corfun = wincor, outfun = outpro, ...)
}
\arguments{
\item{m}{Numeric matrix with 3 columns: two explanatory variables (columns 1-2)
and one response variable (column 3).}

\item{MC}{Logical. If \code{TRUE}, uses multicore version \code{\link{outproMC}}
for outlier detection. Default is \code{FALSE}.}

\item{corfun}{Function for computing correlation. Default is \code{\link{wincor}}
(Winsorized correlation). Other options include \code{\link{pcor}}, \code{\link{pbcor}}, etc.}

\item{outfun}{Function for outlier detection. Default is \code{\link{outpro}}.}

\item{...}{Additional arguments passed to \code{corfun}.}
}
\value{
Numeric scalar: the difference between the two correlations (r₁ - r₂).
}
\description{
For two explanatory variables, computes skipped correlations with a response
variable and returns the difference. Useful for comparing the strength of
association between two predictors and an outcome after outlier removal.
}
\details{
The function:
\enumerate{
\item Identifies and removes outliers from the three-column matrix
\item Computes correlation between column 1 and column 3 (r₁)
\item Computes correlation between column 2 and column 3 (r₂)
\item Returns the difference r₁ - r₂
}

This is useful for testing whether two predictors have different associations
with an outcome, after accounting for outliers.
}
\seealso{
\code{\link{skip}}, \code{\link{skip.gen.cor}}, \code{\link{wincor}},
\code{\link{outpro}}
}
\keyword{multivariate}
\keyword{robust}
