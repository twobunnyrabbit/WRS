% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{ancbbpb}
\alias{ancbbpb}
\title{ANCOVA Bootstrap Bagging with Percentile Bootstrap}
\usage{
ancbbpb(
  x1,
  y1,
  x2,
  y2,
  fr1 = 1,
  est = tmean,
  fr2 = 1,
  nboot = 200,
  pts = NA,
  plotit = TRUE,
  SCAT = TRUE,
  pch1 = "+",
  pch2 = "o",
  SEED = TRUE,
  alpha = 0.05,
  RNA = TRUE,
  sm = FALSE,
  LP = TRUE,
  xout = FALSE,
  outfun = outpro,
  ...
)
}
\arguments{
\item{x1}{Covariate values for group 1.}

\item{y1}{Dependent variable for group 1.}

\item{x2}{Covariate values for group 2.}

\item{y2}{Dependent variable for group 2.}

\item{fr1}{Span for running interval smoother for group 1 (default: 1).}

\item{est}{Estimator function to use for computing location or other
statistics. Common choices include \code{mean}, \code{median}, \code{tmean} (trimmed mean),
\code{mom} (modified one-step M-estimator), \code{mest} (M-estimator).}

\item{fr2}{Span for running interval smoother for group 2 (default: 1).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{pts}{Design points for comparison. If NA, uses 5 empirically chosen points.}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{SCAT}{Logical. If FALSE, suppresses scatterplot (default: TRUE).}

\item{pch1}{Plotting character for group 1 (default: '+').}

\item{pch2}{Plotting character for group 2 (default: 'o').}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{RNA}{Logical. If TRUE, removes NA values when computing bagged estimates.}

\item{sm}{Logical. If FALSE, doesn't use bootstrap bagging when plotting.}

\item{LP}{Logical. If TRUE, keeps leverage points when plotting.}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
List with:
\item{output}{Matrix with design points, sample sizes, differences, CIs, p-values, and critical p-values}
}
\description{
Compares two independent groups using ANCOVA with bootstrap bagging and
running interval smoother. Provides more general estimator options than ancbbmed.
Computes percentile bootstrap confidence intervals.
}
\examples{
\dontrun{
x1 <- rnorm(50)
y1 <- x1 + rnorm(50)
x2 <- rnorm(50)
y2 <- x2 + 0.5 + rnorm(50)
ancbbpb(x1, y1, x2, y2, nboot=500)
}
}
