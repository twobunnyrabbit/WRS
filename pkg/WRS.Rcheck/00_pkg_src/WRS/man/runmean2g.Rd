% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00-utils-core.R
\name{runmean2g}
\alias{runmean2g}
\title{Running Interval Smoother Plot for Two Groups}
\usage{
runmean2g(
  x1,
  y1,
  x2,
  y2,
  fr = 0.8,
  est = tmean,
  xlab = "X",
  ylab = "Y",
  SCAT = TRUE,
  sm = FALSE,
  nboot = 40,
  SEED = TRUE,
  eout = FALSE,
  xout = FALSE,
  outfun = out,
  LP = FALSE,
  pch1 = "*",
  pch2 = "+",
  ...
)
}
\arguments{
\item{x1, y1}{Numeric vectors for group 1 (predictor and response).}

\item{x2, y2}{Numeric vectors for group 2 (predictor and response).}

\item{fr}{Fraction controlling amount of smoothing (default: 0.8).}

\item{est}{Estimator function to use for computing location or other
statistics. Common choices include \code{mean}, \code{median}, \code{tmean} (trimmed mean),
\code{mom} (modified one-step M-estimator), \code{mest} (M-estimator).}

\item{xlab, ylab}{Axis labels for the plot.}

\item{SCAT}{Logical. If TRUE, includes scatterplot points (default: TRUE).}

\item{sm}{Logical. If TRUE, uses bootstrap bagging for smoothing (default: FALSE).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{eout}{Logical value. If \code{TRUE}, outliers are removed based on
eliminating points with large residuals or leverage (default: \code{FALSE}).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{LP}{Logical. If TRUE, uses linear pooling (default: FALSE).}

\item{pch1, pch2}{Plotting characters for groups 1 and 2 (defaults: '*', '+').}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
Invisibly returns NULL. Function is called for its side effect of creating a plot.
}
\description{
Creates a plot comparing running interval smoothers for two independent groups,
allowing visual comparison of regression patterns between groups.
}
\details{
The function fits running interval smoothers to both groups and plots them
for comparison (group 1 with solid line, group 2 with dashed line). The
smoother uses a robust estimator (default: trimmed mean) within moving
windows controlled by \code{fr}.

When \code{sm = TRUE}, bootstrap bagging is used with \code{nboot} samples.
Missing values are automatically removed. Outliers can be removed via
\code{eout} or \code{xout}.
}
\examples{
set.seed(123)
x1 <- rnorm(50)
y1 <- 2*x1 + rnorm(50)
x2 <- rnorm(50)
y2 <- 3*x2 + rnorm(50)
runmean2g(x1, y1, x2, y2)

}
