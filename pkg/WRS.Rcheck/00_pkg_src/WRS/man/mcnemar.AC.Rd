% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{mcnemar.AC}
\alias{mcnemar.AC}
\title{McNemar Test with Agresti-Caffo Method}
\usage{
mcnemar.AC(x, y = NULL, method = "AC", alpha = 0.05)
}
\arguments{
\item{x}{Either a 2x2 contingency matrix or a factor/vector if \code{y} is provided.}

\item{y}{Optional factor/vector if \code{x} is not a matrix. Default is \code{NULL}.}

\item{method}{Method for confidence interval. Currently only 'AC' (Agresti-Caffo)
is supported. Default is 'AC'.}

\item{alpha}{Alpha level for confidence interval. Default is 0.05.}
}
\value{
Confidence interval for the difference in proportions using the
Agresti-Caffo method, or a warning message if B+C < 5.
}
\description{
Perform McNemar's test for paired binary data using the Agresti-Caffo (AC)
confidence interval method for the difference in proportions.
}
\details{
McNemar's test is used for paired nominal data to determine if row and column
marginal frequencies are equal. This version uses the Agresti-Caffo method for
constructing confidence intervals.

If a matrix is provided, it must be 2x2 with non-negative finite entries.
If vectors are provided, they must have the same length and exactly 2 levels each.

The test requires B+C ≥ 5, where B and C are the off-diagonal cells. If B+C < 5,
a warning message is returned instead of results.
}
\seealso{
\code{\link{binom.conf}}, \code{\link{mcnemar.test}}
}
\keyword{htest}
