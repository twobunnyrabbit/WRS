% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{Stein2g}
\alias{Stein2g}
\title{Stein's Two-Stage Method for Two Independent Groups}
\usage{
Stein2g(
  x,
  y = NULL,
  delta,
  alpha = 0.05,
  power = 0.8,
  z.sqrt = NULL,
  tr = 0.2,
  reps = 1e+05,
  SEED = TRUE
)
}
\arguments{
\item{x}{Data for group 1 (vector), or matrix with 2 columns, or list with 2 elements.}

\item{y}{Data for group 2 (vector). If \code{NULL}, \code{x} must contain both groups.}

\item{delta}{Minimum detectable difference between group locations.}

\item{alpha}{Significance level. Default is 0.05.}

\item{power}{Desired power. Default is 0.8.}

\item{z.sqrt}{Optional pre-computed standardization factor. Default \code{NULL}
computes it from data.}

\item{tr}{Amount of trimming. Default is 0.2 (20\% trimming).}

\item{reps}{Number of Monte Carlo replications. Default is 100000.}

\item{SEED}{Logical. If \code{TRUE} (default), set random seed when computing z.sqrt.}
}
\value{
List with components:
\item{n}{Current sample sizes for each group (vector of length 2)}
\item{N}{Required total sample sizes for each group (vector of length 2)}
\item{z.sqrt}{Standardization factor used in calculations}
}
\description{
Stein-type two-stage method for determining required sample sizes for two
independent groups to achieve desired power for detecting a difference in
location of size delta.
}
\details{
This function implements Stein's two-stage method for two independent groups using
robust estimators (trimmed means and winsorized variance).

Based on initial data, it determines how many total observations are needed in each
group to achieve the specified power for detecting a difference of size delta.

If current sample sizes are sufficient, they are returned. Otherwise, the function
indicates how many total observations are needed per group.
}
\seealso{
\code{\link{Stein.pairs}}, \code{\link{stein1.tr}}, \code{\link{yuen}}
}
\keyword{design}
\keyword{htest}
\keyword{robust}
