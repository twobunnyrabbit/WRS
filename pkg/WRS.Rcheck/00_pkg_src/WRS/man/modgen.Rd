% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{modgen}
\alias{modgen}
\title{Generate All Possible Regression Models}
\usage{
modgen(p, adz = FALSE)
}
\arguments{
\item{p}{Number of predictor variables (maximum 5).}

\item{adz}{Logical. If \code{TRUE}, adds a model with only a location measure
(intercept-only model). Default is \code{FALSE}.}
}
\value{
List of numeric vectors, where each vector contains the indices of
predictors to include in that model. If \code{adz=TRUE}, the last element
is 0 (intercept-only model).
}
\description{
Generates all possible subsets of predictors for regression model selection.
Used internally by \code{regpre} for exhaustive model search.
}
\details{
This function generates all possible combinations of \code{p} predictors for
regression analysis. It creates a list where each element specifies which
predictors to include in a model.

Number of models generated:
\itemize{
\item p=1: 1 model
\item p=2: 3 models
\item p=3: 7 models
\item p=4: 15 models
\item p=5: 31 models
}

If \code{adz=TRUE}, one additional model (intercept-only) is added, indicated by 0.

The current version is limited to a maximum of 5 predictors.
}
\examples{
\dontrun{
modgen(3)  # Generates 7 models for 3 predictors
modgen(3, adz=TRUE)  # Generates 8 models (including intercept-only)
}

}
\seealso{
\code{regpre}
}
\keyword{regression}
