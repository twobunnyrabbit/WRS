% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression-advanced.R
\name{regpord}
\alias{regpord}
\title{Compare Strength of Association for Standardized Predictors}
\usage{
regpord(
  x,
  y,
  nboot = 100,
  alpha = 0.05,
  SEED = TRUE,
  xout = FALSE,
  cov.fun = cov.mba,
  pr = TRUE,
  plotit = TRUE,
  xlab = "Standardized Predictors",
  ylab = "Y",
  est = mean,
  scat = var,
  ...
)
}
\arguments{
\item{x}{A numeric matrix containing predictor values (must have 2 or more columns).}

\item{y}{A numeric vector containing the response variable.}

\item{nboot}{Number of bootstrap samples for standard error estimation (default: 100).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{cov.fun}{Covariance function to use for generalized variance computation
(default: \code{cov.mba} for Median Ball Algorithm).}

\item{pr}{Logical. If \code{TRUE}, prints progress messages (default: \code{TRUE}).}

\item{plotit}{Logical. If \code{TRUE} and there are exactly 2 predictors, creates
a scatterplot distinguishing the two predictors (default: \code{TRUE}).}

\item{xlab, ylab}{Axis labels for the plot.}

\item{est}{Estimator for standardizing predictors (default: \code{mean}).}

\item{scat}{Scatter measure for standardizing predictors (default: \code{var}).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
A list with components:
\item{crit.value}{Critical value for significance testing (approximately 2.06 - 5.596/sqrt(n)).}
\item{est}{Vector of estimated generalized variance values for each predictor
(lower values indicate stronger association).}
\item{results}{Data frame with columns:
\itemize{
\item Pred.: Index of first predictor in comparison
\item Pred: Index of second predictor in comparison
\item test.stat: Test statistic for comparing the two predictors
\item Decision: "reject" if predictors differ significantly, "fail to reject" otherwise
}}
}
\description{
Compares the strength of association between the response and each of
multiple predictors using robust covariance-based generalized variance,
with predictors standardized to enable fair comparison.
}
\details{
This function evaluates and compares predictor importance:
\enumerate{
\item Standardizes all predictors to have comparable scales
\item For each predictor, computes the generalized variance (via robust
covariance) of the response with that predictor
\item Uses bootstrap to estimate standard errors for pairwise comparisons
\item Tests whether predictors differ significantly in their association strength
}

The generalized variance provides a robust measure of association strength.
Lower values indicate stronger association (less residual variability).

For visualization when p=2, creates a scatterplot with different symbols
for each predictor plotted against Y (both on standardized scale).
}
\note{
Outlier removal with \code{xout=TRUE} removes cases from predictor space
before analysis using the function specified by \code{outfun}.
}
\examples{
\dontrun{
# Compare strength of association for 3 predictors
set.seed(123)
x <- matrix(rnorm(100*3), ncol=3)
y <- 2*x[,1] + 0.5*x[,2] + rnorm(100)  # x[,1] is strongest
regpord(x, y)
}
}
\references{
Wilcox, R.R. (2022). Introduction to Robust Estimation and Hypothesis Testing
(5th ed.). Academic Press.
}
\seealso{
\code{\link{regvarp}}, \code{\link{regIVstr}}, \code{\link{regIVcom}}
}
