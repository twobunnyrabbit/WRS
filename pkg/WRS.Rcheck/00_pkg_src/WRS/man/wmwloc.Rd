% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{wmwloc}
\alias{wmwloc}
\title{Estimate Location for Distribution of Pairwise Differences}
\usage{
wmwloc(x, y, na.rm = TRUE, est = median, ...)
}
\arguments{
\item{x}{Numeric vector for group 1.}

\item{y}{Numeric vector for group 2.}

\item{na.rm}{Logical; if TRUE, removes missing values (default TRUE).}

\item{est}{Estimator function to apply to the pairwise differences (default \code{median}).}

\item{...}{Additional arguments passed to the estimator function.}
}
\value{
The estimated location measure for the distribution of X - Y.
}
\description{
Estimates a measure of location for the distribution of all pairwise differences
X - Y. This is a helper function used by various WMW-based procedures.
}
\details{
The function computes all pairwise differences between x and y values (creating
an outer product), then applies the specified estimator. This is useful for
WMW-type analyses that don't assume independence.
}
\examples{
x <- c(1, 2, 3, 4, 5)
y <- c(2, 3, 4)

# Median of all pairwise differences
wmwloc(x, y)

# Using mean instead
wmwloc(x, y, est = mean)
}
\seealso{
\code{\link{wmwpb}}, \code{\link{wmwloc2}}, \code{\link{cid}}
}
