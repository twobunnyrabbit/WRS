% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{oph.astig.depcom}
\alias{oph.astig.depcom}
\title{Compare Dependent Groups for Astigmatism Prediction Errors}
\usage{
oph.astig.depcom(
  x,
  tr = 0,
  con = 0,
  alpha = 0.05,
  nboot = 1999,
  invalid = 4,
  SEED = TRUE,
  STOP = TRUE,
  method = "holm"
)
}
\arguments{
\item{x}{Matrix with J columns or list of length J containing prediction error data}

\item{con}{Contrast matrix (default: 0, performs all pairwise comparisons)}

\item{invalid}{Maximum valid value in diopters (default: 4). Values exceeding this
threshold are flagged as invalid}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducibility}

\item{STOP}{Logical. If \code{TRUE}, stops execution when invalid values detected}

\item{method}{P-value adjustment method (default: 'holm')}
}
\value{
List with component:
\describe{
\item{output}{Matrix with columns: V1 (group 1), V2 (group 2), n (sample size),
Mean.1, Mean.2, p.value, adj.p.value (adjusted p-value)}
}
}
\description{
Performs all pairwise comparisons of dependent measures for astigmatism prediction
errors using trimmed means and bootstrap confidence intervals. Validates data by
checking for invalid values (typically > 4 diopters).
}
\details{
Compares all pairs of dependent formulas using difference scores. For each pair:
\itemize{
\item Computes trimmed mean difference with bootstrap-t confidence interval
\item Validates that all values ≤ \code{invalid} diopters and ≥ 0
\item Reports positions of any invalid or negative values
\item Adjusts p-values for multiple comparisons using specified method
}

Missing values are automatically removed before analysis.
}
\seealso{
\code{\link{oph.astig.indepcom}}, \code{\link{oph.astig.depcom.unimean.var}},
\code{\link{trimcibt}}
}
\keyword{ophthalmology}
