% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{reglev.gen}
\alias{reglev.gen}
\alias{outblp}
\title{General Leverage Point Detection for Regression}
\usage{
reglev.gen(
  x,
  y,
  regfun = tsreg,
  outfun = outpro.depth,
  regout = outpro,
  crit = sqrt(qchisq(0.975, 1)),
  plotit = TRUE,
  xlab = "X",
  ylab = "Y",
  outplot = FALSE,
  DIS = FALSE,
  ...
)

outblp(
  x,
  y,
  regfun = tsreg,
  outfun = outpro.depth,
  regout = outpro,
  crit = sqrt(qchisq(0.975, 1)),
  plotit = TRUE,
  xlab = "X",
  ylab = "Y",
  outplot = FALSE,
  DIS = FALSE,
  ...
)
}
\arguments{
\item{x}{Predictor matrix (n × p).}

\item{y}{Response vector.}

\item{regfun}{Regression function to use (default: \code{tsreg} for Theil-Sen).}

\item{outfun}{Outlier detection function for identifying leverage points
(default: \code{outpro.depth}). For univariate, skewed \code{x}, consider
\code{outfun=outmc}.}

\item{regout}{Outlier detection method (default: \code{outpro}). Not currently used.}

\item{crit}{Critical value for residual outlier detection (default:
\code{sqrt(qchisq(.975, 1))} ≈ 1.96).}

\item{plotit}{Logical; if \code{TRUE} and \code{x} is univariate, plots data
with points marked: '.' = kept, '*' = good leverage, 'o' = bad leverage
(default: \code{TRUE}).}

\item{xlab, ylab}{Axis labels for plot (defaults: 'X', 'Y').}

\item{outplot}{Logical; if \code{TRUE}, shows outlier detection plot from
\code{outfun} (default: \code{FALSE}).}

\item{DIS}{Logical; if \code{TRUE}, returns distances used for leverage
detection (default: \code{FALSE}). Currently not implemented.}

\item{...}{Additional arguments passed to \code{outfun} and \code{regfun}.}
}
\value{
A list with components:
\item{n.lev}{Number of leverage points detected}
\item{lev.pts}{Indices of all leverage points}
\item{good.lev}{Indices of good leverage points (leverage but not bad)}
\item{bad.lev}{Indices of bad leverage points}
\item{res.out.id}{Indices of residual outliers}
\item{keep}{Indices of observations to keep (not bad leverage points)}
}
\description{
Identifies good and bad leverage points in regression using a specified
regression estimator and outlier detection method. This is a more general
version of \code{reglev} that allows customization of detection methods.
}
\details{
\strong{Strategy:}
\enumerate{
\item Detect leverage points in predictor space using \code{outfun}
\item If leverage points exist, remove them and fit regression
\item Compute residuals for all data using this fit
\item Flag residual outliers using MAD-median rule
\item \strong{Bad leverage points} are leverage points that are also residual outliers
\item \strong{Good leverage points} are leverage points with non-outlying residuals
}

A bad leverage point has unusual predictor values AND unusual response
values (high influence). Good leverage points have unusual predictors but
fit the regression pattern.
}
\seealso{
\code{\link{reglev.est}}, \code{\link{regtest.blp}}, \code{\link{outpro.depth}}, \code{\link{tsreg}}
}
\keyword{regression}
