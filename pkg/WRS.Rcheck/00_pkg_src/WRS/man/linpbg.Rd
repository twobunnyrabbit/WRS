% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{linpbg}
\alias{linpbg}
\title{Percentile Bootstrap Linear Contrasts for Independent Groups (General Estimator)}
\usage{
linpbg(x, con = 0, alpha = 0.05, nboot = NA, est = mest, ...)
}
\arguments{
\item{x}{Data in matrix or list format. For matrix: each column is a group.
For list: x[\link{j}] contains data for group j.}

\item{con}{J×d matrix of contrast coefficients, where J = number of groups and
d = number of contrasts. Default (con=0) performs all pairwise comparisons.}

\item{alpha}{Significance level (default: 0.05).}

\item{nboot}{Number of bootstrap samples (default: NA, automatically determined as
5000 for >4 contrasts, 2000 otherwise).}

\item{est}{Estimator function (default: \code{\link{mest}} M-estimator).}

\item{...}{Additional arguments passed to the estimator function.}
}
\value{
List with components:
\describe{
\item{psihat}{Matrix with columns: con.num, psihat, ci.lower, ci.upper}
\item{test}{Matrix with columns: con.num, test (p-value), crit.val}
\item{con}{Contrast coefficient matrix used}
}
}
\description{
Computes percentile bootstrap confidence intervals for linear contrasts of
independent groups using a general robust estimator (default: M-estimator).
}
\details{
This function:
\itemize{
\item Assumes independent groups (each group sampled separately)
\item Uses percentile bootstrap method with Rom's critical values
\item Automatically removes missing values
\item Sets random seed to 2 for reproducibility
\item Uses calibrated critical values for simultaneous inference
}

Example contrasts:
\itemize{
\item con\link{,1} = c(1,1,-1,-1,0,0): sum of first two equals sum of second two
\item con\link{,2} = c(1,-1,0,0,1,-1): difference in first pair equals difference in second pair
}
}
\examples{
# Four independent groups with M-estimator
set.seed(123)
x <- list(rnorm(25), rnorm(25, mean = 0.5),
          rnorm(25, mean = 0.3), rnorm(25, mean = 0.9))

# Compare group 1 vs group 2
con <- matrix(c(1, -1, 0, 0), ncol = 1)
result <- linpbg(x, con = con, nboot = 1000)
}
\seealso{
\code{\link{lintpb}} (for trimmed means specifically), \code{\link{lindm}} (dependent groups),
\code{\link{linconpb}}, \code{\link{mest}}
}
