% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{corblp.bca.C}
\alias{corblp.bca.C}
\title{BCA Confidence Interval for Correlation with Bad Leverage Point Removal}
\usage{
corblp.bca.C(
  x,
  y,
  regfun = tsreg,
  varfun = pbvar,
  nboot = 1000,
  alpha = 0.05,
  outfun = outpro.depth,
  SEED = TRUE,
  plotit = FALSE,
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{regfun}{Regression function to use (default: \code{tsreg}).}

\item{varfun}{Variance function for weighted correlation (default: \code{pbvar}).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{outfun}{Function for detecting outliers/leverage points (default: \code{outpro.depth}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{plotit}{Logical. If \code{TRUE}, creates diagnostic plots (default: \code{FALSE}).}

\item{...}{Additional arguments passed to \code{bcajack2}.}
}
\value{
A list with components:
\item{cor}{Robust correlation coefficient}
\item{ci}{BCA confidence interval (vector of length 2)}
}
\description{
Computes a BCA bootstrap confidence interval for a robust correlation
coefficient based on regression with bad leverage points removed.
}
\details{
This function computes a robust correlation coefficient using the
\code{corblp} method, which removes bad leverage points before computing
the correlation. The BCA bootstrap is used to construct confidence
intervals.

Only bivariate data (single predictor and single response) are allowed.
The function will stop with an error if more than one predictor is provided.

Requires the \code{bcaboot} package.
}
\examples{
x <- rnorm(50)
y <- 0.6*x + rnorm(50)
corblp.bca.C(x, y, nboot=500)
}
\seealso{
\code{\link{corblp}}, \code{\link{tsreg}}, \code{\link{pbvar}}
}
