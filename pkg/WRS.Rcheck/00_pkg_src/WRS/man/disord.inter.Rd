% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{disord.inter}
\alias{disord.inter}
\title{Test for Disordinal Interaction in J×K Factorial Design}
\usage{
disord.inter(J, K, x, con = NULL, ROW = TRUE, tr = 0.2)
}
\arguments{
\item{J}{Integer. Number of levels of Factor A.}

\item{K}{Integer. Number of levels of Factor B.}

\item{x}{Numeric matrix, data frame, or list. Data for J×K groups.
If matrix/data frame, should have J×K columns in standard order.}

\item{con}{Numeric matrix or NULL. Contrast coefficients for interaction comparisons
(default: NULL uses all pairwise interaction contrasts).}

\item{ROW}{Logical. Direction of comparison:
\itemize{
\item TRUE: Compare differences over Factor B (second factor) for each pair of
Factor A levels (default)
\item FALSE: Compare differences over Factor A (first factor) for each pair of
Factor B levels
}}

\item{tr}{Numeric. Trimming proportion for robust comparison (default: 0.2).}
}
\value{
List with components:
\describe{
\item{con}{Contrast matrix used}
\item{results}{Matrix with columns:
\itemize{
\item \code{est1.1}: Trimmed mean estimate for group 1 in first comparison
\item \code{est1.2}: Trimmed mean estimate for group 2 in first comparison
\item \code{p.value.1}: P-value for first simple effect
\item \code{est2.1}: Trimmed mean estimate for group 1 in second comparison
\item \code{est2.2}: Trimmed mean estimate for group 2 in second comparison
\item \code{p.value.2}: P-value for second simple effect
}
Each row corresponds to one interaction contrast}
}
}
\description{
Tests for disordinal interactions in a J×K between-subjects factorial design.
A disordinal interaction occurs when the direction of an effect reverses across
levels of another factor.
}
\details{
The function tests whether interaction effects change direction by:
\enumerate{
\item For each interaction contrast, computing simple effects at two levels
\item Using Yuen's test to compare trimmed means for each simple effect
\item Examining whether the two simple effects have opposite signs (disordinal)
}

\strong{ROW=TRUE example}: For a 2×3 design, compares:
\itemize{
\item (A1,B1 vs A1,B2) against (A2,B1 vs A2,B2)
\item Tests if effect of B differs in direction between levels of A
}

\strong{ROW=FALSE example}: Tests if effect of A differs in direction between levels of B

Disordinal interactions are particularly important as they indicate that:
\itemize{
\item Treatment effectiveness reverses across subgroups
\item Simple effects interpretations must be qualified
\item Main effects may be misleading
}
}
\examples{
# 2x2 design with disordinal interaction
set.seed(123)
g11 <- rnorm(20, mean = 0)
g12 <- rnorm(20, mean = 1)
g21 <- rnorm(20, mean = 1)
g22 <- rnorm(20, mean = 0)  # Reversal
x <- list(g11, g12, g21, g22)
result <- disord.inter(J = 2, K = 2, x)
result$results
}
\seealso{
\code{\link{yuen}}, \code{\link{con2way}}, \code{\link{disband}}
}
\keyword{design}
\keyword{htest}
\keyword{robust}
