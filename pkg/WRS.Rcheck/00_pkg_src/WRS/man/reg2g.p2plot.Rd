% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{reg2g.p2plot}
\alias{reg2g.p2plot}
\title{3D Plot of Two Regression Surfaces for Group Comparison}
\usage{
reg2g.p2plot(
  x1,
  y1,
  x2,
  y2,
  xout = FALSE,
  outfun = outpro,
  xlab = "Var 1",
  ylab = "Var 2",
  zlab = "Var 3",
  regfun = tsreg,
  COLOR = TRUE,
  STAND = TRUE,
  tick.marks = TRUE,
  type = "p",
  pr = TRUE,
  ...
)
}
\arguments{
\item{x1}{Matrix or data frame with 2 columns of predictor values for group 1.}

\item{y1}{Numeric vector of response values for group 1.}

\item{x2}{Matrix or data frame with 2 columns of predictor values for group 2.}

\item{y2}{Numeric vector of response values for group 2.}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{xlab}{Label for x-axis in plots (default: "X" or "VAR 1").}

\item{ylab}{Label for y-axis in plots (default: "Y" or "VAR 2").}

\item{regfun}{Regression function to use for fitting (default: \code{tsreg}
for Theil-Sen regression). Must return a list with a \code{coef} component.}

\item{COLOR}{Logical. If \code{TRUE}, plots group 1 plane in blue and group 2
in red (default: \code{TRUE}).}

\item{STAND}{Logical. If \code{TRUE}, uses standardized outlier detection
(default: \code{TRUE}).}

\item{tick.marks}{Logical. If \code{TRUE}, adds tick marks to 3D plot (default: \code{TRUE}).}

\item{type}{Type of plot. "p" for points, "n" for regression planes only
(default: "p").}

\item{pr}{Logical. If \code{TRUE}, prints progress messages (default: \code{TRUE}).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
List with components:
\item{coef.group.1}{Regression coefficients for group 1}
\item{coef.group.2}{Regression coefficients for group 2}
}
\description{
Creates a 3D scatter plot with fitted regression planes for two independent
groups when there are two predictor variables. Useful for visualizing and
comparing multiple regression relationships between groups.
}
\details{
Requires the \code{scatterplot3d} package. The function removes missing values
automatically using \code{elimna()}. First group regression plane is blue (if COLOR=TRUE),
second group is red.
}
\examples{
\dontrun{
# Compare 3D regression surfaces for two groups
x1 <- matrix(rnorm(100), ncol=2)
y1 <- 2*x1[,1] + 3*x1[,2] + rnorm(50)
x2 <- matrix(rnorm(100), ncol=2)
y2 <- 3*x2[,1] + 2*x2[,2] + rnorm(50)
reg2g.p2plot(x1, y1, x2, y2)
}
}
\seealso{
\code{\link{reg2plot}}, \code{\link{regp2plot}}
}
