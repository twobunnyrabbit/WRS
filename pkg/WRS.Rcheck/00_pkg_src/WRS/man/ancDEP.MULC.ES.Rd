% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{ancDEP.MULC.ES}
\alias{ancDEP.MULC.ES}
\title{ANCOVA Effect Sizes for Dependent Groups with Multiple Covariates}
\usage{
ancDEP.MULC.ES(
  x1,
  y1,
  y2,
  fr1 = 1.5,
  fr2 = 1.5,
  tr = 0.2,
  pts = NULL,
  xout = FALSE,
  outfun = outpro,
  cov.fun = skip.cov,
  ...
)
}
\arguments{
\item{x1}{Matrix of covariate values (must have 2+ columns).}

\item{y1}{Response values for condition 1.}

\item{y2}{Response values for condition 2 (paired with y1).}

\item{fr1}{Span for condition 1 running interval smoother (default: 1.5).}

\item{fr2}{Span for condition 2 running interval smoother (default: 1.5).}

\item{tr}{Trimming proportion (default: 0.2). Proportion of observations to
trim from each tail when computing winsorized correlation.}

\item{pts}{Matrix of design points where effect sizes should be computed.
If NULL, uses significant points from ancdetM4.}

\item{xout}{Logical. If TRUE, remove outliers from covariates before analysis.}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{cov.fun}{Covariance function for determining nearest neighbors (default: skip.cov).}

\item{...}{Additional arguments passed to outfun or cov.fun.}
}
\value{
List with:
\item{pts}{Matrix of design points analyzed}
\item{ES}{Matrix of effect sizes (EP, QS, QStr, AKP) at each point}
\item{SML.class}{Classification of effect sizes (small/medium/large)}
}
\description{
For dependent (paired/repeated measures) groups with two or more covariates,
estimates effect sizes at specified design points. Uses a running interval
smoother approach to identify nearest neighbors and compute effect sizes
based on their response values.
}
\note{
Minimum sample size for each group is 50.
}
\examples{
\dontrun{
# Paired data with 2 covariates
x1 <- matrix(rnorm(100), ncol=2)
y1 <- rnorm(50)
y2 <- y1 + rnorm(50, mean=0.5)
result <- ancDEP.MULC.ES(x1, y1, y2, fr1=1.2, fr2=1.2)
}
}
