% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{tscor}
\alias{tscor}
\title{Theil-Sen Correlation}
\usage{
tscor(
  x,
  y,
  xout = FALSE,
  outfun = out,
  varfun = winvar,
  WARN = TRUE,
  HD = FALSE,
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{varfun}{Variance function (default: \code{winvar}).}

\item{WARN}{Logical. Show warnings if \code{TRUE} (default: \code{TRUE}).}

\item{HD}{Logical. Use Harrell-Davis estimator if \code{TRUE} (default: \code{FALSE}).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
List with component:
\item{cor}{Theil-Sen correlation (explanatory measure of association).}
}
\description{
Computes a correlation coefficient based on the Theil-Sen regression estimator.
}
\details{
This function computes an explanatory measure of association based on the
Theil-Sen regression estimator. The sign matches the sign of the Theil-Sen
slope, and the magnitude reflects the strength of association.

To obtain a p-value and confidence interval, use \code{tscorci()}.
}
\examples{
set.seed(123)
x <- rnorm(50)
y <- 2 * x + rnorm(50)
tscor(x, y)

# With outlier removal
x2 <- c(x, 10)
y2 <- c(y, -10)
tscor(x2, y2, xout = TRUE)
}
\seealso{
\code{\link{tscorci}}, \code{\link{tsreg}}, \code{\link{pbcor}}
}
