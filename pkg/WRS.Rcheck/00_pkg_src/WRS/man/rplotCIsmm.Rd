% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{rplotCIsmm}
\alias{rplotCIsmm}
\title{Running Interval Smoother with Flexible Confidence Band}
\usage{
rplotCIsmm(
  x,
  y,
  tr = 0.2,
  fr = 0.5,
  plotit = TRUE,
  scat = TRUE,
  pyhat = FALSE,
  SEED = TRUE,
  dfmin = 2,
  pts = NULL,
  npts = 25,
  nmin = 12,
  eout = FALSE,
  xout = FALSE,
  xlab = "x",
  ylab = "y",
  outfun = out,
  LP = TRUE,
  MID = TRUE,
  alpha = 0.05,
  pch = ".",
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{tr}{Trimming proportion (default: 0.2). Proportion of observations to
trim from each tail when computing winsorized correlation.}

\item{fr}{Span parameter controlling amount of smoothing (default: 0.5).
Lower values = less smoothing.}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{scat}{Logical. If \code{TRUE}, includes scatter plot of data points (default: \code{TRUE}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{dfmin}{Minimum degrees of freedom required for CIs (default: 2).}

\item{pts}{Optional numeric vector of specific points where CIs are computed
(default: NULL, automatically chosen).}

\item{npts}{Number of points where confidence intervals are computed (default: 25).
Pre-computed critical values available for 10 or 25 points.}

\item{nmin}{Minimum sample size required in each interval (default: 12).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{xlab}{Label for x-axis in plots (default: "X" or "VAR 1").}

\item{ylab}{Label for y-axis in plots (default: "Y" or "VAR 2").}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{MID}{Logical. If \code{TRUE}, computes intervals in middle range of data
(default: \code{TRUE}).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{pch}{Plotting character for scatter plot (default: '.').}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
List with components:
\item{output}{Matrix with columns: pts, y.hat, ci.low, ci.up}
\item{str}{Strength of association measure}
\item{n}{Total sample size}
\item{n.keep}{Sample size after outlier removal}
}
\description{
Computes confidence band for running interval smoother based on trimmed means,
using the Studentized maximum modulus distribution. More flexible than \code{rplotCI}
in terms of alpha levels and number of points, though intervals may be slightly
wider.
}
\details{
This function offers more flexibility than \code{rplotCI}: it works with any alpha
level and any number of points (not limited to 10 or 25). The confidence band
uses the Studentized maximum modulus distribution for simultaneous coverage.

Trade-off: \code{rplotCI} provides tighter confidence intervals when alpha=0.05 and
npts=10 or 25, but \code{rplotCIsmm} offers greater flexibility with minimal
computational overhead.
}
\examples{
\dontrun{
# Flexible confidence band with custom alpha
x <- rnorm(100)
y <- 2*x + rnorm(100)
rplotCIsmm(x, y, alpha=0.01, npts=20)
}
}
\seealso{
\code{\link{rplotCI}}, \code{\link{qsmm}}
}
