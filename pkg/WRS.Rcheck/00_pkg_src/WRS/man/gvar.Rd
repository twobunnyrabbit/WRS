% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{gvar}
\alias{gvar}
\title{Generalized Variance}
\usage{
gvar(m)
}
\arguments{
\item{m}{A matrix or data frame where rows are observations and columns
are variables. Missing values are removed before computation.}
}
\value{
A numeric value representing the generalized variance (the product
of eigenvalues of the covariance matrix).
}
\description{
Computes the generalized variance of a multivariate dataset using the
classical sample covariance matrix.
}
\details{
The generalized variance is defined as the determinant of the covariance
matrix. It provides a scalar measure of overall variability in multivariate
data, representing the volume of the concentration ellipsoid.

Mathematically, the generalized variance is:
\deqn{|\Sigma| = \prod_{i=1}^{p} \lambda_i}
where \eqn{\lambda_i} are the eigenvalues of the covariance matrix.

This function uses the classical (non-robust) sample covariance matrix
computed by \code{var()}. For robust alternatives, see \code{\link{gvarg}}
or \code{\link{rgvar}}.
}
\examples{
# Generalized variance of bivariate data
x <- matrix(rnorm(100), 50, 2)
gvar(x)

# Compare with robust version
# gvarg(x, var.fun = covmba)

}
\seealso{
\code{\link{gvarg}}, \code{\link{gvar2g}}, \code{\link{rgvar}}
}
\keyword{multivariate}
