% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{rplotpbCI}
\alias{rplotpbCI}
\title{Bootstrap Running Interval Smoother with Confidence Band}
\usage{
rplotpbCI(
  x,
  y,
  est = onestep,
  fr = 1,
  plotit = TRUE,
  scat = TRUE,
  pyhat = FALSE,
  xout = FALSE,
  xlab = "x",
  ylab = "y",
  outfun = out,
  LP = TRUE,
  alpha = 0.05,
  nboot = 500,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{est}{Estimator function for location (default: \code{onestep} for one-step
M-estimator). Can be any function like \code{mean}, \code{median}, \code{tmean}, \code{mom}.}

\item{fr}{Span parameter controlling amount of smoothing (default: 1).
Higher values = more smoothing.}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{xlab}{Label for x-axis in plots (default: "X" or "VAR 1").}

\item{ylab}{Label for y-axis in plots (default: "Y" or "VAR 2").}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{LP}{Logical. If \code{TRUE}, applies lowess smoothing to the confidence
band for smoother appearance (default: \code{TRUE}).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
List with component:
\item{output}{Fitted values at each point (if pyhat=TRUE), otherwise "Done"}
}
\description{
Running interval smoother based on any measure of location with confidence
band computed via percentile bootstrap. More flexible than \code{rplotCI} as it
works with any location estimator, not just trimmed means.
}
\details{
Unlike \code{rplotCI} which is limited to trimmed means, this function can use
any location estimator specified by \code{est}. Confidence intervals are computed
via percentile bootstrap at each point along the curve.

The bootstrap approach makes this function more computationally intensive
than \code{rplotCI} or \code{rplotCIS}, but provides greater flexibility in choice
of estimator. Set LP=TRUE for a smoother confidence band appearance.
}
\examples{
\dontrun{
# Bootstrap confidence band with median
x <- rnorm(100)
y <- 2*x + rnorm(100)
rplotpbCI(x, y, est=median, nboot=500)

# Using one-step M-estimator
rplotpbCI(x, y, est=onestep, fr=0.8)
}
}
\seealso{
\code{\link{rplotCI}}, \code{\link{rplotCIS}}, \code{\link{onesampb}}
}
