% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{ancGLOB_pv}
\alias{ancGLOB_pv}
\title{Determine Critical P-Value for ancGLOB}
\usage{
ancGLOB_pv(
  n1,
  n2,
  est = tmean,
  fr1 = 0.8,
  fr2 = 0.8,
  nboot = 500,
  SEED = TRUE,
  iter = 1000,
  nmin = 12,
  MC = TRUE,
  alpha = 0.05,
  PRM = FALSE,
  pts = NULL,
  ...
)
}
\arguments{
\item{n1}{Sample size for group 1.}

\item{n2}{Sample size for group 2.}

\item{est}{Estimator function (default: tmean).}

\item{fr1}{Span for group 1 (default: 0.8).}

\item{fr2}{Span for group 2 (default: 0.8).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{iter}{Number of simulation iterations (default: 1000).}

\item{nmin}{Minimum sample size (default: 12).}

\item{MC}{Logical. If \code{TRUE}, uses parallel processing via \code{mclapply}
(default: \code{FALSE}).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{PRM}{Logical. If TRUE, returns all computed p-values.}

\item{pts}{Design points (default: NULL for automatic selection).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
List with:
\item{p.crit}{Critical p-value for specified alpha}
\item{prm}{All p-values (if PRM=TRUE), otherwise NULL}
\item{ef.iter}{Effective number of iterations}
}
\description{
Computes the critical p-value for ancGLOB via simulation under the null
hypothesis (normal distribution). Uses the Harrell-Davis estimator on
simulated p-values.
}
\keyword{internal}
