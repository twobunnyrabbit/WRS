% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{r2mcp}
\alias{r2mcp}
\title{Two-Way ANOVA Rank-Based Pairwise Comparisons}
\usage{
r2mcp(J, K, x, grp = NA, alpha = 0.05, bhop = FALSE)
}
\arguments{
\item{J}{Integer. Number of levels for Factor A.}

\item{K}{Integer. Number of levels for Factor B.}

\item{x}{Data in list mode or matrix format with \code{J*K} groups.}

\item{grp}{Optional vector to rearrange group order. See Details.}

\item{alpha}{Numeric. The family-wise Type I error rate (default: 0.05).}

\item{bhop}{Logical. If \code{TRUE}, uses Benjamini-Hochberg method for FWE
control. If \code{FALSE} (default), uses Rom's technique.}
}
\value{
A list containing:
\item{Factor.A}{Matrix of pairwise comparisons for Factor A main effects}
\item{Factor.B}{Matrix of pairwise comparisons for Factor B main effects}
\item{Factor.AB}{Matrix of pairwise comparisons for A×B interactions}
}
\description{
Performs all pairwise comparisons of main effects (Factor A and B) and
interactions in a two-way design using a rank-based method that tests
for equal distributions.
}
\details{
This function extends \code{\link{r1mcp}} to two-way factorial designs.
It uses the rank-based method to compare all pairs of levels for each
main effect and all pairwise interaction contrasts.

\strong{Data Organization}: By default, groups are ordered as (1,1), (1,2), ..., (1,K),
(2,1), (2,2), ..., (J,K) where the first index is Factor A and second is Factor B.

\strong{Group Rearrangement}: Use \code{grp} to specify custom ordering. For example,
\code{grp=c(2,4,3,1)} indicates: group 2 → (1,1), group 4 → (1,2),
group 3 → (2,1), group 1 → (2,2).

Each output matrix contains test statistics, p-values, and adjusted critical
p-values for multiple comparisons. Missing values are automatically removed.
}
\seealso{
\code{\link{r1mcp}}, \code{\link{bdm2way}}
}
\keyword{htest}
