% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{wwtrimbt}
\alias{wwtrimbt}
\title{Bootstrap-t Test for Within-by-Within ANOVA (J × K Design)}
\usage{
wwtrimbt(
  J,
  K,
  x,
  tr = 0.2,
  JK = J * K,
  con = 0,
  alpha = 0.05,
  grp = c(1:JK),
  nboot = 599,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{J}{Number of levels for first factor.}

\item{K}{Number of levels for second factor.}

\item{x}{Data in list mode or matrix. Groups ordered as (1,1), (1,2), ..., (1,K),
(2,1), ..., (J,K). See Details.}

\item{tr}{Trimming proportion (default: 0.2 for 20\% trimming).}

\item{JK}{Total number of groups (J*K). Usually computed automatically.}

\item{con}{Contrast matrix (default: 0, meaning standard omnibus tests).}

\item{alpha}{Significance level (default: 0.05).}

\item{grp}{Vector specifying which groups to analyze (default: all groups 1:JK).}

\item{nboot}{Number of bootstrap samples (default: 599).}

\item{SEED}{Logical. If \code{TRUE} (default), sets random seed to 2 for reproducibility.}

\item{...}{Additional arguments (currently unused).}
}
\value{
List with components:
\item{p.value.A}{Bootstrap p-value for factor A main effect.}
\item{p.value.B}{Bootstrap p-value for factor B main effect.}
\item{p.value.AB}{Bootstrap p-value for A × B interaction.}
}
\description{
Performs bootstrap-t omnibus tests for main effects and interactions in a
J × K factorial design with repeated measures on both factors.
}
\details{
Uses bootstrap-t method to test main effects of factors A and B, and their
interaction A × B, in a within-by-within design with trimmed means.

Data structure:
\itemize{
\item List mode: \code{x[[1]]} = level (1,1), \code{x[[2]]} = level (1,2), ...,
\code{x[[K]]} = level (1,K), \code{x[[K+1]]} = level (2,1), etc.
\item Matrix mode: columns correspond to groups in the same order
}

The function centers each group by subtracting its trimmed mean, then generates
bootstrap samples and computes test statistics using \code{\link{wwtrim}}.
}
\seealso{
\code{\link{wwtrim}}, \code{\link{tsplit}}
}
\keyword{anova}
\keyword{bootstrap}
\keyword{robust}
