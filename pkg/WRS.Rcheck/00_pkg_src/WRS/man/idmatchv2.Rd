% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{idmatchv2}
\alias{idmatchv2}
\title{Match Cases by ID with Unmatched Case Reporting}
\usage{
idmatchv2(m1, m2, id.col1, id.col2 = id.col1)
}
\arguments{
\item{m1}{First matrix or data frame}

\item{m2}{Second matrix or data frame}

\item{id.col1}{Column number containing IDs in m1}

\item{id.col2}{Column number containing IDs in m2. Default is \code{id.col1}}
}
\value{
List with components:
\itemize{
\item \code{m}: Combined data matrix with ID column followed by columns from
m1 (excluding ID) then columns from m2 (excluding ID)
\item \code{idnm1}: Row indices from m1 that had no match in m2
\item \code{idnm2}: Row indices from m2 that had no match in m1
\item \code{m1.no}: Rows from m1 with no matching ID in m2 (NULL if none)
\item \code{m2.no}: Rows from m2 with no matching ID in m1 (NULL if none)
}
}
\description{
Extended version of \code{\link{idmatch}} that not only extracts matching cases
but also reports which cases from each dataset did not have matches.
}
\details{
This function extends \code{\link{idmatch}} by additionally tracking which cases
failed to match. Useful for data cleaning and understanding sample attrition.
The function checks for duplicate IDs and stops with an error if any are found.
}
\seealso{
\code{\link{idmatch}}
}
\keyword{manip}
\keyword{utilities}
