% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{ancovap2.KMS}
\alias{ancovap2.KMS}
\title{ANCOVA Effect Size (KMS Analog) for Two Covariates}
\usage{
ancovap2.KMS(
  x1,
  y1,
  x2,
  y2,
  pts = NULL,
  BOTH = TRUE,
  npts = 20,
  profun = prodepth,
  xout = FALSE,
  outfun = outpro
)
}
\arguments{
\item{x1}{A matrix with two columns containing the covariate values for group 1.}

\item{y1}{A numeric vector containing the outcome variable for group 1.}

\item{x2}{A matrix with two columns containing the covariate values for group 2.}

\item{y2}{A numeric vector containing the outcome variable for group 2.}

\item{pts}{A matrix with two columns specifying the covariate value combinations
at which to compute effect sizes. If \code{NULL}, points are automatically selected
based on data depth.}

\item{BOTH}{Logical. If \code{TRUE} (default), combines x1 and x2 when selecting
design points. If \code{FALSE}, uses only x1.}

\item{npts}{Number of design points to use when \code{pts = NULL} (default: 20).}

\item{profun}{Function for computing depth of points. Default is \code{prodepth}
(uses product of marginal depths). Alternative: \code{pdepth} (deterministic method,
may be slow for large n).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}
}
\value{
A matrix with columns for each covariate (X1, X2) and the KMS effect
size. Each row corresponds to one covariate value combination specified in
\code{pts} or automatically selected.
}
\description{
Compares two independent regression lines based on an analog of the KMS
(Kraemer-Morgan-Schork) measure of effect size for data with two covariates.
The effect size is computed at specified covariate value combinations.
}
\details{
The function implements a heteroscedastic effect size measure for comparing two
regression lines with two covariates. The KMS effect size at a given covariate
combination is computed as:

\deqn{ES = \frac{\hat{y}_1 - \hat{y}_2}{\sqrt{\sigma^2_{pooled}}}}

where \eqn{\hat{y}_1} and \eqn{\hat{y}_2} are predicted values from quantile
regression, and \eqn{\sigma^2_{pooled}} is a weighted pooled variance estimate.

When \code{pts = NULL}, the function automatically selects design points based on
depth, ranging from the deepest to least deep points in the covariate space.
}
\examples{
\dontrun{
# Generate example data with two covariates
set.seed(123)
n1 <- 50; n2 <- 50
x1 <- matrix(rnorm(n1 * 2), ncol = 2)
y1 <- x1[,1] + x1[,2] + rnorm(n1)
x2 <- matrix(rnorm(n2 * 2), ncol = 2)
y2 <- x2[,1] + x2[,2] + 0.5 + rnorm(n2)

# Compute KMS effect sizes
result <- ancovap2.KMS(x1, y1, x2, y2)
print(result)
}
}
\seealso{
\code{\link{ancovap2.KMSci}} for confidence intervals,
\code{\link{ancovap2.KMS.plot}} for visualization
}
