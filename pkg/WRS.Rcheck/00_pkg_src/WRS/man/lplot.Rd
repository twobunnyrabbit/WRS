% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00-utils-core.R
\name{lplot}
\alias{lplot}
\title{Plot Regression Surface Using LOESS}
\usage{
lplot(
  x,
  y,
  low.span = 2/3,
  span = 0.75,
  pyhat = FALSE,
  eout = FALSE,
  xout = FALSE,
  outfun = outpro,
  plotit = TRUE,
  expand = 0.5,
  varfun = pbvar,
  cor.op = FALSE,
  cor.fun = pbcor,
  pr = TRUE,
  STR = TRUE,
  ZLIM = FALSE,
  pch = NULL,
  scale = TRUE,
  xlab = "X",
  ylab = "Y",
  zlab = "",
  theta = 50,
  phi = 25,
  family = "gaussian",
  duplicate = "error",
  pc = "*",
  ticktype = "simple",
  frame = TRUE,
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{low.span}{Span parameter for lowess when there is one predictor
(default: 2/3).}

\item{span}{Span parameter for loess when there are two or more predictors
(default: 0.75).}

\item{pyhat}{Logical. If TRUE, returns fitted Y values (default: FALSE).}

\item{eout}{Logical value. If \code{TRUE}, outliers are removed based on
eliminating points with large residuals or leverage (default: \code{FALSE}).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{varfun}{Function to compute variance for strength of association
(default: pbvar).}

\item{cor.op}{Logical. If TRUE, uses correlation-based measure of association
(default: FALSE).}

\item{cor.fun}{Correlation function to use when cor.op = TRUE
(default: pbcor).}

\item{pr}{Logical. If \code{TRUE}, prints progress messages (default: \code{TRUE}).}

\item{STR}{Logical. If TRUE, computes strength of association (default: TRUE).}

\item{ZLIM}{Logical. Internal parameter for setting zlim in persp plots
(default: FALSE).}

\item{pch}{Plotting character (default: "*").}

\item{scale}{Logical. If TRUE, scales variables in persp plot (default: TRUE).}

\item{xlab, ylab, zlab}{Axis labels for plots.}

\item{theta, phi}{Angles for 3D perspective plot (defaults: 50, 25).}

\item{family}{Family parameter for loess ("gaussian" or "symmetric",
default: "gaussian").}

\item{duplicate}{How to handle duplicate x values in 3D plots
(default: "error"). Set to "strip" if duplicates occur.}

\item{pc}{Alternative to pch for plotting character (default: "*").}

\item{ticktype}{Type of tick marks for persp plot (default: "simple").}

\item{frame}{Logical. If TRUE, draws frame around 2D plot (default: TRUE).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
A list with components:
\item{Strength.Assoc}{Square root of explanatory power}
\item{Explanatory.power}{Measure of association strength (R-squared analog)}
\item{yhat.values}{Fitted values (NULL unless pyhat = TRUE)}
\item{n}{Original sample size}
\item{n.keep}{Sample size after outlier removal}
}
\description{
Plots a regression surface using LOESS (locally weighted scatterplot smoothing)
for up to four predictors. Also computes a measure of association strength.
}
\details{
For one predictor, uses lowess smoothing and plots a 2D scatterplot with
smooth curve. For two predictors, creates a 3D perspective plot. Supports
up to four predictors (though visualization is only for 1-2 predictors).

The function can eliminate outliers before fitting via \code{eout} (outliers
in both x and y) or \code{xout} (outliers in x only).

For confidence bands with a single predictor, use \code{lplotCI} instead.
For p-values based on the strength of association, use \code{lplotPV}.
}
\note{
When using two predictors with duplicate values, you may need to set
\code{duplicate = "strip"}.
}
\examples{
# Simple example with one predictor
x <- rnorm(50)
y <- 2*x + rnorm(50)
lplot(x, y)

# Two predictors (creates 3D plot)
x <- matrix(rnorm(100), ncol = 2)
y <- x[,1] + x[,2] + rnorm(50)
lplot(x, y)

}
