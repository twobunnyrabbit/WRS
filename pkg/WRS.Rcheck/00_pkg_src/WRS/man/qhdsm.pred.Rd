% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression-advanced.R
\name{qhdsm.pred}
\alias{qhdsm.pred}
\title{Predictions from Quantile Regression Smoother}
\usage{
qhdsm.pred(
  x,
  y,
  pts = x,
  q = 0.5,
  fr = 1,
  nmin = 1,
  xout = FALSE,
  outfun = outpro,
  ...
)
}
\arguments{
\item{x}{A numeric matrix or vector containing predictor values from the
training data.}

\item{y}{A numeric vector containing response values from the training data.}

\item{pts}{A matrix or vector of predictor values at which predictions are
desired (default: \code{x}, predicting at training points).}

\item{q}{Quantile to estimate (default: 0.5 for median). Must be between 0 and 1.}

\item{fr}{Fraction/span parameter controlling the neighborhood size for local
quantile estimation (default: 1). Larger values use more neighbors.}

\item{nmin}{Minimum number of observations required in a neighborhood for
prediction (default: 1).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
A list with components:
\item{Y.hat}{Vector of predicted Y values (qth quantiles) at each point in \code{pts}}
\item{nvals}{Vector indicating the number of observations used for each prediction}
}
\description{
Predicts the qth quantile of Y at specified predictor values using the
quantile regression smoother based on the Harrell-Davis estimator. This
function provides predictions from the model fitted by \code{qhdsm()}.
}
\details{
For each point in \code{pts}, this function:
\enumerate{
\item Identifies nearby observations in the training data (x, y) using the
\code{near()} function with span \code{fr}
\item Computes the qth quantile of Y values in that neighborhood using the
Harrell-Davis estimator (\code{hd()})
\item Returns the predicted quantile and neighborhood size
}

For single-predictor problems, predictions use \code{runhat()}. For multiple
predictors, \code{rung3hat()} is used instead.

If \code{xout = TRUE}, outliers are removed from the predictor space before
prediction using the function specified by \code{outfun}.
}
\examples{
\dontrun{
# Fit and predict median regression
set.seed(123)
x <- runif(100, 0, 10)
y <- 2 + 0.5*x + rnorm(100)

# Predict at new points
newx <- seq(0, 10, length=20)
pred <- qhdsm.pred(x, y, pts=newx, q=0.5)
plot(x, y)
lines(newx, pred$Y.hat)

# Predict 75th percentile
pred75 <- qhdsm.pred(x, y, pts=newx, q=0.75)
lines(newx, pred75$Y.hat, lty=2)
}
}
\seealso{
\code{\link{qhdsm}}, \code{\link{qhdsm2g}}, \code{\link{hd}}, \code{\link{runhat}}
}
