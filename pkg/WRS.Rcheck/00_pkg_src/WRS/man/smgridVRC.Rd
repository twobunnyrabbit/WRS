% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{smgridVRC}
\alias{smgridVRC}
\title{Compare Location Measures on Grid with Value-Based or Quantile-Based Splits}
\usage{
smgridVRC(
  x,
  y,
  IV = c(1, 2),
  Qsplit1 = 0.5,
  Qsplit2 = 0.5,
  TR = 0.2,
  alpha = 0.05,
  VAL1 = NULL,
  VAL2 = NULL,
  PB = FALSE,
  est = tmean,
  nboot = 1000,
  pr = TRUE,
  method = "hoch",
  xout = FALSE,
  outfun = outpro,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{x}{Matrix or data frame containing independent variables.}

\item{y}{Numeric vector containing the dependent variable.}

\item{IV}{Two-element vector specifying which columns of \code{x} to use as
independent variables. Default is \code{c(1,2)}.}

\item{Qsplit1}{Quantile(s) for splitting the first IV. Default is 0.5 (median).
Ignored if \code{VAL1} is provided.}

\item{Qsplit2}{Quantile(s) for splitting the second IV. Default is 0.5 (median).
Ignored if \code{VAL2} is provided.}

\item{TR}{Amount of trimming for non-bootstrap methods. Default is 0.2 (20\\% trimming).}

\item{alpha}{Significance level. Default is 0.05.}

\item{VAL1}{Explicit values for splitting first IV. If \code{NULL} (default),
uses \code{Qsplit1} quantiles.}

\item{VAL2}{Explicit values for splitting second IV. If \code{NULL} (default),
uses \code{Qsplit2} quantiles.}

\item{PB}{Logical. If \code{TRUE}, uses percentile bootstrap. Automatically set
to \code{TRUE} if \code{est=median} or \code{est=hd}. Default is \code{FALSE}.}

\item{est}{Estimator function for location. Default is \code{\link{tmean}}.}

\item{nboot}{Number of bootstrap samples if \code{PB=TRUE}. Default is 1000.}

\item{pr}{Logical. If \code{TRUE} (default), prints messages about eliminated groups.}

\item{method}{P-value adjustment method. Default is \code{'hoch'} (Hochberg).}

\item{xout}{Logical. If \code{TRUE}, removes outliers. Default is \code{FALSE}.}

\item{outfun}{Outlier detection function. Default is \code{\link{outpro}}.}

\item{SEED}{Logical. If \code{TRUE} (default), sets random seed for reproducibility.}

\item{...}{Additional arguments passed to \code{est}.}
}
\value{
List with components:
\itemize{
\item \code{est.loc.4.DV} - Matrix of location estimates for each grid cell
\item \code{n} - Matrix of sample sizes for each grid cell
\item \code{Independent.variables.summary} - Summary statistics for IV ranges
\item \code{Res.4.IV1} - Results for IV1 pairwise comparisons
\item \code{Res.4.IV2} - Results for IV2 pairwise comparisons
}
}
\description{
Compares measures of location among groups defined by grids of two independent
variables. Similar to \code{\link{smgridRC}}, but allows specifying explicit
split values instead of (or in addition to) quantiles.
}
\details{
This function extends \code{\link{smgridRC}} by allowing explicit split values
via \code{VAL1} and \code{VAL2}. This is useful when you want to split on
specific values rather than data-driven quantiles.

For bootstrap methods, use the \code{tr} parameter (via \code{...}) to control
trimming, not \code{TR}.

Groups with n ≤ 5 are automatically excluded.
}
\seealso{
\code{\link{smgridRC}}, \code{\link{smgrid}}, \code{\link{lincon}},
\code{\link{linpairpb}}
}
\keyword{htest}
\keyword{multivariate}
\keyword{robust}
