% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00-utils-core.R
\name{outpro}
\alias{outpro}
\title{Outlier Detection Using Projection Method}
\usage{
outpro(
  m,
  gval = NA,
  center = NA,
  plotit = TRUE,
  op = TRUE,
  MM = FALSE,
  cop = 3,
  xlab = "VAR 1",
  ylab = "VAR 2",
  STAND = TRUE,
  tr = 0.2,
  q = 0.5,
  pr = TRUE,
  ...
)
}
\arguments{
\item{m}{Data matrix (n x p) or vector.}

\item{gval}{Critical value for declaring outliers. If NA, computed automatically
(default: NA).}

\item{center}{Optional pre-specified center. If NA, computed based on cop.}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{op}{Logical. If TRUE, plots 0.5 depth contour after removing outliers;
if FALSE, plots without removing outliers (default: TRUE).}

\item{MM}{Logical. If FALSE, uses interquartile range; if TRUE, uses MAD
(default: FALSE).}

\item{cop}{Center option:
\itemize{
\item 2: MCD center
\item 3: Median of marginal distributions (default)
\item 4: MVE center
\item 5: TBS
\item 6: RMBA (Olive's median ball algorithm)
\item 7: Spatial (L1) median
}}

\item{xlab, ylab}{Labels for x and y axes in plots.}

\item{STAND}{Logical. If TRUE, standardizes marginal distributions before
outlier detection (default: TRUE).}

\item{tr}{Trimming proportion (default: 0.2). Proportion of observations to
trim from each tail when computing winsorized correlation.}

\item{q}{Quantile to estimate. For example, \code{q = 0.5} estimates the median,
\code{q = 0.25} estimates the first quartile (default: 0.5).}

\item{pr}{Logical. If \code{TRUE}, prints progress messages (default: \code{TRUE}).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
A list with components:
\item{out.id}{Indices of outliers}
\item{keep}{Logical vector indicating non-outliers}
\item{outval}{Data values for outliers}
\item{dis}{Projection distances for all points}
\item{crit}{Critical value used}
\item{n}{Sample size}
\item{n.out}{Number of outliers}
}
\description{
Detects outliers using a modification of the Stahel-Donoho projection method.
Projects data onto lines connecting each point with the center, then identifies
outliers based on projection distances.
}
\details{
The projection method connects each data point with the center of the data
cloud, projects all points onto this line, and examines distances between
projected points. Outliers are identified using a boxplot-based method on
the projection distances.

For bivariate data with \code{plotit = TRUE}, creates a scatterplot showing
outliers and the depth contour. The Donoho-Gasko (Tukey) median is marked
with a cross (+).

Default critical value is sqrt(qchisq(0.975, p)) for cop = 2, 3, or 4.
}
\note{
Parameters \code{tr} and \code{q} are not used by this function but are
included for compatibility with smoothing functions.
}
\examples{
# Bivariate example
set.seed(123)
m <- matrix(rnorm(100), ncol = 2)
m <- rbind(m, c(5, 5))  # Add an outlier
result <- outpro(m)
result$out.id  # Index of outlier(s)

}
