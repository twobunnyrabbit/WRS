% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression-advanced.R
\name{multireg.prob}
\alias{multireg.prob}
\title{Multinomial Logistic Regression Probabilities}
\usage{
multireg.prob(
  x,
  y,
  pts = x,
  xout = FALSE,
  outfun = outpro,
  plotit = TRUE,
  xlab = "X",
  ylab = "Prob",
  zlab = "Prob",
  ticktype = "det",
  vplot = NULL,
  L = TRUE,
  scale = TRUE,
  ...
)
}
\arguments{
\item{x}{A numeric matrix or vector containing predictor values.}

\item{y}{A numeric or factor vector containing the categorical response variable
with multiple possible values.}

\item{pts}{Matrix of predictor values at which to estimate probabilities
(default: \code{x}, the observed predictor values).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{plotit}{Logical. If \code{TRUE}, creates a plot of predicted probabilities
(default: \code{TRUE}).}

\item{xlab, ylab, zlab}{Axis labels for plots.}

\item{ticktype}{Type of tick marks for 3D plot (default: "det").}

\item{vplot}{Numeric vector specifying which response categories to plot.
If \code{NULL}, plots the maximum value of Y (default: \code{NULL}).}

\item{L}{Logical. For 2 predictors: if \code{TRUE}, uses \code{lplot()}; if \code{FALSE},
uses \code{rplot()} (default: \code{TRUE}).}

\item{scale}{Logical. For 1 predictor: if \code{TRUE}, y-axis ranges 0-1; if \code{FALSE},
y-axis matches range of probabilities (default: \code{TRUE}).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
A list with components:
\item{estimates}{Matrix with one row per point in \code{pts}. First column is
point number, remaining columns are estimated probabilities for each
response category.}
\item{pts}{Matrix of predictor values where probabilities were estimated.}
}
\description{
Estimates conditional probabilities P(Y=k|X) for multinomial (categorical)
outcomes using a multinomial logit model. Optionally plots the predicted
probabilities as a function of predictors.
}
\details{
This function fits a multinomial logistic regression model:

\deqn{P(Y=k|X) = \frac{\exp(\beta_{k0} + \beta_k' X)}{1 + \sum_j \exp(\beta_{j0} + \beta_j' X)}}

for k = 1, ..., K-1, where K is the number of response categories. The
baseline category (k=0) has P(Y=0|X) = 1 / (1 + sum of other probabilities).

The function:
\enumerate{
\item Fits multinomial logit model using \code{multinom()} from nnet package
\item Computes predicted probabilities at each point in \code{pts}
\item Optionally plots probabilities vs predictors
}

Plotting behavior:
\itemize{
\item 1 predictor: Line plot(s) of probability vs X
\item 2 predictors: 3D surface plot using \code{lplot()} or \code{rplot()}
\item >2 predictors: No automatic plot
}

If \code{xout = TRUE}, outliers in predictor space are removed before fitting.

Requires the nnet package.
}
\examples{
\dontrun{
# Multinomial regression with 3 categories
set.seed(123)
x <- rnorm(100)
# Create 3-category outcome
y <- cut(2*x + rnorm(100), breaks=3, labels=FALSE)
result <- multireg.prob(x, y)
result$estimates  # Probabilities for each category
}
}
\references{
Wilcox, R.R. (2022). Introduction to Robust Estimation and Hypothesis Testing
(5th ed.). Academic Press.
}
\seealso{
\code{\link{logreg}}, \code{\link{logreg.pred}}, \code{\link[nnet]{multinom}}
}
