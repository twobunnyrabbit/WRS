% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{Dancols}
\alias{Dancols}
\title{Dependent Groups ANCOVA Using OLS Regression}
\usage{
Dancols(
  x1,
  y1,
  x2,
  y2,
  pts = NULL,
  fr1 = 1,
  fr2 = 1,
  alpha = 0.05,
  plotit = TRUE,
  xout = FALSE,
  outfun = out,
  nboot = 100,
  SEED = TRUE,
  xlab = "X",
  ylab = "Y",
  CR = FALSE,
  ...
)
}
\arguments{
\item{pts}{Design points for comparison. If NULL, automatically selected. Default is NULL.}

\item{alpha}{Significance level. Default is 0.05.}

\item{plotit}{Logical. Create comparison plot. Default is TRUE.}

\item{xout}{Logical. Remove outliers. Default is FALSE.}

\item{outfun}{Outlier detection function. Default is out.}

\item{nboot}{Number of bootstrap samples. Default is 100.}

\item{SEED}{Logical. Set random seed. Default is TRUE.}

\item{xlab, ylab}{Axis labels for plot.}

\item{CR}{Logical. Determine Johnson-Neyman crossing interval. Default is FALSE.}

\item{...}{Additional arguments.}
}
\value{
List with components:
\describe{
\item{output}{Test results at each design point}
\item{cross.interval}{(If CR=TRUE) Interval outside which lines have crossed}
\item{cr.quant.grp1}{(If CR=TRUE) Quantiles of group 1 at interval endpoints}
}
}
\description{
Compare OLS regression lines of two dependent groups at specified design points.
Robust Johnson-Neyman method for dependent groups using ordinary least squares.
}
\details{
Compares OLS regression lines at specified design points for dependent groups.
If CR=TRUE, determines the interval outside of which the regression lines cross
(analog of Johnson-Neyman method). For robust regression, use Dancts or Danctspb instead.
}
\examples{
\dontrun{
  x <- rnorm(30)
  y1 <- 2*x + rnorm(30)
  y2 <- 2.5*x + rnorm(30)  # Different slope
  result <- Dancols(x, y1, x, y2, CR = TRUE)
}
}
\seealso{
\code{\link{Dancts}}, \code{\link{Danctspb}}, \code{\link{ancJN}}
}
\keyword{ancova}
\keyword{dependent}
\keyword{regression}
