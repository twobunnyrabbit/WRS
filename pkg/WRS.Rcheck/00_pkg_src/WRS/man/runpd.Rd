% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{runpd}
\alias{runpd}
\title{3D Running Smoother Using Projection Distances}
\usage{
runpd(
  x,
  y,
  pts = x,
  est = tmean,
  fr = 0.8,
  plotit = TRUE,
  pyhat = FALSE,
  nmin = 0,
  scale = TRUE,
  expand = 0.5,
  xout = FALSE,
  outfun = out,
  pr = TRUE,
  xlab = "X1",
  ylab = "X2",
  zlab = "",
  LP = TRUE,
  theta = 50,
  phi = 25,
  duplicate = "error",
  MC = FALSE,
  ticktype = "simple",
  ...
)
}
\arguments{
\item{x}{An n by p matrix of predictors}

\item{y}{Vector of response values}

\item{pts}{Matrix of points at which to predict (default: x)}

\item{est}{Measure of location (default: tmean)}

\item{fr}{Span parameter controlling smoothing (default: 0.8)}

\item{plotit}{Logical: create 3D perspective plot (default: TRUE)}

\item{pyhat}{Logical: if TRUE, return predicted values (default: FALSE)}

\item{nmin}{Minimum neighbors for estimation (default: 0)}

\item{scale}{Logical: scale the plot (default: TRUE)}

\item{expand}{Relative length of z-axis in plot (default: 0.5)}

\item{xout}{Logical: remove x outliers (default: FALSE)}

\item{outfun}{Outlier detection function (default: out)}

\item{pr}{Logical: print advisory message about scale (default: TRUE)}

\item{xlab}{Label for x-axis (default: "X1")}

\item{ylab}{Label for y-axis (default: "X2")}

\item{zlab}{Label for z-axis (default: "")}

\item{LP}{Logical: apply lowess post-smoothing (default: TRUE)}

\item{theta}{Azimuthal viewing angle (default: 50)}

\item{phi}{Colatitude viewing angle (default: 25)}

\item{duplicate}{How to handle duplicates for interp() (default: "error")}

\item{MC}{Logical: use parallel computation in pdclose (default: FALSE)}

\item{ticktype}{Type of tick marks (default: "simple")}

\item{...}{Additional arguments passed to est function}
}
\value{
"Done" if pyhat=FALSE, or vector of predicted values if pyhat=TRUE
}
\description{
Compute a 3D running interval smoother where distances from a point are
determined using a projection method rather than Mahalanobis distance.
}
\details{
Similar to runm3d but uses projection-based distances (via pdclose) instead
of Mahalanobis distances to determine nearest neighbors. This can be more
robust in some settings.

For each point in pts, finds neighbors using projection distances, then
applies the specified measure of location. If LP=TRUE, applies additional
lowess smoothing to the surface.

When plotit=TRUE and p=2, creates a perspective plot. Missing values are
automatically removed.
}
\seealso{
\code{\link{pdclose}}, \code{\link{runm3d}}, \code{\link{rung3d}}
}
\keyword{nonparametric}
\keyword{smooth}
