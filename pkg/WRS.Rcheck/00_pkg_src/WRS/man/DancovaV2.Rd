% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{DancovaV2}
\alias{DancovaV2}
\title{Dependent Groups ANCOVA Version 2 with Improved Bootstrap}
\usage{
DancovaV2(
  x1 = NULL,
  y1 = NULL,
  x2 = NULL,
  y2 = NULL,
  xy = NULL,
  fr1 = 1,
  fr2 = 1,
  est = tmean,
  alpha = 0.05,
  plotit = TRUE,
  xlab = "X",
  ylab = "Y",
  qvals = c(0.25, 0.5, 0.75),
  sm = FALSE,
  xout = FALSE,
  eout = FALSE,
  outfun = out,
  DIF = FALSE,
  LP = TRUE,
  method = "hochberg",
  nboot = 500,
  SEED = TRUE,
  nreps = 2000,
  MC = TRUE,
  cpp = FALSE,
  SCAT = TRUE,
  pch1 = "*",
  pch2 = "+",
  nmin = 12,
  q = 0.5,
  ...
)
}
\arguments{
\item{x1, y1, x2, y2}{Covariate and outcome vectors for each condition.}

\item{xy}{Alternatively, data matrix with columns \link{x1, y1, x2, y2}.}

\item{est}{Estimator function. Default is tmean (trimmed mean).}

\item{alpha}{Significance level. Default is 0.05.}

\item{plotit}{Logical. Create diagnostic plot. Default is TRUE.}

\item{xlab, ylab}{Axis labels for plot.}

\item{qvals}{Quantiles for design point selection. Default is c(0.25, 0.5, 0.75).}

\item{sm}{Logical. Use smoothing. Default is FALSE.}

\item{DIF}{Logical. Analyze difference scores if TRUE. Default is FALSE.}

\item{LP}{Logical. Use linear programming. Default is TRUE.}

\item{method}{P-value adjustment method. Default is 'hochberg'.}

\item{nboot}{Number of bootstrap samples. Default is 500.}

\item{SEED, nreps, MC, cpp}{Bootstrap control parameters.}

\item{SCAT, pch1, pch2}{Plotting parameters.}

\item{nmin}{Minimum sample size at design points. Default is 12.}

\item{q}{Quantile for certain estimators. Default is 0.5.}

\item{...}{Additional arguments.}
}
\value{
List with test results and design points.
}
\description{
Compare two dependent groups using nonparametric ANCOVA with improved bootstrap method.
Similar to Dancova but with enhanced power through different bootstrap resampling strategy.
}
\details{
Like Dancova but with improved power. Bootstrap samples are obtained by resampling
from the full data (x1, y1, x2, y2) rather than conditioning on x values.
This unconditional bootstrap often provides better power.
}
\examples{
\dontrun{
  x <- rnorm(30)
  y1 <- x + rnorm(30)
  y2 <- x + 0.5 + rnorm(30)
  result <- DancovaV2(x, y1, x, y2)
}
}
\seealso{
\code{\link{Dancova}}
}
\keyword{ancova}
\keyword{bootstrap}
\keyword{dependent}
