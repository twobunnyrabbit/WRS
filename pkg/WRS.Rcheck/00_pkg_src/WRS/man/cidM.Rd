% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{cidM}
\alias{cidM}
\title{Multiple Group Comparisons Using Cliff's Method}
\usage{
cidM(
  x,
  nboot = 1000,
  alpha = 0.05,
  MC = FALSE,
  SEED = TRUE,
  g = NULL,
  dp = NULL
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{MC}{Logical. If \code{TRUE}, uses parallel processing via \code{mclapply}
(default: \code{FALSE}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{g}{Column number containing group/factor variable (default: \code{NULL}).}

\item{dp}{Column number containing dependent variable (default: \code{NULL}).}
}
\value{
Matrix of pairwise comparison results with adjusted p-values.
}
\description{
Performs all pairwise comparisons using a variation of Cliff's method based
on the median of X-Y. Controls familywise error rate using Hochberg's method.
}
\details{
Uses p = P(X < Y) as an effect size measure and tests whether p = 0.5.
Performs all pairwise comparisons with familywise error rate controlled
via Hochberg's method.

Data can be provided as:
\itemize{
\item Matrix where columns are groups (when \code{g = NULL})
\item List where each element is a group (when \code{g = NULL})
\item Data frame/matrix with group indicator in column \code{g} and data in column \code{dp}
}
}
\examples{
\dontrun{
# Matrix input
x <- matrix(rnorm(150), ncol = 3)
cidM(x)

# With grouping variable
data <- data.frame(group = rep(1:3, each = 50), value = rnorm(150))
cidM(data, g = 1, dp = 2)
}
}
\seealso{
\code{\link{cid}}, \code{\link{cidmul}}
}
