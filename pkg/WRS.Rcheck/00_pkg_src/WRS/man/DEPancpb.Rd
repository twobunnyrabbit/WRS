% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{DEPancpb}
\alias{DEPancpb}
\title{Dependent Groups ANCOVA with Bootstrap Methods}
\usage{
DEPancpb(
  x1,
  y1,
  y2,
  fr1 = 1,
  est = tmean,
  alpha = 0.05,
  plotit = TRUE,
  DISDIF = FALSE,
  DIF = TRUE,
  TLS = FALSE,
  SEED = TRUE,
  pts = NULL,
  sm = FALSE,
  xout = FALSE,
  outfun = out,
  nboot = 500,
  pr = FALSE,
  na.rm = TRUE,
  xlab = "Group 1",
  ylab = "Group 2",
  ...
)
}
\arguments{
\item{x1}{Covariate values (shared for both groups)}

\item{y1}{Response values at time 1/condition 1}

\item{y2}{Response values at time 2/condition 2 (paired with y1)}

\item{fr1}{Span for running interval smoother (default: 1)}

\item{est}{Estimator function to use (default: tmean)}

\item{alpha}{Significance level (default: 0.05)}

\item{plotit}{Logical; if TRUE, create plot (default: TRUE)}

\item{DISDIF}{Logical; if TRUE, compare using median of distribution of D=Y1-Y2 (default: FALSE)}

\item{DIF}{Logical; if TRUE, analyze differences Y1-Y2 directly (default: TRUE)}

\item{TLS}{Logical; if TRUE, use transformed least squares (default: FALSE)}

\item{SEED}{Logical; if TRUE, set random seed (default: TRUE)}

\item{pts}{Vector of design points for comparisons. If NULL, chosen automatically}

\item{sm}{Logical; if TRUE, create smooths using bootstrap bagging (default: FALSE)}

\item{xout}{Logical; if TRUE, remove outliers (default: FALSE)}

\item{outfun}{Function to detect outliers (default: out)}

\item{nboot}{Number of bootstrap samples (default: 500)}

\item{pr}{Logical; if TRUE, print progress messages (default: FALSE)}

\item{na.rm}{Logical; if TRUE, use casewise deletion for missing values (default: TRUE)}

\item{xlab}{Label for x-axis (default: "Group 1")}

\item{ylab}{Label for y-axis (default: "Group 2")}

\item{...}{Additional arguments}
}
\value{
List with output matrix containing estimates, CIs, and p-values
}
\description{
Compare two dependent (paired) groups using a covariate with bootstrap methods.
Similar to DEPanc but uses bootstrap in all cases. Uses running interval smoother
with no parametric assumptions about the regression form.
}
\seealso{
\code{\link{DEPanc}} for trimmed mean version
}
\keyword{internal}
