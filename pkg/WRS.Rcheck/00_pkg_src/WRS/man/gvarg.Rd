% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{gvarg}
\alias{gvarg}
\title{Generalized Variance with Robust Covariance Estimators}
\usage{
gvarg(m, var.fun = cov.mba, ...)
}
\arguments{
\item{m}{A matrix or data frame where rows are observations and columns
are variables. Missing values are removed before computation.}

\item{var.fun}{A function that returns a covariance matrix. Default is
\code{cov.mba}. The function must return a covariance matrix directly
(not in list mode with \code{$cov} component).}

\item{...}{Additional arguments passed to \code{var.fun}.}
}
\value{
A numeric value representing the generalized variance (the product
of eigenvalues of the robust covariance matrix).
}
\description{
Computes the generalized variance of a multivariate dataset using a
user-specified robust covariance estimator.
}
\details{
This function generalizes \code{\link{gvar}} by allowing specification of
robust covariance estimators. The generalized variance is computed as the
product of eigenvalues (equivalently, the determinant) of the covariance
matrix returned by \code{var.fun}.

\strong{Compatible covariance estimators} (return matrix directly):
\itemize{
\item \code{cov.mba} - Donoho-Stahel estimator (default)
\item \code{skipcov} - Skip estimator with outlier detection
\item \code{tbscov} - TBS (Tukey Biweight S) covariance
\item \code{covout} - Outlier-removed covariance
\item \code{covogk} - OGK (Orthogonalized Gnanadesikan-Kettenring) estimator
\item \code{mgvcov} - MGV covariance
}

\strong{Note}: Some covariance functions return results in list mode with a
\code{$cov} component (e.g., \code{covmcd}, \code{covmve}). These are not
directly compatible with this function without a wrapper.
}
\examples{
\dontrun{
# Generalized variance using default robust estimator
x <- matrix(rnorm(100), 50, 2)
gvarg(x)

# Compare different estimators
gvarg(x, var.fun = cov.mba)
gvarg(x, var.fun = skipcov)
}

}
\seealso{
\code{\link{gvar}}, \code{\link{gvar2g}}, \code{\link{rgvar}},
\code{\link{cov.mba}}
}
\keyword{multivariate}
\keyword{robust}
