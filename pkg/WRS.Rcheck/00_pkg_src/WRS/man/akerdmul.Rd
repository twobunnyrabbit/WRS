% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{akerdmul}
\alias{akerdmul}
\title{Multivariate Adaptive Kernel Density Estimate}
\usage{
akerdmul(
  x,
  pts = NA,
  hval = NA,
  aval = 0.5,
  fr = 0.8,
  pr = FALSE,
  plotit = TRUE,
  theta = 50,
  phi = 25,
  expand = 0.5,
  scale = FALSE,
  xlab = "X",
  ylab = "Y",
  zlab = "",
  ticktype = "simple"
)
}
\arguments{
\item{x}{Matrix of data values (n by d).}

\item{pts}{Matrix of points at which to evaluate density (same dimension as \code{x}).
If \code{NA} (default), uses data points.}

\item{hval}{Bandwidth parameter. If \code{NA} (default), automatically selected using
Silverman's rule for multivariate data.}

\item{aval}{Sensitivity parameter for adaptive kernel (default: 0.5). Controls
local adaptation of bandwidth.}

\item{fr}{Span parameter for expected frequency curve (default: 0.8). Used in
initial density estimate.}

\item{pr}{Logical. If \code{TRUE}, returns density values; if \code{FALSE} (default),
returns "Done".}

\item{plotit}{Logical. If \code{TRUE} (default) and d=2, creates 3D perspective plot.}

\item{theta}{Viewing angle for perspective plot (default: 50).}

\item{phi}{Viewing angle for perspective plot (default: 25).}

\item{expand}{Expansion factor for z-axis (default: 0.5).}

\item{scale}{Logical. If \code{TRUE}, scales z-axis to same range as x-y (default: FALSE).}

\item{xlab}{Label for x-axis (default: "X").}

\item{ylab}{Label for y-axis (default: "Y").}

\item{zlab}{Label for z-axis (default: "").}

\item{ticktype}{Tick mark style for 3D plot. Options: "simple" (default) or "detailed".}
}
\value{
If \code{pr=TRUE}, returns vector of density estimates at specified points.
If \code{pr=FALSE}, returns character string "Done".
}
\description{
Computes adaptive kernel density estimate for multivariate data using
Epanechnikov kernel (Silverman, 1986). Uses expected frequency as initial
density estimate.
}
\details{
Uses Epanechnikov kernel with adaptive bandwidth that varies locally based on
density. The method:
\enumerate{
\item Obtains initial density estimate using \code{\link{rdplot}}
\item Standardizes each dimension by its scale (min of SD and IQR/1.34)
\item Computes geometric mean of initial density
\item Adapts local bandwidth: \eqn{\lambda_i = (\hat{f}_i / g)^{-\alpha}}
}

Bandwidth selection (when \code{hval=NA}):
\itemize{
\item d=2: \eqn{h = 1.77 \times n^{-1/(d+4)}}
\item d=3: \eqn{h = 2.78 \times n^{-1/(d+4)}}
\item d>3: Uses formula from Silverman (1986, p. 87)
}

For bivariate data (d=2), optionally creates 3D perspective plot using
\code{\link[akima]{interp}}.
}
\references{
Silverman, B. W. (1986). Density Estimation for Statistics and Data Analysis.
Chapman & Hall.
}
\seealso{
\code{\link{akerdcdf}}, \code{\link{rdplot}}
}
\keyword{distribution}
\keyword{multivariate}
\keyword{smooth}
