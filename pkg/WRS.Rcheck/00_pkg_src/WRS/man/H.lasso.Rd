% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{H.lasso}
\alias{H.lasso}
\title{Huberized Lasso Regression}
\usage{
H.lasso(
  x,
  y,
  lambda.lasso.try = NULL,
  k = 1.5,
  STAND = TRUE,
  xout = FALSE,
  outfun = outpro,
  ...
)
}
\arguments{
\item{x}{Predictor matrix (n x p).}

\item{y}{Response vector (length n).}

\item{lambda.lasso.try}{Vector of lambda values to try for cross-validation.
Default: sequence from 0.01 to 0.6 with 100 values.}

\item{k}{Huber tuning constant controlling the transition between squared
and absolute loss (default: 1.5). Larger values are less robust but
more efficient.}

\item{STAND}{Logical. If \code{TRUE} (default), standardizes predictors
before fitting.}

\item{xout}{Logical. If \code{TRUE}, removes outliers from predictors
using \code{outfun} before fitting.}

\item{outfun}{Outlier detection function (default: \code{\link{outpro}}).}

\item{...}{Additional arguments passed to \code{outfun}.}
}
\value{
A list with components:
\item{coef}{Coefficient estimates (including intercept)}
\item{fit}{Fitted values}
\item{iter}{Number of iterations until convergence}
\item{sigma.est}{Estimated residual variance}
\item{lambda.lasso.opt}{Optimal lambda selected by cross-validation}
\item{residuals}{Final residuals}
}
\description{
Performs robust penalized regression using Huberized lasso, which combines
L1 penalization (lasso) with Huber loss for robustness to outliers.
Implementation based on Jung et al. (2016).
}
\details{
The Huberized lasso combines:
\itemize{
\item \strong{Huber loss}: Robust loss function that uses squared error for
small residuals and absolute error for large residuals (transition at k)
\item \strong{L1 penalty}: Lasso penalty for variable selection
}

The algorithm:
\enumerate{
\item Fits initial lasso using cross-validation to select lambda
\item Iteratively refines by Winsorizing residuals (bounded by k*sigma)
\item Refits lasso with adjusted response until convergence
}

This approach is more robust than ordinary lasso to outliers in the response
while maintaining the variable selection properties of lasso.

\strong{Reference}: Jung, K.M., Seo, S. & Ahn, J. (2016). Selection in lasso-type
problems. Electronic Journal of Statistics.
}
\examples{
\dontrun{
# Simulated data with outliers
set.seed(123)
n <- 100; p <- 10
x <- matrix(rnorm(n*p), n, p)
y <- x[,1] + 2*x[,2] + rnorm(n)
y[1:5] <- y[1:5] + 10  # Add outliers

# Fit Huberized lasso
fit <- H.lasso(x, y)
fit$coef
}

}
\seealso{
\code{\link{HQreg}}, \code{\link{winsorized}}
}
\keyword{regression}
\keyword{robust}
