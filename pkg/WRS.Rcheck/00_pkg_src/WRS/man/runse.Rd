% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{runse}
\alias{runse}
\title{Standard Errors for Running Interval Smoother}
\usage{
runse(
  x,
  y,
  fr = 1,
  tr = 0.2,
  pts = x,
  RNA = FALSE,
  outfun = out,
  xout = FALSE,
  SEED = TRUE
)
}
\arguments{
\item{x}{Predictor variable}

\item{y}{Response variable}

\item{fr}{Span parameter controlling smoothing (default: 1)}

\item{tr}{Trimming proportion (default: 0.2)}

\item{pts}{Points at which to estimate SE (default: x)}

\item{RNA}{Logical: remove NAs (default: FALSE, not used)}

\item{outfun}{Outlier detection function (default: out)}

\item{xout}{Logical: remove x outliers (default: FALSE)}

\item{SEED}{Logical: set random seed (default: TRUE)}
}
\value{
A list with components:
\item{se}{Vector of standard errors}
\item{df}{Vector of degrees of freedom}
}
\description{
Estimate standard errors for predicted values from a running interval
smoother based on trimmed means.
}
\details{
For each point in pts, finds the nearest neighbors and computes the
standard error of the trimmed mean using trimse(). Also returns the
degrees of freedom for each estimate.

Only works with single predictor (not multivariate). Missing values are
automatically removed.
}
\seealso{
\code{\link{trimse}}, \code{\link{runhat}}, \code{\link{runmean}}
}
\keyword{nonparametric}
\keyword{smooth}
