% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{rplot.binCI}
\alias{rplot.binCI}
\title{Confidence Intervals for Binary Outcome Probabilities}
\usage{
rplot.binCI(
  x,
  y,
  pts = NULL,
  alpha = 0.05,
  nmin = 5,
  xout = FALSE,
  outfun = outpro,
  fr = 0.5,
  tr.plot = FALSE,
  method = NULL,
  plotit = TRUE,
  LP = TRUE,
  xlab = "X",
  ylab = "P(Y=1|X)",
  ...
)
}
\arguments{
\item{x}{Numeric vector of predictor values.}

\item{y}{Binary response vector (will be converted to 0/1).}

\item{pts}{Points defining interval centers (default: \code{NULL} uses all
unique x values). If specified as vector like \code{c(-1,0,1,2)}, defines
intervals (-1,0), (0,1), (1,2).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{nmin}{Minimum number of observations required in interval (default: 5).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{fr}{Span for defining neighborhoods (default: 0.5).}

\item{tr.plot}{Logical; if \code{TRUE}, trims plot to middle 80\% of points
(default: \code{FALSE}).}

\item{method}{Method for binomial CI: "AC" (Agresti-Coull) or "CP"
(Clopper-Pearson). If \code{NULL} (default), uses "AC" for n<80, "CP" otherwise.}

\item{plotit}{Logical; if \code{TRUE} (default), creates a plot.}

\item{LP}{Logical; if \code{TRUE} (default), smooths CI bounds with lowess.}

\item{xlab}{Label for x-axis (default: "X").}

\item{ylab}{Label for y-axis (default: "P(Y=1|X)").}

\item{...}{Additional arguments passed to outlier detection function.}
}
\value{
Matrix with columns:
\item{n}{Sample size in interval}
\item{low.end}{Lower end of x interval}
\item{upper.end}{Upper end of x interval}
\item{pts}{Interval center}
\item{p.hat}{Estimated P(Y=1|X)}
\item{ci.low}{Lower confidence bound}
\item{ci.up}{Upper confidence bound}
}
\description{
Computes and plots confidence intervals for P(Y=1|X) at intervals along
the predictor range. Provides a robust alternative to logistic regression
with interval-based probability estimation.
}
\details{
For each interval center in \code{pts}:
\enumerate{
\item Finds all x values within \code{fr} fraction of that point
\item Computes proportion of Y=1 in that neighborhood
\item Computes confidence interval using \code{binom.conf()}
\item Plots estimate and CI bounds
}

The function automatically selects appropriate binomial CI method based on
sample size. Lowess smoothing of bounds (\code{LP=TRUE}) can improve visualization.
}
\examples{
# Binary outcome with confidence intervals
x <- rnorm(200)
p <- plogis(2*x)
y <- rbinom(200, 1, p)
rplot.binCI(x, y, alpha=0.05)
}
\seealso{
\code{\link{rplot.bin}}, \code{\link{binom.conf}}
}
