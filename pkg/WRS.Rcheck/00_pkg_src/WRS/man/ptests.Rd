% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{ptests}
\alias{ptests}
\title{Combine P-values from Independent Tests}
\usage{
ptests(pv, Fisher = TRUE)
}
\arguments{
\item{pv}{Numeric vector of p-values from N independent tests.}

\item{Fisher}{Logical. If \code{TRUE}, use Fisher's method; if \code{FALSE},
use Chen-Nadarajah method (default: \code{TRUE}).}
}
\value{
List with components:
\itemize{
\item \code{test.stat}: Combined test statistic
\item \code{p.value}: Overall p-value for global null hypothesis
}
}
\description{
Tests the global null hypothesis that all individual null hypotheses are true
by combining p-values from N independent tests. Supports Fisher's method and
Chen-Nadarajah method.
}
\details{
\strong{Fisher's method}: Test statistic = -2 * sum(log(p_i)), distributed as chi-square
with 2N degrees of freedom.

\strong{Chen-Nadarajah method}: Test statistic = sum(qnorm(p_i/2)^2), distributed as
chi-square with N degrees of freedom.

Both methods assume p-values come from independent tests.
}
\examples{
\dontrun{
# Combine p-values from 5 tests
pvals <- c(0.03, 0.15, 0.08, 0.22, 0.01)
ptests(pvals, Fisher = TRUE)
ptests(pvals, Fisher = FALSE)
}

}
\references{
Fisher, R. A. (1934). Statistical Methods for Research Workers.

Chen, Z., & Nadarajah, S. (2014). On the optimally weighted z-test for combining
probabilities from independent studies.
}
\keyword{htest}
