% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression.R
\name{wreg}
\alias{wreg}
\title{Wilcoxon Rank-Based Regression}
\usage{
wreg(x, y, iter = 10)
}
\arguments{
\item{x}{A numeric matrix of predictor variables (n by p), or a vector for
simple linear regression.}

\item{y}{A numeric vector of the dependent variable (length n).}

\item{iter}{Maximum number of iterations for the Gauss-Seidel algorithm when
p > 1 (default: 10).}
}
\value{
A list with components:
\describe{
\item{coef}{Vector of regression coefficients: \code{c(intercept, slopes)}.}
\item{residuals}{Residuals from the fit.}
}
}
\description{
Computes the Wilcoxon R-estimator for regression, a rank-based robust
regression method. For multiple predictors, uses the Gauss-Seidel algorithm
to iterate to a solution.
}
\details{
The Wilcoxon R-estimator minimizes the dispersion of residuals based on
Wilcoxon rank scores, providing a robust alternative to least squares.

\strong{For simple regression (p=1)}:
\itemize{
\item Uses \code{\link{wsp1reg}} to find the slope that minimizes
\eqn{\sum_{i<j} |r_i - r_j|} where \eqn{r_i} are residuals
\item Intercept is the median of residuals
}

\strong{For multiple regression (p>1)}:
\itemize{
\item Uses Gauss-Seidel iterative algorithm
\item In each iteration, updates one slope at a time holding others fixed
\item Continues until convergence (change < 0.0001) or \code{iter} reached
\item Initial values from univariate Wilcoxon regressions
}

Properties:
\itemize{
\item Distribution-free (no normality assumption)
\item Resistant to outliers in y
\item Asymptotically as efficient as least squares for normal data
\item Related to Kendall's tau
}
}
\examples{
\dontrun{
# Simple Wilcoxon regression
set.seed(123)
x <- rnorm(50)
y <- 2*x + rnorm(50)
wreg(x, y)

# Multiple regression
x <- matrix(rnorm(100), ncol=2)
y <- x[,1] + 2*x[,2] + rnorm(50)
wreg(x, y)

# With outliers
y[1:3] <- y[1:3] + 10
wreg(x, y)  # Robust result
}
}
\references{
Hettmansperger, T.P. & McKean, J.W. (2011). \emph{Robust Nonparametric
Statistical Methods} (2nd ed.). CRC Press.

Wilcox, R.R. (2022). \emph{Introduction to Robust Estimation and Hypothesis Testing}
(5th ed.). Academic Press.
}
\seealso{
\code{\link{wsp1reg}} for simple Wilcoxon regression,
\code{\link{tsreg}} for Theil-Sen regression

Other robust regression functions: 
\code{\link{LMSreg}()},
\code{\link{MMreg}()},
\code{\link{bireg}()},
\code{\link{bmreg}()},
\code{\link{chreg}()},
\code{\link{depreg}()},
\code{\link{mbmreg}()},
\code{\link{mdepreg}()},
\code{\link{mopreg}()},
\code{\link{snmreg}()},
\code{\link{tsgreg}()},
\code{\link{winreg}()}
}
\concept{robust regression functions}
