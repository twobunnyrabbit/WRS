% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{scorregci}
\alias{scorregci}
\title{Skipped Correlation Regression with Confidence Intervals}
\usage{
scorregci(
  x,
  y,
  nboot = 500,
  alpha = c(0.05, 0.025, 0.01),
  SEED = TRUE,
  corfun = pcor,
  outfun = outpro,
  crit.pv = NULL,
  ALL = TRUE,
  MC = TRUE,
  pvals = NULL,
  hoch = TRUE,
  iter = 500,
  pval.SEED = TRUE,
  pr = TRUE,
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{nboot}{Number of bootstrap samples (default: 500).}

\item{alpha}{Vector of significance levels for critical p-values (default: c(.05, .025, .01)).
FWE rate is taken to be \code{alpha[1]}.}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{corfun}{Correlation function to use. Common options: \code{pbcor} (percentage
bend), \code{pcor} (Pearson), \code{wincor} (winsorized), \code{spear} (Spearman).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{crit.pv}{Pre-computed critical p-values (default: \code{NULL} for automatic).}

\item{ALL}{Logical. Eliminate outliers jointly from cbind(x, y) if \code{TRUE} (default),
or separately for each (x\link{,j}, y) pair if \code{FALSE}.}

\item{MC}{Logical. Use parallel processing (default: \code{TRUE}).}

\item{pvals}{Pre-computed p-value distribution (default: \code{NULL}).}

\item{hoch}{Logical. Use Hochberg adjustment if \code{TRUE} (default: \code{TRUE}).}

\item{iter}{Number of iterations for critical p-value simulation (default: 500).}

\item{pval.SEED}{Logical. Set seed when computing critical p-values (default: \code{TRUE}).}

\item{pr}{Logical. Print progress messages (default: \code{TRUE}).}

\item{...}{Additional arguments passed to \code{scorreg()}.}
}
\value{
List with components:
\item{Estimates}{Matrix with columns: Est., p-value, adjusted p.value.}
\item{confidence.int}{Matrix with columns: Var, ci.low, ci.up.}
\item{critical.p.values}{Critical p-values for controlling FWE (only if \code{hoch = FALSE}).}
}
\description{
Tests zero correlation between y and each predictor in x using skipped
correlation, controlling FWE. Returns confidence intervals when hoch = FALSE.
}
\details{
Tests H0: rho(y, x_j) = 0 for j = 1,...,p while controlling familywise error
rate at level \code{alpha[1]} (default 0.05).

\strong{Two approaches:}
\itemize{
\item \strong{hoch = TRUE} (default, faster): Uses Hochberg adjustment. Reasonable for
n >= 60 and alpha = .05. Returns unadjusted confidence intervals but no
critical p-values.
\item \strong{hoch = FALSE} (recommended for alpha < .05 or n < 60): Computes critical
p-values via simulation (can be slow). Returns confidence intervals with
controlled FWE.
}

Pre-compute critical p-values to reduce execution time:

\if{html}{\out{<div class="sourceCode">}}\preformatted{pv <- scorregci.cr(n, p)$crit.p.values
scorregci(x, y, crit.pv = pv)
}\if{html}{\out{</div>}}
}
\examples{
\dontrun{
set.seed(123)
x <- matrix(rnorm(50 * 3), ncol = 3)
y <- 0.5 * x[, 1] + rnorm(50)

# Fast Hochberg adjustment
result <- scorregci(x, y, hoch = TRUE, nboot = 500)

# Full method with critical p-values
result2 <- scorregci(x, y, hoch = FALSE, nboot = 500)
}
}
\seealso{
\code{\link{scorreg}}, \code{\link{scorregciH}}, \code{\link{scorregci.cr}}, \code{\link{mscorci}}
}
