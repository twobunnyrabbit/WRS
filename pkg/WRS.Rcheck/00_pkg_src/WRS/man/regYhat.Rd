% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00-utils-core.R
\name{regYhat}
\alias{regYhat}
\alias{reg.pred}
\title{Predict Y Values Using Robust Regression}
\usage{
regYhat(
  x,
  y,
  xr = x,
  regfun = tsreg,
  xout = FALSE,
  outfun = outpro,
  pr = FALSE,
  plot.pts = FALSE,
  pts = NULL,
  ...
)

reg.pred(
  x,
  y,
  xr = x,
  regfun = tsreg,
  xout = FALSE,
  outfun = outpro,
  pr = FALSE,
  plot.pts = FALSE,
  pts = NULL,
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{xr}{Matrix or vector of X values at which to predict Y (default: x).}

\item{regfun}{Regression function to use for fitting (default: tsreg for
Theil-Sen regression).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{pr}{Logical. If TRUE, prints progress messages (default: FALSE).}

\item{plot.pts}{Logical. If TRUE, plots the predicted points (default: FALSE).}

\item{pts}{Alternative to xr for specifying prediction points. If not NULL,
overrides xr.}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
A vector of predicted Y values at the specified X points.
}
\description{
Computes predicted Y values at specified X points using a robust regression
function. A convenience wrapper for obtaining predictions from various
regression methods.
}
\details{
This function fits a robust regression using \code{regfun}, then computes
predicted values at the points specified by \code{xr} (or \code{pts}). The
regression function must return a list with a \code{coef} component containing
the regression coefficients.

If \code{xout = TRUE}, outliers in X are removed before fitting the regression.
}
\examples{
x <- rnorm(50)
y <- 2 + 3*x + rnorm(50)

# Predict at original X values
yhat <- regYhat(x, y)

# Predict at new X values
xnew <- seq(-2, 2, length.out = 10)
yhat_new <- regYhat(x, y, xr = xnew)

# Use different regression method
yhat_ols <- regYhat(x, y, regfun = ols)

}
\seealso{
\code{\link{tsreg}}, \code{\link{ols}}
}
