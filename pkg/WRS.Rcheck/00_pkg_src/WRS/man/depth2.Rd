% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outliers.R
\name{depth2}
\alias{depth2}
\title{Compute Exact Halfspace Depth for Bivariate Data}
\usage{
depth2(x, pts = NA, plotit = TRUE, xlab = "VAR 1", ylab = "VAR 2")
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{pts}{Points for which to compute depth. If NA, uses \code{x} (default: NA).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{xlab}{Label for x-axis.}

\item{ylab}{Label for y-axis.}
}
\value{
Vector of depth values for each point in \code{pts}.
}
\description{
Computes exact halfspace (Tukey) depth for bivariate data. For each point,
calculates the minimum proportion of data in any closed halfspace containing
that point.
}
\details{
Only works with bivariate data (2 columns). Uses the exact algorithm for
halfspace depth. With \code{plotit=TRUE}, creates a scatterplot showing the
deepest point (center) marked with "+" and the median depth contour.
}
\seealso{
\code{\link{fdepthv2}}, \code{\link{prodepth}}
}
