% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{l2drmci}
\alias{l2drmci}
\title{Bootstrap CI for Difference in Location with Missing Data Handling}
\usage{
l2drmci(
  x,
  y = NULL,
  est = median,
  alpha = 0.05,
  nboot = 2000,
  SEED = TRUE,
  pr = TRUE,
  na.rm = TRUE,
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix with two columns. If matrix, second column
is treated as y.}

\item{y}{Numeric vector for second group (possibly dependent on x). If \code{NULL}
and x is matrix, y is taken from second column of x. Default is \code{NULL}.}

\item{est}{Estimator function to use (default: \code{median}).}

\item{alpha}{Significance level (default: 0.05).}

\item{nboot}{Number of bootstrap samples (default: 2000).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed to 2 for reproducibility.}

\item{pr}{Logical. If \code{TRUE}, prints progress message.}

\item{na.rm}{Logical. If \code{FALSE}, assumes missing values occur at random and uses
pairwise deletion. If \code{TRUE}, uses complete cases only. Default is \code{TRUE}.}

\item{...}{Additional arguments passed to the estimator function.}
}
\value{
A list with components:
\item{Est.}{Point estimate of the location measure}
\item{ci}{Confidence interval (vector of length 2)}
\item{p.value}{Two-sided p-value for testing H0: location = 0}
}
\description{
Computes a bootstrap confidence interval for a measure of location associated
with the distribution of \code{x-y}. Handles missing data with option for
paired removal.
}
\details{
This function is similar to \code{\link{l2dci}} but provides additional control
over missing data handling via the \code{na.rm} parameter.
}
\seealso{
\code{\link{l2dci}}
}
\keyword{bootstrap}
