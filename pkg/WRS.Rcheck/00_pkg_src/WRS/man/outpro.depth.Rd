% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outliers.R
\name{outpro.depth}
\alias{outpro.depth}
\title{Projection-Based Outlier Detection Using Depth}
\usage{
outpro.depth(
  x,
  ndir = 1000,
  MM = FALSE,
  SEED = TRUE,
  plotit = FALSE,
  xlab = "X",
  ylab = "Y"
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{ndir}{Number of random projections to use for computing depth
(default: 1000).}

\item{MM}{Logical. If TRUE, uses MAD for dispersion in outlier detection.}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{xlab}{Label for x-axis in plots.}

\item{ylab}{Label for y-axis in plots.}
}
\value{
A list with the same structure as \code{outpro()}:
\item{out.id}{Indices of detected outliers.}
\item{keep}{Indices of non-outliers.}
}
\description{
Detects outliers using projection depth. For multivariate data, computes
projection depth for each point and identifies outliers based on inverse depth.
Designed to handle large sample sizes efficiently.
}
\details{
For univariate data, simply calls \code{outpro()}. For multivariate data, computes
projection depth using \code{prodepth()}, then applies outlier detection to the
inverse depths. For bivariate data with \code{plotit=TRUE}, creates a scatterplot
with outliers marked and the median depth contour plotted.
}
\seealso{
\code{\link{outpro}}, \code{\link{prodepth}}
}
