% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{near3dl1}
\alias{near3dl1}
\title{Find Nearby Points (3D Mahalanobis Distance, Lower Bound)}
\usage{
near3dl1(x, pt, fr = 1, m)
}
\arguments{
\item{x}{Numeric matrix (n×p) of multivariate data points.}

\item{pt}{Numeric vector of length p specifying the target point.}

\item{fr}{Numeric. Distance threshold multiplier (default: 1).}

\item{m}{List containing covariance matrix in \code{$cov} component.}
}
\value{
Logical vector of length n: \code{TRUE} for points with
Mahalanobis distance < \code{fr} AND distance > 3rd smallest distance,
\code{FALSE} otherwise.
}
\description{
Internal helper function that identifies points within a specified Mahalanobis
distance from a target point, excluding the 3 closest points. Used in local
smoothing algorithms.
}
\details{
Computes Mahalanobis distance from each row of \code{x} to \code{pt}, then
flags points within threshold \code{fr} but excludes the 3 nearest neighbors.
Useful for kernel smoothing where immediate neighbors are handled separately.
}
\keyword{internal}
