% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{rm.marg.OMCI}
\alias{rm.marg.OMCI}
\title{Marginal Effect Size CI for Dependent Groups}
\usage{
rm.marg.OMCI(
  x,
  y = NULL,
  locfun = onestep,
  nboot = 1000,
  SEED = TRUE,
  alpha = 0.05,
  null.val = 0,
  MC = FALSE,
  ...
)
}
\arguments{
\item{x}{Either a two-column matrix/data frame or a vector (if y is specified)}

\item{y}{Optional second group vector. If provided, combined with x into two-column matrix.}

\item{locfun}{Location estimator function (default: \code{\link{onestep}})}

\item{nboot}{Number of bootstrap samples (default: 1000)}

\item{SEED}{Logical or numeric. Controls random seed. Default is \code{TRUE}.}

\item{alpha}{Alpha level for confidence interval (default: 0.05)}

\item{null.val}{Null hypothesis value for two-sided p-value test (default: 0)}

\item{MC}{Logical. If \code{TRUE}, use parallel processing via \code{\link{mclapply}}.
Default is \code{FALSE}.}

\item{...}{Additional arguments passed to \code{locfun}}
}
\value{
A list with components:
\item{n}{Sample size (number of pairs)}
\item{effect.size}{Estimated marginal effect size}
\item{ci}{Bootstrap confidence interval (lower, upper)}
\item{p.value}{Two-sided p-value testing effect.size = null.val}
}
\description{
Computes a bootstrap confidence interval for a marginal effect size that
accounts for heteroscedasticity and the association between X and Y based
on marginal distributions (not difference scores).
}
\details{
For two dependent groups, there are two approaches to computing effect sizes:
\enumerate{
\item Based on difference scores: \code{(X - Y)}
\item Based on marginal distributions: comparing X and Y directly
}

This function uses the marginal approach, computing effect sizes via
\code{\link{rm.margOM.es}}. For robust estimators, these two approaches
generally yield different results. The marginal approach better accounts
for heteroscedasticity.

A percentile bootstrap is used to construct confidence intervals. The
p-value tests whether the effect size differs from \code{null.val}.
}
\seealso{
\code{\link{rm.margOM.es}}, \code{\link{yuend}} for difference score approach
}
\keyword{htest}
\keyword{robust}
