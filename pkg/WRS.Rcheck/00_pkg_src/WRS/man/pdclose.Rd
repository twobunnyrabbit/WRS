% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{pdclose}
\alias{pdclose}
\title{Identify Close Points Based on Projection Distance}
\usage{
pdclose(x, pts = x, fr = 1, MM = FALSE, MC = FALSE, STAND = TRUE)
}
\arguments{
\item{x}{Matrix or vector of data points (observations).}

\item{pts}{Matrix or vector of target points. Default is \code{x}.}

\item{fr}{Fraction defining closeness threshold. Points with projection distance
< \code{fr} are considered close. Default is 1.}

\item{MM}{Logical. If \code{TRUE}, use marginal medians in projection distance.
Default is \code{FALSE}.}

\item{MC}{Logical. If \code{TRUE}, use parallel processing. Default is \code{FALSE}.}

\item{STAND}{Logical. If \code{TRUE}, standardize \code{x} before computing distances.
Default is \code{TRUE}.}
}
\value{
Matrix with one row per point in \code{pts} and one column per point in \code{x}.
Entry (i,j) indicates whether point j in \code{x} is close to point i in \code{pts}.
}
\description{
For each point in \code{pts}, determines which points in \code{x} are close
to it based on projection distance.
}
\details{
This function uses projection-based distances to determine proximity. The projection
distance is divided by the interquartile range for scale invariance. Points with
distance less than \code{fr} times this scaled distance are flagged as close.

If \code{STAND=TRUE}, standardizes each column of \code{x} to mean 0 and SD 1
before computing distances.
}
\seealso{
\code{\link{pdis}}, \code{\link{pdisMC}}, \code{\link{outpro}}
}
\keyword{cluster}
\keyword{multivariate}
