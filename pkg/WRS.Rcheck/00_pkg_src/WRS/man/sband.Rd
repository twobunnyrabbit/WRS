% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{sband}
\alias{sband}
\title{Confidence Band for Shift Function (Two Independent Groups)}
\usage{
sband(
  x,
  y,
  plotit = TRUE,
  CI = TRUE,
  alpha = 0.05,
  crit = NULL,
  sm = TRUE,
  op = 1,
  xlab = "First Group",
  ylab = "Est. 2 - Est. 1"
)
}
\arguments{
\item{x}{Numeric vector for first (reference) group.}

\item{y}{Numeric vector for second group.}

\item{plotit}{Logical. If \code{TRUE} (default), plot the shift function with
confidence band.}

\item{CI}{Logical. If \code{TRUE} (default), compute confidence intervals.
If \code{FALSE}, only plot without returning CI information.}

\item{alpha}{Numeric. Significance level for confidence band (default: 0.05).}

\item{crit}{Numeric or \code{NULL}. Critical value for the band. If \code{NULL}
(default), computed using Kolmogorov-Smirnov approach via \code{\link{ks.crit}}.}

\item{sm}{Logical. If \code{TRUE} (default), smooth the shift function.}

\item{op}{Integer. If \code{op==1}, use lowess for smoothing; otherwise use
running mean smoother.}

\item{xlab}{Character. Label for x-axis (default: "First Group").}

\item{ylab}{Character. Label for y-axis (default: "Est. 2 - Est. 1").}
}
\value{
List with components:
\item{m}{Matrix with 3 columns: quantiles of x, lower CI, upper CI. \code{NULL}
if \code{CI=FALSE}}
\item{crit}{Critical value used for confidence band}
\item{numsig}{Number of points where CI excludes zero (significant differences)}
\item{q.sig.greater}{Quantiles of x where group 2 significantly greater than group 1}
\item{q.sig.less}{Quantiles of x where group 2 significantly less than group 1}
\item{prob.coverage}{Estimated probability coverage of the confidence band}
}
\description{
Computes a confidence band for the shift function comparing two independent
groups. The shift function shows how the difference between group quantiles
varies across the distribution.
}
\details{
The shift function plots the difference between corresponding quantiles of
two distributions. It reveals where and how the distributions differ beyond
simple location shifts.

The confidence band is based on the Kolmogorov-Smirnov statistic, adjusted
for ties if present. Points where the confidence band excludes zero indicate
significant differences between group quantiles.

When plotting:
\itemize{
\item Solid line: (smoothed) shift function
\item Dashed lines: Confidence band boundaries
\item "+": Median of reference group
\item "o": First and third quartiles of reference group
}

\strong{Note}: Results depend on which group is the reference. \code{sband(x,y)}
often differs from \code{sband(y,x)}.
}
\seealso{
\code{\link{shifthd}}, \code{\link{ks.crit}}, \code{\link{kssig}}
}
\keyword{hplot}
\keyword{htest}
\keyword{robust}
