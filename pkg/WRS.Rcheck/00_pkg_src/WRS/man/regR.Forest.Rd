% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression-advanced.R
\name{regR.Forest}
\alias{regR.Forest}
\title{Random Forest Regression with Robust Location Estimation}
\usage{
regR.Forest(
  x,
  y,
  newdata = NULL,
  pts = x,
  pyhat = FALSE,
  loc.fun = tmean,
  xout = FALSE,
  plotit = TRUE,
  outfun = outpro,
  span = 0.75,
  LP = TRUE,
  pch = ".",
  ZLIM = FALSE,
  scale = TRUE,
  xlab = "X",
  ylab = "Y",
  ticktype = "simple",
  frame = TRUE,
  eout = FALSE,
  zlab = "",
  theta = 50,
  phi = 25,
  ...
)
}
\arguments{
\item{x}{A numeric matrix or vector containing predictor values (training data).}

\item{y}{A numeric vector containing response values (training data).}

\item{newdata}{A data frame of predictor values at which predictions are
desired. If \code{NULL} (default), predictions are made at training data points.}

\item{pts}{Predictor values for smoothing (default: \code{x}).}

\item{pyhat}{Logical; if \code{TRUE}, return predicted values. If \code{FALSE} (default),
return \code{NULL}.}

\item{loc.fun}{Location function to estimate from the random forest quantile
predictions (default: \code{tmean} for 20\% trimmed mean). Can be any location
estimator like \code{median}, \code{mean}, \code{mom}, etc.}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{plotit}{Logical; if \code{TRUE} (default), create a plot of the fitted
regression surface or curve.}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{span}{Span parameter for LOESS smoothing (default: 0.75). Controls the
degree of smoothing.}

\item{LP}{Logical; if \code{TRUE} (default), apply LOESS smoothing to random forest
predictions. If \code{FALSE}, return raw random forest predictions.}

\item{pch}{Plotting character for scatter plot (default: '.').}

\item{ZLIM}{Logical; controls axis limits for 3D plots.}

\item{scale}{Logical; if \code{TRUE} (default), scale axes in plots.}

\item{xlab, ylab, zlab}{Axis labels for plots.}

\item{ticktype}{Tick mark type for 3D plots (default: 'simple').}

\item{frame}{Logical; if \code{TRUE} (default), draw frame around 3D plots.}

\item{eout}{Logical; if \code{TRUE}, remove outliers from fitted values in plots.}

\item{theta, phi}{Viewing angles for 3D plots (defaults: 50, 25).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
If \code{pyhat = TRUE}, returns a vector of predicted values (smoothed or
raw depending on \code{LP}). Otherwise, returns \code{NULL} (produces plot only).
}
\description{
Estimates a measure of location (e.g., trimmed mean, median) at predictor
values using quantile random forests, with optional LOESS smoothing for
visualization and improved predictions.
}
\details{
This function combines random forest regression with robust location estimation:
\enumerate{
\item Fits a quantile random forest to (x, y) using \code{quantregForest()}
\item Predicts conditional quantiles at \code{newdata} points
\item Estimates a robust location measure from the quantile predictions
\item Optionally smooths predictions using LOESS for better visualization
}

For single predictors, creates a 2D scatter plot with fitted curve. For two
predictors, creates a 3D surface plot.

If \code{xout = TRUE}, outliers are removed from the predictor space before
analysis using the function specified by \code{outfun}.
}
\note{
This function requires the \code{randomForest} package.
}
\examples{
\dontrun{
# Random forest regression with 1 predictor
set.seed(123)
x <- runif(100, 0, 10)
y <- sin(x) + rnorm(100, sd=0.3)
regR.Forest(x, y, pyhat=TRUE)

# With 2 predictors (creates 3D plot)
x2 <- matrix(runif(200), ncol=2)
y2 <- x2[,1]^2 + x2[,2] + rnorm(100)
regR.Forest(x2, y2)
}
}
\references{
Wilcox, R.R. (2022). Introduction to Robust Estimation and Hypothesis Testing
(5th ed.). Academic Press.
}
\seealso{
\code{\link{KNNreg}}, \code{\link{qhdsm}}, \code{\link[randomForest]{randomForest}}
}
