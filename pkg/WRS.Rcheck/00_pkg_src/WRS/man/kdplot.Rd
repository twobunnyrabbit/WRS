% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{kdplot}
\alias{kdplot}
\title{Plot Kernel Density Estimate}
\usage{
kdplot(x, rval = 15, xlab = "X", ylab = "Y")
}
\arguments{
\item{x}{Numeric vector of observations.}

\item{rval}{Number of evaluation points for the density curve (default: 15).}

\item{xlab, ylab}{Axis labels (defaults: "X", "Y").}
}
\value{
Invisibly returns \code{NULL}. Creates a plot as a side effect.
}
\description{
Computes and plots a kernel density estimate across a range of values,
providing a smooth estimate of the probability density function.
}
\details{
The function:
\enumerate{
\item Removes missing values from \code{x}
\item Determines evaluation range from 1st to 99th percentiles
\item Computes kernel density at \code{rval} equally-spaced points using \code{\link{kerden}}
\item Plots the density curve
}

Uses a relatively small number of evaluation points (default 15) for faster
computation compared to standard kernel density estimators.
}
\examples{
# Plot kernel density estimate
x <- rnorm(100)
kdplot(x)

# Use more evaluation points for smoother curve
kdplot(x, rval=50)
}
\seealso{
\code{\link{kerden}} for the underlying kernel density computation
}
