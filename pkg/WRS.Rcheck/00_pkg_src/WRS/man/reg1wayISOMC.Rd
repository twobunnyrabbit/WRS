% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression.R
\name{reg1wayISOMC}
\alias{reg1wayISOMC}
\title{One-Way Regression ANOVA with Isotonic Bootstrap (Parallel Processing)}
\usage{
reg1wayISOMC(
  x,
  y,
  regfun = tsreg,
  nboot = 100,
  SEED = TRUE,
  xout = FALSE,
  outfun = outpro,
  STAND = TRUE,
  alpha = 0.05,
  pr = TRUE,
  ...
)
}
\arguments{
\item{x}{List of length J, where \code{x[[j]]} is an n_j by p matrix of
predictors for group j.}

\item{y}{List of length J, where \code{y[[j]]} is a vector of length n_j
containing the response values for group j.}

\item{regfun}{Regression function to use (default: \code{tsreg}).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{STAND}{Logical; if TRUE, standardizes the test statistic (default: TRUE).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{pr}{Logical; if TRUE, prints a reminder about using \code{xout=TRUE}
(default: TRUE).}

\item{...}{Additional arguments passed to \code{regfun}.}
}
\value{
A list with components:
\describe{
\item{test}{The test statistic value.}
\item{crit.val}{Critical value for the test at level \code{alpha}.}
\item{p.value}{Bootstrap p-value for the test.}
\item{n}{Vector of sample sizes for each group (before outlier removal).}
\item{n.keep}{Vector of sample sizes for each group (after outlier removal).}
}
}
\description{
Tests the hypothesis that regression parameters are equal across J independent
groups using bootstrap standard errors and a Johansen-type MANOVA statistic.
This is the parallel processing (multicore) version using \code{mclapply}.
}
\details{
This function extends one-way regression ANOVA to multiple groups using
a robust approach:

\strong{Null Hypothesis}: All J groups have identical regression coefficients
(both intercepts and slopes).

\strong{Algorithm}:
\enumerate{
\item For each group j = 1, ..., J:
\itemize{
\item Optionally remove leverage points using \code{outfun} if \code{xout=TRUE}
\item Estimate regression coefficients using \code{regfun}
\item Bootstrap to estimate standard errors (using parallel processing)
}
\item Compute Johansen-type test statistic comparing coefficient estimates
\item Determine significance via bootstrap critical value
}

The isotonic bootstrap is used to ensure better finite-sample performance.
Parallel processing via \code{mclapply} makes this feasible even with
large \code{nboot}.

\strong{Important}: This is a global test. If rejected, use post-hoc methods
like \code{\link{reg2ciMC}} for pairwise comparisons.
}
\examples{
\dontrun{
# Three groups with different regression relationships
set.seed(123)
x <- list()
y <- list()
for(j in 1:3) {
  x[[j]] <- matrix(rnorm(100), ncol=2)
  # Different slopes for each group
  y[[j]] <- j * x[[j]][,1] + 2*x[[j]][,2] + rnorm(50)
}

# Test for equal regression coefficients
result <- reg1wayISOMC(x, y, nboot=500)
print(result$p.value)

# If significant, do pairwise comparisons
if(result$p.value < 0.05) {
  reg2ciMC(x[[1]], y[[1]], x[[2]], y[[2]])
}
}

}
\seealso{
\code{\link{reg1wayISO}} for non-parallel version,
\code{\link{reg1way}} for standard one-way regression test,
\code{\link{reg2ciMC}} for pairwise comparisons
}
