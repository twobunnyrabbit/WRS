% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{ancJNPVAL}
\alias{ancJNPVAL}
\title{Johnson-Neyman ANCOVA with Multiple Covariates (Improved Version)}
\usage{
ancJNPVAL(
  x1,
  y1,
  x2,
  y2,
  regfun = MMreg,
  p.crit = NULL,
  DEEP = TRUE,
  plotit = TRUE,
  xlab = "X1",
  ylab = "X2",
  null.value = 0,
  WARNS = FALSE,
  alpha = 0.05,
  pts = NULL,
  SEED = TRUE,
  nboot = 100,
  xout = FALSE,
  outfun = outpro,
  ...
)
}
\arguments{
\item{x1}{Matrix of covariates for group 1 (2-6 columns).}

\item{y1}{Outcome values for group 1.}

\item{x2}{Matrix of covariates for group 2 (must match x1 in columns).}

\item{y2}{Outcome values for group 2.}

\item{regfun}{Regression function to use. Default is MMreg (MM-regression).}

\item{p.crit}{Critical p-value for controlling familywise error. If NULL (default), computed automatically.}

\item{DEEP}{Logical. If TRUE (default), use deepest half of combined covariate points.
If FALSE, use depth-based design point selection.}

\item{plotit}{Logical. If TRUE (default), plot results for 2 covariates.}

\item{xlab, ylab}{Axis labels for plots.}

\item{null.value}{Null hypothesis value for difference. Default is 0.}

\item{WARNS}{Logical. If FALSE (default), suppress warnings during computation.}

\item{alpha}{Familywise error rate. Default is 0.05. Also supports 0.01.}

\item{pts}{Design points matrix (p columns). If NULL, chosen automatically.}

\item{SEED}{Logical. Set random seed for reproducibility. Default is TRUE.}

\item{nboot}{Number of bootstrap samples for variance estimation. Default is 100.}

\item{xout}{Logical. Remove outliers from covariates. Default is FALSE.}

\item{outfun}{Outlier detection function. Default is outpro.}

\item{...}{Additional arguments passed to regression function.}
}
\value{
A list with components:
\item{n1, n2}{Sample sizes for groups 1 and 2}
\item{num.sig}{Number of significant comparisons}
\item{p.crit}{Critical p-value used}
\item{points}{Design points used}
\item{output}{Matrix with estimates, tests, and p-values for each point}
\item{significant.points}{Covariate points with significant differences}
}
\description{
Compare two independent groups using Johnson-Neyman ANCOVA allowing multiple covariates.
This is an improved replacement for ancJNmp. Controls familywise error rate across
multiple design points.
}
\details{
For DEEP=FALSE, design points are chosen as the deepest point in combined data
plus points on the 0.5 depth contour. For DEEP=TRUE, uses deepest half of points.

When alpha=0.05 or 0.01 and number of covariates ≤6, p.crit is determined automatically
to control familywise error. For >6 covariates, adjustment method is unknown.
}
\examples{
\dontrun{
  # Two covariates
  x1 <- matrix(rnorm(100), 50, 2)
  y1 <- x1[,1] + x1[,2] + rnorm(50)
  x2 <- matrix(rnorm(100), 50, 2)
  y2 <- x2[,1] + x2[,2] + 1 + rnorm(50)
  result <- ancJNPVAL(x1, y1, x2, y2)
}
}
\keyword{ancova}
\keyword{multivariate}
