% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{twoDNOV}
\alias{twoDNOV}
\title{Compare Two Robust Dependent Correlations (Non-Overlapping)}
\usage{
twoDNOV(
  x,
  y,
  corfun = wincor,
  alpha = 0.05,
  nboot = 500,
  SEED = TRUE,
  MC = FALSE
)
}
\arguments{
\item{x}{Matrix or data frame with 2 columns.}

\item{y}{Matrix or data frame with 2 columns.}

\item{corfun}{Correlation function to use (default \code{wincor} for Winsorized correlation).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{nboot}{Number of bootstrap samples (default 500).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{MC}{Logical; if TRUE, uses parallel processing via mclapply.}
}
\value{
A list with components:
\item{est.rho1}{Correlation between x\link{,1} and x\link{,2}}
\item{est.rho2}{Correlation between y\link{,1} and y\link{,2}}
\item{est.dif}{Difference between the two correlations}
\item{ci}{Bootstrap confidence interval for the difference}
\item{p.value}{P-value for testing equality of correlations}
}
\description{
Compares two robust dependent correlations in the non-overlapping case using bootstrap.
By default, uses Winsorized correlation but other robust correlation measures can
be specified.
}
\details{
This function compares the correlation between x\link{,1} and x\link{,2} to the correlation
between y\link{,1} and y\link{,2}. This is the "non-overlapping" case because the two
correlations involve completely different pairs of variables.

The function uses robust correlation measures (Winsorized correlation by default)
which are less sensitive to outliers than Pearson correlation. The bootstrap
provides valid inference without normality assumptions.
}
\examples{
set.seed(123)
n <- 50
x <- cbind(rnorm(n), rnorm(n))
y <- cbind(rnorm(n), rnorm(n))

# Using Winsorized correlation
result <- twoDNOV(x, y)
result$ci
result$p.value
}
\seealso{
\code{\link{twoDcorR}}, \code{\link{TWOpNOV}}, \code{\link{tworhobt}},
\code{\link{wincor}}
}
