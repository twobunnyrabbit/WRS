% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{dep2.sign}
\alias{dep2.sign}
\title{Estimate Probability P(X<Y) for Dependent Pairs Using Logistic Regression}
\usage{
dep2.sign(
  x,
  y = NULL,
  LP = FALSE,
  CI = TRUE,
  xout = FALSE,
  outfun = outpro,
  plotit = TRUE,
  xlab = "X",
  ylab = "P(X<Y)",
  pr.ci = FALSE,
  ...
)
}
\arguments{
\item{x}{First group data vector, or a two-column matrix/data frame}

\item{y}{Second group data vector (optional if x is a matrix/data frame)}

\item{LP}{Logical: use local polynomial (smoother) method if TRUE (default: FALSE
uses logistic regression)}

\item{CI}{Logical: compute confidence intervals if TRUE (default: TRUE)}

\item{xout}{Logical: remove outliers before analysis (default: FALSE)}

\item{outfun}{Outlier detection function (default: \code{outpro})}

\item{plotit}{Logical: create plot (default: TRUE)}

\item{xlab}{Label for x-axis (default: 'X')}

\item{ylab}{Label for y-axis (default: 'P(X<Y)')}

\item{pr.ci}{Logical: print confidence interval results (default: FALSE)}

\item{...}{Additional arguments passed to underlying functions}
}
\value{
If pr.ci=FALSE, returns 'Done'. If pr.ci=TRUE, returns result from underlying
function (depends on LP and CI settings).
}
\description{
For two dependent groups, estimates the probability that Y > X as a function
of X using logistic regression or smoothing methods.
}
\details{
This function models P(Y > X | X = x) using either:
\itemize{
\item Logistic regression (LP=FALSE): Uses \code{\link{logreg}} or
\code{\link{logreg.P.ci}} depending on CI setting
\item Local polynomial smoothing (LP=TRUE): Uses \code{\link{logSM}} or
\code{\link{rplot.binCI}} depending on CI setting
}

The method creates a binary indicator (1 if X\link{i} < Y\link{i}, 0 otherwise) and
models this as a function of X values.

Useful for understanding how the probability of one group exceeding the other
varies across the range of X values.
}
\examples{
\dontrun{
x <- rnorm(50)
y <- x + rnorm(50, mean=0.3)
dep2.sign(x, y)  # Logistic regression
dep2.sign(x, y, LP=TRUE)  # Local polynomial
}

}
\seealso{
\code{\link{logreg}}, \code{\link{logreg.P.ci}}, \code{\link{logSM}},
\code{\link{rplot.binCI}}
}
\keyword{htest}
\keyword{regression}
\keyword{robust}
