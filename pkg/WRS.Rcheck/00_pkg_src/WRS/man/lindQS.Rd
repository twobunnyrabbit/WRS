% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{lindQS}
\alias{lindQS}
\title{Quantile Shift Effect Size for Linear Contrast (Dependent Groups)}
\usage{
lindQS(x, con, locfun = median, ...)
}
\arguments{
\item{x}{Data in matrix, data frame, or list format. For matrix/data.frame:
each column is a group. For list: x[\link{j}] contains data for group j.}

\item{con}{Numeric vector of contrast coefficients (length J). Must sum to zero.}

\item{locfun}{Location estimator function (default: \code{median}).}

\item{...}{Additional arguments passed to \code{locfun}.}
}
\value{
List with components:
\describe{
\item{Q.effect}{Quantile shift effect size (proportion)}
\item{center}{Location estimate of the contrast distribution}
}
}
\description{
Computes quantile shift measure of effect size for a linear contrast of
dependent variables. For groups X_1, ..., X_J, evaluates the contrast
Y = sum(c_j * X_j).
}
\details{
This function:
\itemize{
\item Forms all within-subject linear contrasts directly (no resampling)
\item Uses \code{\link{linWMWMC.sub}} to compute contrast values
\item Estimates location with the specified function
\item Computes quantile shift as proportion of contrasts on each side of center
}

The quantile effect size indicates the proportion of the distribution
below/above the location measure.
}
\note{
There is another version of this function that uses resampling. See line 33985.
}
\examples{
# Compare first vs second time point
set.seed(123)
x <- matrix(rnorm(100), ncol = 2)
x[,2] <- x[,2] + 0.3
result <- lindQS(x, con = c(1, -1))
result$Q.effect
}
\seealso{
\code{\link{linQS.sub2}} (helper function), \code{\link{linEP}} (explanatory power),
\code{\link{linWMWMC.sub}}
}
