% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{ebarplot}
\alias{ebarplot}
\title{Error Bar Plot for Group Comparisons}
\usage{
ebarplot(
  x,
  y = NULL,
  nse = 2,
  liw = uiw,
  aui = NULL,
  ali = aui,
  err = "y",
  tr = 0,
  ylim = NULL,
  sfrac = 0.01,
  gap = 0,
  add = FALSE,
  col = par("col"),
  lwd = par("lwd"),
  slty = par("lty"),
  xlab = "Group",
  ylab = NULL,
  ...
)
}
\arguments{
\item{x}{Data in matrix mode (groups as columns) or list mode (groups as elements).
If providing two vectors, see y parameter.}

\item{y}{Optional second group (vector). If provided, x should also be a vector
(default: NULL).}

\item{nse}{Number of standard errors for error bar width (default: 2). The error
bars extend ±nse standard errors from the mean.}

\item{liw}{Lower inner width (passed to plotCI, default: uiw).}

\item{aui}{Upper confidence limit (default: NULL, computed from data).}

\item{ali}{Lower confidence limit (default: aui).}

\item{err}{Direction of error bars: "y" for vertical, "x" for horizontal (default: "y").}

\item{tr}{Trimming proportion for trimmed mean (default: 0 = regular mean).
Use tr = 0.2 for 20\% trimmed mean. Maximum is 0.5 (use ebarplot.med for medians).}

\item{ylim}{Y-axis limits (default: NULL, automatically determined).}

\item{sfrac}{Fraction of plot width for error bar caps (default: 0.01).}

\item{gap}{Gap around points where error bars are not drawn (default: 0).}

\item{add}{Logical. Add to existing plot if TRUE (default: FALSE).}

\item{col}{Color for plotting (default: par("col")).}

\item{lwd}{Line width (default: par("lwd")).}

\item{slty}{Line type for error bars (default: par("lty")).}

\item{xlab}{Label for X-axis (default: "Group").}

\item{ylab}{Label for Y-axis (default: NULL).}

\item{...}{Additional graphical parameters passed to plotCI.}
}
\value{
None. Creates a plot.
}
\description{
Creates an error bar plot showing group means (or trimmed means) with error bars
representing standard errors. Useful for visualizing group differences with
uncertainty estimates.
}
\details{
This function creates error bar plots for comparing multiple groups. Each group
is represented by a point (mean or trimmed mean) with error bars extending
±nse standard errors.

\strong{Location measures}:
\itemize{
\item \strong{tr = 0} (default): Regular arithmetic mean
\item \strong{0 < tr < 0.5}: Trimmed mean (e.g., tr = 0.2 trims 20\% from each tail)
\item \strong{tr = 0.5}: Median (but use \code{ebarplot.med} instead for proper CIs)
}

\strong{Input formats}:
\itemize{
\item \strong{Matrix}: Each column represents a group
\item \strong{List}: Each element represents a group
\item \strong{Two vectors}: Provide x and y separately
}

\strong{Error bars}: By default, extend ±2 standard errors from the mean. This
approximately corresponds to a 95\% confidence interval for large samples.

Missing values are automatically removed from each group.

\strong{Note}: For median-based error bars with distribution-free confidence intervals,
use \code{ebarplot.med} instead.
}
\examples{
# Error bar plot for three groups
set.seed(123)
x1 <- rnorm(30, mean = 10, sd = 2)
x2 <- rnorm(30, mean = 12, sd = 2)
x3 <- rnorm(30, mean = 14, sd = 3)
ebarplot(list(x1, x2, x3))

# Using trimmed means
ebarplot(list(x1, x2, x3), tr = 0.2)

# Two-group comparison
ebarplot(x1, x2)
}
\seealso{
\code{\link{ebarplot.med}}, \code{\link{plotCI}}, \code{\link{tmean}}
}
