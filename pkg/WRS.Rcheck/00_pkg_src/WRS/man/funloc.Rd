% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/location.R
\name{funloc}
\alias{funloc}
\title{Functional Data Location Estimates with Confidence Intervals}
\usage{
funloc(
  x,
  tr = 0.2,
  pts = NULL,
  npts = 25,
  plotit = TRUE,
  alpha = 0.05,
  nv = rep(0, ncol(x)),
  xlab = "T",
  ylab = "Est.",
  FBP = TRUE,
  method = "hochberg",
  COLOR = TRUE
)
}
\arguments{
\item{x}{n-by-p matrix (rows = curves, columns = time points).}

\item{tr}{Trimming proportion (default: 0.2). Proportion of observations to
trim from each tail when computing winsorized correlation.}

\item{pts}{Specific time points for estimation (default: NULL uses evenly spaced points).}

\item{npts}{Number of evenly spaced points to use if pts = NULL (default: 25).}

\item{plotit}{Logical. If \code{TRUE}, create plot.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{nv}{Null values for hypothesis testing (default: zero vector).}

\item{xlab}{X-axis label (default: 'T').}

\item{ylab}{Y-axis label (default: 'Est.').}

\item{FBP}{Logical. If \code{TRUE}, create functional boxplot; if \code{FALSE}, plot estimates with CIs.}

\item{method}{Multiple comparison adjustment method (default: 'hochberg').}

\item{COLOR}{Logical. If \code{TRUE}, use color in functional boxplot.}
}
\value{
Matrix with columns: pts, est., s.e., p.value, adjust.p.value, ci.low, ci.hi.
}
\description{
Computes location estimates and confidence intervals at specified time points for functional data.
}
\details{
Designed for functional data where each row is a curve measured at p time points.
Computes trimmed mean estimates and confidence intervals, with p-value adjustments for multiple comparisons.
}
\seealso{
\code{\link{funlocpb}}, \code{\link{trimci}}, \code{\link{FBplot}}
}
