% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression-advanced.R
\name{regGmcp}
\alias{regGmcp}
\title{Global Multiple Comparisons for Regression Parameters}
\usage{
regGmcp(
  x,
  y,
  regfun = tsreg,
  SEED = TRUE,
  nboot = 100,
  xout = FALSE,
  AD = FALSE,
  outfun = outpro,
  STAND = TRUE,
  alpha = 0.05,
  pr = TRUE,
  MC = FALSE,
  ISO = TRUE,
  ...
)
}
\arguments{
\item{x}{A list of length J, where each element contains a numeric matrix or
vector of predictor values for one group.}

\item{y}{A list of length J, where each element contains a numeric vector of
response values for one group.}

\item{regfun}{Regression function to use (default: \code{tsreg} for Theil-Sen).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{nboot}{Number of bootstrap samples (default: 100).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{AD}{Logical; affects bootstrap method (default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{STAND}{Logical; if \code{TRUE} (default), standardize when detecting outliers.}

\item{alpha}{Significance level for familywise error rate control (default: 0.05).}

\item{pr}{Logical; if \code{TRUE} (default), print warning about leverage points.}

\item{MC}{Logical. If \code{TRUE}, uses parallel processing via \code{mclapply}
(default: \code{FALSE}).}

\item{ISO}{Logical; if \code{TRUE} (default), compare only slopes (test for parallel
regression lines). If \code{FALSE}, compare all parameters including intercepts.}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
A list with components:
\item{output}{Matrix containing all pairwise comparison results with adjusted
p-values and decision outcomes}
\item{n}{Vector of original sample sizes for each group (after removing missing values)}
\item{n.keep}{Vector of sample sizes after outlier removal (if \code{xout = TRUE})}
}
\description{
Performs all pairwise comparisons of regression parameters (slopes and/or
intercepts) among J independent groups using bootstrap inference and
Hochberg's method for familywise error rate (FWE) control.
}
\details{
This function performs all pairwise global comparisons among J groups:
\itemize{
\item For each pair of groups (j, k) with j < k, tests H0: all corresponding
parameters are equal
\item If \code{ISO = TRUE}: compares only slopes (intercepts ignored), testing for
parallel regression lines
\item If \code{ISO = FALSE}: compares all parameters (intercepts and slopes)
\item Controls FWE using Hochberg's method across all (J^2-J)/2 pairwise tests
}

The data structure requires x and y to be lists of length J, where x[\link{j}] and
y[\link{j}] contain the data for group j.

If \code{xout = TRUE}, leverage points are removed from the predictor space for
each group using the function specified by \code{outfun}.
}
\note{
For individual parameter comparisons (rather than global tests), use
\code{reg1mcp()} instead.
}
\examples{
\dontrun{
# Compare regression lines for 3 groups
set.seed(123)
n <- 50
x1 <- rnorm(n); y1 <- 2 + 3*x1 + rnorm(n)
x2 <- rnorm(n); y2 <- 2 + 2*x2 + rnorm(n)  # Different slope
x3 <- rnorm(n); y3 <- 3 + 3*x3 + rnorm(n)  # Different intercept

# Test for parallel lines (compare slopes only)
result <- regGmcp(list(x1, x2, x3), list(y1, y2, y3), ISO=TRUE)

# Compare all parameters
result2 <- regGmcp(list(x1, x2, x3), list(y1, y2, y3), ISO=FALSE)
}
}
\references{
Wilcox, R.R. (2022). Introduction to Robust Estimation and Hypothesis Testing
(5th ed.). Academic Press.
}
\seealso{
\code{\link{reg1way}}, \code{\link{tsreg}}, \code{\link{regIVcom}}
}
