% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression-advanced.R
\name{mlrreg}
\alias{mlrreg}
\title{Robust Multivariate Linear Regression}
\usage{
mlrreg(
  x,
  y,
  cov.fun = cov.mcd,
  ols.op = TRUE,
  mcd.op = TRUE,
  quantile.used = floor(0.75 * n),
  RES = FALSE,
  ...
)
}
\arguments{
\item{x}{A numeric matrix containing predictor values (independent variables).}

\item{y}{A numeric matrix containing response values (dependent variables).
Must be multivariate (matrix, not vector).}

\item{cov.fun}{Covariance function for robust estimation (default: \code{cov.mcd}).
Must return components \verb{$center} and \verb{$cov}.}

\item{ols.op}{Logical. If \code{TRUE}, uses OLS on cleaned data; if \code{FALSE}, uses
Theil-Sen regression via \code{mopreg()} (default: \code{TRUE}).}

\item{mcd.op}{Logical. If \code{TRUE}, uses MCD with \code{quantile.used}; if \code{FALSE},
uses \code{cov.fun} with its default settings (default: \code{TRUE}).}

\item{quantile.used}{Quantile for MCD estimation (default: floor(0.75*n),
following Rousseeuw et al.).}

\item{RES}{Logical. If \code{TRUE}, returns residuals; if \code{FALSE}, returns \code{NULL}
for residuals (default: \code{FALSE}).}

\item{...}{Additional arguments passed to covariance functions.}
}
\value{
A list with components:
\item{coef}{Matrix of regression coefficients. First row is intercepts,
subsequent rows are slopes for each predictor.}
\item{residuals}{Matrix of residuals if \code{RES = TRUE}, otherwise \code{NULL}.}
}
\description{
Performs robust multivariate linear regression using the method of Rousseeuw
et al. (2004), which combines MCD-based outlier detection with least squares
or Theil-Sen regression on the cleaned data.
}
\details{
This function implements the robust multivariate regression method from
Rousseeuw et al. (2004):
\enumerate{
\item Computes robust location and scatter estimates for (X,Y)
\item Derives initial regression coefficients from robust covariance
\item Computes Mahalanobis distances of residuals
\item Removes outliers (points with distance > chi-square 99th percentile)
\item Re-fits using OLS or Theil-Sen on cleaned data
}

The \code{quantile.used} parameter controls the breakdown point. The default
(0.75*n) gives a 25\% breakdown point as recommended by Rousseeuw et al.
}
\examples{
\dontrun{
# Multivariate regression with 2 predictors and 2 responses
set.seed(123)
x <- matrix(rnorm(100), ncol=2)
y <- matrix(NA, nrow=50, ncol=2)
y[,1] <- 1 + 2*x[,1] - x[,2] + rnorm(50)
y[,2] <- -1 + x[,1] + 3*x[,2] + rnorm(50)
result <- mlrreg(x, y)
result$coef
}
}
\references{
Rousseeuw, P.J., Van Aelst, S., Van Driessen, K., & Agulló, J. (2004).
Robust multivariate regression. Technometrics, 46, 293-305.
}
\seealso{
\code{\link{mulgreg}}, \code{\link{MULMreg}}, \code{\link{mlrregCI}},
\code{\link{mlrregWtest}}, \code{\link{COVreg}}
}
