% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{ecdf}
\alias{ecdf}
\title{Empirical Cumulative Distribution Function}
\usage{
ecdf(x, val)
}
\arguments{
\item{x}{Numeric vector. Data values.}

\item{val}{Numeric. Value at which to evaluate the ECDF.}
}
\value{
Numeric. Proportion of observations ≤ \code{val}.
}
\description{
Computes the empirical cumulative distribution function (ECDF) for data in \code{x}
evaluated at a specified value. Estimates P(X ≤ val).
}
\details{
The empirical CDF is defined as:
\deqn{F_n(val) = \frac{1}{n} \sum_{i=1}^{n} I(X_i \leq val)}

where I() is the indicator function.

This provides a non-parametric estimate of the cumulative probability at \code{val}.

\strong{Note}: Base R provides \code{stats::ecdf()} which creates an ECDF function object.
This function directly computes the ECDF value at a single point.
}
\examples{
x <- rnorm(100)
ecdf(x, 0)  # Proportion <= 0 (should be near 0.5 for standard normal)
ecdf(x, 1.96)  # Proportion <= 1.96
}
\seealso{
\code{\link[stats]{ecdf}}
}
\keyword{distribution}
\keyword{univar}
