% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{PMD.PCD}
\alias{PMD.PCD}
\title{Probability of Making a Decision and Correct Decision}
\usage{
PMD.PCD(
  n = NULL,
  delta = 0.5,
  x = NULL,
  tr = 0.2,
  SIG = NULL,
  alpha = 0.05,
  p.crit = NULL,
  iter = 5000,
  SEED = TRUE
)
}
\arguments{
\item{n}{Numeric vector of sample sizes (one per group), or \code{NULL} if \code{x} is provided.}

\item{delta}{Numeric. Indifference zone parameter - shift in location for simulation (default: 0.5).}

\item{x}{Data in matrix, data frame, or list format, or \code{NULL} if \code{n} is provided.}

\item{tr}{Numeric. Trimming proportion for trimmed mean (default: 0.2).}

\item{SIG}{Numeric vector of population standard deviations, or \code{NULL} to estimate from data.}

\item{alpha}{Numeric. Significance level for decision rule (default: 0.05).}

\item{p.crit}{Numeric. Critical p-value, or \code{NULL} to compute using \code{\link{anc.best.crit}}.}

\item{iter}{Numeric. Number of simulation iterations (default: 5000).}

\item{SEED}{Logical. If \code{TRUE}, set random seed for reproducibility (default: \code{TRUE}).}
}
\value{
List with components:
\itemize{
\item \code{PMD}: Probability of making a decision
\item \code{PMD.ci}: Confidence interval for PMD
\item \code{PCD}: Probability of correct decision (given a decision was made)
\item \code{PCD.ci}: Confidence interval for PCD
}
}
\description{
Determines which group has the largest trimmed mean using an indifference zone approach.
Computes probabilities of making a decision (PMD) and making a correct decision given
that a decision was made (PCD).
}
\details{
Simulates data from J groups, with group 1 shifted by \code{delta} standard deviations.
Uses \code{\link{anc.best}} to test which group has the largest trimmed mean.
PMD is the proportion of simulations where a decision is made.
PCD is the proportion of correct decisions among simulations where a decision was made.

This function uses trimmed means rather than bootstrap (see \code{\link{PMD.PB.PCD}} for bootstrap version).
}
\examples{
\dontrun{
# Simulate with specific sample sizes
result <- PMD.PCD(n = c(20, 20, 20), delta = 0.5, tr = 0.2, iter = 1000)

# Use real data
result2 <- PMD.PCD(x = your_data, delta = 0.5, iter = 1000)
}

}
\seealso{
\code{\link{PMD.PB.PCD}}, \code{\link{anc.best}}, \code{\link{anc.best.crit}},
\code{\link{RM.PMD.PCD}}
}
\keyword{htest}
