% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{ancESband}
\alias{ancESband}
\title{ANCOVA Effect Size with Confidence Band}
\usage{
ancESband(
  x1 = NULL,
  y1 = NULL,
  x2 = NULL,
  y2 = NULL,
  fr1 = 1,
  fr2 = 1,
  method = "WMW",
  pr = TRUE,
  FAST = TRUE,
  alpha = 0.05,
  plotit = TRUE,
  xlab = "X",
  ylab = "ES",
  npts = 25,
  xout = FALSE,
  outfun = out,
  nboot = 500,
  SEED = TRUE,
  nmin = 12,
  SCAT = TRUE,
  pc = ".",
  ...
)
}
\arguments{
\item{x1}{Covariate values for group 1.}

\item{y1}{Response values for group 1.}

\item{x2}{Covariate values for group 2.}

\item{y2}{Response values for group 2.}

\item{fr1}{Span for running interval smoother for group 1 (default: 1).}

\item{fr2}{Span for running interval smoother for group 2 (default: 1).}

\item{method}{Effect size method: 'WMW' (default), 'AKP', or 'QS'.
\itemize{
\item 'WMW': P(Y1<Y2|X) - probability of superiority
\item 'AKP': Robust analog of Cohen's d (trimmed-Winsorized)
\item 'QS': Quantile shift based on medians
}}

\item{pr}{Logical. If \code{TRUE}, prints progress messages (default: \code{TRUE}).}

\item{FAST}{Logical. If TRUE, uses fast computation mode.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{xlab}{Label for x-axis.}

\item{ylab}{Label for y-axis.}

\item{npts}{Number of design points (default: 25).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{nmin}{Minimum sample size at each design point (default: 12).}

\item{SCAT}{Logical. If TRUE, includes scatter plot.}

\item{pc}{Plot character for points (default: '.').}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
List with components:
\item{output}{Matrix with design points, effect sizes, and confidence intervals}
\item{crit.val}{Critical value for simultaneous inference}
}
\description{
Computes effect size for two independent groups across covariate values with
simultaneous confidence bands. Uses running interval smoother with bootstrap
inference and familywise error rate control.
}
\note{
\itemize{
\item Only one covariate is allowed
\item Confidence intervals have simultaneous probability coverage ~0.95 for npts=25
\item FWE controlled using method similar to TAP (Wilcox, 2017)
\item For npts <= 15, uses Bonferroni adjustment instead
\item Bootstrap samples obtained by resampling (x,y) pairs
}
}
\examples{
\dontrun{
x1 <- rnorm(50)
y1 <- x1 + rnorm(50)
x2 <- rnorm(50)
y2 <- x2 + 0.5 + rnorm(50)
ancESband(x1, y1, x2, y2, method='WMW', nboot=500)
}
}
\references{
Wilcox, R.R. (2017). Introduction to Robust Estimation and Hypothesis Testing (4th ed.).
}
