% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{ols.ridge}
\alias{ols.ridge}
\title{Ridge Regression with OLS}
\usage{
ols.ridge(x, y, k = NULL, xout = FALSE, outfun = outpro, MSF = TRUE)
}
\arguments{
\item{x}{Matrix (n × p). Design matrix with p ≥ 2 predictors.}

\item{y}{Numeric vector of length n. Response variable.}

\item{k}{Numeric or NULL. Ridge shrinkage parameter (penalty). If NULL,
estimated automatically (default: NULL).}

\item{xout}{Logical. If TRUE, removes outliers using \code{outfun} before fitting (default: FALSE).}

\item{outfun}{Function. Outlier detection function (default: \code{\link{outpro}}).}

\item{MSF}{Logical. Method for automatic k estimation:
\itemize{
\item TRUE: Use modified scale factor \eqn{k = p^{(1+1/p)} \sigma} (default)
\item FALSE: Use \code{\link{ridge.est.k}} for optimal k
}}
}
\value{
List with component:
\describe{
\item{coef}{Numeric vector of length p+1. Ridge regression coefficients
(intercept and p slopes)}
}
}
\description{
Fits ridge regression using automatic or user-specified shrinkage parameter.
Optionally removes outliers before fitting.
}
\details{
\strong{Ridge regression} shrinks OLS coefficients toward zero to reduce variance:

\deqn{\hat{\beta}_{ridge} = (X'X + kI)^{-1}X'y}

\strong{Automatic k selection} (when \code{k=NULL}):
\itemize{
\item If \code{MSF=TRUE}: \eqn{k = p^{(1+1/p)} \hat{\sigma}}
\item If \code{MSF=FALSE}: Uses cross-validation via \code{\link{ridge.est.k}}
}

\strong{Benefits}:
\itemize{
\item Reduces multicollinearity problems
\item Stabilizes coefficient estimates when predictors are correlated
\item Can improve prediction accuracy via bias-variance tradeoff
}

\strong{Note}: Requires at least 2 predictors (p ≥ 2). Uses \code{MASS::lm.ridge}
for computation.
}
\seealso{
\code{\link{ols}}, \code{\link{ridge.est.k}}, \code{MASS::lm.ridge}
}
