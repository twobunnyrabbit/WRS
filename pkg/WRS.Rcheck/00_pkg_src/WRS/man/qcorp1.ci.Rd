% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{qcorp1.ci}
\alias{qcorp1.ci}
\title{Quantile Regression Correlation CI (Ratio Method)}
\usage{
qcorp1.ci(
  x,
  y,
  q = 0.5,
  alpha = 0.05,
  nboot = 599,
  SEED = TRUE,
  xout = TRUE,
  method = "PRO",
  regfun = Qreg
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{q}{Quantile to use (default: 0.5 for median).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{nboot}{Number of bootstrap samples (default: 599).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{xout}{Logical. Remove outliers before analysis (default: \code{TRUE}).}

\item{method}{Outlier detection method (default: 'PRO').}

\item{regfun}{Regression function for outlier detection (default: \code{Qreg}).}
}
\value{
List with components:
\item{Est.}{Estimated quantile correlation.}
\item{ci}{Bootstrap confidence interval.}
\item{p.value}{Two-sided p-value for testing zero correlation.}
}
\description{
Bootstrap confidence interval for quantile regression correlation using the
ratio of loss functions method.
}
\details{
Computes quantile regression correlation based on ratio of loss functions
(full model vs null model). See Wilcox (2022, section 11.9).

For n <= 40, outlier removal is skipped to avoid computational issues.
Currently supports single independent variable only.
}
\examples{
\dontrun{
set.seed(123)
x <- rnorm(50)
y <- 2 * x + rnorm(50)
qcorp1.ci(x, y, q = 0.5, nboot = 500)
}
}
\seealso{
\code{\link{qcorp1}}, \code{\link{qcor.R}}, \code{\link{qcor.ci}}
}
