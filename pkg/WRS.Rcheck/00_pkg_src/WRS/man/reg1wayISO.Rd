% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression.R
\name{reg1wayISO}
\alias{reg1wayISO}
\title{Test Equality of Slopes Across Independent Groups (Ignoring Intercepts)}
\usage{
reg1wayISO(
  x,
  y,
  regfun = tsreg,
  nboot = 100,
  SEED = TRUE,
  xout = FALSE,
  outfun = outpro,
  STAND = TRUE,
  alpha = 0.05,
  pr = TRUE,
  ...
)
}
\arguments{
\item{x}{List of predictor matrices, one per group. \code{x[[j]]} contains the
n_j x p predictor matrix for group j.}

\item{y}{List of response vectors, one per group. \code{y[[j]]} contains the
response values for group j.}

\item{regfun}{Regression function to use (default: \code{tsreg} for Theil-Sen).
Must return coefficients in \code{$coef} with intercept as first element.}

\item{nboot}{Number of bootstrap samples per group (default: 100).}

\item{SEED}{Logical; if TRUE, sets random seed for reproducibility (default: TRUE).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{STAND}{Logical. If \code{TRUE}, standardizes data before analysis
(default: \code{TRUE}).}

\item{alpha}{Significance level for the test (default: 0.05).}

\item{pr}{Logical; if TRUE, prints progress messages and warnings (default: TRUE).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
A list with components:
\describe{
\item{n}{Original sample sizes for each group before outlier removal}
\item{n.keep}{Sample sizes after outlier removal (if \code{xout=TRUE})}
\item{test.stat}{The test statistic (chi-square type)}
\item{crit.value}{Critical value at significance level \code{alpha}}
\item{adjusted.crit}{Adjusted critical value (currently NULL, not computed)}
\item{p.value}{P-value for the test}
\item{adjusted.p.value}{Adjusted p-value (currently NULL, not computed)}
\item{est}{Data frame of estimated slopes for each group (intercepts set to 0)}
}
}
\description{
Tests the hypothesis that slope parameters are equal across two or more
independent groups, ignoring any differences in intercepts. Uses a
bootstrap-based Johansen-type MANOVA approach focusing only on slopes.
}
\details{
This function is similar to \code{\link{reg1way}} but tests only whether
the \strong{slopes} are equal across groups, allowing intercepts to differ freely.
This is appropriate when:
\itemize{
\item Different groups may have different baseline levels
\item The research question concerns whether the relationship between predictors
and response is the same across groups
\item You want to test parallelism of regression lines
}

\strong{Test procedure}:
\enumerate{
\item For each group, estimate regression parameters via \code{regfun}
\item Use bootstrap to estimate covariance matrix of slope estimates
\item Compute weighted average of slopes across groups
\item Calculate chi-square type statistic measuring deviation from common slopes
\item Compare to chi-square distribution with p*(J-1) degrees of freedom
}

The test statistic is based on a Johansen-type MANOVA approach that accounts
for different sample sizes and unequal variances across groups.

\strong{Note}: The adjusted critical value and adjusted p-value (Johansen correction)
are currently disabled in the code as they appear not to be needed for this
specific test.
}
\examples{
\dontrun{
# Three groups with same slopes but different intercepts
set.seed(123)
x1 <- matrix(rnorm(100), ncol=1)
x2 <- matrix(rnorm(100), ncol=1)
x3 <- matrix(rnorm(100), ncol=1)
y1 <- 1 + 2*x1 + rnorm(100, sd=0.5)  # intercept = 1
y2 <- 3 + 2*x2 + rnorm(100, sd=0.5)  # intercept = 3
y3 <- 5 + 2*x3 + rnorm(100, sd=0.5)  # intercept = 5

x <- list(x1, x2, x3)
y <- list(as.vector(y1), as.vector(y2), as.vector(y3))

# Should not reject (slopes are equal)
reg1wayISO(x, y, nboot=200)

# Three groups with different slopes
y1b <- 1 + 2*x1 + rnorm(100, sd=0.5)
y2b <- 1 + 3*x2 + rnorm(100, sd=0.5)
y3b <- 1 + 4*x3 + rnorm(100, sd=0.5)
yb <- list(as.vector(y1b), as.vector(y2b), as.vector(y3b))

# Should reject (slopes differ)
reg1wayISO(x, yb, nboot=200)
}

}
\references{
Wilcox, R.R. (2022). \emph{Introduction to Robust Estimation and Hypothesis Testing}
(5th ed.). Academic Press.
}
\seealso{
\code{\link{reg1way}} for testing equality of all parameters (intercepts and slopes),
\code{\link{reg1wayISOMC}} for parallel processing version,
\code{\link{reg1mcp}} for all pairwise comparisons
}
