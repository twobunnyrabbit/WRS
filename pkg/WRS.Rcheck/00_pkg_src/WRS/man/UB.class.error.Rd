% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{UB.class.error}
\alias{UB.class.error}
\title{Cross-Validation Error Rates for Classification Methods}
\usage{
UB.class.error(
  train = NULL,
  g = NULL,
  x1 = NULL,
  x2 = NULL,
  method = c("KNN", "SVM", "DIS", "DEP", "PRO", "PROBAG"),
  alpha = 0.05,
  pro.p = 0.8,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{train}{Training data matrix, or can specify groups separately via \code{x1}/\code{x2}.}

\item{g}{Group membership vector for training data.}

\item{x1}{Training data for group 1 (alternative to \code{train}/\code{g}).}

\item{x2}{Training data for group 2 (alternative to \code{train}/\code{g}).}

\item{method}{Vector of classification methods to evaluate. Options:
\code{'KNN'}, \code{'SVM'}, \code{'DIS'}, \code{'DEP'}, \code{'PRO'}, \code{'PROBAG'}.
To include neural networks, add \code{'NNbag'} (not included by default due to
high computation time).}

\item{alpha}{Significance level (currently not used for CIs; default: 0.05).}

\item{pro.p}{Proportion of each group used for training; remainder used for testing
(default: 0.8).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducibility.}

\item{...}{Additional arguments passed to classification functions.}
}
\value{
Matrix with one row per method and columns: \code{phat.FN}, \code{phat.FP},
\code{phat.TE}.
}
\description{
Estimates classification error rates using cross-validation for various
classification methods. Reports overall error rate, false positive rate,
and false negative rate.
}
\details{
Splits data into training and test sets, fits classifier on training data,
evaluates on test data. Computes:
\itemize{
\item \strong{FN} (False Negative): Incorrectly assign to group 2 (e.g., predict
no fracture when fracture occurs)
\item \strong{FP} (False Positive): Incorrectly assign to group 1 (e.g., predict
fracture when none occurs)
\item \strong{TE} (Total Error): Overall error rate
}

\strong{Note}: Confidence intervals are not included because the standard approach
performs poorly for these error rate estimates.
}
\seealso{
\code{\link{UB.class}}, \code{\link{UBROC}}
}
\keyword{classification}
