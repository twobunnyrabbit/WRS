% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{thd}
\alias{thd}
\title{Trimmed Harrell-Davis Quantile Estimator}
\usage{
thd(x, q = 0.5, width = 1/sqrt(length(x)))
}
\arguments{
\item{x}{Numeric vector. Data for which to estimate quantiles}

\item{q}{Numeric vector. Quantiles to estimate (default: 0.5 for median)}

\item{width}{Numeric. Width of the HDI window (default: 1/sqrt(n))}
}
\value{
Numeric vector of estimated quantiles (same length as \code{q})
}
\description{
Computes quantile estimates using a trimmed version of the Harrell-Davis
estimator based on highest density intervals (HDI).
}
\details{
This function implements a trimmed version of the Harrell-Davis quantile
estimator that uses the highest density interval (HDI) of the beta distribution
to determine which order statistics receive weight.

The algorithm:
\enumerate{
\item For quantile p, finds the HDI of Beta(a, b) where a = (n+1)p, b = (n+1)(1-p)
\item Restricts weights to order statistics within this HDI
\item Computes weighted average of these order statistics
}

This trimming can improve robustness compared to the standard Harrell-Davis
estimator while maintaining good efficiency.
}
\examples{
x <- rnorm(100)
# Estimate median
thd(x, q = 0.5)

# Estimate quartiles
thd(x, q = c(0.25, 0.50, 0.75))

}
\seealso{
\code{\link{hd}}, \code{\link{hdseq}}, \code{\link{hdpbci}}
}
\keyword{robust}
\keyword{univar}
