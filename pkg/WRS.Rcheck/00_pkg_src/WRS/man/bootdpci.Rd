% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{bootdpci}
\alias{bootdpci}
\title{Percentile Bootstrap CI for Dependent Groups}
\usage{
bootdpci(
  x,
  y,
  est = onestep,
  nboot = NA,
  alpha = 0.05,
  plotit = FALSE,
  dif = TRUE,
  BA = FALSE,
  SR = TRUE,
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{est}{Estimator function to use for computing location or other
statistics. Common choices include \code{mean}, \code{median}, \code{tmean} (trimmed mean),
\code{mom} (modified one-step M-estimator), \code{mest} (M-estimator).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{dif}{Logical. If \code{TRUE}, analyzes the difference between groups.
If \code{FALSE}, analyzes individual groups (default: \code{TRUE}).}

\item{BA}{Logical. If \code{TRUE}, uses Bland-Altman plot approach (default: \code{FALSE}).}

\item{SR}{Logical. If \code{TRUE}, uses the Studentized range when appropriate
(default: \code{TRUE}).}

\item{...}{Additional arguments passed to the estimator function \code{est}.}
}
\value{
A list containing:
\item{output}{Matrix with confidence intervals and test results}
}
\description{
Computes a percentile bootstrap confidence interval for the difference
between measures of location or scale when comparing two dependent (paired)
groups.
}
\details{
By default, uses a one-step M-estimator with Huber's psi function. For
a fully iterated M-estimator, use \code{est=mest}.

The function internally calls \code{rmmcppb} for the bootstrap computations.
The \code{SR} parameter is automatically set to \code{FALSE} for estimators other
than \code{onestep} and \code{mom}.
}
\examples{
# Paired data
x <- c(120, 135, 140, 125, 130, 142, 128, 133)
y <- c(115, 130, 138, 120, 125, 140, 125, 130)
bootdpci(x, y, nboot=500)
}
\seealso{
\code{\link{rmmcppb}}, \code{\link{onestep}}, \code{\link{mom}}
}
