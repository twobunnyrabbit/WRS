% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00-utils-core.R
\name{bmp}
\alias{bmp}
\title{Brunner-Munzel Test}
\usage{
bmp(
  x,
  y,
  alpha = 0.05,
  crit = NA,
  plotit = FALSE,
  pop = 0,
  fr = 0.8,
  xlab = "",
  ylab = ""
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{crit}{Optional critical value (default: NA, uses t-distribution).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{pop}{Deprecated parameter (default: 0).}

\item{fr}{Fraction parameter (default: 0.8).}

\item{xlab, ylab}{Labels for optional plot.}
}
\value{
A list with components:
\item{test}{Test statistic}
\item{p.value}{Two-sided p-value}
\item{phat}{P(X > Y) estimate}
\item{ci}{Confidence interval for phat}
\item{dhat}{1 - 2*phat (effect size)}
\item{df}{Degrees of freedom}
\item{n1, n2}{Sample sizes}
\item{probs}{P(X<Y), P(X=Y), P(X>Y)}
}
\description{
Performs the Brunner-Munzel (2000) heteroscedastic analog of the
Wilcoxon-Mann-Whitney test for comparing two independent groups.
}
\details{
Unlike the WMW test, the Brunner-Munzel test does not assume equal
variances and provides better Type I error control when variances differ.
The test is based on ranks and computes P(X > Y).
}
\examples{
x <- rnorm(20, mean = 0, sd = 1)
y <- rnorm(25, mean = 0.5, sd = 2)
bmp(x, y)

}
\references{
Brunner, E., & Munzel, U. (2000). The nonparametric Behrens-Fisher problem.
Journal of Statistical Planning and Inference, 92, 1-10.
}
