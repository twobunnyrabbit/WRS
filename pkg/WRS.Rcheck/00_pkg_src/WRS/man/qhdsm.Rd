% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression-advanced.R
\name{qhdsm}
\alias{qhdsm}
\title{Quantile Regression Smoother Using Harrell-Davis Estimator}
\usage{
qhdsm(
  x,
  y,
  qval = 0.5,
  q = NULL,
  pr = FALSE,
  xout = FALSE,
  outfun = outpro,
  plotit = TRUE,
  xlab = "X",
  ylab = "Y",
  zlab = "Z",
  pyhat = FALSE,
  fr = NULL,
  LP = FALSE,
  theta = 50,
  phi = 25,
  ticktype = "simple",
  nmin = 0,
  scale = TRUE,
  pr.qhd = TRUE,
  pch = ".",
  ...
)
}
\arguments{
\item{x}{A numeric matrix or vector containing predictor values. Can be
multivariate (but plotting works best with 1-2 predictors).}

\item{y}{A numeric vector containing the response variable.}

\item{qval}{A numeric vector specifying the quantiles to estimate (default: 0.5
for median regression). Can specify multiple quantiles, e.g., c(0.25, 0.5, 0.75).}

\item{q}{Alias for \code{qval} (for backward compatibility).}

\item{pr}{Logical. If \code{TRUE}, prints progress messages.}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{xlab, ylab, zlab}{Axis labels for plots.}

\item{pyhat}{Logical. If \code{TRUE}, returns predicted Y values; if \code{FALSE},
returns "DONE" message (default: \code{FALSE}).}

\item{fr}{Span parameter for running interval smoother (default: 0.8 for p=1,
1 for p>1). Controls the fraction of data used in local smoothing.}

\item{LP}{Logical. If \code{TRUE}, applies LOESS smoothing after running interval
smoother (default: \code{FALSE}).}

\item{theta, phi}{Viewing angles for 3D plot when p=2 (defaults: 50, 25).}

\item{ticktype}{Type of tick marks for 3D plot (default: "simple").}

\item{nmin}{Minimum number of points required for smoothing (default: 0).}

\item{scale}{Logical. If \code{TRUE}, standardizes predictors when p > 1
(default: \code{TRUE}).}

\item{pr.qhd}{Logical. If \code{TRUE}, prints warning about scaling with dependent
predictors (default: \code{TRUE}).}

\item{pch}{Plotting character for scatter plot (default: ".").}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
If \code{pyhat = TRUE}, returns a matrix of predicted quantile values.
If \code{pyhat = FALSE}, returns "DONE" message. For p=1, plots the quantile
regression line(s). For p=2, creates a 3D perspective plot.
}
\description{
Computes and plots quantile regression lines using the Harrell-Davis
estimator combined with a running interval smoother and LOESS. This provides
a nonparametric estimate of conditional quantiles.
}
\details{
This function estimates conditional quantiles using a combination of methods:
\enumerate{
\item For each x value, computes the quantile of nearby y values using
Harrell-Davis estimator
\item Applies running interval smoother to reduce variability
\item Optionally applies LOESS smoothing (if LP=TRUE)
}

For a single predictor (p=1), multiple quantile lines can be plotted
simultaneously. For two predictors (p=2), only one quantile can be plotted
at a time using a 3D perspective plot.
}
\examples{
\dontrun{
# Median regression line
set.seed(123)
x <- rnorm(100)
y <- 2*x + rnorm(100)
qhdsm(x, y, qval = 0.5)

# Multiple quantile lines (quartiles)
qhdsm(x, y, qval = c(0.25, 0.5, 0.75))
}
}
\references{
Wilcox, R.R. (2022). Introduction to Robust Estimation and Hypothesis Testing
(5th ed.). Academic Press.
}
\seealso{
\code{\link{qhdsm2g}}, \code{\link{qhdplotsm}}, \code{\link{hd}},
\code{\link{Qreg}}, \code{\link{rplot}}
}
