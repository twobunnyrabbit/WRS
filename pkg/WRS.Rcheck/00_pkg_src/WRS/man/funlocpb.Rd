% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/location.R
\name{funlocpb}
\alias{funlocpb}
\title{Functional Data Location with Percentile Bootstrap}
\usage{
funlocpb(
  x,
  est = tmean,
  nboot = 2000,
  SEED = TRUE,
  pts = NULL,
  npts = 25,
  plotit = TRUE,
  alpha = 0.05,
  nv = rep(0, ncol(x)),
  xlab = "T",
  ylab = "Est.",
  FBP = TRUE,
  method = "hochberg",
  COLOR = TRUE,
  ...
)
}
\arguments{
\item{x}{n-by-p matrix (rows = curves, columns = time points).}

\item{est}{Location estimator function (default: \code{tmean}).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{pts}{Specific time points for estimation (default: NULL uses evenly spaced points).}

\item{npts}{Number of evenly spaced points to use if pts = NULL (default: 25).}

\item{plotit}{Logical. If \code{TRUE}, create plot.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{nv}{Null values for hypothesis testing (default: zero vector).}

\item{xlab}{X-axis label (default: 'T').}

\item{ylab}{Y-axis label (default: 'Est.').}

\item{FBP}{Logical. If \code{TRUE}, create functional boxplot; if \code{FALSE}, plot estimates with CIs.}

\item{method}{Multiple comparison adjustment method (default: 'hochberg').}

\item{COLOR}{Logical. If \code{TRUE}, use color in functional boxplot.}

\item{...}{Additional arguments passed to \code{est}.}
}
\value{
Matrix with columns: pts, est., p.value, adjust.p.value, ci.low, ci.hi.
}
\description{
Computes location estimates and bootstrap confidence intervals at specified time points for functional data.
}
\details{
Similar to \code{\link{funloc}} but uses percentile bootstrap instead of Student's t.
Allows flexible choice of location estimator.
}
\seealso{
\code{\link{funloc}}, \code{\link{onesampb}}
}
