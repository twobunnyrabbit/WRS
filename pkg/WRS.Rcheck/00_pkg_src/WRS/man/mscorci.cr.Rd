% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{mscorci.cr}
\alias{mscorci.cr}
\title{Critical Values for Multiple Skipped Correlation CI}
\usage{
mscorci.cr(
  n,
  p,
  iter = 500,
  corfun = pcor,
  alpha = c(0.05, 0.025, 0.01),
  TV = FALSE,
  SEED = TRUE
)
}
\arguments{
\item{n}{Sample size.}

\item{p}{Number of variables.}

\item{iter}{Number of simulation iterations (default: 500).}

\item{corfun}{Correlation function to use. Common options: \code{pbcor} (percentage
bend), \code{pcor} (Pearson), \code{wincor} (winsorized), \code{spear} (Spearman).}

\item{alpha}{Vector of alpha levels (default: c(0.05, 0.025, 0.01)).}

\item{TV}{Logical. If \code{TRUE}, returns test values; if \code{FALSE}, returns only
critical p-values (default: \code{FALSE}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}
}
\value{
List with components:
\item{crit.p.values}{Critical p-values for each alpha level.}
\item{tval}{Simulated test values (if TV = TRUE).}
}
\description{
Determines critical p-values for \code{mscorci()} via simulation.
}
\details{
This function simulates the distribution of the minimum p-value when all
null hypotheses are true, then determines critical p-values for controlling
the familywise error rate in \code{mscorci()}.

Critical p-values are functions of n and p. Once computed, they can be
supplied to \code{mscorci()} via the \code{crit.pv} argument to save computation time.
}
\seealso{
\code{\link{mscorci}}, \code{\link{mscorpb}}
}
\keyword{internal}
