% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{lplotPV}
\alias{lplotPV}
\title{P-Value for LOESS Strength of Association}
\usage{
lplotPV(
  x,
  y,
  span = 0.75,
  xout = FALSE,
  pr = TRUE,
  outfun = out,
  nboot = 1000,
  SEED = TRUE,
  plotit = TRUE,
  pyhat = FALSE,
  expand = 0.5,
  low.span = 2/3,
  varfun = pbvar,
  cor.op = FALSE,
  cor.fun = pbcor,
  scale = FALSE,
  xlab = "X",
  ylab = "Y",
  zlab = "",
  theta = 50,
  phi = 25,
  family = "gaussian",
  duplicate = "error",
  pc = "*",
  ticktype = "simple",
  ...
)
}
\arguments{
\item{x}{Predictor variable(s). Can be a vector (single predictor) or matrix (multiple predictors).}

\item{y}{Response variable vector.}

\item{span}{Span parameter for LOESS with 2+ predictors (default: 0.75).}

\item{xout}{Logical; if TRUE, removes outliers based on predictor variables (default: FALSE).}

\item{pr}{Logical; if TRUE, prints warnings (default: TRUE).}

\item{outfun}{Outlier detection function (default: \code{out}).}

\item{nboot}{Number of bootstrap permutation samples (default: 1000).}

\item{SEED}{Logical; if TRUE, sets random seed for reproducibility (default: TRUE).}

\item{plotit}{Logical; if TRUE, creates the LOESS plot (default: TRUE).}

\item{pyhat}{Logical; if TRUE, returns fitted values (default: FALSE).}

\item{expand}{Expansion factor for 3D plots (default: 0.5).}

\item{low.span}{Span for LOWESS with 1 predictor (default: 2/3).}

\item{varfun}{Variance function for explanatory power (default: \code{pbvar}).}

\item{cor.op}{Logical; if TRUE, uses correlation for explanatory power (default: FALSE).}

\item{cor.fun}{Correlation function when cor.op=TRUE (default: \code{pbcor}).}

\item{scale}{Logical; if TRUE, scales 3D plots (default: FALSE).}

\item{xlab}{Label for X-axis (default: "X").}

\item{ylab}{Label for Y-axis (default: "Y").}

\item{zlab}{Label for Z-axis in 3D plots (default: "").}

\item{theta}{Rotation angle for 3D plots (default: 50).}

\item{phi}{Colatitude angle for 3D plots (default: 25).}

\item{family}{LOESS family: "gaussian" or "symmetric" (default: "gaussian").}

\item{duplicate}{How to handle duplicate x values: "error" or "strip" (default: "error").}

\item{pc}{Plotting character for scatter points (default: "*").}

\item{ticktype}{Type of tick marks for 3D plots (default: "simple").}

\item{...}{Additional arguments passed to outlier detection function.}
}
\value{
A list with components:
\item{p.value}{Bootstrap permutation p-value for testing independence.}
\item{Strength.Assoc}{Observed strength of association.}
\item{Explanatory.power}{Observed explanatory power.}
\item{yhat.values}{Fitted values (if pyhat=TRUE).}
}
\description{
Tests for dependence between x and y using the strength of association measure
from LOESS regression. Computes a p-value via bootstrap permutation test.
}
\details{
This function tests the null hypothesis of \strong{independence} between x and y.

The test statistic is the \strong{strength of association} computed via LOESS:
\deqn{Strength = sign \times \sqrt{ExplanatoryPower}}

The p-value is computed by:
\enumerate{
\item Computing the observed strength of association
\item Generating \code{nboot} permutation samples where x and y are independent
(achieved by bootstrapping x and y separately)
\item Computing strength for each permutation sample
\item p-value = proportion of permutation strengths >= observed strength
}

A \strong{small p-value} indicates significant evidence of dependence.

The function uses \code{\link{lplot}} internally for fitting and visualization.
}
\examples{
# Test for dependence
x <- rnorm(100)
y <- 0.5 * x + rnorm(100, sd = 0.5)
result <- lplotPV(x, y, nboot = 500)
print(result$p.value)  # Should be small (significant dependence)
}
\seealso{
\code{\link{lplot}}, \code{\link{lplotv2}}, \code{\link{lplotCI}}
}
