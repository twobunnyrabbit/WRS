% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{ancovam}
\alias{ancovam}
\title{Median-Based ANCOVA for Two Independent Groups}
\usage{
ancovam(
  x1,
  y1,
  x2,
  y2,
  fr1 = 1,
  fr2 = 1,
  alpha = 0.05,
  plotit = TRUE,
  pts = NA,
  sm = FALSE,
  pr = TRUE
)
}
\arguments{
\item{x1}{Covariate values for group 1}

\item{y1}{Response values for group 1}

\item{x2}{Covariate values for group 2}

\item{y2}{Response values for group 2}

\item{fr1}{Span parameter for group 1 smoother (default: 1)}

\item{fr2}{Span parameter for group 2 smoother (default: 1)}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{plotit}{Logical; if TRUE, plot running medians (default: TRUE)}

\item{pts}{Optional vector of design points. If NA, uses 5 equally-spaced points
where both groups have at least 12 observations. Maximum 28 points allowed.}

\item{sm}{Logical; if TRUE, use bootstrap bagging for smoother curves (default: FALSE)}

\item{pr}{Logical; if TRUE, print warning about p-values (default: TRUE)}
}
\value{
List with components:
\item{output}{Matrix with columns: X (design point), n1, n2, DIF (median
difference), TEST (test statistic), se (standard error), ci.low, ci.hi
(adjusted confidence limits), p.value}
\item{crit}{Critical value from Studentized Maximum Modulus distribution}
}
\description{
Compare two independent groups using median-based ANCOVA with running interval
smoothers. No parametric assumptions are made about regression line forms.
Confidence intervals are adjusted to control familywise error rate across
design points (via Studentized Maximum Modulus distribution).
}
\note{
\itemize{
\item Confidence intervals control familywise Type I error rate
\item P-values are NOT adjusted for multiple comparisons
\item Warns if fewer than 6 observations at any design point
}
}
\examples{
# Generate data with different medians
set.seed(234)
x1 <- runif(50, 0, 10)
y1 <- 5 + 0.3*x1 + rt(50, df=3)  # Heavy tails
x2 <- runif(50, 0, 10)
y2 <- 7 + 0.3*x2 + rt(50, df=3)  # Different intercept

# Compare at default 5 points
result <- ancovam(x1, y1, x2, y2, plotit = FALSE)
print(result$output)

# Custom design points
result2 <- ancovam(x1, y1, x2, y2, pts = c(2, 5, 8), plotit = FALSE)

}
