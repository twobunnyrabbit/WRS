% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{pb2trmcp}
\alias{pb2trmcp}
\title{Two-Way ANOVA Multiple Comparisons with Trimmed Means (Bootstrap)}
\usage{
pb2trmcp(
  J,
  K,
  x,
  grp = c(1:p),
  p = J * K,
  tr = 0.2,
  nboot = NA,
  alpha = 0.05,
  SEED = TRUE,
  pr = TRUE,
  bhop = FALSE
)
}
\arguments{
\item{J}{Number of levels for Factor A.}

\item{K}{Number of levels for Factor B.}

\item{x}{Data in list mode or matrix format. If list, \code{x[[1]]} is level (1,1),
\code{x[[2]]} is level (1,2), etc. Matrix format will be converted to list.}

\item{grp}{Subset of groups to analyze (default: all groups \code{1:p}).}

\item{p}{Total number of groups J\emph{K (default: J}K).}

\item{tr}{Trimming proportion (default: 0.2 for 20\% trimming).}

\item{nboot}{Number of bootstrap samples (default: NA, uses function default).}

\item{alpha}{Significance level (default: 0.05).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducibility (default: \code{TRUE}).}

\item{pr}{Logical. If \code{TRUE}, prints progress messages (default: \code{TRUE}).}

\item{bhop}{Logical. If \code{TRUE}, uses Benjamini-Hochberg procedure (default: \code{FALSE}).}
}
\value{
A list with components:
\itemize{
\item \code{Factor.A}: Multiple comparison results for Factor A main effects
\item \code{Factor.B}: Multiple comparison results for Factor B main effects
\item \code{Factor.AB}: Multiple comparison results for interaction effects
\item \code{bhop}: Echo of the bhop parameter
\item \code{SEED}: Echo of SEED setting
}
}
\description{
Performs multiple comparisons for a J-by-K factorial design using
trimmed means and bootstrap methods. Tests main effects and interactions.
}
\details{
This function performs pairwise comparisons for all effects in a two-way design
using trimmed means and bootstrap-t methods. The data organization follows the
convention where groups are ordered first by Factor A, then by Factor B.
}
\examples{
\dontrun{
# Create 2x3 factorial design data
set.seed(123)
data <- lapply(1:6, function(i) rnorm(20, mean = i))

# Perform two-way ANOVA with multiple comparisons
result <- pb2trmcp(J = 2, K = 3, x = data, nboot = 500)

# View Factor A comparisons
result$Factor.A
}
}
\seealso{
\code{\link{pbtrmcp}}, \code{\link{con2way}}, \code{\link{t2way}}
}
