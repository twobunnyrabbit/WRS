% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{QSmcp.ci}
\alias{QSmcp.ci}
\title{Quantile Shift Pairwise Comparisons with Confidence Intervals}
\usage{
QSmcp.ci(x, locfun = median, alpha = 0.05, SEED = TRUE, nboot = 500, CI = TRUE)
}
\arguments{
\item{x}{Data in matrix/data.frame format (columns = groups) or list format
(each element = one group).}

\item{locfun}{Location estimator function (default: \code{\link{median}}).
Used to define the reference point for quantile shift calculation.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{nboot}{Number of bootstrap samples for confidence intervals (default: 500).}

\item{CI}{Logical. If \code{TRUE}, compute confidence intervals for effect sizes
(default: \code{TRUE}).}
}
\value{
A matrix with columns:
\describe{
\item{\code{Group}}{First group index}
\item{\code{Group}}{Second group index}
\item{\code{Effect.Size}}{Estimated quantile shift}
\item{\code{low.ci}}{Lower CI bound (if \code{CI=TRUE})}
\item{\code{up.ci}}{Upper CI bound (if \code{CI=TRUE})}
}
}
\description{
Estimates quantile shift effect sizes for all pairwise comparisons in a
one-way independent groups design, with optional confidence intervals.
}
\details{
For each pair of groups (j, k) where j < k:
\enumerate{
\item If \code{CI=FALSE}: Computes quantile shift effect size using \code{\link{shiftes}}
\item If \code{CI=TRUE}: Computes effect size and bootstrap CI using \code{\link{shiftPBci}}
}

The quantile shift measures the difference in cumulative probabilities when
using the location estimator as a reference point.
}
\seealso{
\code{\link{shiftes}}, \code{\link{shiftPBci}}, \code{\link{qshiftpb}},
\code{\link{Qmcp}}
}
\keyword{htest}
\keyword{robust}
