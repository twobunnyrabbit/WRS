% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{rslope}
\alias{rslope}
\title{Estimate Running Slope at Specified Points}
\usage{
rslope(
  x,
  y,
  fr = 1,
  est = tmean,
  nmin = 10,
  pts = x,
  plotit = FALSE,
  xlab = "X",
  ylab = "Y",
  ...
)
}
\arguments{
\item{x}{Numeric vector of predictor values.}

\item{y}{Numeric vector of response values (same length as \code{x}).}

\item{fr}{Numeric. Span parameter controlling window width (default: 1).
Larger values increase smoothing.}

\item{est}{Function for location estimation. Default is \code{\link{tmean}}.
Also try \code{median}, \code{onestep}, etc.}

\item{nmin}{Integer. Minimum number of observations required in each window
(left and right) to compute slope (default: 10).}

\item{pts}{Numeric vector of points at which to estimate slope. Default is
\code{x} (estimate at all predictor values).}

\item{plotit}{Logical. If \code{TRUE}, plot estimated slopes vs x (default: \code{FALSE}).}

\item{xlab}{Character. Label for x-axis when \code{plotit=TRUE}.}

\item{ylab}{Character. Label for y-axis when \code{plotit=TRUE}.}

\item{...}{Additional arguments passed to \code{est}.}
}
\value{
Numeric vector of estimated slopes at each point in \code{pts}.
Returns \code{NA} for points with insufficient observations.
}
\description{
Estimates local slope of a regression function at specified points using
a running window approach with robust location estimators.
}
\details{
For each point in \code{pts}, the function:
\enumerate{
\item Identifies observations in a left window and right window
\item Computes robust location estimate for x and y in each window
\item Estimates slope as (y_right - y_left) / (x_right - x_left)
}

The span parameter \code{fr} controls window width via \code{\link{nearl}}
and \code{\link{nearr}} functions.

Missing values are automatically removed before estimation.
}
\seealso{
\code{\link{rslopesm}}, \code{\link{runhat}}, \code{\link{nearl}},
\code{\link{nearr}}
}
\keyword{regression}
\keyword{robust}
\keyword{smooth}
