% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression-advanced.R
\name{regmediate}
\alias{regmediate}
\title{Test for Mediation Effect in Regression}
\usage{
regmediate(
  x,
  y,
  regfun = tsreg,
  nboot = 400,
  alpha = 0.05,
  xout = FALSE,
  outfun = out,
  MC = FALSE,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{x}{An n-by-2 numeric matrix where column 1 is the predictor and
column 2 is the mediator variable.}

\item{y}{A numeric vector containing the outcome variable.}

\item{regfun}{Regression function to use (default: \code{tsreg} for Theil-Sen).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{MC}{Logical. If \code{TRUE}, uses parallel processing via \code{mclapply}
(default: \code{FALSE}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
A list with components:
\item{conf.interval}{Bootstrap confidence interval for b11 - b13 (the
mediation effect).}
\item{p.value}{P-value for testing H0: b11 - b13 = 0.}
}
\description{
Tests for mediation by comparing the direct effect (predictor → outcome)
with the conditional effect (predictor → outcome | mediator) using bootstrap
confidence intervals. Computes CI for the difference in slopes.
}
\details{
Mediation analysis examines whether the effect of a predictor X on outcome Y
operates through a mediator M. This function compares two regression models:
\itemize{
\item Direct effect: Y = b01 + b11\emph{X + e1
\item Conditional effect: Y = b03 + b13}X + b23*M + e3
}

If mediation is present, b13 should be smaller than b11 in absolute value,
as some of X's effect on Y is now explained by M.

The function computes a bootstrap confidence interval for (b11 - b13), which
estimates the indirect effect through the mediator. Significant mediation
occurs when this CI excludes zero.

\strong{Requirements}:
\itemize{
\item x must have exactly 2 columns: \link{predictor, mediator}
\item Use \code{MC = TRUE} for parallel processing with multiple cores
\item Use \code{xout = TRUE} to remove leverage points before analysis
}
}
\examples{
\dontrun{
# Mediation analysis
set.seed(123)
n <- 100
x1 <- rnorm(n)          # Predictor
m <- 0.5*x1 + rnorm(n)  # Mediator
y <- 0.3*x1 + 0.4*m + rnorm(n)  # Outcome (partial mediation)
x <- cbind(x1, m)
regmediate(x, y)
}
}
\references{
Wilcox, R.R. (2022). Introduction to Robust Estimation and Hypothesis Testing
(5th ed.). Academic Press.
}
\seealso{
\code{\link{regmed2}}, \code{\link{tsreg}}, \code{\link{regci}}
}
