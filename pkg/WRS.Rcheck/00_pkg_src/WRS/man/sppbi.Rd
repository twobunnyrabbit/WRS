% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anova.R
\name{sppbi}
\alias{sppbi}
\title{Percentile Bootstrap for Interactions in Split-Plot Design}
\usage{
sppbi(
  J,
  K,
  x,
  est = tmean,
  JK = J * K,
  grp = c(1:JK),
  nboot = 500,
  SEED = TRUE,
  pr = TRUE,
  ...
)
}
\arguments{
\item{J}{Number of levels for Factor A (between-subjects factor)}

\item{K}{Number of levels for Factor B (within-subjects factor)}

\item{x}{Data in list mode (length J*K) or matrix (columns are groups).
For list mode: x[\link{1}] through x[\link{K}] are data for first level of Factor A
at all K levels of Factor B, x[\link{K+1}] through x[\link{2K}] are for second level
of Factor A, etc.}

\item{est}{Estimator function to use (default: tmean for trimmed mean)}

\item{JK}{Total number of groups, J\emph{K (default: J}K)}

\item{grp}{Numeric vector specifying which groups to include (default: all groups)}

\item{nboot}{Number of bootstrap samples (default: 500)}

\item{SEED}{Logical. If TRUE (default), set random seed for reproducibility}

\item{pr}{Logical. If TRUE (default), print informational messages}

\item{...}{Additional arguments passed to the estimator function}
}
\value{
A list with components:
\item{test}{Test statistic for interaction}
\item{p.value}{Bootstrap p-value}
\item{crit.value}{Critical value}
}
\description{
Tests for interactions in a J by K split-plot (mixed) design using percentile
bootstrap. The analysis examines whether differences among dependent groups
(Factor B) vary across levels of the independent factor (Factor A).
}
\details{
This function analyzes split-plot designs by computing difference scores among
all pairs of dependent groups (Factor B levels) and testing whether these
differences vary across the independent factor (Factor A levels).

Data organization: For each subject, measurements at all K levels of Factor B
are in the same row. Missing values are handled by listwise deletion within
each level of Factor A.

As of October 2014, the default estimator is tmean (trimmed mean).
}
\seealso{
\code{\link{sppba}}, \code{\link{spp}}
}
