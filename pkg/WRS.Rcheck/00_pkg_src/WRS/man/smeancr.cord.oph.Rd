% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{smeancr.cord.oph}
\alias{smeancr.cord.oph}
\title{Multivariate Skipped Mean Test (One-Phase Version)}
\usage{
smeancr.cord.oph(
  m,
  nullv = rep(0, ncol(m)),
  cop = 3,
  MM = FALSE,
  SEED = TRUE,
  nboot = 500,
  plotit = TRUE,
  MC = FALSE,
  xlab = "VAR 1",
  ylab = "VAR 2",
  STAND = TRUE
)
}
\arguments{
\item{m}{Matrix (n-by-p) of data.}

\item{nullv}{Null hypothesis vector (default: zero vector).}

\item{cop}{Center determination method: 1 = Donoho-Gasko, 2 = MCD,
3 = marginal medians (default), 4 = MVE.}

\item{MM}{Logical. If \code{FALSE} (default), use boxplot rule for outliers;
if \code{TRUE}, use MAD-based rule.}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{nboot}{Number of bootstrap samples (default: 500).}

\item{plotit}{Logical. Create diagnostic plot (default: \code{TRUE}).}

\item{MC}{Logical. Use parallel processing (default: \code{FALSE}).}

\item{xlab, ylab}{Axis labels for plot.}

\item{STAND}{Logical. Standardize before outlier detection (default: \code{TRUE}).}
}
\value{
List with components:
\item{p.value}{P-value for testing H0: mean = nullv.}
\item{boot.vals}{Matrix of bootstrap estimates.}
\item{center}{Estimated skipped mean.}
}
\description{
Tests whether multivariate skipped means equal a null value using
one-phase outlier detection.
}
\details{
Tests H0: skipped mean = nullv using projection-based outlier detection.
For each point, projects all data onto the line from that point to the center,
identifies outliers via boxplot (or MAD) rule, then repeats for all points.
A point is an outlier if flagged in any projection.

Uses one-phase outlier detection (oph) where outliers are removed once before
computing the test statistic. Compare to \code{smeancr.cord()} which uses iterative
outlier removal.

Critical levels are adjusted for small sample sizes to maintain nominal .05 level.
}
\examples{
\dontrun{
set.seed(123)
m <- matrix(rnorm(50 * 3), ncol = 3)
smeancr.cord.oph(m, nboot = 500, plotit = FALSE)
}
}
\seealso{
\code{\link{smeancr.cord}}, \code{\link{smean}}, \code{\link{outpro}}
}
