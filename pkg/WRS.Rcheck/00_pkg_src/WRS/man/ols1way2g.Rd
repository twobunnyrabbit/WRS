% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{ols1way2g}
\alias{ols1way2g}
\title{Heteroscedastic Test for Equality of Slopes (Multiple Groups)}
\usage{
ols1way2g(
  x,
  y,
  grp = c(1, 2),
  iv = 1,
  xout = FALSE,
  outfun = outpro,
  STAND = TRUE,
  alpha = 0.05,
  pr = TRUE,
  BLO = FALSE,
  ...
)
}
\arguments{
\item{x}{List of length J. Each element is a matrix or vector of predictors for one group.}

\item{y}{List of length J. Each element is a numeric vector of responses for one group.}

\item{grp}{Numeric vector. Group indices to compare (default: c(1,2)).}

\item{iv}{Integer. Which independent variable (slope) to test (default: 1).}

\item{xout}{Logical. If TRUE, removes leverage points using \code{outfun} (default: FALSE).}

\item{outfun}{Function. Outlier detection function (default: \code{\link{outpro}}).}

\item{STAND}{Logical. Passed to \code{outfun} for standardization (default: TRUE).}

\item{alpha}{Numeric. Significance level (default: 0.05).}

\item{pr}{Logical. If TRUE, prints results (default: TRUE).}

\item{BLO}{Logical. If TRUE, removes only bad leverage points (default: FALSE).}

\item{...}{Additional arguments passed to \code{outfun}.}
}
\value{
List with components:
\describe{
\item{test}{Numeric. Test statistic for slope equality}
\item{crit.val}{Numeric. Adjusted critical value}
\item{p.value}{Numeric. Approximate p-value}
\item{est}{Matrix (J × (p+1)). OLS coefficient estimates for each group}
\item{n}{Integer vector. Sample sizes after removing missing values}
\item{nv.keep}{Integer vector. Sample sizes after removing leverage points}
}
}
\description{
Tests whether J independent groups have equal regression slopes (allowing
different intercepts) using heteroscedasticity-consistent standard errors (HC4)
and a Johansen-type MANOVA test statistic.
}
\details{
\strong{Null hypothesis}: All J groups have equal slopes (intercepts may differ).
This is an ANCOVA-like test allowing heteroscedasticity.

\strong{Difference from \code{\link{ols1way}}}:
\itemize{
\item \code{ols1way}: Tests H₀: All parameters (intercepts AND slopes) are equal
\item \code{ols1way2g}: Tests H₀: Slopes are equal (intercepts may differ)
}

\strong{Test procedure}:
\enumerate{
\item Fit OLS regression for each group
\item Compute HC4 covariance matrices for slope parameters only
\item Apply Johansen-type test statistic for slope equality
\item Adjust critical value for heteroscedasticity
}

\strong{Use case}: Testing whether the relationship between X and Y has the same
strength across groups, allowing for baseline differences (different intercepts).
}
\references{
Johansen, S. (1980). The Welch-James approximation to the distribution of
the residual sum of squares in a weighted linear regression. Biometrika, 67, 85-92.
}
\seealso{
\code{\link{ols1way}} for testing all parameters,
\code{\link{olshc4}} for HC4 covariances, \code{\link{ols}}
}
