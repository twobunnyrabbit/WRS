% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{ancsm}
\alias{ancsm}
\title{ANCOVA Using Depths of Smooths}
\usage{
ancsm(
  x1,
  y1,
  x2,
  y2,
  crit.mat = NULL,
  nboot = 200,
  SEED = TRUE,
  REP.CRIT = FALSE,
  LP = TRUE,
  est = tmean,
  fr = NULL,
  plotit = TRUE,
  sm = FALSE,
  xout = FALSE,
  outfun = out,
  xlab = "X",
  ylab = "Y",
  ...
)
}
\arguments{
\item{x1, y1}{Covariate and outcome for group 1.}

\item{x2, y2}{Covariate and outcome for group 2.}

\item{crit.mat}{Optional matrix of bootstrap critical values. If \code{NULL},
computes via bootstrap.}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{REP.CRIT}{Logical. If \code{TRUE}, returns bootstrap critical values matrix.}

\item{LP}{Logical. Use LOESS for plot (default: \code{TRUE}).}

\item{est}{Estimator function (default: \code{tmean} for trimmed mean).}

\item{fr}{Span for running interval smoother. If \code{NULL}, chosen automatically
based on sample size.}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{sm}{Logical. If \code{TRUE}, uses bootstrap bagging for smooths.}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{xlab, ylab}{Axis labels.}

\item{...}{Additional arguments.}
}
\value{
List with:
\describe{
\item{p.value}{Bootstrap p-value for testing equal regression lines.}
\item{crit.mat}{Bootstrap critical values (if \code{REP.CRIT = TRUE}).}
\item{test.depth}{Observed depth statistic.}
}
}
\description{
Compares two independent group regression lines using the depths of smooths
rather than traditional hypothesis tests. Uses bootstrap to determine
significance based on smooth depth.
}
\details{
Uses running interval smoother to create nonparametric regression estimates.
Tests equality of regression lines by comparing depth of observed smooth
difference to bootstrap distribution under null hypothesis.
}
\examples{
\dontrun{
x1 <- rnorm(50); y1 <- x1 + rnorm(50)
x2 <- rnorm(50); y2 <- x2 + 0.5 + rnorm(50)
result <- ancsm(x1, y1, x2, y2, nboot = 500)
}
}
