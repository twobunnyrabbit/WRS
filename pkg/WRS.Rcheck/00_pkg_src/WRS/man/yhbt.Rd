% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{yhbt}
\alias{yhbt}
\title{Bootstrap-t with Hall's Transformation for Independent Groups}
\usage{
yhbt(x, y, tr = 0.2, alpha = 0.05, nboot = 600, SEED = TRUE, PV = FALSE)
}
\arguments{
\item{x}{Numeric vector for first group.}

\item{y}{Numeric vector for second group.}

\item{tr}{Trimming proportion (default: 0.2 for 20\% trimming).}

\item{alpha}{Significance level (default: 0.05 for 95\% CI).}

\item{nboot}{Number of bootstrap samples (default: 600).}

\item{SEED}{Logical. If \code{TRUE} (default), sets random seed to 2 for reproducibility.}

\item{PV}{Logical. If \code{TRUE}, computes p-value (default: \code{FALSE}).}
}
\value{
List with components:
\item{est.dif}{Estimated difference in trimmed means (x - y).}
\item{conf.interval}{Confidence interval (lower, upper).}
\item{p.value}{Approximate p-value (only when \code{PV=TRUE}; otherwise \code{NULL}).}
}
\description{
Computes a confidence interval for the difference between trimmed means of
two independent groups using the bootstrap-t method with Hall's transformation.
}
\details{
Combines the bootstrap-t method with Hall's transformation to construct
confidence intervals. Hall's transformation adjusts for skewness, potentially
improving coverage accuracy compared to standard bootstrap-t.

Each group is centered by subtracting its trimmed mean before bootstrap
resampling. The bootstrap distribution of test statistics is transformed
using Hall's cube-root transformation to account for skewness.

When \code{PV=TRUE}, the function searches for the smallest alpha level at
which the CI excludes zero, providing an approximate p-value.

Missing values are automatically removed.
}
\seealso{
\code{\link{yuen}}, \code{\link{yhall}}, \code{\link{yuenbt}}
}
\keyword{bootstrap}
\keyword{htest}
\keyword{robust}
