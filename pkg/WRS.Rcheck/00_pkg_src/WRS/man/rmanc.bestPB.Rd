% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{rmanc.bestPB}
\alias{rmanc.bestPB}
\title{Identify Group with Largest Measure (Percentile Bootstrap Version)}
\usage{
rmanc.bestPB(
  x,
  alpha = 0.05,
  est = tmean,
  iter = 5000,
  SEED = TRUE,
  nboot = 2000,
  PB = FALSE,
  ...
)
}
\arguments{
\item{x}{Matrix or data frame with repeated measures (rows=subjects, cols=groups)}

\item{alpha}{Significance level (default: 0.05)}

\item{est}{Estimator function to use (default: tmean)}

\item{iter}{Number of simulation iterations for determining critical p-values (default: 5000)}

\item{SEED}{Logical; if TRUE, set random seed (default: TRUE)}

\item{nboot}{Number of bootstrap samples (default: 2000)}

\item{PB}{Logical; if FALSE use rmanc.best.crit (faster), if TRUE use percentile bootstrap (default: FALSE)}

\item{...}{Additional arguments passed to estimator}
}
\value{
S4 object with slots: Group.with.largest.estimate, Larger.than,
n, and output (detailed comparison results)
}
\description{
For J dependent groups, identify the group with the largest measure of location
using percentile bootstrap or simulation-based critical values. Supports any
estimator function, not just trimmed means.
}
\keyword{internal}
