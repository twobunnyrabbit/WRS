% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{TWOpovPV}
\alias{TWOpovPV}
\title{Compare Two Dependent Correlations with P-Value (Overlapping Case)}
\usage{
TWOpovPV(x, y, alpha = 0.05, CN = FALSE)
}
\arguments{
\item{x}{Matrix with 2 columns containing the two variables to correlate with y.}

\item{y}{Vector to correlate with both columns of x.}

\item{alpha}{Significance level (default: 0.05).}

\item{CN}{Logical. If \code{TRUE}, uses Zou's confidence interval method (default: \code{FALSE}).}
}
\value{
A list with components:
\itemize{
\item \code{p.value}: P-value for testing equality of correlations
\item \code{est.rho1}: Correlation between x\link{,1} and y
\item \code{est.rho2}: Correlation between x\link{,2} and y
\item \code{ci}: Confidence interval for difference in correlations
}
}
\description{
Compares two dependent Pearson correlations in the overlapping case
(when both correlations share a common variable). Returns both confidence
interval and p-value.
}
\details{
This function extends \code{TWOpov} by computing a p-value via confidence
interval inversion. Tests whether cor(x\link{,1}, y) = cor(x\link{,2}, y) in the
overlapping case where y is shared.
}
\examples{
\dontrun{
# Compare two correlations sharing a common variable
set.seed(123)
x <- matrix(rnorm(60), ncol = 2)
y <- 0.5 * x[,1] + rnorm(30)
TWOpovPV(x, y)
}
}
\seealso{
\code{\link{TWOpov}}, \code{\link{TWOpNOVPV}}
}
