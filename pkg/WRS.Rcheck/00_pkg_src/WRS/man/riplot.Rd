% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{riplot}
\alias{riplot}
\title{Regression Interaction Plot}
\usage{
riplot(
  x,
  y,
  adfun = adrun,
  plotfun = lplot,
  eout = FALSE,
  xout = TRUE,
  scale = FALSE
)
}
\arguments{
\item{x}{Matrix with exactly 2 columns of predictor values.}

\item{y}{Numeric vector of response values.}

\item{adfun}{Additive model fitting function (default: \code{adrun}).}

\item{plotfun}{Plotting function for residuals (default: \code{lplot}).}

\item{eout}{Logical value. If \code{TRUE}, outliers are removed based on
eliminating points with large residuals or leverage (default: \code{FALSE}).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{scale}{Logical. If \code{TRUE}, scales variables (default: \code{FALSE}).}
}
\value{
Output from the plotting function (usually a plot).
}
\description{
Investigates regression interaction by fitting a generalized additive model
and plotting residuals. Useful for assessing whether an additive model
adequately fits the data or if interaction terms are needed.
}
\details{
The function first fits an additive model using \code{adfun}, then plots the
residuals using \code{plotfun}. If the additive model is adequate, residuals
should show no systematic pattern. The x argument must have exactly 2 columns.
}
\examples{
\dontrun{
# Check for interaction in regression
x <- matrix(rnorm(100), ncol=2)
y <- 2*x[,1] + 3*x[,2] + x[,1]*x[,2] + rnorm(50)  # Has interaction
riplot(x, y)
}
}
\seealso{
\code{\link{adrun}}, \code{\link{lplot}}
}
