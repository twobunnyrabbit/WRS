% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{rqtest}
\alias{rqtest}
\title{Omnibus Test for Quantile Regression}
\usage{
rqtest(
  x,
  y,
  qval = 0.5,
  nboot = 200,
  alpha = 0.05,
  SEED = TRUE,
  xout = FALSE,
  outfun = outpro,
  ...
)
}
\arguments{
\item{x}{Matrix or data frame of predictor variables (must have at least 2 columns).}

\item{y}{Numeric vector of response variable values.}

\item{qval}{Numeric. Quantile to estimate (default: 0.5 for median regression).
Must be between 0 and 1.}

\item{nboot}{Integer. Number of bootstrap samples (default: 200).}

\item{alpha}{Numeric. Significance level for adjusted critical value (default: 0.05).}

\item{SEED}{Logical. If \code{TRUE} (default), set random seed to 2 for reproducibility.}

\item{xout}{Logical. If \code{TRUE}, remove outliers from predictors before testing
(default: \code{FALSE}).}

\item{outfun}{Function for outlier detection when \code{xout=TRUE}. Default is
\code{\link{outpro}}.}

\item{...}{Additional arguments passed to \code{outfun}.}
}
\value{
List with components:
\item{test.stat}{Numeric. Test statistic value}
\item{p.value}{Numeric. P-value based on F-distribution with p and n-p degrees of freedom}
\item{adjusted.alpha}{Numeric or \code{NULL}. Adjusted significance level based on
simulation (only for n ≤ 60 and specific p values)}
}
\description{
Performs a bootstrap-based omnibus test for quantile regression with multiple
predictors. Tests the null hypothesis that all slope coefficients are zero.
}
\details{
This function performs an omnibus test for quantile regression models with
multiple predictors. The test statistic is based on a bootstrap estimate of
the covariance matrix of the slope coefficients.

The test uses an F-statistic-like quantity:
\deqn{T = (\mathbf{b}' \mathbf{S}^{-1} \mathbf{b}) \times (n-p) / ((n-1)p)}
where \eqn{\mathbf{b}} is the vector of slope estimates and \eqn{\mathbf{S}}
is the bootstrap covariance matrix.

For small sample sizes (n ≤ 60) and specific values of p and alpha, the
function computes an adjusted critical value based on simulation studies.
}
\note{
For single predictor models (p=1), use \code{\link{qregci}} instead.
}
\seealso{
\code{\link{qregci}}, \code{\link{rqfit}}, \code{\link{qreg}}
}
\keyword{htest}
\keyword{regression}
\keyword{robust}
