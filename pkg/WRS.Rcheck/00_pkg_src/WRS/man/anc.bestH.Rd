% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{anc.bestH}
\alias{anc.bestH}
\title{Select Best Group with Adjusted P-Values}
\usage{
anc.bestH(x, rem = NULL, alpha = 0.05, tr = 0.2, iter = 5000, SEED = TRUE)
}
\arguments{
\item{x}{Matrix or list of data where each column (or list element) is a group.}

\item{rem}{Matrix of simulated p-value distributions. If NULL, computed via simulation.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{tr}{Trimming proportion (default: 0.2). Proportion of observations to
trim from each tail when computing winsorized correlation.}

\item{iter}{Number of iterations for simulation if rem is NULL (default: 5000).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}
}
\value{
S4 object of class 'BIN' with slots:
\item{Group.with.largest.estimate}{Index of group with largest estimate}
\item{Larger.than}{Groups that the best group is significantly larger than}
\item{n}{Sample sizes}
\item{output}{Matrix with detailed comparison results including adjusted p-values}
}
\description{
Similar to anc.best but uses adjusted p-values (via p.adjust) instead of
critical p-values determined by simulation. Identifies the group with the largest
trimmed mean and provides inference with familywise error control.
}
\note{
Confidence intervals have simultaneous probability coverage 1-alpha.
}
\examples{
\dontrun{
x <- list(rnorm(30), rnorm(30, mean=0.5), rnorm(30, mean=1))
result <- anc.bestH(x, alpha=0.05)
}
}
