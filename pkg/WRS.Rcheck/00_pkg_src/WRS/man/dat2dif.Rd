% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{dat2dif}
\alias{dat2dif}
\title{Compute All Pairwise Differences for Dependent Groups}
\usage{
dat2dif(x)
}
\arguments{
\item{x}{A matrix or data frame with at least 2 columns, where columns
represent dependent groups}
}
\value{
A matrix with n rows and N=(J^2-J)/2 columns, where each column contains
the pairwise difference between two groups. For J groups, there are
J(J-1)/2 pairwise comparisons.
}
\description{
For J dependent groups (columns), computes all pairwise differences and
returns them as a matrix. Used for dependent group comparisons.
}
\details{
For J groups, computes all pairwise differences x\link{,j} - x\link{,k} where j < k.
The ordering follows: (1-2), (1-3), ..., (1-J), (2-3), (2-4), ..., ((J-1)-J).
}
\examples{
\dontrun{
x <- matrix(rnorm(30), ncol=3)  # 3 dependent groups
dif <- dat2dif(x)  # 3 pairwise differences
}

}
\seealso{
\code{\link{dat2form}}
}
\keyword{manip}
