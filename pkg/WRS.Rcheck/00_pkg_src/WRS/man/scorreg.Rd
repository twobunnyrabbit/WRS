% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{scorreg}
\alias{scorreg}
\title{Skipped Correlation Regression}
\usage{
scorreg(
  x,
  y,
  corfun = spear,
  cop = 3,
  MM = FALSE,
  gval = NA,
  outfun = outpro,
  alpha = 0.05,
  MC = NULL,
  SEED = TRUE,
  ALL = TRUE,
  ...
)
}
\arguments{
\item{x}{Numeric matrix (n-by-p). Predictor variables.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{corfun}{Correlation function to use. Common options: \code{pbcor} (percentage
bend), \code{pcor} (Pearson), \code{wincor} (winsorized), \code{spear} (Spearman).}

\item{cop}{Copula type for skipped correlation (default: 3). Options: 1-5.}

\item{MM}{Logical value. If \code{TRUE}, uses the MM-estimator for robust
regression (default: \code{FALSE}).}

\item{gval}{Critical value for outlier detection (default: \code{NA} for automatic).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{MC}{Logical. Use parallel processing if \code{TRUE}. If \code{NULL} (default),
automatically uses \code{TRUE} for n >= 200.}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{ALL}{Logical. If \code{TRUE} (default), eliminates outliers from cbind(x, y)
jointly; if \code{FALSE}, eliminates outliers separately for each (x\link{,j}, y) pair.}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
List with component:
\item{cor}{Vector of p correlations cor(y, x_j) for j = 1,...,p.}
}
\description{
Computes skipped correlations between a response variable y and each
predictor in matrix x.
}
\details{
Computes skipped correlations between y and each column of x. Two approaches:
\itemize{
\item \code{ALL = TRUE}: Detects outliers in the full (x, y) space once, then computes
correlations on the cleaned data. More conservative.
\item \code{ALL = FALSE}: For each predictor x\link{,j}, detects outliers in the (x\link{,j}, y)
bivariate space separately. Can detect more variable-specific outliers.
}

For n >= 500, uses faster depth-based outlier detection.
}
\examples{
set.seed(123)
x <- matrix(rnorm(50 * 3), ncol = 3)
y <- rnorm(50)
scorreg(x, y)

# Separate outlier detection per variable
scorreg(x, y, ALL = FALSE)
}
\seealso{
\code{\link{scorreg ci}}, \code{\link{scorregciH}}, \code{\link{scor}}
}
