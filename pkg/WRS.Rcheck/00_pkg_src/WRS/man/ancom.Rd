% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{ancom}
\alias{ancom}
\title{Omnibus ANCOVA Test}
\usage{
ancom(
  x1,
  y1,
  x2,
  y2,
  dchk = FALSE,
  plotit = TRUE,
  plotfun = rplot,
  nboot = 500,
  alpha = 0.05,
  SEED = TRUE,
  PARTEST = FALSE,
  tr = 0,
  ...
)
}
\arguments{
\item{x1}{Covariate values for group 1 (vector or matrix).}

\item{y1}{Response values for group 1.}

\item{x2}{Covariate values for group 2 (vector or matrix).}

\item{y2}{Response values for group 2.}

\item{dchk}{Logical. If TRUE, eliminates design space points with halfspace depth of zero.}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{plotfun}{Plotting function to use (default: rplot). Use rplotsm for smoothed plots via bagging.}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{PARTEST}{Logical. If FALSE, tests that regression surface is horizontal plane through origin.
If TRUE, tests that two regression surfaces are parallel.}

\item{tr}{Trimming proportion. tr=0 recommended for general use; tr>0 may result in poor
Type I error control (default: 0).}

\item{...}{Additional arguments passed to plotfun.}
}
\value{
List with:
\item{dstat}{Test statistic}
\item{critd}{Critical value}
}
\description{
Performs an omnibus ANCOVA test comparing two independent groups. Can test either
that the regression surface is a horizontal plane through the origin, or that two
regression surfaces are parallel. Uses robust depth-based approach.
}
\note{
tr=0 is strongly recommended. Higher trimming may compromise Type I error control.
}
\examples{
\dontrun{
x1 <- matrix(rnorm(100), ncol=2)
y1 <- rowSums(x1) + rnorm(50)
x2 <- matrix(rnorm(100), ncol=2)
y2 <- rowSums(x2) + rnorm(50, mean=0.5)
# Test for horizontal plane
result1 <- ancom(x1, y1, x2, y2, PARTEST=FALSE)
# Test for parallel surfaces
result2 <- ancom(x1, y1, x2, y2, PARTEST=TRUE)
}
}
