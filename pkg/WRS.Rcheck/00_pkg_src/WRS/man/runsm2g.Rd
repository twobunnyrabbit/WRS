% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{runsm2g}
\alias{runsm2g}
\title{Running Interval Smoother Comparing Two Groups}
\usage{
runsm2g(
  x1,
  y1,
  x2,
  val = median(x2),
  est = tmean,
  sm = FALSE,
  fr = 0.8,
  xlab = "X",
  ylab = "Y",
  ...
)
}
\arguments{
\item{x1}{Predictor variable for both groups}

\item{y1}{Response variable for both groups}

\item{x2}{Grouping variable used to split data}

\item{val}{Split point for x2 (default: median(x2))}

\item{est}{Measure of location (default: tmean)}

\item{sm}{Logical: use bootstrap smoothing (default: FALSE)}

\item{fr}{Span parameter controlling smoothing (default: 0.8)}

\item{xlab}{Label for x-axis (default: "X")}

\item{ylab}{Label for y-axis (default: "Y")}

\item{...}{Additional arguments passed to plotting function}
}
\value{
NULL (function creates plot as side effect)
}
\description{
Create running interval smoothers for two groups defined by a splitting
variable. Plots both smooths on the same graph for comparison.
}
\details{
Splits the data into two groups based on whether x2 < val, then creates
a running interval smooth of x1 vs y1 for each group. Both smooths are
plotted on the same graph for comparison.

If sm=TRUE, uses runmean2g with smoothing. Otherwise uses basic comparison.
Missing values are automatically removed.
}
\seealso{
\code{\link{runmean2g}}, \code{\link{rungen}}
}
\keyword{nonparametric}
\keyword{smooth}
