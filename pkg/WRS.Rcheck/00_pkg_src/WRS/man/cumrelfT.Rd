% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{cumrelfT}
\alias{cumrelfT}
\title{Compare Cumulative Relative Frequencies for Two Groups with Testing}
\usage{
cumrelfT(
  x,
  y,
  pts = NULL,
  q = c(0.1, 0.25, 0.5, 0.75, 0.9),
  xlab = "X",
  ylab = "CUM REL FREQ",
  plotit = TRUE,
  op = 1
)
}
\arguments{
\item{x}{First group data (numeric vector)}

\item{y}{Second group data (numeric vector)}

\item{pts}{Evaluation points (default: NULL, uses quantiles from x)}

\item{q}{Quantiles to use if pts is NULL (default: c(0.1, 0.25, 0.5, 0.75, 0.9))}

\item{xlab}{Label for x-axis (default: 'X')}

\item{ylab}{Label for y-axis (default: 'CUM REL FREQ')}

\item{plotit}{Logical: create plot (default: TRUE)}

\item{op}{Method: 1 = twobinom, 2 = bi2KMS (default: 1)}
}
\value{
A matrix with columns:
\item{pts}{Evaluation points}
\item{est.p1}{Estimated proportion for group 1}
\item{est.p2}{Estimated proportion for group 2}
\item{est.dif}{Estimated difference in proportions}
\item{p.value}{p-value for testing difference}
}
\description{
Compares cumulative relative frequency distributions for two independent groups
at specified evaluation points, with statistical testing of differences.
}
\details{
This function compares the cumulative relative frequencies at specified points
and tests whether the proportions differ between groups.

If pts is NULL, evaluation points are determined by computing quantiles of x
specified in the q parameter. At each point, the function computes the proportion
of observations at or below that value for each group and tests for a difference.

Two testing methods are available via the op parameter:
\itemize{
\item op=1: Uses twobinom for binomial comparison
\item op=2: Uses bi2KMS for KMS-based comparison
}
}
\seealso{
\code{\link{cumrelf}}, \code{\link{twobinom}}
}
\keyword{distribution}
\keyword{htest}
