% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{qregplots}
\alias{qregplots}
\title{Plot Quantile Regression Lines Using quantreg Package}
\usage{
qregplots(
  x,
  y,
  qval = 0.5,
  q = NULL,
  op = 1,
  pr = FALSE,
  xout = FALSE,
  outfun = out,
  plotit = FALSE,
  xlab = "X",
  ylab = "Y",
  ...
)
}
\arguments{
\item{x}{Numeric vector of predictor values (univariate only).}

\item{y}{Numeric vector of response values.}

\item{qval}{Numeric vector of quantiles to estimate (default: 0.5 for median).}

\item{q}{Alternative specification for \code{qval} (for backward compatibility).}

\item{op}{Operation code (currently only \code{op=1} is used).}

\item{pr}{Logical; if \code{TRUE}, prints progress messages (default: \code{FALSE}).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{plotit}{Logical; if \code{TRUE}, plots the scatter plot (default: \code{FALSE}).}

\item{xlab}{Label for x-axis (default: "X").}

\item{ylab}{Label for y-axis (default: "Y").}

\item{...}{Additional arguments passed to \code{outfun}.}
}
\value{
Matrix with columns "q" (quantile), "Inter" (intercept), and
"Slope", with one row per quantile in \code{qval}.
}
\description{
Computes and plots quantile regression lines using the \code{quantreg} package.
Alternative to \code{qplotreg()} that uses the Koenker-Bassett approach via
\code{rq()} function.
}
\details{
Uses the \code{quantreg} package to fit quantile regression models via the
Koenker-Bassett approach. For each quantile in \code{qval}:
\enumerate{
\item Fits quantile regression using \code{rq()}
\item Plots the regression line
\item Returns coefficient matrix
}

Only one predictor is allowed. This function loads the \code{quantreg} package.

Example: \code{qregplots(x, y, q=c(.25,.5,.75))} plots regression lines for
the quartiles (25th, 50th, 75th percentiles).
}
\examples{
# Plot quartile regression lines
x <- rnorm(100)
y <- x + rnorm(100)
qregplots(x, y, q=c(0.25, 0.5, 0.75), plotit=TRUE)
}
\seealso{
\code{\link{qplotreg}}, \code{\link{qreg}},
\code{\link[quantreg]{rq}}
}
