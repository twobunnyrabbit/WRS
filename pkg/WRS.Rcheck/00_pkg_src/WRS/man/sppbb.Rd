% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anova.R
\name{sppbb}
\alias{sppbb}
\title{Split-Plot Bootstrap Test for Main Effects in Dependent Groups}
\usage{
sppbb(
  J,
  K,
  x,
  est = tmean,
  JK = J * K,
  grp = c(1:JK),
  nboot = 500,
  SEED = TRUE,
  pr = TRUE,
  ...
)
}
\arguments{
\item{J}{Number of levels for between-subjects factor.}

\item{K}{Number of levels for within-subjects factor.}

\item{x}{Data in matrix or list format. If list, x[\link{1}] contains data for
level (1,1), x[\link{2}] for level (1,2), ..., x[\link{K}] for level (1,K),
x[\link{K+1}] for level (2,1), etc. If matrix, columns correspond to groups.}

\item{est}{Estimator to use (default is tmean for trimmed mean).}

\item{JK}{Total number of groups (default is J*K).}

\item{grp}{Vector specifying group ordering (default is 1:JK).}

\item{nboot}{Number of bootstrap samples (default is 500).}

\item{SEED}{Logical indicating whether to set random seed (default is TRUE).}

\item{pr}{Logical indicating whether to print messages (default is TRUE).}

\item{...}{Additional arguments passed to the estimator function.}
}
\value{
A list with components:
\item{p.value}{p-values for the tests}
\item{center}{Estimated centers for the groups}
}
\description{
Performs a percentile bootstrap test for main effects among dependent groups
in a split-plot design. Tests whether all pairwise differences have a
typical value of zero.
}
\details{
This function analyzes split-plot designs where subjects are measured under
multiple conditions (within-subjects factor) and there may be different groups
of subjects (between-subjects factor). The analysis is based on all pairs of
difference scores.

Missing values are automatically removed. The random seed is set to 2 if
SEED=TRUE for reproducibility.
}
\examples{
# Create data for 2x3 split-plot design
set.seed(123)
x <- list()
for(i in 1:6) x[[i]] <- rnorm(20)

# Run split-plot bootstrap test
result <- sppbb(J=2, K=3, x=x, nboot=100)
}
