% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{cumrelf}
\alias{cumrelf}
\title{Plot Cumulative Relative Frequencies for Multiple Groups}
\usage{
cumrelf(x, y = NA, xlab = "X", ylab = "CUM REL FREQ", pr.freq = FALSE)
}
\arguments{
\item{x}{Data for groups. Can be a matrix (columns = groups), list, or
vector (if y is specified)}

\item{y}{Optional second group data if x contains first group (default: NA)}

\item{xlab}{Label for x-axis (default: 'X')}

\item{ylab}{Label for y-axis (default: 'CUM REL FREQ')}

\item{pr.freq}{Logical: print frequency tables (default: FALSE)}
}
\value{
None (creates a plot)
}
\description{
Plots the cumulative relative frequency distributions for one or more groups
on a single plot, allowing visual comparison of distributions.
}
\details{
This function plots cumulative relative frequency distributions for two or
more groups. Different line types are used to distinguish groups.

Data can be provided in several formats:
\itemize{
\item Matrix or data frame with columns representing groups
\item List with each component containing data for one group
\item Two vectors x and y for two-group comparisons
}

The function requires at least two groups; it will stop with an error if
only one group is provided.
}
\seealso{
\code{\link{cumrelfT}}
}
\keyword{distribution}
\keyword{hplot}
