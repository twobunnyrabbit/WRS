% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression-advanced.R
\name{logreg.P.ci}
\alias{logreg.P.ci}
\title{Confidence Intervals for Logistic Regression Probabilities}
\usage{
logreg.P.ci(
  x,
  y,
  alpha = 0.05,
  plotit = TRUE,
  xlab = "X",
  ylab = "P(Y=1|X)",
  xout = FALSE,
  outfun = outpro,
  ...
)
}
\arguments{
\item{x}{A numeric vector or matrix containing predictor values.}

\item{y}{A binary numeric vector (will be converted to 0/1).}

\item{alpha}{Significance level for confidence intervals (default: 0.05 for
95\% CIs).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{xlab, ylab}{Axis labels for the plot.}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
A list with components:
\item{Strength.Assoc}{Strength of association measured as sd(fitted
probabilities) / sd(y).}
\item{output}{Matrix with columns: X (predictor values), est.p (estimated
probability), ci.low (lower CI bound), ci.up (upper CI bound), sorted by X.}
}
\description{
Computes pointwise confidence intervals for the probability P(Y=1|X) at each
observed x value in a logistic regression model, with optional outlier removal
and visualization.
}
\details{
This function fits a standard logistic regression model using \code{glm()} and
computes pointwise confidence intervals for P(Y=1|X) based on the standard
errors from the linear predictor scale:
\enumerate{
\item Fits logistic model: logit(P(Y=1|X)) = b0 + b1*X + ...
\item Obtains standard errors for fitted values on logit scale
\item Constructs normal-theory CIs on logit scale
\item Transforms back to probability scale via inverse logit
}

If \code{xout = TRUE}, outliers in the predictor space are removed before fitting
using the function specified by \code{outfun} (default: projection-based outlier
detection via \code{outpro}).

If \code{plotit = TRUE}, creates a plot showing:
\itemize{
\item Estimated probability curve (solid line)
\item Confidence bands (dashed lines)
}
}
\note{
The output matrix column is named "ci,up" (with comma) due to legacy
naming - this is maintained for backwards compatibility.
}
\examples{
\dontrun{
# Logistic regression with confidence bands
set.seed(123)
x <- seq(-3, 3, length=100)
p <- exp(1 + 2*x) / (1 + exp(1 + 2*x))
y <- rbinom(100, 1, p)
result <- logreg.P.ci(x, y, plotit = TRUE)
head(result$output)
}
}
\references{
Wilcox, R.R. (2022). Introduction to Robust Estimation and Hypothesis Testing
(5th ed.). Academic Press.
}
\seealso{
\code{\link{logreg}}, \code{\link{logreg.pred}}, \code{\link{wlogreg}}
}
