% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{qcmul}
\alias{qcmul}
\title{Bootstrap Confidence Intervals for Multiple Quantile Regression Correlations}
\usage{
qcmul(
  x,
  y,
  q = 0.5,
  nboot = 599,
  alpha = 0.05,
  SEED = FALSE,
  xout = FALSE,
  outfun = outpro,
  method = "BH"
)
}
\arguments{
\item{x}{Matrix or data frame of independent variables (predictors).}

\item{y}{Numeric vector of the dependent variable.}

\item{q}{Numeric value in (0,1). Quantile for regression (default: 0.5 for median).}

\item{nboot}{Integer. Number of bootstrap samples (default: 599).}

\item{alpha}{Numeric. Alpha level for confidence intervals (default: 0.05).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed to 2 (default: \code{FALSE}).}

\item{xout}{Logical. If \code{TRUE}, removes outliers from predictors using
\code{outfun} (default: \code{FALSE}).}

\item{outfun}{Function for multivariate outlier detection (default: \code{\link{outpro}}).}

\item{method}{Character string specifying p-value adjustment method (default: 'BH'
for Benjamini-Hochberg).}
}
\value{
Matrix with one row per predictor and columns:
\describe{
\item{Est.}{Quantile regression correlation estimate}
\item{ci.low}{Lower confidence limit}
\item{ci.up}{Upper confidence limit}
\item{p-value}{Bootstrap p-value}
\item{adj.p.value}{Adjusted p-value using specified method}
}
}
\description{
For each independent variable (predictor), computes a bootstrap confidence interval
for a quantile regression correlation with the dependent variable. Optionally removes
multivariate outliers from predictors before analysis.
}
\details{
For each predictor, computes quantile regression correlation via \code{\link{qcorp1}}.
Bootstrap resampling is used to construct confidence intervals and p-values.
}
\seealso{
\code{\link{qcorp1}}, \code{\link{outpro}}
}
\keyword{htest}
\keyword{regression}
