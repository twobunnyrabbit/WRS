% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{twowayESM}
\alias{twowayESM}
\title{Two-Way Effect Size Measures}
\usage{
twowayESM(J, K, x, fun = ES.summary, ...)
}
\arguments{
\item{J}{Number of levels for Factor A.}

\item{K}{Number of levels for Factor B.}

\item{x}{Data in matrix or list format (J*K groups).}

\item{fun}{Function to compute effect sizes (default: \code{ES.summary}).}

\item{...}{Additional arguments passed to \code{fun}.}
}
\value{
A list with components:
\itemize{
\item \code{FactorA}: Effect size results for Factor A (pooled over B)
\item \code{FactorB}: Effect size results for Factor B (pooled over A)
}
}
\description{
For a two-way design, computes effect size measures for each level of Factor A
(pooling over Factor B) and for each level of Factor B (pooling over Factor A).
}
\details{
This function pools data appropriately for a two-way design and computes
effect size measures using the specified function (typically \code{ES.summary}
or related functions from the effect size module).
}
\examples{
\dontrun{
# Two-way design: 2 x 3
set.seed(123)
J <- 2; K <- 3
x <- matrix(rnorm(120), ncol = J*K)

# Compute effect sizes for main effects
result <- twowayESM(J, K, x)
print(result)
}
}
\seealso{
\code{\link{twoway.pool}}, \code{\link{ES.summary}}, \code{\link{IND.PAIR.ES}}
}
