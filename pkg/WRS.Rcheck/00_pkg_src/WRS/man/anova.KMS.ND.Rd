% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anova.R
\name{anova.KMS.ND}
\alias{anova.KMS.ND}
\title{Generate Null Distribution for KMS ANOVA}
\usage{
\method{anova}{KMS.ND}(n, tr = 0.2, iter = 5000, nulldist = nulldist, SEED = TRUE)
}
\arguments{
\item{n}{Vector of sample sizes for each group}

\item{tr}{Proportion to trim from each end (default: 0.2, meaning 20\% from each tail)}

\item{iter}{Number of iterations for simulation (default: 5000)}

\item{nulldist}{Placeholder parameter for null distribution (currently unused in function body)}

\item{SEED}{Logical. If TRUE (default), set random seed for reproducibility}
}
\value{
Numeric vector of length iter containing simulated effect sizes under
the null hypothesis
}
\description{
Generates the null distribution for the Kulinskaya-Staudte (KMS) ANOVA effect
size measure. Used for hypothesis testing with \code{anova.KMS}.
}
\details{
This function simulates data under the null hypothesis of no group differences
by generating samples from a g-and-h distribution (with g=0.75) for each group.
For each iteration, it computes the KMS effect size using \code{KS.ANOVA.ES}.

The resulting vector can be used to determine critical values or p-values for
observed KMS effect sizes. The g-and-h distribution with g=0.75 provides a
flexible distribution for the null.
}
\seealso{
\code{\link{KS.ANOVA.ES}}, \code{\link{anova.KMS}}
}
