% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{lplotse}
\alias{lplotse}
\title{Bootstrap Standard Errors for LOESS Predictions}
\usage{
lplotse(x, y, pts = x, nboot = 100, SEED = TRUE, span = 2/3)
}
\arguments{
\item{x}{Predictor variable vector.}

\item{y}{Response variable vector.}

\item{pts}{Points at which to estimate standard errors (default: uses x values).}

\item{nboot}{Number of bootstrap samples (default: 100).}

\item{SEED}{Logical; if TRUE, sets random seed to 2 for reproducibility (default: TRUE).}

\item{span}{Span parameter for LOESS (default: 2/3). Controls degree of smoothing.}
}
\value{
Vector of standard errors corresponding to the prediction points in \code{pts}.
}
\description{
Computes bootstrap standard errors for LOESS (locally weighted scatterplot smoothing)
predictions at specified points.
}
\details{
This is an internal helper function used by other plotting functions to estimate
standard errors for LOESS-based smoothers.

The function:
\enumerate{
\item Orders data by x values
\item Generates bootstrap samples by resampling observations
\item Fits LOESS to each bootstrap sample
\item Computes predictions at specified points
\item Returns the standard deviation of predictions across bootstrap samples
}
}
\examples{
x <- rnorm(50)
y <- x + rnorm(50)
se_vals <- lplotse(x, y, nboot = 50)
}
\seealso{
\code{\link{lplotCI}}, \code{\link{lplot.pred}}
}
\keyword{internal}
