% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression.R
\name{tssnmreg}
\alias{tssnmreg}
\title{Hybrid Theil-Sen and Skipped Regression with Outlier Removal}
\usage{
tssnmreg(x, y, sc = pbvar, xout = FALSE, outfun = out, plotit = FALSE, ...)
}
\arguments{
\item{x}{Numeric vector or matrix of predictor values (n by p).}

\item{y}{Numeric vector of response values (length n).}

\item{sc}{Robust scale function for skipped regression (default: \code{pbvar}).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{plotit}{Logical; currently unused (default: FALSE).}

\item{...}{Additional arguments passed to \code{outfun}.}
}
\value{
A list with components:
\describe{
\item{coef}{Vector of regression coefficients (intercept, slopes).}
\item{residuals}{Residuals from the final Theil-Sen fit.}
}
}
\description{
Combines skipped regression with Theil-Sen estimation by first using
skipped regression to identify outliers, then applying standard Theil-Sen
to the remaining clean data.
}
\details{
This hybrid approach proceeds in three stages:
\enumerate{
\item \strong{Stage 1}: Fits skipped regression (\code{\link{snmreg}}) which uses
Nelder-Mead optimization with a "skipped" correlation approach.
\item \strong{Stage 2}: Identifies outliers as observations with standardized
residuals exceeding 2 (in MAD units).
\item \strong{Stage 3}: Applies standard Theil-Sen regression (\code{\link{tsreg}})
to the remaining observations after outlier removal.
}

\strong{Why this hybrid?}
\itemize{
\item Skipped regression (\code{snmreg}) is effective at handling outliers
in both x and y directions
\item Theil-Sen provides simple, distribution-free inference once outliers
are removed
\item The combination leverages strengths of both methods
}

The method is particularly useful when:
\itemize{
\item You suspect leverage points (outliers in predictor space)
\item You want the simplicity of Theil-Sen for final inference
\item Initial outlier identification needs to handle multivariate outliers
}
}
\examples{
\dontrun{
# Data with leverage points
set.seed(123)
x <- rnorm(50)
y <- 2*x + rnorm(50)
# Add leverage points
x[1:3] <- x[1:3] + 5
y[1:3] <- y[1:3] - 5

# Standard Theil-Sen (affected by leverage)
tsreg(x, y)

# Hybrid approach (more robust)
tssnmreg(x, y)

# Multiple predictors
x <- matrix(rnorm(100), ncol=2)
y <- x[,1] + 2*x[,2] + rnorm(50)
x[1:5,] <- x[1:5,] + 4  # Leverage points
tssnmreg(x, y)
}

}
\seealso{
\code{\link{snmreg}} for skipped regression,
\code{\link{tsreg}} for standard Theil-Sen,
\code{\link{tstsreg}} for S-type Theil-Sen with outlier removal
}
