% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{YYmcp}
\alias{YYmcp}
\title{Pairwise Comparisons for Multivariate Data (Yanagihara-Yuan Method)}
\usage{
YYmcp(
  x,
  alpha = 0.05,
  grp = NA,
  tr = 0.2,
  bhop = FALSE,
  J = NULL,
  p = NULL,
  ...
)
}
\arguments{
\item{x}{Data in list mode (preferred) or matrix. In list mode, \code{x[[j]]} is
a matrix containing multivariate data for group j. In matrix mode, must specify
\code{J} or \code{p}.}

\item{alpha}{Family-wise error rate (default: 0.05).}

\item{grp}{Vector specifying subset of groups to compare (default: all groups).
Example: \code{grp=c(1,3,5)} compares groups 1, 3, and 5.}

\item{tr}{Trimming proportion (default: 0.2 for 20\% trimming).}

\item{bhop}{Logical. If \code{FALSE} (default), uses Rom's method for FWE control;
if \code{TRUE}, uses Benjamini-Hochberg method.}

\item{J}{Number of groups (required when \code{x} is a matrix).}

\item{p}{Number of variables (required when \code{x} is a matrix).}

\item{...}{Additional arguments (currently unused).}
}
\value{
List with components:
\item{output}{Matrix with columns: Group (first), Group (second), p.value, p.crit.}
\item{num.sig}{Number of significant comparisons.}
}
\description{
Performs all pairwise comparisons among J independent groups using trimmed means
with multivariate data. Uses the Yanagihara-Yuan method for each pair and controls
family-wise error rate via Rom's or Benjamini-Hochberg method.
}
\details{
The Yanagihara-Yuan method tests equality of multivariate trimmed means. For each
pairwise comparison, \code{\link{YYmanova}} is called.

Data formats:
\itemize{
\item List mode: Each element is a matrix (observations × variables)
\item Matrix mode: Columns ordered as group 1 variables, group 2 variables, etc.
}

Critical values for Rom's method are built-in for alpha = 0.05 and 0.01.
For other alpha levels, Bonferroni adjustment is used.

Missing values are automatically removed.
}
\seealso{
\code{\link{YYmanova}}, \code{\link{mulrank}}, \code{\link{YYmanovaMC}}
}
\keyword{htest}
\keyword{multivariate}
\keyword{robust}
