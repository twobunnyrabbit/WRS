% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{rtdep}
\alias{rtdep}
\title{Regression-Based Tukey Depth for Multivariate Data}
\usage{
rtdep(pts, m, nsamp = 100, SEED = NA)
}
\arguments{
\item{pts}{Numeric vector representing the point for which depth is computed.
Length should equal \code{ncol(m)}.}

\item{m}{Numeric matrix of data (n x p). Each row is an observation.}

\item{nsamp}{Integer. Number of random samples to take (default: 100).}

\item{SEED}{Numeric or NA. Random seed for reproducibility. If \code{NA},
seed is set to 2. To use a different seed, specify a numeric value.}
}
\value{
Numeric value representing the approximate Tukey depth of the point,
scaled to \link{0, 1}.
}
\description{
Computes approximate Tukey depth for a point in multivariate space using
a random sampling approach based on regression residuals.
}
\details{
For bivariate data (p=2), the function uses the exact \code{\link{depth}}
calculation. For higher dimensions (p>2), it uses a regression-based
approximation:
\enumerate{
\item Randomly sample p-1 points from the data
\item Combine with the target point \code{pts}
\item Fit a regression hyperplane through these p points
\item Count residuals above and below the hyperplane
\item Compute min(positive residuals, negative residuals)
\item Repeat \code{nsamp} times and take minimum
}

The depth is normalized by sample size n.

Points with higher depth are more central in the data cloud. Outliers have
low depth values.
}
\seealso{
\code{\link{depth}}, \code{\link{dmul}}
}
\keyword{multivariate}
\keyword{robust}
