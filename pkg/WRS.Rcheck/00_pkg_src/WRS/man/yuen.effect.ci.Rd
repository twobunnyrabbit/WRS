% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{yuen.effect.ci}
\alias{yuen.effect.ci}
\title{Explanatory Measure of Effect Size for Yuen's Test with CI}
\usage{
yuen.effect.ci(x, y, SEED = TRUE, nboot = 400, tr = 0.2, alpha = 0.05)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{tr}{Trimming proportion (default: 0.2). Proportion of observations to
trim from each tail when computing winsorized correlation.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}
}
\value{
A list with components:
\itemize{
\item \code{estimate}: Effect size estimate
\item \code{ci}: Bootstrap confidence interval for the effect size
}
}
\description{
Computes an explanatory measure of effect size for Yuen's test along with
a bootstrap confidence interval.
}
\details{
The effect size measure indicates the degree to which distributions differ
in a way that is directly interpretable. Bootstrap methods are used to
compute the confidence interval.
}
\examples{
\dontrun{
x <- rnorm(50)
y <- rnorm(60, mean = 0.5)
yuen.effect.ci(x, y)
}
}
\seealso{
\code{\link{yuen.effect}}, \code{\link{akp.effect}}
}
