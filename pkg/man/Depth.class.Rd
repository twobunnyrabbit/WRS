% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classification.R
\name{Depth.class}
\alias{Depth.class}
\title{Depth-Based Classification}
\usage{
Depth.class(
  train = NULL,
  test = NULL,
  g = NULL,
  x1 = NULL,
  x2 = NULL,
  depthfun = prodepth,
  DIST = TRUE,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{train}{Training data matrix or data frame (optional if x1 and x2 are provided).}

\item{test}{Test data matrix or data frame containing observations to classify.}

\item{g}{Group labels for training data (required if train is provided). Should be
a vector with two unique values indicating the two groups.}

\item{x1}{Training data for group 1 (alternative to train/g specification).}

\item{x2}{Training data for group 2 (alternative to train/g specification).}

\item{depthfun}{Function for computing data depth (default: \code{prodepth} for
projection depth). Must have the form \code{depthfun(x, pts)} where it computes
depth for points in \code{pts} relative to points in \code{x}.}

\item{DIST}{Logical. If TRUE (default), uses depth distribution method. If FALSE,
uses maximum depth method.}

\item{SEED}{Logical. If TRUE (default), sets random seed for reproducibility.}

\item{...}{Additional arguments passed to the depth function.}
}
\value{
A numeric vector of predicted class labels for the test data. The labels
correspond to the unique values in the training group labels.
}
\description{
Performs binary classification using data depth, as suggested by Makinde and
Fasoranbaku (2018).
}
\details{
This function implements depth-based classification using two approaches:
\itemize{
\item \strong{Maximum Depth (DIST=FALSE)}: Classifies each test point to the group for which
it has the maximum depth.
\item \strong{Depth Distribution (DIST=TRUE)}: Classifies based on the cumulative distribution
of depths, comparing where the test point's depth falls in each group's depth
distribution. Recommended method.
}

The training data can be specified in two ways:
\enumerate{
\item Using \code{train} and \code{g}: Provide all training data in \code{train} with group labels in \code{g}
\item Using \code{x1} and \code{x2}: Provide the two groups separately
}

Missing values are automatically removed from training and test data.

Common depth functions include:
\itemize{
\item \code{prodepth}: Projection depth (default, robust)
\item \code{zonoid}: Zonoid depth
\item Other depth functions following the same interface
}
}
\note{
This function requires the \code{class} package to be installed.
}
\examples{
\dontrun{
# Generate training data
set.seed(123)
x1 <- matrix(rnorm(50), ncol = 2)
x2 <- matrix(rnorm(50, mean = 2), ncol = 2)

# Generate test data
test <- matrix(rnorm(20, mean = 1), ncol = 2)

# Depth distribution method (recommended)
Depth.class(x1 = x1, x2 = x2, test = test, DIST = TRUE)

# Maximum depth method
Depth.class(x1 = x1, x2 = x2, test = test, DIST = FALSE)

# Using train/g specification
train <- rbind(x1, x2)
g <- c(rep(1, nrow(x1)), rep(2, nrow(x2)))
Depth.class(train = train, g = g, test = test)
}

}
\references{
Makinde, O. S., & Fasoranbaku, O. A. (2018). On classification based on depth functions.
Journal of Applied Statistics, 45(8), 1541-1555.
}
\seealso{
\code{\link{CLASS.fun}} for general classification dispatcher,
\code{\link{KNNdist}} for KNN with depths, \code{\link{CLASS.BAG}} for bagged version
}
