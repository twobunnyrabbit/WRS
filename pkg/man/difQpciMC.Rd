% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{difQpciMC}
\alias{difQpciMC}
\title{Quantile Difference Plot with Bootstrap CI and P-Values}
\usage{
difQpciMC(
  x,
  y = NULL,
  q = seq(5, 40, 5)/100,
  xlab = "Quantile",
  ylab = "Group 1 minus Group 2",
  plotit = TRUE,
  SEED = TRUE,
  alpha = 0.05,
  nboot = 1000
)
}
\arguments{
\item{x}{Numeric vector, matrix with 1-2 columns, or first group data.}

\item{y}{Optional second group data. If NULL and x is a matrix with 2 columns,
differences are computed as x\link{,1} - x\link{,2}.}

\item{q}{Numeric vector of quantiles to compare (must be < 0.5).
Default: seq(5,40,5)/100.}

\item{xlab}{Label for x-axis. Default: "Quantile".}

\item{ylab}{Label for y-axis. Default: "Group 1 minus Group 2".}

\item{plotit}{Logical. If TRUE (default), creates plot with sum estimates (*),
confidence interval bounds (+).}

\item{SEED}{Logical. If TRUE (default), sets random seed for reproducibility.}

\item{alpha}{Numeric. Significance level for confidence intervals and FWE control.
Default: 0.05.}

\item{nboot}{Integer. Number of bootstrap samples. Default: 1000.}
}
\value{
List with components:
\item{n}{Sample size after removing missing values}
\item{output}{Data frame with columns: quantile, Est_q, Est_1.minus.q, SUM,
ci.low, ci.up, p_crit (adjusted critical p-value), p-value, signif (YES/NO)}
}
\description{
Creates diagnostic plot to assess symmetry of difference scores around zero by
comparing quantile sums (q + 1-q quantiles). Provides bootstrap confidence
intervals and controls familywise error rate using Hochberg's method.
}
\details{
For symmetric distributions, the plot of quantile sums should be approximately
horizontal. If the median difference is zero, the line should pass through zero.

The function:
\itemize{
\item Computes q and 1-q quantiles for each specified quantile
\item Calculates bootstrap confidence intervals for the sum
\item Controls familywise error rate using Hochberg's method
\item Reports adjusted critical p-values for each test
}

Missing values are automatically removed.
}
\seealso{
\code{\link{DqdifMC}}
}
