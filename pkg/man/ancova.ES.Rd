% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{ancova.ES}
\alias{ancova.ES}
\title{ANCOVA Effect Size (KMS Analog) with Automatic Design Points}
\usage{
ancova.ES(
  x1,
  y1,
  x2,
  y2,
  pts = NULL,
  plotit = TRUE,
  xout = FALSE,
  outfun = outpro,
  xlab = "X",
  ylab = "KMS.effect",
  npts = 10,
  ql = 0.1,
  line = TRUE,
  ylim = NULL,
  ...
)
}
\arguments{
\item{x1}{Covariate values for group 1}

\item{y1}{Response values for group 1}

\item{x2}{Covariate values for group 2}

\item{y2}{Response values for group 2}

\item{pts}{Optional vector of design points. If NULL, uses npts equally-spaced
points in the overlapping range determined by ql quantiles}

\item{plotit}{Logical; if TRUE, plot effect sizes (default: TRUE)}

\item{xout}{Logical; if TRUE, remove outliers before analysis (default: FALSE)}

\item{outfun}{Function for detecting outliers (default: outpro)}

\item{xlab}{Label for x-axis (default: 'X')}

\item{ylab}{Label for y-axis (default: 'KMS.effect')}

\item{npts}{Number of design points to use when pts=NULL (default: 10)}

\item{ql}{Quantile level for determining overlapping range (default: .1)}

\item{line}{Logical; if TRUE, connect points with line in plot (default: TRUE)}

\item{ylim}{Y-axis limits for plot (default: NULL for automatic)}

\item{...}{Additional arguments (currently unused)}
}
\value{
Matrix with columns:
\item{pts}{Design points}
\item{Effect.size}{KMS effect size at each point}
}
\description{
Compute KMS analog effect size for comparing two independent regression lines.
If design points not specified, automatically selects points within the overlapping
quantile range of both groups' covariate distributions.
}
\note{
\itemize{
\item Only one covariate supported
\item Design points chosen in \link{max(q_ql^1, q_ql^2), min(q_{1-ql}^1, q_{1-ql}^2)}
where q_p^j is the p-th quantile of group j
}
}
\examples{
# Generate data with group differences
set.seed(111)
x1 <- runif(50, 0, 10)
y1 <- 2 + 0.5*x1 + rnorm(50, 0, 1.5)
x2 <- runif(50, 0, 10)
y2 <- 4 + 0.5*x2 + rnorm(50, 0, 2)

# Compute effect sizes at 10 design points
es <- ancova.ES(x1, y1, x2, y2, plotit = FALSE)
print(es)

# Use custom design points
es2 <- ancova.ES(x1, y1, x2, y2, pts = seq(2, 8, by = 1), plotit = FALSE)

}
