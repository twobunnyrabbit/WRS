% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{Stein.LC1}
\alias{Stein.LC1}
\title{Stein's Method for Single Linear Contrast}
\usage{
Stein.LC1(
  x,
  delta,
  con,
  alpha = 0.05,
  power = 0.8,
  z.sqrt = NULL,
  tr = 0,
  reps = 1e+05,
  SEED = TRUE
)
}
\arguments{
\item{x}{Data in list mode or matrix form with J groups.}

\item{delta}{Minimum detectable difference for the linear contrast.}

\item{con}{Vector of contrast coefficients (length J).}

\item{alpha}{Significance level. Default is 0.05.}

\item{power}{Desired power. Default is 0.8.}

\item{z.sqrt}{Optional pre-computed standardization factor. Default \code{NULL}
computes it from data.}

\item{tr}{Amount of trimming. Default is 0 (no trimming).}

\item{reps}{Number of Monte Carlo replications. Default is 100000.}

\item{SEED}{Logical. If \code{TRUE} (default), set random seed for reproducibility.}
}
\value{
List with components:
\item{n}{Current sample sizes for each group}
\item{N}{Required total sample sizes for each group}
\item{z.sqrt}{Standardization factor used in calculations}
}
\description{
For a single linear contrast, determines the total number of observations needed
in each group to achieve desired power when the contrast equals or exceeds delta.
This is the workhorse function called by \code{\link{Stein.LC}}.
}
\details{
This function implements Stein's two-stage method for a single linear contrast.
It uses winsorized standard deviations and accounts for trimming when estimating
required sample sizes.

The algorithm:
\enumerate{
\item Computes winsorized variances from initial data
\item Determines critical values via Monte Carlo simulation
\item Calculates required sample size for each group
}
}
\seealso{
\code{\link{Stein.LC}}, \code{\link{winsdN}}
}
\keyword{design}
\keyword{htest}
