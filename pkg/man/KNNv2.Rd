% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classification.R
\name{KNNv2}
\alias{KNNv2}
\title{K-Nearest Neighbors Classification (Version 2)}
\usage{
KNNv2(
  train = NULL,
  test = NULL,
  g = NULL,
  x1 = NULL,
  x2 = NULL,
  k = 3,
  prob = TRUE,
  SEED = NULL
)
}
\arguments{
\item{train}{Training data matrix or data frame (optional if x1 and x2 are provided).}

\item{test}{Test data matrix or data frame containing observations to classify.}

\item{g}{Group labels for training data (required if train is provided). Should be
a vector with two unique values indicating the two groups.}

\item{x1}{Training data for group 1 (alternative to train/g specification).}

\item{x2}{Training data for group 2 (alternative to train/g specification).}

\item{k}{Number of nearest neighbors to use (default: 3).}

\item{prob}{Logical. If TRUE (default), includes class probabilities in the result.}

\item{SEED}{Seed value for reproducibility. If NULL, no seed is set. Included for
compatibility when called by other functions.}
}
\value{
A numeric vector of predicted class labels (1 or 2) for the test data.
}
\description{
Performs binary classification using the K-nearest neighbors (KNN) method. This is
an alternative version of \code{\link{KNN}} that returns numeric class labels (1, 2)
instead of factor levels.
}
\details{
This function is a wrapper for \code{knn} from the class package. The key difference
from \code{\link{KNN}} is that it returns numeric class labels (1, 2) instead of
factor levels, which can be more convenient for downstream processing.

\strong{KNN algorithm:}
\enumerate{
\item For each test point, find the k nearest training points (by Euclidean distance)
\item Assign the class based on majority vote among the k neighbors
\item If \code{prob=TRUE}, also compute the proportion of votes for the winning class
}

The training data can be specified in two ways:
\enumerate{
\item Using \code{train} and \code{g}: Provide all training data in \code{train} with group labels in \code{g}
\item Using \code{x1} and \code{x2}: Provide the two groups separately
}

Missing values are automatically removed from training and test data.
}
\note{
This function requires the \code{class} package to be installed. For more information
about the underlying KNN implementation, see \code{?knn} from the class package.
}
\examples{
\dontrun{
# Generate training data
set.seed(123)
x1 <- matrix(rnorm(50), ncol = 2)
x2 <- matrix(rnorm(50, mean = 2), ncol = 2)

# Generate test data
test <- matrix(rnorm(20, mean = 1), ncol = 2)

# KNN classification with k=3
KNNv2(x1 = x1, x2 = x2, test = test, k = 3)

# Try different k values
KNNv2(x1 = x1, x2 = x2, test = test, k = 5)
KNNv2(x1 = x1, x2 = x2, test = test, k = 7)

# Using train/g specification
train <- rbind(x1, x2)
g <- c(rep(1, nrow(x1)), rep(2, nrow(x2)))
KNNv2(train = train, g = g, test = test, k = 3)
}

}
\seealso{
\code{\link{KNN}} for standard KNN, \code{\link{KNNdist}} for KNN with
data depths, \code{\link{CLASS.fun}} for general classification dispatcher
}
