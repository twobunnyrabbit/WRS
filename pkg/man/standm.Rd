% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00-utils-core.R
\name{standm}
\alias{standm}
\title{Standardize a Matrix}
\usage{
standm(x, locfun = lloc, est = mean, scat = var, ...)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{locfun}{Function to compute location for each column (default: lloc).}

\item{est}{Estimator function to use for computing location or other
statistics. Common choices include \code{mean}, \code{median}, \code{tmean} (trimmed mean),
\code{mom} (modified one-step M-estimator), \code{mest} (M-estimator).}

\item{scat}{Function to compute scale for each column (default: var).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
Standardized matrix with same dimensions as x.
}
\description{
Standardizes each column of a matrix by subtracting a location measure
and dividing by a scale measure.
}
\details{
Each column is transformed to (x - location) / sqrt(scale). Missing values
are removed before standardization.
}
\examples{
m <- matrix(rnorm(100, mean = c(5, 10)), ncol = 2)
standm(m)  # Standardized matrix

}
