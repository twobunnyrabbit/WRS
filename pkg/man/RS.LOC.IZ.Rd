% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{RS.LOC.IZ}
\alias{RS.LOC.IZ}
\title{Probability of Correct Decision with Indifference Zone}
\usage{
RS.LOC.IZ(n, J = NULL, locfun = tmean, delta, iter = 10000, SEED = TRUE, ...)
}
\arguments{
\item{n}{Integer or numeric vector. Sample size(s). If a single value, all J
groups have the same sample size. If a vector, specifies sample size for
each group.}

\item{J}{Integer. Number of groups. Required if \code{n} is a single value.
Ignored if \code{n} is a vector.}

\item{locfun}{Function for computing location estimate. Default is \code{\link{tmean}}
(20\% trimmed mean). Other options include \code{mean}, \code{median}, etc.}

\item{delta}{Numeric. Size of indifference zone (effect size). The advantage of
the best group over all others.}

\item{iter}{Integer. Number of Monte Carlo iterations (default: 10000).}

\item{SEED}{Logical. If \code{TRUE} (default), set random seed to 2 for
reproducibility.}

\item{...}{Additional arguments passed to \code{locfun}.}
}
\value{
List with components:
\item{PCD}{Numeric. Estimated probability of correct decision}
\item{PCD.ci}{Numeric vector of length 2. Confidence interval for PCD}
}
\description{
Estimates the probability of correct selection (PCS) for identifying the group
with the largest location parameter using an indifference zone approach.
}
\details{
This function uses Monte Carlo simulation to estimate the probability of
correctly selecting the group with the largest location parameter when the
best group has a location advantage of \code{delta} over all other groups.

The simulation assumes:
\itemize{
\item Normal distributions for all groups
\item Group 1 has location shifted by \code{delta}
\item All other groups have location 0
\item Equal variances across groups
}

For each iteration, data are generated, location estimates computed, and
success is counted when group 1 (the best group) has the highest estimate.
}
\seealso{
\code{\link{tmean}}, \code{\link{binom.conf}}
}
\keyword{design}
\keyword{htest}
