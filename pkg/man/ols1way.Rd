% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{ols1way}
\alias{ols1way}
\title{Heteroscedastic Test for Equality of Regression Lines (Multiple Groups)}
\usage{
ols1way(
  x,
  y,
  xout = FALSE,
  outfun = outpro,
  STAND = TRUE,
  alpha = 0.05,
  pr = TRUE,
  BLO = FALSE,
  HC3 = FALSE,
  ...
)
}
\arguments{
\item{x}{List of length J. Each element is a matrix or vector of predictors for one group.}

\item{y}{List of length J. Each element is a numeric vector of responses for one group.}

\item{xout}{Logical. If TRUE, removes leverage points using \code{outfun} (default: FALSE).}

\item{outfun}{Function. Outlier detection function (default: \code{\link{outpro}}).}

\item{STAND}{Logical. Passed to \code{outfun} for standardization (default: TRUE).}

\item{alpha}{Numeric. Significance level (default: 0.05).}

\item{pr}{Logical. If TRUE, prints results (default: TRUE).}

\item{BLO}{Logical. If TRUE, removes only bad leverage points (default: FALSE).}

\item{HC3}{Logical. If TRUE, uses HC3 estimator; otherwise HC4 (default: FALSE, use HC4).}

\item{...}{Additional arguments passed to \code{outfun}.}
}
\value{
List with components:
\describe{
\item{test}{Numeric. Test statistic}
\item{crit.val}{Numeric. Adjusted critical value}
\item{p.value}{Numeric. Approximate p-value}
\item{est}{Matrix (J × (p+1)). OLS coefficient estimates for each group}
\item{n}{Integer vector. Sample sizes after removing missing values}
\item{nv.keep}{Integer vector. Sample sizes after removing leverage points}
}
}
\description{
Tests whether J independent groups have equal regression parameters (intercepts
AND slopes) using heteroscedasticity-consistent standard errors (HC4 or HC3)
and a Johansen-type MANOVA test statistic.
}
\details{
\strong{Null hypothesis}: All J groups have identical regression lines
(same intercepts AND slopes).

\strong{Test procedure}:
\enumerate{
\item For each group j, fit OLS and compute HC4 (or HC3) covariance matrix
\item Compute pooled estimate \eqn{\bar{\beta}} as weighted average
\item Compute test statistic:
\deqn{F = \sum_j (\hat{\beta}_j - \bar{\beta})' W_j (\hat{\beta}_j - \bar{\beta})}
where \eqn{W_j} is the inverse HC4 covariance matrix
\item Apply Johansen correction for heteroscedasticity
\item Compare to adjusted chi-square critical value
}

\strong{Leverage point handling}:
\itemize{
\item \code{xout=TRUE}: Removes all detected leverage points
\item \code{BLO=TRUE}: Removes only "bad" leverage points (regression outliers)
}

\strong{Comparison}: To test equality of slopes ONLY (allowing different intercepts),
use \code{\link{ols1way2g}} for 2 groups.
}
\references{
Johansen, S. (1980). The Welch-James approximation to the distribution of
the residual sum of squares in a weighted linear regression. Biometrika, 67, 85-92.
}
\seealso{
\code{\link{ols1way2g}} for slope comparison in 2 groups,
\code{\link{olshc4}} for HC4 covariances, \code{\link{ols}}
}
