% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{ancmg1}
\alias{ancmg1}
\title{ANCOVA for Multiple Groups with Single Covariate}
\usage{
ancmg1(
  x,
  y,
  pool = TRUE,
  jcen = 1,
  fr = 1,
  depfun = fdepth,
  nmin = 8,
  op = 3,
  tr = 0.2,
  SEED = TRUE,
  pr = TRUE,
  pts = NA,
  con = 0,
  nboot = NA,
  alpha = 0.05,
  bhop = FALSE
)
}
\arguments{
\item{x}{Covariate data. Matrix with J columns (one per group) or list of length J.}

\item{y}{Outcome data. Matrix with J columns or list of length J.}

\item{pool}{Logical. If TRUE (default), pool data when determining design points.}

\item{jcen}{Group index to use as center when pool=FALSE. Default is 1.}

\item{fr}{Span for running interval smoother. Default is 1.}

\item{depfun}{Function to compute depth (not used for single covariate). Default is fdepth.}

\item{nmin}{Minimum sample size required at design points. Default is 8.}

\item{op}{Operation mode: 1=omnibus trimmed means, 2=omnibus medians,
3=multiple comparisons trimmed means (recommended), 4=multiple comparisons medians. Default is 3.}

\item{tr}{Trim proportion. Default is 0.2.}

\item{SEED}{Logical. Set random seed. Default is TRUE.}

\item{pr}{Logical. Print messages. Default is TRUE.}

\item{pts}{Design points. If NA (default), three points are chosen automatically.}

\item{con}{Contrast specification. Default is 0.}

\item{nboot}{Number of bootstrap samples. Default is NA.}

\item{alpha}{Alpha level. Default is 0.05.}

\item{bhop}{Logical. Use Benjamini-Hochberg. Default is FALSE.}
}
\value{
List with components: points.chosen, sample.sizes, point (test results), contrast.coef.
}
\description{
ANCOVA for J groups (J >= 2) with a single covariate, using trimmed means or medians.
For two or more covariates, use ancmg instead. For two groups, also consider ancova and ancGpar.
}
\details{
Single covariate version of ancmg. op=2 not recommended for tied values; use op=4.
}
\examples{
\dontrun{
  x <- cbind(rnorm(30), rnorm(30), rnorm(30))
  y <- cbind(rnorm(30), rnorm(30, 0.5), rnorm(30, 1))
  result <- ancmg1(x, y)
}
}
\keyword{ancova}
