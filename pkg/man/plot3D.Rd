% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot3D}
\alias{plot3D}
\title{Create a 3D Scatter or Surface Plot}
\usage{
plot3D(
  x,
  y,
  xlab = "X1",
  ylab = "X2",
  zlab = "Y",
  theta = 50,
  phi = 25,
  duplicate = "error",
  pc = "*",
  ticktype = "simple",
  expand = 0.5
)
}
\arguments{
\item{x}{Matrix with 2 columns containing the two predictor variables
(X1 and X2).}

\item{y}{Numeric vector of response values (Z).}

\item{xlab, ylab, zlab}{Axis labels (defaults: 'X1', 'X2', 'Y').}

\item{theta, phi}{Viewing angles for the 3D plot (defaults: 50, 25).
\code{theta} controls azimuthal direction, \code{phi} controls colatitude.}

\item{duplicate}{How to handle duplicate (x,y) points (default: "error").
Options: "error", "strip", "mean", etc. (passed to \code{akima::interp}).}

\item{pc}{Point character (currently unused, default: '*').}

\item{ticktype}{Tick mark type (default: 'simple').}

\item{expand}{Expansion factor for Z-axis (default: 0.5).}
}
\value{
Invisibly returns the perspective matrix from \code{persp()}.
Creates a 3D plot as a side effect.
}
\description{
Creates a 3D perspective plot (surface plot) using interpolation. Useful
for visualizing regression surfaces or any trivariate relationship.
}
\details{
The function uses the \code{akima} package's \code{interp()} function to
interpolate irregularly-spaced (X1, X2, Z) data onto a regular grid,
then creates a 3D surface plot using \code{persp()}.

\strong{Note}: The \code{scale} variable is referenced but not defined as a parameter.
This may cause an error. Typically \code{scale = TRUE} is desired.
}
\examples{
\dontrun{
# Create 3D surface from regression model
n <- 100
x1 <- rnorm(n)
x2 <- rnorm(n)
y <- 2*x1 + 3*x2 + rnorm(n, sd=0.5)
plot3D(cbind(x1, x2), y)
}
}
\seealso{
\code{\link{regp2plot}}, \code{\link{reg2g.p2plot}} for regression-specific 3D plots
}
