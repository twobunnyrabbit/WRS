% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{anc.bestpb.PV}
\alias{anc.bestpb.PV}
\title{Select Best Group with Overall P-Value - Bootstrap Version}
\usage{
anc.bestpb.PV(
  x,
  loc.fun = tmean,
  nboot = 2000,
  alpha = 0.05,
  iter = 5000,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{x}{Matrix or list of data where each column (or list element) is a group.}

\item{loc.fun}{Location estimator function (default: tmean for trimmed mean).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{iter}{Number of iterations for determining critical values (default: 5000).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{...}{Additional arguments passed to loc.fun.}
}
\value{
List with:
\item{Group.with.largest.est}{Index of group with largest estimate}
\item{Best}{Index of best group if significant, 'No Decision' otherwise}
\item{overall.pvalue}{Overall p-value for the selection decision}
\item{n}{Sample sizes}
\item{output}{Matrix with detailed comparison results}
}
\description{
Similar to anc.bestpb but also computes an overall p-value for the decision about
which group has the largest measure of location. Uses bootstrap percentile method.
}
\note{
The overall p-value is computed by comparing the observed decision pattern
to its null distribution over a range of alpha levels.
}
\examples{
\dontrun{
x <- list(rnorm(30), rnorm(30, mean=0.5), rnorm(30, mean=1))
result <- anc.bestpb.PV(x, nboot=2000)
result$overall.pvalue
}
}
