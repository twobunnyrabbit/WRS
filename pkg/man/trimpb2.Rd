% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{trimpb2}
\alias{trimpb2}
\title{Percentile Bootstrap CI for Difference in Trimmed Means}
\usage{
trimpb2(
  x,
  y,
  tr = 0.2,
  alpha = 0.05,
  nboot = 2000,
  WIN = FALSE,
  win = 0.1,
  plotit = FALSE,
  op = 4,
  SEED = TRUE
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{tr}{Trimming proportion (default: 0.2). Proportion of observations to
trim from each tail when computing winsorized correlation.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{WIN}{Logical. If \code{TRUE}, winsorizes data before bootstrapping (default: \code{FALSE}).}

\item{win}{Amount of Winsorizing to apply when \code{WIN = TRUE} (default: 0.1).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{op}{Plot type parameter for \code{g2plot} when \code{plotit = TRUE} (default: 4).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}
}
\value{
A list with components:
\itemize{
\item \code{Est1}: Trimmed mean for group 1
\item \code{Est2}: Trimmed mean for group 2
\item \code{p.value}: P-value for the test
\item \code{ci}: Two-element vector with confidence interval bounds
\item \code{est.dif}: Estimated difference (Est1 - Est2)
}
}
\description{
Computes a percentile bootstrap confidence interval for the difference
between two trimmed means. Independent groups are assumed.
}
\details{
This function computes a bootstrap confidence interval for the difference
between trimmed means of two independent groups. Missing values are automatically removed.

When \code{WIN = TRUE}, data are winsorized before bootstrapping. The amount of trimming
should be at least 0.2 when winsorizing. Sample sizes less than 15 may result in
poor control of Type I error when winsorizing.

If \code{plotit = TRUE}, creates a plot comparing the bootstrap distributions using \code{g2plot}.
}
\examples{
\dontrun{
set.seed(123)
x <- rnorm(50)
y <- rnorm(60, mean = 0.5)

# Basic usage
trimpb2(x, y)

# With winsorizing
trimpb2(x, y, WIN = TRUE, win = 0.1)

# With plotting
trimpb2(x, y, plotit = TRUE)
}
}
\seealso{
\code{\link{trimpb}}, \code{\link{pb2gen}}
}
