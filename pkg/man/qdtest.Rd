% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{qdtest}
\alias{qdtest}
\title{Test Equality of Quantiles for Two Dependent Groups}
\usage{
qdtest(x, y = NA, q = 0.5, bop = FALSE, nboot = 100, se.val = NA)
}
\arguments{
\item{x}{Numeric vector or matrix with 2 columns. If vector, must also specify \code{y}.}

\item{y}{Numeric vector for second group (ignored if \code{x} is a matrix).}

\item{q}{Numeric value in (0,1). Quantile to test (default: 0.5 for median).}

\item{bop}{Logical. If \code{TRUE}, uses bootstrap to estimate standard error;
if \code{FALSE} (default), uses analytical estimate via \code{\link{sedm}}.}

\item{nboot}{Integer. Number of bootstrap samples when \code{bop=TRUE} (default: 100).}

\item{se.val}{Numeric. If specified, uses this value for standard error instead
of computing it (default: \code{NA}).}
}
\value{
List with components:
\describe{
\item{test.stat}{t-statistic}
\item{p.value}{Two-sided p-value}
\item{se}{Standard error used}
}
}
\description{
Tests the hypothesis that the q-th quantile is equal for two dependent
(paired) groups. Uses a t-test with estimated standard error from either
analytical or bootstrap methods.
}
\details{
Appears to adequately control Type I error when n ≥ 20. Uses \code{\link{qest}}
for quantile estimation.
}
\seealso{
\code{\link{sedm}}, \code{\link{bootdse}}, \code{\link{qest}}
}
\keyword{htest}
