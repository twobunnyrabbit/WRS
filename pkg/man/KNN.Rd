% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classification.R
\name{KNN}
\alias{KNN}
\title{K-Nearest Neighbors Classification}
\usage{
KNN(
  train = NULL,
  test = NULL,
  g = NULL,
  x1 = NULL,
  x2 = NULL,
  k = 3,
  prob = TRUE,
  SEED = NULL
)
}
\arguments{
\item{train}{Training data matrix or data frame (optional if x1 and x2 are provided).}

\item{test}{Test data matrix or data frame containing observations to classify.}

\item{g}{Group labels for training data (required if train is provided). Should be
a vector with two unique values indicating the two groups.}

\item{x1}{Training data for group 1 (alternative to train/g specification).}

\item{x2}{Training data for group 2 (alternative to train/g specification).}

\item{k}{Number of nearest neighbors to use for classification (default: 3).}

\item{prob}{Logical. If TRUE (default), returns probability estimates. If FALSE,
returns only class labels.}

\item{SEED}{Seed value for reproducibility. If NULL, no seed is set. Included for
compatibility when called by other functions.}
}
\value{
A factor vector of predicted class labels for the test data, with attributes
containing probability estimates if prob=TRUE.
}
\description{
Performs binary classification using the K-nearest neighbors (KNN) method via
the class package.
}
\details{
K-nearest neighbors is a non-parametric classification method that assigns a test
observation to the most common class among its k nearest training observations.

The training data can be specified in two ways:
\enumerate{
\item Using \code{train} and \code{g}: Provide all training data in \code{train} with group labels in \code{g}
\item Using \code{x1} and \code{x2}: Provide the two groups separately (function creates labels automatically)
}

Missing values are automatically removed from training and test data.

The choice of k affects the bias-variance tradeoff:
\itemize{
\item Small k: More flexible, lower bias, higher variance
\item Large k: Less flexible, higher bias, lower variance
}
}
\note{
This function requires the \code{class} package to be installed.
}
\examples{
\dontrun{
# Generate training data
set.seed(123)
x1 <- matrix(rnorm(50), ncol = 2)
x2 <- matrix(rnorm(50, mean = 2), ncol = 2)

# Generate test data
test <- matrix(rnorm(20, mean = 1), ncol = 2)

# KNN classification with k=3
KNN(x1 = x1, x2 = x2, test = test, k = 3)

# KNN classification with k=5
KNN(x1 = x1, x2 = x2, test = test, k = 5)

# Using train/g specification
train <- rbind(x1, x2)
g <- c(rep(1, nrow(x1)), rep(2, nrow(x2)))
KNN(train = train, g = g, test = test, k = 3)
}

}
\seealso{
\code{\link{KNNdist}} for KNN using data depths, \code{\link{KNNv2}} for
alternative KNN implementation, \code{\link{CLASS.fun}} for general classification
dispatcher, \code{\link{knn}} for the underlying function
}
