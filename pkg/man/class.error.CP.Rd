% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classification.R
\name{class.error.CP}
\alias{class.error.CP}
\title{Classification Error with Confusion Matrix (Leave-One-Out)}
\usage{
class.error.CP(
  x1 = NULL,
  x2 = NULL,
  train = NULL,
  g = NULL,
  method = "KNN",
  nboot = 100,
  EN = TRUE,
  FAST = TRUE,
  AUC = FALSE,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{x1}{Training data for group 1 (alternative to train/g specification).}

\item{x2}{Training data for group 2 (alternative to train/g specification).}

\item{train}{Training data matrix or data frame (optional if x1 and x2 are provided).}

\item{g}{Group labels for training data (required if train is provided). Should be
a vector with two unique values indicating the two groups.}

\item{method}{Character string specifying single classification method. Available
methods: 'KNN' (K-nearest neighbors, default), 'DIS' (depth-based), 'DEP' (Makinde &
Fasoranbaku depth), 'SVM' (support vector machine), 'RF' (random forest), 'NN' (neural
network), 'ADA' (AdaBoost), 'PRO' (projection-based), 'LSM' (logistic regression).}

\item{nboot}{Number of samples to use for leave-one-out cross-validation (default: 100).
Limited by min(n1, n2).}

\item{EN}{Logical. If TRUE (default), use equal sample sizes for both groups to deal
with classification bias.}

\item{FAST}{Logical. Currently not used (included for consistency).}

\item{AUC}{Logical. If TRUE, computes area under ROC curve. Default is FALSE because
certain conditions can create errors. Requires ROCR package.}

\item{SEED}{Logical or numeric. If TRUE, sets seed to 2 for reproducibility. If numeric,
sets that seed value (default: TRUE).}

\item{...}{Additional arguments passed to the classification method.}
}
\value{
A list with components:
\item{C.MAT}{Confusion matrix showing proportions: rows are true classes, columns
are predicted classes}
\item{COUNTS}{Confusion matrix showing counts instead of proportions}
\item{AUC}{Area under ROC curve (if AUC=TRUE), otherwise NULL}
}
\description{
Estimates classification error for a single classification method using leave-one-out
cross-validation and returns a confusion matrix showing classification performance.
}
\details{
This function performs leave-one-out cross-validation:
\enumerate{
\item Randomly selects one observation from each group
\item Trains classifier on remaining data
\item Predicts class for the two held-out observations
\item Repeats \code{nboot} times (or min(n1, n2) times, whichever is smaller)
\item Summarizes results in a confusion matrix
}

\strong{Confusion matrix interpretation:}
\itemize{
\item Rows: True class labels
\item Columns: Predicted class labels
\item Diagonal elements: Correct classifications
\item Off-diagonal elements: Misclassifications
}

The training data can be specified in two ways:
\enumerate{
\item Using \code{train} and \code{g}: Provide all training data in \code{train} with group labels in \code{g}
\item Using \code{x1} and \code{x2}: Provide the two groups separately
}

Missing values are automatically removed from data.
}
\note{
\itemize{
\item Only one method can be analyzed at a time
\item ROCR package required if AUC=TRUE
\item Different methods may require different packages to be installed
}
}
\examples{
\dontrun{
# Generate two groups
set.seed(123)
x1 <- matrix(rnorm(50), ncol = 2)
x2 <- matrix(rnorm(50, mean = 2), ncol = 2)

# Get confusion matrix for KNN
result <- class.error.CP(x1 = x1, x2 = x2)
result$C.MAT  # Proportion matrix
result$COUNTS # Count matrix

# Try different method
class.error.CP(x1 = x1, x2 = x2, method = 'RF')

# Include AUC
class.error.CP(x1 = x1, x2 = x2, AUC = TRUE)
}

}
\seealso{
\code{\link{class.error.loo}} for multiple methods with leave-one-out,
\code{\link{class.error.com}} for cross-validation comparison,
\code{\link{CLASS.fun}} for general classification dispatcher
}
