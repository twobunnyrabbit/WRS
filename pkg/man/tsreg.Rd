% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression.R
\name{tsreg}
\alias{tsreg}
\title{Theil-Sen Regression Estimator}
\usage{
tsreg(
  x,
  y,
  xout = FALSE,
  outfun = outpro,
  iter = 5,
  varfun = pbvar,
  tr = FALSE,
  do.stre = TRUE,
  corfun = pbcor,
  plotit = FALSE,
  WARN = TRUE,
  HD = FALSE,
  OPT = FALSE,
  xlab = "X",
  ylab = "Y",
  ...
)
}
\arguments{
\item{x}{A numeric vector or matrix containing the predictor variable(s). For
multiple predictors, rows represent observations and columns represent variables.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{iter}{Number of iterations for the Gauss-Seidel algorithm when there are
multiple predictors (default: 5). Values of 1 can be unsatisfactory; values
of 10 may result in excessive execution time without improving Type I error rates.}

\item{varfun}{Function used to compute variance for strength of association
(default: \code{pbvar}, percentage bend midvariance). Can be any function that
computes a measure of variation.}

\item{tr}{Logical or numeric. If \code{TRUE} or numeric, uses Harrell-Davis estimator
for the intercept (default: \code{FALSE} for median).}

\item{do.stre}{Logical. If \code{TRUE}, computes strength of association measure
(default: \code{TRUE}). Set to \code{FALSE} when \code{varfun} cannot be computed.}

\item{corfun}{Correlation function used for computing explanatory power when
the variance ratio exceeds 1 (default: \code{pbcor}).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{WARN}{Logical. If \code{TRUE}, prints warning when measure of variation equals
zero (default: \code{TRUE}).}

\item{HD}{Logical. If \code{TRUE}, uses Harrell-Davis estimator for the intercept;
if \code{FALSE}, uses median (default: \code{FALSE}).}

\item{OPT}{Logical. If \code{TRUE}, computes intercept as \code{median(y) - b1*median(X)};
if \code{FALSE}, computes intercept as \code{median(y - b1*X)} (default: \code{FALSE}).
The default is consistent with other R implementations of Theil-Sen.}

\item{xlab}{Label for x-axis when \code{plotit=TRUE} (default: "X").}

\item{ylab}{Label for y-axis when \code{plotit=TRUE} (default: "Y").}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
A list with components:
\item{n}{Original sample size before outlier removal}
\item{n.keep}{Sample size after outlier removal (if \code{xout=TRUE})}
\item{coef}{Vector of regression coefficients (intercept followed by slopes)}
\item{residuals}{Vector of residuals}
\item{Strength.Assoc}{Strength of association (square root of explanatory power)}
\item{Explanatory.Power}{Proportion of variance explained}
}
\description{
Computes the Theil-Sen robust regression estimator, a non-parametric alternative
to least squares regression that is resistant to outliers. For multiple predictors,
uses a Gauss-Seidel algorithm for estimation.
}
\details{
The Theil-Sen estimator computes the slope as the median of all pairwise slopes
between points. For a single predictor, this is computed directly. For multiple
predictors, a Gauss-Seidel algorithm is used for iterative refinement.

The Theil-Sen estimator has a breakdown point of approximately 29\%, meaning
it remains robust even when up to 29\% of the data are outliers. This makes it
substantially more robust than ordinary least squares regression.

Strength of association is computed as the square root of the ratio of the
variance of fitted values to the variance of y (using \code{varfun}). If this
ratio exceeds 1, the squared correlation between fitted and observed values
is used instead.
}
\examples{
# Simple regression with one predictor
x <- rnorm(50)
y <- 2 + 3*x + rnorm(50)
result <- tsreg(x, y)
result$coef  # Intercept and slope

# Multiple regression
x <- matrix(rnorm(100), ncol=2)
y <- 1 + 2*x[,1] - 3*x[,2] + rnorm(50)
tsreg(x, y)

# With outlier removal
x <- c(rnorm(45), rep(10, 5))  # 5 outliers
y <- 2 + 3*x + rnorm(50)
tsreg(x, y, xout=TRUE)  # Remove outliers before fitting

}
\references{
Theil, H. (1950). A rank-invariant method of linear and polynomial regression
analysis. \emph{Indagationes Mathematicae}, 12, 85-91.

Sen, P. K. (1968). Estimates of the regression coefficient based on Kendall's tau.
\emph{Journal of the American Statistical Association}, 63, 1379-1389.

Wilcox, R. R. (2022). \emph{Introduction to Robust Estimation and Hypothesis
Testing} (5th ed.). Academic Press.
}
\seealso{
\code{\link{tshdreg}} for Theil-Sen with Harrell-Davis estimator,
\code{\link{tsp1reg}} for single predictor version, \code{\link{tsregNW}}
for neighborhood-weighted version, \code{\link{ltsreg}} for least trimmed
squares, \code{\link{opreg}} for outlier-pruned regression
}
