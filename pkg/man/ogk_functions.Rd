% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{ogk}
\alias{ogk}
\alias{OGK}
\title{OGK Robust Covariance Estimator (Orthogonalized Gnanadesikan-Kettenring)}
\usage{
ogk(x, sigmamu = taulc, v = gkcov, n.iter = 1, beta = 0.9, ...)

OGK(x, niter = 2, beta = 0.9, control)
}
\arguments{
\item{x}{Matrix (n × p). Data matrix.}

\item{sigmamu}{Function. Robust scale estimator with signature \code{sigmamu(x, mu.too=FALSE)}.
If \code{mu.too=FALSE}, returns robust scale; if \code{mu.too=TRUE}, returns
list with both location and scale (default: \code{\link{taulc}}).}

\item{v}{Function. Robust covariance estimator (default: \code{\link{gkcov}}).}

\item{n.iter}{Integer. Number of iterations; 1 seems optimal (default: 1).}

\item{beta}{Numeric. Coverage probability for consistency (default: 0.9).}

\item{...}{Additional arguments passed to \code{sigmamu}.}

\item{niter}{Integer. Number of reweighting iterations (default: 2).}

\item{control}{Control object. Additional control parameters for \code{rrcov::CovOgk}.}
}
\value{
List with components:
\describe{
\item{center}{Numeric vector of length p. Robust multivariate location estimate}
\item{cov}{Matrix (p × p). Robust weighted covariance matrix}
}

List with components:
\describe{
\item{center}{Numeric vector of length p. Robust multivariate location estimate}
\item{cov}{Matrix (p × p). Robust covariance matrix}
}
}
\description{
Computes robust weighted covariance matrix using the OGK (Orthogonalized
Gnanadesikan-Kettenring) method of Maronna and Zamar (2002).

Wrapper for the OGK (Orthogonalized Gnanadesikan-Kettenring) robust covariance
estimator from the \pkg{rrcov} package.
}
\details{
\strong{OGK algorithm} (Maronna & Zamar, 2002, Equation 7):
\enumerate{
\item Compute pairwise robust covariances using Gnanadesikan-Kettenring formula
\item Orthogonalize covariance matrix via eigendecomposition
\item Compute robust scales in principal component directions
\item Compute weights based on robust Mahalanobis distances
\item Recompute weighted covariance matrix
}

\strong{Key features}:
\itemize{
\item High breakdown point (inherits from \code{sigmamu})
\item Affine equivariant
\item Computationally efficient for moderate dimensions
\item Iteration improves finite-sample performance
}

This implementation uses the pairwise algorithm. For the rrcov package version,
see \code{\link{OGK}}.

This function provides a simple interface to \code{rrcov::CovOgk}, extracting
the center and covariance components from the S4 object returned by rrcov.

\strong{Advantages of rrcov version}:
\itemize{
\item More thoroughly tested implementation
\item Additional diagnostic outputs available
\item Integrated with rrcov ecosystem
}

\strong{Note}: Requires the \pkg{rrcov} package to be installed.

For a native implementation without external dependencies, use \code{\link{ogk}}.
}
\references{
Maronna, R. A. and Zamar, R. H. (2002). Robust estimates of location and
dispersion for high-dimensional datasets. Technometrics, 44, 307-317.
}
\seealso{
\code{\link{OGK}} for rrcov version, \code{\link{ogk.pairwise}},
\code{\link{gkcov}}, \code{\link{taulc}}

\code{\link{ogk}} for native implementation, \code{rrcov::CovOgk}
}
