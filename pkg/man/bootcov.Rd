% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{bootcov}
\alias{bootcov}
\title{Bootstrap Covariance Matrix for Multivariate Estimator}
\usage{
bootcov(x, est = median, nboot = 100, pr = TRUE, SEED = FALSE, ...)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{est}{Estimator function to use for computing location or other
statistics. Common choices include \code{mean}, \code{median}, \code{tmean} (trimmed mean),
\code{mom} (modified one-step M-estimator), \code{mest} (M-estimator).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{pr}{Logical. If \code{TRUE}, prints progress message (default: \code{TRUE}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{...}{Additional arguments passed to the estimator function \code{est}.}
}
\value{
Covariance matrix of the bootstrap estimates (p by p matrix,
where p is the number of variables).
}
\description{
Computes bootstrap estimates of squared standard errors and covariances
for a multivariate estimator applied to multivariate data.
}
\details{
For multivariate data, this function generates bootstrap samples and
computes the specified estimator for each variable. The covariance
matrix of the bootstrap estimates provides estimates of the squared
standard errors (diagonal elements) and covariances (off-diagonal
elements) for the estimator.

The input can be a matrix or list. Missing values are automatically removed.
}
\examples{
# Multivariate data
x <- matrix(rnorm(200), ncol=4)
cov_mat <- bootcov(x, est=median, nboot=500)
sqrt(diag(cov_mat))  # Bootstrap standard errors
}
