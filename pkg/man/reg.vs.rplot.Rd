% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{reg.vs.rplot}
\alias{reg.vs.rplot}
\title{Compare Regression Estimates vs Running Smoother Estimates}
\usage{
reg.vs.rplot(
  x,
  y,
  xout = FALSE,
  fr = 1,
  est = median,
  regfun = Qreg,
  Qreg.plot = TRUE,
  qv = c(0.25, 0.75),
  SMQ = FALSE,
  pr = TRUE,
  xlab = "Reg.Est",
  ylab = "Rplot.Est",
  pch = "*"
)
}
\arguments{
\item{x}{Numeric vector or matrix of predictor values.}

\item{y}{Numeric vector of response values.}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{fr}{Span for running interval smoother (default: 1).}

\item{est}{Measure of location for smoother (default: \code{median}).}

\item{regfun}{Regression function to use (default: \code{Qreg} for quantile
regression).}

\item{Qreg.plot}{Logical; if \code{TRUE} (default), uses quantile regression
plot. If \code{FALSE}, uses lowess plot.}

\item{qv}{Quantiles for quantile regression plot (default: c(0.25, 0.75)).}

\item{SMQ}{Logical; if \code{TRUE}, uses smoothed quantile regression
(default: \code{FALSE}).}

\item{pr}{Logical; if \code{TRUE} (default), prints update message.}

\item{xlab}{Label for x-axis (default: "Reg.Est").}

\item{ylab}{Label for y-axis (default: "Rplot.Est").}

\item{pch}{Plotting character (default: '*').}
}
\value{
Invisibly returns \code{NULL}. Creates diagnostic plot as side effect.
}
\description{
Diagnostic plot comparing predicted values from parametric regression
against nonparametric running interval smoother. Useful for assessing
linearity assumptions.
}
\details{
If the linear model is correct, points should cluster tightly around
a line with slope=1 and intercept=0 (shown as dashed reference line).
Systematic deviations indicate model misspecification.

The function:
\enumerate{
\item Computes regression predictions using \code{regYhat()}
\item Computes smoother predictions using \code{rplot.pred()}
\item Plots one against the other with reference line y=x
}
}
\examples{
# Linear relationship - should follow y=x line
x <- rnorm(100)
y <- 2*x + rnorm(100)
reg.vs.rplot(x, y)

# Nonlinear relationship - will deviate from y=x
y_nl <- x^2 + rnorm(100)
reg.vs.rplot(x, y_nl)
}
\seealso{
\code{\link{reg.vs.lplot}}, \code{\link{regYhat}},
\code{\link{rplot.pred}}
}
