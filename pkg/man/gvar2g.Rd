% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{gvar2g}
\alias{gvar2g}
\title{Test Equality of Generalized Variances (Two Independent Groups)}
\usage{
gvar2g(
  x,
  y,
  nboot = 100,
  DF = TRUE,
  eop = 1,
  est = skipcov,
  alpha = 0.05,
  cop = 3,
  op = 1,
  MM = FALSE,
  SEED = TRUE,
  pr = FALSE,
  fast = FALSE,
  ...
)
}
\arguments{
\item{x}{A matrix or data frame for group 1 (must have ncol > 1).}

\item{y}{A matrix or data frame for group 2 (must have ncol > 1).}

\item{nboot}{Number of bootstrap samples (default: 100).}

\item{DF}{Logical. If \code{TRUE} (default), uses \code{skipcov} with adjusted
critical values for equal sample sizes when \code{alpha = 0.05}.}

\item{eop}{Parameter for \code{skipcov} (see \code{\link{skipcov}}).}

\item{est}{Covariance estimator function. Default is \code{\link{skipcov}}.
Other choices: \code{var}, \code{covmcd}, \code{covmve}, \code{covroc}.}

\item{alpha}{Significance level (default: 0.05). Adjusted critical values
only available for alpha = 0.05.}

\item{cop}{Parameter for \code{skipcov} controlling outlier detection method
(default: 3). See \code{\link{skipcov}}.}

\item{op}{Outlier detection method for \code{skipcov}. \code{op = 1} uses
MGV method; \code{op = 2} uses projection method (default: 1).}

\item{MM}{Logical. For \code{skipcov}: \code{FALSE} uses boxplot method,
\code{TRUE} uses MAD-MEDIAN method (default: FALSE).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed to 2 for reproducibility.}

\item{pr}{Logical. If \code{TRUE}, prints progress during bootstrap.}

\item{fast}{Logical. If \code{TRUE}, uses \code{skipcov.for} if available.}

\item{...}{Additional arguments passed to the covariance estimator.}
}
\value{
A list with components:
\item{p.value}{Bootstrap p-value for testing equal generalized variances}
\item{adjusted.crit.level}{Adjusted critical level (if available), otherwise NA}
\item{ratio.of.estimates}{Ratio of generalized variance estimates (group 1 / group 2)}
\item{n1}{Sample size of group 1}
\item{n2}{Sample size of group 2}
}
\description{
Tests the hypothesis that two independent groups have equal generalized
variances using a bootstrap approach with robust covariance estimation.
}
\details{
This function tests whether two independent multivariate samples have equal
generalized variances (determinants of covariance matrices).

\strong{Bootstrap Procedure}:
\enumerate{
\item Compute difference in generalized variances for original samples
\item Bootstrap: resample each group with replacement \code{nboot} times
\item Compute generalized variance difference for each bootstrap sample
\item P-value is proportion of bootstrap differences with opposite sign
}

\strong{Adjusted Critical Values} (when \code{DF = TRUE}, equal n, alpha = 0.05):
Available for p = 2, 3, 4, 5, 6 dimensions with minimum sample sizes.

The function uses robust covariance estimators to handle outliers. Default
is \code{skipcov}, which combines projection-based outlier detection with
robust estimation.
}
\examples{
\dontrun{
# Two bivariate groups
x <- matrix(rnorm(100), 50, 2)
y <- matrix(rnorm(120, sd = 1.5), 60, 2)  # Different variance

# Test equality of generalized variances
gvar2g(x, y, nboot = 500)
}

}
\seealso{
\code{\link{gvar}}, \code{\link{gvarg}}, \code{\link{rgvar}},
\code{\link{skipcov}}
}
\keyword{htest}
\keyword{multivariate}
