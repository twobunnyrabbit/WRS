% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{rd2plot}
\alias{rd2plot}
\title{Plot Expected Frequency Curves for Two Groups}
\usage{
rd2plot(x, y, fr = 0.8, xlab = "", ylab = "")
}
\arguments{
\item{x}{Numeric vector for group 1.}

\item{y}{Numeric vector for group 2.}

\item{fr}{Span parameter controlling smoothing (default: 0.8). Larger values
produce smoother curves.}

\item{xlab, ylab}{Axis labels (defaults: empty strings).}
}
\value{
Invisibly returns \code{NULL}. Creates a plot as a side effect.
}
\description{
Creates smoothed expected frequency curves for two independent groups,
useful for comparing distributions. The curves show the relative density
at each point based on local neighborhoods.
}
\details{
For each value in a group, the expected frequency is computed as the
proportion of observations in a neighborhood defined by \code{fr}. The
function then plots smoothed frequency curves for both groups:
\itemize{
\item Group 1 (x): solid line
\item Group 2 (y): dashed line
}

Higher curve values indicate regions of higher data density.
}
\examples{
# Compare distributions of two groups
x <- rnorm(100, mean=0)
y <- rnorm(100, mean=1)
rd2plot(x, y)
}
\seealso{
\code{\link{g2plot}}, \code{\link{splot}}, \code{\link{near}}
}
