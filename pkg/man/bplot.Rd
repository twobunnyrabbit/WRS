% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{bplot}
\alias{bplot}
\title{Split Data by Quantiles and Compare Groups}
\usage{
bplot(x, y, q = c(0.25, 0.5, 0.75), vals = NULL, FUN = lincon, ...)
}
\arguments{
\item{x}{Predictor variable (vector) used for splitting into groups.}

\item{y}{Response variable (vector) to be compared across groups.}

\item{q}{Quantiles of x to use for splitting (default: c(.25, .5, .75), creating
4 groups based on quartiles). Ignored if vals is specified.}

\item{vals}{Specific values of x to use for splitting (default: NULL). If provided,
overrides the q argument.}

\item{FUN}{Function to apply for group comparisons and plotting (default: lincon,
linear contrasts). Can be any function that accepts grouped data.}

\item{...}{Additional arguments passed to FUN.}
}
\value{
Output from the comparison function specified by FUN.
}
\description{
Splits the response variable into groups based on quantiles (or specified values)
of a predictor variable, then compares and plots the groups using a specified
comparison function.
}
\details{
This function implements a common strategy in robust statistics: examining how
a response variable behaves across different levels of a predictor by:
\enumerate{
\item Splitting the predictor x into groups based on quantiles or specified values
\item Grouping the response y accordingly
\item Comparing the groups using the specified function (FUN)
}

\strong{Default behavior} (vals = NULL):
\itemize{
\item Uses quantiles specified in q to create groups
\item q = c(.25, .5, .75) creates 4 groups: (0-25\%), (25-50\%), (50-75\%), (75-100\%)
}

\strong{Custom split points} (vals specified):
\itemize{
\item Groups are formed based on the specified values
\item Useful for meaningful cutoffs (e.g., clinical thresholds)
}

The grouped data is passed to FUN for comparison and plotting. Common choices:
\itemize{
\item \code{lincon}: Linear contrasts with multiple comparisons
\item \code{t1way}: One-way ANOVA with trimmed means
\item \code{med1way}: One-way ANOVA with medians
}
}
\examples{
# Split data into quartiles and compare groups
set.seed(123)
x <- rnorm(100)
y <- 2 * x + rnorm(100)
bplot(x, y, FUN = t1way)

# Use specific cutoff values
bplot(x, y, vals = c(-1, 0, 1), FUN = lincon)
}
\seealso{
\code{\link{split.mat}}, \code{\link{lincon}}, \code{\link{t1way}}
}
