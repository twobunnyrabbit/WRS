% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00-utils-core.R
\name{rdplot}
\alias{rdplot}
\title{Running Interval (Expected Frequency) Density Plot}
\usage{
rdplot(
  x,
  fr = NA,
  plotit = TRUE,
  theta = 50,
  phi = 25,
  expand = 0.5,
  pyhat = FALSE,
  pts = NA,
  xlab = "X",
  ylab = "",
  ticktype = "simple"
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{fr}{Fraction controlling amount of smoothing (default: 0.8 for univariate,
0.6 for multivariate data).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{theta, phi}{Angles for 3D perspective plot (defaults: 50, 25).}

\item{expand}{Expansion factor for persp plot (default: 0.5).}

\item{pts}{Points at which to estimate density. If NA, uses data points.}

\item{xlab, ylab}{Axis labels for plot.}

\item{ticktype}{Type of tick marks (default: "simple").}
}
\value{
A vector of density estimates at the specified points (or data points).
}
\description{
Computes and plots an expected frequency curve (robust density estimate)
for univariate or bivariate data.
}
\details{
For univariate data, uses a running interval smoother based on robust
scale (MAD). For bivariate data, creates a 3D perspective plot of the
density surface.
}
\examples{
x <- rnorm(100)
rdplot(x)

}
