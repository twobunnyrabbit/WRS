% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{Bagplot}
\alias{Bagplot}
\title{Create a Bagplot for Bivariate Data}
\usage{
Bagplot(
  x,
  plotit = TRUE,
  colorbag = NULL,
  colorloop = NULL,
  colorchull = NULL,
  databag = TRUE,
  dataloop = TRUE,
  plot.fence = FALSE,
  type = "hdepth"
)
}
\arguments{
\item{x}{Matrix or data frame with 2 columns containing bivariate data.}

\item{plotit}{Logical; if \code{TRUE}, creates the plot (default: TRUE).}

\item{colorbag}{Color for the bag region (50\\% depth contour). NULL uses default.}

\item{colorloop}{Color for the loop region (fence). NULL uses default.}

\item{colorchull}{Color for the convex hull. NULL uses default.}

\item{databag}{Logical; if \code{TRUE}, plots points in the bag region.}

\item{dataloop}{Logical; if \code{TRUE}, plots points in the loop region.}

\item{plot.fence}{Logical; if \code{TRUE}, plots the fence boundary.}

\item{type}{Character string specifying depth measure (default: 'hdepth'):
\itemize{
\item \code{'hdepth'}: Halfspace depth (Tukey depth)
\item \code{'projdepth'}: Projection depth
\item \code{'sprojdepth'}: Skewness-adjusted projection depth
}}
}
\value{
A bagplot object from the \code{mrfDepth} package. Creates a plot as
a side effect.
}
\description{
Creates a bagplot, which is a bivariate generalization of the boxplot. Uses
data depth to identify the "bag" (analogous to the box in a boxplot),
"loop" (fence), and outliers.
}
\details{
The bagplot extends the boxplot concept to two dimensions using data depth:
\itemize{
\item \strong{Bag}: Contains the 50\\% deepest points (analogous to the IQR)
\item \strong{Loop}: Extends from the bag (analogous to the whiskers)
\item \strong{Outliers}: Points beyond the loop
}

\strong{Requires}: The \code{mrfDepth} and \code{ggplot2} packages must be installed.
}
\examples{
\dontrun{
# Bivariate data with outliers
x <- cbind(rnorm(100), rnorm(100))
x <- rbind(x, c(5, 5))  # Add an outlier
Bagplot(x)
}
}
\references{
Rousseeuw, P.J., Ruts, I., and Tukey, J.W. (1999). The bagplot: A bivariate
boxplot. \emph{The American Statistician}, 53, 382-387.
}
\seealso{
\code{\link{outpro}} for outlier detection using projection methods
}
