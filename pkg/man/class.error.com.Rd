% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classification.R
\name{class.error.com}
\alias{class.error.com}
\title{Compare Classification Methods Using Cross-Validation}
\usage{
class.error.com(
  x1 = NULL,
  x2 = NULL,
  train = NULL,
  g = NULL,
  method = NULL,
  pro.p = 0.8,
  nboot = 100,
  EN = FALSE,
  FAST = TRUE,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{x1}{Training data for group 1 (alternative to train/g specification).}

\item{x2}{Training data for group 2 (alternative to train/g specification).}

\item{train}{Training data matrix or data frame (optional if x1 and x2 are provided).}

\item{g}{Group labels for training data (required if train is provided). Should be
a vector with two unique values indicating the two groups.}

\item{method}{Character vector specifying classification methods to compare. Available
methods: 'KNN' (K-nearest neighbors), 'DIS' (depth-based), 'DEP' (Makinde & Fasoranbaku
depth), 'SVM' (support vector machine), 'RF' (random forest), 'NN' (neural network),
'ADA' (AdaBoost), 'PRO' (projection-based), 'LSM' (logistic/smooth logistic regression).
If NULL, all methods are compared (except 'PRO' if FAST=TRUE).}

\item{pro.p}{Proportion of data to use for training (default: 0.8 = 80\%).}

\item{nboot}{Number of bootstrap samples for cross-validation (default: 100).}

\item{EN}{Logical. If TRUE, use equal sample sizes for both groups to deal with
classification bias. If FALSE, use the natural ratio n1/n2 (default: FALSE).}

\item{FAST}{Logical. If TRUE (default), excludes 'PRO' method which can be slow for
large samples.}

\item{SEED}{Logical or numeric. If TRUE, sets seed to 2 for reproducibility. If numeric,
sets that seed value (default: TRUE).}

\item{...}{Additional arguments passed to individual classification methods.}
}
\value{
A list with component:
\item{Error.rates}{A 3 x J matrix (J = number of methods) with rows:
\itemize{
\item TE: Total Error rate (proportion of misclassifications)
\item FP: False Positive rate (proportion of x1 classified as x2)
\item FN: False Negative rate (proportion of x2 classified as x1)
}}
}
\description{
Compares multiple classification methods using cross-validation coupled with
resampling to estimate error rates (total error, false positives, false negatives).
}
\details{
This function performs cross-validation by:
\enumerate{
\item Randomly sampling \code{pro.p} proportion of data for training
\item Using the remaining data for testing
\item Repeating \code{nboot} times
\item Computing average error rates across all bootstrap samples
}

\strong{Error rate definitions:}
\itemize{
\item \strong{Total Error (TE)}: Overall proportion of misclassifications
\item \strong{False Positive (FP)}: Proportion of group 1 observations misclassified as group 2
(e.g., predict fracture when no fracture occurred)
\item \strong{False Negative (FN)}: Proportion of group 2 observations misclassified as group 1
(e.g., predict no fracture when fracture occurred)
}

The training data can be specified in two ways:
\enumerate{
\item Using \code{train} and \code{g}: Provide all training data in \code{train} with group labels in \code{g}
\item Using \code{x1} and \code{x2}: Provide the two groups separately
}

Missing values are automatically removed from data.
}
\note{
\itemize{
\item The 'PRO' method can take several minutes for large samples
\item Different methods may require different packages to be installed
}
}
\examples{
\dontrun{
# Generate two groups
set.seed(123)
x1 <- matrix(rnorm(50), ncol = 2)
x2 <- matrix(rnorm(50, mean = 2), ncol = 2)

# Compare all fast methods
class.error.com(x1 = x1, x2 = x2)

# Compare specific methods
class.error.com(x1 = x1, x2 = x2, method = c('KNN', 'RF', 'SVM'))

# Use 90\% of data for training
class.error.com(x1 = x1, x2 = x2, pro.p = 0.9)

# Use equal sample sizes for testing
class.error.com(x1 = x1, x2 = x2, EN = TRUE)
}

}
\seealso{
\code{\link{class.error.loo}} for leave-one-out cross-validation,
\code{\link{class.error.CP}} for confusion matrix with single method,
\code{\link{CLASS.fun}} for general classification dispatcher
}
