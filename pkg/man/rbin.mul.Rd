% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{rbin.mul}
\alias{rbin.mul}
\title{Generate Correlated Multivariate Binomial Data}
\usage{
rbin.mul(n, N, prob = 0.5, p = 2)
}
\arguments{
\item{n}{Integer. Number of observations (rows).}

\item{N}{Integer. Number of trials for each binomial variable.}

\item{prob}{Numeric. Success probability for the binomial draws (default: 0.5).}

\item{p}{Integer. Number of correlated binomial variables to generate (default: 2).}
}
\value{
A matrix with \code{n} rows and \code{p} columns, where each column is
a binomial variable and columns have pairwise correlations of approximately 0.5.
}
\description{
Generates multivariate binomial data with pairwise correlation of approximately 0.5
by creating correlated variables through a common component.
}
\details{
The function generates correlations by:
\enumerate{
\item Generating (p+1) independent binomial variables
\item Adding the (p+1)-th variable to each of the first p variables
}

This creates a common component that induces positive correlation between
all pairs of variables. The theoretical correlation is approximately 0.5.
}
\examples{
\dontrun{
# Generate 100 observations of 3 correlated binomial variables
x <- rbin.mul(n=100, N=10, prob=0.5, p=3)
cor(x)  # Should show correlations around 0.5
}

}
\seealso{
\code{\link[stats]{rbinom}}
}
\keyword{datagen}
