% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{Danc.grid}
\alias{Danc.grid}
\title{Grid-Based ANCOVA for Dependent Groups with Two Covariates}
\usage{
Danc.grid(
  x,
  y1,
  y2,
  alpha = 0.05,
  DIF = TRUE,
  METHOD = "TR",
  AUTO = TRUE,
  PVSD = FALSE,
  Qsplit1 = 0.5,
  Qsplit2 = 0.5,
  SV1 = NULL,
  SV2 = NULL,
  tr = 0.2,
  PB = FALSE,
  est = tmean,
  nboot = 1000,
  xout = FALSE,
  outfun = outpro,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{x}{Covariate matrix with 2 columns (two covariates required).}

\item{y1, y2}{Outcome vectors for the two dependent conditions.}

\item{alpha}{Significance level. Default is 0.05.}

\item{DIF}{Logical. Analyze difference scores. Default is TRUE.}

\item{METHOD}{Comparison method: 'TR' (trimmed mean), 'TRPB' (percentile bootstrap),
'MED' (median), 'AD' (adaptive), or 'SIGN' (sign test). Default is 'TR'.}

\item{AUTO}{Logical. Automatically select method. Default is TRUE.}

\item{PVSD}{Logical. Use p-value standard deviation criterion. Default is FALSE.}

\item{Qsplit1, Qsplit2}{Quantiles for splitting first and second covariates.
Default is 0.5 (median split) for each.}

\item{SV1, SV2}{Alternatively, specific split values for covariates. Default is NULL.}

\item{tr}{Trimming proportion for METHOD='TR'. Default is 0.2.}

\item{PB}{Logical. Use percentile bootstrap. Default is FALSE.}

\item{est}{Estimator function. Default is tmean.}

\item{nboot}{Number of bootstrap samples. Default is 1000.}

\item{xout}{Logical. Remove outliers. Default is FALSE.}

\item{outfun}{Outlier detection function. Default is outpro.}

\item{SEED}{Logical. Set random seed. Default is TRUE.}

\item{...}{Additional arguments.}
}
\value{
List with grid intervals, grid structure, and effect sizes for each region.
}
\description{
Compare two dependent groups using grid-based ANCOVA with two covariates.
Splits covariate space into regions and compares groups within each region.
}
\details{
Splits the covariate space into a grid based on quantiles (Qsplit1, Qsplit2) or
specific values (SV1, SV2). For example, Qsplit1=c(0.25, 0.5, 0.75) and Qsplit2=0.5
creates a 4x2 grid. Compares dependent groups within each grid cell.
}
\examples{
\dontrun{
  x <- matrix(rnorm(100), 50, 2)
  y1 <- rnorm(50)
  y2 <- y1 + 0.5 + rnorm(50)
  result <- Danc.grid(x, y1, y2, Qsplit1 = c(0.25, 0.75))
}
}
\seealso{
\code{\link{Dancova}}, \code{\link{Dancovamp}}
}
\keyword{ancova}
\keyword{dependent}
\keyword{grid}
\keyword{multivariate}
