% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{disker}
\alias{disker}
\title{Effect Size via Kernel Density Classification}
\usage{
disker(x, y, z = NA, plotit = FALSE, op = 1)
}
\arguments{
\item{x}{Numeric vector. First group.}

\item{y}{Numeric vector. Second group.}

\item{z}{Numeric vector or NA. Optional values to classify (default: NA).}

\item{plotit}{Logical. If TRUE, produces plot (default: FALSE).}

\item{op}{Integer. Type of kernel density estimator:
\itemize{
\item 1: Rosenblatt's shifted histogram (default)
\item 2: Adaptive kernel estimate based on expected frequency curve
}}
}
\value{
List with components:
\describe{
\item{phat}{Proportion of group 1 observations correctly classified (apparent effect size)}
\item{zhat}{Vector of 0s and 1s classifying values in \code{z} (1 = classified as group 1)}
}
}
\description{
Estimates effect size using probability of correct classification based on kernel
density estimation. Computes the proportion of observations from the first group
that would be correctly classified based on comparing kernel density estimates.
}
\details{
This function provides an effect size measure based on distributional overlap:
\enumerate{
\item Estimates kernel densities for both groups
\item For each observation in group 1, classifies it as coming from group 1 if
\eqn{f_1(x_i) > f_2(x_i)} where \eqn{f_1} and \eqn{f_2} are the density estimates
\item Computes \code{phat} = proportion of group 1 observations correctly classified
}

\strong{Interpretation}:
\itemize{
\item \code{phat} ≈ 0.5: Large overlap, small effect size
\item \code{phat} ≈ 1.0: Little overlap, large effect size
\item \code{phat} between 0.5 and 1.0: Moderate effect size
}

If \code{z} is provided, the function also classifies these values as belonging
to group 1 (coded as 1) or group 2 (coded as 0) based on the density ratio.

This approach is sensitive to distributional differences beyond location shifts,
including differences in spread and shape.
}
\examples{
# Compare two groups with kernel-based classification
set.seed(123)
x <- rnorm(50, mean = 0)
y <- rnorm(50, mean = 1)
result <- disker(x, y)
result$phat  # Proportion correctly classified
}
\seealso{
\code{\link{akerd}}, \code{\link{rdplot}}
}
\keyword{htest}
\keyword{nonparametric}
