% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{scorregciH}
\alias{scorregciH}
\title{Skipped Correlation Regression CI with Hochberg Adjustment}
\usage{
scorregciH(
  x,
  y,
  nboot = 500,
  alpha = 0.05,
  SEED = TRUE,
  corfun = pcor,
  outfun = outpro,
  crit.pv = NULL,
  ALL = TRUE,
  MC = TRUE,
  pvals = NULL,
  iter = 500,
  pval.SEED = TRUE,
  pr = TRUE,
  STOP = TRUE
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{nboot}{Number of bootstrap samples (default: 500).}

\item{alpha}{Significance level (default: 0.05). Each CI has approximately
1-alpha coverage, but FWE is approximately alpha.}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{corfun}{Correlation function to use. Common options: \code{pbcor} (percentage
bend), \code{pcor} (Pearson), \code{wincor} (winsorized), \code{spear} (Spearman).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{crit.pv}{Pre-computed critical p-values (default: \code{NULL}).}

\item{ALL}{Logical. Eliminate outliers jointly from cbind(x, y) if \code{TRUE} (default),
or separately for each (x\link{,j}, y) pair if \code{FALSE}.}

\item{MC}{Logical. Use parallel processing (default: \code{TRUE}).}

\item{pvals}{Pre-computed p-value distribution (default: \code{NULL}).}

\item{iter}{Number of iterations for critical p-value simulation (default: 500).}

\item{pval.SEED}{Logical. Set seed when computing critical p-values (default: \code{TRUE}).}

\item{pr}{Logical. Print progress messages (default: \code{TRUE}).}

\item{STOP}{Logical. Stop with error if only one predictor (default: \code{TRUE}).}
}
\value{
List with components:
\item{p.values}{P-values for testing zero correlation with each predictor.}
\item{cor.est}{Vector of correlation estimates for each predictor.}
\item{confidence.int}{Matrix with columns: Var j, ci.low, ci.up.}
\item{hoch.adjusted.p.values}{Hochberg-adjusted p-values.}
}
\description{
Tests zero correlation between y and each predictor in x using skipped
correlation, controlling FWE with Hochberg adjustment.
}
\details{
Tests H0: rho(y, x_j) = 0 for j = 1,...,p while controlling familywise error
rate at approximately alpha using Hochberg's adjustment.

Each confidence interval has approximately 1-alpha coverage, but the
simultaneous coverage is not 1-alpha. It's possible for Hochberg to fail
to reject when confidence intervals exclude zero.

For a single predictor (p=1), use \code{scorci()} instead.
}
\examples{
\dontrun{
set.seed(123)
x <- matrix(rnorm(50 * 3), ncol = 3)
y <- 0.5 * x[, 1] + rnorm(50)
scorregciH(x, y, nboot = 500)
}
}
\seealso{
\code{\link{scorreg}}, \code{\link{scorregci}}, \code{\link{scorci}}, \code{\link{mscorci}}
}
