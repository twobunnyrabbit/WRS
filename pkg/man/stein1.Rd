% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{stein1}
\alias{stein1}
\title{Stein's Two-Stage Method - First Stage (Classical)}
\usage{
stein1(x, del, alpha = 0.05, pow = 0.8, oneside = FALSE, n = NULL, VAR = NULL)
}
\arguments{
\item{x}{Vector of initial observations.}

\item{del}{Minimum detectable difference from hypothesized mean (effect size).}

\item{alpha}{Significance level. Default is 0.05.}

\item{pow}{Desired power. Default is 0.8.}

\item{oneside}{Logical. If \code{TRUE}, use one-sided test. Default is \code{FALSE}
(two-sided test).}

\item{n}{Optional. Sample size (default uses \code{length(x)}).}

\item{VAR}{Optional. Variance estimate (default uses \code{var(x)}).}
}
\value{
Integer: Required total sample size N.
}
\description{
Performs first stage of Stein's two-stage method for sample size determination.
Determines how many total observations are needed to achieve desired power for
detecting a mean difference of size del from a hypothesized value.
}
\details{
This implements the classical Stein's method using standard variance estimates.
Based on initial data, it calculates the required total sample size N to achieve
the specified power.

If N > n (current sample size), collect N - n additional observations and proceed
to second stage using \code{\link{stein2}}.

For robust version with trimming, use \code{\link{stein1.tr}}.
}
\seealso{
\code{\link{stein2}}, \code{\link{stein1.tr}}, \code{\link{Stein2g}}
}
\keyword{design}
\keyword{htest}
