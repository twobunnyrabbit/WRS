% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{qdec2ci}
\alias{qdec2ci}
\title{Compare Deciles Between Two Dependent Groups}
\usage{
qdec2ci(
  x,
  y = NA,
  nboot = 500,
  alpha = 0.05,
  pr = FALSE,
  SEED = TRUE,
  plotit = TRUE
)
}
\arguments{
\item{x}{Numeric vector for group 1, or a matrix with two columns (in which
case \code{y} is ignored).}

\item{y}{Numeric vector for group 2 (ignored if \code{x} is a matrix).}

\item{nboot}{Integer. Number of bootstrap samples (default: 500).}

\item{alpha}{Numeric. Alpha level for confidence intervals (default: 0.05).}

\item{pr}{Logical. If \code{TRUE}, prints progress information (default: \code{FALSE}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed to 2 (default: \code{TRUE}).}

\item{plotit}{Logical. If \code{TRUE}, creates a plot of decile differences and
confidence intervals (default: \code{TRUE}).}
}
\value{
Matrix with one row per decile and columns for estimates, differences,
confidence intervals, and significance indicators.
}
\description{
Compares the 9 deciles between two dependent (paired) groups using single order
statistics for quantile estimation. Uses percentile bootstrap for confidence
intervals. Does not allow tied values.
}
\details{
Stops with error if tied values are detected. For data with ties, use
\code{\link{Dqcomhd}} instead. Uses \code{\link{qest}} for quantile estimation.
}
\seealso{
\code{\link{Dqcomhd}}, \code{\link{qest}}, \code{\link{qdec}}
}
\keyword{htest}
