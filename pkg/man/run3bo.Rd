% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{run3bo}
\alias{run3bo}
\title{3D Running Interval Smoother with Bootstrap}
\usage{
run3bo(
  x,
  y,
  fr = 1,
  est = tmean,
  theta = 50,
  phi = 25,
  nmin = 0,
  pyhat = FALSE,
  eout = FALSE,
  outfun = out,
  plotit = TRUE,
  xout = FALSE,
  nboot = 40,
  SEED = TRUE,
  STAND = TRUE,
  expand = 0.5,
  scale = FALSE,
  xlab = "X",
  ylab = "Y",
  zlab = "",
  ticktype = "simple",
  ...
)
}
\arguments{
\item{x}{An n by p matrix of predictors (p > 1 for 3D plotting)}

\item{y}{Vector of response values}

\item{fr}{Span parameter controlling amount of smoothing (default: 1)}

\item{est}{Measure of location to use for smoothing (default: tmean)}

\item{theta}{Azimuthal viewing angle for 3D plot (default: 50)}

\item{phi}{Colatitude viewing angle for 3D plot (default: 25)}

\item{nmin}{Minimum number of points needed to estimate y|x (default: 0)}

\item{pyhat}{Logical: if TRUE, return predicted values; if FALSE, return "Done" (default: FALSE)}

\item{eout}{Logical: remove outliers based on (x,y) jointly (default: FALSE)}

\item{outfun}{Outlier detection function (default: out)}

\item{plotit}{Logical: create 3D plot (default: TRUE)}

\item{xout}{Logical: remove outliers based on x values (default: FALSE)}

\item{nboot}{Number of bootstrap samples (default: 40)}

\item{SEED}{Logical: set random seed for reproducibility (default: TRUE)}

\item{STAND}{Logical: standardize data for outlier detection (default: TRUE)}

\item{expand}{Relative length of z-axis in plot (default: 0.5)}

\item{scale}{Logical: scale the plot (default: FALSE)}

\item{xlab}{Label for x-axis (default: "X")}

\item{ylab}{Label for y-axis (default: "Y")}

\item{zlab}{Label for z-axis (default: "")}

\item{ticktype}{Type of tick marks for 3D plot (default: "simple")}

\item{...}{Additional arguments passed to est function}
}
\value{
A list with component:
\item{output}{"Done" if pyhat=FALSE, or vector of predicted values if pyhat=TRUE}
}
\description{
Compute a 3D running interval smoother with bootstrap aggregating (bagging).
Uses bootstrap resampling to create a more stable smooth surface estimate
for multivariate predictors.
}
\details{
This function implements a 3D running interval smoother with bootstrap
aggregating to reduce variance. For each point, the function:
\enumerate{
\item Draws nboot bootstrap samples from the data
\item Computes a running interval smooth for each bootstrap sample
\item Averages the bootstrap smooths to get final estimates
}

When plotting with p=2 predictors, creates a perspective plot of the
smoothed surface. Missing values are automatically removed.
}
\seealso{
\code{\link{rung3hat}}, \code{\link{runmbo}}, \code{\link{runm3d}}
}
\keyword{bootstrap}
\keyword{nonparametric}
\keyword{smooth}
