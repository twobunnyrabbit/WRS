% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{difQplot}
\alias{difQplot}
\title{Plot Quantile Sums to Assess Symmetry About Zero}
\usage{
difQplot(x, y = NULL, xlab = "Quantile", ylab = "Effect Size")
}
\arguments{
\item{x}{Data vector, or matrix/data.frame with 2 columns for paired data.}

\item{y}{Optional second vector for paired data (default: NULL).}

\item{xlab}{Label for X-axis (default: "Quantile").}

\item{ylab}{Label for Y-axis (default: "Effect Size").}
}
\value{
None. Creates a plot.
}
\description{
Creates a plot to assess whether a distribution is symmetric about zero by
plotting the sum of q and (1-q) quantiles. Useful for examining the symmetry
of difference scores or effect sizes.
}
\details{
This function assesses distributional symmetry about zero by plotting the sum
of complementary quantiles: Q(q) + Q(1-q) for various values of q.

\strong{For paired data} (x and y provided, or x is a 2-column matrix): Computes
differences x - y and plots quantile sums.

\strong{For single sample} (only x provided): Directly plots quantile sums of x.

\strong{Interpretation}:
\itemize{
\item \strong{Symmetric distribution about zero}: Plot should be approximately a horizontal
line at y = 0 (since Q(q) + Q(1-q) ≈ 0 when symmetric about 0).
\item \strong{Symmetric about median ≠ 0}: Plot should be approximately horizontal but
not necessarily at y = 0.
\item \strong{Asymmetric distribution}: Plot will show curvature or trend.
}

The function plots quantile sums for 9 quantiles (0.1 through 0.9) using
the Harrell-Davis estimator (\code{hd}), which provides smooth quantile estimates.

Missing values are automatically removed.
}
\examples{
# Symmetric distribution about zero
set.seed(123)
x <- rnorm(100, mean = 0, sd = 1)
difQplot(x)

# Skewed distribution
y <- rexp(100, rate = 1)
difQplot(y)

# Paired data
pre <- rnorm(50, mean = 10, sd = 2)
post <- pre + rnorm(50, mean = 1, sd = 1)  # Symmetric shift
difQplot(pre, post)
}
\seealso{
\code{\link{hd}}, \code{\link{sband}}, \code{\link{g2plotdifxy}}
}
