% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{sum.T}
\alias{sum.T}
\title{Sum of Independent T Distributions}
\usage{
\method{sum}{T}(df, reps = 1e+05, con)
}
\arguments{
\item{df}{Numeric vector of degrees of freedom for each t distribution}

\item{reps}{Integer. Number of Monte Carlo replications (default: 100000)}

\item{con}{Numeric vector of weights/coefficients for each t distribution}
}
\value{
Numeric vector of length \code{reps} containing simulated values from the
distribution of the weighted sum
}
\description{
Estimates the distribution of a weighted sum of independent t random variables
via Monte Carlo simulation.
}
\details{
This function estimates the distribution of \eqn{T = \sum_{j=1}^{K} c_j T_j}
where each \eqn{T_j} follows an independent t distribution with degrees of
freedom \code{df[j]} and weight \code{con[j]}.

The distribution is approximated via Monte Carlo simulation with the specified
number of replications. Larger values of \code{reps} provide more accurate
approximations at the cost of computation time.
}
\examples{
# Sum of two t distributions with equal weights
dist <- sum.T(df = c(5, 10), reps = 10000, con = c(1, 1))
hist(dist)
quantile(dist, c(0.025, 0.975))

}
\keyword{distribution}
