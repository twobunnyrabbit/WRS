% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{corbMC}
\alias{corbMC}
\title{Bootstrap Correlation CI with Parallel Processing}
\usage{
corbMC(x, y, corfun = pbcor, nboot = 599, alpha = 0.05, SEED = TRUE, ...)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{corfun}{Correlation function to use. Common options: \code{pbcor} (percentage
bend), \code{pcor} (Pearson), \code{wincor} (winsorized), \code{spear} (Spearman).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
List with components:
\item{cor.ci}{Bootstrap confidence interval.}
\item{p.value}{Two-sided p-value.}
\item{cor.est}{Point estimate of correlation.}
}
\description{
Computes a percentile bootstrap confidence interval for correlation using
parallel processing via \code{mclapply()}.
}
\details{
This is the parallel version of \code{corb()}, using \code{mclapply()} for faster
computation on multi-core systems. The default correlation is percentage
bend, but any correlation function can be used via \code{corfun}.

\strong{Note}: \code{mclapply()} does not work on Windows. Use \code{corb()} instead on
Windows systems.

When using \code{corfun = scor}, set \code{plotit = FALSE} to avoid plotting issues
in parallel execution.
}
\examples{
\dontrun{
# Fast bootstrap on multi-core systems (not Windows)
set.seed(123)
x <- rnorm(100)
y <- 0.6 * x + rnorm(100)
corbMC(x, y, nboot = 2000)  # Much faster than corb()

# Using Spearman correlation
corbMC(x, y, corfun = spear, nboot = 2000)
}
}
\seealso{
\code{\link{corb}}, \code{\link{scorciMC}}, \code{\link{pbcor}}
}
