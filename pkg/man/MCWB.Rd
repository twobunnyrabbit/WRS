% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{MCWB}
\alias{MCWB}
\title{Multiple Comparisons with the Best (Trimmed Means)}
\usage{
MCWB(x, tr = 0.2, alpha = 0.05, SEED = TRUE, REPS = 5000, ...)
}
\arguments{
\item{x}{Data in list, matrix, or data frame format. Each group should be a
separate element (list) or column (matrix/data frame).}

\item{tr}{Trimming proportion (default: 0.2)}

\item{alpha}{Familywise error rate (default: 0.05)}

\item{SEED}{Logical or numeric. Controls random seed. Default is \code{TRUE}.}

\item{REPS}{Number of simulation replications for computing critical value (default: 5000)}

\item{...}{Additional arguments passed to \code{\link{lincon}}}
}
\value{
A list with components:
\item{n}{Sample sizes for each group}
\item{tests}{Test statistics from \code{\link{lincon}}}
\item{psihat}{Matrix of contrast estimates, standard errors, CIs, and p-values}
\item{con}{Contrast matrix used (from \code{\link{conCON}})}
\item{Best.Group}{Index of the group with largest trimmed mean}
\item{Est.}{Vector of trimmed means for all groups}
\item{IND.p.values}{Individual (unadjusted) p-values for each comparison}
\item{p.crit}{Adjusted critical p-value for controlling FWE}
\item{num.sig}{Number of significant comparisons at adjusted level}
}
\description{
Performs multiple comparisons with the best (MCB) procedure for J independent
groups using trimmed means, controlling familywise error rate (FWE) when all
groups have a common trimmed mean.
}
\details{
The MCB procedure:
\enumerate{
\item Identifies the group with the largest trimmed mean
\item Constructs contrasts comparing this "best" group with all others
\item Computes critical p-value via simulation using \code{\link{MCWB.crit}}
\item Performs linear contrasts using \code{\link{lincon}}
\item Counts how many comparisons are significant at the adjusted level
}

The critical p-value is computed to control FWE at level alpha under the
null hypothesis that all groups have equal trimmed means.
}
\seealso{
\code{\link{MCWB.crit}}, \code{\link{lincon}}, \code{\link{conCON}}
}
\keyword{htest}
