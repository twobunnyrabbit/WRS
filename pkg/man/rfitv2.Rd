% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{rfitv2}
\alias{rfitv2}
\title{Modified Rank-Based Regression (rfitv2)}
\usage{
rfitv2(
  formula,
  data,
  subset,
  yhat0 = NULL,
  scores = Rfit::wscores,
  symmetric = FALSE,
  TAU = "F0",
  ...
)
}
\arguments{
\item{formula}{A formula object specifying the model (e.g., \code{y ~ x}).}

\item{data}{Optional data frame containing the variables in \code{formula}.}

\item{subset}{Optional vector specifying a subset of observations to use.}

\item{yhat0}{Optional initial fitted values. If \code{NULL}, uses quantile
regression for initialization.}

\item{scores}{Score function to use (default: \code{Rfit::wscores} for
Wilcoxon scores).}

\item{symmetric}{Logical; if \code{TRUE}, uses signed-rank estimate of
intercept (default: \code{FALSE}).}

\item{TAU}{Method for computing tau estimate. Options: 'F0', 'R', 'N'
(default: 'F0').}

\item{...}{Additional arguments (currently unused).}
}
\value{
An object of class \code{"rfit"} containing:
\item{coefficients}{Estimated coefficients}
\item{residuals}{Residuals}
\item{fitted.values}{Fitted values}
\item{scores}{Score function used}
\item{x}{Design matrix}
\item{y}{Response vector}
\item{tauhat}{Estimated tau (scale parameter)}
\item{taushat}{Studentized tau estimate}
\item{disp}{Dispersion value at solution}
}
\description{
A modified version of the \code{rfit()} function from the \pkg{Rfit} package,
incorporating code from \pkg{quantreg}. Implements rank-based regression
using Jaeckel's dispersion function with additional options.
}
\details{
This function combines elements from the \pkg{Rfit} and \pkg{quantreg} packages
(both under GPL). It:
\enumerate{
\item Orthogonalizes the design matrix using QR decomposition
\item Initializes using quantile regression (or provided \code{yhat0})
\item Minimizes Jaeckel's dispersion function using \code{jaeckel()}
\item Computes scale estimates and transforms back to original coordinates
}

The function includes a check for convergence and will attempt to re-fit
with jittered initial values if convergence fails.

\strong{Note:} This is an internal implementation used by \code{\link{rfit.est}}.
Users should typically use \code{\link{Rfit}} or \code{\link{rfit.est}} instead.
}
\seealso{
\code{\link{Rfit}}, \code{\link{rfit.est}}
}
\keyword{internal}
\keyword{regression}
