% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{wincorci}
\alias{wincorci}
\title{Winsorized Correlation with Bootstrap CI}
\usage{
wincorci(x, y, nboot = 1000, alpha = 0.05, SEED = TRUE, MC = FALSE, tr = 0.2)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{MC}{Logical. If \code{TRUE}, uses parallel processing (default: \code{FALSE}).}

\item{tr}{Trimming proportion (default: 0.2). Proportion of observations to
trim from each tail when computing winsorized correlation.}
}
\value{
List with components:
\item{cor.ci}{Bootstrap confidence interval.}
\item{p.value}{Two-sided p-value.}
\item{cor.est}{Winsorized correlation estimate.}
}
\description{
Computes bootstrap confidence interval for winsorized correlation.
}
\details{
Wrapper that calls \code{corb()} (or \code{corbMC()}) with \code{corfun = wincor}.
Winsorized correlation replaces extreme values with less extreme ones
before computing Pearson correlation.
}
\examples{
set.seed(123)
x <- c(rnorm(45), 10, 11, 12, 13, 14)  # Some outliers
y <- c(0.7 * rnorm(45), -10, -11, 12, 13, -14)
wincorci(x, y, tr = 0.2, nboot = 500)

# Less trimming
wincorci(x, y, tr = 0.1, nboot = 500)
}
\seealso{
\code{\link{wincor}}, \code{\link{corb}}, \code{\link{pbcor}}
}
