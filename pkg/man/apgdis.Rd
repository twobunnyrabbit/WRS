% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{apgdis}
\alias{apgdis}
\title{Approximate Depth Based on Multivariate Distance}
\usage{
apgdis(m, est = sum, se = TRUE, ...)
}
\arguments{
\item{m}{Numeric matrix (n by d) of multivariate data. Missing values are removed.}

\item{est}{Function to aggregate distances (default: \code{sum}). Applied to
distance vector for each point.}

\item{se}{Logical. If \code{TRUE} (default), ensures proper ordering of distance
matrix by transposing if needed.}

\item{...}{Additional arguments passed to \code{est}.}
}
\value{
List with components:
\item{center}{Coordinates of the deepest point (minimum distance)}
\item{distance}{Vector of aggregated distances for each point}
}
\description{
For multivariate data, computes distance between each pair of points and measures
depth of a point in terms of its distance to all other points. Generalizes
\code{\link{apdis}} to arbitrary dimensions.
}
\details{
Computes Euclidean distance from each point to all other points using the
\code{\link[stats]{dist}} function, then aggregates these distances using
\code{est}. The point with minimum aggregated distance is the "deepest" or
most central point.

The \code{se=TRUE} option ensures the distance matrix is properly oriented for
row-wise aggregation.
}
\seealso{
\code{\link{apdis}}, \code{\link[stats]{dist}}
}
\keyword{multivariate}
\keyword{robust}
