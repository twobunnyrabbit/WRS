% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{qcor.ci}
\alias{qcor.ci}
\title{Quantile Regression Correlation CI (Li et al. Method)}
\usage{
qcor.ci(
  x,
  y,
  q = 0.5,
  alpha = 0.05,
  nboot = 599,
  SEED = TRUE,
  xout = TRUE,
  method = "PRO",
  regfun = Qreg
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{q}{Quantile to use (default: 0.5 for median).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{nboot}{Number of bootstrap samples (default: 599).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{xout}{Logical. Remove outliers before analysis (default: \code{TRUE}).}

\item{method}{Outlier detection method (default: 'PRO').}

\item{regfun}{Regression function for outlier detection (default: \code{Qreg}).}
}
\value{
List with components:
\item{Est.}{Estimated quantile correlation.}
\item{ci}{Bootstrap confidence interval.}
\item{p.value}{Two-sided p-value for testing zero correlation.}
}
\description{
Bootstrap confidence interval for quantile regression correlation using the
method by Li, Li & Tsai (2015).
}
\details{
Computes quantile regression correlation using the approach by Li, Li & Tsai
(2015). Different from \code{qcorp1.ci()} in how correlation is calculated.

For n <= 40, outlier removal is skipped to avoid computational issues.
Currently supports single independent variable only.
}
\examples{
\dontrun{
set.seed(123)
x <- rnorm(50)
y <- 2 * x + rnorm(50)
qcor.ci(x, y, q = 0.5, nboot = 500)
}
}
\references{
Li, Y., Li, T., & Tsai, C. L. (2015). Quantile correlations and quantile
autoregressive modeling. Journal of the American Statistical Association,
110(509), 246-261.
}
\seealso{
\code{\link{qcor}}, \code{\link{qcorp1.ci}}, \code{\link{qcor.EP}}
}
