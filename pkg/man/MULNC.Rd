% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{MULNC}
\alias{MULNC}
\title{Bivariate Analog of Cliff's Method (Dominance Test)}
\usage{
MULNC(
  x1,
  x2,
  alpha = 0.05,
  SEED = TRUE,
  nboot = 500,
  nullval = 0.5,
  PV = FALSE,
  pr = TRUE
)
}
\arguments{
\item{x1}{First group data matrix (n1 × 2)}

\item{x2}{Second group data matrix (n2 × 2)}

\item{alpha}{Significance level (default: 0.05)}

\item{SEED}{Logical; if TRUE sets random seed (default: TRUE)}

\item{nboot}{Number of bootstrap samples (currently unused) (default: 500)}

\item{nullval}{Null hypothesis value for P(PL) (default: 0.5)}

\item{PV}{Logical; if TRUE computes p-value for unequal sample sizes via bootstrap (default: FALSE)}

\item{pr}{Logical; if TRUE prints messages (default: TRUE)}
}
\value{
A list with components:
\item{phat.GT}{Estimated probability that x1 dominates x2}
\item{phat.LT}{Estimated probability that x2 dominates x1}
\item{d.ci}{Confidence interval for Pr(PL) - Pr(PG)}
\item{phat}{Estimated probability that x2 dominance is more likely than x1 dominance}
\item{phat.ci}{Confidence interval for phat}
\item{p.value}{P-value for testing Pr(PL) = Pr(PG) (if n1=n2, or if PV=TRUE)}
}
\description{
Performs a bivariate analog of Cliff's method (related to Wilcoxon-Mann-Whitney)
to test whether one bivariate distribution dominates another in the sense that
both coordinates tend to be larger.
}
\details{
Defines dominance as: x1\link{i,} dominates x2\link{j,} if x1\link{i,1} > x2\link{j,1} AND x1\link{i,2} > x2\link{j,2}.
Computes Cliff-type statistic comparing rates of dominance in both directions.

For equal sample sizes, p-value computed analytically. For unequal sizes, set PV=TRUE
for bootstrap p-value.
}
\seealso{
\code{\link{MULNC.sub}}, \code{\link{cid}}
}
