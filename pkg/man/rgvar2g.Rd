% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{rgvar2g}
\alias{rgvar2g}
\title{Test Equality of Robust Generalized Variances for Two Groups}
\usage{
rgvar2g(
  x,
  y,
  nboot = 100,
  est = covmcd,
  alpha = 0.05,
  cop = 3,
  op = 2,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{x}{A matrix or data frame containing data for group 1.}

\item{y}{A matrix or data frame containing data for group 2.}

\item{nboot}{Number of bootstrap samples for computing standard errors (default: 100).}

\item{est}{Function for estimating covariance matrix (default: \code{covmcd}).
See \code{\link{rgvar}} for available options.}

\item{alpha}{Nominal alpha level (currently not used in function).}

\item{cop}{Parameter value (currently not used in function).}

\item{op}{Parameter value (currently not used in function).}

\item{SEED}{Logical; if \code{TRUE} (default), set random seed to 2 for reproducibility.}

\item{...}{Additional arguments passed to the covariance estimation function.}
}
\value{
Test statistic value. Large absolute values indicate departure from H0.
}
\description{
Tests the hypothesis of equal generalized variances for two independent groups
using robust covariance estimators and bootstrap standard errors.
}
\details{
The function tests H0: generalized variance of group 1 = generalized variance of group 2.

Bootstrap standard errors are computed for each group using \code{\link{rgvarseb}},
and the test statistic is the difference in generalized variances divided by the
pooled standard error: (GV1 - GV2) / sqrt(SE1^2 + SE2^2).
}
\seealso{
\code{\link{rgvar}}, \code{\link{rgvarseb}}, \code{\link{gvar2g}}
}
\keyword{htest}
\keyword{multivariate}
\keyword{robust}
