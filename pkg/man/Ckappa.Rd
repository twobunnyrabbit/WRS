% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{Ckappa}
\alias{Ckappa}
\title{Cohen's Kappa and Weighted Kappa}
\usage{
Ckappa(x, fleiss = FALSE, w = NULL)
}
\arguments{
\item{x}{Square matrix of agreement frequencies (rater 1 in rows, rater 2 in columns)}

\item{fleiss}{Logical: if TRUE, use Fleiss quadratic weights; if FALSE (default),
use exponential weights w = 0.5^|i-j|}

\item{w}{Optional custom weight matrix (same dimensions as x). If provided,
overrides \code{fleiss} parameter}
}
\value{
List with components:
\item{kappa}{Unweighted Cohen's kappa coefficient}
\item{weighted.kappa}{Weighted kappa coefficient}
}
\description{
Computes Cohen's kappa coefficient for inter-rater agreement, including
both unweighted and weighted versions. Supports Fleiss weights or custom
weighting schemes.
}
\details{
Cohen's kappa measures inter-rater agreement beyond chance agreement.
Values range from -1 to +1:
\itemize{
\item 1 = perfect agreement
\item 0 = agreement no better than chance
\item < 0 = agreement worse than chance
}

\strong{Weight schemes}:
\itemize{
\item Fleiss (quadratic): w\link{i,j} = 1 - (i-j)^2 / (p-1)^2
\item Exponential (default): w\link{i,j} = 0.5^|i-j|
\item Custom: provide weight matrix w
}

Weighted kappa gives partial credit for near-agreement, useful for ordinal categories.
}
\seealso{
\code{\link[psych]{cohen.kappa}} in the psych package
}
\keyword{htest}
\keyword{multivariate}
