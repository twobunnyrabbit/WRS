% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{zwe}
\alias{zwe}
\title{Zuo's Weighted Estimator of Location}
\usage{
zwe(x, k = 3, C = 0.2)
}
\arguments{
\item{x}{Numeric vector.}

\item{k}{Tuning constant controlling steepness of weight function (default: 3).
Larger values downweight outliers more aggressively.}

\item{C}{Threshold constant (default: 0.2). Observations with standardized distance
≥ C receive full weight; those < C are downweighted.}
}
\value{
Numeric scalar: weighted estimate of location.
}
\description{
Computes Zuo's (2010) weighted estimator of location, which downweights
outliers based on their standardized distance from the median.
}
\details{
The estimator uses adaptive weights based on each observation's standardized
distance from the median:
\deqn{SD_i = |x_i - median(x)| / MAD(x)}
\deqn{D_i = 1/(SD_i + 1)}

Weights are computed as:
\itemize{
\item \code{w_i = 1} if \code{D_i >= C} (close to median)
\item \code{w_i = [exp(-k(1 - D_i^2/C^2)^2) - exp(-k)] / (1 - exp(-k))} if \code{D_i < C}
}

The weighted estimate is: \eqn{\sum w_i x_i / \sum w_i}

Missing values are automatically removed.
}
\references{
Zuo, Y. (2010). Robust location and scatter estimators in multivariate analysis.
In Y. Dodge (Ed.), \emph{The Oxford Handbook of Nonparametric Statistics}.
}
\seealso{
\code{\link{onestep}}, \code{\link{mom}}, \code{\link{tmean}}
}
\keyword{robust}
\keyword{univar}
