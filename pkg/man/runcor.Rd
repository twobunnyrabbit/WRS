% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{runcor}
\alias{runcor}
\title{Running Correlation}
\usage{
runcor(
  x,
  y,
  z,
  fr = 1,
  corflag = FALSE,
  corfun = pbcor,
  plotit = TRUE,
  rhat = FALSE
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{z}{Numeric vector. The moderator variable.}

\item{fr}{Numeric. Controls the fraction/span for smoothing (default: 1).
Values near 0 use narrow windows, values near 1 use wide windows.}

\item{corflag}{Logical. If \code{TRUE}, uses Pearson correlation; if \code{FALSE}
(default), uses \code{corfun}.}

\item{corfun}{Correlation function to use. Common options: \code{pbcor} (percentage
bend), \code{pcor} (Pearson), \code{wincor} (winsorized), \code{spear} (Spearman).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{rhat}{Logical. If \code{TRUE}, returns the vector of running correlations;
if \code{FALSE} (default), returns "Done" after plotting.}
}
\value{
If \code{rhat = TRUE}, returns a vector of running correlations
corresponding to each value of z. Otherwise returns "Done" (used for side
effect of plotting).
}
\description{
Estimates how the correlation between x and y varies as a function of a
third variable z using a running interval method.
}
\details{
For each observation i, the correlation between x and y is computed using
observations where z is "near" z\link{i}. The nearness is determined by the \code{fr}
parameter.

This is useful for detecting effect modification or context-dependent
associations.
}
\examples{
# Correlation varies with moderator
set.seed(123)
n <- 100
z <- runif(n, 0, 10)
x <- rnorm(n)
y <- (0.2 + 0.08 * z) * x + rnorm(n)  # Correlation increases with z
runcor(x, y, z, fr = 0.5, rhat = TRUE)
}
\seealso{
\code{\link{cori}}, \code{\link{pbcor}}
}
