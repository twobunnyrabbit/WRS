% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{twocor}
\alias{twocor}
\title{Compare Two Correlation Coefficients from Independent Groups}
\usage{
twocor(
  x1,
  y1,
  x2,
  y2,
  corfun = pbcor,
  nboot = 599,
  alpha = 0.05,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{x1}{First variable for group 1.}

\item{y1}{Second variable for group 1.}

\item{x2}{First variable for group 2.}

\item{y2}{Second variable for group 2.}

\item{corfun}{Correlation function to use (default: \code{pbcor} for percentage bend).
Must return correlation in \code{$cor} component. Can use \code{wincor}, \code{pbcor}, etc.}

\item{nboot}{Number of bootstrap samples (default: 599).}

\item{alpha}{Significance level for confidence interval (default: 0.05).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducibility.}

\item{...}{Additional arguments passed to correlation function.}
}
\value{
List with components:
\item{r1}{Correlation for group 1.}
\item{r2}{Correlation for group 2.}
\item{ci.dif}{Confidence interval for r1 - r2.}
\item{p.value}{Two-sided p-value for testing r1 = r2.}
}
\description{
Computes bootstrap confidence interval for the difference between two
correlation coefficients from independent groups. Works with any correlation
function that returns results in \code{$cor} component.
}
\details{
Uses bootstrap resampling to construct confidence interval for r1 - r2.
Bootstrap samples are taken independently from each group, and the difference
in correlations is computed for each bootstrap sample.

\strong{Note}: For Pearson's correlation specifically, use \code{\link{twopcor}} instead,
which may be more efficient.

The function works with any robust correlation estimator that follows the
convention of returning the correlation in a component named \code{$cor}.
}
\seealso{
\code{\link{twopcor}}, \code{\link{pbcor}}, \code{\link{wincor}}
}
\keyword{correlation}
\keyword{robust}
