% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{rplotsm}
\alias{rplotsm}
\title{Running Interval Smoother with Strength of Association}
\usage{
rplotsm(
  x,
  y,
  est = tmean,
  fr = 1,
  plotit = TRUE,
  pyhat = FALSE,
  nboot = 40,
  atr = 0,
  nmin = 0,
  pch = "*",
  outfun = outpro,
  eout = FALSE,
  xlab = "X",
  ylab = "Y",
  scat = TRUE,
  SEED = TRUE,
  expand = 0.5,
  scale = FALSE,
  STAND = TRUE,
  varfun = pbvar,
  pr = TRUE,
  ticktype = "simple",
  theta = 50,
  phi = 25,
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix of predictor values.}

\item{y}{Numeric vector of response values.}

\item{est}{Measure of location for the smoother (default: \code{tmean}).}

\item{fr}{Span for running interval (default: 1).}

\item{plotit}{Logical; if \code{TRUE} (default), creates a plot.}

\item{pyhat}{Logical; if \code{TRUE}, returns predicted values (default: \code{FALSE}).}

\item{nboot}{Number of bootstrap samples for variance estimation (default: 40).}

\item{atr}{Amount of trimming for bootstrap variance (default: 0).}

\item{nmin}{Minimum number of points in neighborhood (default: 0).}

\item{pch}{Plotting character (default: '*').}

\item{outfun}{Outlier detection function (default: \code{outpro}).}

\item{eout}{Logical; if \code{TRUE}, removes outliers before smoothing.}

\item{xlab}{Label for x-axis (default: "X").}

\item{ylab}{Label for y-axis (default: "Y").}

\item{scat}{Logical; if \code{TRUE} (default), shows scatter plot.}

\item{SEED}{Logical; if \code{TRUE} (default), sets random seed for reproducibility.}

\item{expand}{Expansion factor for 3D plots (default: 0.5).}

\item{scale}{Logical; if \code{TRUE}, uses Mahalanobis distance for multivariate
predictors (default: \code{FALSE}).}

\item{STAND}{Logical; if \code{TRUE} (default), standardizes predictors.}

\item{varfun}{Function to measure variation (default: \code{pbvar}).}

\item{pr}{Logical; if \code{TRUE} (default), prints warnings about scaling.}

\item{ticktype}{Tick mark type for 3D plots (default: "simple").}

\item{theta}{Rotation angle for 3D plots (default: 50).}

\item{phi}{Colatitude angle for 3D plots (default: 25).}

\item{...}{Additional arguments passed to smoother functions.}
}
\value{
List with components:
\item{Strength.Assoc}{Square root of explanatory power (correlation-like measure).}
\item{Explanatory.Power}{Proportion of variance explained.}
\item{yhat}{Predicted values (if \code{pyhat=TRUE}, otherwise \code{NULL}).}
}
\description{
Applies a running interval smoother and computes the strength of association
(explanatory power) between predictors and response. Works with univariate
or multivariate predictors and uses bootstrap for robust estimation.
}
\details{
The function measures how well the predictors explain the response by:
\enumerate{
\item Fitting a running interval smoother to get predicted values
\item Computing variance of predicted values
\item Computing variance of response values
\item Taking ratio: explanatory power = var(predicted) / var(response)
}

Strength of association is the square root of explanatory power.
For multivariate predictors (ncol(x) > 1), uses \code{run3bo()} for 3D plotting.
}
\examples{
# Univariate predictor
x <- rnorm(100)
y <- 2*x + rnorm(100)
rplotsm(x, y, fr=0.8)

# Multivariate predictors
x <- matrix(rnorm(200), ncol=2)
y <- x[,1] + x[,2] + rnorm(100, sd=0.5)
rplotsm(x, y, scale=TRUE)
}
\seealso{
\code{\link{runmbo}}, \code{\link{run3bo}}, \code{\link{rplot}}
}
