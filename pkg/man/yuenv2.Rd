% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{yuenv2}
\alias{yuenv2}
\title{Yuen's Test for Independent Groups with Visualization}
\usage{
yuenv2(
  x,
  y = NULL,
  tr = 0.2,
  alpha = 0.05,
  plotit = FALSE,
  plotfun = splot,
  op = TRUE,
  VL = TRUE,
  cor.op = FALSE,
  loc.fun = median,
  xlab = "Groups",
  ylab = "",
  PB = FALSE,
  nboot = 100,
  SEED = TRUE
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{tr}{Trimming proportion (default: 0.2). Proportion of observations to
trim from each tail when computing winsorized correlation.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{plotfun}{Plotting function to use (default: \code{splot}).}

\item{op}{Logical. If \code{TRUE}, prints output (default: \code{TRUE}).}

\item{VL}{Logical. If \code{TRUE}, adds vertical lines to plot (default: \code{TRUE}).}

\item{cor.op}{Logical. If \code{TRUE}, displays correlation information (default: \code{FALSE}).}

\item{loc.fun}{Location function for plotting (default: \code{median}).}

\item{xlab}{Label for x-axis (default: "Groups").}

\item{ylab}{Label for y-axis (default: "").}

\item{PB}{Logical. If \code{TRUE}, adds percentile bootstrap confidence interval (default: \code{FALSE}).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}
}
\value{
A list with Yuen test results including estimates, test statistic,
p-value, confidence interval, and degrees of freedom.
}
\description{
Performs Yuen's test for comparing trimmed means between two independent
groups with optional visualization and various display options.
}
\details{
This is an enhanced version of Yuen's test that provides visualization options
and additional diagnostic information. The test compares trimmed means of two
independent groups using Welch's approach for unequal variances.

When \code{plotit = TRUE}, creates a plot comparing the two groups. When \code{PB = TRUE},
also computes a percentile bootstrap confidence interval for comparison.

Missing values are automatically removed.
}
\examples{
\dontrun{
x <- rnorm(50)
y <- rnorm(60, mean = 0.5)

# Basic test with plot
yuenv2(x, y, plotit = TRUE)

# With bootstrap CI
yuenv2(x, y, plotit = TRUE, PB = TRUE)
}
}
\seealso{
\code{\link{yuen}}, \code{\link{yuenbt}}
}
