% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{wmw.bcav2}
\alias{wmw.bcav2}
\title{BCA Confidence Interval with Iterative Averaging}
\usage{
wmw.bcav2(x, y, alpha = 0.05, nboot = 500, iter = 100, SEED = TRUE, ...)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{iter}{Number of iterations for averaging confidence limits (default: 100).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{...}{Additional arguments passed to \code{bcajack}.}
}
\value{
A list with components:
\item{n1}{Sample size of group 1}
\item{n2}{Sample size of group 2}
\item{phat}{Estimated probability P(X<Y)}
\item{ci.low}{Average lower confidence limit}
\item{ci.upper}{Average upper confidence limit}
}
\description{
Computes a BCA bootstrap confidence interval for P(X<Y) using iterative
averaging to handle extreme estimates (0 or 1) more robustly.
}
\details{
This function provides more stable confidence intervals when the
estimate of P(X<Y) is near 0 or 1. When the estimate is exactly
0 or 1, it uses \code{cid}. Otherwise, it computes confidence intervals
on \code{iter} subsamples of size min(n1, n2) and averages the limits.

The averaging approach can provide better coverage properties when
sample sizes are unequal or when the estimate is extreme.

Requires the \code{bcaboot} package.
}
\examples{
x <- rnorm(40, mean=100, sd=15)
y <- rnorm(30, mean=110, sd=15)
wmw.bcav2(x, y, nboot=500, iter=50)
}
\seealso{
\code{\link{wmw.bca}}, \code{\link{wmw.bcapv.v2}}
}
