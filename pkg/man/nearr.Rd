% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{nearr}
\alias{nearr}
\title{Find Nearby Points Greater Than Target (Running Interval Helper)}
\usage{
nearr(x, pt, fr = 1)
}
\arguments{
\item{x}{Numeric vector of observations.}

\item{pt}{Numeric scalar target point.}

\item{fr}{Numeric. Bandwidth multiplier for defining "nearness" (default: 1).}
}
\value{
Logical vector same length as \code{x}: \code{TRUE} for values
within \code{fr*MAD} of \code{pt} AND greater than \code{pt}, \code{FALSE} otherwise.
}
\description{
Identifies observations within a robust distance threshold that are also
numerically greater than a target value. Mirror image of \code{\link{nearl}}.
}
\details{
\strong{Distance metric}: Uses MAD (median absolute deviation) as robust scale.
If MAD = 0, falls back to IQR-based scale, then winsorized variance.

\strong{Selection criterion}: \code{abs(x - pt) <= fr*MAD  AND  x > pt}

Used by running-interval smoothers to identify right-neighborhood observations.
}
\seealso{
\code{\link{nearl}} for left-neighborhood version,
\code{\link{near}} for two-sided version
}
\keyword{internal}
