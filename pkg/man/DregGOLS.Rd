% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression.R
\name{DregGOLS}
\alias{DregGOLS}
\title{Global Test for Equality of Dependent OLS Regression Lines}
\usage{
DregGOLS(
  x1,
  y1,
  x2,
  y2,
  xout = FALSE,
  outfun = outpro,
  SEED = TRUE,
  nboot = 200,
  STAND = TRUE,
  ...
)
}
\arguments{
\item{x1}{Numeric vector or matrix of predictor variable(s) for the first group.}

\item{y1}{Numeric vector of the dependent variable for the first group.}

\item{x2}{Numeric vector or matrix of predictor variable(s) for the second group.}

\item{y2}{Numeric vector of the dependent variable for the second group.}

\item{xout}{Logical; if TRUE, removes leverage points before analysis (default: FALSE).}

\item{outfun}{Function for outlier detection (default: \code{outpro}).}

\item{SEED}{Logical; if TRUE, sets random seed for reproducibility (default: TRUE).}

\item{nboot}{Number of bootstrap samples (default: 200).}

\item{STAND}{Logical; if TRUE, standardizes predictors for outlier detection (default: TRUE).}

\item{...}{Additional arguments passed to \code{outfun}.}
}
\value{
A list with components:
\describe{
\item{test.statistic}{The F-type test statistic}
\item{degrees_of_freedom}{Vector of numerator and denominator df: c(p+1, n-p-1)}
\item{p.value}{P-value from the F distribution}
\item{est.1}{OLS coefficient estimates for group 1}
\item{est.2}{OLS coefficient estimates for group 2}
\item{estimate.dif}{Difference in estimates (group 1 - group 2)}
}
}
\description{
Tests whether two dependent (paired) OLS regression lines are identical
using a variation of Hotelling's T-squared test with bootstrap-estimated
covariance matrix. This is the OLS-specific version of a global test for
dependent groups.
}
\details{
This function performs a \strong{global test} that all regression parameters
(intercept and all slopes) are equal between two dependent groups. It is
designed specifically for \strong{OLS regression} on paired/dependent data.

\strong{Test procedure}:
\enumerate{
\item Bootstrap resample pairs (x1, y1, x2, y2) together to preserve dependence
\item For each bootstrap sample, estimate parameters for both groups via OLS
\item Compute differences in parameters
\item Estimate covariance matrix S of parameter differences from bootstrap
\item Construct Hotelling-type statistic: \eqn{T^2 = d' S^{-1} d} where d = est.1 - est.2
\item Transform to F-statistic and compute p-value
}

\strong{Key features}:
\itemize{
\item Uses bootstrap to estimate covariance of differences (robust to non-normality)
\item Tests all parameters simultaneously (omnibus test)
\item Accounts for dependence between groups
\item Specific to OLS regression (uses \code{lsfit})
}

\strong{When to use}:
\itemize{
\item Comparing regression lines from repeated measures (e.g., pre-post)
\item Want single omnibus test rather than parameter-by-parameter comparisons
\item Willing to assume approximate normality of parameter differences
\item Using OLS regression (for robust methods, see \code{\link{difreg}})
}
}
\examples{
\dontrun{
# Dependent groups with same regression line
set.seed(123)
x1 <- matrix(rnorm(100), ncol=1)
x2 <- x1 + rnorm(100, sd=0.3)  # Correlated predictors
y1 <- 1 + 2*x1 + rnorm(100, sd=0.5)
y2 <- 1 + 2*x2 + rnorm(100, sd=0.5)

# Should not reject (same parameters)
DregGOLS(x1, as.vector(y1), x2, as.vector(y2), nboot=500)

# Different slopes
y2b <- 1 + 3*x2 + rnorm(100, sd=0.5)
# Should reject
DregGOLS(x1, as.vector(y1), x2, as.vector(y2b), nboot=500)
}

}
\references{
Wilcox, R.R. (2022). \emph{Introduction to Robust Estimation and Hypothesis Testing}
(5th ed.). Academic Press.
}
\seealso{
\code{\link{difreg}} for parameter-specific tests with robust regression,
\code{\link{difregOLS}} for OLS with bootstrap CIs,
\code{\link{reg2ci}} for independent groups
}
