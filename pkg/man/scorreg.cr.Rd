% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{scorreg.cr}
\alias{scorreg.cr}
\title{Critical Values for Skipped Correlation Regression CI}
\usage{
scorreg.cr(
  n,
  p,
  iter = 500,
  nboot = 500,
  corfun = pcor,
  alpha = c(0.05, 0.025, 0.01),
  TV = FALSE,
  ALL = TRUE,
  SEED = TRUE,
  outfun = outpro
)
}
\arguments{
\item{n}{Sample size.}

\item{p}{Number of predictors.}

\item{iter}{Number of iterations for simulation (default: 500).}

\item{nboot}{Number of bootstrap samples per iteration (default: 500).}

\item{corfun}{Correlation function to use. Common options: \code{pbcor} (percentage
bend), \code{pcor} (Pearson), \code{wincor} (winsorized), \code{spear} (Spearman).}

\item{alpha}{Vector of significance levels (default: c(.05, .025, .01)).}

\item{TV}{Logical. Return test statistic values if \code{TRUE} (default: \code{FALSE}).}

\item{ALL}{Logical. Eliminate outliers jointly (default: \code{TRUE}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}
}
\value{
List with components:
\item{crit.p.values}{Critical p-values for each alpha level.}
\item{pvals}{Vector of minimum p-values from \code{iter} simulations (if \code{TV = TRUE}).}
}
\description{
Determines critical p-values for \code{scorregci()} via simulation under the null
hypothesis of zero correlation.
}
\details{
Estimates the null distribution of the minimum p-value across all p predictors
when all correlations are zero. Uses simulation to generate critical p-values
that control the familywise error rate at the specified alpha levels.

Pre-computing these critical values can significantly reduce execution time
when repeatedly calling \code{scorregci()} with the same n and p.
}
\seealso{
\code{\link{scorregci}}, \code{\link{mscorci.cr}}
}
\keyword{internal}
