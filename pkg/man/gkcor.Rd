% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{gkcor}
\alias{gkcor}
\title{Gnanadesikan-Kettenring Correlation or Covariance}
\usage{
gkcor(x, y, varfun = tauvar, ccov = FALSE, ...)
}
\arguments{
\item{x}{Numeric vector. First variable.}

\item{y}{Numeric vector. Second variable (same length as \code{x}).}

\item{varfun}{Function to compute robust variance (default: \code{\link{tauvar}}).
Can be any robust variance estimator function.}

\item{ccov}{Logical. If \code{TRUE}, returns covariance instead of correlation
(default: \code{FALSE}).}

\item{...}{Additional arguments passed to \code{varfun}.}
}
\value{
Numeric value:
\itemize{
\item If \code{ccov = FALSE}: Robust correlation coefficient
\item If \code{ccov = TRUE}: Robust covariance
}
}
\description{
Computes a robust correlation coefficient (or covariance) using the
Gnanadesikan-Kettenring (GK) method based on robust variance estimates
of sum and difference variables.
}
\details{
The Gnanadesikan-Kettenring method estimates covariance as:

\deqn{cov(X, Y) = \frac{Var(X + Y) - Var(X - Y)}{4}}

where \eqn{Var} is a robust variance estimator specified by \code{varfun}.

The correlation is then:

\deqn{cor(X, Y) = \frac{cov(X, Y)}{\sqrt{Var(X)} \sqrt{Var(Y)}}}

This approach provides robust estimation of association that is resistant
to outliers. Different variance estimators can be used via the \code{varfun}
parameter to achieve different robustness properties.
}
\examples{
# Robust correlation using GK method
x <- c(1, 2, 3, 4, 5, 100)  # Last value is outlier
y <- c(2, 4, 6, 8, 10, 12)
gkcor(x, y)  # Robust correlation
cor(x, y)    # Compare with classical Pearson correlation

}
\references{
Gnanadesikan, R., & Kettenring, J. R. (1972). Robust estimates, residuals, and
outlier detection with multiresponse data. Biometrics, 28(1), 81-124.

Maronna, R. A., & Zamar, R. H. (2002). Robust estimates of location and dispersion
for high-dimensional datasets. Technometrics, 44(4), 307-317.
}
\seealso{
\code{\link{gk}}, \code{\link{gk.sigmamu}}, \code{\link{tauvar}}
}
\keyword{multivariate}
\keyword{robust}
