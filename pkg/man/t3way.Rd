% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anova.R
\name{t3way}
\alias{t3way}
\title{Three-Way ANOVA on Trimmed Means for Independent Groups}
\usage{
t3way(
  J,
  K,
  L,
  x,
  tr = 0.2,
  grp = c(1:p),
  alpha = 0.05,
  p = J * K * L,
  MAT = FALSE,
  lev.col = c(1:3),
  var.col = 4,
  pr = TRUE,
  IV1 = NULL,
  IV2 = NULL,
  IV3 = NULL
)
}
\arguments{
\item{J}{Number of levels for Factor A.}

\item{K}{Number of levels for Factor B.}

\item{L}{Number of levels for Factor C.}

\item{x}{Data in list or matrix format. If list, x[\link{1}] contains data for
level (1,1,1), x[\link{2}] for (1,1,2), ..., x[\link{L}] for (1,1,L), x[\link{L+1}] for
(1,2,1), etc.}

\item{tr}{Proportion of trimming (default is 0.2 for 20\% trimming).}

\item{grp}{Vector to rearrange group order if needed (default is 1:p).}

\item{alpha}{Significance level (default is 0.05).}

\item{p}{Total number of groups (default is J\emph{K}L).}

\item{MAT}{Logical; if TRUE, x is a matrix with columns for factor levels
(default is FALSE).}

\item{lev.col}{Column numbers indicating factor levels when MAT=TRUE
(default is c(1:3)).}

\item{var.col}{Column number for the dependent variable when MAT=TRUE
(default is 4).}

\item{pr}{Logical indicating whether to print messages (default is TRUE).}

\item{IV1}{Optional vector specifying levels of Factor A (for alternative input).}

\item{IV2}{Optional vector specifying levels of Factor B (for alternative input).}

\item{IV3}{Optional vector specifying levels of Factor C (for alternative input).}
}
\value{
A list with components for each effect:
\item{Qa, Qb, Qc}{Test statistics for main effects A, B, C}
\item{Qa.crit, Qb.crit, Qc.crit}{Critical values for main effects}
\item{A.p.value, B.p.value, C.p.value}{p-values for main effects}
\item{Qab, Qac, Qbc}{Test statistics for two-way interactions}
\item{Qab.crit, Qac.crit, Qbc.crit}{Critical values for two-way interactions}
\item{AB.p.value, AC.p.value, BC.p.value}{p-values for two-way interactions}
\item{Qabc}{Test statistic for three-way interaction}
\item{Qabc.crit}{Critical value for three-way interaction}
\item{ABC.p.value}{p-value for three-way interaction}
}
\description{
Performs a J by K by L three-way ANOVA on trimmed means where all JKL groups
are independent. Tests all main effects, two-way interactions, and the
three-way interaction.
}
\details{
This function performs a heteroscedastic three-way ANOVA using trimmed means.
It computes test statistics and p-values for all main effects, all two-way
interactions, and the three-way interaction.

The function can accept data in multiple formats: list mode, matrix with
columns for groups, or matrix with factor level columns. When using IV1, IV2,
and IV3, x should be a vector of dependent variable values.
}
\examples{
# Create data for 2x2x2 design
set.seed(123)
x <- list()
for(i in 1:8) x[[i]] <- rnorm(20)

# Run three-way ANOVA
result <- t3way(J=2, K=2, L=2, x=x)
}
