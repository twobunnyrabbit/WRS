% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{smgrid.est}
\alias{smgrid.est}
\title{Estimate Location for Grid Cells Defined by Two-Variable Splitting}
\usage{
smgrid.est(
  x,
  y,
  est = tmean,
  IV = c(1, 2),
  Qsplit1 = c(0.3, 0.7),
  Qsplit2 = c(0.3, 0.7),
  tr = 0.2,
  xout = FALSE,
  outfun = outpro,
  ...
)
}
\arguments{
\item{x}{Matrix or data frame of predictor variables}

\item{y}{Response variable vector}

\item{est}{Location estimator function. Default is \code{\link{tmean}}.}

\item{IV}{Vector of length 2 specifying which columns of x to use for splitting.
Default is \code{c(1,2)} (first two columns).}

\item{Qsplit1}{Quantile(s) for splitting first independent variable. Default is
\code{c(.3,.7)} (tertiles).}

\item{Qsplit2}{Quantile(s) for splitting second independent variable. Default is
\code{c(.3,.7)} (tertiles).}

\item{tr}{Trimming proportion (0 to 0.5). Default is 0.2.}

\item{xout}{Logical. If \code{TRUE}, remove outliers from x. Default is \code{FALSE}.}

\item{outfun}{Outlier detection function. Default is \code{\link{outpro}}.}

\item{...}{Additional arguments passed to estimator or outlier function.}
}
\value{
Matrix of location estimates with rows corresponding to levels of IV1
and columns to levels of IV2.
}
\description{
Splits data based on quantiles of two independent variables and computes
location estimates for each cell of the resulting J × K grid. Returns only
the matrix of estimates without performing tests.
}
\details{
This is a simplified version of \code{\link{smgrid}} that only computes estimates
without performing hypothesis tests. Useful for exploring location estimates
across grid cells defined by two variables.
}
\examples{
# Compute trimmed means for grid cells
set.seed(123)
x <- matrix(rnorm(200), ncol=2)
y <- x[,1] + x[,2] + rnorm(100)
estimates <- smgrid.est(x, y, Qsplit1=c(.3,.7), Qsplit2=c(.3,.7))
}
\seealso{
\code{\link{smgrid}} for version with hypothesis testing
}
