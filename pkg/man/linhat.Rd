% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{linhat}
\alias{linhat}
\title{Estimate Linear Contrasts}
\usage{
linhat(x, con, est = tmean, ...)
}
\arguments{
\item{x}{Data in matrix or list format. Each column/element represents a group.}

\item{con}{J×d matrix of contrast coefficients, where J = number of groups and
d = number of contrasts. Each column specifies one contrast.}

\item{est}{Estimator function to use (default: \code{\link{tmean}}).}

\item{...}{Additional arguments passed to the estimator function.}
}
\value{
Numeric vector of length d containing the estimated contrasts.
}
\description{
Computes estimates for all linear contrasts specified by a contrast matrix.
}
\details{
Simple utility function that:
\itemize{
\item Computes location estimates for each group using \code{est}
\item Forms each linear contrast as sum of coefficients × estimates
\item Returns vector of contrast estimates
}
}
\examples{
# Three groups
set.seed(123)
x <- list(rnorm(30), rnorm(30, mean = 0.5), rnorm(30, mean = 1))

# Two contrasts: 1 vs 2, and 1 vs 3
con <- matrix(c(1, -1, 0,
                1, 0, -1), nrow = 3, ncol = 2)
linhat(x, con)
}
\seealso{
\code{\link{llocv2}}, \code{\link{lindm}}, \code{\link{linpbg}}
}
