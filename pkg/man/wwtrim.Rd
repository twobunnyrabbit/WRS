% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{wwtrim}
\alias{wwtrim}
\title{J × K ANOVA with Trimmed Means (Within-by-Within Design)}
\usage{
wwtrim(J, K, x, grp = c(1:p), p = J * K, tr = 0.2)
}
\arguments{
\item{J}{Number of levels for first factor.}

\item{K}{Number of levels for second factor.}

\item{x}{Data in list mode, matrix, or data frame. Groups ordered as (1,1), (1,2), ...,
(1,K), (2,1), ..., (J,K).}

\item{grp}{Vector specifying which groups to analyze (default: all groups 1:p).}

\item{p}{Total number of groups (J*K).}

\item{tr}{Trimming proportion (default: 0.2 for 20\% trimming).}
}
\value{
List with components:
\item{Qa}{Test statistic for factor A.}
\item{Qa.siglevel}{P-value for factor A.}
\item{Qb}{Test statistic for factor B.}
\item{Qb.siglevel}{P-value for factor B.}
\item{Qab}{Test statistic for A × B interaction.}
\item{Qab.siglevel}{P-value for interaction.}
}
\description{
Performs J × K factorial ANOVA using trimmed means with repeated measures
on both factors. Tests main effects and interactions.
}
\details{
Uses Johansen's method for within-by-within designs with trimmed means.
Computes test statistics via \code{\link{trimww.sub}} for:
\itemize{
\item Factor A main effect
\item Factor B main effect
\item A × B interaction
}

Significance levels based on F-distribution with 999 denominator df
(conservative approximation).

Data structure: In list mode, \code{x[[1]]} = level (1,1), \code{x[[2]]} = level (1,2), etc.
In matrix mode, columns correspond to groups in the same order.
}
\seealso{
\code{\link{tsplit}}, \code{\link{wwtrimbt}}, \code{\link{trimww.sub}}
}
\keyword{anova}
\keyword{robust}
