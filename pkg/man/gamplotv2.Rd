% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression-advanced.R
\name{gamplotv2}
\alias{gamplotv2}
\title{Plot GAM Regression Surface with Strength of Association}
\usage{
gamplotv2(
  x,
  y,
  sop = FALSE,
  pyhat = FALSE,
  eout = FALSE,
  xout = FALSE,
  outfun = out,
  plotit = TRUE,
  varfun = pbvar,
  xlab = "X",
  ylab = "",
  zlab = "",
  theta = 50,
  phi = 25,
  expand = 0.5,
  SCALE = FALSE,
  cor.fun = pbcor,
  ADJ = FALSE,
  nboot = 20,
  pr = TRUE,
  SEED = TRUE,
  ticktype = "simple"
)
}
\arguments{
\item{x}{A numeric matrix containing predictor values (maximum 4 columns).}

\item{y}{A numeric vector containing the response variable.}

\item{sop}{Logical. If \code{TRUE}, uses spline smoothing (s()) in the GAM;
if \code{FALSE}, uses linear terms (default: \code{FALSE}).}

\item{pyhat}{Logical. If \code{TRUE}, returns fitted values; if \code{FALSE},
returns strength/explanatory power measures (default: \code{FALSE}).}

\item{eout}{Logical value. If \code{TRUE}, outliers are removed based on
eliminating points with large residuals or leverage (default: \code{FALSE}).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{varfun}{Function to compute variance for explanatory power
(default: \code{pbvar} for percentage bend variance).}

\item{xlab, ylab, zlab}{Axis labels for plots.}

\item{theta, phi, expand, ticktype}{Parameters for 3D perspective plots.}

\item{SCALE}{Logical. For 3D plots, controls z-axis scaling (default: \code{FALSE}).}

\item{cor.fun}{Function to compute correlation as fallback when
explanatory power ≥ 1 (default: \code{pbcor}).}

\item{ADJ}{Logical. If \code{TRUE}, computes adjusted measures accounting for
expected association under independence (default: \code{FALSE}).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{pr}{Logical. If \code{TRUE}, prints progress messages (default: \code{TRUE}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}
}
\value{
A list with components:
\item{Strength.Assoc}{Estimated strength of association (square root of
explanatory power).}
\item{Explanatory.power}{Ratio of variance of fitted values to variance
of y (capped at correlation^2 if > 1).}
\item{Strength.Adj}{Adjusted strength of association (if \code{ADJ = TRUE}),
accounting for expected value under independence.}
\item{Explanatory.Adj}{Adjusted explanatory power (if \code{ADJ = TRUE}).}
}
\description{
Enhanced version of \code{gamplot()} that fits and plots a GAM regression surface
while also computing measures of strength of association and explanatory power,
with optional adjustment for independence.
}
\details{
This function extends \code{gamplot()} by computing:
\itemize{
\item \strong{Explanatory power}: var(fitted values) / var(y), measuring proportion
of variance explained
\item \strong{Strength of association}: sqrt(explanatory power), analogous to
correlation
}

When \code{ADJ = TRUE}, the function estimates the expected strength under
independence via bootstrap permutation and adjusts the observed values:
\deqn{Adjusted = (Observed - E[Independent]) / (1 - E[Independent])}

The variance and correlation functions (\code{varfun}, \code{cor.fun}) can be changed
to use different robust estimators.
}
\note{
Requires \code{mgcv} package for GAM fitting and \code{akima} for 3D surface
interpolation.
}
\examples{
\dontrun{
# GAM with nonlinear relationship
set.seed(123)
x <- seq(-3, 3, length=100)
y <- sin(x) + rnorm(100, sd=0.2)
result <- gamplotv2(x, y, sop=TRUE)
result$Strength.Assoc

# With adjusted measures
result_adj <- gamplotv2(x, y, sop=TRUE, ADJ=TRUE, nboot=100)
result_adj$Strength.Adj
}
}
\references{
Wilcox, R.R. (2022). Introduction to Robust Estimation and Hypothesis Testing
(5th ed.). Academic Press.
}
\seealso{
\code{\link{gamplot}}, \code{\link{gamindt}}, \code{\link{gamplotINT}}
}
