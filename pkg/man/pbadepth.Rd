% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outliers.R
\name{pbadepth}
\alias{pbadepth}
\title{Bootstrap Test for Linear Contrasts Using Depth}
\usage{
pbadepth(
  x,
  est = onestep,
  con = 0,
  alpha = 0.05,
  nboot = 2000,
  grp = NA,
  op = 3,
  allp = TRUE,
  MM = FALSE,
  MC = FALSE,
  cop = 3,
  SEED = TRUE,
  na.rm = FALSE,
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{est}{Estimator function to use for computing location or other
statistics. Common choices include \code{mean}, \code{median}, \code{tmean} (trimmed mean),
\code{mom} (modified one-step M-estimator), \code{mest} (M-estimator).}

\item{con}{Contrast matrix (J x C) where J = number of groups and C = number
of contrasts. If 0, tests all pairwise comparisons (default).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{grp}{A vector indicating group membership. Can be numeric, factor, or
character. Used for grouping observations in multi-group comparisons.}

\item{op}{Number of outliers to remove from each tail when using outlier
detection methods. Default varies by function.}

\item{allp}{Logical. If TRUE and con=0, tests all pairwise contrasts;
if FALSE, tests sequential contrasts (default: TRUE).}

\item{MM}{Logical. If TRUE, uses MAD in projection distance computation.}

\item{MC}{Logical. If \code{TRUE}, uses parallel processing via \code{mclapply}
(default: \code{FALSE}).}

\item{cop}{Center computation option (see \code{outproMC}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{na.rm}{Logical value. If \code{TRUE}, removes \code{NA} values before computation
(default: \code{TRUE} for most functions).}

\item{...}{Additional arguments passed to \code{est}.}
}
\value{
A list with components:
\item{p.value}{Bootstrap p-value for testing all contrasts equal zero.}
\item{psihat}{Estimated contrast values.}
\item{con}{Contrast matrix used.}
\item{n}{Sample sizes for each group.}
}
\description{
Tests whether specified linear contrasts among J independent groups equal
zero, using bootstrap and data depth methods.
}
\details{
Uses bootstrap to test joint hypothesis that all contrasts equal zero.
Depth is measured via Mahalanobis distance (op=1), MCD-based Mahalanobis
(op=2), or projection distance (op=3, default and recommended).

Data can be in list mode or matrix mode (columns = groups).
}
\seealso{
\code{\link{pdis}}, \code{\link{pdisMC}}
}
