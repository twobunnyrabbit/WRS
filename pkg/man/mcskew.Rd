% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{mcskew}
\alias{mcskew}
\title{Medcouple Skewness Measure}
\usage{
mcskew(z)
}
\arguments{
\item{z}{Numeric vector.}
}
\value{
Numeric value of the medcouple (robust skewness measure).
}
\description{
Compute the medcouple (MC), a robust measure of skewness based on the median
of pairwise kernel function values. The medcouple is resistant to outliers
and bounded between -1 and 1.
}
\details{
The medcouple is defined as the weighted median of the kernel function
\eqn{h(x_i, x_j) = (x_i - m) - (m - x_j)) / (x_i - x_j)} for all pairs
where \eqn{x_i \ge m \ge x_j}, where m is the median of z.

Properties:
\itemize{
\item MC = 0 for symmetric distributions
\item MC > 0 for right-skewed distributions
\item MC < 0 for left-skewed distributions
\item -1 ≤ MC ≤ 1 (bounded)
\item 50\% breakdown point
}

The function uses an efficient O(n log n) algorithm rather than computing
all O(n²) pairwise values.
}
\references{
Brys, G., Hubert, M., & Struyf, A. (2004). A robust measure of skewness.
\emph{Journal of Computational and Graphical Statistics}, \emph{13}(4), 996-1017.
}
\seealso{
\code{\link{skew}}, \code{\link{skerd}}
}
\keyword{robust}
\keyword{univar}
