% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{pbad2way}
\alias{pbad2way}
\title{Two-Way ANOVA Using Projection-Based Depth with Bootstrap}
\usage{
pbad2way(
  J,
  K,
  x,
  est = tmean,
  conall = TRUE,
  alpha = 0.05,
  nboot = 2000,
  grp = NA,
  op = FALSE,
  pro.dis = TRUE,
  MM = FALSE,
  pr = TRUE,
  ...
)
}
\arguments{
\item{J}{Number of levels for Factor A.}

\item{K}{Number of levels for Factor B.}

\item{x}{Data in list mode or matrix. Each element contains data for one cell.
Cells ordered as: (1,1), (1,2), ..., (1,K), (2,1), ..., (J,K).}

\item{est}{Measure of location. Default is \code{\link{tmean}} (20\% trimmed mean).}

\item{conall}{Logical. If \code{TRUE} (default), test all pairwise comparisons.
If \code{FALSE}, test only sequential comparisons.}

\item{alpha}{Significance level. Default is 0.05.}

\item{nboot}{Number of bootstrap samples. Default is 2000.}

\item{grp}{Optional vector to reorder groups. Default is \code{NA} (standard order).}

\item{op}{Logical. If \code{TRUE}, use outlier projection method. Default is \code{FALSE}.}

\item{pro.dis}{Logical. If \code{TRUE} (default), use projection distance for depth.
If \code{FALSE}, use Mahalanobis distance.}

\item{MM}{Logical. If \code{TRUE}, use marginal medians in projection distance.
Default is \code{FALSE}.}

\item{pr}{Logical. If \code{TRUE}, print informational messages. Default is \code{TRUE}.}

\item{...}{Additional arguments passed to the location estimator.}
}
\value{
List with components:
\item{sig.levelA}{P-value for main effect of Factor A}
\item{sig.levelB}{P-value for main effect of Factor B}
\item{sig.levelAB}{P-value for A × B interaction}
\item{conA}{Contrast matrix used for Factor A}
\item{conB}{Contrast matrix used for Factor B}
\item{conAB}{Contrast matrix used for A × B interaction}
}
\description{
Tests main effects and interaction in a two-way independent groups design
using projection-based depth and percentile bootstrap. A robust alternative
to traditional two-way ANOVA.
}
\details{
As of June 2022, defaults to using:
\itemize{
\item 20\% trimmed mean for location (\code{est=tmean})
\item Projection-based depth rather than Mahalanobis distance (\code{pro.dis=TRUE})
}

The function creates contrast matrices for testing main effects and interaction,
then uses bootstrap with projection-based depth to compute p-values.
Missing values are automatically removed.
}
\seealso{
\code{\link{pbad3way}}, \code{\link{t2way}}, \code{\link{con2way}}
}
\keyword{htest}
\keyword{robust}
