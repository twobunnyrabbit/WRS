% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{long2g}
\alias{long2g}
\title{Compare Two Groups in Longitudinal Data via Robust Regression}
\usage{
long2g(x, x.col, y.col, s.id, grp.id, regfun = tsreg, MAR = TRUE, tr = 0.2)
}
\arguments{
\item{x}{Matrix or data frame containing longitudinal data (one row per observation).}

\item{x.col}{Integer vector indicating columns containing predictor variables.}

\item{y.col}{Integer. Column number containing the outcome (dependent) variable.}

\item{s.id}{Integer. Column number containing subject IDs.}

\item{grp.id}{Integer. Column number containing group membership indicator.}

\item{regfun}{Function for regression (default: \code{\link{tsreg}} for Theil-Sen).}

\item{MAR}{Logical. If TRUE, assumes Missing At Random and uses \code{\link{yuen}} for
each parameter separately; if FALSE, uses \code{\link{smean2}} for multivariate comparison
(default: TRUE).}

\item{tr}{Numeric. Trimming proportion for Yuen's test (default: 0.2).}
}
\value{
If \code{MAR=TRUE}: List where each element contains results from \code{\link{yuen}}
comparing the groups on one regression parameter.

If \code{MAR=FALSE}: Results from \code{\link{smean2}} comparing multivariate
regression parameter vectors.
}
\description{
Compares two groups in longitudinal data by fitting a robust regression model
to each subject, extracting subject-specific parameter estimates, and comparing
these estimates between groups. Handles repeated measures structure.
}
\details{
\strong{Method}:
\enumerate{
\item Splits data by group using \code{\link{matsplit}}
\item For each group, fits regression model for each subject via \code{\link{longreg}}
\item Extracts subject-specific parameter estimates
\item Compares parameter distributions between groups
}

\strong{Data format}: Assumes long format (one row per observation) similar to the
\code{Orthodont} dataset in the \code{nlme} package.
}
\examples{
\dontrun{
# Compare growth trajectories between two groups
library(nlme)
data(Orthodont)
# Column layout: distance, age, Subject, Sex
# Compare males vs females on age effect
long2g(Orthodont, x.col = 2, y.col = 1, s.id = 3, grp.id = 4)
}
}
\seealso{
\code{\link{longreg}} for within-subject regression,
\code{\link{matsplit}} for splitting by group,
\code{\link{yuen}} for robust t-test,
\code{\link{smean2}} for multivariate comparison
}
\keyword{regression}
\keyword{robust}
