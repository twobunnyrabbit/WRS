% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{ltareg}
\alias{ltareg}
\title{Least Trimmed Absolute Value Regression}
\usage{
ltareg(x, y, tr = 0.2, h = NA, op = 2)
}
\arguments{
\item{x}{Matrix or vector of predictors}

\item{y}{Response variable vector}

\item{tr}{Trimming proportion (default: 0.2). Ignored if h is specified.}

\item{h}{Number of smallest residuals to use. If NA (default), computed as
n - floor(tr * n) where n is sample size.}

\item{op}{Initial estimator option: 1 = use \code{\link{ltsreg}},
other = use \code{\link{tsreg}} (default: 2)}
}
\value{
A list with components:
\item{coef}{Vector of regression coefficients (intercept and slopes)}
\item{residuals}{Vector of residuals (y - fitted values)}
}
\description{
Computes the least trimmed absolute value (LTA) regression estimator, which
minimizes the sum of the h smallest absolute residuals. This provides a
robust alternative to ordinary least squares regression.
}
\details{
The LTA estimator finds coefficients that minimize the sum of the h smallest
absolute residuals, where h determines the amount of trimming. This provides
robustness against outliers in both the response and predictor space.

The algorithm:
\enumerate{
\item Obtains initial coefficient estimates from \code{\link{ltsreg}} (op=1)
or \code{\link{tsreg}} (op≠1)
\item Refines estimates using Nelder-Mead optimization via \code{\link{nelderv2}}
with \code{\link{lta.sub}} as the objective function
}

Missing values are automatically removed.
}
\seealso{
\code{\link{lta.sub}}, \code{\link{ltsreg}}, \code{\link{tsreg}},
\code{\link{nelderv2}}
}
\keyword{regression}
\keyword{robust}
