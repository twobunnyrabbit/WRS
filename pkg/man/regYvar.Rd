% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{regYvar}
\alias{regYvar}
\title{Bootstrap Standard Errors for Regression Predictions}
\usage{
regYvar(
  x,
  y,
  regfun = tsreg,
  pts = x,
  nboot = 100,
  xout = FALSE,
  outfun = out,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{x}{Predictor matrix (n × p) used for fitting.}

\item{y}{Response vector.}

\item{regfun}{Regression function (default: \code{tsreg} for Theil-Sen).}

\item{pts}{Matrix of predictor values at which to compute prediction variances
(default: \code{x}, the training data points).}

\item{nboot}{Number of bootstrap samples (default: 100).}

\item{xout}{Logical; if \code{TRUE}, removes outliers from \code{x} before
bootstrap using \code{outfun} (default: \code{FALSE}).}

\item{outfun}{Outlier detection function used when \code{xout=TRUE}
(default: \code{out}).}

\item{SEED}{Logical; if \code{TRUE}, sets random seed to 2 for reproducibility
(default: \code{TRUE}).}

\item{...}{Additional arguments passed to \code{regfun} and \code{outfun}.}
}
\value{
A numeric vector of prediction variances (squared standard errors),
one per row of \code{pts}.
}
\description{
Estimates the standard error (variance) of predicted Y values from a
regression model using the bootstrap method. Predictions are made at
specified predictor values.
}
\details{
For each bootstrap resample:
\enumerate{
\item Sample n observations with replacement from the data
\item Fit regression using \code{regfun}
\item Compute predicted values at points specified in \code{pts}
}

The variance across bootstrap samples provides an estimate of prediction
uncertainty. Take the square root to get standard errors.
}
\seealso{
\code{\link{regYhat}}, \code{\link{tsreg}}, \code{\link{regplot}}
}
\keyword{regression}
