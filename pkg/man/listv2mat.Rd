% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{listv2mat}
\alias{listv2mat}
\title{Convert List of Equal-Length Vectors to Matrix}
\usage{
listv2mat(x)
}
\arguments{
\item{x}{List where each element is a numeric vector of the same length \code{p}.}
}
\value{
Matrix with:
\describe{
\item{Rows}{n rows, one for each list element}
\item{Columns}{p columns, matching the length of each vector}
}
}
\description{
Converts a list where each element is a vector of the same length into a matrix.
Each list element becomes a row in the resulting matrix.
}
\details{
Unlike \code{\link{list2matrix}} which creates columns from list elements and handles
unequal lengths, \code{listv2mat} creates rows from equal-length vectors. This is
useful when each list element represents a multivariate observation or a row of data.

\strong{Requirements}: All vectors in the list must have the same length.
}
\examples{
# Each observation is a 3-element vector
x <- list(c(1, 2, 3), c(4, 5, 6), c(7, 8, 9))
listv2mat(x)  # 3x3 matrix
}
\seealso{
\code{\link{list2matrix}} for unequal-length lists (creates columns),
\code{\link{list2vec}} for flattening to vector,
\code{\link{matl}} for matrix to list
}
\keyword{manip}
