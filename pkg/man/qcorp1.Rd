% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{qcorp1}
\alias{qcorp1}
\title{Quantile Correlation (Projection Method 1)}
\usage{
qcorp1(
  x,
  y,
  qest = hd,
  q = 0.5,
  xout = FALSE,
  outfun = outpro,
  plotit = FALSE,
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{qest}{Quantile estimator function (default: \code{hd} for Harrell-Davis).}

\item{q}{Quantile to use (default: 0.5 for median).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
List with component:
\item{cor}{Quantile-based correlation measure.}
}
\description{
Computes a correlation-like measure based on quantile regression, using
projection method 1.
}
\details{
This function computes a measure of association based on quantile regression
lines. It generalizes the idea of correlation to quantiles other than the mean.

The measure is based on the proportional reduction in a dispersion measure
when using quantile regression (at quantile q) compared to using just the
marginal quantile of y.

For univariate x (p=1), the sign of the correlation matches the sign of the
regression slope.
}
\examples{
# Median-based correlation
set.seed(123)
x <- rexp(50, rate = 1)
y <- 0.7 * x + rexp(50, rate = 2)
qcorp1(x, y, q = 0.5)

# Upper quartile correlation
qcorp1(x, y, q = 0.75)

# With outlier removal
qcorp1(x, y, q = 0.5, xout = TRUE)
}
\seealso{
\code{\link{qcor}}, \code{\link{qcorp1.ci}}, \code{\link{qreg}},
\code{\link{hd}}
}
