% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{anc.best.crit.det}
\alias{anc.best.crit.det}
\title{Determine Critical P-Values for anc.best - Detailed Version}
\usage{
anc.best.crit.det(J, n, alpha = 0.05, tr = 0.2, iter = 5000, SEED = TRUE)
}
\arguments{
\item{J}{Number of groups.}

\item{n}{Sample size (single value for equal n, or vector of length J for unequal n).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{tr}{Trimming proportion (default: 0.2). Proportion of observations to
trim from each tail when computing winsorized correlation.}

\item{iter}{Number of simulation iterations (default: 5000).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}
}
\value{
Matrix where each row corresponds to an alpha level and columns contain
critical p-values for each of the J-1 comparisons.
}
\description{
Similar to anc.best.crit but computes critical p-values for a range of alpha levels
(from 0.001 to 0.99). Useful for sensitivity analysis and understanding the relationship
between nominal alpha and critical p-values.
}
\examples{
\dontrun{
# Compute critical values for range of alpha levels
crit_matrix <- anc.best.crit.det(J=4, n=30, iter=1000)
}
}
\keyword{internal}
