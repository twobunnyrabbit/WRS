% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{mgvar}
\alias{mgvar}
\title{Multivariate Generalized Variance for Data Depth}
\usage{
mgvar(m, se = FALSE, op = 0, cov.fun = covmve, SEED = TRUE)
}
\arguments{
\item{m}{Matrix of numeric data (N by p).}

\item{se}{Logical. If \code{TRUE}, uses skipped Euclidean distances. Default is \code{FALSE}.}

\item{op}{Method for finding central points. If 0 (default), uses pairwise differences.
If not 0, uses the measure of location from \code{cov.fun}.}

\item{cov.fun}{Covariance function to use when \code{op} is not 0. Options include
\code{\link{covmve}}, \code{\link{covmcd}}, \code{tbs}, or \code{rmba}.
Default is \code{\link{covmve}}.}

\item{SEED}{Logical. If \code{TRUE} (default), sets random seed for reproducibility.}
}
\value{
Numeric vector of generalized variance values, one per row of \code{m}.
Central points have \code{NA} values.
}
\description{
Computes generalized variance values for each point by iteratively adding
points to the central subset. Points are ordered by how much they increase
the generalized variance.
}
\details{
This function computes a generalized variance-based measure of outlyingness
for multivariate data. It starts from the central point(s) and iteratively
adds the point that increases the generalized variance by the smallest amount.

The central values and points closest to them receive \code{NA} values.
Points further from the center have larger generalized variance values.
}
\seealso{
\code{\link{mgvdep}}, \code{\link{gvar}}, \code{\link{apgdis}}
}
\keyword{multivariate}
\keyword{robust}
