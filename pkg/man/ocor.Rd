% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{ocor}
\alias{ocor}
\title{Correlation with Multivariate Outlier Detection}
\usage{
ocor(x, y, corfun = pbcor, outfun = outmgvf, pcor = FALSE, plotit = FALSE)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{corfun}{Correlation function to use. Common options: \code{pbcor} (percentage
bend), \code{pcor} (Pearson), \code{wincor} (winsorized), \code{spear} (Spearman).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{pcor}{Logical. If \code{TRUE}, uses Pearson correlation after outlier removal.}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}
}
\value{
List with component:
\item{cor}{Correlation coefficient after removing outliers.}
}
\description{
Computes correlation after removing bivariate outliers detected using
multivariate outlier detection methods.
}
\details{
Unlike \code{ecor()}, which detects univariate outliers separately for x and y,
this function uses multivariate outlier detection on the (x, y) pairs.
This can better identify influential bivariate outliers.
}
\examples{
set.seed(123)
x <- rnorm(50)
y <- 0.6 * x + rnorm(50)
# Add bivariate outlier
x <- c(x, 5)
y <- c(y, 5)
ocor(x, y)
}
\seealso{
\code{\link{ecor}}, \code{\link{scorci}}
}
