% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression-advanced.R
\name{regmed2}
\alias{regmed2}
\title{Test Mediation Pathways in Regression}
\usage{
regmed2(
  x,
  y,
  regfun = tsreg,
  nboot = 400,
  alpha = 0.05,
  xout = FALSE,
  outfun = out,
  MC = FALSE,
  SEED = TRUE,
  pr = TRUE,
  ...
)
}
\arguments{
\item{x}{An n-by-2 numeric matrix where column 1 is the predictor and
column 2 is the mediator variable.}

\item{y}{A numeric vector containing the outcome variable.}

\item{regfun}{Regression function to use (default: \code{tsreg} for Theil-Sen).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{MC}{Logical. If \code{TRUE}, uses parallel processing via \code{mclapply}
(default: \code{FALSE}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{pr}{Logical. If \code{TRUE}, prints explanatory text about the output
(default: \code{TRUE}).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
A list with components:
\item{res1}{Results for predicting mediator from predictor: 95\% CI, estimate,
SE, and p-value for slope b12 in mediator ~ predictor.}
\item{res2}{Results for the mediator's effect: 95\% CI, estimate, SE, and
p-value for slope b23 in outcome ~ predictor + mediator.}
}
\description{
Tests the two key regression pathways in mediation analysis: (1) the effect
of the predictor on the mediator, and (2) the effect of the mediator on the
outcome controlling for the predictor. Uses bootstrap confidence intervals.
}
\details{
Mediation analysis examines three regression equations:
\enumerate{
\item \strong{Predictor → Mediator}: \eqn{M = b_{02} + b_{12} X + \epsilon_2}
\item \strong{Direct effect}: \eqn{Y = b_{01} + b_{11} X + e_1}
\item \strong{Conditional effect}: \eqn{Y = b_{03} + b_{13} X + b_{23} M + e_3}
}

This function tests two critical hypotheses:
\itemize{
\item \strong{H0: b12 = 0} (predictor doesn't affect mediator)
\item \strong{H0: b23 = 0} (mediator doesn't affect outcome after controlling for predictor)
}

For mediation to be supported, both hypotheses should typically be rejected,
indicating that:
\enumerate{
\item The predictor influences the mediator (res1)
\item The mediator influences the outcome even after accounting for the
direct effect of the predictor (res2)
}

\strong{Requirements}:
\itemize{
\item x must have exactly 2 columns: \link{predictor, mediator}
\item Use \code{MC = TRUE} for parallel processing
\item Use \code{xout = TRUE} to remove leverage points
}
}
\note{
Unlike \code{regmediate()} which tests the change in the predictor's
coefficient, this function tests the individual pathway slopes.
}
\examples{
\dontrun{
# Mediation analysis
set.seed(123)
n <- 100
x1 <- rnorm(n)          # Predictor
m <- 0.7*x1 + rnorm(n)  # Mediator (strong effect from predictor)
y <- 0.2*x1 + 0.5*m + rnorm(n)  # Outcome (partial mediation)
x <- cbind(x1, m)
result <- regmed2(x, y, nboot=200)
result$res1  # Test X → M (should be significant)
result$res2  # Test M → Y | X (should be significant)
}
}
\references{
Wilcox, R.R. (2022). Introduction to Robust Estimation and Hypothesis Testing
(5th ed.). Academic Press.
}
\seealso{
\code{\link{regmediate}}, \code{\link{regci}}, \code{\link{tsreg}}
}
