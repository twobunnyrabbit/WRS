% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outliers.R
\name{NNbag}
\alias{NNbag}
\title{Bagged Nearest Neighbor Classification}
\usage{
NNbag(
  train = NULL,
  test = NULL,
  g = NULL,
  x1 = NULL,
  x2 = NULL,
  nboot = 100,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{train}{Training data matrix (optional if \code{x1} and \code{x2} provided).}

\item{test}{Test data matrix to classify.}

\item{x1, x2}{Training data for groups 1 and 2 (alternative to \code{train}/\code{g}).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{...}{Additional arguments.}
}
\value{
Vector of predicted class labels (1 or 2) for test observations.
}
\description{
Bootstrap aggregating version of nearest neighbor classification. Uses bootstrap
sampling to address bias when group sizes differ.
}
\details{
Performs nearest neighbor classification via \code{NN.class()} with bootstrap
bagging. Each bootstrap sample uses balanced group sizes (min(n1,n2)) to
reduce bias when group sizes are unequal (n1 != n2).
}
\seealso{
\code{\link{NN.class}}, \code{\link{Depth.class.bag}}
}
