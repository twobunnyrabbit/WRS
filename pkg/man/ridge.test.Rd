% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classification.R
\name{ridge.test}
\alias{ridge.test}
\title{Ridge Regression Hypothesis Tests with Heteroscedastic Standard Errors}
\usage{
ridge.test(
  x,
  y,
  k = NULL,
  alpha = 0.05,
  pr = TRUE,
  xout = FALSE,
  outfun = outpro,
  STAND = TRUE,
  method = "hoch",
  locfun = mean,
  scat = var,
  MSF = TRUE,
  ...
)
}
\arguments{
\item{x}{Predictor matrix (n x p). Can be a matrix or data frame.}

\item{y}{Response vector of length n.}

\item{k}{Ridge parameter. If NULL (default), estimated using Kibria's method via
\code{\link{ridge.est.k}}.}

\item{alpha}{Significance level for hypothesis tests (default: 0.05).}

\item{pr}{Logical. If TRUE (default), print results to console.}

\item{xout}{Logical. If TRUE, eliminate leverage points using \code{outfun} before
fitting (default: FALSE).}

\item{outfun}{Function for detecting outliers/leverage points (default: \code{outpro}).}

\item{STAND}{Logical. If TRUE (default), standardize predictors and center response.}

\item{method}{Method for p-value adjustment: 'hoch' (Hochberg, default), 'bh'
(Benjamini-Hochberg), or 'rom' (Rom's method).}

\item{locfun}{Location estimator for standardization (default: mean).}

\item{scat}{Scale estimator for standardization (default: var).}

\item{MSF}{Logical. If TRUE (default), use MSE to estimate standard errors. If FALSE,
use residual-based method.}

\item{...}{Additional arguments passed to the outlier detection function.}
}
\value{
A list with components:
\item{coef}{Vector of ridge regression coefficient estimates.}
\item{TEST}{Test statistics for each slope.}
\item{p.value}{Unadjusted p-values for each slope.}
\item{p.crit}{Critical p-values after adjustment for multiple testing.}
\item{num.sig}{Number of slopes significantly different from zero.}
\item{k.used}{Ridge parameter value used in the analysis.}
}
\description{
Tests hypotheses about individual regression slopes using ridge regression with
heteroscedastic-consistent standard errors. Controls family-wise error rate when
testing multiple slopes.
}
\details{
This function performs ridge regression and tests H0: beta_j = 0 for each predictor j.

\strong{Ridge regression} adds an L2 penalty to handle multicollinearity, but introduces
bias. This function uses heteroscedastic-consistent standard errors (Long & Ervin, 2000)
to compute test statistics and p-values.

\strong{Multiple testing procedure:}
\enumerate{
\item Compute p-value for each slope
\item Adjust p-values using the specified method to control family-wise error rate
\item If smallest p-value ≤ alpha, reject that hypothesis AND the global null (all slopes = 0)
\item Due to ridge bias, no other slopes can be declared significant
}

\strong{Advantages:}
\itemize{
\item Power often at least as high as OLS, potentially much higher
\item Handles multicollinearity better than OLS
}

\strong{Limitations:}
\itemize{
\item Confidence intervals can be highly inaccurate when null is false
\item Only the most significant slope can be formally tested
}

Missing values are automatically removed.
}
\note{
This function requires at least 2 predictors (p ≥ 2).
}
\examples{
\dontrun{
# Generate data with multicollinearity
set.seed(123)
x1 <- rnorm(100)
x2 <- x1 + rnorm(100, sd = 0.3)
x3 <- rnorm(100)
x <- cbind(x1, x2, x3)
y <- x1 + 2*x2 + rnorm(100)

# Ridge regression tests
ridge.test(x, y)

# Specify ridge parameter
ridge.test(x, y, k = 0.5)

# With outlier removal
ridge.test(x, y, xout = TRUE)
}

}
\references{
Long, J. S., & Ervin, L. H. (2000). Using heteroscedasticity consistent standard
errors in the linear regression model. The American Statistician, 54(3), 217-224.
}
\seealso{
\code{\link{ridge.est.k}} for estimating ridge parameter,
\code{\link{ridge.Gtest}} for global test variant, \code{\link{lasso.est}}
for LASSO regression
}
