% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{wmw.ancp2}
\alias{wmw.ancp2}
\title{Conditional WMW Effect Size at Design Points}
\usage{
wmw.ancp2(x1, y1, x2, y2, pts = NULL, xout = FALSE, outfun = outpro)
}
\arguments{
\item{x1}{Covariate matrix/vector for group 1.}

\item{y1}{Response vector for group 1.}

\item{x2}{Covariate matrix/vector for group 2.}

\item{y2}{Response vector for group 2.}

\item{pts}{Design points matrix (default: NULL for 3 automatic points).}

\item{xout}{Logical. If \code{TRUE}, removes outliers (default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro}).}
}
\value{
Vector of WMW effect size estimates at each design point.
}
\description{
Estimates the conditional WMW effect size at specified design points
for two independent groups with covariates.
}
\details{
For regression lines from two independent groups, estimates the conditional
WMW effect size P(Y1 < Y2 | X = x) at each point in pts. If pts is NULL,
automatically selects three design points based on the data.
}
\examples{
\dontrun{
# Estimate conditional WMW effect
set.seed(123)
x1 <- rnorm(50); y1 <- x1 + rnorm(50)
x2 <- rnorm(50); y2 <- x2 + 0.5 + rnorm(50)
pts <- matrix(c(-1, 0, 1), ncol = 1)
wmw.ancp2(x1, y1, x2, y2, pts = pts)
}
}
\seealso{
\code{\link{wmw.ancbsep2}}, \code{\link{wmw.anc}}, \code{\link{reg.con.dist}}
}
