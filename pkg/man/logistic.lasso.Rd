% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{logistic.lasso}
\alias{logistic.lasso}
\title{Logistic Regression with Lasso Regularization}
\usage{
logistic.lasso(x, y, xout = FALSE, outfun = outpro)
}
\arguments{
\item{x}{Matrix or data frame of predictor variables.}

\item{y}{Numeric vector of binary outcomes (0/1).}

\item{xout}{Logical. If TRUE, removes outliers from predictors before fitting (default: FALSE).}

\item{outfun}{Function for multivariate outlier detection (default: \code{\link{outpro}}).}
}
\value{
List with components:
\describe{
\item{n}{Original sample size}
\item{n.keep}{Sample size after outlier removal (if \code{xout=TRUE})}
\item{estimates}{Sparse coefficient vector from \code{\link[glmnet]{cv.glmnet}} at optimal lambda}
}
}
\description{
Fits logistic regression with L1 (lasso) regularization using the \code{glmnet}
package. Automatically selects the tuning parameter via cross-validation and
returns coefficient estimates with optional outlier removal.
}
\details{
\strong{Method}:
\enumerate{
\item Optionally removes multivariate outliers from predictors using \code{outfun}
\item Applies cross-validated lasso via \code{\link[glmnet]{cv.glmnet}} with alpha=1
\item Returns coefficients at lambda.min (minimizes CV error)
}

Lasso shrinks some coefficients exactly to zero, performing automatic variable selection.
The cross-validation determines the optimal amount of regularization.

\strong{Note}: Requires the \code{glmnet} package.
}
\examples{
\dontrun{
# Logistic lasso with 10 predictors
set.seed(123)
x <- matrix(rnorm(200), ncol = 10)
p <- plogis(x[,1] + x[,5] - x[,8])  # Only 3 predictors matter
y <- rbinom(20, 1, p)
res <- logistic.lasso(x, y)
res$estimates  # Sparse coefficients
}
}
\seealso{
\code{\link{logistic.LR}} for ridge regression version,
\code{\link[glmnet]{cv.glmnet}} for cross-validated lasso,
\code{\link{H.lasso}} for heteroscedastic lasso,
\code{\link{outpro}} for projection-based outlier detection
}
\keyword{regression}
\keyword{robust}
