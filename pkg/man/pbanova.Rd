% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anova.R
\name{pbanova}
\alias{pbanova}
\title{Percentile Bootstrap One-Way ANOVA for Trimmed Means}
\usage{
pbanova(
  x,
  tr = 0.2,
  alpha = 0.05,
  nboot = NA,
  grp = NA,
  WIN = FALSE,
  win = 0.1
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{tr}{Trimming proportion (default: 0.2). Proportion of observations to
trim from each tail when computing winsorized correlation.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{grp}{A vector indicating group membership. Can be numeric, factor, or
character. Used for grouping observations in multi-group comparisons.}

\item{WIN}{Logical value. If \code{TRUE}, uses winsorized values instead of
trimmed values (default: \code{FALSE}).}

\item{win}{Amount of Winsorizing when WIN=TRUE (default: 0.1). Must be
less than or equal to \code{tr}.}
}
\value{
A list with components:
\item{test}{The test statistic.}
\item{crit.val}{Critical value from the bootstrap distribution.}
\item{p.value}{Bootstrap p-value.}
\item{con}{Contrast matrix used for testing.}
\item{num.sig}{Number of significant contrasts.}
}
\description{
Tests the hypothesis that J independent groups have equal trimmed means
using a percentile bootstrap method. Provides robust alternative to
parametric ANOVA.
}
\details{
This function uses the percentile bootstrap to test for equality of trimmed
means across J independent groups. It generates bootstrap samples from each
group and computes the distribution of the test statistic under the null
hypothesis.

The number of bootstrap samples is determined automatically if not specified:
5000 for J >= 10, otherwise 2000.

If WIN=TRUE, data are Winsorized before bootstrap resampling. This can
improve performance but should only be used with tr >= 0.2 and n >= 15.
}
\examples{
# Three groups
set.seed(123)
x1 <- rnorm(25, mean=0, sd=1)
x2 <- rnorm(25, mean=0.5, sd=1.2)
x3 <- rnorm(25, mean=1, sd=0.8)
pbanova(list(x1, x2, x3), tr=0.2, nboot=1000)
}
\seealso{
\code{\link{t1way}}, \code{\link{t1waybt}}, \code{\link{MEDanova}},
\code{\link{pbanovag}}
}
