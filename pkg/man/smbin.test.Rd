% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{smbin.test}
\alias{smbin.test}
\title{Test Binary Outcome Probabilities Across Quantile-Based Groups}
\usage{
smbin.test(
  x,
  y,
  IV = 1,
  Qsplit = 0.5,
  method = "SK",
  nboot = 1000,
  xout = FALSE,
  outfun = outpro,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{x}{Matrix or data frame of predictor variables}

\item{y}{Binary dependent variable vector (0/1 or two unique values)}

\item{IV}{Column index in x for splitting. Default is 1 (first column).}

\item{Qsplit}{Quantile(s) for splitting. Default is 0.5 (median split).
Can be vector (e.g., \code{c(.25,.5,.75)} for quartiles).}

\item{method}{Method for pairwise comparisons passed to \code{\link{binpair}}.
Default is 'SK'. Options include 'SK', 'KMS', etc.}

\item{nboot}{Number of bootstrap samples (if method requires). Default is 1000.}

\item{xout}{Logical. If \code{TRUE}, remove outliers before analysis.
Default is \code{FALSE}.}

\item{outfun}{Outlier detection function. Default is \code{\link{outpro}}.}

\item{SEED}{Logical or numeric. Controls random seed. Default is \code{TRUE}.}

\item{...}{Additional arguments passed to outlier function.}
}
\value{
List with components:
\describe{
\item{Group.summary}{Summary statistics for each group}
\item{output}{Pairwise comparison results from \code{\link{binpair}}}
}
}
\description{
Splits data based on quantiles of one independent variable and compares
probabilities of success (binary outcome) across resulting groups using
pairwise binomial tests.
}
\details{
This function:
\enumerate{
\item Splits data into groups based on quantiles of one variable
\item Computes number of successes and total observations per group
\item Performs all pairwise comparisons using \code{\link{binpair}}
}

The dependent variable y must be binary. Unlike \code{\link{smbin.inter}},
this function splits on only one variable.
}
\examples{
# Compare binary outcome across median split
set.seed(123)
x <- matrix(rnorm(200), ncol=2)
y <- rbinom(100, 1, plogis(x[,1]))
result <- smbin.test(x, y, IV=1, Qsplit=.5)
}
\seealso{
\code{\link{binpair}} for pairwise binomial tests,
\code{\link{smbin.inter}} for two-variable splitting
}
