% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{ancdet}
\alias{ancdet}
\title{Detailed ANCOVA Analysis with Multiple Design Points}
\usage{
ancdet(
  x1,
  y1,
  x2,
  y2,
  fr1 = 1,
  fr2 = 1,
  tr = 0.2,
  alpha = 0.05,
  method = "EP",
  plotit = TRUE,
  plot.dif = FALSE,
  pts = NA,
  sm = FALSE,
  pr = TRUE,
  xout = FALSE,
  outfun = out,
  MC = FALSE,
  npts = 25,
  p.crit = NULL,
  nreps = 5000,
  SEED = TRUE,
  EST = FALSE,
  SCAT = TRUE,
  xlab = "X",
  ylab = "Y",
  pch1 = "*",
  pch2 = "+",
  ...
)
}
\arguments{
\item{x1}{Covariate values for group 1.}

\item{y1}{Response values for group 1.}

\item{x2}{Covariate values for group 2.}

\item{y2}{Response values for group 2.}

\item{fr1}{Span for group 1 running interval smoother (default: 1).}

\item{fr2}{Span for group 2 running interval smoother (default: 1).}

\item{tr}{Trimming proportion (default: 0.2). Proportion of observations to
trim from each tail when computing winsorized correlation.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{method}{Effect size measure: "EP" (explanatory power), "QS" (quantile shift),
"AKP" (trimmed mean analog of Cohen's d), or "WMW" (P(X<Y)).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{plot.dif}{Logical. If TRUE, plots the difference with confidence band.}

\item{pts}{Design points for analysis. If NA, determined automatically.}

\item{sm}{Logical. Use smoothing for estimates.}

\item{pr}{Logical. Print progress information.}

\item{xout}{Logical. Remove outliers from covariates before analysis.}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{MC}{Logical. If \code{TRUE}, uses parallel processing via \code{mclapply}
(default: \code{FALSE}).}

\item{npts}{Number of covariate values to use (default: 25).}

\item{p.crit}{Critical p-value for familywise error control. If NULL, determined by simulation.}

\item{nreps}{Number of simulation replications for determining p.crit (default: 5000).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{EST}{Logical. If TRUE, estimate critical value via simulation rather than using stored values.}

\item{SCAT}{Logical. Show scatterplot of data.}

\item{xlab, ylab}{Axis labels for plots.}

\item{pch1, pch2}{Plot characters for groups 1 and 2.}

\item{...}{Additional arguments.}
}
\value{
List containing analysis results with adjusted confidence intervals
having simultaneous probability coverage 1-alpha.
}
\description{
Enhanced version of ancova that provides detailed analysis at multiple design points
along the covariate range. Computes group differences and confidence bands with
familywise error rate control.
}
\note{
One covariate only is allowed with this function.
}
\examples{
\dontrun{
x1 <- rnorm(50); y1 <- x1 + rnorm(50)
x2 <- rnorm(50); y2 <- 0.5*x2 + rnorm(50)
result <- ancdet(x1, y1, x2, y2, method="EP", npts=20)
}
}
