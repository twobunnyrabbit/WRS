% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{rmaha}
\alias{rmaha}
\title{Robust Mahalanobis Distances}
\usage{
rmaha(x)
}
\arguments{
\item{x}{Numeric matrix or data frame with observations in rows.}
}
\value{
Numeric vector of robust Mahalanobis distances, one for each observation.
}
\description{
Computes robust Mahalanobis distances using the Minimum Covariance Determinant (MCD)
estimator, scaled to match the chi-square distribution under normality.
}
\details{
Uses \code{\link[robustbase]{cov.mcd}} to obtain robust estimates of center and covariance.
The distances are scaled by a constant so that the median distance equals the square root
of the median of a chi-square distribution with p degrees of freedom, where p is the
number of columns in x. This scaling makes the distances comparable to classical
Mahalanobis distances under multivariate normality.
}
\examples{
\dontrun{
# Generate multivariate normal data
x <- matrix(rnorm(100 * 3), ncol = 3)

# Compute robust Mahalanobis distances
rd <- rmaha(x)

# Identify potential outliers
outliers <- rd > qchisq(0.975, df = 3)
}

}
\seealso{
\code{\link[robustbase]{cov.mcd}}, \code{\link{mahalanobis}}, \code{\link{outpro}}
}
\keyword{multivariate}
\keyword{robust}
