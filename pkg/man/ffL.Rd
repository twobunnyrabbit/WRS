% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{ffL}
\alias{ffL}
\title{FF Lambda Plot for Power Transformation Selection}
\usage{
ffL(x, y)
}
\arguments{
\item{x}{Numeric vector, matrix, or data frame. Predictor variable(s).}

\item{y}{Numeric vector. Response variable (must be positive for transformations).}
}
\value{
Numeric. Minimum correlation among all pairs of fitted value sets.
}
\description{
Creates a Four-Fold (FF) lambda plot to aid in selecting an appropriate power
transformation for the response variable in regression. Compares fitted values
across different Box-Cox transformations: λ = -1, -0.5, 0 (log), 0.5, 1.
}
\details{
The FF lambda plot helps select the power transformation \eqn{Y^\lambda} that best
linearizes and stabilizes variance in the regression relationship.

\strong{Method}:
\enumerate{
\item For each λ ∈ {-1, -0.5, 0, 0.5, 1}, transforms Y using:
\deqn{Y^{(\lambda)} = \begin{cases} (Y^\lambda - 1)/\lambda & \lambda \neq 0 \\
   \log(Y) & \lambda = 0 \end{cases}}
\item Regresses transformed Y on x to get fitted values
\item Creates scatterplot matrix (pairs plot) of all fitted value sets
\item Computes minimum correlation among all pairs
}

\strong{Interpretation}:
\itemize{
\item Strong linear relationships among fitted values → transformations similar
\item Nonlinear patterns → some transformations more appropriate
\item Choose λ that produces best model diagnostics (residual plots, etc.)
\item Returned value = minimum pairwise correlation among transformations
}

Lower correlations suggest greater sensitivity to transformation choice.

\strong{Graphics device}: For Unix/Linux, activate with \code{X11()} before calling.
}
\examples{
\dontrun{
# Exponential relationship that benefits from log transformation
set.seed(123)
x <- 1:50
y <- exp(0.1 * x + rnorm(50, sd = 0.2))
min_cor <- ffL(x, y)
# Pairs plot shows which transformations are similar
min_cor  # Minimum correlation between transformation pairs
}
}
\seealso{
\code{\link{lsfit}}, \code{\link{pairs}}, \code{\link[MASS]{boxcox}}
}
\keyword{hplot}
\keyword{regression}
