% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outliers.R
\name{outogk}
\alias{outogk}
\title{OGK-Based Outlier Detection}
\usage{
outogk(
  x,
  sigmamu = taulc,
  v = gkcov,
  op = TRUE,
  SEED = FALSE,
  beta = max(c(0.95, min(c(0.99, 1/nrow(x) + 0.94)))),
  n.iter = 1,
  plotit = TRUE,
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{sigmamu}{Univariate scale estimator (default: taulc).}

\item{v}{Bivariate covariance estimator (default: gkcov).}

\item{op}{Number of outliers to remove from each tail when using outlier
detection methods. Default varies by function.}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{beta}{Consistency factor for OGK (default: max(0.95, min(0.99, 1/n+0.94))).}

\item{n.iter}{Number of iterations for OGK (default: 1).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{...}{Additional arguments passed to OGK functions.}
}
\value{
A list with components:
\item{out.id}{Indices of outliers.}
\item{keep}{Indices of non-outliers.}
\item{distances}{Robust Mahalanobis distances.}
}
\description{
Detects outliers using the Orthogonalized Gnanadesikan-Kettenring (OGK)
robust covariance estimator.
}
\details{
If \code{op=TRUE} (recommended), uses robust Mahalanobis distance with the OGK
estimator and beta adjusted for approximately 5\% outlier rate under normality.
If \code{op=FALSE}, uses the distances from the OGK estimator directly. For
bivariate data with \code{plotit=TRUE}, creates a scatterplot marking outliers.
}
\seealso{
\code{\link{ogk.pairwise}}, \code{\link{out}}, \code{\link{outDETMCD}}
}
