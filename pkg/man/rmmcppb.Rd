% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00-utils-core.R
\name{rmmcppb}
\alias{rmmcppb}
\title{Repeated Measures Comparisons via Percentile Bootstrap}
\usage{
rmmcppb(
  x,
  y = NULL,
  alpha = 0.05,
  con = 0,
  est = onestep,
  plotit = FALSE,
  dif = TRUE,
  grp = NA,
  nboot = NA,
  BA = FALSE,
  hoch = FALSE,
  xlab = "Group 1",
  ylab = "Group 2",
  pr = TRUE,
  SEED = TRUE,
  SR = FALSE,
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{con}{J x C contrast matrix (default: 0 performs all pairwise comparisons).}

\item{est}{Measure of location (default: onestep). Use onestep or mom for method SR.}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{dif}{Logical: if TRUE (default), analyze difference scores; if FALSE,
analyze marginal distributions.}

\item{grp}{A vector indicating group membership. Can be numeric, factor, or
character. Used for grouping observations in multi-group comparisons.}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{BA}{Logical: when dif=FALSE, use correction term for p-values (default: FALSE).}

\item{hoch}{Logical: use Hochberg's method (default: FALSE, uses method SR).}

\item{xlab, ylab}{Labels for optional plot.}

\item{pr}{Logical. If \code{TRUE}, prints progress messages (default: \code{TRUE}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{SR}{Logical: use sequentially rejective method SR (default: FALSE).
Only valid when est=onestep or est=mom.}

\item{...}{Additional arguments passed to est function.}
}
\value{
A list with components:
\item{output}{Matrix with contrast results, estimates, and confidence intervals}
\item{con}{Contrast matrix used}
\item{num.sig}{Number of significant contrasts}
}
\description{
Performs percentile bootstrap comparisons for dependent groups (repeated measures).
Computes confidence intervals for linear contrasts with control of family-wise
error rate.
}
\details{
For two groups, data can be in x and y. Otherwise, x should be an n x J matrix
or list with J groups. Method SR controls family-wise error rate using a
sequentially rejective procedure. With dif=FALSE and M-estimators or MOM,
suggest using BA=TRUE and hoch=TRUE.
}
\examples{
# Repeated measures with 3 conditions (n=20)
set.seed(123)
x <- matrix(rnorm(60), ncol = 3)
x[,2] <- x[,2] + 0.5  # Add effect to condition 2
rmmcppb(x, nboot = 500)

}
