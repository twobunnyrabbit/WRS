% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{cidMULT}
\alias{cidMULT}
\title{Multiple Cliff's Analog Tests for Multivariate Data}
\usage{
cidMULT(x1, x2, alpha = 0.05, BMP = FALSE)
}
\arguments{
\item{x1}{Matrix or data frame for group 1 (rows are observations, columns are variables).}

\item{x2}{Matrix or data frame for group 2 (rows are observations, columns are variables).}

\item{alpha}{Significance level (default: 0.05).}

\item{BMP}{Logical. If \code{TRUE}, uses \code{bmp} function; if \code{FALSE}, uses \code{cidv2} (default: \code{FALSE}).}
}
\value{
A matrix with one row per variable and columns:
\itemize{
\item \code{n1}, \code{n2}: Sample sizes for groups 1 and 2
\item \code{p-value}: P-value for the comparison
\item \code{p.hat}: Estimate of P(X < Y)
\item \code{ci.low}, \code{ci.up}: Confidence interval bounds
\item \code{P(X<Y)}, \code{P(X=Y)}, \code{P(X>Y)}: Summary of pairwise comparisons
}
}
\description{
For two independent p-variate random variables, performs Cliff's analog
of the Wilcoxon-Mann-Whitney test for each variable separately.
}
\details{
This function applies Cliff's analog test to each variable in a multivariate
dataset. It provides a robust alternative to multivariate t-tests when comparing
distributions rather than just means.

The function does NOT adjust for multiple comparisons; users should apply
appropriate corrections (e.g., Bonferroni, FDR) if needed.
}
\examples{
\dontrun{
# Compare two groups on multiple variables
set.seed(123)
x1 <- matrix(rnorm(100), ncol = 5)
x2 <- matrix(rnorm(120, mean = 0.3), ncol = 5)

# Apply Cliff's test to each variable
result <- cidMULT(x1, x2)
print(result)
}
}
\seealso{
\code{\link{cidv2}}, \code{\link{bmp}}, \code{\link{cid}}
}
