% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{funyuenpb}
\alias{funyuenpb}
\title{Functional Data Analysis: Compare Two Groups at Multiple Time Points}
\usage{
funyuenpb(
  x1,
  x2,
  tr = 0.2,
  pts = NULL,
  npts = 25,
  plotit = TRUE,
  alpha = 0.05,
  SEED = TRUE,
  nboot = 2000,
  xlab = "T",
  ylab = "Est.dif",
  FBP = TRUE,
  method = "hochberg",
  COLOR = TRUE
)
}
\arguments{
\item{x1}{Matrix (n1-by-p) for group 1, where p is number of time points.}

\item{x2}{Matrix (n2-by-p) for group 2, same number of columns as x1.}

\item{tr}{Trimming proportion (default: 0.2 for 20\% trimming).}

\item{pts}{Vector of time point indices for comparisons (default: NULL for automatic selection).}

\item{npts}{Number of evenly-spaced points if pts=NULL (default: 25).}

\item{plotit}{Logical. If \code{TRUE}, plots results (default: \code{TRUE}).}

\item{alpha}{Significance level (default: 0.05).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed (default: \code{TRUE}).}

\item{nboot}{Number of bootstrap samples (default: 2000).}

\item{xlab}{X-axis label (default: 'T').}

\item{ylab}{Y-axis label (default: 'Est.dif').}

\item{FBP}{Logical. If \code{TRUE}, uses family-wise error control (default: \code{TRUE}).}

\item{method}{Multiple comparison adjustment method (default: 'hochberg').}

\item{COLOR}{Logical. If \code{TRUE}, uses color in plot (default: \code{TRUE}).}
}
\value{
A list with test results at each time point including estimates,
confidence intervals, and adjusted p-values.
}
\description{
Compares two groups using trimmed means with percentile bootstrap for
functional data (multiple measurements over time or space). Tests are
performed at specified time points with FWE control.
}
\details{
Designed for functional data where many measurements are taken over time.
Uses Yuen's test with percentile bootstrap at multiple time points.
Controls family-wise error rate across time points.
}
\examples{
\dontrun{
# Functional data: 30 subjects, 50 time points
set.seed(123)
x1 <- matrix(rnorm(30 * 50), nrow = 30)
x2 <- matrix(rnorm(30 * 50, mean = 0.5), nrow = 30)
funyuenpb(x1, x2, npts = 10, nboot = 500)
}
}
\seealso{
\code{\link{yuenbt}}, \code{\link{pb2gen}}
}
