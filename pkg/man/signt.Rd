% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{signt}
\alias{signt}
\title{Sign Test for Paired Data}
\usage{
signt(
  x,
  y = NULL,
  dif = NULL,
  alpha = 0.05,
  method = "AC",
  AUTO = TRUE,
  PVSD = FALSE
)
}
\arguments{
\item{x}{Numeric vector of first group data, or a matrix/data frame with two columns,
or a list with two components when \code{y = NULL}.}

\item{y}{Numeric vector of second group data. If \code{NULL}, \code{x} should be a matrix
or list with two columns/components (default: \code{NULL}).}

\item{dif}{Numeric vector of pre-computed difference scores. If specified, tests
whether P(dif < 0) = 0.5 (default: \code{NULL}).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{method}{Character string specifying the confidence interval method:
\itemize{
\item \code{"AC"}: Agresti-Coull method (default)
\item \code{"P"}: Pratt's method
\item \code{"CP"}: Clopper-Pearson method
\item \code{"KMS"}: Kulinskaya et al. (2008, p. 140)
\item \code{"WIL"}: Wilson's method
\item \code{"SD"}: Schilling-Doi method (returns CI but no p-value)
}}

\item{AUTO}{Logical. If \code{TRUE} and n < 35, uses Schilling-Doi method
automatically (default: \code{TRUE}).}

\item{PVSD}{Logical. If \code{TRUE}, uses Schilling-Doi method for p-value computation
(default: \code{FALSE}).}
}
\value{
A list with components:
\item{Prob_x_less_than_y}{Estimated probability that x < y}
\item{ci}{Confidence interval for the probability}
\item{n}{Original sample size}
\item{N}{Number of paired observations that are not equal to one another}
\item{p.value}{P-value for testing if probability equals 0.5}
}
\description{
Performs a sign test for two dependent groups with confidence interval for
the probability that x < y.
}
\details{
The sign test is a non-parametric test for paired data that only uses the
direction (sign) of differences, ignoring their magnitude. Zero differences
are excluded from the analysis.

The function tests the null hypothesis that P(x < y) = 0.5 against a
two-sided alternative. Multiple confidence interval methods are available,
with automatic selection based on sample size when \code{AUTO = TRUE}.
}
\examples{
# Paired data comparison
x <- c(10, 12, 15, 18, 20, 22, 25, 28)
y <- c(12, 14, 16, 20, 22, 24, 27, 30)
signt(x, y)

# Data in matrix format
dat <- cbind(x, y)
signt(dat)

# Pre-computed differences
diff <- x - y
signt(dif = diff)
}
