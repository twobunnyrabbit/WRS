% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{ancovad.ESci}
\alias{ancovad.ESci}
\title{Dependent Samples ANCOVA Effect Size with Confidence Intervals}
\usage{
ancovad.ESci(
  x1,
  y1,
  x2,
  y2,
  pts = NULL,
  regfun = tsreg,
  alpha = 0.05,
  nboot = 100,
  SEED = TRUE,
  QM = FALSE,
  ql = 0.2,
  xout = FALSE,
  outfun = outpro,
  xlab = "Pts",
  ylab = "Y",
  method = "hoch",
  plotit = TRUE
)
}
\arguments{
\item{x1}{A numeric vector of covariate values at time 1 (or condition 1).}

\item{y1}{A numeric vector of outcome values at time 1.}

\item{x2}{A numeric vector of covariate values at time 2 (or condition 2),
paired with x1.}

\item{y2}{A numeric vector of outcome values at time 2, paired with y1.}

\item{pts}{A numeric vector specifying design points where effect sizes are
computed. If \code{NULL}, points are selected automatically (see \code{QM} parameter).}

\item{regfun}{Regression function to use (default: \code{tsreg} for Theil-Sen).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{QM}{Logical. If \code{FALSE} (default) and \code{pts = NULL}, uses default points
from \code{\link{ancovad.ES}}. If \code{TRUE}, selects three points based on
quantiles determined by \code{ql}.}

\item{ql}{Quantile level for determining range of design points when \code{QM = TRUE}
(default: 0.2, uses 0.2 and 0.8 quantiles).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{xlab}{Label for x-axis in plot (default: 'Pts').}

\item{ylab}{Label for y-axis in plot (default: 'Y').}

\item{method}{Method for adjusting p-values for multiple comparisons (default: 'hoch'
for Hochberg). See \code{\link{p.adjust}} for options.}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}
}
\value{
A matrix with columns:
\describe{
\item{pts}{The design points where effect sizes were computed.}
\item{Est.}{Effect size estimate at each point.}
\item{SE}{Bootstrap standard error of the effect size.}
\item{Test.Stat}{Test statistic (Est./SE).}
\item{ci.low}{Lower confidence interval bound.}
\item{ci.up}{Upper confidence interval bound.}
\item{p-value}{Unadjusted p-value for testing H0: effect size = 0.}
\item{p.adjusted}{P-value adjusted for multiple comparisons.}
}
}
\description{
Computes robust effect sizes for two dependent (paired/repeated measures) regression
lines with bootstrap confidence intervals and hypothesis tests. Tests whether the
effect size significantly differs from zero at specified design points.
}
\details{
This function extends \code{\link{ancovad.ES}} by adding inferential statistics.
For each design point, it computes:
\enumerate{
\item Effect size using \code{\link{ancovad.ES}}
\item Bootstrap standard error via \code{ancovad.ES.SEpb}
\item Confidence intervals assuming normality
\item P-values for testing whether effect size differs from zero
\item Adjusted p-values for multiple comparisons
}

The heteroscedastic effect size accounts for the conditional dispersion of y
given x, rather than using an overall measure of location.

If \code{plotit = TRUE}, displays effect size estimates with confidence bands.
}
\examples{
\dontrun{
# Paired data with effect size inference
set.seed(123)
n <- 30
x1 <- rnorm(n)
y1 <- 2 + 3*x1 + rnorm(n)
x2 <- x1 + rnorm(n, 0, 0.5)
y2 <- 2.5 + 3*x2 + rnorm(n)

result <- ancovad.ESci(x1, y1, x2, y2, nboot = 500)
print(result)
}
}
\seealso{
\code{\link{ancovad.ES}}, \code{\link{Dancova}}
}
