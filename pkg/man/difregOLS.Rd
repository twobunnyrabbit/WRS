% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression.R
\name{difregOLS}
\alias{difregOLS}
\title{Compare OLS Regression Parameters for Dependent Groups}
\usage{
difregOLS(
  x1,
  y1,
  x2,
  y2,
  regfun = lsfit,
  xout = FALSE,
  outfun = outpro,
  nboot = 200,
  alpha = 0.05,
  SEED = TRUE,
  plotit = FALSE,
  xlab = "X",
  ylab = "Y",
  ...
)
}
\arguments{
\item{x1}{Numeric vector or matrix of predictor variable(s) for the first group.}

\item{y1}{Numeric vector of the dependent variable for the first group.}

\item{x2}{Numeric vector or matrix of predictor variable(s) for the second group.}

\item{y2}{Numeric vector of the dependent variable for the second group.}

\item{regfun}{Regression function (default: \code{lsfit} for OLS).
Should not be changed unless using a specific OLS variant.}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{plotit}{Logical. If \code{TRUE}, creates a scatterplot with both
regression lines (default: \code{FALSE}).}

\item{xlab}{Label for x-axis when \code{plotit=TRUE} (default: "X").}

\item{ylab}{Label for y-axis when \code{plotit=TRUE} (default: "Y").}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
A list with components:
\describe{
\item{n}{Original sample size before any outlier removal}
\item{n.keep}{Sample size after outlier removal (if \code{xout=TRUE})}
\item{est.dif}{Difference in estimates (group 1 - group 2)}
\item{est.1}{OLS coefficient estimates for group 1}
\item{est.2}{OLS coefficient estimates for group 2}
\item{test.stat}{t-statistics for each parameter difference}
\item{standard.error}{Bootstrap standard errors}
\item{p.values}{Two-sided p-values for each parameter difference}
\item{conf.intervals}{Matrix with columns: Param, ci.low, ci.hi}
}
}
\description{
Computes bootstrap confidence intervals and tests for comparing ordinary
least squares (OLS) regression parameters between two dependent (paired) groups.
This is the OLS-specific version of \code{\link{difreg}}.
}
\details{
This function compares OLS regression parameters between two dependent (paired)
groups using bootstrap methods. It's specifically designed for OLS regression
with paired/repeated measures data.

\strong{Method}:
\enumerate{
\item Bootstrap resamples preserve pairing by resampling entire rows
\item Fits OLS to each group in each bootstrap sample
\item Computes differences in parameters
\item Uses bootstrap SE and t-distribution for inference
}

\strong{Inference}: Unlike \code{\link{difreg}} which uses percentile bootstrap,
this function uses bootstrap standard errors with t-distribution critical values.
This provides both confidence intervals and p-values.

\strong{When to use}:
\itemize{
\item OLS regression (not robust regression)
\item Paired/repeated measures design (pre-post, matched pairs, etc.)
\item Want parametric-style inference with bootstrap SEs
\item Comparing specific parameters (intercepts, slopes)
}

\strong{Comparison with alternatives}:
\itemize{
\item \code{\link{difreg}}: For robust regression (tsreg, chreg, etc.)
\item \code{\link{DregGOLS}}: For global test of all parameters
\item \code{\link{reg2ci}}: For independent groups
}
}
\examples{
\dontrun{
# Pre-post intervention data
set.seed(123)
n <- 50
x1 <- rnorm(n)
y1 <- 1 + 2*x1 + rnorm(n)
x2 <- x1 + rnorm(n, sd=0.3)
y2 <- 1.5 + 2.5*x2 + rnorm(n)

# Compare OLS regression parameters
result <- difregOLS(x1, y1, x2, y2, nboot=500)
print(result$p.values)
print(result$conf.intervals)

# Multiple predictors
x1 <- matrix(rnorm(100), ncol=2)
y1 <- 1 + 2*x1[,1] - x1[,2] + rnorm(50)
x2 <- x1 + matrix(rnorm(100, sd=0.2), ncol=2)
y2 <- 1.5 + 2.2*x2[,1] - 1.1*x2[,2] + rnorm(50)
difregOLS(x1, y1, x2, y2, nboot=500, plotit=FALSE)
}

}
\references{
Wilcox, R.R. (2022). \emph{Introduction to Robust Estimation and Hypothesis Testing}
(5th ed.). Academic Press. Chapter 10.
}
\seealso{
\code{\link{difreg}} for robust regression version,
\code{\link{DregGOLS}} for global test,
\code{\link{reg2ci}} for independent groups
}
