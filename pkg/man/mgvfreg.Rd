% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression-advanced.R
\name{mgvfreg}
\alias{mgvfreg}
\title{Regression with MGV Outlier Detection}
\usage{
mgvfreg(x, y, regfun = tsreg, outfun = outbox, plotit = TRUE)
}
\arguments{
\item{x}{A numeric vector or matrix containing predictor values.}

\item{y}{A numeric vector containing the response variable.}

\item{regfun}{Regression function to use on non-outlier points
(default: \code{tsreg} for Theil-Sen regression). Can be any regression
function that returns \code{coef} and \code{residuals} components.}

\item{outfun}{Outlier detection function to apply to the MGV method
(default: \code{outbox}). Used within \code{outmgvf()}.}

\item{plotit}{Logical. If \code{TRUE} and data is bivariate, plots the data
and overlays the regression line (default: \code{TRUE}).}
}
\value{
A list with components:
\item{coef}{Regression coefficients from the regression on non-outlier points.}
\item{residuals}{Residuals from the regression fit.}
}
\description{
Performs regression after removing outliers detected using the faster
inward Minimum Generalized Variance (MGV) method. Fits regression only
on points not flagged as outliers.
}
\details{
This function combines outlier detection with robust regression:
\enumerate{
\item Identifies outliers using the faster inward MGV method (\code{outmgvf})
\item Removes flagged outliers from the dataset
\item Fits the specified regression function to the remaining points
}

The MGV (Minimum Generalized Variance) method provides a computationally
efficient approach to multivariate outlier detection, making it suitable
for larger datasets or when speed is important.

Missing values are automatically removed before analysis.
}
\note{
Requires the \code{SEED} variable to be defined in the calling environment
for reproducible outlier detection.
}
\examples{
\dontrun{
# Regression with MGV outlier detection
set.seed(123)
x <- rnorm(50)
y <- 2*x + rnorm(50)
# Add outliers
x[1:3] <- c(5, 5, -5)
y[1:3] <- c(-5, 5, 5)
result <- mgvfreg(x, y)
result$coef
}
}
\references{
Wilcox, R.R. (2022). Introduction to Robust Estimation and Hypothesis Testing
(5th ed.). Academic Press.
}
\seealso{
\code{\link{mgvreg}}, \code{\link{outmgvf}}, \code{\link{tsreg}}
}
