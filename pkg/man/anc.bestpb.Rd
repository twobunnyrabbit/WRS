% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{anc.bestpb}
\alias{anc.bestpb}
\title{Select Best Group - Bootstrap Percentile Method}
\usage{
anc.bestpb(
  x,
  loc.fun = tmean,
  nboot = 3000,
  p.crit = NULL,
  alpha = 0.05,
  iter = 5000,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{x}{Matrix or list of data where each column (or list element) is a group.}

\item{loc.fun}{Location estimator function (default: tmean for trimmed mean).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{p.crit}{Critical p-values for comparisons. If NULL, determined via simulation.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{iter}{Number of iterations for determining critical values (default: 5000).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{...}{Additional arguments passed to loc.fun.}
}
\value{
List with:
\item{Group.with.largest.est}{Index of group with largest estimate}
\item{Best}{Index of best group if significant decision can be made, 'No Decision' otherwise}
\item{n}{Sample sizes}
\item{output}{Matrix with detailed comparison results}
}
\description{
Bootstrap percentile version of anc.best. Identifies the group with the largest
location estimate (default: trimmed mean) and makes inference using bootstrap
percentile confidence intervals with familywise error rate control.
}
\examples{
\dontrun{
x <- list(rnorm(30), rnorm(30, mean=0.5), rnorm(30, mean=1))
result <- anc.bestpb(x, nboot=2000, alpha=0.05)
}
}
