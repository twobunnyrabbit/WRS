% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{RCmcp}
\alias{RCmcp}
\title{Row and Column Pairwise Comparisons in Two-Way Design}
\usage{
RCmcp(
  J,
  K,
  x,
  alpha = 0.05,
  est = tmean,
  PB = FALSE,
  SEED = TRUE,
  pr = TRUE,
  ...
)
}
\arguments{
\item{J}{Integer. Number of levels for Factor A (rows).}

\item{K}{Integer. Number of levels for Factor B (columns).}

\item{x}{Data in list mode, matrix, or data frame with J*K groups.}

\item{alpha}{Numeric. Family-wise Type I error rate (default: 0.05).}

\item{est}{Estimator function (default: \code{tmean}).}

\item{PB}{Logical. If \code{TRUE}, uses percentile bootstrap method.
If \code{FALSE} (default), uses method based on trimmed means.}

\item{SEED}{Logical. If \code{TRUE} (default), sets random seed for reproducibility.}

\item{pr}{Logical. If \code{TRUE} (default), prints informational messages.}

\item{...}{Additional arguments passed to \code{est} or comparison functions.}
}
\value{
A list containing:
\item{A}{List of length J. Element j contains pairwise comparisons among
K levels of Factor B at the j-th level of Factor A}
\item{B}{List of length K. Element k contains pairwise comparisons among
J levels of Factor A at the k-th level of Factor B}
}
\description{
For a J × K two-way design, performs all pairwise comparisons among levels
of Factor B within each level of Factor A, and all pairwise comparisons
among levels of Factor A within each level of Factor B.
}
\details{
This function provides simple effect comparisons by examining pairwise
differences within each level of the other factor. It complements
tests of main effects and interactions.

When \code{PB=FALSE}, uses \code{\link{lincon}} with trimming.
When \code{PB=TRUE}, uses \code{\link{linconpb}} for percentile bootstrap.
}
\seealso{
\code{\link{lincon}}, \code{\link{linconpb}}, \code{\link{r2mcp}}
}
\keyword{htest}
