% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{test.4.break}
\alias{test.4.break}
\title{Test for Regression Breakpoint}
\usage{
test.4.break(
  x,
  y,
  int = NULL,
  xout = TRUE,
  regfun = tsreg,
  outfun = outpro,
  plotit = TRUE,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{x}{Numeric vector. Independent variable (predictor)}

\item{y}{Numeric vector. Dependent variable (response)}

\item{int}{Numeric vector. Search interval for breakpoint (see \code{reg.break})}

\item{xout}{Logical. If TRUE, remove outliers before analysis (default: TRUE)}

\item{regfun}{Function. Regression function to use (default: \code{tsreg})}

\item{outfun}{Function. Outlier detection function (default: \code{outpro})}

\item{plotit}{Logical. If TRUE, create diagnostic plots (default: TRUE)}

\item{SEED}{Logical. If TRUE, set random seed for reproducibility (default: TRUE)}

\item{...}{Additional arguments passed to regression function}
}
\value{
List with components:
\item{n}{Sample sizes for regions below and above breakpoint}
\item{break.est}{Estimated breakpoint location}
\item{test.results}{Matrix with test results for intercept and slope,
including confidence intervals, p-values, and estimates for each region}
}
\description{
Tests for the existence of a breakpoint in regression by estimating the
breakpoint location and comparing slopes and intercepts before and after.
}
\details{
This function:
\enumerate{
\item Estimates the breakpoint location using \code{reg.break}
\item Fits separate regression models for data below and above the breakpoint
\item Compares the slopes and intercepts using \code{reg2ci}
\item Tests whether the parameters differ significantly
}

Rejecting the null hypothesis provides evidence that a breakpoint exists.
Currently limited to a single predictor variable.
}
\examples{
# Create data with a breakpoint
x <- 1:100
y <- c(2 + 0.5*x[1:50] + rnorm(50),
       10 + 2*x[51:100] + rnorm(50))
result <- test.4.break(x, y)

}
\seealso{
\code{\link{reg.break}}, \code{\link{reg2ci}}, \code{\link{tsreg}}
}
\keyword{regression}
\keyword{robust}
