% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{rplotCI}
\alias{rplotCI}
\title{Running Interval Smoother with Confidence Band (Trimmed Mean)}
\usage{
rplotCI(
  x,
  y,
  tr = 0.2,
  fr = 0.5,
  p.crit = NA,
  plotit = TRUE,
  scat = TRUE,
  SEED = TRUE,
  pyhat = FALSE,
  npts = 25,
  xout = FALSE,
  xlab = "X",
  ylab = "Y",
  low.span = 2/3,
  nmin = 12,
  pr = TRUE,
  outfun = outpro,
  LPCI = FALSE,
  MID = TRUE,
  alpha = 0.05,
  pch = ".",
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{tr}{Trimming proportion (default: 0.2). Proportion of observations to
trim from each tail when computing winsorized correlation.}

\item{fr}{Span parameter controlling amount of smoothing (default: 0.5).
Lower values = less smoothing.}

\item{p.crit}{Critical p-value for confidence band (default: NA, automatically
estimated). Pre-computed values available for npts=10 or 25 and alpha=0.05.}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{scat}{Logical. If \code{TRUE}, includes scatter plot of data points (default: \code{TRUE}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{npts}{Number of points where confidence intervals are computed (default: 25).
Pre-computed critical values available for 10 or 25 points.}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{xlab}{Label for x-axis in plots (default: "X" or "VAR 1").}

\item{ylab}{Label for y-axis in plots (default: "Y" or "VAR 2").}

\item{low.span}{Span parameter for lowess smoothing of confidence band (default: 2/3).}

\item{nmin}{Minimum sample size required in each interval (default: 12).}

\item{pr}{Logical. If \code{TRUE}, prints progress messages (default: \code{TRUE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{LPCI}{Logical. If \code{TRUE}, smooths the confidence intervals via lowess
for a smoother appearance (default: \code{FALSE}).}

\item{MID}{Logical. If \code{TRUE}, computes intervals in middle range of data
(default: \code{TRUE}).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{pch}{Plotting character for scatter plot (default: '.').}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
List with components:
\item{output}{Matrix with columns: pts, y.hat, ci.low, ci.up, n.used}
\item{str}{Strength of association measure}
\item{n}{Total sample size}
\item{n.keep}{Sample size after outlier removal}
}
\description{
Plots a running interval smoother based on trimmed means with an approximate
simultaneous confidence band. Provides both point estimates and confidence
intervals at specified points along the predictor range.
}
\details{
The function creates a confidence band with simultaneous probability coverage
of 1-alpha across \code{npts} points. For n >= 50, critical values are pre-computed
for npts=10 or 25 with alpha=0.05. For other combinations, critical values
are estimated via simulation (slower).

The running interval smoother uses a trimmed mean (controlled by \code{tr}) within
a moving window (controlled by \code{fr}). Set LPCI=TRUE for a smoother confidence
band appearance.
}
\examples{
\dontrun{
# Running interval smoother with confidence band
x <- rnorm(100)
y <- 2*x + rnorm(100)
rplotCI(x, y, fr=0.5)

# Smoother confidence band
rplotCI(x, y, fr=0.5, LPCI=TRUE)
}
}
\seealso{
\code{\link{rplotCIS}}, \code{\link{rplotCIsmm}}, \code{\link{rplotpbCI}}
}
