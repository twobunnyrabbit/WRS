% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{nelder}
\alias{nelder}
\title{Nelder-Mead Simplex Optimization}
\usage{
nelder(
  x,
  N,
  FN,
  START = c(rep(1, N)),
  STEP = c(rep(1, N)),
  XMIN = c(rep(0, N)),
  XSEC = c(rep(0, N))
)
}
\arguments{
\item{x}{Matrix (n × p). Data matrix passed to the function being minimized.}

\item{N}{Integer. Number of parameters to optimize.}

\item{FN}{Function to minimize. Must have signature \code{FN(x, theta)} where
\code{theta} is a vector of N parameter values.}

\item{START}{Numeric vector of length N. Starting parameter values (default: all 1s).}

\item{STEP}{Numeric vector of length N. Initial step sizes for simplex vertices (default: all 1s).}

\item{XMIN}{Numeric vector of length N. Placeholder for minimum (default: all 0s).}

\item{XSEC}{Numeric vector of length N. Placeholder for second-best (default: all 0s).}
}
\value{
Numeric vector of length N containing parameter values that minimize \code{FN}.
}
\description{
Minimizes a function of N parameters using the Nelder-Mead simplex algorithm.
Implementation based on Olsson (1974).
}
\details{
\strong{Nelder-Mead simplex algorithm} is a derivative-free optimization method:
\enumerate{
\item Constructs a simplex (N+1 vertices in N dimensions)
\item Iteratively improves the simplex through reflection, expansion, and contraction
\item Converges when vertices are sufficiently close (REQMIN = 0.0000001)
\item Maximum iterations: 500
}

\strong{Convergence criteria}:
\itemize{
\item Relative function value difference < REQMIN
\item Vertex coordinates converge
\item Maximum iterations reached
}

This is a general-purpose optimizer for smooth or moderately non-smooth functions
when derivatives are unavailable or expensive to compute.
}
\references{
Olsson, D. M. (1974). A sequential simplex program for solving minimization problems.
Journal of Quality Technology, 6, 53-57.
}
\seealso{
\code{\link{nelderv2}} for version with configurable tolerance
}
\keyword{internal}
\keyword{optimize}
