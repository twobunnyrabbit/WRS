% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{linWMW}
\alias{linWMW}
\title{Linear Contrast with Wilcoxon-Mann-Whitney Method}
\usage{
linWMW(x, con, locfun = median, nreps = 100, SEED = TRUE)
}
\arguments{
\item{x}{Data matrix, data frame, or list. Each column (for matrix/data frame)
or element (for list) represents a different group.}

\item{con}{Vector of contrast coefficients. Must sum to zero.}

\item{locfun}{Function for computing the measure of location (default: \code{median}).
Can be any function that takes a vector and returns a scalar (e.g., \code{mean}, \code{tmean}).}

\item{nreps}{Number of replications for determining the distribution (default: 100).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducibility (default: \code{TRUE}).}
}
\value{
A list with components:
\itemize{
\item \code{p}: Estimated probability that the linear contrast is less than zero
\item \code{center}: Estimated measure of location of the linear contrast
}
}
\description{
Determines the distribution of a linear contrast Y_i = sum_j c_j*X_j and
estimates P(Y < 0) and a measure of location based on the specified function.
}
\details{
This function creates a linear contrast across J groups and uses a resampling
approach to estimate the distribution of the contrast. For each replication,
it samples \code{nmin} observations from each group (where \code{nmin} is the minimum
group size) and computes the linear contrast.

The contrast coefficients must sum to zero (a requirement for linear contrasts).
The function estimates:
\itemize{
\item The probability that the contrast is negative
\item A measure of central tendency (default: median) of the contrast distribution
}

This is useful for testing specific hypotheses about group differences using
a distribution-free approach based on the WMW method.
}
\note{
Missing values are automatically removed from all groups.
}
\examples{
\dontrun{
# Compare three groups with contrast (-1, 0.5, 0.5)
set.seed(123)
x1 <- rnorm(30, mean = 0)
x2 <- rnorm(30, mean = 0.5)
x3 <- rnorm(30, mean = 0.5)
x <- list(x1, x2, x3)

# Test if group 1 differs from average of groups 2 and 3
result <- linWMW(x, con = c(-1, 0.5, 0.5))
print(result)

# Use mean instead of median as location measure
linWMW(x, con = c(-1, 0.5, 0.5), locfun = mean)
}
}
\seealso{
\code{\link{linWMWpb}} for bootstrap confidence intervals,
\code{\link{wmw.ref.dif}} for WMW-based comparisons
}
