% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{ks}
\alias{ks}
\title{Kolmogorov-Smirnov Two-Sample Test}
\usage{
ks(x, y, w = FALSE, sig = TRUE, alpha = 0.05)
}
\arguments{
\item{x}{Numeric vector. First sample data.}

\item{y}{Numeric vector. Second sample data.}

\item{w}{Logical. If TRUE, computes weighted KS statistic; if FALSE (default),
standard KS statistic.}

\item{sig}{Logical. If TRUE (default), computes exact significance level.}

\item{alpha}{Numeric. Significance level for critical value (default: 0.05).}
}
\value{
List with components:
\describe{
\item{test}{KS test statistic value}
\item{critval}{Critical value for specified alpha}
\item{p.value}{Exact p-value (if sig=TRUE), otherwise NA}
}
}
\description{
Computes the Kolmogorov-Smirnov test statistic for comparing two independent
samples, with options for weighted version and exact p-values.
}
\details{
Computes the Kolmogorov-Smirnov test for comparing distributions:
\itemize{
\item Standard KS: \eqn{D = \max_z |F_1(z) - F_2(z)|}
\item Weighted KS: Accounts for pooled variance, more powerful for location shifts
\item Handles ties: Exact p-values for unweighted test; conservative for weighted
}

The exact significance level is computed when \code{sig=TRUE}. For ties in
the unweighted test, the p-value is exact. For the weighted test with ties,
the reported significance level may be conservative.

Missing values are automatically removed.
}
\examples{
set.seed(123)
x <- rnorm(30)
y <- rnorm(30, mean = 0.5)
ks(x, y)

# Weighted version
ks(x, y, w = TRUE)
}
\seealso{
\code{\link{ks.test}} for base R KS test,
\code{\link{ksw.crit}} for weighted KS critical values,
\code{\link{ksnorm.test}} for testing normality
}
