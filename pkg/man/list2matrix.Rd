% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{list2matrix}
\alias{list2matrix}
\title{Convert List to Matrix}
\usage{
list2matrix(x)
}
\arguments{
\item{x}{List where each element is a numeric vector (can have different lengths).}
}
\value{
Matrix where:
\describe{
\item{Columns}{Correspond to list elements (J columns for length-J list)}
\item{Rows}{Number of rows equals length of longest list element}
\item{Missing values}{Shorter vectors are padded with NA to match longest}
}
}
\description{
Takes data stored in a list and converts it to a matrix. Each list element
becomes a column in the resulting matrix. Handles unequal-length list elements
by padding with NA values.
}
\details{
This is the inverse operation of \code{\link{listm}}. Useful for converting
group-based data from list format (convenient for analysis) to matrix format
(convenient for visualization or certain computations).
}
\examples{
# Unequal length groups
x <- list(c(1, 2, 3), c(4, 5), c(6, 7, 8, 9))
list2matrix(x)  # 4x3 matrix with NAs
}
\seealso{
\code{\link{listm}} for matrix to list conversion,
\code{\link{list2vec}} for flattening list to vector,
\code{\link{listv2mat}} for equal-length list vectors to matrix
}
\keyword{manip}
