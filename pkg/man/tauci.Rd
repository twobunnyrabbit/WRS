% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{tauci}
\alias{tauci}
\title{Kendall's Tau with Bootstrap CI}
\usage{
tauci(x, y, nboot = 1000, alpha = 0.05, SEED = TRUE, MC = FALSE)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{MC}{Logical. If \code{TRUE}, uses parallel processing (default: \code{FALSE}).}
}
\value{
List with components:
\item{cor.ci}{Bootstrap confidence interval.}
\item{p.value}{Two-sided p-value.}
\item{cor.est}{Kendall's tau estimate.}
}
\description{
Computes bootstrap confidence interval for Kendall's tau correlation.
}
\details{
Wrapper function that calls \code{corb()} (or \code{corbMC()} if \code{MC = TRUE}) with
\code{corfun = tau}. See \code{tau()} for details on Kendall's tau.
}
\examples{
set.seed(123)
x <- 1:50
y <- x + rnorm(50, sd = 10)
tauci(x, y, nboot = 500)

\dontrun{
# Parallel version (not Windows)
tauci(x, y, nboot = 2000, MC = TRUE)
}
}
\seealso{
\code{\link{tau}}, \code{\link{corb}}, \code{\link{corbMC}}
}
