% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{mscorci}
\alias{mscorci}
\title{Multiple Skipped Correlation Confidence Intervals}
\usage{
mscorci(
  x,
  y = NULL,
  nboot = 1000,
  alpha = c(0.05, 0.025, 0.01),
  SEED = TRUE,
  STAND = TRUE,
  corfun = pcor,
  outfun = outpro,
  crit.pv = NULL,
  pvals = NULL,
  hoch = FALSE,
  iter = 500,
  pval.SEED = TRUE,
  pr = TRUE
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Optional second matrix or vector to combine with \code{x} (default: \code{NULL}).}

\item{nboot}{Number of bootstrap samples (default: 1000).}

\item{alpha}{Vector of significance levels for critical p-values (default: c(.05, .025, .01)).
FWE rate is taken to be \code{alpha[1]}.}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{STAND}{Logical. Standardize variables before outlier detection (default: \code{TRUE}).}

\item{corfun}{Correlation function to use. Common options: \code{pbcor} (percentage
bend), \code{pcor} (Pearson), \code{wincor} (winsorized), \code{spear} (Spearman).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{crit.pv}{Pre-computed critical p-values (default: \code{NULL} for automatic).}

\item{pvals}{Pre-computed p-value distribution for minimum p-value (default: \code{NULL}).}

\item{hoch}{Logical. Use Hochberg adjustment if \code{TRUE} (default: \code{FALSE}).}

\item{iter}{Number of iterations for critical p-value simulation (default: 500).}

\item{pval.SEED}{Logical. Set seed when computing critical p-values (default: \code{TRUE}).}

\item{pr}{Logical. Print progress messages (default: \code{TRUE}).}
}
\value{
List with components:
\item{p.values}{Matrix of p-values for testing zero correlation.}
\item{cor.est}{Vector of correlation estimates (upper triangle only).}
\item{confidence.int}{Matrix with columns: Var i, Var j, ci.low, ci.up (only if \code{hoch = FALSE}).}
\item{critical.p.values}{Critical p-values for controlling FWE.}
\item{hoch.adjusted.p.values}{Hochberg-adjusted p-values (only if \code{hoch = TRUE}).}
}
\description{
Tests zero correlation for all pairs of variables using skipped correlation,
controlling familywise error rate (FWE). Optionally computes confidence intervals.
}
\details{
For p-variate data, tests H0: rho_ij = 0 for all pairs (i,j) while controlling
the probability of one or more Type I errors at level \code{alpha[1]} (default 0.05).

\strong{Two approaches:}
\itemize{
\item \strong{hoch = FALSE} (recommended for alpha < .05 or n < 60): Computes critical p-values
via simulation (can be slow). Returns confidence intervals.
\item \strong{hoch = TRUE} (faster): Uses Hochberg adjustment. Reasonable for n >= 60 and
alpha = .05, but may have reduced power. Returns unadjusted 1-alpha\link{1} confidence
intervals.
}

Critical p-values depend on n and p. Pre-compute them using \code{mscorci.cr()} and
supply via \code{crit.pv} to reduce execution time:

\if{html}{\out{<div class="sourceCode">}}\preformatted{pv <- mscorci.cr(n, p)$crit.p.values
mscorci(x, crit.pv = pv)
}\if{html}{\out{</div>}}
}
\examples{
\dontrun{
set.seed(123)
x <- matrix(rnorm(60 * 4), ncol = 4)

# Using Hochberg adjustment (faster)
result <- mscorci(x, hoch = TRUE)
result$hoch.adjusted.p.values

# Full method with confidence intervals (slower)
result2 <- mscorci(x, hoch = FALSE, nboot = 500)
result2$confidence.int
}
}
\seealso{
\code{\link{mscorci.cr}}, \code{\link{mscor}}, \code{\link{scorci}}, \code{\link{mscorpb}}
}
