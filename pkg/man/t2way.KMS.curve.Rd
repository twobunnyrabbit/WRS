% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anova.R
\name{t2way.KMS.curve}
\alias{t2way.KMS.curve}
\title{Plot KMS Effect Size Curve for 2x2 Design with Covariate}
\usage{
t2way.KMS.curve(
  x,
  y,
  pts = NULL,
  SW = FALSE,
  npts = 15,
  xlab = "X",
  ylab = "Effect.Size"
)
}
\arguments{
\item{x}{List of length 4 containing covariate values for each of the four groups}

\item{y}{List of length 4 containing response values for each of the four groups}

\item{pts}{Vector of covariate values at which to evaluate effect size
(NULL = use deciles from 10th to 90th percentile)}

\item{SW}{Logical. If TRUE, switch rows and columns (default: FALSE)}

\item{npts}{Number of points to use if pts is NULL (default: 15)}

\item{xlab}{Label for x-axis (default: 'X')}

\item{ylab}{Label for y-axis (default: 'Effect.Size')}
}
\value{
Creates a plot (no return value)
}
\description{
For a 2-by-2 design with a covariate, plots the KMS measure of effect size
associated with the two levels of the first factor as a function of the
covariate. Visualizes interaction effects.
}
\details{
This function compares KMS effect sizes for the two levels of the first factor
in a 2x2 design, conditional on covariate values. For each point on the covariate,
it computes effect sizes using \code{ancova.KMS} and plots the difference.

The function expects exactly 4 groups in both x and y. Missing values are
removed pairwise. When SW=TRUE, the groups are reordered as c(1,3,2,4) to
switch the factor levels.

The plot shows v1-v2 where v1 and v2 are effect sizes for the two levels of
Factor A.
}
\seealso{
\code{\link{t2way.KMS.interbt}}, \code{\link{ancova.KMS}}
}
