% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{ancJN.LC}
\alias{ancJN.LC}
\title{Johnson-Neyman ANCOVA for Linear Contrasts}
\usage{
ancJN.LC(
  x,
  y,
  pts = NULL,
  con = NULL,
  regfun = tsreg,
  nmin = 12,
  npts = 5,
  alpha = 0.05,
  xout = FALSE,
  outfun = out,
  nboot = 100,
  SEED = TRUE,
  pr = TRUE,
  ...
)
}
\arguments{
\item{x}{Covariate values in list or matrix form (J groups).}

\item{y}{Outcome values in list or matrix form (J groups).}

\item{pts}{Design points where regression lines are compared. If NULL, points are chosen automatically.}

\item{con}{Contrast matrix for linear contrasts. If NULL, uses default contrasts.}

\item{regfun}{Regression function to use. Default is tsreg (Theil-Sen estimator).}

\item{nmin}{Minimum sample size required at each design point. Default is 12.}

\item{npts}{Number of equally-spaced design points if pts=NULL. Default is 5.}

\item{alpha}{Alpha level for confidence intervals. Default is 0.05.}

\item{xout}{Logical. If TRUE, remove outliers from covariates. Default is FALSE.}

\item{outfun}{Function to detect outliers. Default is out.}

\item{nboot}{Number of bootstrap samples for variance estimation. Default is 100.}

\item{SEED}{Logical. If TRUE (default), set random seed for reproducibility.}

\item{pr}{Logical. If TRUE (default), print results.}

\item{...}{Additional arguments passed to regression function.}
}
\value{
A list with test results for linear contrasts at each design point.
}
\description{
Perform Johnson-Neyman ANCOVA for linear contrasts among J independent groups
using robust regression (default: Theil-Sen estimator). Compares regression lines
at specified design points.
}
\details{
For adjusted p-values controlling FWE, critical values from simulation studies
can be obtained from files p6n50.csv and p6n100.csv at the Wilcox lab website.
}
\examples{
\dontrun{
  # Three groups
  x <- list(rnorm(30), rnorm(30), rnorm(30))
  y <- list(2*x[[1]]+rnorm(30), x[[2]]+rnorm(30), x[[3]]+2+rnorm(30))
  result <- ancJN.LC(x, y, npts=7)
}
}
\keyword{ancova}
\keyword{contrasts}
