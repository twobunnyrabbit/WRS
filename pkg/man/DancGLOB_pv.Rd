% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{DancGLOB_pv}
\alias{DancGLOB_pv}
\title{Determine Critical P-Value for DancovaV2/DancGLOBv2}
\usage{
DancGLOB_pv(
  n,
  est = tmean,
  fr1 = 1,
  fr2 = 1,
  nboot = 500,
  SEED = TRUE,
  iter = 1000,
  nmin = 12,
  MC = TRUE,
  alpha = 0.05,
  PRM = FALSE,
  qvals = c(0.25, 0.5, 0.75),
  cpp = FALSE,
  ...
)
}
\arguments{
\item{n}{Sample size (number of pairs)}

\item{est}{Estimator function to use (default: tmean)}

\item{fr1}{Span for first group's smoother (default: 1)}

\item{fr2}{Span for second group's smoother (default: 1)}

\item{nboot}{Number of bootstrap samples per iteration (default: 500)}

\item{SEED}{Logical; if TRUE, set random seed for reproducibility (default: TRUE)}

\item{iter}{Number of simulation iterations (default: 1000)}

\item{nmin}{Minimum sample size for running interval smoother (default: 12)}

\item{MC}{Logical; if TRUE, use parallel processing via mclapply (default: TRUE)}

\item{alpha}{Significance level for determining critical value (default: 0.05)}

\item{PRM}{Logical; if TRUE, return all p-values computed (default: FALSE)}

\item{qvals}{Vector of quantiles to use for design points (default: c(0.25, 0.5, 0.75))}

\item{cpp}{Logical; if TRUE, use C++ implementation from WRScpp (default: FALSE)}

\item{...}{Additional arguments passed to smoother}
}
\value{
List with p.crit, the critical p-value for family-wise error control
}
\description{
Helper function to determine the critical p-value for the DancovaV2 (formerly DancGLOB)
function when testing dependent groups ANCOVA. Generates paired data from null
distribution (normal) and computes minimum p-value across iterations to establish
the critical value for controlling family-wise error rate.
}
\keyword{internal}
