% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression.R
\name{difregYvar}
\alias{difregYvar}
\title{Estimate Variance of Predicted Y Differences for Dependent Groups}
\usage{
difregYvar(
  x1,
  y1,
  x2,
  y2,
  regfun = tsreg,
  pts = NULL,
  nboot = 100,
  xout = FALSE,
  outfun = out,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{x1}{Numeric vector or matrix of predictor variable(s) for the first group.}

\item{y1}{Numeric vector of the dependent variable for the first group.}

\item{x2}{Numeric vector or matrix of predictor variable(s) for the second group.}

\item{y2}{Numeric vector of the dependent variable for the second group.}

\item{regfun}{Regression function to use (default: \code{tsreg} for Theil-Sen).
Must return coefficients in \code{$coef} with intercept as first element.}

\item{pts}{Matrix of predictor points at which to evaluate predictions.
If \code{NULL}, uses all unique predictor combinations from x1 and x2 (default: \code{NULL}).}

\item{nboot}{Number of bootstrap samples (default: 100).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{SEED}{Logical; if TRUE, sets random seed for reproducibility (default: TRUE).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
A numeric vector of length \code{nrow(pts)} containing the estimated
variance of the difference in predicted Y values at each point in \code{pts}.
}
\description{
Estimates the standard error (variance) of the difference between predicted Y
values from two dependent regression models at specified predictor points.
Uses bootstrap to estimate the sampling variability.
}
\details{
For each point in \code{pts}, this function estimates the variance of:
\deqn{\hat{Y}_1(x) - \hat{Y}_2(x)}
where \eqn{\hat{Y}_1(x)} and \eqn{\hat{Y}_2(x)} are predicted values from
the two regression models.

\strong{Bootstrap procedure}:
\enumerate{
\item Resample cases (x1, y1, x2, y2) together to preserve dependence
\item For each bootstrap sample:
\itemize{
\item Fit regression model 1 and predict at \code{pts}
\item Fit regression model 2 and predict at \code{pts}
\item Compute difference in predictions
}
\item For each point in \code{pts}, compute variance across bootstrap samples
}

\strong{When to use}:
\itemize{
\item Want to understand precision of predicted Y differences at specific X values
\item Building confidence bands for difference in regression functions
\item Comparing regression surfaces for dependent groups
\item Need point-wise standard errors for visualization or inference
}

\strong{Note}: This function returns \strong{variances}, not standard errors. To get
standard errors, take the square root of the returned values.

If \code{pts} is not specified, predictions are made at all unique predictor
combinations observed in either group, which can be computationally intensive
for large datasets or many predictors.
}
\examples{
\dontrun{
# Dependent groups with different slopes
set.seed(123)
x1 <- matrix(seq(-2, 2, length=50), ncol=1)
x2 <- x1 + rnorm(50, sd=0.2)  # Correlated predictors
y1 <- 1 + 2*x1 + rnorm(50, sd=0.5)
y2 <- 1 + 3*x2 + rnorm(50, sd=0.5)

# Evaluate at specific points
pts <- matrix(seq(-2, 2, by=0.5), ncol=1)
vars <- difregYvar(x1, as.vector(y1), x2, as.vector(y2), pts=pts, nboot=200)

# Standard errors
ses <- sqrt(vars)
print(data.frame(x=pts, se=ses))
}

}
\references{
Wilcox, R.R. (2022). \emph{Introduction to Robust Estimation and Hypothesis Testing}
(5th ed.). Academic Press.
}
\seealso{
\code{\link{difreg}} for testing parameter differences,
\code{\link{regYci}} for confidence intervals on predicted values (single group),
\code{\link{reg2difplot}} for plotting regression line differences (independent groups)
}
