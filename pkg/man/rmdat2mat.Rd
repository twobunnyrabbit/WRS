% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{rmdat2mat}
\alias{rmdat2mat}
\title{Convert Long-Format Repeated Measures Data to Wide Matrix}
\usage{
rmdat2mat(m, id.col = NULL, dv.col = NULL)
}
\arguments{
\item{m}{Matrix or data frame in long format.}

\item{id.col}{Integer. Column number indicating subject ID.}

\item{dv.col}{Integer vector. Column numbers for dependent variables at each
time point.}
}
\value{
A matrix in wide format: rows = subjects, columns = time points.
}
\description{
Converts repeated measures data from long format (one row per observation) to
wide format matrix (one row per subject, columns = time points).
}
\details{
In long format, each subject appears multiple times (once per measurement occasion).
This function reshapes to wide format where each subject appears once, with
measurements across time in separate columns.

For example, if subjects were measured at 3 time points and the data is in long
format with subject IDs in column 1 and measurements in columns 2-4, this function
creates a matrix where:
\itemize{
\item Each row = one subject
\item Columns 1, 2, 3 = measurements at times 1, 2, 3
}
}
\seealso{
\code{\link{rmc2list}}, \code{\link{bw2list}}
}
\keyword{manip}
