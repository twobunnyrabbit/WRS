% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{linWMWpb}
\alias{linWMWpb}
\title{Bootstrap Confidence Interval for Linear Contrast with WMW Method}
\usage{
linWMWpb(
  x,
  con,
  nreps = 100,
  SEED = TRUE,
  nboot = 500,
  alpha = 0.05,
  MC = FALSE
)
}
\arguments{
\item{x}{Data matrix, data frame, or list. Each column (for matrix/data frame)
or element (for list) represents a different group.}

\item{con}{Vector of contrast coefficients. Must sum to zero and have the
same length as the number of groups.}

\item{nreps}{Number of replications for WMW estimation (default: 100).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducibility (default: \code{TRUE}).}

\item{nboot}{Number of bootstrap samples (default: 500).}

\item{alpha}{Significance level for confidence interval (default: 0.05).}

\item{MC}{Logical. If \code{TRUE}, uses multicore processing via \code{mclapply} (default: \code{FALSE}).}
}
\value{
A list with components:
\itemize{
\item \code{p.est}: Point estimate of P(contrast < 0)
\item \code{ci}: Two-element vector with confidence interval bounds
\item \code{p.value}: P-value for testing H0: P(contrast < 0) = 0.5
}
}
\description{
Computes a percentile bootstrap confidence interval for the probability
that a linear contrast is less than zero, using the WMW approach.
}
\details{
This function extends \code{linWMW} by adding bootstrap confidence intervals.
For each bootstrap sample, it resamples with replacement from each group,
computes the linear contrast using \code{linWMW}, and estimates the probability
that the contrast is negative.

The confidence interval is constructed using the percentile method from
the bootstrap distribution. The p-value tests whether the probability
differs from 0.5 (which would indicate no difference).

When \code{MC = TRUE}, the bootstrap samples are processed in parallel using
multiple cores, which can significantly speed up computation for large
datasets or many bootstrap samples.
}
\note{
Missing values are automatically removed from all groups before analysis.
}
\examples{
\dontrun{
# Compare three groups with contrast (-1, 0.5, 0.5)
set.seed(123)
x1 <- rnorm(30, mean = 0)
x2 <- rnorm(30, mean = 0.3)
x3 <- rnorm(30, mean = 0.3)
x <- list(x1, x2, x3)

# Get bootstrap CI for P(contrast < 0)
result <- linWMWpb(x, con = c(-1, 0.5, 0.5), nboot = 1000)
print(result)

# Use multicore processing for faster computation
linWMWpb(x, con = c(-1, 0.5, 0.5), nboot = 2000, MC = TRUE)
}
}
\seealso{
\code{\link{linWMW}} for the basic estimation without bootstrap,
\code{\link{wmw.ref.dif}} for two-group WMW comparisons
}
