% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{adtestv2}
\alias{adtestv2}
\title{Test for Interaction in Two-Predictor Additive Model}
\usage{
adtestv2(
  x,
  y,
  est = tmean,
  nboot = 500,
  alpha = 0.05,
  fr = NA,
  xout = TRUE,
  outfun = outpro,
  com.pval = FALSE,
  SEED = TRUE,
  qval = 0.5,
  ...
)
}
\arguments{
\item{x}{Matrix of two predictor variables (n by 2).}

\item{y}{Vector of response values.}

\item{est}{Estimator function for smoother (default: \code{\link{tmean}}).}

\item{nboot}{Number of bootstrap samples (default: 500).}

\item{alpha}{Significance level (default: 0.05).}

\item{fr}{Span parameter for running interval smoother. If \code{NA} (default),
automatically selected based on sample size.}

\item{xout}{Logical. If \code{TRUE} (default), removes outliers before testing.}

\item{outfun}{Outlier detection function (default: \code{\link{outpro}}).}

\item{com.pval}{Logical. If \code{TRUE}, computes comparison p-values.}

\item{SEED}{Logical. If \code{TRUE}, sets random seed to 2 for reproducibility.}

\item{qval}{Quantile to test (default: 0.5 for median).}

\item{...}{Additional arguments passed to \code{outfun}.}
}
\value{
List returned by \code{\link{medind}} containing test results for the interaction term.
}
\description{
Tests the hypothesis that a regression model with two predictors is additive
(no interaction). Specifically, for the model \eqn{Y = g_1(X_1) + g_2(X_2) + g_3(X_1 X_2)},
tests \eqn{H_0: g_3(X_1 X_2) = 0}.
}
\details{
Fits an additive model using running interval smoother and backfitting, then
tests whether the median of \eqn{X_1 X_2} given the residuals is a horizontal
line (indicating no interaction).

Automatic span selection (when \code{fr=NA}):
\itemize{
\item n <= 400: Uses interpolation from empirically determined values
\item n > 400: Uses constant value of 0.01
}
}
\seealso{
\code{\link{adtest}}, \code{\link{adtestl}}, \code{\link{medind}}
}
\keyword{htest}
\keyword{regression}
\keyword{robust}
