% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{pb3trmcp}
\alias{pb3trmcp}
\title{Three-Way ANOVA Multiple Comparisons for Trimmed Means}
\usage{
pb3trmcp(
  J,
  K,
  L,
  data,
  tr = 0.2,
  grp = c(1:p),
  alpha = 0.05,
  p = J * K * L,
  nboot = NA,
  SEED = TRUE,
  bhop = FALSE
)
}
\arguments{
\item{J}{Number of levels for Factor A.}

\item{K}{Number of levels for Factor B.}

\item{L}{Number of levels for Factor C.}

\item{data}{Data in list mode or matrix. Each list element contains data for one
group. Groups ordered as: (1,1,1), (1,1,2), ..., (1,1,L), (1,2,1), ..., (J,K,L).}

\item{tr}{Proportion of trimming (0 to 0.5). Default is 0.2 (20\% trimming).}

\item{grp}{Optional vector to reorder groups if data not in standard order.
Default is \code{c(1:p)} where \code{p=J*K*L}.}

\item{alpha}{Significance level. Default is 0.05.}

\item{p}{Total number of groups (J\emph{K}L). Default is \code{J*K*L}.}

\item{nboot}{Number of bootstrap samples. Default is \code{NA} (determined automatically).}

\item{SEED}{Logical. If \code{TRUE}, set random seed for reproducibility. Default is \code{TRUE}.}

\item{bhop}{Logical. If \code{TRUE}, use Benjamini-Hochberg procedure for p-value adjustment.
Default is \code{FALSE}.}
}
\value{
List with components for each factor and interaction:
\item{Factor.A}{Multiple comparison results for main effect of Factor A}
\item{Factor.B}{Multiple comparison results for main effect of Factor B}
\item{Factor.C}{Multiple comparison results for main effect of Factor C}
\item{Factor.AB}{Multiple comparison results for A × B interaction}
\item{Factor.AC}{Multiple comparison results for A × C interaction}
\item{Factor.BC}{Multiple comparison results for B × C interaction}
\item{Factor.ABC}{Multiple comparison results for A × B × C interaction}
}
\description{
Performs all pairwise multiple comparisons for main effects and interactions
in a three-way independent groups ANOVA design using percentile bootstrap
and trimmed means.
}
\details{
Data organization: For a 2×2×2 design with factors A, B, C:
\itemize{
\item \code{data[[1]]}: Level A=1, B=1, C=1
\item \code{data[[2]]}: Level A=1, B=1, C=2
\item \code{data[[3]]}: Level A=1, B=2, C=1
\item etc.
}

Uses percentile bootstrap to perform all pairwise comparisons for each
main effect and interaction. Missing values are automatically removed.
}
\seealso{
\code{\link{pbtrmcp}}, \code{\link{con3way}}, \code{\link{t3way}}
}
\keyword{htest}
\keyword{robust}
