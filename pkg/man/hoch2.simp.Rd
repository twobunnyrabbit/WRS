% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{hoch2.simp}
\alias{hoch2.simp}
\title{Hochberg Two-Stage Sample Size Determination (Simplified)}
\usage{
hoch2.simp(n, V, cil, tr, alpha = 0.05, con = NULL)
}
\arguments{
\item{n}{Vector of initial sample sizes for each group.}

\item{V}{Vector of variances (or Winsorized variances if \code{tr > 0})
for each group.}

\item{cil}{Desired length of confidence intervals for comparisons.}

\item{tr}{Trimming proportion (e.g., 0.2 for 20\% trimming). If \code{tr > 0},
\code{V} should contain Winsorized variances.}

\item{alpha}{Significance level (default: 0.05).}

\item{con}{Contrast matrix for comparisons. If NULL, uses default contrasts
from \code{\link{con1way}}.}
}
\value{
NULL (invisibly). Prints required additional sample sizes for each group.
}
\description{
Computes required sample sizes for a two-stage procedure to achieve desired
confidence interval lengths in multiple comparisons. Uses Hochberg's method
with given sample sizes, variances, trimming level, and target CI length.
}
\details{
Hochberg's two-stage procedure determines how many additional observations
are needed in each group to achieve confidence intervals of desired length
for multiple comparisons.

\strong{Procedure}:
\enumerate{
\item Stage 1: Collect initial samples and estimate variances
\item Compute degrees of freedom based on trimming level
\item Determine Studentized range critical value
\item Calculate required sample size for each group
\item Stage 2: Collect additional observations if needed
}

The function prints the number of additional observations needed for each
group to achieve the desired CI length.

\strong{Note}: Raw data are not provided to this function; only summary statistics
(n, variances) are used.
}
\examples{
\dontrun{
# Three groups with initial data
n <- c(20, 20, 20)
V <- c(1.2, 1.5, 1.3)  # Sample variances

# Determine additional samples needed for CI length = 1.0
hoch2.simp(n = n, V = V, cil = 1.0, tr = 0.2)
}

}
\seealso{
\code{\link{con1way}}, \code{\link{lincon}}
}
\keyword{design}
