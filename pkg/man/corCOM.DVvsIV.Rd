% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{corCOM.DVvsIV}
\alias{corCOM.DVvsIV}
\title{Compare DV-IV Correlations: Largest vs Others}
\usage{
corCOM.DVvsIV(
  x,
  y,
  com.p.dist = FALSE,
  corfun = wincor,
  iter = 200,
  PV = NULL,
  pr = TRUE,
  neg.col = NULL,
  LARGEST = TRUE,
  alpha = 0.05,
  nboot = 500,
  SEED = TRUE,
  MC = FALSE,
  xout = FALSE,
  outfun = outpro,
  FWE.method = "hoch",
  ...
)
}
\arguments{
\item{x}{Matrix (n-by-p) of independent variables.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{com.p.dist}{Logical. Compute null distribution for critical p-values
(default: \code{FALSE}). Set to \code{TRUE} for p > 6.}

\item{corfun}{Correlation function to use. Common options: \code{pbcor} (percentage
bend), \code{pcor} (Pearson), \code{wincor} (winsorized), \code{spear} (Spearman).}

\item{iter}{Number of iterations for null distribution simulation (default: 200).}

\item{PV}{Pre-computed null distribution from \code{corCOM.DVvsIV.crit()} (default: \code{NULL}).}

\item{pr}{Logical. Print progress messages (default: \code{TRUE}).}

\item{neg.col}{Vector of column indices to negate before computing correlations
(default: \code{NULL}).}

\item{LARGEST}{Logical. Test whether largest correlation is significantly higher
than others (default: \code{TRUE}).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{nboot}{Number of bootstrap samples (default: 500).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{MC}{Logical. Use parallel processing (default: \code{FALSE}).}

\item{xout}{Logical. Remove outliers from x before analysis (default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{FWE.method}{Method for controlling FWE (default: 'hoch' for Hochberg).}

\item{...}{Additional arguments passed to \code{corfun}.}
}
\value{
List with components:
\item{I}{Index of predictor with largest correlation to y.}
\item{test}{Test results for comparing largest to each other predictor.}
\item{est}{Vector of all correlations with y.}
\item{p.value}{P-value for comparing largest to each other predictor.}
\item{adj.p.value}{Adjusted p-values controlling FWE.}
\item{sig}{Logical indicating which comparisons are significant.}
}
\description{
Tests whether the predictor with the largest correlation to y has significantly
higher correlation than other predictors, controlling FWE.
}
\details{
For p >= 3 predictors, identifies the predictor x_I with highest |cor(y, x_I)|
and tests whether this correlation is significantly different from cor(y, x_k)
for all k != I, using Hochberg's method to control familywise error rate.

Winsorized correlation is used by default. Alternative choices include:
\code{spear}, \code{tau}, \code{pbcor}, \code{bicor}, \code{scor}, \code{mve.cor}, \code{mcd.cor}.

For p > 6, recommend using \code{com.p.dist = TRUE} to compute the null distribution
for more accurate critical p-values (can be slow). Pre-compute with
\code{corCOM.DVvsIV.crit()} and supply via \code{PV} to reduce execution time.
}
\examples{
\dontrun{
set.seed(123)
x <- matrix(rnorm(50 * 4), ncol = 4)
y <- 0.8 * x[, 1] + 0.2 * x[, 2] + rnorm(50)
corCOM.DVvsIV(x, y, nboot = 500)
}
}
\seealso{
\code{\link{corCOM.DVvsIV.crit}}, \code{\link{corREG.best}}, \code{\link{TWOpov}}
}
