% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{runmq}
\alias{runmq}
\title{Running Interval Smoother for Multiple Quantiles}
\usage{
runmq(
  x,
  y,
  HD = FALSE,
  qval = c(0.2, 0.5, 0.8),
  xlab = "X",
  ylab = "Y",
  fr = 1,
  sm = FALSE,
  nboot = 40,
  SEED = TRUE,
  eout = FALSE,
  xout = FALSE,
  ...
)
}
\arguments{
\item{x}{Predictor variable}

\item{y}{Response variable}

\item{HD}{Logical: use Harrell-Davis quantile estimator (default: FALSE)}

\item{qval}{Vector of quantiles to estimate (default: c(0.2, 0.5, 0.8))}

\item{xlab}{Label for x-axis (default: "X")}

\item{ylab}{Label for y-axis (default: "Y")}

\item{fr}{Span parameter controlling smoothing (default: 1)}

\item{sm}{Logical: use bootstrap smoothing (bagging) (default: FALSE)}

\item{nboot}{Number of bootstrap samples if sm=TRUE (default: 40)}

\item{SEED}{Logical: set random seed (default: TRUE)}

\item{eout}{Logical: remove (x,y) outliers (default: FALSE)}

\item{xout}{Logical: remove x outliers (default: FALSE)}

\item{...}{Additional arguments passed to smoother}
}
\value{
NULL (function creates plot as side effect)
}
\description{
Create a plot with running interval smoothers for multiple specified
quantiles. Can use either Harrell-Davis or standard quantile estimator.
}
\details{
Creates a single plot with multiple smoothed quantile curves. For each
quantile in qval, computes a running interval smoother using either:
\itemize{
\item Standard quantile estimator (qest) if HD=FALSE
\item Harrell-Davis estimator (hd) if HD=TRUE
}

If sm=TRUE, uses bootstrap bagging (runmbo) for more stable estimates.
Otherwise uses basic running smoother (rungen).

Missing values are automatically removed.
}
\seealso{
\code{\link{rungen}}, \code{\link{runmbo}}, \code{\link{hd}}, \code{\link{qest}}
}
\keyword{nonparametric}
\keyword{smooth}
