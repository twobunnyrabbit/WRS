% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{q2gci}
\alias{q2gci}
\title{Compare Multiple Quantiles Between Two Independent Groups}
\usage{
q2gci(
  x,
  y,
  q = c(0.1, 0.25, 0.5, 0.75, 0.9),
  nboot = 2000,
  plotit = TRUE,
  SEED = TRUE,
  xlab = "Group 1",
  ylab = "Est.1-Est.2",
  alpha = 0.05
)
}
\arguments{
\item{x}{Numeric vector for group 1. Must not contain duplicates.}

\item{y}{Numeric vector for group 2. Must not contain duplicates.}

\item{q}{Numeric vector of quantiles to compare (default: c(.1,.25,.5,.75,.9)).}

\item{nboot}{Integer. Number of bootstrap samples (default: 2000).}

\item{plotit}{Logical. If \code{TRUE}, creates a plot of estimates and CIs
(default: \code{TRUE}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed to 2 (default: \code{TRUE}).}

\item{xlab}{Character string for x-axis label when plotting (default: 'Group 1').}

\item{ylab}{Character string for y-axis label when plotting (default: 'Est.1-Est.2').}

\item{alpha}{Numeric. Alpha level for confidence intervals (default: 0.05).}
}
\value{
Data frame with columns:
\describe{
\item{q}{Quantile being tested}
\item{n1, n2}{Sample sizes}
\item{est.1, est.2}{Quantile estimates for each group}
\item{est.1_minus_est.2}{Difference in estimates}
\item{ci.low, ci.up}{Bootstrap confidence interval}
\item{p_crit}{Hochberg-adjusted critical p-value}
\item{p.value}{Bootstrap p-value}
\item{signif}{YES/NO indicating significance}
}
}
\description{
Compares multiple quantiles between two independent groups using percentile
bootstrap with Harrell-Davis quantile estimator. Returns confidence intervals,
p-values, and Hochberg-adjusted critical values. Optionally plots results.
}
\details{
Uses \code{\link{pb2gen}} with \code{\link{qest}} for each quantile. Stops with
error if duplicate values detected (use \code{\link{qcomhd}} or \code{\link{medpb2}}
for data with ties).
}
\seealso{
\code{\link{pb2gen}}, \code{\link{qest}}, \code{\link{qcomhd}}
}
\keyword{htest}
