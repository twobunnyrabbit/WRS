% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{logSM}
\alias{logSM}
\title{Kernel Smoother for Logistic Regression (Multiple Predictors)}
\usage{
logSM(
  x,
  y,
  pyhat = FALSE,
  plotit = TRUE,
  xlab = "X",
  ylab = "Pred.Prob",
  zlab = " ",
  xout = FALSE,
  outfun = outpro,
  pr = TRUE,
  theta = 50,
  phi = 25,
  duplicate = "error",
  LP = TRUE,
  Lspan = 0.75,
  expand = 0.5,
  scale = TRUE,
  fr = 2,
  ticktype = "simple",
  ...
)
}
\arguments{
\item{x}{Matrix or data frame of predictor variables (n by p).}

\item{y}{Binary response vector (will be converted to 0/1).}

\item{pyhat}{Logical. If \code{TRUE}, returns predicted probabilities (default: \code{FALSE}).}

\item{plotit}{Logical. If \code{TRUE}, produces plots (default: \code{TRUE}).}

\item{xlab}{Label for x-axis (default: "X").}

\item{ylab}{Label for y-axis (default: "Pred.Prob").}

\item{zlab}{Label for z-axis in 3D plots (default: " ").}

\item{xout}{Logical. If \code{TRUE}, removes outliers before smoothing (default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{\link{outpro}}).}

\item{pr}{Logical. If \code{TRUE}, prints progress messages (default: \code{TRUE}).}

\item{theta}{Viewing angle for 3D plot (default: 50).}

\item{phi}{Viewing angle for 3D plot (default: 25).}

\item{duplicate}{How to handle duplicate points (default: "error").}

\item{LP}{Logical. If \code{TRUE}, applies LOESS smooth to result for p=2 (default: \code{TRUE}).}

\item{Lspan}{Span parameter for LOESS smoothing (default: 0.75).}

\item{expand}{Expansion factor for plotting (default: 0.5).}

\item{scale}{Logical. Scaling parameter for plots (default: \code{TRUE}).}

\item{fr}{Bandwidth/span parameter for kernel (default: 2).}

\item{ticktype}{Tick type for 3D plots (default: "simple").}

\item{...}{Additional arguments passed to \code{outfun}.}
}
\value{
If \code{pyhat = TRUE}, returns vector of predicted probabilities.
Otherwise returns "Done".
}
\description{
Nonparametric smoother designed specifically for binary response data,
using kernel weighting based on Mahalanobis distance. Handles one or more
predictors.
}
\details{
Uses kernel weighting with Mahalanobis distances computed using MVE
(minimum volume ellipsoid) covariance estimate for robustness. The kernel
weight is exp(-d) where d is the Mahalanobis distance.

For p = 1: Creates scatter plot with smooth curve
For p = 2: Creates 3D surface plot (optionally smoothed with LOESS)
For p > 2: No plot produced

Points are weighted if sqrt(Mahalanobis distance) ≤ \code{fr}.

For single predictor, consider using \code{\link{logrsm}} instead.
}
\seealso{
\code{\link{logrsm}}, \code{\link{logSMpred}}, \code{\link{logSM2g}},
\code{\link{logrchk}}
}
