% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{llrdata}
\alias{llrdata}
\title{Generate Data for Loglinear Regression}
\usage{
llrdata(n = 100, q = 5)
}
\arguments{
\item{n}{Sample size (number of observations, default: 100).}

\item{q}{Number of predictor variables (default: 5).}
}
\value{
List with components:
\item{x}{n by q matrix of predictor variables (scaled normal variates)}
\item{y}{Vector of n Poisson response values}
}
\description{
Generates simulated data for testing loglinear (Poisson) regression methods.
Creates a design matrix with normal predictors and a Poisson response with
a log-linear relationship.
}
\details{
Data generation model:
\enumerate{
\item Generates n×q matrix X from N(0,1), scaled: X = 0.5*X + 1
\item Sets β = (1, 1, 1, 0, ..., 0) (first 3 coefficients are 1)
\item Sets intercept α = -2.5
\item Computes λ = exp(α + Xβ)
\item Generates Y ~ Poisson(λ)
}

This creates data with a true loglinear relationship suitable for testing
Poisson regression diagnostics and robust methods.
}
\examples{
# Generate data and fit Poisson regression
dat <- llrdata(n = 100, q = 5)
# fit <- glm(dat$y ~ dat$x, family = poisson)
}
\seealso{
\code{\link{llressp}}, \code{\link{llrplot}}
}
