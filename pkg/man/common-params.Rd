% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/common-params.R, R/correlation.R
\name{common-params}
\alias{common-params}
\title{Common parameter documentation for WRS package functions}
\arguments{
\item{q}{Quantile to estimate. For example, \code{q = 0.5} estimates the median,
\code{q = 0.25} estimates the first quartile (default: 0.5).}

\item{grp}{A vector indicating group membership. Can be numeric, factor, or
character. Used for grouping observations in multi-group comparisons.}

\item{est}{Estimator function to use for computing location or other
statistics. Common choices include \code{mean}, \code{median}, \code{tmean} (trimmed mean),
\code{mom} (modified one-step M-estimator), \code{mest} (M-estimator).}

\item{null.value}{Null hypothesis value for testing (default: 0 for most
tests, indicating no difference or no correlation).}

\item{side}{Character string specifying the alternative hypothesis. Options:
\itemize{
\item \code{"both"} or \code{"two.sided"}: two-sided test (default)
\item \code{"less"}: one-sided test for negative differences
\item \code{"greater"}: one-sided test for positive differences
}}

\item{op}{Number of outliers to remove from each tail when using outlier
detection methods. Default varies by function.}

\item{yout}{Logical value. If \code{TRUE}, outliers are removed from \code{y} before
analysis (default: \code{FALSE}).}

\item{eout}{Logical value. If \code{TRUE}, outliers are removed based on
eliminating points with large residuals or leverage (default: \code{FALSE}).}

\item{MM}{Logical value. If \code{TRUE}, uses the MM-estimator for robust
regression (default: \code{FALSE}).}

\item{bend}{Bending constant for percentage bend correlation (default: 0.2).}

\item{WIN}{Logical value. If \code{TRUE}, uses winsorized values instead of
trimmed values (default: \code{FALSE}).}

\item{JR}{Logical value. If \code{TRUE}, uses the Johansen-Rousseeuw estimator
(default: \code{TRUE} for some functions).}

\item{na.rm}{Logical value. If \code{TRUE}, removes \code{NA} values before computation
(default: \code{TRUE} for most functions).}

\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{tr}{Trimming proportion (default: 0.2). Proportion of observations to
trim from each tail when computing winsorized correlation.}

\item{beta}{Bending constant for percentage bend correlation (default: 0.2).
Controls the amount of downweighting for outliers.}

\item{corfun}{Correlation function to use. Common options: \code{pbcor} (percentage
bend), \code{pcor} (Pearson), \code{wincor} (winsorized), \code{spear} (Spearman).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{xlab}{Label for x-axis in plots (default: "X" or "VAR 1").}

\item{ylab}{Label for y-axis in plots (default: "Y" or "VAR 2").}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{cop}{Copula type for skipped correlation (default: 3). Options: 1-5.}

\item{MC}{Logical. If \code{TRUE}, uses parallel processing via \code{mclapply}
(default: \code{FALSE}).}

\item{pr}{Logical. If \code{TRUE}, prints progress messages (default: \code{TRUE}).}

\item{STAND}{Logical. If \code{TRUE}, standardizes data before analysis
(default: \code{TRUE}).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\description{
This file contains documentation for parameters that are used across
multiple functions in the WRS package. Other functions can inherit
these parameter descriptions using @inheritParams.
}
\keyword{internal}
