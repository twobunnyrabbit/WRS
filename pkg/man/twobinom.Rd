% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{twobinom}
\alias{twobinom}
\title{Test for Equality of Two Binomial Proportions}
\usage{
twobinom(
  r1 = sum(elimna(x)),
  n1 = length(elimna(x)),
  r2 = sum(elimna(y)),
  n2 = length(elimna(y)),
  x = NA,
  y = NA,
  alpha = 0.05
)
}
\arguments{
\item{r1}{Number of successes in group 1.}

\item{n1}{Sample size for group 1.}

\item{r2}{Number of successes in group 2.}

\item{n2}{Sample size for group 2.}

\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}
}
\value{
A list with components:
\itemize{
\item \code{p.value}: P-value for the test
\item \code{p1}: Estimated proportion for group 1 (r1/n1)
\item \code{p2}: Estimated proportion for group 2 (r2/n2)
\item \code{est.dif}: Estimated difference (p1 - p2)
}
}
\description{
Tests the hypothesis that two independent binomial distributions have equal
probability of success using the Storer-Kim method.
}
\details{
The Storer-Kim method provides an exact test for comparing two binomial
proportions without relying on large-sample approximations. It has better
control of Type I error rates than asymptotic methods, especially with
small sample sizes.
}
\examples{
\dontrun{
# Test if success rates differ
twobinom(r1 = 15, n1 = 50, r2 = 25, n2 = 60)

# Using binary data vectors
x <- rbinom(50, 1, 0.3)
y <- rbinom(60, 1, 0.4)
twobinom(x = x, y = y)
}
}
\seealso{
\code{\link{twobici}}, \code{\link{twobicipv}}
}
