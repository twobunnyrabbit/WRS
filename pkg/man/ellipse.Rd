% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{ellipse}
\alias{ellipse}
\title{Plot Confidence Ellipse for Bivariate Data}
\usage{
ellipse(x, center = apply(x, 2, mean), cov = var(x), alph = 0.95)
}
\arguments{
\item{x}{Numeric matrix. Bivariate data with 2 columns (n × 2).}

\item{center}{Numeric vector of length 2. Center of ellipse
(default: column means of \code{x}).}

\item{cov}{Numeric 2×2 matrix. Covariance matrix (default: sample covariance of \code{x}).}

\item{alph}{Numeric. Confidence level (default: 0.95).}
}
\value{
NULL (function is called for plotting side effect).
}
\description{
Creates and plots a confidence ellipse for bivariate data based on a specified
center, covariance matrix, and confidence level. Provides visualization of the
joint distribution and multivariate confidence region.
}
\details{
The function constructs a confidence ellipse satisfying:

\deqn{(\mathbf{x} - \mu)^T \Sigma^{-1} (\mathbf{x} - \mu) \leq \chi^2_2(\alpha)}

where:
\itemize{
\item \eqn{\mu} is the center (location estimate)
\item \eqn{\Sigma} is the covariance matrix (scatter estimate)
\item \eqn{\chi^2_2(\alpha)} is the \code{alph}-quantile of chi-squared with 2 df
}

The ellipse boundary is computed by:
\enumerate{
\item Solving the quadratic form equation for a grid of y₂ values
\item Computing corresponding x₁ values that satisfy the equation
\item Plotting the resulting points
}

\strong{Uses}: Visualizing bivariate distributions, identifying outliers, comparing
groups via overlapping ellipses, assessing multivariate normality.
}
\examples{
# Bivariate normal data
set.seed(123)
m <- matrix(rnorm(100), ncol = 2)
ellipse(m, alph = 0.95)

# With robust center and covariance
rob <- cov.mcd(m)
ellipse(m, center = rob$center, cov = rob$cov)
}
\seealso{
\code{\link{cov}}, \code{\link{cov.mcd}}, \code{\link{qchisq}}
}
\keyword{hplot}
\keyword{multivariate}
