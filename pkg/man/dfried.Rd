% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{dfried}
\alias{dfried}
\title{Compare Dependent Groups Using Halfspace Depth (Friedman-Type Test)}
\usage{
dfried(m, plotit = TRUE, pop = 0, fr = 0.8, v2 = FALSE, op = FALSE)
}
\arguments{
\item{m}{Numeric matrix or list. If matrix, rows are observations and columns are groups.
If list, each element is a vector representing one group.}

\item{plotit}{Logical. If TRUE, plots density of difference scores when K=2 (default: TRUE).}

\item{pop}{Integer. Type of plot for difference scores when K=2:
\itemize{
\item 0: Adaptive kernel density (default)
\item 1: Expected frequency curve
\item 2: Kernel density estimate
\item 3: S+ kernel density estimate
\item 4: Boxplot
}}

\item{fr}{Numeric. Span for kernel density (default: 0.8).}

\item{v2}{Logical. If TRUE, uses alternative depth computation (default: FALSE).}

\item{op}{Logical. If TRUE, uses alternative centering based on median (default: FALSE).
Recommended when n ≤ 10.}
}
\value{
List with components:
\describe{
\item{phat}{Ratio of depth of null vector to maximum depth}
\item{crit.val}{Critical value for test at nominal level (typically 0.05)}
}
}
\description{
Tests for differences among dependent groups by assessing whether the null vector
(representing no difference) has unusually low halfspace depth relative to the
distribution of pairwise differences. A depth-based alternative to Friedman's test.
}
\details{
The function:
\enumerate{
\item Computes all pairwise differences between groups
\item Calculates halfspace depth of the null vector (all differences = 0) relative to
the distribution of observed differences
\item Compares depth to critical value based on sample size and number of groups
}

The null hypothesis is rejected if \code{phat < crit.val}, indicating that the
null vector is unusually far from the center of the difference distribution.

For K=2 groups, this reduces to a depth-based paired comparison. For K≥3, it tests
the global null of no differences among any groups.

Critical values are calibrated for K = 2 to 8+ groups based on simulation studies.
}
\examples{
# Compare 3 dependent groups
set.seed(123)
m <- matrix(rnorm(60), ncol = 3)
m[, 3] <- m[, 3] + 0.5  # Shift third group
result <- dfried(m, plotit = FALSE)
result$phat < result$crit.val  # Significant if TRUE
}
\seealso{
\code{\link{fdepth}}, \code{\link{fdepthv2}}, \code{\link{unidepth}},
\code{\link{cov.mcd}}
}
\keyword{htest}
\keyword{multivariate}
\keyword{robust}
