% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{regtestMC}
\alias{regtestMC}
\title{Regression Coefficient Test with Multicore Bootstrap}
\usage{
regtestMC(
  x,
  y,
  regfun = tsreg,
  nboot = 600,
  alpha = 0.05,
  plotit = TRUE,
  grp = c(1:ncol(x)),
  nullvec = c(rep(0, length(grp))),
  xout = FALSE,
  outfun = outpro,
  SEED = TRUE,
  pr = TRUE,
  ...
)
}
\arguments{
\item{x}{Matrix of predictors (n by p).}

\item{y}{Response variable vector.}

\item{regfun}{Regression function to use. Default is \code{\link{tsreg}} (Theil-Sen).
Must return a list with \code{$coef} component containing coefficient estimates.}

\item{nboot}{Number of bootstrap samples. Default is 600.}

\item{alpha}{Alpha level for confidence region. Default is 0.05.}

\item{plotit}{Logical. If \code{TRUE} and testing 2 coefficients, plot bootstrap
distribution and confidence region. Default is \code{TRUE}.}

\item{grp}{Indices of coefficients to test (1-based, where 1 is first predictor, etc.).
Default tests all p predictors.}

\item{nullvec}{Vector of null hypothesis values for coefficients in \code{grp}.
Default is all zeros.}

\item{xout}{Logical. If \code{TRUE}, remove outliers before analysis. Default is \code{FALSE}.}

\item{outfun}{Outlier detection function. Default is \code{\link{outpro}}.}

\item{SEED}{Logical. If \code{TRUE}, set random seed to 2 for reproducibility. Default is \code{TRUE}.}

\item{pr}{Logical. If \code{TRUE}, print messages. Default is \code{TRUE}.}

\item{...}{Additional arguments passed to \code{outfun}.}
}
\value{
List with components:
\item{test}{Test statistic (Mahalanobis distance of estimate from null hypothesis)}
\item{crit}{Critical value from bootstrap distribution}
\item{p.value}{Bootstrap p-value}
\item{nullvec}{Null hypothesis values tested}
\item{est}{Coefficient estimates for tested parameters}
}
\description{
Test hypotheses about regression coefficients using percentile bootstrap with
multicore processing. Tests whether specified regression coefficients equal
specified constants using a confidence ellipsoid based on Mahalanobis distance.
}
\details{
This function tests \eqn{H_0: \beta_j = c_j} for coefficients indexed by \code{grp},
where \eqn{c_j} are values in \code{nullvec}. The test statistic is based on
Mahalanobis distance from a bootstrap-based confidence ellipsoid.

The function uses \code{mclapply} for parallel bootstrap sampling, making it faster
than non-multicore versions on systems with multiple cores.

When \code{plotit=TRUE} and exactly 2 coefficients are tested, produces a scatterplot
of bootstrap coefficient estimates with the confidence region boundary and null
hypothesis point marked.
}
\seealso{
\code{\link{regtest}}, \code{\link{tsreg}}, \code{\link{outpro}}
}
\keyword{multivariate}
\keyword{regression}
