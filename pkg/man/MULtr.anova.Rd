% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anova.R
\name{MULtr.anova}
\alias{MULtr.anova}
\title{Multivariate ANOVA for Trimmed Means Using Johansen's Method}
\usage{
MULtr.anova(x, J = NULL, p = NULL, tr = 0.2, alpha = 0.05)
}
\arguments{
\item{x}{Data in list mode where x[\link{j}] is an n_j by p matrix for group j,
or a matrix when J and p are specified (stored as expected by bwtrim)}

\item{J}{Number of groups (NULL by default, required if x is a matrix)}

\item{p}{Number of variables (NULL by default, required if x is a matrix)}

\item{tr}{Proportion to trim from each end (default: 0.2, meaning 20\% from each tail)}

\item{alpha}{Significance level for critical value computation (default: 0.05)}
}
\value{
A list with components:
\item{test.stat}{Test statistic}
\item{crit.value}{Critical value at specified alpha level}
}
\description{
Performs multivariate ANOVA (MANOVA) for J independent groups using trimmed
means based on Johansen's method. Tests whether J groups have identical
multivariate trimmed mean vectors.
}
\details{
When x is a list, length(x) = J (number of groups) and x[\link{j}] is an n_j by p
matrix where p is the number of variables. When x is a matrix, both J and p
must be specified.

The method uses winsorized covariance matrices and weighted grand means. The
test statistic follows an approximate chi-squared distribution with adjustment
for trimmed data.

To obtain a p-value, use the function \code{MULAOVp}. Missing values are
removed via listwise deletion within each group.
}
\seealso{
\code{\link{YYmanova}}, \code{\link{MULAOVp}}
}
