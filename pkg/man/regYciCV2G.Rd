% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression-advanced.R
\name{regYciCV2G}
\alias{regYciCV2G}
\title{Simulation-Based Critical Value for Two-Group Regression Comparison}
\usage{
regYciCV2G(
  n1,
  n2,
  crit = NULL,
  g = 0,
  h = 0,
  nboot = 1000,
  regfun = tsreg,
  ALL = TRUE,
  alpha = 0.05,
  SEED = TRUE,
  MC = FALSE,
  null.value = 0,
  pts = NULL,
  npts = 100,
  nmiss = 0,
  ...
)
}
\arguments{
\item{n1}{Sample size for first group.}

\item{n2}{Sample size for second group.}

\item{crit}{Optional predetermined critical value; if provided, function
computes Type I error rate at this value.}

\item{g}{Shape parameter for g-and-h distribution (default: 0, normal).}

\item{h}{Shape parameter for g-and-h distribution (default: 0, normal).}

\item{nboot}{Number of bootstrap samples for simulation (default: 1000).}

\item{regfun}{Regression function to use (default: \code{tsreg}).}

\item{ALL}{Logical; if TRUE, use all unique x-values as test points; if
FALSE, use \code{npts} equally-spaced points (default: TRUE).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{MC}{Logical. If \code{TRUE}, uses parallel processing via \code{mclapply}
(default: \code{FALSE}).}

\item{null.value}{Null hypothesis value for testing (default: 0).}

\item{pts}{Optional vector of specific design points for testing (default: NULL).}

\item{npts}{Number of design points if \code{ALL=FALSE} (default: 100).}

\item{nmiss}{Number of missing values to simulate in group 2 (default: 0).}

\item{...}{Additional arguments passed to \code{regfun}.}
}
\value{
A list with components:
\itemize{
\item global.p.value: If \code{crit} provided, empirical Type I error rate
\item crit.est: Estimated critical value at level \code{alpha} (Harrell-Davis
quantile estimator)
}
}
\description{
Determines critical values for \code{regYci2Gv2()} via simulation, used to control
family-wise error rate when comparing regression lines from two independent
groups at multiple design points simultaneously.
}
\details{
Simulates data from specified g-and-h distributions (bivariate normal when
g=h=0) to determine critical values for simultaneous inference when comparing
two regression lines. Accommodates:
\itemize{
\item Unequal sample sizes between groups
\item Missing data (via \code{nmiss} parameter)
\item Non-normal distributions via g-and-h family
}

The simulation generates paired datasets, computes regression predictions
at all/specified design points, and determines the minimum p-value across
points for each simulation run.
}
\examples{
\dontrun{
# Critical value for equal groups, n=30 each
regYciCV2G(n1=30, n2=30, nboot=500)

# Unequal groups with missing data
regYciCV2G(n1=40, n2=25, nmiss=5, nboot=500)

# Non-normal data (g-and-h with skew and heavy tails)
regYciCV2G(n1=50, n2=50, g=0.5, h=0.2, nboot=500)
}
}
\references{
Wilcox, R.R. (2022). Introduction to Robust Estimation and Hypothesis Testing
(5th ed.). Academic Press.
}
\seealso{
\code{\link{regYci2Gv2}}, \code{\link{regYciCV}}
}
