% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{ROCmul.curve}
\alias{ROCmul.curve}
\title{Multiple ROC Curves for Classification Methods}
\usage{
ROCmul.curve(
  train = NULL,
  g = NULL,
  x1 = NULL,
  x2 = NULL,
  method = c("KNN", "DIS"),
  pro.p = 0.8,
  SEED = TRUE,
  reps = 10,
  POS = TRUE,
  ...
)
}
\arguments{
\item{train}{Optional matrix or data frame of training data. If provided, \code{g}
must specify group memberships.}

\item{g}{Optional vector of group IDs (must have exactly 2 unique values). Required
if \code{train} is specified.}

\item{x1}{Optional matrix/data frame for group 1 observations. Used if \code{train}
is \code{NULL}.}

\item{x2}{Optional matrix/data frame for group 2 observations. Used if \code{train}
is \code{NULL}.}

\item{method}{Character vector specifying classification methods. Options include:
\code{"KNN"} (k-nearest neighbor using robust depths), \code{"DIS"} (depth-based
classification), \code{"DEP"} (Makinde & Fasoranbaku 2018 depth method),
\code{"SVM"} (support vector machine), \code{"RF"} (random forest),
\code{"NN"} (neural network), \code{"ADA"} (ada boost), \code{"PRO"} (projection method).}

\item{pro.p}{Numeric. Proportion of data used for training in cross-validation
(default: 0.8). Remainder used for testing.}

\item{SEED}{Logical. If \code{TRUE} (default), set random seed for reproducibility.}

\item{reps}{Integer. Number of cross-validation resamples. ROC curves are averaged
across resamples (default: 10).}

\item{POS}{Logical. If \code{TRUE} (default), plot true positive rate vs false
positive rate. If \code{FALSE}, plot true negative rate vs false negative rate.}

\item{...}{Additional arguments passed to classification functions.}
}
\value{
Invisible NULL. Produces a plot with ROC curves for each specified method.
}
\description{
Plots Receiver Operating Characteristic (ROC) curves for multiple classification
methods using cross-validation. Compares performance across different robust and
machine learning classifiers.
}
\details{
This function performs repeated cross-validation to generate ROC curves for
multiple classification methods. Each method is represented by a different line
type on the plot.

The function requires the \code{ROCR} package for ROC curve generation and performance
evaluation.

For each resample:
\enumerate{
\item Randomly split data into training (\code{pro.p} proportion) and test sets
\item Train classifier on training set
\item Predict class memberships for test set
\item Accumulate predictions across resamples
\item Plot averaged ROC curve
}
}
\references{
Makinde, O. S., & Fasoranbaku, O. A. (2018). On some classification methods based
on data depth. Journal of Applied Statistics.
}
\seealso{
\code{\link{CLASS.fun}}, \code{\link{UBROC}}
}
\keyword{classif}
\keyword{hplot}
\keyword{multivariate}
