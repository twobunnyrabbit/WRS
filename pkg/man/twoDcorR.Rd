% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{twoDcorR}
\alias{twoDcorR}
\title{Compare Two Robust Dependent Correlations (Overlapping)}
\usage{
twoDcorR(
  x,
  y,
  corfun = wincor,
  alpha = 0.05,
  nboot = 500,
  SEED = TRUE,
  MC = FALSE,
  outfun = outpro,
  ...
)
}
\arguments{
\item{x}{Matrix or data frame with 2 columns.}

\item{y}{Numeric vector shared by both correlations.}

\item{corfun}{Correlation function to use (default \code{wincor} for Winsorized correlation).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{nboot}{Number of bootstrap samples (default 500).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{MC}{Logical; if TRUE, uses parallel processing via mclapply.}

\item{outfun}{Outlier detection function (default \code{outpro}).}

\item{...}{Additional arguments passed to the correlation function.}
}
\value{
A list with components:
\item{est.rho1}{Correlation between x\link{,1} and y}
\item{est.rho2}{Correlation between x\link{,2} and y}
\item{ci}{Bootstrap confidence interval for the difference}
\item{p.value}{P-value for testing equality of correlations}
}
\description{
Compares two robust dependent correlations in the overlapping case using bootstrap.
By default, uses Winsorized correlation but other robust correlation measures can
be specified.
}
\details{
This function compares the correlation of x\link{,1} with y to the correlation of x\link{,2}
with y using a bootstrap approach. This is the "overlapping" case because both
correlations share the common variable y.

The function uses robust correlation measures (Winsorized correlation by default)
which are less sensitive to outliers than Pearson correlation. The bootstrap
provides valid inference without normality assumptions.
}
\examples{
set.seed(123)
n <- 50
x <- cbind(rnorm(n), rnorm(n))
y <- rnorm(n)

# Using Winsorized correlation
result <- twoDcorR(x, y)
result$ci
result$p.value
}
\seealso{
\code{\link{twoDNOV}}, \code{\link{TWOpov}}, \code{\link{tworhobt}},
\code{\link{wincor}}
}
