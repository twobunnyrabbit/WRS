% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anova.R
\name{t2way}
\alias{t2way}
\title{Heteroscedastic Two-Way ANOVA for Trimmed Means}
\usage{
t2way(
  J,
  K,
  x,
  tr = 0.2,
  grp = c(1:p),
  p = J * K,
  MAT = FALSE,
  lev.col = c(1:2),
  var.col = 3,
  pr = TRUE,
  IV1 = NULL,
  IV2 = NULL
)
}
\arguments{
\item{J}{Number of levels for Factor A.}

\item{K}{Number of levels for Factor B.}

\item{x}{Data in list mode (length J*K), matrix (columns = groups), or
vector (when used with IV1 and IV2).}

\item{tr}{Trimming proportion (default: 0.2). Proportion of observations to
trim from each tail when computing winsorized correlation.}

\item{grp}{A vector indicating group membership. Can be numeric, factor, or
character. Used for grouping observations in multi-group comparisons.}

\item{p}{Total number of groups (default: J*K).}

\item{MAT}{Logical. If TRUE, \code{x} is a matrix with factor levels in
columns specified by \code{lev.col} and data in \code{var.col}.}

\item{lev.col}{Column numbers for the two factors when MAT=TRUE (default: c(1,2)).}

\item{var.col}{Column number for the outcome variable when MAT=TRUE (default: 3).}

\item{pr}{Logical. If \code{TRUE}, prints progress messages (default: \code{TRUE}).}

\item{IV1}{Vector of first factor levels (when x is a vector of data).}

\item{IV2}{Vector of second factor levels (when x is a vector of data).}
}
\value{
A list with components:
\item{Qa}{Test statistic for main effect of Factor A.}
\item{Qa.crit}{Critical value for Factor A.}
\item{Qa.p.value}{P-value for Factor A.}
\item{Qb}{Test statistic for main effect of Factor B.}
\item{Qb.crit}{Critical value for Factor B.}
\item{Qb.p.value}{P-value for Factor B.}
\item{Qab}{Test statistic for A by B interaction.}
\item{Qab.crit}{Critical value for interaction.}
\item{Qab.p.value}{P-value for interaction.}
}
\description{
Performs a J by K two-way ANOVA on trimmed means for independent groups.
Does not assume equal variances. Tests main effects and interactions.
}
\details{
This function performs a heteroscedastic two-way ANOVA using trimmed means.
It uses the Johansen procedure and does not assume equal variances across
groups.

Data can be provided in several formats:
\itemize{
\item List mode: x[\link{1}] = group (1,1), x[\link{2}] = group (1,2), ..., x[\link{K}] = group (1,K), x[\link{K+1}] = group (2,1), etc.
\item Matrix: Columns correspond to groups in the same order
\item Long format: Use MAT=TRUE with factor levels in lev.col and data in var.col
\item Vectors: Provide data in x with factor levels in IV1 and IV2
}
}
\examples{
# 2x3 design with 6 groups
set.seed(123)
x <- vector("list", 6)
for(i in 1:6) x[[i]] <- rnorm(15, mean=i*0.3)
t2way(2, 3, x, tr=0.2)

# Using IV vectors
n <- 30
data <- rnorm(n)
factor1 <- rep(1:2, each=15)
factor2 <- rep(1:3, times=10)
t2way(2, 3, data, IV1=factor1, IV2=factor2)
}
\seealso{
\code{\link{t2wayv2}}, \code{\link{t3way}}, \code{\link{t2waybt}},
\code{\link{bwtrim}}, \code{\link{sppbb}}
}
