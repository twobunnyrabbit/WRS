% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{adcom}
\alias{adcom}
\title{Test Components in Generalized Additive Model}
\usage{
adcom(
  x,
  y,
  est = mean,
  tr = 0,
  nboot = 600,
  alpha = 0.05,
  fr = NA,
  jv = NA,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{x}{Matrix of predictor variables (must have 2 or more columns)}

\item{y}{Response vector}

\item{est}{Robust estimator function (default: \code{mean})}

\item{tr}{Trim proportion for robust estimators. Currently only 0 or 0.2 supported
(default: 0)}

\item{nboot}{Number of bootstrap samples for computing p-values (default: 600)}

\item{alpha}{Type I error rate (default: 0.05)}

\item{fr}{Span parameter for running interval smoother. If \code{NA} (default),
automatically determined based on sample size and trimming level}

\item{jv}{Component index to test. If \code{NA} (default), tests all components}

\item{SEED}{Logical. If \code{TRUE} (default), sets random seed for reproducibility}

\item{...}{Additional arguments passed to estimator function}
}
\value{
List with component:
\describe{
\item{\code{results}}{Matrix with columns \code{d.stat} (test statistic) and
\code{p.value} for each tested component}
}
}
\description{
Tests whether specific components in a generalized additive model are zero.
Uses a variation of the Stute et al. (1998) method combined with the running
interval smoother and backfitting algorithm. Tests H0: f_j(X_j) = 0.
}
\details{
For each component j, the function:
\enumerate{
\item Fits additive model using all predictors except j via \code{\link{adrun}}
\item Computes residuals from this reduced model
\item Tests whether predictor j is independent of residuals using \code{\link{indt}}
}

The span parameter \code{fr} is automatically selected based on sample size if not
specified. Current implementation supports only 0\\% or 20\\% trimming.
}
\references{
Stute, W., González Manteiga, W., & Presedo Quindimil, M. (1998). Bootstrap
approximations in model checks for regression. Journal of the American Statistical
Association, 93(441), 141-149.
}
\seealso{
\code{\link{adrun}}, \code{\link{adtest}}, \code{\link{indt}}
}
\keyword{models}
\keyword{regression}
