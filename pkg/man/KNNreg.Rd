% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression-advanced.R
\name{KNNreg}
\alias{KNNreg}
\title{K-Nearest Neighbors Regression with Robust Estimation}
\usage{
KNNreg(
  x,
  y,
  pts = NULL,
  K = 10,
  est = tmean,
  cov.fun = covmcd,
  xout = FALSE,
  outfun = outpro,
  ...
)
}
\arguments{
\item{x}{A numeric matrix containing predictor values (must have 2 or more columns).}

\item{y}{A numeric vector containing the response variable.}

\item{pts}{A matrix of points at which predictions are desired. If \code{NULL},
predictions are made at the observed x values (default: \code{NULL}).}

\item{K}{Number of nearest neighbors to use (default: 10).}

\item{est}{Estimator function for combining neighbors' Y values (default: \code{tmean}).
Can be any location estimator like \code{median}, \code{mean}, \code{mom}, etc.}

\item{cov.fun}{Covariance function used for computing Mahalanobis distances
to find nearest neighbors (default: \code{covmcd}).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
A vector of predicted Y values corresponding to rows of \code{pts} (or \code{x}
if \code{pts} is \code{NULL}).
}
\description{
Performs K-nearest neighbors regression using robust location estimators
instead of the conventional mean. Predictions are based on a robust estimator
of the K nearest neighbors' Y values.
}
\details{
K-nearest neighbors regression predicts the response at a point by finding the
K closest points in the predictor space and combining their Y values. This
function uses several robust enhancements:
\itemize{
\item Mahalanobis distance (via robust covariance) instead of Euclidean
\item Robust location estimators (e.g., trimmed mean) instead of mean
\item Optional outlier removal from predictor space (\code{xout = TRUE})
}

The function requires at least 2 predictors. For single-predictor problems,
use other smoothing methods like \code{qhdsm()} or \code{rplot()}.
}
\examples{
\dontrun{
# KNN regression with 2 predictors
set.seed(123)
x <- matrix(rnorm(200), ncol=2)
y <- x[,1]^2 + x[,2] + rnorm(100)
# Predict at observed points using 10 nearest neighbors
yhat <- KNNreg(x, y, K=10)
}
}
\references{
Wilcox, R.R. (2022). Introduction to Robust Estimation and Hypothesis Testing
(5th ed.). Academic Press.
}
\seealso{
\code{\link{regR.Forest}}, \code{\link{qhdsm}}, \code{\link{rplot}}
}
