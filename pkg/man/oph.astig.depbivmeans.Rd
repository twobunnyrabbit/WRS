% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{oph.astig.depbivmeans}
\alias{oph.astig.depbivmeans}
\title{Compare Bivariate Means for Dependent Astigmatism Data}
\usage{
oph.astig.depbivmeans(m, alpha = 0.05, nboot = 1999, SEED = TRUE, tr = 0)
}
\arguments{
\item{m}{Matrix or data frame with J columns (J must be even). Columns represent pairs:
columns 1-2 are formula 1, 3-4 are formula 2, etc.}

\item{alpha}{Significance level for hypothesis tests. Default is 0.05.}

\item{nboot}{Number of bootstrap samples. Default is 1999.}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducibility. Default is \code{TRUE}.}

\item{tr}{Trimming proportion (0 to 0.5). Default is 0 (no trimming, uses means).}
}
\value{
List of matrices, one for each pairwise comparison. Each matrix has 2 rows
(one per variable) and columns: Mean 1, Mean 2, p.value, p.adjusted.
}
\description{
Compares bivariate means of dependent groups for astigmatism prediction errors.
Uses bootstrap methods with trimmed means on difference scores and applies
Holm's multiple testing correction.
}
\details{
For each pair of formulas (e.g., columns 1-2 vs 3-4), the function:
\enumerate{
\item Computes difference scores: column 1 minus column 3, column 2 minus column 4
\item Uses \code{\link{trimcibt}} to compute bootstrap confidence intervals for each difference
\item Computes trimmed means for each variable
\item Tests null hypothesis that both differences equal zero
\item Applies Holm's method to adjust p-values for multiple comparisons
}
}
\seealso{
\code{\link{oph.astig.indepbivmeans}}, \code{\link{trimcibt}},
\code{\link{oph.astig.depbivtotvars}}
}
\keyword{ophthalmology}
