% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{spca}
\alias{spca}
\title{Spherical Principal Component Analysis}
\usage{
spca(x, var.fun = pbvar, SCORES = FALSE, pval = 1)
}
\arguments{
\item{x}{Numeric matrix (n×p) containing the data.}

\item{var.fun}{Function for variance estimation. Default is \code{\link{pbvar}}
(percentage bend variance). Used to compute robust eigenvalues.}

\item{SCORES}{Logical. If \code{TRUE}, computes principal component scores.
Default is \code{FALSE}.}

\item{pval}{Integer. Number of principal components to compute scores for
(when \code{SCORES=TRUE}). Default is 1.}
}
\value{
List with components:
\item{eigen.val}{Robust eigenvalues (length p)}
\item{B}{Eigenvector matrix (p×p)}
\item{spat.mu}{Spatial median used for centering (length p)}
\item{scores}{If \code{SCORES=TRUE}, matrix of scores (n×pval); otherwise NULL}
}
\description{
Performs robust principal component analysis using the spherical PCA method,
which projects data onto the unit sphere before computing principal components.
}
\details{
Spherical PCA is a robust alternative to classical PCA that:
\enumerate{
\item Centers data using spatial median (\code{\link{spat}})
\item Projects each observation onto unit sphere: yᵢ = (xᵢ - center) / ||xᵢ - center||
\item Computes covariance of spherical data
\item Obtains eigenvectors from spherical covariance
\item Computes robust eigenvalues using \code{var.fun} on projected scores
}

This approach downweights extreme observations and provides resistance to outliers.
The method can handle heavy-tailed distributions better than classical PCA.
}
\references{
Locantore, N., et al. (1999). Robust principal component analysis for functional data.
Test, 8(1), 1-73.

Maronna, R. A. (2005). Principal components and orthogonal regression based on
robust scales. Technometrics, 47(3), 264-273.
}
\seealso{
\code{\link{SPCA}}, \code{\link{spat}}, \code{\link{pbvar}}
}
\keyword{multivariate}
