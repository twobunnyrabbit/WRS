% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression-advanced.R
\name{regYciCV}
\alias{regYciCV}
\title{Determine Critical Value for regYci via Simulation}
\usage{
regYciCV(
  n,
  alpha = 0.05,
  nboot = 1000,
  regfun = tsreg,
  SEED = TRUE,
  MC = FALSE,
  null.value = 0,
  xout = FALSE,
  ...
)
}
\arguments{
\item{n}{Sample size for simulation.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{nboot}{Number of bootstrap samples for simulation (default: 1000).}

\item{regfun}{Regression function to use (default: \code{tsreg}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{MC}{Logical. If \code{TRUE}, uses parallel processing via \code{mclapply}
(default: \code{FALSE}).}

\item{null.value}{Null hypothesis value for testing (default: 0).}

\item{xout}{Logical; if TRUE, remove outliers from simulated data before
regression (default: FALSE).}

\item{...}{Additional arguments passed to \code{regfun}.}
}
\value{
Scalar critical value for use in \code{regYci(..., crit=)}.
}
\description{
Computes a critical value for simultaneous confidence intervals in \code{regYci()}
via simulation. Used to control family-wise error rate when testing at
multiple design points simultaneously.
}
\details{
This function simulates data under the null hypothesis to empirically
determine the critical value needed for simultaneous inference across
multiple design points. The critical value adjusts for the family-wise
error rate when making multiple comparisons.

The simulation:
\itemize{
\item Generates \code{nboot} datasets with uncorrelated bivariate normal data
\item Fits regression at multiple points for each dataset
\item Computes minimum p-values across all points
\item Returns the alpha quantile of these minimum p-values
}
}
\examples{
\dontrun{
# Determine critical value for n=50, alpha=0.05
crit <- regYciCV(n=50, alpha=0.05, nboot=500)

# Use with regYci for simultaneous inference
x <- rnorm(50)
y <- 2*x + rnorm(50)
regYci(x, y, crit=crit)
}
}
\references{
Wilcox, R.R. (2022). Introduction to Robust Estimation and Hypothesis Testing
(5th ed.). Academic Press.
}
\seealso{
\code{\link{regYci}}, \code{\link{regYband}}
}
