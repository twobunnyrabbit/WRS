% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00-utils-core.R
\name{lplot.pred}
\alias{lplot.pred}
\title{Predict Values Using LOESS}
\usage{
lplot.pred(
  x,
  y,
  pts = NULL,
  xout = FALSE,
  outfun = outpro,
  span = 2/3,
  family = "gaussian",
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{pts}{Points at which to predict Y values. If NULL, uses x (default: NULL).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{span}{Span parameter for loess (default: 2/3).}

\item{family}{Family for loess ('gaussian' or 'symmetric', default: 'gaussian').}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
A list with components:
\item{n}{Original sample size}
\item{n.keep}{Sample size after outlier removal}
\item{x.used}{X values used for fitting}
\item{yhat}{Predicted Y values at pts}
}
\description{
Computes predicted Y values at specified X points using LOESS (locally
weighted scatterplot smoothing).
}
\details{
This function fits a LOESS smooth to the data and computes predictions
at the points specified in pts. If \code{xout = TRUE}, outliers in X
are removed before fitting.
}
\examples{
x <- rnorm(50)
y <- 2*x + rnorm(50)
result <- lplot.pred(x, y)
plot(x, y)
points(x, result$yhat, col = "red", pch = 19)

}
