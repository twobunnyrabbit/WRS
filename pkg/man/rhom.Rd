% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{rhom}
\alias{rhom}
\title{Test for Homoscedasticity in Regression}
\usage{
rhom(
  x,
  y,
  op = 1,
  op2 = FALSE,
  tr = 0.2,
  plotit = TRUE,
  xlab = "NA",
  ylab = "NA",
  zlab = "ABS(res)",
  est = median,
  sm = FALSE,
  SEED = TRUE,
  xout = FALSE,
  outfun = outpro,
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{op}{Test method: 1 = regression (default), 2 = Winsorized correlation.}

\item{op2}{Logical. If \code{FALSE} (default), tests homogeneity using running
interval smoother; if \code{TRUE}, tests independence based on residuals.}

\item{tr}{Trimming proportion (default: 0.2). Proportion of observations to
trim from each tail when computing winsorized correlation.}

\item{plotit}{Logical. Create diagnostic plot (default: \code{TRUE}).}

\item{xlab, ylab, zlab}{Axis labels for plot.}

\item{est}{Location estimator for residuals (default: \code{median}).}

\item{sm}{Logical. Use smoother for plot (default: \code{FALSE}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{xout}{Logical. Remove outliers (default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{...}{Additional arguments passed to plotting functions.}
}
\value{
List with test results for homoscedasticity.
}
\description{
Tests the hypothesis of homoscedasticity (constant variance) in regression
model Y = m(X) + s(X)e.
}
\details{
Tests H0: s(X) = 1 for all X, where s(X) models heteroscedasticity.

\strong{Method options:}
\itemize{
\item op = 1: Uses regression method (regci)
\item op = 2: Uses Winsorized correlation with heteroscedastic bootstrap
}

\strong{Note:} op2 = TRUE should NOT be used when testing for homoscedastic error term.

For p > 1 predictors, tests each predictor separately.
}
\examples{
\dontrun{
set.seed(123)
x <- rnorm(50)
y <- 2 * x + rnorm(50, sd = abs(x))  # Heteroscedastic
rhom(x, y, plotit = FALSE)
}
}
\seealso{
\code{\link{regci}}, \code{\link{wincor}}
}
