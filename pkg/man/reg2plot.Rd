% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{reg2plot}
\alias{reg2plot}
\title{Plot Two Regression Lines for Group Comparison}
\usage{
reg2plot(
  x1,
  y1,
  x2,
  y2,
  regfun = tsreg,
  xlab = "X",
  ylab = "Y",
  xout = FALSE,
  outfun = outpro,
  pch1 = ".",
  pch2 = "+",
  ...
)
}
\arguments{
\item{x1}{Numeric vector of predictor values for group 1.}

\item{y1}{Numeric vector of response values for group 1.}

\item{x2}{Numeric vector of predictor values for group 2.}

\item{y2}{Numeric vector of response values for group 2.}

\item{regfun}{Regression function to use for fitting (default: \code{tsreg}
for Theil-Sen regression). Can be any regression function that returns
a list with a \code{coef} component.}

\item{xlab}{Label for x-axis in plots (default: "X" or "VAR 1").}

\item{ylab}{Label for y-axis in plots (default: "Y" or "VAR 2").}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
Invisibly returns \code{NULL}. Creates a plot as a side effect.
}
\description{
Creates a scatter plot with fitted regression lines for two independent
groups. Useful for visualizing and comparing regression relationships
between groups.
}
\details{
The function removes missing values automatically using \code{elimna()}.
Group 1 regression line is plotted as a solid line, group 2 as a dashed
line (lty=2). Points for group 1 use pch1 character, group 2 uses pch2.
}
\examples{
# Compare regression lines for two groups
x1 <- rnorm(50)
y1 <- 2*x1 + rnorm(50)
x2 <- rnorm(50)
y2 <- 3*x2 + rnorm(50)
reg2plot(x1, y1, x2, y2)
}
\seealso{
\code{\link{tsreg}}, \code{\link{reg2g.p2plot}} for 3D version
}
