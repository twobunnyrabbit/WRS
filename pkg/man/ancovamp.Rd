% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{ancovamp}
\alias{ancovamp}
\title{ANCOVA for Multiple Covariates with Trimmed Means}
\usage{
ancovamp(
  x1,
  y1,
  x2,
  y2,
  fr1 = 1,
  fr2 = 1,
  tr = 0.2,
  alpha = 0.05,
  pts = NA,
  SEED = TRUE,
  plotit = FALSE,
  FWE = TRUE,
  xlab = "V1",
  ylab = "V2"
)
}
\arguments{
\item{x1}{Matrix of covariate values for group 1 (two or more columns)}

\item{y1}{Response values for group 1}

\item{x2}{Matrix of covariate values for group 2 (two or more columns)}

\item{y2}{Response values for group 2}

\item{fr1}{Span parameter for group 1 smoother (default: 1)}

\item{fr2}{Span parameter for group 2 smoother (default: 1)}

\item{tr}{Trimming proportion (default: 0.2). Proportion of observations to
trim from each tail when computing winsorized correlation.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{pts}{Optional matrix of design points (one row per point). If NA, uses
points with deepest depth in x1 (via ancdes)}

\item{SEED}{Logical; if TRUE, set seed for reproducible cov.mve results (default: TRUE)}

\item{plotit}{Logical; if TRUE and exactly 2 covariates, plot design points
marking significant ones (default: FALSE)}

\item{FWE}{Logical; if TRUE, use familywise error rate for determining
significance; if FALSE, use individual p-values (default: TRUE)}

\item{xlab}{Label for first covariate in plot (default: 'V1')}

\item{ylab}{Label for second covariate in plot (default: 'V2')}
}
\value{
List with components:
\item{points}{Matrix of design points used for comparisons}
\item{output}{Matrix with columns: n1, n2, DIF (trimmed mean difference),
TEST (test statistic), se, ci.low, ci.hi (adjusted CI), p.value, adj.p.value
(FWE-adjusted p-value)}
\item{crit}{Critical value from Studentized Maximum Modulus distribution}
\item{sig.pts}{Matrix of significant design points (based on FWE setting)}
}
\description{
Compare two independent groups using ANCOVA with multiple covariates and running
interval smoothers. Uses Yuen's trimmed mean test at design points chosen based
on data depth. No parametric assumptions about regression surface forms.
Confidence intervals control familywise error rate.
}
\note{
\itemize{
\item Design points with fewer than 10 observations in either group are excluded
\item Warnings issued if fewer than 6 observations at any design point
\item Uses MVE (Minimum Volume Ellipsoid) for robust covariance estimation
}
}
\examples{
\dontrun{
# Generate data with two covariates
set.seed(567)
n1 <- 60; n2 <- 60
x1 <- matrix(rnorm(n1*2), ncol=2)
y1 <- 2 + 0.5*x1[,1] + 0.3*x1[,2] + rnorm(n1)
x2 <- matrix(rnorm(n2*2), ncol=2)
y2 <- 3 + 0.5*x2[,1] + 0.3*x2[,2] + rnorm(n2)

# Compare at data-driven design points
result <- ancovamp(x1, y1, x2, y2, plotit = TRUE)
print(result$output)
}

}
