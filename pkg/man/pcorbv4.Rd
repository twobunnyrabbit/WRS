% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{pcorbv4}
\alias{pcorbv4}
\title{Pearson Correlation Bootstrap Variant 4}
\usage{
pcorbv4(x, y, SEED = TRUE)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}
}
\value{
List with components:
\item{r}{Pearson correlation coefficient.}
\item{ci}{95\% confidence interval.}
}
\description{
Computes a 95\% bootstrap confidence interval for Pearson correlation using
an adjusted percentile method (variant 4).
}
\details{
This function uses an adjusted percentile bootstrap method that performs
well when the error term is heteroscedastic. The adjustment varies by sample
size to improve coverage.

\strong{Warning}: If the number of bootstrap samples (nboot = 599) is altered,
the confidence interval adjustment for n < 250 may not be valid. While an
obvious linear adjustment seems to work, no formal investigations have been
performed.

This is variant 4 in a series of Pearson bootstrap methods; see also \code{pcorb()}.
}
\examples{
set.seed(123)
x <- rnorm(50)
y <- 0.7 * x + rnorm(50, sd = abs(x))  # Heteroscedastic
pcorbv4(x, y)
}
\seealso{
\code{\link{pcorb}}, \code{\link{pcor}}, \code{\link{corb}}
}
