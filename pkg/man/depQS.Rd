% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{depQS}
\alias{depQS}
\title{Quantile Shift Effect Size for Dependent Groups}
\usage{
depQS(x, y = NULL, null.value = 0, locfun = median, ...)
}
\arguments{
\item{x}{First group data vector, or difference scores if y is NULL}

\item{y}{Second group data vector (optional)}

\item{null.value}{Null value for comparison (default: 0)}

\item{locfun}{Location estimator function (default: \code{median})}

\item{...}{Additional arguments passed to locfun}
}
\value{
A list with component:
\item{Q.effect}{Quantile shift effect size}
}
\description{
Computes a probabilistic measure of effect size based on quantile shift
for two dependent groups using difference scores.
}
\details{
For dependent groups, computes difference scores L = X - Y (or uses x directly
if y is NULL). The effect size is computed as:
\deqn{P(L - \hat{\theta} + \delta \leq \hat{\theta})}

where \eqn{\hat{\theta}} is the location estimate and \eqn{\delta} is the
null value.

This provides a probabilistic interpretation of effect size: the proportion
of shifted difference scores that are less than or equal to the location estimate.
}
\examples{
\dontrun{
x <- rnorm(30)
y <- x + rnorm(30, mean=0.5)
depQS(x, y)  # Using median
depQS(x, y, locfun=tmean, tr=0.2)  # Using 20\% trimmed mean
}

}
\seealso{
\code{\link{depQSci}}, \code{\link{QS.sum.ci}}
}
\keyword{htest}
\keyword{robust}
\keyword{univar}
