% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{discstep}
\alias{discstep}
\title{Step-Down Multiple Comparisons for Discrete Distributions}
\usage{
discstep(x, nboot = 500, alpha = 0.05, SEED = TRUE)
}
\arguments{
\item{x}{Numeric matrix or list. If matrix, rows are observations and columns are groups.
If list, each element is a vector of discrete values for one group.}

\item{nboot}{Integer. Number of bootstrap samples for null distribution (default: 500).}

\item{alpha}{Numeric. Familywise Type I error rate (default: 0.05).}

\item{SEED}{Logical. If TRUE, sets random seed to 2 for reproducibility (default: TRUE).}
}
\value{
List with component:
\describe{
\item{results}{Matrix with columns:
\itemize{
\item \code{Groups}: Group comparison coded as integer (e.g., 12 = groups 1 vs 2)
\item \code{p-value}: Bootstrap p-value for comparison
\item \code{p.crit}: Critical p-value controlling FWE
}
Results ordered from most to least significant}
}
}
\description{
Performs step-down multiple comparison procedure for comparing J (≤5) independent
discrete random variables. Based on a generalization of the Storer-Kim method for
binomial comparisons; can detect distributional differences not captured by location measures.
}
\details{
This function performs all pairwise comparisons of J discrete distributions using
a step-down procedure that controls familywise error rate.

\strong{Method}:
\enumerate{
\item Tests all pairwise comparisons using a discrete distribution test statistic
\item Generates null distribution via bootstrap sampling under the global null hypothesis
\item Uses step-down logic: tests from most to least significant, stopping when
a non-significant comparison is found
\item Critical values are adjusted for number of groups being compared
}

The procedure is particularly useful for:
\itemize{
\item Ordinal data
\item Count data
\item Categorical outcomes with ordered or unordered categories
\item Situations where location-based tests may miss distributional shifts
}

\strong{Limitations}:
\itemize{
\item Designed for J ≤ 5 groups (stops with error if J > 5)
\item For J = 2, use \code{\link{disc2.chi.sq}} instead
}
}
\examples{
# Compare 3 discrete distributions
set.seed(123)
x1 <- sample(1:4, 30, replace = TRUE, prob = c(0.4, 0.3, 0.2, 0.1))
x2 <- sample(1:4, 30, replace = TRUE, prob = c(0.2, 0.3, 0.3, 0.2))
x3 <- sample(1:4, 30, replace = TRUE, prob = c(0.1, 0.2, 0.3, 0.4))
x <- cbind(x1, x2, x3)
result <- discstep(x, nboot = 200)
result$results
}
\seealso{
\code{\link{disc2.chi.sq}}, \code{\link{chisq.test}}
}
\keyword{htest}
\keyword{nonparametric}
