% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{effectg}
\alias{effectg}
\title{Robust Heteroscedastic Effect Size (General Estimators)}
\usage{
effectg(x, y, locfun = tmean, varfun = winvarN, nboot = 100, SEED = TRUE, ...)
}
\arguments{
\item{x}{Numeric vector. First group.}

\item{y}{Numeric vector. Second group.}

\item{locfun}{Function. Location estimator (default: \code{tmean}).}

\item{varfun}{Function. Variance/scale estimator (default: \code{winvarN}).}

\item{nboot}{Integer. Number of bootstrap samples when sample sizes differ (default: 100).}

\item{SEED}{Logical. If TRUE, sets random seed to 2 for reproducibility (default: TRUE).}

\item{...}{Additional arguments passed to \code{locfun} and \code{varfun}.}
}
\value{
List with components:
\describe{
\item{Explanatory.power}{Proportion of variance explained (ES²)}
\item{Effect.Size}{Effect size (ES = sqrt of explanatory power)}
}
}
\description{
Computes a robust heteroscedastic measure of explanatory power (effect size) using
user-specified location and scale estimators. Allows flexible effect size computation
beyond trimmed means.
}
\details{
The function computes a standardized effect size:

\deqn{ES^2 = \frac{(L_X - L_Y)^2}{V_X + V_Y}}

where:
\itemize{
\item \eqn{L_X}, \eqn{L_Y} are location estimates (e.g., trimmed means, medians)
\item \eqn{V_X}, \eqn{V_Y} are scale estimates (e.g., winsorized variances, MAD)
}

\strong{Equal sample sizes}: Computes effect size directly
\strong{Unequal sample sizes}: Resamples to equalize n using bootstrap, then averages
effect size estimates to avoid bias

This generalizes the effect size from Yuen's test to arbitrary robust estimators.

\strong{Common choices}:
\itemize{
\item \code{locfun=tmean, varfun=winvarN}: Trimmed mean effect size (default)
\item \code{locfun=median, varfun=mad}: Median-based effect size
\item \code{locfun=onestep, varfun=winvarN}: One-step M-estimator effect size
}
}
\examples{
# Trimmed mean effect size
set.seed(123)
x <- rnorm(30, mean = 0)
y <- rnorm(30, mean = 0.8)
effectg(x, y)

# Median-based effect size
effectg(x, y, locfun = median, varfun = mad)
}
\seealso{
\code{\link{ees.ci}}, \code{\link{yuenv2}}, \code{\link{tmean}},
\code{\link{winvarN}}, \code{\link{onestep}}
}
\keyword{htest}
\keyword{robust}
