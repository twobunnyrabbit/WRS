% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{wmw.ref.mul}
\alias{wmw.ref.mul}
\title{Test Symmetry at Multiple Reference Points}
\usage{
wmw.ref.mul(
  x,
  y,
  refp = NULL,
  pts = NULL,
  q = seq(0.6, 0.9, 0.1),
  center = FALSE,
  estfun = hdmq,
  alpha = 0.05,
  nboot = 1000,
  SEED = TRUE,
  method = "BH",
  plotit = FALSE,
  xlab = "Difference",
  ylab = "Density",
  plotfun = kerSORT
)
}
\arguments{
\item{x}{Observations from group 1.}

\item{y}{Observations from group 2.}

\item{refp}{Vector of reference points (default: NULL for quantile-based).}

\item{pts}{Alias for refp (default: NULL).}

\item{q}{Quantiles for reference points if refp=NULL (default: seq(0.6, 0.9, 0.1)).}

\item{center}{Logical. If \code{TRUE}, centers differences at median (default: \code{FALSE}).}

\item{estfun}{Quantile estimator (default: \code{hdmq} for Harrell-Davis).}

\item{alpha}{Significance level (default: 0.05).}

\item{nboot}{Number of bootstrap samples (default: 1000).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed (default: \code{TRUE}).}

\item{method}{Multiple testing adjustment method (default: 'BH').}

\item{plotit}{Logical. If \code{TRUE}, plots distribution (default: \code{FALSE}).}

\item{xlab}{X-axis label (default: 'Difference').}

\item{ylab}{Y-axis label (default: 'Density').}

\item{plotfun}{Plotting function (default: \code{kerSORT}).}
}
\value{
Matrix with columns for reference points, tail probabilities,
differences, confidence intervals, p-values, and adjusted p-values.
}
\description{
Tests symmetry of the X-Y distribution at multiple reference points using
bootstrap. Performs multiple tests with p-value adjustment.
}
\details{
Extension of \code{wmw.ref.dif} to multiple reference points with
family-wise error control via p-value adjustment.
}
\examples{
\dontrun{
# Multiple reference points test
set.seed(123)
x <- rnorm(50)
y <- rnorm(50, mean = 0.3)
wmw.ref.mul(x, y, nboot = 500)
}
}
\seealso{
\code{\link{wmw.ref.dif}}, \code{\link{wmw.QC.mul}}
}
