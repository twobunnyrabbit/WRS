% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/location.R
\name{Dcbmhd}
\alias{Dcbmhd}
\title{CI for Sum of Symmetric Quantiles of Difference (Dependent Groups)}
\usage{
Dcbmhd(
  x = NULL,
  y = NULL,
  d = NULL,
  qest = hd,
  alpha = 0.05,
  q = 0.25,
  plotit = FALSE,
  pop = 0,
  fr = 0.8,
  rval = 15,
  xlab = "",
  ylab = "",
  nboot = 600,
  SEED = TRUE
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{d}{Optional vector of pre-computed differences. If \code{NULL},
computed as x - y.}

\item{qest, pop, fr, rval, xlab, ylab}{See \code{\link{cbmhd}}.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{q}{Quantile to estimate. For example, \code{q = 0.5} estimates the median,
\code{q = 0.25} estimates the first quartile (default: 0.5).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}
}
\value{
Same as \code{\link{cbmhd}} plus component \code{n} (sample size).
}
\description{
Computes a confidence interval for the sum of the qth and (1-q)th quantiles
of D = X - Y for dependent (paired) groups.
}
\details{
For dependent groups, this function works with the difference scores
directly. If the distribution of X - Y is symmetric about zero, the sum
of symmetric quantiles should be zero.
}
\examples{
x <- rnorm(30)
y <- x + rnorm(30, sd = 0.5)
Dcbmhd(x, y, q = 0.25, nboot = 500)

}
\seealso{
\code{\link{cbmhd}} for independent groups,
\code{\link{Dqcomhd}} for comparing multiple quantiles
}
