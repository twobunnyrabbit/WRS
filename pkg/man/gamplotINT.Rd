% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression-advanced.R
\name{gamplotINT}
\alias{gamplotINT}
\title{Plot GAM Regression Surface with Interaction Term}
\usage{
gamplotINT(
  x,
  y,
  pyhat = FALSE,
  plotit = TRUE,
  theta = 50,
  phi = 25,
  expand = 0.5,
  xout = FALSE,
  SCALE = FALSE,
  zscale = TRUE,
  eout = FALSE,
  outfun = out,
  ticktype = "simple",
  xlab = "X",
  ylab = "Y",
  zlab = "",
  ...
)
}
\arguments{
\item{x}{A numeric matrix with exactly 2 columns containing the two predictor
variables.}

\item{y}{A numeric vector containing the response variable.}

\item{pyhat}{Logical. If \code{TRUE}, returns predicted Y values; if \code{FALSE},
returns \code{NULL} (default: \code{FALSE}).}

\item{plotit}{Logical. If \code{TRUE}, creates 3D surface plot (default: \code{TRUE}).}

\item{theta}{Azimuthal viewing angle for 3D plot in degrees (default: 50).}

\item{phi}{Colatitude viewing angle for 3D plot in degrees (default: 25).}

\item{expand}{Expansion factor for z-axis (default: 0.5).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{SCALE}{Logical. If \code{TRUE}, standardizes predictors before fitting
(default: \code{FALSE}).}

\item{zscale}{Logical. If \code{TRUE}, standardizes the response variable
(default: \code{TRUE}).}

\item{eout}{Logical. If \code{TRUE}, removes outliers from joint (X,Y) space
before fitting (default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{ticktype}{Type of tick marks for 3D plot (default: "simple").}

\item{xlab, ylab, zlab}{Axis labels for the 3D plot.}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
If \code{pyhat = TRUE}, returns a vector of predicted Y values;
otherwise returns \code{NULL}. Creates a 3D plot as a side effect.
}
\description{
Plots a 3D regression surface for two predictors using generalized additive
models (GAM) with an interaction term included. This extends \code{gamplot()} by
allowing the predictors to interact.
}
\details{
This function extends \code{gamplot()} by including an interaction term between
the two predictors:

\deqn{E(Y|X_1,X_2) = s_1(X_1) + s_2(X_2) + s_{12}(X_1 \times X_2)}

where s() represents smooth functions estimated via GAM.

The interaction term allows the effect of one predictor to vary with the
level of the other predictor, providing more flexibility than the additive
model in \code{gamplot()}.

Outlier handling options:
\itemize{
\item \code{xout = TRUE}: Remove outliers from predictor space only
\item \code{eout = TRUE}: Remove outliers from joint (X,Y) space
\item Cannot set both \code{xout} and \code{eout} to TRUE
}

The 3D plot is created using \code{persp()} with viewing angles controlled by
\code{theta} (rotation) and \code{phi} (elevation).

Requires the mgcv package for GAM fitting.
}
\examples{
\dontrun{
# GAM surface with interaction
set.seed(123)
x <- matrix(rnorm(100), ncol=2)
y <- 2*x[,1] + 3*x[,2] + x[,1]*x[,2] + rnorm(50)
gamplotINT(x, y)
}
}
\references{
Wilcox, R.R. (2022). Introduction to Robust Estimation and Hypothesis Testing
(5th ed.). Academic Press.
}
\seealso{
\code{\link{gamplot}}, \code{\link{gamplotv2}}, \code{\link{gamindt}}
}
