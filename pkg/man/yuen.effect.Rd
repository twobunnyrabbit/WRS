% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{yuen.effect}
\alias{yuen.effect}
\title{Explanatory Measure of Effect Size for Yuen's Test}
\usage{
yuen.effect(
  x,
  y,
  tr = 0.2,
  alpha = 0.05,
  plotit = FALSE,
  plotfun = splot,
  op = TRUE,
  VL = TRUE,
  cor.op = FALSE,
  xlab = "Groups",
  ylab = "",
  PB = FALSE
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{tr}{Trimming proportion (default: 0.2). Proportion of observations to
trim from each tail when computing winsorized correlation.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{plotfun}{Plotting function to use (default: \code{splot}).}

\item{op}{Number of outliers to remove from each tail when using outlier
detection methods. Default varies by function.}

\item{VL}{Logical. If \code{TRUE}, adds vertical lines to plot (default: \code{TRUE}).}

\item{cor.op}{Logical. If \code{TRUE}, displays correlation information (default: \code{FALSE}).}

\item{xlab}{Label for x-axis in plots (default: "X" or "VAR 1").}

\item{ylab}{Label for y-axis in plots (default: "Y" or "VAR 2").}

\item{PB}{Logical. If \code{TRUE}, adds percentile bootstrap confidence interval (default: \code{FALSE}).}
}
\value{
A list with effect size estimate and related information.
}
\description{
Computes an explanatory measure of effect size when comparing trimmed means.
}
\details{
This effect size measure is an explanatory measure that indicates the
probability-based interpretation of the difference between trimmed means.
Same as \code{yuen} but additionally computes explanatory power and related
effect size measures.
}
\examples{
\dontrun{
x <- rnorm(50)
y <- rnorm(60, mean = 0.5)
yuen.effect(x, y)
}
}
\seealso{
\code{\link{yuen.effect.ci}}, \code{\link{akp.effect}}
}
