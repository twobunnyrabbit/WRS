% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{smgridRC}
\alias{smgridRC}
\title{Compare Location Measures on Grid Defined by Two IVs with Row/Column Comparisons}
\usage{
smgridRC(
  x,
  y,
  IV = c(1, 2),
  Qsplit1 = 0.5,
  Qsplit2 = 0.5,
  tr = 0.2,
  alpha = 0.05,
  PB = FALSE,
  est = tmean,
  nboot = 1000,
  pr = TRUE,
  method = "hoch",
  xout = FALSE,
  outfun = outpro,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{x}{Matrix or data frame containing independent variables.}

\item{y}{Numeric vector containing the dependent variable.}

\item{IV}{Two-element vector specifying which columns of \code{x} to use as
independent variables. Default is \code{c(1,2)}.}

\item{Qsplit1}{Quantile(s) for splitting the first IV. Default is 0.5 (median).
Can be vector, e.g., \code{c(.25,.5,.75)} for quartiles.}

\item{Qsplit2}{Quantile(s) for splitting the second IV. Default is 0.5 (median).}

\item{tr}{Amount of trimming for trimmed mean. Default is 0.2 (20\\% trimming).
Ignored if \code{PB=TRUE}.}

\item{alpha}{Significance level. Default is 0.05.}

\item{PB}{Logical. If \code{TRUE}, uses percentile bootstrap. Automatically set
to \code{TRUE} if \code{est=median} or \code{est=hd}. Default is \code{FALSE}.}

\item{est}{Estimator function for location. Default is \code{\link{tmean}}.
Other options: \code{median}, \code{hd}.}

\item{nboot}{Number of bootstrap samples if \code{PB=TRUE}. Default is 1000.}

\item{pr}{Logical. If \code{TRUE} (default), prints messages about eliminated groups.}

\item{method}{P-value adjustment method. Default is \code{'hoch'} (Hochberg).}

\item{xout}{Logical. If \code{TRUE}, removes outliers based on \code{outfun}.
Default is \code{FALSE}.}

\item{outfun}{Outlier detection function. Default is \code{\link{outpro}}.}

\item{SEED}{Logical. If \code{TRUE} (default), sets random seed for reproducibility.}

\item{...}{Additional arguments passed to \code{est}.}
}
\value{
List with components:
\itemize{
\item \code{est.loc.4.DV} - Matrix of location estimates for each grid cell
\item \code{n} - Matrix of sample sizes for each grid cell
\item \code{Independent.variables.summary} - Summary statistics for IV ranges
\item \code{Res.4.IV1} - Results for IV1 pairwise comparisons (within IV2 levels)
\item \code{Res.4.IV2} - Results for IV2 pairwise comparisons (within IV1 levels)
}
}
\description{
Compares measures of location among groups defined by quantile-based grids of two
independent variables. Performs pairwise comparisons separately for each level of
IV1 (row comparisons) and each level of IV2 (column comparisons).
}
\details{
This function creates a grid of groups by splitting the data based on quantiles of
two independent variables, then performs pairwise comparisons of location measures:
\itemize{
\item Row comparisons: Compares groups within each level of IV2
\item Column comparisons: Compares groups within each level of IV1
}

Groups with n ≤ 5 are automatically excluded. Uses \code{\link{lincon}} for
non-bootstrap methods and \code{\link{linpairpb}} for bootstrap methods.
P-values are adjusted for multiple comparisons.
}
\seealso{
\code{\link{smgrid}}, \code{\link{smgridVRC}}, \code{\link{lincon}},
\code{\link{linpairpb}}
}
\keyword{htest}
\keyword{multivariate}
\keyword{robust}
