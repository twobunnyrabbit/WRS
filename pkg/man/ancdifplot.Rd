% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{ancdifplot}
\alias{ancdifplot}
\title{Plot Difference Between Two Groups with Confidence Band - ANCOVA}
\usage{
ancdifplot(
  x1,
  y1,
  x2,
  y2,
  fr1 = 1,
  fr2 = 1,
  tr = 0.2,
  alpha = 0.05,
  pr = TRUE,
  xout = FALSE,
  outfun = out,
  LP = TRUE,
  nmin = 8,
  scat = TRUE,
  xlab = "X",
  ylab = "Y",
  report = FALSE,
  ...
)
}
\arguments{
\item{x1}{Covariate values for group 1.}

\item{y1}{Response values for group 1.}

\item{x2}{Covariate values for group 2.}

\item{y2}{Response values for group 2.}

\item{fr1}{Span for group 1 running interval smoother (default: 1).}

\item{fr2}{Span for group 2 running interval smoother (default: 1).}

\item{tr}{Trimming proportion (default: 0.2). Proportion of observations to
trim from each tail when computing winsorized correlation.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{pr}{Logical. Print progress information.}

\item{xout}{Logical. Remove outliers from covariates before analysis.}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{LP}{Logical. Use lowess/loess smoothing for the difference plot.}

\item{nmin}{Minimum number of values close to a design point (default: 8).}

\item{scat}{Logical. Show scatterplot of data.}

\item{xlab, ylab}{Axis labels for the plot.}

\item{report}{Logical. If TRUE, return detailed output instead of just plotting.}

\item{...}{Additional arguments passed to outfun.}
}
\value{
If report=TRUE, returns list with detailed results. Otherwise creates plot.
}
\description{
Compares two independent groups using ANCOVA with running interval smoother.
Creates a plot showing the difference between groups with a confidence band
having simultaneous probability coverage 1-alpha.
}
\note{
No parametric assumption is made about the form of regression lines.
One covariate only is allowed with this function.
}
\examples{
\dontrun{
x1 <- rnorm(50); y1 <- x1 + rnorm(50)
x2 <- rnorm(50); y2 <- 0.5*x2 + rnorm(50, mean=1)
ancdifplot(x1, y1, x2, y2, alpha=0.05)
}
}
