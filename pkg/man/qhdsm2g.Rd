% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression-advanced.R
\name{qhdsm2g}
\alias{qhdsm2g}
\title{Two-Group Quantile Regression Smoother}
\usage{
qhdsm2g(
  x1,
  y1,
  x2,
  y2,
  q = 0.5,
  qval = NULL,
  LP = TRUE,
  fr = 0.8,
  xlab = "X",
  ylab = "Y",
  xout = FALSE,
  outfun = outpro,
  ...
)
}
\arguments{
\item{x1}{Numeric vector of predictor values for group 1.}

\item{y1}{Numeric vector of response values for group 1.}

\item{x2}{Numeric vector of predictor values for group 2.}

\item{y2}{Numeric vector of response values for group 2.}

\item{q}{Quantile to estimate (default: 0.5 for median). Must be between 0 and 1.}

\item{qval}{Deprecated; use \code{q} instead.}

\item{LP}{Logical; if \code{TRUE} (default), apply lowess smoothing to the quantile
curves for visualization clarity.}

\item{fr}{Fraction of data used for local smoothing (default: 0.8). Controls
the bandwidth - larger values produce smoother curves.}

\item{xlab}{Label for x-axis (default: 'X').}

\item{ylab}{Label for y-axis (default: 'Y').}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
This function is called for its plotting side effect. It creates a
scatter plot with overlaid quantile regression curves for both groups.
}
\description{
Plots quantile regression smoothers for two independent groups using the
Harrell-Davis estimator. Useful for comparing regression curves at a specific
quantile (e.g., median regression) across groups.
}
\details{
For each group, the function:
\enumerate{
\item Computes the qth quantile of Y values near each X value using the
Harrell-Davis estimator via \code{hd()} and \code{near()}
\item Optionally smooths the resulting curve using lowess (\code{lplot()})
\item Plots the data and fitted curves
}

Group 1 is plotted with circles and a solid line, group 2 with plus signs
and a dashed line.

If \code{xout = TRUE}, outliers are removed from the predictor space before
fitting using the function specified by \code{outfun}.
}
\note{
This function requires only one predictor variable. For multiple
predictors, see other regression methods.
}
\examples{
\dontrun{
# Compare median regression curves for two groups
set.seed(123)
n <- 100
x1 <- runif(n, 0, 10)
x2 <- runif(n, 0, 10)
y1 <- 2 + 0.5*x1 + rnorm(n)
y2 <- 3 + 0.3*x2 + rnorm(n, sd=2)
qhdsm2g(x1, y1, x2, y2, q=0.5)

# Compare 75th percentile regression
qhdsm2g(x1, y1, x2, y2, q=0.75)
}
}
\seealso{
\code{\link{qhdsm}}, \code{\link{qhdsm.pred}}, \code{\link{hd}}
}
