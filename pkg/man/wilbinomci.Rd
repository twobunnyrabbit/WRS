% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{wilbinomci}
\alias{wilbinomci}
\title{Wilson Binomial Confidence Interval}
\usage{
wilbinomci(x = sum(y), n = length(y), y = NULL, alpha = 0.05)
}
\arguments{
\item{x}{Number of successes (default: \code{sum(y)} if y provided).}

\item{n}{Number of trials (default: \code{length(y)} if y provided).}

\item{y}{Optional binary vector (0/1 or TRUE/FALSE).}

\item{alpha}{Significance level (default: 0.05).}
}
\value{
List with components:
\item{phat}{Sample proportion (x/n).}
\item{ci}{Two-element vector with lower and upper confidence limits.}
\item{n}{Sample size.}
}
\description{
Computes Wilson-type confidence interval for binomial proportion, as used by
Zou et al. (2009). Falls back to Clopper-Pearson for extreme cases.
}
\details{
The Wilson interval provides better coverage than the normal approximation,
especially for small samples or proportions near 0 or 1.

For extreme cases (x = 0, 1, n-1, or n), uses Clopper-Pearson exact method
instead of Wilson formula.
}
\references{
Zou, G. Y., et al. (2009). Confidence interval estimation for binomial
proportions and their differences.
}
\seealso{
\code{\link{binomci}}, \code{\link{binomCP}}
}
\keyword{htest}
