% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00-utils-core.R
\name{rplot}
\alias{rplot}
\title{Running Interval Smoother Plot}
\usage{
rplot(
  x,
  y,
  est = tmean,
  scat = TRUE,
  fr = NA,
  plotit = TRUE,
  pyhat = FALSE,
  efr = 0.5,
  theta = 50,
  phi = 25,
  scale = TRUE,
  expand = 0.5,
  SEED = TRUE,
  varfun = pbvar,
  outfun = outpro,
  nmin = 0,
  xout = FALSE,
  out = FALSE,
  eout = FALSE,
  xlab = "X",
  ylab = "Y",
  zscale = FALSE,
  zlab = " ",
  pr = TRUE,
  duplicate = "error",
  ticktype = "simple",
  LP = TRUE,
  OLD = FALSE,
  pch = ".",
  prm = TRUE,
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{est}{Estimator function to use for computing location or other
statistics. Common choices include \code{mean}, \code{median}, \code{tmean} (trimmed mean),
\code{mom} (modified one-step M-estimator), \code{mest} (M-estimator).}

\item{scat}{Logical. If TRUE, includes scatterplot (default: TRUE).}

\item{fr}{Fraction for smoothing window (default: 0.8 for 1D, NA otherwise).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{pyhat}{Logical. If TRUE, returns fitted values (default: FALSE).}

\item{efr}{Span for computing explanatory strength (default: 0.5).}

\item{theta, phi}{Angles for 3D plots (defaults: 50, 25).}

\item{scale, expand}{Parameters for persp plots.}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{varfun}{Variance function for strength computation (default: pbvar).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{nmin}{Minimum sample size (default: 0).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{out, eout}{Deprecated. Use xout and eout instead.}

\item{xlab, ylab, zlab}{Axis labels.}

\item{zscale}{Logical. If TRUE, standardizes y for 2D plots (default: FALSE).}

\item{pr}{Logical. If \code{TRUE}, prints progress messages (default: \code{TRUE}).}

\item{duplicate}{How to handle duplicates in 3D (default: 'error').}

\item{ticktype}{Tick type for persp (default: 'simple').}

\item{LP}{Logical. If TRUE, further smooths via lplot (default: TRUE).}

\item{OLD}{Logical. Use old strength estimate if TRUE (default: FALSE).}

\item{pch}{Plotting character (default: '.').}

\item{prm}{Logical. Print messages (default: TRUE).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
A list with fitted values and strength of association measures.
}
\description{
Creates a plot of a running interval smoother using a robust estimator,
with options for outlier removal and strength of association computation.
}
\details{
Uses running interval smoothing with a robust estimator. For 1 predictor,
creates 2D plot; for 2 predictors, creates 3D perspective plot. Supports
up to 4 predictors.
}
\examples{
x <- rnorm(50)
y <- 2*x + rnorm(50)
rplot(x, y)

}
