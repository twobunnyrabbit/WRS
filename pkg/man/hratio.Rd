% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression.R
\name{hratio}
\alias{hratio}
\title{Compute Half-Slope Ratios}
\usage{
hratio(x, y, regfun = bmreg)
}
\arguments{
\item{x}{A numeric matrix of predictor variables (n by p).}

\item{y}{A numeric vector of the dependent variable (length n).}

\item{regfun}{Regression function to use (default: \code{bmreg}). Must return
coefficients in \code{regfun$coef} with intercept as first element.}
}
\value{
A p by p matrix of half-slope ratios where:
\itemize{
\item Row i: half-slope ratios when data is split based on the i-th predictor
\item Column j: ratio of slopes for the j-th predictor
\item Element \link{i,j}: ratio of slope for predictor j in upper half vs. lower half
when split by predictor i
}
}
\description{
Computes a matrix of half-slope ratios for regression diagnostics. This
method divides the data based on each predictor and compares regression
slopes in the two halves, helping detect interactions and non-constant
regression relationships.
}
\details{
For each predictor variable:
\enumerate{
\item Order observations by that predictor
\item Split data into two halves (lower and upper)
\item Fit regression model to each half
\item Compute ratio of slopes: (upper half slope) / (lower half slope)
}

\strong{Interpretation}:
\itemize{
\item Ratio near 1: slope is similar in both halves (good)
\item Ratio far from 1: slope differs between halves, suggesting:
\itemize{
\item Non-constant relationship
\item Interaction effects
\item Possible need for transformation
}
}

The diagonal elements \link{i,i} show whether predictor i's relationship with y
changes across the range of predictor i. Off-diagonal elements \link{i,j} show
whether predictor j's relationship with y changes across the range of
predictor i.
}
\examples{
\dontrun{
# Example with constant relationship
set.seed(123)
x <- matrix(rnorm(100), ncol=2)
y <- x[,1] + 2*x[,2] + rnorm(50, sd=0.5)
hratio(x, y)  # Should have ratios near 1

# Example with interaction
y2 <- x[,1] + 2*x[,2] + 3*x[,1]*x[,2] + rnorm(50, sd=0.5)
hratio(x, y2)  # Ratios will deviate from 1
}
}
\references{
Wilcox, R.R. (2022). \emph{Introduction to Robust Estimation and Hypothesis Testing}
(5th ed.). Academic Press.
}
\seealso{
\code{\link{bmreg}}, \code{\link{reglev}}

Other robust regression diagnostics: 
\code{\link{reglev}()}
}
\concept{robust regression diagnostics}
