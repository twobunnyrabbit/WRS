% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{ritest}
\alias{ritest}
\title{Test for Regression Interaction Using Robust Smooths}
\usage{
ritest(
  x,
  y,
  adfun = adrun,
  plotfun = lplot,
  eout = FALSE,
  xout = TRUE,
  plotit = TRUE,
  flag = 3,
  nboot = 500,
  alpha = 0.05,
  tr = 0.2,
  ...
)
}
\arguments{
\item{x}{An n-by-2 matrix of predictor variables.}

\item{y}{Vector of response values.}

\item{adfun}{Function for fitting additive model (default: \code{adrun}).}

\item{plotfun}{Function for plotting residuals (default: \code{lplot}).}

\item{eout}{Logical; if \code{TRUE}, remove outliers among residuals (default: FALSE).}

\item{xout}{Logical; if \code{TRUE}, remove outliers in x space (default: TRUE).}

\item{plotit}{Logical; if \code{TRUE} (default), create plot of residuals versus x.}

\item{flag}{Test method: 1=Mantel's test, 2=parallel test, 3=both (default: 3).}

\item{nboot}{Number of bootstrap samples (default: 500).}

\item{alpha}{Nominal alpha level for the test (default: .05).}

\item{tr}{Trimming proportion for robust estimators (default: .2).}

\item{...}{Additional arguments passed to plotting function.}
}
\value{
Output from \code{\link{indt}}, typically containing test statistics and p-values
for testing independence of residuals from predictors.
}
\description{
Tests for regression interaction (departure from additivity) by fitting an additive
model and testing whether the residuals form a horizontal plane. This is one of two
methods for detecting interactions with robust smooths.
}
\details{
This function implements one of two approaches for testing regression interactions:
\enumerate{
\item This function (\code{ritest}): Fits an additive model and tests whether
residuals (given x) form a horizontal plane using \code{\link{indt}}.
\item Alternative (\code{\link{adtest}}): Tests whether a generalized additive
model fits the data.
}

The additive model is fitted using the specified \code{adfun} (typically \code{adrun}),
then residuals are computed and tested for independence/flatness. Significant results
indicate interaction effects.
}
\seealso{
\code{\link{adrun}}, \code{\link{adtest}}, \code{\link{indt}},
\code{\link{lplot}}
}
\keyword{htest}
\keyword{nonparametric}
\keyword{regression}
