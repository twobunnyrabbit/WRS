% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression-advanced.R
\name{smeancrv2}
\alias{smeancrv2}
\title{Test Hypothesis About Multivariate Skipped Mean (Version 2)}
\usage{
smeancrv2(
  m,
  nullv = rep(0, ncol(m)),
  cop = 3,
  MM = FALSE,
  SEED = TRUE,
  nboot = 500,
  plotit = TRUE,
  MC = FALSE,
  xlab = "VAR 1",
  ylab = "VAR 2",
  STAND = TRUE
)
}
\arguments{
\item{m}{An n-by-p numeric matrix containing the multivariate data.}

\item{nullv}{Numeric vector of length p specifying the null hypothesis values
(default: vector of zeros).}

\item{cop}{Integer (1-4) specifying the center for projection-based outlier detection:
\itemize{
\item 1 = Donoho-Gasko median
\item 2 = MCD (Minimum Covariance Determinant)
\item 3 = Marginal medians (default)
\item 4 = MVE (Minimum Volume Ellipsoid)
}}

\item{MM}{Logical value. If \code{TRUE}, uses the MM-estimator for robust
regression (default: \code{FALSE}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{nboot}{Number of bootstrap samples (default: 500).}

\item{plotit}{Logical. If \code{TRUE} and p=2, creates a plot showing bootstrap
distribution and confidence region (default: \code{TRUE}).}

\item{MC}{Logical. If \code{TRUE}, uses parallel processing via \code{mclapply}
(default: \code{FALSE}).}

\item{xlab, ylab}{Axis labels for bivariate plot.}

\item{STAND}{Logical. If \code{TRUE}, standardizes data before projection
(default: \code{TRUE}).}
}
\value{
A list with component:
\item{p.value}{Bootstrap p-value for testing H0: skipped mean = nullv.}
}
\description{
Tests the hypothesis that the multivariate skipped mean equals a specified
null value using bootstrap methods. This is an improved version of \code{smeancr()}
with enhanced distance computation and optional multicore support.
}
\details{
This function is an improved version of \code{smeancr()} that:
\itemize{
\item Uses \code{smean()} directly to compute skipped means (more efficient)
\item Computes projection distances centered at the estimated skipped mean
rather than at the origin
\item Supports multicore processing via \code{MC} parameter
}

The test procedure:
\enumerate{
\item Compute the skipped mean estimate for the full sample
\item For each bootstrap sample, compute the skipped mean
\item Calculate projection distances of bootstrap estimates from the null value,
centered at the sample estimate
\item p-value is the proportion of bootstrap distances less than the distance
of the null value from the estimate
}

For bivariate data (p=2) with \code{plotit=TRUE}, creates a scatterplot showing:
\itemize{
\item Bootstrap estimates of the skipped mean
\item The estimated skipped mean (marked with +)
\item Confidence region (convex hull of central bootstrap estimates)
}
}
\note{
This version does not adjust the critical level based on sample size
(unlike \code{smeancr()}), instead using a fixed 0.05 level.
}
\examples{
\dontrun{
# Test if bivariate skipped mean equals (0,0)
set.seed(123)
m <- matrix(rnorm(100), ncol=2)
m <- rbind(m, matrix(c(5,5,-5,-5), ncol=2))  # Add outliers
smeancrv2(m)

# Use multicore processing
smeancrv2(m, MC=TRUE)
}
}
\references{
Wilcox, R.R. (2022). Introduction to Robust Estimation and Hypothesis Testing
(5th ed.). Academic Press.
}
\seealso{
\code{\link{smeancr}}, \code{\link{smean}}, \code{\link{smean2v2}}
}
