% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{ODDSR.CI}
\alias{ODDSR.CI}
\title{Confidence Interval for Odds Ratio}
\usage{
ODDSR.CI(x, y = NULL, alpha = 0.05)
}
\arguments{
\item{x}{Either a 2×2 contingency table (matrix) or a binary vector (0s and 1s).}

\item{y}{Binary vector (0s and 1s). Required if \code{x} is a vector, ignored otherwise.}

\item{alpha}{Numeric. Significance level (default: 0.05 for 95\\% CI).}
}
\value{
List with components:
\describe{
\item{odds.ratio}{Numeric. Estimated odds ratio (exp of logistic regression coefficient)}
\item{ci}{Numeric vector of length 2. Confidence interval \code{c(lower, upper)}}
}
}
\description{
Computes confidence interval for the odds ratio in a 2×2 contingency table
using logistic regression.
}
\details{
\strong{Input format}:

\strong{Option 1 - Matrix}: If \code{x} is a 2×2 matrix:
\itemize{
\item Column 1: X=1, Column 2: X=0
\item Row 1: Y=1, Row 2: Y=0
}

\strong{Option 2 - Vectors}: If \code{x} and \code{y} are binary vectors of equal length

\strong{Method}:
\enumerate{
\item Fits logistic regression: \code{logit(P(Y=1)) = β₀ + β₁X}
2. Odds ratio = exp(β₁)
\item CI constructed using normal approximation: exp(β₁ ± z*SE)
}

The odds ratio quantifies the association between X and Y:
\itemize{
\item OR = 1: No association
\item OR > 1: Positive association (X=1 increases odds of Y=1)
\item OR < 1: Negative association (X=1 decreases odds of Y=1)
}
}
\seealso{
\code{\link{logreg}} for the underlying logistic regression
}
