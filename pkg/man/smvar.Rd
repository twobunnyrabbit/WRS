% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{smvar}
\alias{smvar}
\title{Estimate Conditional Variance Using Bagged Running Interval Smoother}
\usage{
smvar(
  x,
  y,
  fr = 0.6,
  xout = TRUE,
  eout = FALSE,
  xlab = "X",
  ylab = "VAR(Y|X)",
  pyhat = FALSE,
  plotit = TRUE,
  nboot = 40,
  RNA = FALSE,
  SEED = TRUE
)
}
\arguments{
\item{x}{Vector of predictor values.}

\item{y}{Vector of response values.}

\item{fr}{Span (fraction of data) for the running interval smoother. Default is 0.6.}

\item{xout}{Logical. If \code{TRUE} (default), eliminate points where x is an outlier.}

\item{eout}{Logical. If \code{TRUE}, eliminate points where (x,y) is a bivariate outlier.
Default is \code{FALSE}.}

\item{xlab}{Label for x-axis. Default is "X".}

\item{ylab}{Label for y-axis. Default is "VAR(Y|X)".}

\item{pyhat}{Logical. If \code{TRUE}, return variance estimates for each x value.
If \code{FALSE} (default), return "Done" message.}

\item{plotit}{Logical. If \code{TRUE} (default), create scatterplot of squared residuals
with smooth curve showing estimated conditional variance.}

\item{nboot}{Number of bootstrap samples for bagging. Default is 40.}

\item{RNA}{Logical. If \code{TRUE}, remove missing values when applying smooth.
If \code{FALSE} (default), may return NA for some predicted values.}

\item{SEED}{Logical. If \code{TRUE} (default), set random seed for reproducibility.}
}
\value{
If \code{pyhat=TRUE}, returns vector of estimated conditional variance values
at each x point. If \code{pyhat=FALSE}, returns character string "Done".
}
\description{
Estimates VAR(Y|X), the conditional variance of Y given X, using a bagged
(bootstrap aggregated) running interval smoother. Provides robust estimation
of heteroscedasticity patterns.
}
\details{
The function:
\enumerate{
\item Fits LOESS smooth to estimate E(Y|X)
\item Computes squared residuals (Y - Yhat)^2
\item Uses bagged running interval smoother (\code{\link{runmbo}}) to estimate variance
\item Optionally plots squared residuals with smooth variance curve
}

This is useful for detecting and modeling heteroscedasticity in regression relationships.
}
\seealso{
\code{\link{runmbo}}, \code{\link{lplot}}, \code{\link{runmean}}
}
\keyword{regression}
\keyword{smooth}
