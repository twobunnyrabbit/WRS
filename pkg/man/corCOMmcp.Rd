% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{corCOMmcp}
\alias{corCOMmcp}
\title{Multiple Comparison Procedure for Dependent Correlations}
\usage{
corCOMmcp(
  x,
  y,
  corfun = wincor,
  alpha = 0.05,
  nboot = 500,
  SEED = TRUE,
  MC = FALSE,
  xout = FALSE,
  outfun = outpro,
  method = "hommel",
  ...
)
}
\arguments{
\item{x}{Numeric matrix. Independent variables (columns are variables).}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{corfun}{Correlation function to use. Common options: \code{pbcor} (percentage
bend), \code{pcor} (Pearson), \code{wincor} (winsorized), \code{spear} (Spearman).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{MC}{Logical. If \code{TRUE}, uses parallel processing via \code{mclapply}
(default: \code{FALSE}).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{method}{Multiple comparison method (default: 'hommel'). Options include
'hochberg', 'hommel', 'BH' (Benjamini-Hochberg), 'BY' (Benjamini-Yekutieli),
etc. See \code{p.adjust()}.}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
List with components:
\item{estimates}{Vector of correlation estimates cor(y, x_j) for each j.}
\item{test.stat}{Matrix of test statistics for all pairwise comparisons.}
\item{p.values}{Matrix of unadjusted p-values.}
\item{adjusted.p}{Matrix of adjusted p-values (FWE controlled).}
\item{num.sig}{Number of significant pairwise differences.}
\item{method}{Multiple comparison method used.}
}
\description{
Compares multiple dependent correlations with familywise error rate control.
Tests all pairwise differences cor(y, x_j) vs cor(y, x_k) for j < k.
}
\details{
This function addresses the overlapping correlations problem: comparing
correlations that share a common variable (y). For example, comparing
cor(IQ, Math) vs cor(IQ, Reading) where IQ is the shared variable.

Uses bootstrap to account for dependence among the correlations. The default
is winsorized correlation, but any correlation function can be used via
\code{corfun}.

Hommel's method (default) controls the familywise error rate and is generally
more powerful than Bonferroni for multiple comparisons.
}
\examples{
\dontrun{
# Compare correlations of DV with multiple IVs
set.seed(123)
x <- matrix(rnorm(50 * 4), ncol = 4)  # 4 independent variables
y <- rnorm(50)  # Dependent variable
result <- corCOMmcp(x, y, nboot = 500)
result$estimates  # Correlations
result$num.sig    # Number of significant differences
}
}
\seealso{
\code{\link{corCOM.DVvsIV}}, \code{\link{wincor}}, \code{\link{twocor}}
}
