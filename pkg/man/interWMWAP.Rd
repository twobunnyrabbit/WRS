% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{interWMWAP}
\alias{interWMWAP}
\title{WMW Interaction Test for 2-by-2 Design (Adaptive Percentile Bootstrap)}
\usage{
interWMWAP(
  x,
  nreps = 100,
  SEED = TRUE,
  nboot = 500,
  alpha = 0.05,
  nmax = 10^8,
  MC = TRUE
)
}
\arguments{
\item{x}{Data in matrix or list format with 4 groups.}

\item{nreps}{Number of replications for adaptive method (default: 100).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed (default: \code{TRUE}).}

\item{nboot}{Number of bootstrap samples (default: 500).}

\item{alpha}{Significance level (default: 0.05).}

\item{nmax}{Maximum sample size product (default: 10^8).}

\item{MC}{Logical. If \code{TRUE}, uses parallel processing (default: \code{TRUE}).}
}
\value{
Test results including effect size estimate and confidence interval.
}
\description{
Tests for interaction in a 2-by-2 factorial design using a WMW-type measure
with adaptive percentile bootstrap. Better than \code{WMWinterci} for
small unequal sample sizes with heteroscedasticity.
}
\details{
Tests interaction using P(X1-X2 < X3-X4). The adaptive percentile bootstrap
provides better Type I error control than \code{WMWinterci} when sample
sizes are unequal and heteroscedasticity is present.
}
\examples{
\dontrun{
# 2x2 factorial design
set.seed(123)
data <- list(rnorm(15), rnorm(20, 1), rnorm(25, 1), rnorm(18, 3))
interWMWAP(data, nboot = 500)
}
}
\seealso{
\code{\link{WMWinterci}}, \code{\link{WMWinter.est}}, \code{\link{linWMWpb}}
}
