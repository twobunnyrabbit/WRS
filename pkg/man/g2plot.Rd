% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{g2plot}
\alias{g2plot}
\title{Plot Density Functions for Two Groups}
\usage{
g2plot(x1, x2, op = 4, rval = 15, fr = 0.8, aval = 0.5, xlab = "X", ylab = "")
}
\arguments{
\item{x1}{Data for group 1 (vector).}

\item{x2}{Data for group 2 (vector).}

\item{op}{Option for density estimation method (default: 4):
\itemize{
\item 1: Rosenblatt shifted histogram
\item 2: Kernel density estimate (Epanechnikov kernel)
\item 3: Expected frequency curve
\item 4: Adaptive kernel estimator (recommended)
}}

\item{rval}{Number of points for density estimation when op=1 (default: 15).}

\item{fr}{Span parameter for expected frequency curve when op=3 (default: 0.8).}

\item{aval}{Scaling parameter for adaptive kernel estimator when op=4 (default: 0.5).}

\item{xlab}{Label for X-axis (default: "X").}

\item{ylab}{Label for Y-axis (default: "").}
}
\value{
None. Creates a plot.
}
\description{
Creates overlaid density plots for two independent groups, allowing visual
comparison of distributions. Multiple estimation methods are available.
}
\details{
This function compares the distributions of two groups by plotting density estimates.

\strong{Density estimation methods}:
\itemize{
\item \strong{op=1}: Rosenblatt shifted histogram using kernel density estimation.
\item \strong{op=2}: Standard kernel density estimate with Epanechnikov kernel.
\item \strong{op=3}: Expected frequency curve using running interval smoother (calls \code{rd2plot}).
If graph is ragged, suggests using op=4.
\item \strong{op=4}: Adaptive kernel estimator (recommended). Uses \code{akerd} for smoother
density estimates that adapt to local data density.
}

\strong{Plot appearance}:
\itemize{
\item Group 1: solid line (lty=1)
\item Group 2: dashed line (lty=2)
}

Missing values are automatically removed.
}
\examples{
# Compare two normal distributions
set.seed(123)
x1 <- rnorm(100, mean = 0, sd = 1)
x2 <- rnorm(100, mean = 1, sd = 1.5)
g2plot(x1, x2)

# Try different estimation methods
g2plot(x1, x2, op = 2)  # Standard kernel density
g2plot(x1, x2, op = 3)  # Expected frequency curve
}
\seealso{
\code{\link{akerd}}, \code{\link{rd2plot}}, \code{\link{g5plot}}
}
