% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{pro.class}
\alias{pro.class}
\title{Projection-Based Classification}
\usage{
pro.class(
  train = NULL,
  test = NULL,
  g = NULL,
  x1 = NULL,
  x2 = NULL,
  nonpar = TRUE,
  rule = 0.5,
  fr = 2,
  SEED = TRUE
)
}
\arguments{
\item{train}{Numeric matrix of training data, or \code{NULL} if \code{x1} and \code{x2} provided.}

\item{test}{Numeric matrix of test data to classify.}

\item{g}{Numeric vector of group labels (0 or 1) for training data.}

\item{x1}{Numeric matrix for group 1 training data (alternative to \code{train} + \code{g}).}

\item{x2}{Numeric matrix for group 2 training data (alternative to \code{train} + \code{g}).}

\item{nonpar}{Logical. If \code{TRUE}, use smoother; if \code{FALSE}, use logistic regression
(default: \code{TRUE}).}

\item{rule}{Numeric. Classification threshold for probability (default: 0.5).
Classify as group 1 if P(group 1) < rule.}

\item{fr}{Numeric. Span parameter for smoother when \code{nonpar=TRUE} (default: 2).}

\item{SEED}{Logical. Set random seed (default: \code{TRUE}).}
}
\value{
Numeric vector of classifications (1 or 2) for test observations.
}
\description{
Projects multivariate data onto a line using robust distance measures, then classifies
test observations based on estimated probability of group membership. Uses either
nonparametric smoothing or logistic regression.
}
\details{
Projects data onto a line using \code{\link{mulwmw.dist.new}} based on robust distances.
When \code{nonpar=TRUE}, estimates probabilities via \code{\link{logSMpred}} smoother.
When \code{FALSE}, uses \code{\link{logreg.pred}} for logistic regression.

\strong{Note}: Impacted by unequal sample sizes. Use \code{\link{pro.class.bag}} or ensure
equal-sized test groups to avoid bias.
}
\examples{
\dontrun{
# Using separate group matrices
result <- pro.class(x1 = group1_data, x2 = group2_data, test = test_data)

# Using combined training data with labels
result2 <- pro.class(train = all_train, g = labels, test = test_data)
}

}
\seealso{
\code{\link{pro.class.probs}}, \code{\link{pro.classPD}}, \code{\link{pro.class.bag}},
\code{\link{mulwmw.dist.new}}
}
\keyword{classif}
\keyword{multivariate}
