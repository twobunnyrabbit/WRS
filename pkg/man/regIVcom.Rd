% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression-advanced.R
\name{regIVcom}
\alias{regIVcom}
\title{Compare Strength of Association for Two Predictor Subsets}
\usage{
regIVcom(
  x,
  y,
  IV1 = 1,
  IV2 = 2,
  regfun = tsreg,
  nboot = 200,
  xout = FALSE,
  outfun = outpro,
  SEED = TRUE,
  MC = FALSE,
  tr = 0.2,
  ...
)
}
\arguments{
\item{x}{A numeric matrix containing predictor values.}

\item{y}{A numeric vector containing the response variable.}

\item{IV1}{Vector of column indices in \code{x} indicating the first subset of
predictors to compare (default: 1, the first predictor).}

\item{IV2}{Vector of column indices in \code{x} indicating the second subset of
predictors to compare (default: 2, the second predictor).}

\item{regfun}{Regression function to use (default: \code{tsreg}). Using \code{Qreg}
reduces execution time but may reduce power.}

\item{nboot}{Number of bootstrap samples (default: 200).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{MC}{Logical. If \code{TRUE}, uses parallel processing via \code{mclapply}
(default: \code{FALSE}).}

\item{tr}{Trimming proportion for Winsorized variance (default: 0.2). Controls
the amount of Winsorizing when computing explanatory power.}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
A list with components:
\item{n}{Original sample size}
\item{n.keep}{Sample size after outlier removal (if \code{xout = TRUE})}
\item{est.1}{Estimated strength measure for predictor subset 1 (Winsorized
variance of fitted values)}
\item{est.2}{Estimated strength measure for predictor subset 2}
\item{e.pow1}{Explanatory power for subset 1 (proportion of Y variance explained)}
\item{e.pow2}{Explanatory power for subset 2}
\item{strength.assoc.1}{Strength of association for subset 1 (sqrt of explanatory power)}
\item{strength.assoc.2}{Strength of association for subset 2}
\item{ratio}{Ratio of strength measures (est.1 / est.2)}
\item{strength.ratio}{Ratio of association strengths (sqrt of ratio)}
\item{p.value}{Bootstrap p-value for testing H0: equal strength}
}
\description{
Compares the strength of association (explanatory power) for two subsets of
independent variables in a regression model using bootstrap inference.
}
\details{
This function tests whether two subsets of predictors have equal strength of
association with the response:
\enumerate{
\item Fits regression using all predictors
\item For each bootstrap sample, computes strength measures for both subsets
based on Winsorized variance of fitted values
\item Compares bootstrap distributions to test H0: equal strength
}

The subsets IV1 and IV2 must be disjoint (no overlap). For example, \code{IV1=c(1,2)}
and \code{IV2=3} compares the first two predictors jointly to the third predictor.

If \code{xout = TRUE}, outliers are removed from the predictor space before
analysis using the function specified by \code{outfun}.
}
\note{
The subsets IV1 and IV2 must not have duplicate values and their combined
length cannot exceed the number of columns in \code{x}.
}
\examples{
\dontrun{
# Compare first predictor to second predictor
set.seed(123)
x <- matrix(rnorm(300), ncol=3)
y <- 2 + 3*x[,1] - 0.5*x[,2] + 0.1*x[,3] + rnorm(100)

# Test if first predictor has stronger association than second
regIVcom(x, y, IV1=1, IV2=2)

# Compare first two predictors jointly to third
regIVcom(x, y, IV1=c(1,2), IV2=3)
}
}
\references{
Wilcox, R.R. (2022). Introduction to Robust Estimation and Hypothesis Testing
(5th ed.). Academic Press.
}
\seealso{
\code{\link{regIVstr}}, \code{\link{regIVcommcp}}, \code{\link{tsreg}}
}
