% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{mcppb20}
\alias{mcppb20}
\title{Multiple Comparisons via Percentile Bootstrap (Rom's Method)}
\usage{
mcppb20(
  x,
  crit = NA,
  con = 0,
  tr = 0.2,
  alpha = 0.05,
  nboot = 2000,
  grp = NA,
  WIN = FALSE,
  win = 0.1
)
}
\arguments{
\item{x}{Data in list mode (each element is a group) or matrix (columns are groups).}

\item{crit}{Critical value for Rom's method (default: \code{NA}, computed automatically for tr=0.2).}

\item{con}{Matrix of contrast coefficients (rows = groups, columns = contrasts).
If 0, performs all pairwise comparisons (default: 0).}

\item{tr}{Trimming proportion (default: 0.2).}

\item{alpha}{Significance level (default: 0.05).}

\item{nboot}{Number of bootstrap samples (default: 2000).}

\item{grp}{Vector specifying which groups to analyze (default: \code{NA}, uses all groups).}

\item{WIN}{Logical. If \code{TRUE}, uses Winsorized means instead of trimmed means (default: \code{FALSE}).}

\item{win}{Winsorizing proportion when \code{WIN=TRUE} (default: 0.1).}
}
\value{
A list with components:
\itemize{
\item \code{num.sig}: Number of significant contrasts
\item \code{con}: Matrix of contrast coefficients used
\item \code{psihat}: Vector of contrast estimates
\item \code{test}: Test statistics
\item \code{crit.val}: Critical value(s) used
\item \code{p.value}: P-values for each contrast
\item Various other components depending on contrast type
}
}
\description{
Computes confidence intervals for linear contrasts of trimmed means across
multiple independent groups using the percentile bootstrap method. Uses
Rom's (1990) method to control the familywise error rate.
}
\details{
This function performs multiple comparisons of trimmed means using Rom's (1990)
sequentially rejective procedure, which controls the familywise Type I error rate.
The percentile bootstrap is used to obtain p-values and confidence intervals.

By default, all pairwise comparisons are performed. Custom linear contrasts can
be specified via the \code{con} matrix, where each column represents one contrast.

When \code{WIN=TRUE}, Winsorized means are used instead of trimmed means. In this
case, the trimming proportion should typically be at least 0.2.
}
\examples{
\dontrun{
# Compare four groups
set.seed(123)
x <- list(
  rnorm(30, mean = 0),
  rnorm(30, mean = 0.5),
  rnorm(30, mean = 0.3),
  rnorm(30, mean = 0.8)
)

# All pairwise comparisons
result <- mcppb20(x, nboot = 1000)
print(result)
}
}
\references{
Rom, D. M. (1990). A sequentially rejective test procedure based on a modified
Bonferroni inequality. Biometrika, 77, 663-665.
}
\seealso{
\code{\link{lincon}}, \code{\link{pb2gen}}, \code{\link{tmean}}
}
