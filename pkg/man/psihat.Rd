% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{psihat}
\alias{psihat}
\title{Estimate Linear Contrasts}
\usage{
psihat(x, est = hd, con, ...)
}
\arguments{
\item{x}{Data in list, matrix, or data frame format.}

\item{est}{Function for estimating location (default: \code{\link{hd}}).}

\item{con}{Numeric matrix of contrasts. Each column defines one contrast.}

\item{...}{Additional arguments passed to \code{est}.}
}
\value{
Numeric vector of contrast estimates, one per column of \code{con}.
}
\description{
Computes estimates of linear contrasts using a user-specified estimator.
Applies the estimator to each group, then computes contrasts.
}
\details{
For each group in \code{x}, applies \code{est} to obtain a location estimate.
Then computes linear combinations of these estimates as specified by columns
of the contrast matrix \code{con}.
}
\examples{
\dontrun{
# Three groups, two contrasts
data_list <- list(group1 = rnorm(20), group2 = rnorm(20), group3 = rnorm(20))
con_matrix <- matrix(c(1, -1, 0,   # Group 1 vs Group 2
                       1, 0, -1),  # Group 1 vs Group 3
                     nrow = 3, ncol = 2)
psihat(data_list, est = mean, con = con_matrix)
}

}
\seealso{
\code{\link{hd}}, \code{\link{tmean}}, \code{\link{lincon}}
}
\keyword{htest}
