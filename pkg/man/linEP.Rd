% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{linEP}
\alias{linEP}
\title{Explanatory Power for Linear Contrast}
\usage{
linEP(x, con, locfun = tmean, tr = 0.2, nreps = 200, SEED = TRUE)
}
\arguments{
\item{x}{Data in matrix or list format. For matrix: each column is a group.
For list: x[\link{j}] contains data for group j.}

\item{con}{Numeric vector of contrast coefficients (length J). Must sum to zero.}

\item{locfun}{Location estimator function (default: \code{\link{tmean}}).}

\item{tr}{Trimming proportion for \code{tmean} (default: 0.2).}

\item{nreps}{Number of resampling iterations (default: 200).}

\item{SEED}{Logical. If TRUE (default), sets random seed to 2 for reproducibility.}
}
\value{
List with component:
\describe{
\item{Effect.Size}{Average explanatory power across resampling iterations}
}
}
\description{
Estimates explanatory power (effect size) for a linear contrast of group means.
Uses resampling to estimate the distribution of the contrast and computes
effect size via \code{\link{yuenv2}}.
}
\details{
For main effects, could pool data and use \code{\link{yuenv2}} directly.
This function instead estimates the distribution of the linear contrast,
which is necessary for interactions and provides a unified approach.

Process:
\itemize{
\item Resamples equal-sized groups from each condition
\item Computes linear contrast for each resampled dataset
\item If 2 groups: uses yuenv2 directly
\item If >2 groups: partitions contrast into positive/negative coefficients,
then computes effect size between the two sides
\item Averages effect sizes across resampling iterations
}
}
\examples{
# Three-group comparison
set.seed(123)
x <- list(rnorm(30), rnorm(30, mean = 0.3), rnorm(30, mean = 0.6))

# Linear trend contrast
con <- c(-1, 0, 1)
result <- linEP(x, con, nreps = 100)
result$Effect.Size
}
\seealso{
\code{\link{yuenv2}}, \code{\link{linWMWMC.sub}}, \code{\link{linQS}}
}
