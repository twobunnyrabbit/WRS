% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression-advanced.R
\name{qhdplotsm}
\alias{qhdplotsm}
\title{Plot Smoothed Quantile Regression Lines}
\usage{
qhdplotsm(
  x,
  y,
  q = 0.5,
  xlab = "X",
  ylab = "Y",
  pc = ".",
  xout = FALSE,
  outfun = out,
  nboot = 40,
  fr = 1,
  ...
)
}
\arguments{
\item{x}{A numeric vector containing the single predictor variable.}

\item{y}{A numeric vector containing the response variable.}

\item{q}{Numeric vector of quantiles to plot (default: 0.5 for median).
Can specify multiple quantiles, e.g., \code{c(0.25, 0.5, 0.75)}.}

\item{xlab, ylab}{Axis labels for the plot.}

\item{pc}{Plotting character for data points (default: ".").}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{nboot}{Number of bootstrap samples for smoothing (default: 40).}

\item{fr}{Span parameter for smoothing, between 0 and 1 (default: 1).
Controls bandwidth of the smoother.}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
Invisibly returns NULL. Creates a plot as a side effect.
}
\description{
Plots one or more smoothed quantile regression lines using the Harrell-Davis
estimator via \code{rplotsm()}. Useful for visualizing conditional quantiles as
smooth functions of a predictor.
}
\details{
This function visualizes conditional quantiles:
\enumerate{
\item Plots the scatterplot of (x, y)
\item For each quantile in \code{q}:
\itemize{
\item Computes quantile regression using Harrell-Davis estimator
\item Smooths the fitted values using running-interval smoother
\item Overlays the smooth curve on the plot
}
}

Multiple quantiles can be plotted simultaneously to show the conditional
distribution of Y given X. Common choices:
\itemize{
\item \code{q = 0.5}: Median regression line
\item \code{q = c(0.25, 0.5, 0.75)}: Quartile regression lines
\item \code{q = c(0.1, 0.5, 0.9)}: Shows variability and skewness
}

The \code{fr} parameter controls smoothness: smaller values give more local
smoothing, larger values give more global smoothing.

Only one predictor is allowed. For multiple predictors, use \code{qhdsm()} or
\code{gamplot()} instead.
}
\examples{
\dontrun{
# Plot median regression line
set.seed(123)
x <- rnorm(100)
y <- 2*x + rnorm(100)
qhdplotsm(x, y, q = 0.5)

# Plot quartile regression lines
qhdplotsm(x, y, q = c(0.25, 0.5, 0.75))
}
}
\references{
Wilcox, R.R. (2022). Introduction to Robust Estimation and Hypothesis Testing
(5th ed.). Academic Press.
}
\seealso{
\code{\link{qhdsm}}, \code{\link{rplotsm}}, \code{\link{hd}},
\code{\link{qreg}}
}
