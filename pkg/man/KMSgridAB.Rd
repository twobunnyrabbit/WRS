% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{KMSgridAB}
\alias{KMSgridAB}
\title{Grid-Based Analysis: Test Main Effects Across Quantile Grids}
\usage{
KMSgridAB(
  x,
  y,
  IV = c(1, 2),
  Qsplit1 = 0.5,
  Qsplit2 = 0.5,
  tr = 0.2,
  VAL1 = NULL,
  VAL2 = NULL,
  PB = FALSE,
  est = tmean,
  nboot = 1000,
  pr = TRUE,
  fun = ES.summary,
  xout = FALSE,
  outfun = outpro,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{x}{Matrix or data frame containing independent variables.}

\item{y}{Numeric vector containing the dependent variable.}

\item{IV}{Integer vector of length 2 specifying columns in \code{x} to use as IVs (default: c(1,2)).}

\item{Qsplit1}{Numeric vector of quantiles for splitting first IV (default: 0.5 for median split).}

\item{Qsplit2}{Numeric vector of quantiles for splitting second IV (default: 0.5 for median split).}

\item{tr}{Numeric. Trimming proportion for robust estimation (default: 0.2).}

\item{VAL1}{Optional numeric vector of specific values for splitting first IV (overrides \code{Qsplit1}).}

\item{VAL2}{Optional numeric vector of specific values for splitting second IV (overrides \code{Qsplit2}).}

\item{PB}{Logical. If TRUE, uses percentile bootstrap; if FALSE, uses parametric methods (default: FALSE).}

\item{est}{Estimator function for location (default: \code{tmean}).}

\item{nboot}{Integer. Number of bootstrap samples (default: 1000).}

\item{pr}{Logical. If TRUE, prints detailed results (default: TRUE).}

\item{fun}{Function for computing effect size summaries (default: \code{ES.summary}).}

\item{xout}{Logical. If TRUE, removes outliers from IVs before analysis (default: FALSE).}

\item{outfun}{Function for outlier detection (default: \code{outpro}).}

\item{SEED}{Logical. If TRUE, sets random seed for reproducibility (default: TRUE).}

\item{...}{Additional arguments passed to estimator and outlier functions.}
}
\value{
Object containing test results for main effects across the grid, with effect size
summaries and p-values.
}
\description{
Splits data based on quantiles of two independent variables and tests hypotheses
of equal location measures across the resulting grid cells. Designed for exploring
main effects in a flexible grid-based framework.
}
\details{
Creates a quantile-based grid analysis:
\enumerate{
\item Splits data into bins based on quantiles of both IVs
\item Tests hypothesis of equal location measures across grid cells
\item Can use either quantile splits (Qsplit1/2) or specific values (VAL1/2)
\item Supports both parametric and bootstrap inference
}

Unlike \code{\link{KMSgrid.mcp}} which focuses on KMS effect sizes, this function
tests location differences directly.
}
\examples{
# 2x2 grid analysis
set.seed(123)
x <- matrix(rnorm(200), ncol = 2)
y <- rnorm(100)
result <- KMSgridAB(x, y, Qsplit1 = .5, Qsplit2 = .5)
}
\seealso{
\code{\link{KMSgrid.mcp}} for KMS-based grid analysis,
\code{\link{KMSgridAV}} for grid analysis with ANOVA,
\code{\link{smgrid.GLOB}} for smoothing-based approach
}
