% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{wmw.QC.mul}
\alias{wmw.QC.mul}
\title{Test Symmetry at Multiple Quantile-Based Reference Points}
\usage{
wmw.QC.mul(
  x,
  y,
  q = seq(0.1, 0.4, 0.1),
  estfun = hdmq,
  alpha = 0.05,
  nboot = 1000,
  SEED = TRUE,
  method = "BH",
  plotit = FALSE,
  xlab = "Difference",
  ylab = "Density",
  plotfun = kerSORT
)
}
\arguments{
\item{x}{Observations from group 1.}

\item{y}{Observations from group 2.}

\item{q}{Vector of quantiles less than 0.5 (default: seq(0.1, 0.4, 0.1)).}

\item{estfun}{Quantile estimator (default: \code{hdmq} for Harrell-Davis).}

\item{alpha}{Significance level (default: 0.05).}

\item{nboot}{Number of bootstrap samples (default: 1000).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed (default: \code{TRUE}).}

\item{method}{Multiple testing adjustment method (default: 'BH').}

\item{plotit}{Logical. If \code{TRUE}, plots distribution (default: \code{FALSE}).}

\item{xlab}{X-axis label (default: 'Difference').}

\item{ylab}{Y-axis label (default: 'Density').}

\item{plotfun}{Plotting function (default: \code{kerSORT}).}
}
\value{
Matrix with test results at each quantile including adjusted p-values.
}
\description{
Tests symmetry of X-Y distribution at multiple quantile-based reference
points. Similar to \code{wmw.ref.mul} but specifically for quantile-based testing.
}
\details{
For each quantile q < 0.5, tests whether the q and 1-q quantiles of X-Y
sum to zero (as they should under symmetry). Uses bootstrap with
multiple comparison adjustment.
}
\examples{
\dontrun{
# Test at multiple quantiles
set.seed(123)
x <- rnorm(50)
y <- rnorm(50, mean = 0.2)
wmw.QC.mul(x, y, q = c(0.1, 0.25), nboot = 500)
}
}
\seealso{
\code{\link{wmw.ref.mul}}, \code{\link{wmw.ref.dif}}
}
