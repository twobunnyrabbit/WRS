% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{logSMpred}
\alias{logSMpred}
\title{Predict Probabilities Using Logistic Smoother for Binary Outcomes}
\usage{
logSMpred(
  x,
  y,
  pts = NULL,
  fr = 2,
  LP = TRUE,
  xout = FALSE,
  outfun = outpro,
  ...
)
}
\arguments{
\item{x}{Predictor variable(s), vector or matrix}

\item{y}{Binary outcome variable}

\item{pts}{Design points at which to compute predictions; if NULL, uses \code{x} (default: NULL)}

\item{fr}{Span parameter controlling smoothing bandwidth (default: 2)}

\item{LP}{Logical; if TRUE and p=2, applies LOESS to smooth initial predictions (default: TRUE)}

\item{xout}{Logical; if TRUE removes outliers before smoothing (default: FALSE)}

\item{outfun}{Function for outlier detection (default: outpro)}

\item{...}{Additional arguments passed to \code{outfun}}
}
\value{
Vector of predicted probabilities for each point in \code{pts}
}
\description{
Computes smoothed predicted probabilities for binary outcomes using a
kernel-weighted logistic smoother based on Mahalanobis distance.
}
\details{
Specialized smoother for binary outcomes that:
\itemize{
\item Uses Mahalanobis distance from MVE covariance estimate
\item Applies kernel weighting based on distance: \eqn{w = exp(-d)}
\item Computes local weighted average of binary responses
\item Automatically converts binary outcomes to 0/1 coding
}

Points within Mahalanobis distance \code{fr} receive non-zero weights.
}
\seealso{
\code{\link{logSM}}, \code{\link{logSM2g}}
}
