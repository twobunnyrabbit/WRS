% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{acbinomci}
\alias{acbinomci}
\title{Agresti-Coull Confidence Interval for Binomial Probability}
\usage{
acbinomci(x = sum(y), nn = length(y), y = NULL, n = NA, alpha = 0.05)
}
\arguments{
\item{x}{Number of successes. Default uses \code{sum(y)}}

\item{nn}{Sample size. Default uses \code{length(y)}}

\item{y}{Optional vector of 1s (successes) and 0s (failures). If provided, \code{x}
and \code{nn} are computed from \code{y}}

\item{n}{Deprecated. Use \code{nn} instead}

\item{alpha}{Type I error rate (default: 0.05 for 95\\% confidence interval)}
}
\value{
List with components:
\describe{
\item{\code{phat}}{Estimated probability of success (x/n)}
\item{\code{se}}{Standard error of the estimate}
\item{\code{ci}}{Two-element vector containing lower and upper confidence limits}
\item{\code{n}}{Sample size used}
}
}
\description{
Computes a confidence interval for the probability of success (p) in a binomial
distribution using the Agresti-Coull method as studied by Brown, Cai, and DasGupta
(2002, Annals of Statistics, 30, 160-201).
}
\details{
The Agresti-Coull method adds \eqn{z^2} successes and failures to the data before
computing the Wald interval, where \eqn{z = z_{1-\alpha/2}}. This provides better
coverage properties than the standard Wald interval, especially for extreme values
of p or small sample sizes.

Special cases (Clopper-Pearson method used):
\itemize{
\item When \code{x = 0} or \code{x = n}: Exact binomial intervals
\item When \code{x = 1} or \code{x = n-1}: Modified exact intervals
}
}
\references{
Brown, L. D., Cai, T. T., & DasGupta, A. (2002). Interval estimation for a binomial
proportion. Statistical Science, 16(2), 101-133.
}
\seealso{
\code{\link{acbinomciv2}}, \code{\link{binomci}}, \code{\link{binomCP}}
}
\keyword{htest}
