% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{lressp}
\alias{lressp}
\title{Plot ESSP (Estimated Sufficient Summary Plot) for Logistic Regression}
\usage{
lressp(x, y, slices = 10)
}
\arguments{
\item{x}{n × 5 matrix of predictor variables.}

\item{y}{Vector of binary responses (0 or 1).}

\item{slices}{Integer. Number of slices for the sliced mean smoother (default: 10).}
}
\value{
NULL (invisibly). The function produces a plot as a side effect.
}
\description{
Creates an Estimated Sufficient Summary Plot (ESSP) for logistic regression,
showing the relationship between the estimated sufficient predictor (linear
combination of predictors) and the binary response. Includes both the fitted
logistic curve and a sliced mean smoother.
}
\details{
The function:
\enumerate{
\item Fits logistic regression using \code{glm} with all 5 predictors
\item Computes ESP = X*beta + alpha (estimated sufficient predictor)
\item Creates scatter plot of ESP vs. Y
\item Overlays horizontal line at mean(Y)
\item Adds fitted logistic curve: exp(ESP)/(1 + exp(ESP))
\item Adds sliced mean smoother by dividing ESP into slices and computing
mean Y within each slice
}

The ESSP allows visual assessment of how well a single linear combination of
predictors captures the relationship with the binary response.
}
\note{
\itemize{
\item Currently hardcoded for 5 predictors. If different number needed,
must modify GLM formula in source code.
\item Graphics device must be activated before calling (use \code{X11()} or similar)
}
}
\references{
Li, K.C. (1991). Sliced inverse regression for dimension reduction.
Journal of the American Statistical Association, 86, 316-327.
}
\keyword{hplot}
\keyword{regression}
