% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{rslopesm}
\alias{rslopesm}
\title{Smoothed Running Slope with Bagging}
\usage{
rslopesm(
  x,
  y,
  fr = 1,
  est = tmean,
  nmin = 10,
  pts = x,
  plotit = FALSE,
  xlab = "X",
  ylab = "Y",
  SEED = TRUE,
  nboot = 40,
  xout = FALSE,
  RNA = TRUE,
  atr = 0.2,
  scat = TRUE,
  pyhat = TRUE,
  ...
)
}
\arguments{
\item{x}{Numeric vector of predictor values.}

\item{y}{Numeric vector of response values (same length as \code{x}).}

\item{fr}{Numeric. Span parameter for \code{\link{rslope}} (default: 1).}

\item{est}{Function for location estimation in \code{\link{rslope}}. Default
is \code{\link{tmean}}.}

\item{nmin}{Integer. Minimum observations per window in \code{\link{rslope}}
(default: 10).}

\item{pts}{Numeric vector of points at which to estimate slope (default: \code{x}).}

\item{plotit}{Logical. If \code{TRUE}, plot smoothed slope estimates (default: \code{FALSE}).}

\item{xlab}{Character. Label for x-axis when \code{plotit=TRUE}.}

\item{ylab}{Character. Label for y-axis when \code{plotit=TRUE}.}

\item{SEED}{Logical. If \code{TRUE} (default), set random seed for reproducibility.}

\item{nboot}{Integer. Number of bootstrap samples for bagging (default: 40).}

\item{xout}{Logical. If \code{TRUE}, remove outliers before estimation (default: \code{FALSE}).}

\item{RNA}{Logical. If \code{TRUE} (default), remove NAs when averaging bootstrap slopes.}

\item{atr}{Numeric. Amount of trimming when averaging bootstrap slopes (default: 0.2).}

\item{scat}{Logical. Unused parameter (retained for compatibility).}

\item{pyhat}{Logical. If \code{TRUE} (default), return smoothed slope values;
if \code{FALSE}, return character string "Done".}

\item{...}{Additional arguments passed to \code{est} in \code{\link{rslope}}.}
}
\value{
If \code{pyhat=TRUE}, numeric vector of smoothed slope estimates.
If \code{pyhat=FALSE}, character string "Done".
}
\description{
Estimates local slope using bagging (bootstrap aggregation) followed by smoothing.
Provides more stable slope estimates than \code{\link{rslope}} alone.
}
\details{
This function combines bagging and smoothing to estimate local slopes:
\enumerate{
\item Generate \code{nboot} bootstrap samples
\item For each sample, compute slope estimates using \code{\link{rslope}}
\item Average slopes across bootstrap samples using trimmed mean
\item Smooth averaged slopes using \code{\link{lplot}}
}

Bagging reduces variability in slope estimates, especially when sample sizes
are small or data are noisy.
}
\seealso{
\code{\link{rslope}}, \code{\link{runhat}}, \code{\link{lplot}}
}
\keyword{regression}
\keyword{robust}
\keyword{smooth}
