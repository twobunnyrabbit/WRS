% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{kerden}
\alias{kerden}
\title{Kernel Density Estimate at Quantile}
\usage{
kerden(x, q = 0.5, xval = 0)
}
\arguments{
\item{x}{Numeric vector. Data for density estimation.}

\item{q}{Numeric. Quantile at which to evaluate density (default: 0.5 for median).
Set to 0 to evaluate at a specific value instead.}

\item{xval}{Numeric. If \code{q=0}, evaluates density at this value (default: 0).}
}
\value{
Numeric. Estimated density value at the specified quantile or value.
}
\description{
Computes the kernel density estimator of the probability density function
evaluated at a specified quantile (or at a specified value).
}
\details{
Uses a kernel density estimator with bandwidth:
\deqn{h = 1.2 \times IQR / n^{0.2}}

where IQR is based on ideal fourths. The density estimate at point x is:
\deqn{\hat{f}(x) = \frac{|\{y_i : x - h < y_i \leq x + h\}|}{2nh}}

This provides a robust estimate of the density at a quantile, useful for
standard error estimation and inference.
}
\examples{
x <- rnorm(100)
# Density at median
kerden(x, q = 0.5)

# Density at specific value
kerden(x, q = 0, xval = 0)
}
\seealso{
\code{\link{kerSORT}} for Silverman's kernel density,
\code{\link{akerd}} for adaptive kernel density,
\code{\link{idealf}} for ideal fourths
}
