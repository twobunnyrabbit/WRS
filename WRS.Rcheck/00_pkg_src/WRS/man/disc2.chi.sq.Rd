% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{disc2.chi.sq}
\alias{disc2.chi.sq}
\alias{disc2com}
\title{Chi-Squared Test for Comparing Two Discrete Distributions}
\usage{
disc2.chi.sq(x, y, simulate.p.value = FALSE, B = 2000)

disc2com(x, y, simulate.p.value = FALSE, B = 2000)
}
\arguments{
\item{x}{Numeric or factor vector. First sample (discrete values).}

\item{y}{Numeric or factor vector. Second sample (discrete values).}

\item{simulate.p.value}{Logical. If TRUE, computes p-value via Monte Carlo simulation
rather than asymptotic chi-squared distribution (default: FALSE).}

\item{B}{Integer. Number of Monte Carlo replicates if \code{simulate.p.value=TRUE}
(default: 2000).}
}
\value{
List with components:
\describe{
\item{X.squared}{Chi-squared test statistic}
\item{p.value}{P-value for test of equal distributions}
}
}
\description{
Tests whether two independent samples come from identical discrete distributions
using a chi-squared test with optional Monte Carlo p-value simulation.
}
\details{
This function tests:
\itemize{
\item H0: The two groups have identical discrete distributions
\item H1: The distributions differ
}

The test is based on a contingency table cross-classifying observations by group
(x vs y) and discrete value. The chi-squared statistic measures association between
group membership and observed values.

\strong{When to use simulation}: For small sample sizes or sparse contingency tables,
set \code{simulate.p.value=TRUE} to obtain valid p-values via Monte Carlo methods
rather than relying on asymptotic approximations.

This is a wrapper around \code{chisq.test} with output formatting for the WRS package.
}
\examples{
# Compare two discrete distributions
set.seed(123)
x <- sample(1:5, 50, replace = TRUE, prob = c(0.2, 0.3, 0.2, 0.2, 0.1))
y <- sample(1:5, 50, replace = TRUE, prob = c(0.1, 0.2, 0.3, 0.2, 0.2))
disc2.chi.sq(x, y)

# With simulated p-value
disc2.chi.sq(x, y, simulate.p.value = TRUE)
}
\seealso{
\code{\link{chisq.test}}, \code{\link{discstep}}
}
\keyword{htest}
\keyword{nonparametric}
