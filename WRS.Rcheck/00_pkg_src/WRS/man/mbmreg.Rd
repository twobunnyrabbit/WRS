% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression.R
\name{mbmreg}
\alias{mbmreg}
\title{Modified Bounded M-Regression}
\usage{
mbmreg(
  x,
  y,
  iter = 20,
  bend = 2 * sqrt(ncol(x) + 1)/nrow(x),
  xout = FALSE,
  outfun = outpro,
  ...
)
}
\arguments{
\item{x}{A numeric matrix of predictor variables (n by p).}

\item{y}{A numeric vector of the dependent variable (length n).}

\item{iter}{Maximum number of iterations for convergence (default: 20).}

\item{bend}{Bending constant for Huber's psi (default: \code{2*sqrt((p+1)/n)}).}

\item{xout}{Logical. Not used in this function but included to avoid conflicts
when using with other functions like \code{regci} (default: \code{FALSE}).}
}
\value{
A list with components:
\describe{
\item{coef}{Vector of regression coefficients (intercept, slopes).}
\item{residuals}{Residuals from the final fit.}
\item{w}{Final weights assigned to each observation.}
}
}
\description{
Computes a modified bounded M-regression estimator using Huber's psi function
with Schweppe weights. Regression outliers (identified via LMS) are given
zero weight, providing strong protection against outliers.
}
\details{
This is the modified M-regression estimator described in Wilcox (2022), Chapter 8.
The procedure:
\enumerate{
\item \strong{Initial outlier detection}: Uses LMS regression to identify regression
outliers (standardized residuals > 2.5). These get weight 0 throughout.
\item \strong{Iterative reweighting}:
\itemize{
\item Computes Schweppe weights: \eqn{w_i = \nu_i \psi(r_i / \sigma \nu_i)}
\item Where \eqn{\nu_i = \sqrt{1 - h_{ii}}} (leverages from hat matrix)
\item \eqn{\psi} is Huber's psi function with tuning constant \code{bend}
\item Scale \eqn{\sigma} estimated robustly from residuals
}
\item \strong{Convergence}: Iterates until coefficient changes < 0.0001
}

The Schweppe weighting down-weights high leverage points more than low
leverage points, providing better finite-sample performance than standard
M-estimation. Regression outliers identified initially maintain zero weight,
protecting against severe outliers.
}
\examples{
\dontrun{
# Simple example
set.seed(123)
x <- matrix(rnorm(50), ncol=1)
y <- 2*x + rnorm(50, sd=0.5)
y[1:3] <- y[1:3] + 10  # add outliers
mbmreg(x, y)

# Multiple regression
x <- matrix(rnorm(100), ncol=2)
y <- x[,1] + 2*x[,2] + rnorm(50)
mbmreg(x, y)
}
}
\references{
Wilcox, R.R. (2022). \emph{Introduction to Robust Estimation and Hypothesis Testing}
(5th ed.). Academic Press. Chapter 8.
}
\seealso{
\code{\link{bmreg}} for standard bounded M-regression,
\code{\link{lmsreg}} for least median of squares

Other robust regression functions: 
\code{\link{LMSreg}()},
\code{\link{MMreg}()},
\code{\link{bireg}()},
\code{\link{bmreg}()},
\code{\link{chreg}()},
\code{\link{depreg}()},
\code{\link{mdepreg}()},
\code{\link{mopreg}()},
\code{\link{snmreg}()},
\code{\link{tsgreg}()},
\code{\link{winreg}()},
\code{\link{wreg}()}
}
\concept{robust regression functions}
