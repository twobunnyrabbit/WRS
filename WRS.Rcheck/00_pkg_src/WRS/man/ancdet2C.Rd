% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{ancdet2C}
\alias{ancdet2C}
\title{ANCOVA Detection Method for Two Covariates}
\usage{
ancdet2C(
  x1,
  y1,
  x2,
  y2,
  fr1 = 1,
  fr2 = 1,
  tr = 0.2,
  test = yuen,
  q = 0.5,
  alpha = 0.05,
  plotit = TRUE,
  op = FALSE,
  pts = NA,
  sm = FALSE,
  FRAC = 0.5,
  pr = TRUE,
  xout = FALSE,
  outfun = outpro,
  MC = FALSE,
  p.crit = NULL,
  nreps = 2000,
  SEED = TRUE,
  FAST = TRUE,
  ticktype = "detail",
  xlab = "X1",
  ylab = "X2",
  zlab = "Y",
  pch1 = "*",
  pch2 = "+",
  ...
)
}
\arguments{
\item{x1}{Matrix of covariate values for group 1 (n1 x 2).}

\item{y1}{Dependent variable for group 1.}

\item{x2}{Matrix of covariate values for group 2 (n2 x 2).}

\item{y2}{Dependent variable for group 2.}

\item{fr1}{Span for running interval smoother for group 1 (default: 1).}

\item{fr2}{Span for running interval smoother for group 2 (default: 1).}

\item{tr}{Trimming proportion (default: 0.2). Proportion of observations to
trim from each tail when computing winsorized correlation.}

\item{test}{Test function to use: yuen (default), qcomhd, or qcomhdMC.}

\item{q}{Quantile for qcomhd test (default: 0.5).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{op}{Logical. If TRUE, plots estimated differences for covariate points.}

\item{pts}{Design points (currently not fully implemented).}

\item{sm}{Logical. Smoothing parameter.}

\item{FRAC}{Fraction for depth-based methods (default: 0.5 for deepest half).}

\item{pr}{Logical. If \code{TRUE}, prints progress messages (default: \code{TRUE}).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{MC}{Logical. If \code{TRUE}, uses parallel processing via \code{mclapply}
(default: \code{FALSE}).}

\item{p.crit}{Critical p-value for significance. If NULL, computed via simulation.}

\item{nreps}{Number of replications for p.crit computation (default: 2000).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{FAST}{Logical. If TRUE, uses pre-computed critical values when possible.}

\item{ticktype}{Tick type for lplot ('simple' or 'detail').}

\item{xlab}{Label for first covariate axis.}

\item{ylab}{Label for second covariate axis.}

\item{zlab}{Label for z-axis when op=TRUE.}

\item{pch1}{Plot character for non-significant points (default: '*').}

\item{pch2}{Plot character for significant points (default: '+').}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
List with components:
\item{num.sig}{Number of significant points found}
\item{p.crit}{Critical p-value used}
\item{points.used}{Matrix of covariate points and test results}
\item{sig.points}{Matrix of significant covariate points}
}
\description{
Performs two-group ANCOVA comparisons using two covariates with familywise
error rate control. Uses method MC3 from Wilcox (2017) with an improvement
on Hochberg's method. Compares groups at the deepest half of covariate values.
}
\note{
Minimum sample size of 50 per group required.
}
\examples{
\dontrun{
x1 <- matrix(rnorm(100), ncol=2)
y1 <- x1[,1] + x1[,2] + rnorm(50)
x2 <- matrix(rnorm(100), ncol=2)
y2 <- x2[,1] + x2[,2] + 0.5 + rnorm(50)
ancdet2C(x1, y1, x2, y2)
}
}
\references{
Wilcox, R.R. (2017). Introduction to Robust Estimation and Hypothesis Testing (4th ed.).
}
