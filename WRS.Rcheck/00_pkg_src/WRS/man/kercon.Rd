% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{kercon}
\alias{kercon}
\title{Conditional Local Weighted Regression with Epanechnikov Kernel}
\usage{
kercon(
  x,
  y,
  pyhat = FALSE,
  cval = NA,
  plotit = TRUE,
  eout = FALSE,
  xout = FALSE,
  outfun = out,
  iran = 0.05,
  xlab = "X",
  ylab = "Y",
  pch = "."
)
}
\arguments{
\item{x}{Matrix or data frame. Must have exactly 2 columns (two predictors).}

\item{y}{Numeric vector. Response variable.}

\item{pyhat}{Logical. If TRUE, returns predicted values; if FALSE (default), returns "Done".}

\item{cval}{Numeric vector. Conditioning values for second predictor (default: quartiles and median).}

\item{plotit}{Logical. If TRUE (default), plots the regression lines.}

\item{eout}{Logical. If TRUE, removes outliers based on full data (x and y); default FALSE.}

\item{xout}{Logical. If TRUE, removes outliers based on predictors only; default FALSE.}

\item{outfun}{Function. Outlier detection function (default: \code{\link{out}}).}

\item{iran}{Numeric. Fraction determining range restriction (default: 0.05).}

\item{xlab}{Character. X-axis label for plot (default: "X").}

\item{ylab}{Character. Y-axis label for plot (default: "Y").}

\item{pch}{Plotting character (default: '.').}
}
\value{
If \code{pyhat=FALSE}: Character string "Done"
If \code{pyhat=TRUE}: Numeric vector of predicted values
}
\description{
Computes conditional local weighted regression using the Epanechnikov kernel
for two predictors. Based on Fan (1993).
}
\details{
Performs local linear regression conditioned on specific values of the second
predictor:
\enumerate{
\item Selects observations near each conditioning value
\item Applies Epanechnikov kernel weights
\item Fits local weighted regression
\item Optionally plots regression lines for each conditioning value
}

Reference: Fan, J. (1993). Local linear regression smoothers and their
minimax efficiencies. Annals of Statistics, 21, 196-217.
}
\examples{
\dontrun{
set.seed(123)
x <- matrix(c(rnorm(100), rnorm(100)), ncol = 2)
y <- x[,1] + 0.5 * x[,2] + rnorm(100, sd = 0.5)
kercon(x, y, plotit = TRUE)
}
}
\seealso{
\code{\link{lplot}} for lowess plots,
\code{\link{runmean}} for running means,
\code{\link{kerden}} for kernel density estimation
}
