% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{essp}
\alias{essp}
\title{Effective Sample Size Plot (ESSP) / Trimmed View for Regression}
\usage{
essp(x, Y, M = 50)
}
\arguments{
\item{x}{Numeric matrix or data frame. Predictor variables (n × p).}

\item{Y}{Numeric vector. Response variable (length n).}

\item{M}{Numeric. Percentage of observations to trim based on Mahalanobis distance
(default: 50). Should be between 0 and 100.}
}
\value{
Numeric vector. Estimated projection coefficients β (excluding intercept).
}
\description{
Creates an Effective Sample Size Plot (ESSP) or "trimmed view" for visualizing
the relationship g in models of the form y = g(x'β, ε) after trimming M\% of
outliers based on robust distances. Allows visual assessment of linearity and
estimation of the projection direction β.
}
\details{
The ESSP method:
\enumerate{
\item Computes robust Mahalanobis distances using MCD (Minimum Covariance Determinant)
\item Trims the M\% most outlying observations based on these distances
\item Estimates β via least squares on the trimmed data
\item Computes effective sufficient predictor: ESP = x'β
\item Plots Y vs ESP, allowing interactive point identification
}

\strong{Purpose}: In dimension reduction models (single-index, projection pursuit),
the response depends on predictors only through a linear combination x'β.
This plot helps:
\itemize{
\item Visualize this reduced relationship
\item Check if the relationship g is linear or nonlinear
\item Crudely estimate the projection direction β
\item Identify influential points in the reduced space
}

The function is interactive: click on points to identify them, then click "stop"
(or right-click) to finish.

\strong{Graphics device}: May need to activate with \code{X11()} or similar before calling.
}
\examples{
\dontrun{
# Simulated single-index model: Y = (X1 + X2)^2 + error
set.seed(123)
n <- 100
x <- matrix(rnorm(n * 2), ncol = 2)
Y <- (x[,1] + x[,2])^2 + rnorm(n)
beta_est <- essp(x, Y, M = 50)
# Plot shows nonlinear relationship with true projection
}
}
\seealso{
\code{\link{cov.mcd}}, \code{\link{lsfit}}, \code{\link{identify}}
}
\keyword{hplot}
\keyword{regression}
\keyword{robust}
