% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{KMSgridAV}
\alias{KMSgridAV}
\title{Grid-Based KMS ANOVA: Average Pairwise Effect Sizes}
\usage{
KMSgridAV(
  x,
  y,
  IV = c(1, 2),
  Qsplit1 = 0.5,
  Qsplit2 = 0.5,
  alpha = 0.05,
  est = tmean,
  nboot = 1000,
  pr = TRUE,
  method = "hoch",
  xout = FALSE,
  outfun = outpro,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{x}{Matrix or data frame containing independent variables.}

\item{y}{Numeric vector containing the dependent variable.}

\item{IV}{Integer vector of length 2 specifying columns in \code{x} to use as IVs (default: c(1,2)).}

\item{Qsplit1}{Numeric vector of quantiles for splitting first IV (default: 0.5 for median split).}

\item{Qsplit2}{Numeric vector of quantiles for splitting second IV (default: 0.5 for median split).}

\item{alpha}{Numeric. Significance level (default: 0.05).}

\item{est}{Estimator function for location (default: \code{tmean}).}

\item{nboot}{Integer. Number of bootstrap samples (default: 1000).}

\item{pr}{Logical. If TRUE, prints messages about eliminated groups (default: TRUE).}

\item{method}{Character. Multiple comparison adjustment method (default: 'hoch' for Hochberg).}

\item{xout}{Logical. If TRUE, removes outliers from IVs before analysis (default: FALSE).}

\item{outfun}{Function for outlier detection (default: \code{outpro}).}

\item{SEED}{Logical. If TRUE, sets random seed for reproducibility (default: TRUE).}

\item{...}{Additional arguments passed to estimator and outlier functions.}
}
\value{
List with components:
\describe{
\item{Factor.A}{Results for Factor A main effects from \code{\link{AOV2KMS.mcp}}}
\item{Factor.B}{Results for Factor B main effects from \code{\link{AOV2KMS.mcp}}}
}
}
\description{
Compares effect sizes among groups defined by quantile splits of two independent
variables. Tests main effects based on average pairwise KMS effect sizes for each
factor level in a grid-based two-way ANOVA framework.
}
\details{
Performs grid-based ANOVA using average KMS effect sizes:
\enumerate{
\item Splits data into J×K grid based on quantiles of two IVs
\item For each factor level, computes pairwise KMS effect sizes
\item Averages KMS values within each factor level
\item Tests main effects using \code{\link{AOV2KMS.mcp}}
\item Eliminates groups with n ≤ 5
}

Unlike \code{\link{KMSgridRC}} which uses global KMS, this function uses
average pairwise comparisons, which can be more sensitive to specific
pairwise differences.
}
\examples{
# 2x2 grid with average KMS effect sizes
set.seed(123)
x <- matrix(rnorm(200), ncol = 2)
y <- rnorm(100)
result <- KMSgridAV(x, y, Qsplit1 = .5, Qsplit2 = .5, nboot = 500)
result$Factor.A
}
\seealso{
\code{\link{AOV2KMS.mcp}} for average KMS comparisons,
\code{\link{KMSgridRC}} for global KMS version,
\code{\link{KMSgrid.mcp}} for signed KMS version
}
