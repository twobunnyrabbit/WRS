% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{pb2genMC}
\alias{pb2genMC}
\title{Percentile Bootstrap for Two-Group Comparisons (Multicore Version)}
\usage{
pb2genMC(
  x,
  y,
  alpha = 0.05,
  nboot = 2000,
  est = onestep,
  SEED = TRUE,
  pr = FALSE,
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{est}{Estimator function to use for computing location or other
statistics. Common choices include \code{mean}, \code{median}, \code{tmean} (trimmed mean),
\code{mom} (modified one-step M-estimator), \code{mest} (M-estimator).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{pr}{Logical. If \code{TRUE}, prints progress message (default: \code{FALSE}).}

\item{...}{Additional arguments passed to the estimator function \code{est}.}
}
\value{
A list with components:
\itemize{
\item \code{est.1}: Estimate for group 1
\item \code{est.2}: Estimate for group 2
\item \code{ci}: Two-element vector with confidence interval bounds
\item \code{p.value}: P-value for testing H0: difference = 0
\item \code{sq.se}: Squared standard error of the bootstrap distribution
\item \code{n1}, \code{n2}: Sample sizes for groups 1 and 2
}
}
\description{
Computes a percentile bootstrap confidence interval for the difference
between any two parameters for independent groups. Uses multicore processing
via \code{mclapply} for improved performance. This is the parallel version of \code{pb2gen}.
}
\details{
This function is identical to \code{pb2gen} except that it uses \code{mclapply} from
the \code{parallel} package to process bootstrap samples in parallel across
multiple CPU cores. This can provide substantial speedups, especially with
large datasets or many bootstrap samples.

The function is a general-purpose two-sample bootstrap that can compare any
parameter between independent groups. By default, it compares M-estimators
of location (\code{est = onestep}).

Common choices for \code{est}:
\itemize{
\item \code{onestep} - One-step M-estimator (default)
\item \code{mean} - Arithmetic mean
\item \code{median} - Median
\item \code{tmean} - Trimmed mean (use with \code{trim = 0.2})
\item \code{mom} - Modified one-step M-estimator
\item \code{hd} - Harrell-Davis estimator (use with \code{q = 0.5} for median)
}

Missing values are automatically removed. The function constructs a
percentile bootstrap confidence interval and computes a p-value for
testing whether the difference equals zero.

\strong{Performance Note}: Multicore processing is most beneficial when:
\itemize{
\item \code{nboot} is large (e.g., >= 2000)
\item The estimator function is computationally intensive
\item Multiple CPU cores are available
}

On single-core systems, \code{pb2gen} may be faster due to reduced overhead.
}
\note{
The \code{parallel} package must be loaded for multicore functionality.
Results may vary slightly across runs even with \code{SEED = TRUE} due to
the random number generation in parallel processing.
}
\examples{
\dontrun{
set.seed(123)
x <- rnorm(50)
y <- rnorm(60, mean = 0.5)

# Compare M-estimators (default) using multicore
pb2genMC(x, y, nboot = 4000)

# Compare means
pb2genMC(x, y, est = mean, nboot = 4000)

# Compare medians
pb2genMC(x, y, est = median, nboot = 4000)

# Compare trimmed means
pb2genMC(x, y, est = mean, trim = 0.2, nboot = 4000)

# Compare specific quantiles using Harrell-Davis
pb2genMC(x, y, est = hd, q = 0.75, nboot = 4000)  # Third quartile
}
}
\seealso{
\code{\link{pb2gen}} for single-core version,
\code{\link{trimpb2}} for trimmed means specifically,
\code{\link{yuenbt}} for bootstrap-t method
}
