% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{Flplot2g}
\alias{Flplot2g}
\title{Plot Average Curves for Two Groups}
\usage{
Flplot2g(x1, x2, est = mean, xlab = "Time", ylab = "Y", plotit = TRUE)
}
\arguments{
\item{x1}{Matrix (n1 × p) for group 1, where rows are curves and columns are time points.}

\item{x2}{Matrix (n2 × p) for group 2, where rows are curves and columns are time points.}

\item{est}{Function to compute measure of location at each time point (default: mean).}

\item{xlab}{Label for X-axis (default: "Time").}

\item{ylab}{Label for Y-axis (default: "Y").}

\item{plotit}{Logical. Create plot if TRUE (default: TRUE).}
}
\value{
List with components:
\item{est.1}{Vector of estimated values for group 1 at each time point.}
\item{est.2}{Vector of estimated values for group 2 at each time point.}
}
\description{
Computes and plots pointwise averages (or other location measures) for two
independent groups of functional curves, allowing visual comparison of trends.
}
\details{
This function compares functional data from two independent groups by computing
and overlaying their summary curves. At each time point, the specified estimator
is computed separately for each group.

\strong{Plot appearance}:
\itemize{
\item \strong{Solid line}: Group 1 average curve
\item \strong{Dashed line}: Group 2 average curve
}

\strong{Input requirements}:
\itemize{
\item Both matrices must have the same number of columns (time points)
\item Missing values are automatically removed
}

\strong{Common estimators}:
\itemize{
\item \strong{mean}: Sensitive to outliers but efficient
\item \strong{median}: Robust to outlying curves
\item \strong{tmean}: 20\% trimmed mean (robust compromise)
}

This is particularly useful for comparing treatment vs. control groups in
longitudinal or functional data settings.
}
\examples{
# Compare two groups of curves
set.seed(123)
t <- seq(0, 1, length = 50)

# Group 1: baseline curves
x1 <- matrix(0, nrow = 15, ncol = 50)
for(i in 1:15) {
  x1[i,] <- sin(2*pi*t) + rnorm(50, 0, 0.2)
}

# Group 2: shifted curves
x2 <- matrix(0, nrow = 15, ncol = 50)
for(i in 1:15) {
  x2[i,] <- sin(2*pi*t) + 0.5 + rnorm(50, 0, 0.2)
}

Flplot2g(x1, x2)
}
\seealso{
\code{\link{Flplot}}, \code{\link{FQplot}}, \code{\link{lplot2g}}
}
