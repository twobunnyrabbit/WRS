% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{ecor}
\alias{ecor}
\title{Explanatory Correlation with Outlier Detection}
\usage{
ecor(
  x,
  y,
  pcor = FALSE,
  regfun = tsreg,
  corfun = pbcor,
  outkeep = FALSE,
  outfun = outmgvf
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{pcor}{Logical. If \code{TRUE}, uses Pearson correlation; if \code{FALSE}, uses \code{corfun}.}

\item{regfun}{Regression function for outlier detection (default: \code{tsreg}).}

\item{corfun}{Correlation function to use. Common options: \code{pbcor} (percentage
bend), \code{pcor} (Pearson), \code{wincor} (winsorized), \code{spear} (Spearman).}

\item{outkeep}{Logical. If \code{TRUE}, keeps outliers; if \code{FALSE} (default), removes them.}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}
}
\value{
Numeric value representing the explanatory correlation coefficient.
}
\description{
Estimates the explanatory correlation between x and y after removing outliers
detected by projection methods.
}
\details{
This function computes an explanatory (predictive) correlation by first
fitting a robust regression of y on x, then computing the correlation between
fitted values and observed y after removing outliers.

Outliers are detected using the specified \code{outfun}. The sign of the correlation
matches the sign of the regression coefficient.
}
\examples{
set.seed(123)
x <- c(rnorm(40), 5, 6)  # Two outliers
y <- c(2 + 0.5 * rnorm(40), -5, 7)
ecor(x, y)
}
\seealso{
\code{\link{ocor}}, \code{\link{pbcor}}
}
