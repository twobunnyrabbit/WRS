% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{cor.skip.com}
\alias{cor.skip.com}
\title{Compare Overlapping Skipped Correlations}
\usage{
cor.skip.com(
  x,
  y,
  corfun = wincor,
  outfun = outpro,
  alpha = 0.05,
  nboot = 1000,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{x}{Matrix with two predictors (n-by-2).}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{corfun}{Correlation function to use. Common options: \code{pbcor} (percentage
bend), \code{pcor} (Pearson), \code{wincor} (winsorized), \code{spear} (Spearman).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{nboot}{Number of bootstrap samples (default: 1000).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{...}{Additional arguments passed to \code{corfun}.}
}
\value{
List with components:
\item{Est1}{Skipped correlation between x\link{,1} and y.}
\item{Est2}{Skipped correlation between x\link{,2} and y.}
\item{dif}{Difference Est1 - Est2.}
\item{ci}{BCa bootstrap confidence interval for the difference.}
}
\description{
Compares two skipped correlations with a common dependent variable using
bias-corrected accelerated (BCa) bootstrap.
}
\details{
For two predictors, compares their skipped correlations with the dependent
variable (overlapping case). Uses BCa bootstrap to handle the dependence
between the two correlation estimates.

Requires the \code{bcaboot} package for bias-corrected accelerated bootstrap.
Limited to (1-alpha) confidence interval for the difference.
}
\examples{
\dontrun{
set.seed(123)
x <- matrix(rnorm(50 * 2), ncol = 2)
y <- 0.7 * x[, 1] + 0.2 * x[, 2] + rnorm(50)
cor.skip.com(x, y, nboot = 500)
}
}
\references{
Efron, B., & Tibshirani, R. J. (1993). An Introduction to the Bootstrap.
Chapman and Hall.
}
\seealso{
\code{\link{corskip.comPV}}, \code{\link{scor}}, \code{\link{TWOpov}}
}
