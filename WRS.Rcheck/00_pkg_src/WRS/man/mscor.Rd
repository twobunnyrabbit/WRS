% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{mscor}
\alias{mscor}
\title{Multiple Skipped Correlation}
\usage{
mscor(
  m,
  corfun = spear,
  cop = 3,
  MM = FALSE,
  gval = NA,
  ap = TRUE,
  pw = TRUE,
  STAND = TRUE,
  outfun = outpro,
  alpha = 0.05
)
}
\arguments{
\item{m}{Numeric matrix (n-by-p). Rows are observations, columns are variables.}

\item{corfun}{Correlation function to use. Common options: \code{pbcor} (percentage
bend), \code{pcor} (Pearson), \code{wincor} (winsorized), \code{spear} (Spearman).}

\item{cop}{Copula type for skipped correlation (default: 3). Options: 1-5.}

\item{MM}{Logical. If \code{TRUE}, uses MM-estimator (default: \code{FALSE}).}

\item{gval}{Numeric. Critical value for outlier detection. If \code{NA} (default),
determined automatically.}

\item{ap}{Logical. If \code{TRUE}, applies adjustment for p-values (default: \code{TRUE}).}

\item{pw}{Logical. If \code{TRUE}, performs pairwise comparisons (default: \code{TRUE}).}

\item{STAND}{Logical. If \code{TRUE}, standardizes data before analysis
(default: \code{TRUE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}
}
\value{
List with components:
\item{cor.mat}{p-by-p matrix of correlations (after removing outliers).}
\item{p.values}{Matrix of p-values for testing H0: rho = 0.}
\item{outliers}{Indices of detected outliers.}
\item{n.keep}{Number of observations retained.}
}
\description{
Computes a correlation matrix after removing multivariate outliers using
projection-based outlier detection.
}
\details{
This function computes a "skipped" correlation matrix by first detecting
and removing multivariate outliers, then computing correlations on the
remaining data.

Outliers are detected using projection methods (see \code{outpro()}). For each
point, the function considers the line between it and the center, projects
all points onto this line, and checks for outliers using a boxplot rule.

The \code{cop} parameter determines how the center is estimated:
\itemize{
\item cop=1: Donoho-Gasko halfspace median
\item cop=2: MCD measure of location
\item cop=3: Marginal medians (default)
\item cop=4: MVE measure of location
}

The \code{corfun} parameter specifies the correlation to use (default: Spearman).
}
\examples{
# Correlation matrix with outlier detection
set.seed(123)
m <- matrix(rnorm(50 * 4), ncol = 4)
# Add some outliers
m[1, ] <- c(5, 5, 5, 5)
m[2, ] <- c(-5, -5, -5, -5)
result <- mscor(m)
result$cor.mat
result$outliers
}
\seealso{
\code{\link{scorci}}, \code{\link{outpro}}, \code{\link{scor}}
}
