% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{scorv2}
\alias{scorv2}
\title{Skipped Correlation Version 2}
\usage{
scorv2(
  x,
  y = NULL,
  corfun = pcor,
  gval = NA,
  plotit = FALSE,
  op = TRUE,
  cop = 3,
  xlab = "VAR 1",
  ylab = "VAR 2",
  STAND = TRUE,
  pr = TRUE,
  SEED = TRUE,
  MC = FALSE
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{corfun}{Correlation function to use. Common options: \code{pbcor} (percentage
bend), \code{pcor} (Pearson), \code{wincor} (winsorized), \code{spear} (Spearman).}

\item{gval}{Critical value for outlier detection (default: \code{NA} for automatic).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{op}{Logical. Operational flag (default: \code{TRUE}).}

\item{cop}{Copula type for skipped correlation (default: 3). Options: 1-5.}

\item{xlab}{Label for x-axis in plots (default: "X" or "VAR 1").}

\item{ylab}{Label for y-axis in plots (default: "Y" or "VAR 2").}

\item{STAND}{Logical. If \code{TRUE}, standardizes data before analysis
(default: \code{TRUE}).}

\item{pr}{Logical. If \code{TRUE}, prints progress messages (default: \code{TRUE}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{MC}{Logical. If \code{TRUE}, uses parallel processing via \code{mclapply}
(default: \code{FALSE}).}
}
\value{
List with components:
\item{cor}{Skipped correlation coefficient.}
\item{test.stat}{Test statistic.}
\item{crit.05}{Critical value at 0.05 level.}
}
\description{
Computes skipped correlation using version 2 algorithm (does not reset SEED).
}
\details{
Nearly identical to \code{scor()}, but does not reset the random seed, which
prevents problems when called from other functions that manage their own
seeds.

This version is used internally by functions that need skipped correlation
without seed interference.
}
\examples{
set.seed(123)
x <- c(rnorm(45), 5, -5)
y <- c(0.6 * rnorm(45), -5, 5)
scorv2(x, y)
}
\seealso{
\code{\link{scor}}, \code{\link{scorci}}
}
