% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{SMpre}
\alias{SMpre}
\title{Model Selection via Prediction Error with Smoothing}
\usage{
SMpre(
  x,
  y,
  est = tmean,
  fr = NA,
  varfun = pbvar,
  model = NULL,
  adz = TRUE,
  xout = FALSE,
  outfun = out,
  ...
)
}
\arguments{
\item{x}{Numeric matrix of predictor variables (maximum 5 columns).}

\item{y}{Numeric vector of response variable.}

\item{est}{Function for location estimation. Default is \code{\link{tmean}}
(trimmed mean).}

\item{fr}{Numeric. Span for running interval smoother. If \code{NA}, an automatic
choice is made. See \code{\link{rplotCV}}.}

\item{varfun}{Function for variance estimation. Default is \code{\link{pbvar}}
(percentage bend variance).}

\item{model}{List of integer vectors specifying which predictors to include in each
model. If \code{NULL}, all possible models are considered via \code{\link{modgen}}.}

\item{adz}{Logical. If \code{TRUE} (default), adds a "null model" (predicting with
mean only, no predictors) to the comparison.}

\item{xout}{Logical. If \code{TRUE}, removes leverage points from predictors using
\code{outfun} before analysis. Default is \code{FALSE}.}

\item{outfun}{Function for outlier detection when \code{xout=TRUE}. Default is
\code{\link{out}}.}

\item{...}{Additional arguments passed to \code{outfun}.}
}
\value{
List with component:
\item{estimates}{Matrix with columns "error" (prediction variance), "var.used"
(encoded model), and "rank" (1 = best model)}
}
\description{
Estimates prediction error for multiple regression models using leave-one-out
cross-validation with running interval smoothers. Useful for comparing different
predictor combinations (up to 5 predictors).
}
\details{
For each model specification in \code{model}:
\enumerate{
\item Uses leave-one-out cross-validation with \code{\link{rplotCV}}
\item Computes prediction error variance (VAR.Y.HAT)
\item Ranks models by prediction error (lower is better)
}

Models are encoded as integers formed from their predictor indices. For example,
using predictors 1 and 3 is encoded as 13.

If \code{adz=TRUE}, adds a null model (coded as 0) that predicts y using trimmed
mean of all other observations.

\strong{Note}: Limited to maximum of 5 predictors due to computational constraints.
}
\seealso{
\code{\link{rplotCV}}, \code{\link{modgen}}, \code{\link{lplot}}
}
\keyword{regression}
