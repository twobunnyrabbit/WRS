% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{hard.rejection}
\alias{hard.rejection}
\title{Hard Rejection Weighting Function}
\usage{
hard.rejection(distances, p, beta = 0.9, ...)
}
\arguments{
\item{distances}{Vector of distances (typically Mahalanobis or robust distances).}

\item{p}{Dimension of the data (number of variables).}

\item{beta}{Quantile of chi-squared distribution for threshold (default: 0.9).}

\item{...}{Additional arguments (not currently used).}
}
\value{
A numeric vector of weights (0 or 1) with the same length as
\code{distances}.
}
\description{
Computes hard rejection weights for robust estimation based on Mahalanobis-type
distances. Observations beyond a threshold receive zero weight.
}
\details{
This function implements a hard rejection scheme where observations are either
fully included (weight = 1) or completely excluded (weight = 0) based on their
distance from the center.

The cutoff threshold is:
\deqn{d_0 = \chi^2_{p,\beta} \times \text{median}(distances) / \chi^2_{p,0.5}}

where \eqn{\chi^2_{p,q}} is the q-th quantile of the chi-squared distribution
with p degrees of freedom.

Observations with distance \eqn{\leq d_0} receive weight 1; others receive
weight 0. This is more severe than soft rejection schemes like Huber or
Tukey bisquare weighting.
}
\examples{
# Generate distances for bivariate data
set.seed(1)
x <- matrix(rnorm(100), 50, 2)
dist <- mahalanobis(x, colMeans(x), cov(x))

# Compute hard rejection weights
w <- hard.rejection(dist, p = 2, beta = 0.9)
sum(w)  # Number of observations retained

}
\seealso{
\code{\link{outpro}}, \code{\link{covmcd}}
}
\keyword{robust}
