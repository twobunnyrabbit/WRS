% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{corblppb}
\alias{corblppb}
\title{Percentile Bootstrap CI for Correlation Based on Robust Regression}
\usage{
corblppb(
  x,
  y,
  regfun = tsreg,
  varfun = pbvar,
  nboot = 1000,
  alpha = 0.05,
  outfun = outpro.depth,
  SEED = TRUE,
  plotit = FALSE,
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{regfun}{Robust regression function (default: \code{tsreg}).}

\item{varfun}{Variance function (default: \code{pbvar}).}

\item{nboot}{Number of bootstrap samples (default: 1000).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{outfun}{Outlier detection function (default: \code{outpro.depth}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{plotit}{Logical. Create diagnostic plot (default: \code{FALSE}).}

\item{...}{Additional arguments passed to regression functions.}
}
\value{
List with components:
\item{cor.est}{Correlation estimate.}
\item{p.value}{Bootstrap p-value for testing zero correlation.}
\item{ci}{Percentile bootstrap confidence interval.}
}
\description{
Computes percentile bootstrap confidence interval for correlation based on
explanatory power from robust regression.
}
\details{
Computes correlation based on robust regression explanatory power. Uses
percentile bootstrap for both confidence interval and p-value. More robust
to non-normality than \code{corblp.ci()} which uses normal approximation.

Only supports single predictor.
}
\examples{
\dontrun{
set.seed(123)
x <- rnorm(50)
y <- 2 * x + rnorm(50)
corblppb(x, y, nboot = 500)
}
}
\seealso{
\code{\link{corblp.ci}}, \code{\link{corblp.EP}}, \code{\link{tsreg}}
}
