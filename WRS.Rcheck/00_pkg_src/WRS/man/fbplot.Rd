% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{fbplot}
\alias{fbplot}
\title{Functional Boxplot for Functional Data}
\usage{
fbplot(
  fit,
  x = NULL,
  method = "MBD",
  depth = NULL,
  plot = TRUE,
  prob = 0.5,
  color = 6,
  outliercol = 2,
  barcol = 4,
  fullout = FALSE,
  factor = 1.5,
  xlab = "Time",
  ylab = "Y",
  ...
)
}
\arguments{
\item{fit}{Functional data. Can be:
\itemize{
\item fd object (from fda package)
\item fdSmooth or fdPar object
\item Matrix (n × p: n curves evaluated at p time points)
}}

\item{x}{Evaluation points for fd objects (default: NULL, uses 101 equally-spaced points).}

\item{method}{Depth measure for ordering curves (default: "MBD", Modified Band Depth):
\itemize{
\item "MBD": Modified Band Depth (recommended)
\item "BD2": Band Depth with 2 curves
\item "BD3": Band Depth with 3 curves
\item "Both": Combination of BD2 and MBD
}}

\item{depth}{Pre-computed depth values (default: NULL, computed from data).}

\item{plot}{Logical. Create plot if TRUE (default: TRUE).}

\item{prob}{Probability for central region (default: 0.5 for 50\% region). Can be a vector
for multiple regions.}

\item{color}{Color(s) for central region(s) (default: 6).}

\item{outliercol}{Color for outlier curves (default: 2, red).}

\item{barcol}{Color for whiskers and envelope (default: 4, blue).}

\item{fullout}{Logical. Show all outliers if TRUE (default: FALSE, outliers shown behind envelope).}

\item{factor}{Whisker extension factor (default: 1.5). Determines outlier threshold.}

\item{xlab}{Label for X-axis (default: "Time").}

\item{ylab}{Label for Y-axis (default: "Y").}

\item{...}{Additional graphical parameters.}
}
\value{
List with components:
\item{depth}{Depth values for all curves.}
\item{outpoint}{Indices of curves identified as outliers.}
}
\description{
Creates a functional boxplot (Sun & Genton method) for visualizing functional
data, identifying outlying curves, and displaying the central 50\% region. Works
with both functional data objects and matrices.
}
\details{
The functional boxplot extends the traditional boxplot to functional data by:
\enumerate{
\item Computing depth for each curve (most central curve has highest depth)
\item Ordering curves by depth
\item Identifying the central 50\% region (or other probability)
\item Computing envelope of central region
\item Identifying outliers using whisker rule (factor * envelope height)
}

\strong{Depth measures}:
\itemize{
\item \strong{MBD} (Modified Band Depth): Recommended. Measures how often a curve is
contained in bands formed by pairs of other curves.
\item \strong{BD2/BD3}: Standard band depth using 2 or 3 curves
\item \strong{Both}: Lexicographic combination (BD2 breaks MBD ties)
}

\strong{Outlier detection}: Curves exceeding the envelope ± factor × (envelope range)
are flagged as outliers and plotted with different color.

\strong{Plot elements}:
\itemize{
\item Median curve (highest depth): Solid black line
\item Central region: Shaded polygon
\item Envelope: Maximum/minimum of non-outlying curves (blue lines)
\item Whiskers: Vertical bars showing envelope extent (blue bars)
\item Outliers: Dashed lines (red)
}

Requires the \code{fda} package for functional data objects and depth computations.
}
\examples{
\dontrun{
# Functional data example
library(fda)
# Create some functional data
t <- seq(0, 1, length = 51)
y <- matrix(0, nrow = 30, ncol = 51)
for(i in 1:30) {
  y[i,] <- sin(2*pi*t) + rnorm(51, 0, 0.1)
}
# Add an outlier curve
y[1,] <- sin(2*pi*t) + 2

fbplot(t(y))
}
}
\references{
Sun, Y. and Genton, M. G. (2011). Functional Boxplots. Journal of Computational
and Graphical Statistics, 20, 316-334.
}
\seealso{
\code{\link{func.plot}}, \code{\link{Flplot}}, \code{\link{spag.plot}}
}
