% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{DETS}
\alias{DETS}
\title{Robust Multivariate Location and Scatter via SDET}
\usage{
DETS(m)
}
\arguments{
\item{m}{Numeric matrix. Data matrix with observations in rows and variables in columns.}
}
\value{
List with components:
\describe{
\item{center}{Numeric vector of robust location estimates}
\item{cov}{Robust covariance matrix}
}
}
\description{
Computes robust multivariate location and scatter estimator using the SDET
(Stahel-Donoho Estimator with Ties) method by Hubert et al. (2015).
}
\details{
This function uses the SDET method from the \pkg{rrcov} package to compute robust
estimates of multivariate location (center) and scatter (covariance matrix).

The SDET estimator is particularly effective for high-dimensional data and handles
outliers robustly. It is based on outlyingness measures computed via projections.

\strong{Reference}: Hubert, M., Rousseeuw, P. J., & Verdonck, T. (2015). Robust PCA
for skewed data and its outlier map. Computational Statistics & Data Analysis, 53, 2264-2274.
}
\examples{
# Multivariate data with outliers
set.seed(123)
m <- matrix(rnorm(100), ncol = 2)
m[1:5, ] <- m[1:5, ] + 5  # Add outliers
result <- DETS(m)
result$center
}
\seealso{
\code{\link[rrcov]{CovSest}}, \code{\link{cov.mcd}}, \code{\link{rmba}}
}
\keyword{multivariate}
\keyword{robust}
