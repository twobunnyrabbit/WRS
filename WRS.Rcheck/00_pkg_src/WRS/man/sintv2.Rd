% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{sintv2}
\alias{sintv2}
\title{CI and Hypothesis Test for Median (Hettmansperger-Sheather)}
\usage{
sintv2(x, y = NULL, alpha = 0.05, nullval = 0, null.value = NULL, pr = TRUE)
}
\arguments{
\item{x}{Numeric vector of data, or first variable for paired comparison}

\item{y}{Optional numeric vector for paired comparison (default: NULL). If
provided, analysis is performed on x-y differences.}

\item{alpha}{Significance level (default: 0.05)}

\item{nullval}{Null hypothesis value for the median (default: 0)}

\item{null.value}{Alternative parameter name for \code{nullval} (default: NULL)}

\item{pr}{Logical indicating whether to print warning about duplicate values
(default: TRUE)}
}
\value{
A list with components:
\item{ci}{Confidence interval (lower, upper)}
\item{p.value}{Two-sided p-value for test of H0: median = nullval}
}
\description{
Computes confidence interval and p-value for the median (or median difference
for paired data) using the Hettmansperger-Sheather interpolation method.
}
\details{
This function extends \code{\link{sint}} by adding a hypothesis test. It computes:
\enumerate{
\item A (1-alpha) confidence interval for the median using HS interpolation
\item A p-value for testing H0: median = nullval
}

For paired data (when \code{y} is provided), the analysis is performed on the
differences x-y, testing whether the median difference equals nullval.

The p-value is computed by finding the smallest alpha level at which the
confidence interval would exclude the null value. The search is conducted
to 0.001 precision for p-values ≤ 0.01.

Missing values are automatically removed.
}
\seealso{
\code{\link{sint}}, \code{\link{sintv2mcp}}, \code{\link{hdpb}}
}
\keyword{htest}
\keyword{robust}
