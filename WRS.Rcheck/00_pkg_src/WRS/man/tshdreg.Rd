% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression.R
\name{tshdreg}
\alias{tshdreg}
\title{Theil-Sen Regression with Harrell-Davis Estimator}
\usage{
tshdreg(
  x,
  y,
  HD = TRUE,
  xout = FALSE,
  outfun = out,
  iter = 5,
  varfun = pbvar,
  tr = FALSE,
  do.stre = TRUE,
  corfun = pbcor,
  plotit = FALSE,
  tol = 1e-04,
  RES = TRUE,
  OPT = FALSE,
  xlab = "X",
  ylab = "Y",
  ...
)
}
\arguments{
\item{x}{A numeric vector or matrix containing the predictor variable(s). For
multiple predictors, rows represent observations and columns represent variables.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{HD}{Logical. If \code{TRUE}, uses Harrell-Davis estimator for the intercept;
if \code{FALSE}, uses median (default: \code{TRUE}).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{out}). See \code{\link{tsreg}}
for other options.}

\item{iter}{Number of iterations for the back-fitting algorithm when there are
multiple predictors (default: 5).}

\item{varfun}{Function used to compute variance for strength of association
(default: \code{pbvar}).}

\item{tr}{Logical or numeric. Trimming parameter for Harrell-Davis estimator
(default: \code{FALSE} for no trimming).}

\item{do.stre}{Logical. If \code{TRUE}, computes strength of association measure
(default: \code{TRUE}).}

\item{corfun}{Correlation function used for computing explanatory power when
the variance ratio exceeds 1 (default: \code{pbcor}).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{tol}{Convergence tolerance for the back-fitting algorithm (default: 0.0001).
Iteration stops when the maximum absolute change in coefficients is less than \code{tol}.}

\item{RES}{Logical. If \code{TRUE}, returns residuals in the output (default: \code{TRUE}).
Set to \code{FALSE} to save memory when residuals are not needed.}

\item{OPT}{Logical. If \code{TRUE}, computes intercept as \code{HD(y) - b1*HD(X)};
if \code{FALSE}, computes intercept as \code{HD(y - b1*X)} (default: \code{FALSE}).}

\item{xlab}{Label for x-axis when \code{plotit=TRUE} (default: "X").}

\item{ylab}{Label for y-axis when \code{plotit=TRUE} (default: "Y").}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
A list with components:
\item{coef}{Vector of regression coefficients (intercept followed by slopes)}
\item{residuals}{Vector of residuals (NULL if \code{RES=FALSE})}
\item{Strength.Assoc}{Strength of association (square root of explanatory power)}
\item{Explanatory.Power}{Proportion of variance explained}
}
\description{
Computes the Theil-Sen robust regression estimator using the Harrell-Davis
estimator for the intercept. This variant can provide improved efficiency
compared to the standard median-based approach while maintaining robustness.
}
\details{
This function implements the Theil-Sen estimator with the Harrell-Davis estimator
used for computing the intercept and for the back-fitting algorithm (when multiple
predictors are present). The Harrell-Davis estimator is a weighted average of
order statistics that can be more efficient than the sample median while
maintaining robustness.

For multiple predictors, a back-fitting algorithm is used, which iteratively
updates each coefficient while holding the others fixed. The algorithm continues
until convergence (based on \code{tol}) or until \code{iter} iterations are completed.

When using this function with bootstrap methods, the residuals must be available.
Ensure \code{RES=TRUE} when using \code{tshdreg} as the \code{regfun} argument in bootstrap
functions.
}
\examples{
# Simple regression with Harrell-Davis estimator
x <- rnorm(50)
y <- 2 + 3*x + rnorm(50)
result <- tshdreg(x, y)
result$coef

# Multiple regression with outlier removal
x <- matrix(rnorm(100), ncol=2)
y <- 1 + 2*x[,1] - 3*x[,2] + rnorm(50)
tshdreg(x, y, xout=TRUE)

# Ensure residuals are available for bootstrap use
result <- tshdreg(x, y, RES=TRUE)

}
\references{
Harrell, F. E. & Davis, C. E. (1982). A new distribution-free quantile estimator.
\emph{Biometrika}, 69, 635-640.

Wilcox, R. R. (2022). \emph{Introduction to Robust Estimation and Hypothesis
Testing} (5th ed.). Academic Press.
}
\seealso{
\code{\link{tsreg}} for standard Theil-Sen regression,
\code{\link{tsp1reg}} for single predictor version, \code{\link{hd}}
for Harrell-Davis estimator
}
