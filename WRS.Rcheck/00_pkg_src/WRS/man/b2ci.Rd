% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{b2ci}
\alias{b2ci}
\title{Bootstrap Confidence Interval for Difference Between Two Parameters}
\usage{
b2ci(x, y, alpha = 0.05, nboot = 2000, est = bivar, SEED = TRUE, ...)
}
\arguments{
\item{x}{Numeric vector for first group.}

\item{y}{Numeric vector for second group.}

\item{alpha}{Significance level. Default is 0.05.}

\item{nboot}{Number of bootstrap samples. Default is 2000.}

\item{est}{Estimator function to apply to each group. Default is \code{\link{bivar}}
(biweight midvariance).}

\item{SEED}{Logical. If \code{TRUE} (default), sets random seed to 2 for reproducibility.}

\item{...}{Additional arguments passed to the estimator function.}
}
\value{
A list with components:
\item{est1}{Estimate for first group.}
\item{est2}{Estimate for second group.}
\item{ratio}{Ratio of estimates (est1/est2).}
\item{ci}{Bootstrap confidence interval for the difference.}
\item{p.value}{Two-sided p-value for testing equality.}
}
\description{
Computes a percentile bootstrap confidence interval for the difference between
any two parameters corresponding to independent groups. By default, biweight
midvariances are compared.
}
\details{
This function uses the percentile bootstrap method to construct a confidence
interval for \eqn{\theta_1 - \theta_2}{θ1 - θ2}, where \eqn{\theta_j}{θj} is
the parameter estimate for group j.

By setting \code{est=mean}, you get a bootstrap CI for the difference between means.
By setting \code{est=median}, you get a bootstrap CI for the difference between medians.
Any univariate estimator can be used.

The p-value is computed as twice the minimum of the proportion of bootstrap
differences less than or equal to zero.

Missing values are automatically removed.
}
\examples{
x <- rnorm(30)
y <- rnorm(35, 0.5)
# Compare means
b2ci(x, y, est = mean)
# Compare medians
b2ci(x, y, est = median)

}
\seealso{
\code{\link{bivar}}, \code{\link{pb2gen}}, \code{\link{trimpb2}}
}
\keyword{htest}
\keyword{robust}
