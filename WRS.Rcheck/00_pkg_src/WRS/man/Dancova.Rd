% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{Dancova}
\alias{Dancova}
\title{ANCOVA for Dependent (Paired/Repeated Measures) Groups}
\usage{
Dancova(
  x1,
  y1,
  x2 = x1,
  y2,
  fr1 = 1,
  fr2 = 1,
  tr = 0.2,
  alpha = 0.05,
  plotit = TRUE,
  pts = NA,
  sm = FALSE,
  xout = FALSE,
  outfun = out,
  DIF = FALSE,
  LP = FALSE,
  xlab = "X",
  ylab = "Y",
  ...
)
}
\arguments{
\item{x1}{Covariate values for condition 1 (single covariate only).}

\item{y1}{Response values for condition 1.}

\item{x2}{Covariate values for condition 2 (default: same as x1).}

\item{y2}{Response values for condition 2 (paired with y1).}

\item{fr1}{Span for condition 1 running interval smoother (default: 1).}

\item{fr2}{Span for condition 2 running interval smoother (default: 1).}

\item{tr}{Trimming proportion (default: 0.2). Proportion of observations to
trim from each tail when computing winsorized correlation.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{pts}{Design points where regression lines are compared. If NA, five points chosen automatically.}

\item{sm}{Logical. If TRUE, create smooths using bootstrap bagging.}

\item{xout}{Logical. Remove outliers from covariates before analysis.}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{DIF}{Logical. If FALSE, marginal trimmed means are compared.
If TRUE, trimmed means of difference scores are used.}

\item{LP}{Logical. Use lowess/loess smoothing for plots.}

\item{xlab, ylab}{Axis labels for plots.}

\item{...}{Additional arguments passed to outfun.}
}
\value{
Matrix with columns: X (design points), n (sample size), DIF (difference),
TEST (test statistic), se (standard error), ci.low, ci.hi, p.value, p.adjust.
Also creates plot if plotit=TRUE.
}
\description{
Compares two dependent (paired or repeated measures) groups using a robust ANCOVA
approach with running interval smoother. No parametric assumptions about regression
line form are required.
}
\note{
Data must be paired (same length for all vectors). One covariate only.
}
\examples{
\dontrun{
# Paired data (e.g., pre-post with covariate)
x <- rnorm(50)  # Same covariate for both conditions
y1 <- x + rnorm(50)  # Condition 1
y2 <- x + rnorm(50, mean=0.5)  # Condition 2
result <- Dancova(x, y1, x, y2)
}
}
