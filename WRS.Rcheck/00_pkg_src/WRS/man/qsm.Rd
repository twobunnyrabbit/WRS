% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{qsm}
\alias{qsm}
\title{Running Interval Smoother for Multiple Quantiles}
\usage{
qsm(
  x,
  y,
  qval = c(0.2, 0.5, 0.8),
  fr = 0.8,
  plotit = TRUE,
  scat = TRUE,
  pyhat = FALSE,
  eout = FALSE,
  xout = FALSE,
  outfun = out,
  op = TRUE,
  LP = TRUE,
  tr = FALSE,
  xlab = "X",
  ylab = "Y",
  pch = "."
)
}
\arguments{
\item{x}{Numeric vector. Predictor variable.}

\item{y}{Numeric vector. Response variable.}

\item{qval}{Numeric vector of quantile values to estimate (default: c(0.2, 0.5, 0.8)).}

\item{fr}{Numeric. Span parameter controlling amount of smoothing (default: 0.8).
Proportion of data used in each local window.}

\item{plotit}{Logical. If \code{TRUE}, produce plot (default: \code{TRUE}).}

\item{scat}{Logical. If \code{TRUE}, show scatter plot of data (default: \code{TRUE}).}

\item{pyhat}{Logical. If \code{TRUE}, return predicted values (default: \code{FALSE}).}

\item{eout}{Logical. If \code{TRUE}, remove outliers based on (x,y) jointly (default: \code{FALSE}).}

\item{xout}{Logical. If \code{TRUE}, remove outliers based on x only (default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{\link{out}}).}

\item{op}{Logical. If \code{TRUE}, use Harrell-Davis quantile estimator;
if \code{FALSE}, use order statistic (default: \code{TRUE}).}

\item{LP}{Logical. If \code{TRUE}, smooth initial fit using LOESS (default: \code{TRUE}).}

\item{tr}{Trimming parameter for Harrell-Davis estimator when \code{op=TRUE} (default: \code{FALSE}).}

\item{xlab}{Label for x-axis (default: "X").}

\item{ylab}{Label for y-axis (default: "Y").}

\item{pch}{Plotting character (default: ".").}
}
\value{
A list with component \code{output}:
\itemize{
\item If \code{pyhat=TRUE}: matrix with predicted values (one column per quantile)
\item If \code{pyhat=FALSE}: character string "Done"
}
}
\description{
Creates a running interval smoother for multiple quantile regression lines.
Estimates and plots conditional quantiles as smooth functions of a predictor.
}
\details{
For each quantile in \code{qval}, this function:
\enumerate{
\item For each x value, estimates the quantile using nearby y values
\item Optionally applies LOESS smoothing to the initial quantile estimates
\item Plots the smooth quantile regression line
}

The function supports outlier removal (\code{eout}, \code{xout}) and choice
of quantile estimator (Harrell-Davis vs. order statistic via \code{op}).
}
\seealso{
\code{\link{qsmcobs}}, \code{\link{runmq}}, \code{\link{hd}}, \code{\link{qest}}
}
\keyword{regression}
\keyword{smooth}
