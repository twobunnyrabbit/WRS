% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{ci2bin}
\alias{ci2bin}
\title{Confidence Interval for Difference Between Two Binomial Probabilities}
\usage{
ci2bin(
  r1 = sum(x),
  n1 = length(x),
  r2 = sum(y),
  n2 = length(y),
  x = NA,
  y = NA,
  alpha = 0.05
)
}
\arguments{
\item{r1}{Number of successes in group 1 (default: sum(x))}

\item{n1}{Number of observations in group 1 (default: length(x))}

\item{r2}{Number of successes in group 2 (default: sum(y))}

\item{n2}{Number of observations in group 2 (default: length(y))}

\item{x}{Optional binary vector for group 1 (if provided, r1 and n1 computed from it)}

\item{y}{Optional binary vector for group 2 (if provided, r2 and n2 computed from it)}

\item{alpha}{Significance level (default: 0.05)}
}
\value{
List with component:
\item{ci}{Confidence interval c(lower, upper) for p1 - p2}
}
\description{
Computes a confidence interval for the difference between success probabilities
(p1 - p2) for two independent binomial distributions.
}
\details{
Uses a score-based method accounting for variance and correlation structure.
More accurate than simple normal approximation, especially for small samples.
}
\keyword{htest}
