% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outliers.R
\name{aov2depth}
\alias{aov2depth}
\title{Two-by-K ANOVA Using Depth-Based Methods}
\usage{
aov2depth(
  x1,
  x2,
  est = tmean,
  nboot = 500,
  SEED = TRUE,
  nmin = 12,
  CR = FALSE,
  xlab = " DIF 1",
  ylab = "DIF 2",
  zlab = "DIF 3",
  alpha = 0.05,
  ...
)
}
\arguments{
\item{x1, x2}{Data for the two levels of Factor A. Can be matrices (columns =
Factor B levels) or lists.}

\item{est}{Estimator function to use for computing location or other
statistics. Common choices include \code{mean}, \code{median}, \code{tmean} (trimmed mean),
\code{mom} (modified one-step M-estimator), \code{mest} (M-estimator).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{nmin}{Minimum sample size (default: 12).}

\item{CR}{Logical. If TRUE, creates a critical region plot.}

\item{xlab, ylab, zlab}{Axis labels for plotting (when K=2 or K=3).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{...}{Additional arguments passed to \code{est}.}
}
\value{
A list with components:
\item{p.value}{Bootstrap p-value for testing no main effect of Factor A.}
\item{est1,est2}{Estimated locations for each group.}
\item{dif}{Estimated differences.}
\item{n1,n2}{Sample sizes.}
}
\description{
Tests for main effect of Factor A in a 2 x K design using depth of difference
vectors. Accounts for the pattern of measures across Factor B levels.
}
\details{
For each bootstrap sample, computes the depth of the zero vector relative
to the distribution of difference vectors. This tests whether Factor A has
an effect while accounting for the multivariate pattern across Factor B levels.
}
