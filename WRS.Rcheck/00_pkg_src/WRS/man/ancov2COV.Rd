% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{ancov2COV}
\alias{ancov2COV}
\title{ANCOVA with Two Covariates and Depth-Based Design Points}
\usage{
ancov2COV(
  x1,
  y1,
  x2,
  y2,
  tr = 0.2,
  test = yuen,
  cr = NULL,
  pr = TRUE,
  DETAILS = FALSE,
  cp.value = FALSE,
  plotit = FALSE,
  xlab = "X",
  ylab = "Y",
  zlab = NULL,
  span = 0.75,
  PV = TRUE,
  FRAC = 0.5,
  MC = FALSE,
  q = 0.5,
  iter = 1000,
  alpha = 0.05,
  TPM = FALSE,
  tau = 0.05,
  est = tmean,
  fr = 1,
  ...
)
}
\arguments{
\item{x1}{Matrix of two covariates for group 1.}

\item{y1}{Outcome values for group 1.}

\item{x2}{Matrix of two covariates for group 2.}

\item{y2}{Outcome values for group 2.}

\item{tr}{Trim proportion. Default is 0.2.}

\item{test}{Test function to use. Default is yuen (trimmed means test).}

\item{cr}{Critical value. If NULL, computed via simulation.}

\item{pr}{Logical. Print results. Default is TRUE.}

\item{DETAILS}{Logical. Return detailed output. Default is FALSE.}

\item{cp.value}{Logical. Compute critical p-value. Default is FALSE.}

\item{plotit}{Logical. Create plots. Default is FALSE.}

\item{xlab, ylab, zlab}{Axis labels for plots.}

\item{span}{Span for LOESS smoothing. Default is 0.75.}

\item{PV}{Logical. If TRUE, plot p-values; if FALSE, plot differences. Default is TRUE.}

\item{FRAC}{Fraction of deepest points to use. Default is 0.5.}

\item{MC}{Logical. Use multicore processing. Default is FALSE.}

\item{q}{Quantile for median-based tests. Default is 0.5.}

\item{iter}{Number of iterations for computing critical value. Default is 1000.}

\item{alpha}{Significance level. Default is 0.05.}

\item{TPM}{Logical. Use truncated product method (Zaykin et al., 2002). Default is FALSE.}

\item{tau}{Parameter for truncated product method. Default is 0.05.}

\item{est}{Estimator function. Default is tmean.}

\item{fr}{Span for running interval smoother. Default is 1.}

\item{...}{Additional arguments passed to test function.}
}
\value{
List with global test statistic, critical value, p-values, and design points.
}
\description{
ANCOVA for two independent groups with two covariates, using no parametric assumptions
about the regression surface. Uses deeply nested design points and combines p-values
across multiple locations.
}
\details{
Global test statistic is the average of p-values from tests at each design point.
With TPM=TRUE, uses truncated product method instead of averaging.
}
\examples{
\dontrun{
  x1 <- matrix(rnorm(100), 50, 2)
  y1 <- rowSums(x1) + rnorm(50)
  x2 <- matrix(rnorm(100), 50, 2)
  y2 <- rowSums(x2) + rnorm(50, 0.5)
  result <- ancov2COV(x1, y1, x2, y2)
}
}
\references{
Zaykin, D.V., Zhivotovsky, L.A., Westfall, P.H., & Weir, B.S. (2002).
Truncated product method for combining p-values. Genetic Epidemiology, 22, 170-185.
}
\keyword{ancova}
\keyword{multivariate}
