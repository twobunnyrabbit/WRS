% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{yuend.QS.ci}
\alias{yuend.QS.ci}
\title{Confidence Interval for Quantile Shift Effect Size (Dependent Groups)}
\usage{
yuend.QS.ci(
  x,
  y = NULL,
  tr = 0.2,
  alpha = 0.05,
  nboot = 1000,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{x}{Either a vector of observations for group 1, or a two-column matrix/data frame
where the first column is group 1 and the second is group 2.}

\item{y}{Vector of observations for group 2 (optional if \code{x} is a matrix).}

\item{tr}{Trimming proportion (default: 0.2 for 20\% trimming).}

\item{alpha}{Significance level for confidence interval (default: 0.05 for 95\% CI).}

\item{nboot}{Number of bootstrap samples (default: 1000).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducibility (default: \code{TRUE}).}

\item{...}{Additional arguments passed to \code{tmean}.}
}
\value{
A list with components:
\itemize{
\item \code{Q.effect}: The quantile shift effect size estimate
\item \code{ci}: Two-element vector with lower and upper confidence interval bounds
}
}
\description{
Computes a bootstrap confidence interval for the quantile shift measure of
effect size for dependent (paired) groups. The quantile shift is a robust
measure of how much one distribution is shifted relative to another.
}
\details{
This function uses the percentile bootstrap method to construct a confidence
interval for the quantile shift measure. The quantile shift (Q) is computed
using the \code{shiftes} function with trimmed means as the location estimator.

The bootstrap resamples pairs of observations (preserving the dependency
structure) and computes the quantile shift for each bootstrap sample. The
confidence interval is then obtained from the percentiles of the bootstrap
distribution.
}
\examples{
\dontrun{
# Generate paired data with a shift
set.seed(123)
x <- rnorm(30)
y <- x + rnorm(30, mean = 0.5)

# Compute quantile shift effect size with CI
result <- yuend.QS.ci(x, y, nboot = 1000)
print(result)
}
}
\seealso{
\code{\link{shiftes}}, \code{\link{yuend}}, \code{\link{tmean}}
}
