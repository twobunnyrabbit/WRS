% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/location.R
\name{cbmhd}
\alias{cbmhd}
\title{CI for Sum of Symmetric Quantiles of Difference (Independent Groups)}
\usage{
cbmhd(
  x,
  y,
  qest = hd,
  alpha = 0.05,
  q = 0.25,
  plotit = FALSE,
  pop = 0,
  fr = 0.8,
  rval = 15,
  xlab = "",
  ylab = "",
  nboot = 600,
  SEED = TRUE
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{qest}{Quantile estimator function (default: \code{hd}).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{q}{Quantile to estimate. For example, \code{q = 0.5} estimates the median,
\code{q = 0.25} estimates the first quartile (default: 0.5).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{pop, fr, rval, xlab, ylab}{Plot parameters (see \code{\link{Dqcihd}}).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}
}
\value{
A list with components:
\itemize{
\item q: The quantile used
\item Est1, Est2: The qth and (1-q)th quantile estimates
\item sum: Sum of Est1 and Est2
\item ci: Confidence interval for the sum
\item p.value: P-value for testing H0: sum = 0
}
}
\description{
Computes a confidence interval for the sum of the qth and (1-q)th quantiles
of D = X - Y for independent groups. Tests symmetry about zero.
}
\details{
If X and Y have identical distributions, D = X - Y is symmetric about zero,
so the qth and (1-q)th quantiles should sum to zero. This provides a test
of distributional equality based on specific quantiles.
}
\examples{
x <- rnorm(30)
y <- rnorm(30)
cbmhd(x, y, q = 0.25, nboot = 500)

}
\seealso{
\code{\link{cbmhdMC}} for parallel version,
\code{\link{Dcbmhd}} for dependent groups
}
