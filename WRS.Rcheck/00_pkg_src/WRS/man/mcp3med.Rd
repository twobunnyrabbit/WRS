% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcp.R
\name{mcp3med}
\alias{mcp3med}
\title{Three-Way Median-Based Multiple Comparisons}
\usage{
mcp3med(J, K, L, x, tr = 0.2, con = 0, alpha = 0.05, grp = NA, op = FALSE)
}
\arguments{
\item{J}{Number of levels for Factor A.}

\item{K}{Number of levels for Factor B.}

\item{L}{Number of levels for Factor C.}

\item{x}{Data in list mode or matrix. If matrix, columns correspond to groups.
Groups ordered as: (1,1,1), (1,1,2), ..., (1,1,L), (1,2,1), ..., (J,K,L).}

\item{tr}{Trim proportion (default: 0.2, currently unused - uses medians).}

\item{con}{Contrast matrix (default: 0 generates appropriate contrasts).}

\item{alpha}{Family-wise Type I error rate (default: 0.05).}

\item{grp}{Vector for reordering groups if data not in standard order (default: NA).}

\item{op}{Logical. If FALSE (default), return separate results for each effect.
If TRUE, combine all tests and return in \code{All.Tests}.}
}
\value{
A list with components:
\item{Factor.A}{Results for Factor A main effect (unless \code{op=TRUE}).}
\item{Factor.B}{Results for Factor B main effect (unless \code{op=TRUE}).}
\item{Factor.C}{Results for Factor C main effect (unless \code{op=TRUE}).}
\item{Factor.AB}{Results for A×B interaction (unless \code{op=TRUE}).}
\item{Factor.AC}{Results for A×C interaction (unless \code{op=TRUE}).}
\item{Factor.BC}{Results for B×C interaction (unless \code{op=TRUE}).}
\item{Factor.ABC}{Results for A×B×C interaction (unless \code{op=TRUE}).}
\item{All.Tests}{Combined results for all tests (only if \code{op=TRUE}).}
\item{conA, conB, conC, conAB, conAC, conBC, conABC}{Contrast matrices used.}
}
\description{
Performs all pairwise comparisons for main effects and interactions in a three-way
design using median-based methods. Tests Factor A, B, C main effects and all
two-way and three-way interactions.
}
\details{
This function uses \code{msmed} to perform median-based comparisons for all
main effects and interactions in a three-way design. Results are organized by
effect unless \code{op=TRUE}, which combines all tests into a single output.
}
\examples{
\dontrun{
# 2x2x2 design
set.seed(123)
x <- list(rnorm(20), rnorm(20), rnorm(20, mean=0.5), rnorm(20),
          rnorm(20), rnorm(20), rnorm(20), rnorm(20, mean=0.5))
result <- mcp3med(J=2, K=2, L=2, x=x)
print(result$Factor.A)
}
}
\seealso{
\code{\link{msmed}}, \code{\link{con3way}}, \code{\link{mcp3atm}}

Other MCP functions: 
\code{\link{bbbmcppb}()},
\code{\link{bbmcpEP}()},
\code{\link{bbmcpQS}()},
\code{\link{bbmcppb}()},
\code{\link{bwimcpAKP}()},
\code{\link{bwimcpES}()},
\code{\link{bwmcpAKP}()},
\code{\link{bwmcpORD}()},
\code{\link{bwmcpQS}()},
\code{\link{bwmedbmcp}()},
\code{\link{bwmedimcp}()},
\code{\link{esmcp}()},
\code{\link{linconMpb}()},
\code{\link{linconSpb}()},
\code{\link{mcpOV}()},
\code{\link{pbtrmcp}()},
\code{\link{rm3mcp}()},
\code{\link{wmcppb}()},
\code{\link{wwmcpQS}()},
\code{\link{wwmcpbt}()},
\code{\link{wwmcppb}()},
\code{\link{wwwmcpQS}()},
\code{\link{wwwmcppb}()}
}
\concept{MCP functions}
