% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{MCWB.crit}
\alias{MCWB.crit}
\title{Critical Value for Multiple Comparisons with the Best}
\usage{
MCWB.crit(n, alpha, SEED = TRUE, REPS = 5000, ...)
}
\arguments{
\item{n}{Vector of sample sizes for each group}

\item{alpha}{Familywise error rate (default: 0.05)}

\item{SEED}{Logical or numeric. Controls random seed. Default is \code{TRUE}.}

\item{REPS}{Number of simulation replications (default: 5000)}

\item{...}{Additional arguments (not currently used)}
}
\value{
Critical p-value (alpha quantile of minimum p-value distribution)
}
\description{
Computes the critical p-value for the MCB (Multiple Comparisons with the Best)
procedure via Monte Carlo simulation under the null hypothesis of equal trimmed
means across all groups.
}
\details{
For each simulation iteration:
\enumerate{
\item Generates J independent N(0,1) samples with specified sample sizes
\item Identifies the group with largest trimmed mean
\item Constructs contrasts comparing "best" with all others via \code{\link{conCON}}
\item Performs linear contrasts using \code{\link{lincon}}
\item Records the minimum p-value across all comparisons
}

Returns the alpha quantile of the distribution of minimum p-values, which
is used as the critical value for controlling familywise error rate in
\code{\link{MCWB}}.
}
\seealso{
\code{\link{MCWB}}, \code{\link{lincon}}, \code{\link{conCON}}
}
\keyword{htest}
