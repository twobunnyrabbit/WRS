% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{rm.margOM.es}
\alias{rm.margOM.es}
\title{Robust Marginal Effect Size Using One-Step M-Estimator}
\usage{
rm.margOM.es(x, y = NULL, locfun = onestep)
}
\arguments{
\item{x}{Either a two-column matrix/data frame or a vector (if y is specified).}

\item{y}{Optional second variable. If NULL, x must be a two-column matrix.}

\item{locfun}{Location estimator function (default: \code{onestep}).}
}
\value{
Numeric value: the robust marginal effect size estimate.
}
\description{
Computes a robust analog of the KMS effect size for two dependent groups using
a one-step M-estimator for location and percentage bend variance/correlation.
}
\details{
This function is similar to \code{\link{rm.marg.es}} but uses the percentage bend
measures for variance and correlation instead of winsorized versions. By default,
it uses the one-step M-estimator for location.

The effect size is: sqrt(2) * (L1 - L2) / sqrt(V1 + V2 - 2\emph{r}sqrt(V1)*sqrt(V2))
where L = location estimate (default: one-step M), V = percentage bend variance,
r = percentage bend correlation.

This provides an alternative robust effect size measure that may be more resistant
to outliers than the winsorized version.

Missing values are automatically removed.
}
\seealso{
\code{\link{rm.marg.es}}, \code{\link{rm.marg.esCI}},
\code{\link{onestep}}, \code{\link{pbvar}}, \code{\link{pbcor}}
}
\keyword{htest}
