% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{twoKlin}
\alias{twoKlin}
\title{Multiple Comparisons for K Independent Tests (Linear Contrasts/Trimmed Means)}
\usage{
twoKlin(
  x = NULL,
  x1 = NULL,
  x2 = NULL,
  tr = 0.2,
  alpha = 0.05,
  pr = TRUE,
  opt = 1
)
}
\arguments{
\item{x}{Data in matrix or list format (optional if \code{x1} and \code{x2} provided).
If matrix: 2K columns (K pairs). If list: length 2K.}

\item{x1}{Data for group 1 (matrix with K columns or list of length K).}

\item{x2}{Data for group 2 (matrix with K columns or list of length K).}

\item{tr}{Trimming proportion (default: 0.2).}

\item{alpha}{Significance level (default: 0.05).}

\item{pr}{Logical. If \code{TRUE}, prints results (default: \code{TRUE}).}

\item{opt}{Integer option for data format (default: 1).}
}
\value{
A list with components:
\itemize{
\item \code{pvalues}: Vector of p-values from Yuen's test for each variable
\item \code{adj.pvalues}: Adjusted p-values using Hochberg's method
\item \code{num.sig}: Number of significant tests
\item \code{test.stats}: Test statistics for each comparison
}
}
\description{
Performs a step-down multiple comparisons procedure for K independent tests
comparing trimmed means. Uses the Fisher method combined with Hochberg's
adjustment. Requires that the tests be independent.
}
\details{
This function performs Yuen's test (trimmed mean comparison) for each of K
independent variables, then combines the p-values using the Fisher method
and adjusts for multiple comparisons using Hochberg's procedure.

The tests MUST be independent for this method to be valid. This is a
specialized version of \code{twoKgen} specifically for trimmed mean comparisons.
}
\examples{
\dontrun{
# Compare two groups on multiple independent variables
set.seed(123)
x1 <- matrix(rnorm(100), ncol = 5)
x2 <- matrix(rnorm(120, mean = 0.3), ncol = 5)

# Apply Yuen's test to each variable
result <- twoKlin(x1 = x1, x2 = x2, tr = 0.2)
print(result)
}
}
\seealso{
\code{\link{twoKgen}}, \code{\link{yuen}}
}
