% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{iband}
\alias{iband}
\title{Compare Quantiles of Difference Scores in 2×2 Design}
\usage{
iband(
  x,
  alpha = 0.05,
  q = c(0.1, 0.25, 0.5, 0.75, 0.9),
  method = "BH",
  SW = FALSE,
  plotit = FALSE,
  SEED = TRUE,
  nboot = 500,
  grp = c(1:4),
  xlab = "X"
)
}
\arguments{
\item{x}{Data in matrix, data frame, or list format with 4 groups/variables.}

\item{alpha}{Significance level (default: 0.05).}

\item{q}{Vector of quantiles to compare (default: c(0.1, 0.25, 0.5, 0.75, 0.9)).}

\item{method}{P-value adjustment method (default: 'BH' for Benjamini-Hochberg).
See \code{\link{p.adjust}} for options.}

\item{SW}{Logical. If \code{TRUE}, switches rows and columns in the design
(default: FALSE).}

\item{plotit}{Logical. If \code{TRUE}, creates a plot of the results (default: FALSE).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed to 2 for reproducibility.}

\item{nboot}{Number of bootstrap samples (default: 500).}

\item{grp}{Vector specifying which 4 groups to use (default: c(1:4) for
first 4 groups/columns).}

\item{xlab}{Label for x-axis in plot (default: 'X').}
}
\value{
A list with results from the quantile comparisons, including:
\item{quantiles}{The quantiles that were compared}
\item{ci}{Confidence intervals for quantile differences}
\item{p.value}{P-values (possibly adjusted)}
}
\description{
For a 2×2 design, compares the quantiles of two difference score distributions
using a percentile bootstrap approach. Tests whether the quantiles of
d1 = x1 - x2 differ from those of d2 = x3 - x4.
}
\details{
This function is designed for 2×2 within-subjects or mixed designs. It
compares the quantile functions of two difference score distributions:
\itemize{
\item \strong{d1}: Difference between conditions 1 and 2 (x1 - x2)
\item \strong{d2}: Difference between conditions 3 and 4 (x3 - x4)
}

\strong{Test Procedure}:
\enumerate{
\item Compute difference scores d1 and d2 for each subject
\item Estimate specified quantiles for both distributions
\item Bootstrap: resample subjects and recompute quantile differences
\item Construct confidence intervals for quantile differences
\item Adjust p-values for multiple comparisons across quantiles
}

This approach is useful for examining interaction effects at different
points in the distribution (not just the mean).

\strong{Note}: \code{SW = TRUE} switches the role of rows and columns, effectively
changing which variables are differenced.
}
\examples{
\dontrun{
# 2×2 within-subjects design
# 4 conditions measured on each subject
data <- matrix(rnorm(200), 50, 4)
data[,3] <- data[,3] + 0.5  # Add shift to condition 3

# Compare quantiles of (x1-x2) vs (x3-x4)
result <- iband(data, alpha = 0.05, plotit = TRUE)
}

}
\seealso{
\code{\link{Dqcomhd}}, \code{\link{qcomhd}}, \code{\link{sband}}
}
\keyword{htest}
\keyword{robust}
