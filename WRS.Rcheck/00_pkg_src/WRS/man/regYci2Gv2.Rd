% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression-advanced.R
\name{regYci2Gv2}
\alias{regYci2Gv2}
\title{Two-Group Comparison of Regression Predictions (ANCOVA-Style)}
\usage{
regYci2Gv2(
  x1,
  y1,
  x2,
  y2,
  regfun = tsreg,
  pts = NULL,
  ALL = FALSE,
  npts = 25,
  plotit = TRUE,
  SCAT = TRUE,
  pch1 = "*",
  pch2 = "+",
  nboot = 100,
  ADJ = FALSE,
  xout = FALSE,
  outfun = outpro,
  SEED = TRUE,
  p.crit = 0.015,
  alpha = 0.05,
  crit = NULL,
  null.value = 0,
  plotPV = FALSE,
  scale = TRUE,
  span = 0.75,
  xlab = "X",
  xlab1 = "X1",
  xlab2 = "X2",
  ylab = "p-values",
  ylab2 = "Y",
  theta = 50,
  phi = 25,
  MC = FALSE,
  nreps = 1000,
  pch = "*",
  ...
)
}
\arguments{
\item{x1, y1}{Numeric vectors containing predictor and response for group 1.}

\item{x2, y2}{Numeric vectors containing predictor and response for group 2.}

\item{regfun}{Regression function to use (default: \code{tsreg} for Theil-Sen).}

\item{pts}{Covariate values at which to compare groups. If \code{NULL}, uses
either all unique covariate values (\code{ALL=TRUE}) or evenly spaced points
(\code{ALL=FALSE}).}

\item{ALL}{Logical. If \code{TRUE}, evaluates at all unique covariate values;
if \code{FALSE}, uses \code{npts} evenly spaced points (default: \code{FALSE}).}

\item{npts}{Number of evenly spaced evaluation points when \code{ALL=FALSE}
(default: 25).}

\item{plotit}{Logical. If \code{TRUE}, plots data and fitted regression lines
for both groups (default: \code{TRUE}).}

\item{SCAT}{Logical. If \code{TRUE}, includes scatterplot of data points;
if \code{FALSE}, shows only regression lines (default: \code{TRUE}).}

\item{pch1, pch2}{Plot characters for groups 1 and 2 (defaults: '*', '+').}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{ADJ}{Logical. If \code{TRUE}, adjusts critical values so that the simultaneous
probability coverage is 1-alpha (i.e., creates a confidence band). If \code{FALSE},
computes pointwise confidence intervals (default: \code{FALSE}).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{p.crit}{Critical p-value for simultaneous testing when \code{ADJ=TRUE}
(default: 0.015).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{crit}{Optional user-specified critical value. If \code{NULL}, computed from
bootstrap (default: \code{NULL}).}

\item{null.value}{Null hypothesis value for testing (default: 0 for most
tests, indicating no difference or no correlation).}

\item{plotPV}{Logical. If \code{TRUE}, plots p-values vs. predictor values
(default: \code{FALSE}).}

\item{scale, span, theta, phi, pch}{Parameters for plotting.}

\item{ylab2}{Y-axis label for main plot (default: 'Y').}

\item{MC}{Logical. If \code{TRUE}, uses parallel processing via \code{mclapply}
(default: \code{FALSE}).}

\item{nreps}{Number of replications for computing adjusted critical value
(default: 1000).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
A list with components:
\item{output}{Matrix with columns: X, Est.Dif (estimated difference),
Lower.ci, Upper.ci, p.value}
\item{p.crit}{Critical p-value used for testing.}
\item{crit.value}{Critical value used for confidence intervals.}
\item{num.sig}{Number of covariate values where groups differ significantly.}
}
\description{
Computes confidence intervals for the difference between predicted Y values
from two independent groups at specified covariate values, providing an
ANCOVA-style analysis using robust regression methods.
}
\details{
This function performs ANCOVA-style analysis by comparing regression functions
between two groups:
\enumerate{
\item Fits robust regression for each group separately
\item At each covariate value, estimates the difference in predicted Y
\item Computes bootstrap confidence intervals for the difference
\item Optionally adjusts for simultaneous testing across multiple covariate values
}

With \code{ADJ=TRUE}, the critical value is adjusted so that the overall family-wise
error rate is controlled at level alpha. This adjustment uses a method that
typically has better power than Hochberg or Hommel adjustments.

For \code{regfun=tsreg} and \code{alpha=0.05} with single covariate, a pre-computed
adjustment is used (fast). Otherwise, the critical value is computed via
simulation (can be slow; use \code{MC=TRUE} to speed up with multicore processing).
}
\note{
This function is identical to \code{anclin()}. Currently restricted to
single covariate (p=1) models.
}
\examples{
\dontrun{
# Compare two groups with ANCOVA-style analysis
set.seed(123)
x1 <- rnorm(50)
y1 <- 2*x1 + rnorm(50)
x2 <- rnorm(50)
y2 <- 2*x2 + 1 + rnorm(50)  # Same slope, different intercept

# Pointwise comparisons
result <- regYci2Gv2(x1, y1, x2, y2)

# Simultaneous confidence band
result <- regYci2Gv2(x1, y1, x2, y2, ADJ=TRUE)
}
}
\references{
Wilcox, R.R. (2022). Introduction to Robust Estimation and Hypothesis Testing
(5th ed.). Academic Press.
}
\seealso{
\code{\link{regYci}}, \code{\link{ancJN}}, \code{\link{regYband}}
}
