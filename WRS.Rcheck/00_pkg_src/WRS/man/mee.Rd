% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{mee}
\alias{mee}
\title{Mee's Confidence Interval for P(X < Y)}
\usage{
mee(x, y, alpha = 0.05)
}
\arguments{
\item{x}{Numeric vector for first group.}

\item{y}{Numeric vector for second group.}

\item{alpha}{Alpha level for confidence interval. Default is 0.05.}
}
\value{
List with components:
\item{phat}{Estimated probability \eqn{P(X < Y)}}
\item{ci}{Confidence interval (vector of length 2)}
}
\description{
Compute a confidence interval for the probability \eqn{p = P(X < Y)} for two
independent groups using Mee's method. Assumes no ties in the data.
}
\details{
Mee's method computes a confidence interval for the probability that a randomly
selected observation from group X is less than a randomly selected observation
from group Y. The method is based on the Mann-Whitney U statistic.

Important assumptions and warnings:
\itemize{
\item The method assumes NO TIES in the pooled data. If ties are detected,
a warning is printed.
\item With ties, Type I error may exceed the nominal level.
\item Results may differ depending on order: \code{mee(x,y)} may give different
results than \code{mee(y,x)}.
\item When distributions are identical, \eqn{P(X < Y)} is not necessarily 0.5
if ties are present.
}

Missing values are automatically removed.

The interval is computed using an effective sample size adjustment based on
within-group correlation structure.
}
\references{
Mee, R. W. (1990). Confidence intervals for probabilities and tolerance regions
based on a generalization of the Mann-Whitney statistic. \emph{Journal of the
American Statistical Association}, \emph{85}(411), 793-800.
}
\seealso{
\code{\link{wmw}}, \code{\link{cidv2}}, \code{\link{meemul}}
}
\keyword{htest}
