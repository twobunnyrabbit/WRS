% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{mgvdep}
\alias{mgvdep}
\title{Multivariate Generalized Variance Depth}
\usage{
mgvdep(m, se = FALSE)
}
\arguments{
\item{m}{Matrix of numeric data (N by p).}

\item{se}{Logical. If \code{TRUE}, uses skipped Euclidean distances. Default is \code{FALSE}.}
}
\value{
Numeric vector of depth values between 0 and 1, one per row of \code{m}.
Deepest points have values closest to 1.
}
\description{
Computes MGV (multivariate generalized variance) depth values for each point.
Unlike \code{\link{mgvar}}, this function follows the convention that deepest
points have the largest numerical values (depth = 1).
}
\details{
This function is similar to \code{\link{mgvar}} but returns depth values instead
of distances. The deepest (most central) points have depth equal to 1, while
outlying points have smaller depth values.

The depth is computed as \code{1/(1 + gvar)}, where \code{gvar} is the
generalized variance contribution of each point.
}
\seealso{
\code{\link{mgvar}}, \code{\link{gvar}}, \code{\link{apgdis}}
}
\keyword{multivariate}
\keyword{robust}
