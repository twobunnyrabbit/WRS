% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{akerdcdf}
\alias{akerdcdf}
\title{Adaptive Kernel Density CDF Estimate}
\usage{
akerdcdf(
  xx,
  hval = NA,
  aval = 0.5,
  op = 1,
  fr = 0.8,
  pyhat = TRUE,
  pts = 0,
  plotit = FALSE,
  xlab = "",
  ylab = ""
)
}
\arguments{
\item{xx}{Vector or matrix of data values. For multivariate data, uses \code{\link{akerdmul}}.}

\item{hval}{Bandwidth parameter. If \code{NA} (default), automatically selected using
Silverman's rule of thumb.}

\item{aval}{Sensitivity parameter for adaptive kernel (default: 0.5). Controls
local adaptation of bandwidth.}

\item{op}{Method for initial density estimate (univariate only):
\itemize{
\item 1 (default): Use expected frequency via \code{\link{rdplot}}
\item 2: Use normal kernel via \code{\link[stats]{density}}
}}

\item{fr}{Span parameter for initial density estimate when \code{op=1} (default: 0.8).}

\item{pyhat}{Logical. If \code{TRUE} (default), returns density values; if \code{FALSE}, returns "Done".}

\item{pts}{Points at which to evaluate CDF. If \code{NA}, uses data points (default: 0).}

\item{plotit}{Logical. If \code{TRUE}, plots the CDF curve.}

\item{xlab}{Label for x-axis (default: "").}

\item{ylab}{Label for y-axis (default: "").}
}
\value{
If \code{pyhat=TRUE}, returns vector of CDF values at specified points.
If \code{pyhat=FALSE}, returns character string "Done".
For multivariate data, returns result from \code{\link{akerdmul}}.
}
\description{
Computes cumulative distribution function using adaptive kernel density estimation
for univariate or multivariate data (Silverman, 1986). By default, determines
\eqn{P(X \le pts)} where pts=0.
}
\details{
Uses Epanechnikov kernel with adaptive bandwidth. The adaptive approach allows
bandwidth to vary locally based on density:
\itemize{
\item Narrow bandwidth in high-density regions (better resolution)
\item Wide bandwidth in low-density regions (more smoothing)
}

Bandwidth selection (when \code{hval=NA}):
\deqn{h = 1.06 \times A \times n^{-0.2}}
where \eqn{A = \min(\sigma, IQR/1.34)} and falls back to Winsorized SD if needed.
}
\references{
Silverman, B. W. (1986). Density Estimation for Statistics and Data Analysis.
Chapman & Hall.
}
\seealso{
\code{\link{akerdmul}}, \code{\link{rdplot}}, \code{\link[stats]{density}}
}
\keyword{distribution}
\keyword{nonparametric}
\keyword{smooth}
