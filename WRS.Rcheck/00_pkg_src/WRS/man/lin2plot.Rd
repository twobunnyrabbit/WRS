% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{lin2plot}
\alias{lin2plot}
\title{Plot Two Distributions Based on Positive and Negative Contrast Coefficients}
\usage{
lin2plot(x, con, op = 4, nboot = 800, plotit = TRUE, pyhat = FALSE)
}
\arguments{
\item{x}{Data in list mode or matrix mode. Each element/column represents a group.}

\item{con}{Contrast coefficients (vector of length J, required). Positive coefficients
define one distribution, negative coefficients define the other.}

\item{op}{Density estimation method for plotting (default: 4, adaptive kernel estimator).
See \code{\link{g2plot}} for options.}

\item{nboot}{Number of bootstrap samples (default: 800).}

\item{plotit}{Logical. Create plot if TRUE (default: TRUE).}

\item{pyhat}{Logical. Passed to plotting function for additional output (default: FALSE).}
}
\value{
Output from \code{g2plot} containing density estimates for both distributions.
}
\description{
Plots two distributions: one for the linear contrast of groups with positive
coefficients and one for groups with negative coefficients. Useful for visualizing
the two sides of a contrast separately.
}
\details{
This function separates a contrast into two parts based on the sign of coefficients:
\itemize{
\item \strong{Distribution 1}: Sum of groups with positive coefficients (ci > 0)
\item \strong{Distribution 2}: Sum of groups with negative coefficients (ci < 0)
}

This is particularly useful for understanding contrasts where you're comparing
one set of groups against another (e.g., treatment vs. control groups).

\strong{Example contrast}: c = (1, 1, -1, -1) compares the sum of groups 1 & 2 against
the sum of groups 3 & 4. This function plots:
\itemize{
\item Distribution of X1 + X2
\item Distribution of X3 + X4
}

The two distributions are plotted using \code{g2plot} with overlaid density estimates.
Missing values are automatically removed.

\strong{Note}: The con argument is required and must have both positive and negative values.
}
\examples{
# Compare two sets of groups
set.seed(123)
x1 <- rnorm(30, mean = 0)
x2 <- rnorm(30, mean = 0.5)
x3 <- rnorm(30, mean = 1)
x4 <- rnorm(30, mean = 1.5)
x <- list(x1, x2, x3, x4)

# Compare (X1 + X2) vs (X3 + X4)
lin2plot(x, con = c(1, 1, -1, -1))
}
\seealso{
\code{\link{linplot}}, \code{\link{g2plot}}, \code{\link{lincon}}
}
