% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{best.PB}
\alias{best.PB}
\title{Determine Group with Largest Location Estimate via Bootstrap}
\usage{
best.PB(x, est = tmean, nboot = 1000, SEED = TRUE, ...)
}
\arguments{
\item{x}{Data in matrix, data frame, or list mode. Columns (or list elements)
represent groups.}

\item{est}{Estimator function for location. Default is \code{\link{tmean}}
(trimmed mean). Other options: \code{median}, \code{mean}, \code{onestep}.}

\item{nboot}{Number of bootstrap samples. Default is 1000.}

\item{SEED}{Logical. If \code{TRUE} (default), sets random seed for reproducibility.}

\item{...}{Additional arguments passed to \code{est}.}
}
\value{
List with components:
\itemize{
\item \code{n} - Sample sizes for each group
\item \code{Est} - Location estimates for each group
\item \code{p.value} - Two-sided pseudo p-value
}
}
\description{
Uses percentile bootstrap to test whether it is reasonable to decide which of
J independent groups has the largest measure of location. Returns a pseudo
p-value based on the proportion of bootstrap samples where the initially
identified group remains the largest.
}
\details{
This function:
\enumerate{
\item Computes location estimate for each group
\item Identifies which group has the largest estimate
\item Takes \code{nboot} bootstrap samples (sampling with replacement within each group)
\item Counts how often the initially identified group remains largest
\item Returns two-sided pseudo p-value: \code{2*min(prop, 1-prop)}
}

A small p-value suggests it is reasonable to conclude that the identified group
has the largest location measure. A large p-value indicates the decision is
unreliable due to sampling variability.

Missing values are automatically removed.
}
\seealso{
\code{\link{best.DO}}, \code{\link{best.cell.DO}}, \code{\link{rmbestPB.DO}}
}
\keyword{bootstrap}
\keyword{htest}
\keyword{robust}
