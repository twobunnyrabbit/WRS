% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{rplotN}
\alias{rplotN}
\title{Running Interval Smoother for Large Datasets}
\usage{
rplotN(
  x,
  y,
  nsub = 5000,
  est = tmean,
  fr = 1,
  xout = FALSE,
  xlab = "X",
  ylab = "Y",
  zlab = "",
  ticktype = "simple",
  theta = 50,
  phi = 25,
  scale = TRUE,
  expand = 0.5,
  SEED = TRUE,
  frame = TRUE
)
}
\arguments{
\item{x}{Numeric vector or matrix of predictor values.}

\item{y}{Numeric vector of response values.}

\item{nsub}{Size of random subsample for prediction points (default: 5000).
Actual subsample size is \code{min(n, nsub)} where n is the sample size.}

\item{est}{Measure of location for the smoother (default: \code{tmean}).}

\item{fr}{Span for running interval (default: 1).}

\item{xout}{Logical; if \code{TRUE}, removes outliers using projection depth.}

\item{xlab}{Label for x-axis (default: "X").}

\item{ylab}{Label for y-axis (default: "Y").}

\item{zlab}{Label for z-axis (default: "").}

\item{ticktype}{Tick mark type for 3D plots (default: "simple").}

\item{theta}{Rotation angle for 3D plots (default: 50).}

\item{phi}{Colatitude angle for 3D plots (default: 25).}

\item{scale}{Logical; if \code{TRUE} (default), scales axes.}

\item{expand}{Expansion factor for 3D plots (default: 0.5).}

\item{SEED}{Logical; if \code{TRUE} (default), sets random seed for reproducibility.}

\item{frame}{Logical; if \code{TRUE} (default), draws frame around plot.}
}
\value{
Invisibly returns the result from \code{lplot()}.
}
\description{
Efficient running interval smoother designed for large sample sizes.
Plots the regression surface without a scatter plot by using a random
subsample for prediction points.
}
\details{
This function is designed for situations with large sample sizes where
plotting all points is impractical. It:
\enumerate{
\item Randomly selects \code{nsub} points to serve as prediction locations
\item Uses all data to fit the running interval smoother
\item Predicts at the \code{nsub} points
\item Plots the smooth surface using \code{lplot()}
}

For multivariate predictors with outlier removal, uses \code{outpro.depth()}.
}
\examples{
# Large dataset example
set.seed(123)
n <- 10000
x <- rnorm(n)
y <- sin(x) + rnorm(n, sd=0.3)
rplotN(x, y, nsub=1000)
}
\seealso{
\code{\link{rplot}}, \code{\link{rplot.pred}}, \code{\link{lplot}}
}
