% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{tworhobt}
\alias{tworhobt}
\title{Bootstrap-t Test for Comparing Two Independent Correlations}
\usage{
tworhobt(X1, Y1, X2, Y2, alpha = 0.05, nboot = 499, SEED = TRUE)
}
\arguments{
\item{X1}{First variable for group 1.}

\item{Y1}{Second variable for group 1.}

\item{X2}{First variable for group 2.}

\item{Y2}{Second variable for group 2.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}
}
\value{
A list with components:
\itemize{
\item \code{test}: Test statistic
\item \code{crit.val}: Bootstrap critical values
\item \code{p.value}: P-value for the test
}
}
\description{
Compares two independent correlations using a bootstrap-t method with the
HC4 heteroscedasticity-consistent estimator.
}
\details{
Uses a bootstrap-t approach with the HC4 estimator to compare correlations
between two independent groups. This method provides better control of Type I
error rates than methods based on Fisher's z-transformation, especially with
non-normal data.

The function standardizes the variables before computing correlations and
uses the HC4 method for robust standard error estimation.
}
\examples{
\dontrun{
set.seed(123)
X1 <- rnorm(50)
Y1 <- 0.5 * X1 + rnorm(50)
X2 <- rnorm(60)
Y2 <- 0.3 * X2 + rnorm(60)

result <- tworhobt(X1, Y1, X2, Y2)
print(result)
}
}
\seealso{
\code{\link{twopcor}}, \code{\link{twohc4cor}}
}
