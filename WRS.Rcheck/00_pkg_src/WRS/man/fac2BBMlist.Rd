% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{fac2BBMlist}
\alias{fac2BBMlist}
\title{Convert Two-Way Data to List Format for MANOVA}
\usage{
fac2BBMlist(x, grp.col, lev.col, pr = TRUE)
}
\arguments{
\item{x}{Matrix or data frame containing the data}

\item{grp.col}{Vector of length 2: column indices for the two grouping factors}

\item{lev.col}{Vector: column indices containing the p dependent variables}

\item{pr}{Logical: print grouping information (default: TRUE)}
}
\value{
List of length J×K (where J and K are the number of levels of the two factors).
Each element is a matrix with p columns containing the multivariate observations
for that factor combination.
}
\description{
Reorganizes data from a matrix or data frame into list format for two-way
multivariate analysis. Each list element contains p-variate data for one
combination of factor levels.
}
\details{
This function is useful for two-way MANOVA designs. It takes data stored in
a single matrix/data frame and creates groups based on two factor columns.

\strong{Example}: \code{z=fac2BBMlist(plasma,c(2,3),c(7,8))} creates groups based on
values in columns 2 (Factor A) and 3 (Factor B). \code{z[[1]]} contains a matrix
with two columns, with data taken from columns 7 and 8 of plasma.
}
\seealso{
\code{\link{fac2Mlist}}, \code{\link{selbybbw}}
}
\keyword{manip}
\keyword{multivariate}
