% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{ancbootg}
\alias{ancbootg}
\title{Bootstrap ANCOVA at Specified Design Points}
\usage{
ancbootg(x1, y1, x2, y2, pts, fr1 = 1, fr2 = 1, tr = 0.2, nboot = 599)
}
\arguments{
\item{x1}{Covariate values for group 1.}

\item{y1}{Response values for group 1.}

\item{x2}{Covariate values for group 2.}

\item{y2}{Response values for group 2.}

\item{pts}{Vector of design points at which to make comparisons (required).}

\item{fr1}{Span for running interval smoother in group 1 (default: 1).}

\item{fr2}{Span for running interval smoother in group 2 (default: 1).}

\item{tr}{Trimming proportion (default: 0.2). Proportion of observations to
trim from each tail when computing winsorized correlation.}

\item{nboot}{Number of bootstrap samples (default varies by function).}
}
\value{
A list with components:
\item{output}{Matrix with columns: X (design point), n1, n2, DIF (difference),
TEST (test statistic), se (standard error), ci.low, ci.hi}
\item{crit}{Critical value used for the bootstrap test}
}
\description{
Compares two independent groups using robust ANCOVA at user-specified
design points. Uses running interval smoothers without parametric assumptions.
}
\details{
This function is similar to \code{ancboot} but requires the user to specify the
design points for comparison. It uses running interval smoothers to estimate
conditional means at each design point.

The function does not perform automatic design point selection; the user
must ensure that adequate sample sizes exist within the span at each point.
}
\examples{
# Two groups with covariate
x1 <- runif(50, 0, 10)
y1 <- 2*x1 + rnorm(50, 0, 2)
x2 <- runif(50, 0, 10)
y2 <- 2.5*x2 + rnorm(50, 0, 2)
# Compare at specific design points
ancbootg(x1, y1, x2, y2, pts=c(2, 5, 8), nboot=500)
}
\seealso{
\code{\link{ancboot}}, \code{\link{linconb}}
}
