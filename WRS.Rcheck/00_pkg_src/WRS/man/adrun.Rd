% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{adrun}
\alias{adrun}
\title{Additive Model via Running Interval Smoother}
\usage{
adrun(
  x,
  y,
  est = tmean,
  iter = 10,
  pyhat = FALSE,
  plotit = TRUE,
  fr = 1,
  xlab = "X",
  ylab = "Y",
  zlab = "",
  theta = 50,
  phi = 25,
  expand = 0.5,
  scale = TRUE,
  zscale = TRUE,
  xout = FALSE,
  eout = xout,
  outfun = out,
  ticktype = "simple",
  ...
)
}
\arguments{
\item{x}{Matrix of predictor variables (2 or more columns)}

\item{y}{Response vector}

\item{est}{Robust estimator function for smoothing (default: \code{\link{tmean}})}

\item{iter}{Maximum number of backfitting iterations (default: 10)}

\item{pyhat}{Logical. If \code{TRUE}, returns fitted values instead of plotting
(default: \code{FALSE})}

\item{plotit}{Logical. If \code{TRUE} (default) and p=2, creates 3D plot of fitted surface}

\item{fr}{Span for running interval smoother (default: 1)}

\item{xlab}{Label for x-axis (default: 'X')}

\item{ylab}{Label for y-axis (default: 'Y')}

\item{zlab}{Label for z-axis (default: '')}

\item{theta}{Azimuthal viewing angle for 3D plot (default: 50)}

\item{phi}{Colatitude viewing angle for 3D plot (default: 25)}

\item{expand}{Expansion factor for z-axis (default: 0.5)}

\item{scale}{Logical. Scale x, y, z to unit box for 3D plot (default: \code{TRUE})}

\item{zscale}{Logical. Scale z-axis separately (default: \code{TRUE})}

\item{xout}{Logical. Remove multivariate outliers before fitting (default: \code{FALSE})}

\item{eout}{Logical. Remove outliers in residuals (default: same as \code{xout})}

\item{outfun}{Outlier detection function (default: \code{\link{out}})}

\item{ticktype}{Type of axis tickmarks for 3D plot (default: 'simple')}

\item{...}{Additional arguments passed to estimator function}
}
\value{
If \code{pyhat=TRUE}, returns vector of fitted values. Otherwise, creates plot
(for 2 predictors only) and returns fitted values invisibly.
}
\description{
Fits a generalized additive model using running interval smoothers and the
backfitting algorithm. Models E(Y|X) as sum of smooth functions: f₁(X₁) + ... + fₚ(Xₚ).
}
\details{
The backfitting algorithm iteratively:
\enumerate{
\item Initializes: f_j(X_j) = 0 for all j
\item For each predictor j: computes partial residuals Y - Σ_{k≠j} f_k(X_k)
\item Smooths partial residuals vs X_j using running interval smoother
\item Repeats until convergence or \code{iter} iterations
}

The running interval smoother uses \code{\link{runhat}} with robust estimator
\code{est}. For prediction at x₀, averages Y values where X is within distance
\code{fr × range(X)} from x₀.
}
\references{
Hastie, T. J., & Tibshirani, R. J. (1990). Generalized Additive Models.
Chapman and Hall/CRC.
}
\seealso{
\code{\link{runhat}}, \code{\link{adtest}}, \code{\link{adcom}},
\code{\link{adrunl}}, \code{\link{runm3d}}
}
\keyword{models}
\keyword{regression}
\keyword{smooth}
