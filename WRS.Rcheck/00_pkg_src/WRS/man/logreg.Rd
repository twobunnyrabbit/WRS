% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression-advanced.R
\name{logreg}
\alias{logreg}
\title{Logistic Regression with Outlier Detection}
\usage{
logreg(
  x,
  y,
  xout = FALSE,
  outfun = outpro,
  plotit = FALSE,
  POLY = FALSE,
  xlab = "X",
  ylab = "Y",
  zlab = "",
  scale = TRUE,
  expand = 0.5,
  theta = 50,
  phi = 25,
  duplicate = "error",
  ticktype = "simple",
  ...
)
}
\arguments{
\item{x}{A numeric matrix or vector containing predictor values. Can have
multiple columns for multiple predictors.}

\item{y}{A binary numeric vector (coded as 0/1) or any binary variable that
will be converted to 0/1.}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{POLY}{Logical. If \code{TRUE} and p=1, assumes columns 2, 3, etc. of x
contain transformations of column 1 (e.g., x^2, x^3) for polynomial
regression (default: \code{FALSE}).}

\item{xlab, ylab, zlab}{Axis labels for plots.}

\item{scale}{Logical. If \code{TRUE}, uses scaling in 3D plots (default: \code{TRUE}).}

\item{expand, theta, phi, ticktype}{Parameters for 3D perspective plots when p=2.}

\item{duplicate}{How to handle duplicate points in 2D plots. Options: "error"
(default), "strip", "mean", etc. Passed to \code{interp()}.}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
A matrix with coefficient estimates, standard errors, z-statistics,
p-values, and adjusted p-values (using Hochberg's method for multiple
slopes). The first row is the intercept, subsequent rows are slopes.
}
\description{
Performs logistic regression for binary outcomes, with optional outlier
removal from the predictor space and visualization of the fitted model.
}
\details{
This function fits a logistic regression model using \code{glm()} with optional
robust outlier detection in the predictor space before fitting.

If \code{xout = TRUE}, outliers are removed from x before fitting using the
function specified by \code{outfun} (default is projection-based outlier
detection via \code{outpro}).

For visualization:
\itemize{
\item With 1 predictor: plots fitted probabilities vs. x
\item With 2 predictors: creates 3D perspective plot of fitted surface
\item Use \code{POLY=TRUE} for polynomial models with transformed predictors
}
}
\examples{
\dontrun{
# Simple logistic regression
set.seed(123)
x <- rnorm(100)
p <- exp(1 + 2*x) / (1 + exp(1 + 2*x))
y <- rbinom(100, 1, p)
logreg(x, y, plotit = TRUE)
}
}
\references{
Wilcox, R.R. (2022). Introduction to Robust Estimation and Hypothesis Testing
(5th ed.). Academic Press.
}
\seealso{
\code{\link{logreg.P.ci}}, \code{\link{logreg.pred}},
\code{\link{wlogreg}}
}
