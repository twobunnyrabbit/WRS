% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{ancovaV2}
\alias{ancovaV2}
\title{Bootstrap ANCOVA with Unconditional Resampling (Version 2)}
\usage{
ancovaV2(
  x1 = NULL,
  y1 = NULL,
  x2 = NULL,
  y2 = NULL,
  fr1 = 1,
  fr2 = 1,
  p.crit = NULL,
  padj = TRUE,
  pr = TRUE,
  method = "hochberg",
  FAST = TRUE,
  est = tmean,
  alpha = 0.05,
  plotit = TRUE,
  xlab = "X",
  ylab = "Y",
  qpts = FALSE,
  qvals = c(0.25, 0.5, 0.75),
  sm = FALSE,
  xout = FALSE,
  eout = FALSE,
  outfun = out,
  LP = TRUE,
  nboot = 500,
  SEED = TRUE,
  nreps = 2000,
  MC = FALSE,
  nmin = 12,
  q = 0.5,
  SCAT = TRUE,
  pch1 = "*",
  pch2 = "+",
  ...
)
}
\arguments{
\item{x1}{Covariate values for group 1}

\item{y1}{Response values for group 1}

\item{x2}{Covariate values for group 2}

\item{y2}{Response values for group 2}

\item{fr1}{Span parameter for group 1 smoother (default: 1)}

\item{fr2}{Span parameter for group 2 smoother (default: 1)}

\item{p.crit}{Critical p-value for significance. If NULL, determined via simulation
or fast approximation (default: NULL)}

\item{padj}{Logical; if TRUE, use adjusted p-values via Hochberg/Hommel; if FALSE,
use critical p-value approach for better power (default: TRUE)}

\item{pr}{Logical; if TRUE, print messages (default: TRUE)}

\item{method}{P-value adjustment method: 'hochberg' or 'hommel' (default: 'hochberg')}

\item{FAST}{Logical; if TRUE and alpha=.05, use fast approximation for p.crit;
if FALSE, compute via simulation (default: TRUE)}

\item{est}{Estimator function to use (default: tmean for trimmed mean)}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{plotit}{Logical; if TRUE, plot running smoothers (default: TRUE)}

\item{xlab}{Label for x-axis (default: 'X')}

\item{ylab}{Label for y-axis (default: 'Y')}

\item{qpts}{Logical; if TRUE, use quantiles of x1 as design points; if FALSE,
use ancova() default points (default: FALSE)}

\item{qvals}{Quantile values for design points when qpts=TRUE (default: c(.25,.5,.75))}

\item{sm}{Logical; if TRUE, use bootstrap bagging for smoother curves (default: FALSE)}

\item{xout}{Logical; if TRUE, remove outliers in x before analysis (default: FALSE)}

\item{eout}{Logical; if TRUE, eliminate all outliers when plotting (default: FALSE)}

\item{outfun}{Function for detecting outliers (default: out)}

\item{LP}{Logical; if TRUE, smooth running interval smoother again using lplot (default: TRUE)}

\item{nboot}{Number of bootstrap samples (default: 500)}

\item{SEED}{Logical; if TRUE, set seed for reproducibility (default: TRUE)}

\item{nreps}{Number of replications for determining critical p-value (default: 2000)}

\item{MC}{Logical; if TRUE, use parallel processing via mclapply (default: FALSE)}

\item{nmin}{Minimum sample size at design points (default: 12)}

\item{q}{Quantile for Harrell-Davis estimator when est=hd (default: .5 for median)}

\item{SCAT}{Logical; if TRUE, add scatterplot (default: TRUE)}

\item{pch1}{Point character for group 1 in plot (default: '*')}

\item{pch2}{Point character for group 2 in plot (default: '+')}

\item{...}{Additional arguments passed to estimator function}
}
\value{
List with components:
\item{pts}{Design points where comparisons made}
\item{p.value}{P-values at each design point}
\item{p.crit}{Critical p-value (if padj=FALSE)}
\item{num.sig}{Number of significant comparisons}
\item{n1}{Sample size group 1}
\item{n2}{Sample size group 2}
}
\description{
Compare two independent groups using ANCOVA with bootstrap resampling from (x,y)
pairs rather than conditioning on x. This unconditional resampling approach tends
to have more power than standard ancova(). Uses running interval smoothers with
no parametric assumptions. Supports only one covariate.
}
\note{
\itemize{
\item Only one covariate supported
\item FAST=TRUE uses approximation valid for alpha=.05 and n <= 800
\item For faster execution with large samples, set MC=TRUE (requires multicore)
}
}
\examples{
\dontrun{
# Generate data with covariate relationship
set.seed(345)
x1 <- runif(60, 0, 10)
y1 <- 2 + 0.5*x1 + rnorm(60, 0, 1.5)
x2 <- runif(60, 0, 10)
y2 <- 3 + 0.5*x2 + rnorm(60, 0, 1.5)

# Compare using bootstrap ANCOVA
result <- ancovaV2(x1, y1, x2, y2, nboot = 200, plotit = FALSE)
print(result$p.value)
}

}
