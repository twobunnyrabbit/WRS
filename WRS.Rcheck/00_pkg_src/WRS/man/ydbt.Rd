% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{ydbt}
\alias{ydbt}
\title{Bootstrap-t Confidence Interval for Difference in Trimmed Means (Dependent Groups)}
\usage{
ydbt(
  x,
  y,
  tr = 0.2,
  alpha = 0.05,
  nboot = 599,
  side = TRUE,
  plotit = FALSE,
  op = 1,
  SEED = TRUE
)
}
\arguments{
\item{x}{Numeric vector for first group (paired observations).}

\item{y}{Numeric vector for second group (must have same length as \code{x}).}

\item{tr}{Trimming proportion (default: 0.2 for 20\% trimming).}

\item{alpha}{Significance level (default: 0.05 for 95\% CI).}

\item{nboot}{Number of bootstrap samples (default: 599).}

\item{side}{Logical. If \code{TRUE} (default), returns symmetric CI; if \code{FALSE},
returns equal-tailed CI.}

\item{plotit}{Logical. If \code{TRUE}, plots the bootstrap distribution (default: \code{FALSE}).}

\item{op}{Plot type when \code{plotit=TRUE}: 1 = adaptive kernel density (\code{\link{akerd}}),
2 = robust density plot (\code{\link{rdplot}}), 3 = boxplot.}

\item{SEED}{Logical. If \code{TRUE} (default), sets random seed to 2 for reproducibility.}
}
\value{
List with components:
\item{ci}{Confidence interval (lower, upper).}
\item{Est.1}{Trimmed mean of first group.}
\item{Est.2}{Trimmed mean of second group.}
\item{dif}{Difference in trimmed means.}
\item{p.value}{P-value (only when \code{side=TRUE}).}
}
\description{
Computes a bootstrap-t confidence interval for the difference between trimmed means
of paired/dependent data. Optionally returns symmetric or equal-tailed CI.
}
\details{
Uses Yuen's bootstrap-t method for dependent groups. Centers each variable by
subtracting its trimmed mean, generates bootstrap samples, and constructs CI
based on the bootstrap distribution of t-statistics.

For symmetric CI (\code{side=TRUE}), uses absolute values of bootstrap statistics.
For equal-tailed CI (\code{side=FALSE}), uses quantiles of raw bootstrap statistics.

Missing values are automatically removed pairwise.
}
\seealso{
\code{\link{yuend}}, \code{\link{yhbt}}, \code{\link{bootdpci}}
}
\keyword{bootstrap}
\keyword{htest}
\keyword{robust}
