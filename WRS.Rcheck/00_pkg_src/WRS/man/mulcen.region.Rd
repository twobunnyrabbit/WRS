% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{mulcen.region}
\alias{mulcen.region}
\title{Compute and Plot Central Region Based on Data Depth}
\usage{
mulcen.region(
  m,
  region = 0.05,
  plotit = TRUE,
  est = median,
  xlab = "VAR 1",
  ylab = "VAR 2",
  ...
)
}
\arguments{
\item{m}{Numeric matrix (n × 2) containing bivariate data}

\item{region}{Proportion of deepest points to include (default: 0.05 for 95\% region)}

\item{plotit}{Logical; if TRUE plots the data and convex hull (default: TRUE)}

\item{est}{Function for estimating center (default: median)}

\item{xlab}{X-axis label (default: "VAR 1")}

\item{ylab}{Y-axis label (default: "VAR 2")}

\item{...}{Additional arguments passed to \code{fdepth}}
}
\value{
A list with components:
\item{center}{Bivariate center estimate}
\item{convex.hull.pts}{Matrix of points forming the convex hull boundary}
}
\description{
Determines the deepest points in a bivariate dataset using halfspace depth,
and computes/plots the convex hull containing those points as a confidence
region for the bivariate center.
}
\details{
Uses halfspace depth via \code{fdepth} to identify the (1-region) deepest points,
then computes their convex hull. This provides a depth-based confidence region.
}
\seealso{
\code{\link{fdepth}}, \code{\link{mulcen.region.MF}}
}
