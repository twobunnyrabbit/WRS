% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{ancmg}
\alias{ancmg}
\title{ANCOVA for Multiple Groups with Multiple Covariates}
\usage{
ancmg(
  x,
  y,
  pool = TRUE,
  jcen = 1,
  fr = 1,
  depfun = fdepth,
  nmin = 8,
  op = 3,
  tr = 0.2,
  pts = NULL,
  SEED = TRUE,
  pr = TRUE,
  cop = 3,
  con = 0,
  nboot = NA,
  alpha = 0.05,
  bhop = FALSE
)
}
\arguments{
\item{x}{Covariate data. Either a matrix with Jp columns (p covariates per group) or a list of
J matrices each with p columns.}

\item{y}{Outcome data. Either a matrix with J columns or a list of length J.}

\item{pool}{Logical. If TRUE (default), pool data when determining design points and scatter.
If FALSE, use data from group jcen.}

\item{jcen}{Group index to use as center when pool=FALSE. Default is 1.}

\item{fr}{Span for running interval smoother. Default is 1.}

\item{depfun}{Function to compute depth of points. Default is fdepth (projection depth).}

\item{nmin}{Minimum sample size required for each group at design points. Default is 8.}

\item{op}{Operation mode: 1=omnibus test with trimmed means, 2=omnibus test with medians,
3=multiple comparisons with trimmed means (recommended), 4=multiple comparisons with medians.
Default is 3.}

\item{tr}{Trim proportion for trimmed means. Default is 0.2.}

\item{pts}{Matrix of design points (p columns). If NULL, chosen automatically.}

\item{SEED}{Logical. Set random seed for reproducibility. Default is TRUE.}

\item{pr}{Logical. Print informational messages. Default is TRUE.}

\item{cop}{Parameter for ancdes function. Default is 3.}

\item{con}{Contrast specification. Default is 0.}

\item{nboot}{Number of bootstrap samples for op=3 or op=4. Default is NA.}

\item{alpha}{Alpha level for confidence intervals. Default is 0.05.}

\item{bhop}{Logical. Use Benjamini-Hochberg procedure. Default is FALSE.}
}
\value{
A list with test results at each design point and a matrix of sample sizes.
For op=1 or 2, returns matrix with columns: point, test.stat, p-value.
For op=3 or 4, returns list with results for each design point.
}
\description{
ANCOVA for J groups (J >= 2) with two or more covariates, using trimmed means or medians.
Provides omnibus tests or pairwise multiple comparisons with various estimation methods.
}
\details{
Requires at least two covariates (for single covariate, use ancmg1).
Design points with sample size < nmin are eliminated.
op=2 not recommended for discrete data with ties; use op=4 instead.
}
\examples{
\dontrun{
  # Three groups, two covariates
  x1 <- cbind(rnorm(30), rnorm(30))
  x2 <- cbind(rnorm(30), rnorm(30))
  x3 <- cbind(rnorm(30), rnorm(30))
  x <- list(x1, x2, x3)
  y <- list(rnorm(30), rnorm(30, 0.5), rnorm(30, 1))
  result <- ancmg(x, y, op=3)
}
}
\keyword{ancova}
\keyword{multivariate}
