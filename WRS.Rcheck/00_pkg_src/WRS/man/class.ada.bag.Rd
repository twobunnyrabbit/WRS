% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outliers.R
\name{class.ada.bag}
\alias{class.ada.bag}
\title{Bagged AdaBoost Classification}
\usage{
class.ada.bag(
  train = NULL,
  test = NULL,
  g = NULL,
  x1 = NULL,
  x2 = NULL,
  nboot = 100,
  SEED = TRUE,
  baselearner = "bbs",
  ...
)
}
\arguments{
\item{train}{Training data matrix (optional if \code{x1} and \code{x2} provided).}

\item{test}{Test data matrix to classify.}

\item{x1, x2}{Training data for groups 1 and 2 (alternative to \code{train}/\code{g}).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{baselearner}{Base learner for AdaBoost (default: 'bbs').}

\item{...}{Additional arguments.}
}
\value{
Vector of predicted class labels (1 or 2) for test observations.
}
\description{
Bootstrap aggregating version of AdaBoost classification. Uses bootstrap
sampling to address bias when group sizes differ (n1 != n2).
}
\details{
Performs AdaBoost classification via \code{class.ada()} with bootstrap bagging.
When group sizes are unequal (n1 != n2) and there is no association, the
expected probability of correct classification can differ from 0.5. Bootstrap
bagging addresses this bias by using balanced samples (min(n1,n2)).
}
\seealso{
\code{\link{class.ada}}
}
\keyword{internal}
