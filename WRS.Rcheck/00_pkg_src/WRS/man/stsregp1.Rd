% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression.R
\name{stsregp1}
\alias{stsregp1}
\title{S-Type Theil-Sen Regression (Single Predictor)}
\usage{
stsregp1(x, y, sc = pbvar, xout = FALSE, outfun = out, ...)
}
\arguments{
\item{x}{Numeric vector of predictor values (single predictor only).}

\item{y}{Numeric vector of response values.}

\item{sc}{Robust scale estimator to minimize (default: \code{pbvar}).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
A list with components:
\describe{
\item{coef}{Vector of coefficients (intercept, slope).}
\item{residuals}{Residuals from the fit.}
}
}
\description{
Computes an S-type modification of the Theil-Sen regression estimator for
a single predictor variable. The modification chooses the slope that minimizes
a robust scale measure of the residuals.
}
\details{
This function is a specialized version of S-type regression for the single
predictor case. It:
\enumerate{
\item Computes all pairwise slopes (as in standard Theil-Sen)
\item For each candidate slope, calculates the robust scale (\code{sc}) of residuals
\item Selects the slope that minimizes this scale
\item Determines the intercept using medians
}

The S-type modification provides better efficiency than median-of-slopes
while maintaining high breakdown point. The scale function \code{sc} is
typically a robust variance estimator like percentage bend variance
(\code{\link{pbvar}}).

\strong{Note}: This version is limited to a single predictor. For multiple predictors,
use \code{\link{stsreg}} which employs a Gauss-Seidel algorithm.
}
\examples{
\dontrun{
# Simple linear regression
x <- rnorm(50)
y <- 2*x + rnorm(50, sd=0.5)
stsregp1(x, y)

# With outliers - S-type is more efficient than median-of-slopes
y[1:5] <- y[1:5] + 5
fit <- stsregp1(x, y)
print(fit$coef)

# Using different scale estimator
stsregp1(x, y, sc=winvar)
}

}
\seealso{
\code{\link{stsreg}} for general S-type Theil-Sen regression,
\code{\link{tsreg}} for standard Theil-Sen,
\code{\link{pbvar}} for percentage bend variance
}
