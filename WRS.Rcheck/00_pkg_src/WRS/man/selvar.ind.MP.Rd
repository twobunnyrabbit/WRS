% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{selvar.ind.MP}
\alias{selvar.ind.MP}
\title{Select Group with Smallest Variance (Independent Groups)}
\usage{
selvar.ind.MP(
  x,
  alpha = 0.05,
  rem = NULL,
  iter = 2000,
  p.crit = NULL,
  SEED = TRUE
)
}
\arguments{
\item{x}{A matrix, data frame, or list of numeric vectors, one for each group.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{rem}{Pre-computed null distribution from \code{\link{selvar.ind.crit}}.
If \code{NULL}, computes it via simulation (default: \code{NULL}).}

\item{iter}{Integer. Number of Monte Carlo iterations for computing null
distribution if \code{rem = NULL} (default: 2000).}

\item{p.crit}{Deprecated. Critical p-value threshold (not used in current
implementation).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}
}
\value{
An S4 object of class 'SSV' with slots:
\item{Group.with.smallest.estimate}{Index of the group with smallest variance}
\item{Less.than}{Character string indicating the decision:
\itemize{
\item \code{"No Decision"}: Cannot determine if smallest variance is significantly smaller
\item \code{"Smaller.than.all"}: Smallest variance is significantly smaller than all others
\item Group indices: Smallest variance is significantly smaller than these specific groups
}}
\item{n}{Vector of sample sizes for each group}
\item{output}{Matrix with columns: \code{Smallest.Est} (smallest variance),
\code{Grp} (comparison group index), \code{Est} (comparison group variance),
\code{p.value} (raw p-value), \code{p.adj} (Hochberg-adjusted p-value)}
}
\description{
Identifies which of J independent groups has the smallest variance and
determines whether this group is significantly smaller than the others,
controlling for multiple comparisons.
}
\details{
This function addresses the problem of identifying the group with smallest
variance while controlling for multiple comparisons. The procedure:
\enumerate{
\item Identifies the group with smallest variance
\item Compares it to each other group using \code{\link{varcom.IND.MP}}
\item Adjusts p-values using Hochberg's method
\item Makes a decision based on which adjusted p-values are significant
}

P-values are calibrated against a null distribution (computed via simulation
or provided in \code{rem}) to account for the selection process.

Requires at least 3 groups. For pairwise variance comparison, use
\code{\link{varcom.IND.MP}} directly.
}
\examples{
# Compare variances across 4 groups
set.seed(123)
x1 <- rnorm(20, sd = 1)
x2 <- rnorm(20, sd = 2)
x3 <- rnorm(20, sd = 1.5)
x4 <- rnorm(20, sd = 1.8)
result <- selvar.ind.MP(list(x1, x2, x3, x4), iter = 500)
result@Group.with.smallest.estimate
result@Less.than
}
\references{
Wilcox, R.R. (2017). Introduction to Robust Estimation and Hypothesis Testing
(4th ed.). Academic Press.
}
\seealso{
\code{\link{selvar.ind.crit}} for pre-computing null distribution,
\code{\link{selvar.ind.ex}} for internal helper,
\code{\link{varcom.IND.MP}} for pairwise variance comparison
}
