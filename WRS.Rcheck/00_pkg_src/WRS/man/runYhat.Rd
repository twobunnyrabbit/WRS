% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{runYhat}
\alias{runYhat}
\title{General Running Interval Smoother Prediction}
\usage{
runYhat(
  x,
  y,
  pts = NULL,
  est = tmean,
  fr = 1,
  nmin = 1,
  xout = FALSE,
  outfun = outpro,
  XY.used = FALSE,
  ...
)
}
\arguments{
\item{x}{Vector or matrix of predictors}

\item{y}{Vector of response values}

\item{pts}{Points at which to predict y (default: NULL, uses all x values)}

\item{est}{Measure of location (default: tmean)}

\item{fr}{Span parameter controlling smoothing (default: 1)}

\item{nmin}{Minimum neighbors for estimation (default: 1)}

\item{xout}{Logical: remove x outliers (default: FALSE)}

\item{outfun}{Outlier detection function (default: outpro)}

\item{XY.used}{Logical: if TRUE, return data used (default: FALSE)}

\item{...}{Additional arguments passed to est function}
}
\value{
A list with components:
\item{vals}{Vector of predicted values}
\item{nvals}{Vector of sample sizes used for each prediction}
\item{XY}{Matrix of x and y values used (only if XY.used=TRUE)}
}
\description{
Fit a running interval smoother and estimate the typical value of Y
at specified covariate values. Handles both univariate and multivariate
predictors automatically.
}
\details{
A general wrapper function that automatically chooses the appropriate
smoother based on the dimensionality of x:
\itemize{
\item If x is univariate, uses runhat()
\item If x is multivariate, uses rung3hat()
}

If pts=NULL, predictions are made at all x values (after removing missing
values). Missing values are automatically removed from the data.
}
\seealso{
\code{\link{runhat}}, \code{\link{rung3hat}}, \code{\link{rungen}}
}
\keyword{nonparametric}
\keyword{smooth}
