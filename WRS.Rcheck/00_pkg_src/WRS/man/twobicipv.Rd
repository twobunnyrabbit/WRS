% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{twobicipv}
\alias{twobicipv}
\title{P-Value for Comparing Two Independent Binomial Proportions (Beal's Method)}
\usage{
twobicipv(
  r1 = sum(x),
  n1 = length(x),
  r2 = sum(y),
  n2 = length(y),
  x = NA,
  y = NA,
  alpha = 0.05
)
}
\arguments{
\item{r1}{Number of successes in group 1 (default: \code{sum(x)} if x provided).}

\item{n1}{Sample size for group 1 (default: \code{length(x)} if x provided).}

\item{r2}{Number of successes in group 2 (default: \code{sum(y)} if y provided).}

\item{n2}{Sample size for group 2 (default: \code{length(y)} if y provided).}

\item{x}{Binary data vector for group 1 (optional).}

\item{y}{Binary data vector for group 2 (optional).}

\item{alpha}{Significance level (default: 0.05).}
}
\value{
A list with components:
\itemize{
\item \code{p.value}: P-value for testing equality of proportions
\item \code{ci}: Confidence interval for difference in proportions
\item \code{p1}: Proportion for group 1
\item \code{p2}: Proportion for group 2
}
}
\description{
Computes a p-value for comparing two independent binomial proportions
using Beal's method based on confidence interval inversion.
}
\details{
This function uses Beal's method for comparing two independent binomial
proportions by inverting confidence intervals. The p-value is found by
determining the smallest alpha level at which the confidence interval
excludes zero.
}
\examples{
\dontrun{
# Compare two proportions
x <- rbinom(30, 1, 0.3)
y <- rbinom(30, 1, 0.6)
twobicipv(x = x, y = y)
}
}
\seealso{
\code{\link{twobici}}
}
