% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{rmdzeroG}
\alias{rmdzeroG}
\title{Repeated Measures ANOVA Using Depth of Zero for Multivariate Estimators}
\usage{
rmdzeroG(x, est = skipSPR, grp = NA, nboot = 500, SEED = TRUE, ...)
}
\arguments{
\item{x}{Data in matrix format (columns = groups) or list mode.}

\item{est}{Multivariate location estimator function returning list with \code{$center}
(default: \code{\link{skipSPR}}).}

\item{grp}{Optional vector to specify subset of groups.}

\item{nboot}{Number of bootstrap samples (default: 500).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{...}{Additional arguments passed to \code{est}.}
}
\value{
A list with components:
\describe{
\item{\code{p.value}}{Bootstrap p-value}
\item{\code{center}}{Estimated center from \code{est}}
}
}
\description{
Performs one-way ANOVA for dependent groups using depth of zero among bootstrap
values based on difference scores. Designed for multivariate location estimators
that return a list with a \code{$center} component (e.g., \code{\link{skipSPR}},
\code{\link{dmean}}).
}
\details{
Similar to \code{\link{rmdzero}} but designed for estimators that return a list
with a \code{$center} component rather than a single value. Handles the special
case of J=2 groups using a simpler two-sample test.

For J ≥ 3: Uses Mahalanobis distance-based depth of zero on all pairwise differences.
For J = 2: Uses proportion-based test comparing bootstrap estimates.
}
\seealso{
\code{\link{rmdzero}} for univariate estimators,
\code{\link{rmdzeroGMC}} for parallel version,
\code{\link{skipSPR}}, \code{\link{dmean}}
}
\keyword{htest}
\keyword{multivariate}
\keyword{robust}
