% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{ees.ci}
\alias{ees.ci}
\title{Confidence Interval for Robust Heteroscedastic Effect Size}
\usage{
ees.ci(x, y, SEED = TRUE, nboot = 400, tr = 0.2, alpha = 0.05, pr = TRUE)
}
\arguments{
\item{x}{Numeric vector. First group.}

\item{y}{Numeric vector. Second group.}

\item{SEED}{Logical. If TRUE, sets random seed to 2 for reproducibility (default: TRUE).}

\item{nboot}{Integer. Number of bootstrap samples (default: 400).}

\item{tr}{Numeric. Trimming proportion (default: 0.2).}

\item{alpha}{Numeric. Type I error rate; constructs (1-alpha)×100\\% CI (default: 0.05).}

\item{pr}{Logical. If TRUE, prints results (default: TRUE).}
}
\value{
Numeric vector of length 2 containing lower and upper confidence limits
for the effect size (square root of explanatory power).
}
\description{
Computes a confidence interval for a robust, heteroscedastic measure of explanatory
power (effect size) based on Yuen's trimmed mean comparison. Provides bootstrap
CI for the proportion of variance explained.
}
\details{
This function computes a confidence interval for the squared effect size measure:

\deqn{ES^2 = \frac{(\bar{X}_t - \bar{Y}_t)^2}{s_X^2 + s_Y^2}}

where \eqn{\bar{X}_t} and \eqn{\bar{Y}_t} are trimmed means, and \eqn{s_X^2}, \eqn{s_Y^2}
are winsorized variances.

The function:
\enumerate{
\item Bootstraps both samples independently
\item Computes explanatory power for each bootstrap sample using \code{yuenv2}
\item Constructs percentile confidence interval
\item Takes square root to get effect size scale (like correlation)
\item If groups don't differ significantly (Yuen's test), lower CI is set to 0
}

\strong{Interpretation}: The effect size ranges from 0 (no effect) to 1 (perfect separation).
Values can be interpreted similarly to correlation coefficients.
}
\examples{
# Effect size CI for two groups
set.seed(123)
x <- rnorm(30, mean = 0)
y <- rnorm(30, mean = 0.8)
ci <- ees.ci(x, y, nboot = 200, pr = FALSE)
ci  # 95\% CI for effect size
}
\seealso{
\code{\link{yuenv2}}, \code{\link{yuen}}, \code{\link{effectg}}
}
\keyword{htest}
\keyword{robust}
