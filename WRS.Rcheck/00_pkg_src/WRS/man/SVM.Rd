% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{SVM}
\alias{SVM}
\title{Support Vector Machine Classification}
\usage{
SVM(
  train = NULL,
  test = NULL,
  g = NULL,
  x1 = NULL,
  x2 = NULL,
  kernel = "radial",
  SEED = NULL,
  ...
)
}
\arguments{
\item{train}{Matrix or data frame of training data. Each row is an observation,
columns are predictors}

\item{test}{Matrix or data frame of test data to classify. Can be a single observation}

\item{g}{Vector of group IDs/class labels for training data. Required if using \code{train}}

\item{x1}{Optional. Training data for first group (alternative to \code{train}/\code{g})}

\item{x2}{Optional. Training data for second group (used with \code{x1})}

\item{kernel}{Character. Kernel type: 'radial' (default), 'linear', 'polynomial', 'sigmoid'}

\item{SEED}{Logical or NULL. Random seed control (for internal use)}

\item{...}{Additional arguments passed to \code{e1071::svm}}
}
\value{
Numeric vector of predicted class labels (0 or 1) for test observations
}
\description{
Performs Support Vector Machine (SVM) classification for training data
and predicts classes for test data.
}
\details{
This function provides a wrapper around \code{e1071::svm} with some conveniences:
\itemize{
\item Automatic missing value removal
\item Two input formats: combined (\code{train}/\code{g}) or separate (\code{x1}/\code{x2})
\item Automatic class labeling (0 and 1)
\item Handles single-point test data correctly
}

If using \code{x1} and \code{x2}, the function automatically creates group labels
(0 for group 1, 1 for group 2) and combines the data.

For cross-validation considerations, see Shao (1993). Linear Model Selection by
Cross-Validation, JASA, 88, 486-494.
}
\examples{
# Two-group classification with separate group data
x1 <- matrix(rnorm(50, mean = 0), ncol = 2)
x2 <- matrix(rnorm(50, mean = 2), ncol = 2)
test <- matrix(c(0.5, 0.5), ncol = 2)
pred <- SVM(x1 = x1, x2 = x2, test = test)

}
\references{
Shao, J. (1993). Linear Model Selection by Cross-Validation.
Journal of the American Statistical Association, 88, 486-494.
}
\seealso{
\code{\link[e1071]{svm}}
}
\keyword{classif}
\keyword{multivariate}
