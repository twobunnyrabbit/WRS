% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{idmatch}
\alias{idmatch}
\title{Match Cases by ID Across Two Datasets}
\usage{
idmatch(m1, m2, id.col1, id.col2 = id.col1)
}
\arguments{
\item{m1}{First matrix or data frame}

\item{m2}{Second matrix or data frame}

\item{id.col1}{Column number containing IDs in m1}

\item{id.col2}{Column number containing IDs in m2. Default is \code{id.col1}}
}
\value{
List with components:
\itemize{
\item \code{M1}: Rows from m1 that have matching IDs in m2
\item \code{M2}: Rows from m2 that have matching IDs in m1
}

The order of rows in M1 and M2 correspond to each other (i.e., M1\link{i,} and M2\link{i,}
have the same ID).
}
\description{
Extracts matching cases from two matrices or data frames based on ID columns.
Returns only rows where IDs appear in both datasets.
}
\details{
This function identifies cases with matching IDs across two datasets and returns
the matched rows from each dataset. The function:
\itemize{
\item Removes rows with missing IDs
\item Checks for duplicate IDs (stops with error if found)
\item Extracts only rows with matching IDs in both datasets
\item Preserves data frame or matrix structure
}
}
\seealso{
\code{\link{idmatchv2}}
}
\keyword{manip}
\keyword{utilities}
