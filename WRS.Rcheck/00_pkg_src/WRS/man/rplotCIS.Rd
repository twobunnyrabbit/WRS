% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{rplotCIS}
\alias{rplotCIS}
\title{Simple Running Interval Smoother with Confidence Band}
\usage{
rplotCIS(
  x,
  y,
  tr = 0.2,
  fr = 0.8,
  plotit = TRUE,
  scat = TRUE,
  pyhat = FALSE,
  SEED = TRUE,
  dfmin = 8,
  eout = FALSE,
  xout = FALSE,
  xlab = "x",
  ylab = "y",
  outfun = out,
  LP = TRUE,
  alpha = 0.05,
  pch = ".",
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{tr}{Trimming proportion (default: 0.2). Proportion of observations to
trim from each tail when computing winsorized correlation.}

\item{fr}{Span parameter controlling amount of smoothing (default: 0.8).
Higher values = more smoothing.}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{dfmin}{Minimum degrees of freedom required for confidence intervals
(default: 8). Points with df < dfmin are excluded.}

\item{eout}{Logical value. If \code{TRUE}, outliers are removed based on
eliminating points with large residuals or leverage (default: \code{FALSE}).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{xlab}{Label for x-axis in plots (default: "X" or "VAR 1").}

\item{ylab}{Label for y-axis in plots (default: "Y" or "VAR 2").}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{LP}{Logical. If \code{TRUE}, applies lowess smoothing to the confidence
band for smoother appearance (default: \code{TRUE}).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{pch}{Plotting character for scatter plot (default: '.').}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
List with components:
\item{output}{Matrix with columns: x, y.hat, ci.low, ci.up}
\item{str}{Strength of association measure}
\item{n}{Total sample size}
\item{n.keep}{Sample size after outlier removal}
}
\description{
Computes a confidence band for a running interval smoother based on trimmed
means using a simple method. Unlike \code{rplotCI}, this function does not adjust
for familywise error rate across multiple points.
}
\details{
This is a simpler alternative to \code{rplotCI} that computes pointwise confidence
intervals without adjusting for simultaneous coverage. The confidence band
uses Student's t-distribution based on the degrees of freedom at each point.

The function excludes points where degrees of freedom fall below \code{dfmin} to
ensure adequate precision. Set LP=TRUE for a smoother confidence band appearance.

Note: \code{rplotCI} provides better control of familywise error rate (FWE) across
multiple points.
}
\examples{
\dontrun{
# Simple confidence band for running smoother
x <- rnorm(100)
y <- 2*x + rnorm(100)
rplotCIS(x, y, fr=0.8)
}
}
\seealso{
\code{\link{rplotCI}}, \code{\link{rplotpbCI}}
}
