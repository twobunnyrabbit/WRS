% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{signtpv}
\alias{signtpv}
\title{Sign Test with P-value (Deprecated)}
\usage{
signtpv(x, y, nullval = 0.5, alpha = 0.05, AC = FALSE)
}
\arguments{
\item{x}{Numeric vector of first group data.}

\item{y}{Numeric vector of second group data.}

\item{nullval}{Null hypothesis value for the probability that x < y
(default: 0.5).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{AC}{Logical. If \code{TRUE}, uses Agresti-Coull method (default: \code{FALSE}).
This parameter is deprecated; use the \code{method} parameter in \code{\link{signt}}
instead.}
}
\value{
A list with components:
\item{output}{Full output from \code{\link{signt}}}
\item{p.value}{P-value for testing if probability equals \code{nullval}}
}
\description{
Performs a sign test for two dependent groups, returning primarily a p-value.
This function is largely superseded by \code{\link{signt}}, which now returns
p-values as well as confidence intervals.
}
\details{
This function computes a p-value for the sign test by inverting confidence
intervals using a search algorithm. It repeatedly calls \code{\link{signt}}
with different alpha levels to find the smallest alpha where the confidence
interval excludes \code{nullval}.

The function is deprecated because \code{\link{signt}} now returns p-values
directly, making this wrapper function unnecessary.
}
\examples{
x <- c(10, 12, 15, 18, 20, 22, 25, 28)
y <- c(12, 14, 16, 20, 22, 24, 27, 30)
signtpv(x, y)
}
\seealso{
\code{\link{signt}} for the main sign test function
}
