% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{anc.grid}
\alias{anc.grid}
\title{Grid-Based ANCOVA for Two Independent Groups}
\usage{
anc.grid(
  x1,
  y1,
  x2,
  y2,
  alpha = 0.05,
  Qsplit1 = 0.5,
  Qsplit2 = 0.5,
  SV1 = NULL,
  SV2 = NULL,
  tr = 0.2,
  PB = FALSE,
  est = tmean,
  nboot = 1000,
  CI = FALSE,
  xout = FALSE,
  outfun = outpro,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{x1}{Matrix of covariates for group 1 (must have 2 columns).}

\item{y1}{Outcome values for group 1.}

\item{x2}{Matrix of covariates for group 2 (must have 2 columns).}

\item{y2}{Outcome values for group 2.}

\item{alpha}{Alpha level for confidence intervals. Default is 0.05.}

\item{Qsplit1}{Quantile(s) for splitting first covariate. Default is 0.5 (median).}

\item{Qsplit2}{Quantile(s) for splitting second covariate. Default is 0.5 (median).}

\item{SV1}{Alternative to Qsplit1: specific values for splitting first covariate. Default is NULL.}

\item{SV2}{Alternative to Qsplit2: specific values for splitting second covariate. Default is NULL.}

\item{tr}{Trim proportion for trimmed mean. Default is 0.2.}

\item{PB}{Logical. If TRUE, use percentile bootstrap for p-values. Default is FALSE.}

\item{est}{Estimator function. Default is tmean (trimmed mean).}

\item{nboot}{Number of bootstrap samples if PB=TRUE. Default is 1000.}

\item{CI}{Logical. If TRUE, compute confidence intervals for effect sizes. Default is FALSE.}

\item{xout}{Logical. If TRUE, remove outliers from covariates. Default is FALSE.}

\item{outfun}{Function to detect outliers. Default is outpro.}

\item{SEED}{Logical. If TRUE (default), set random seed for reproducibility.}

\item{...}{Additional arguments.}
}
\value{
A list with components:
\item{GRID.INTERVALS}{Matrix defining the grid cell boundaries}
\item{GRID}{Matrix with test results for each cell (sample sizes, estimates, differences, p-values)}
\item{Effect.Sizes}{List of effect size summaries for each cell}
}
\description{
Split data into grid cells based on two covariates and compare trimmed means
between two independent groups within each cell. Provides multiple comparison adjustments.
}
\examples{
\dontrun{
  # Split data at medians of two covariates
  result <- anc.grid(x1, y1, x2, y2)

  # Split at quartiles for first covariate, median for second
  result <- anc.grid(x1, y1, x2, y2, Qsplit1=c(.25,.5,.75))
}
}
\keyword{ancova}
\keyword{grid}
