% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{wmw.ref.dif}
\alias{wmw.ref.dif}
\title{Test Symmetry of Difference Distribution Using Reference Points}
\usage{
wmw.ref.dif(
  x,
  y,
  q = 0.25,
  pts = NULL,
  nboot = 1000,
  alpha = 0.05,
  SEED = TRUE,
  plotit = FALSE,
  xlab = "Difference",
  ylab = "Density",
  estfun = hdmq,
  plotfun = kerSORT
)
}
\arguments{
\item{x}{Observations from group 1.}

\item{y}{Observations from group 2.}

\item{q}{Quantile for reference point if pts=NULL (default: 0.25).}

\item{pts}{Reference point(s) (default: NULL for quantile-based).}

\item{nboot}{Number of bootstrap samples (default: 1000).}

\item{alpha}{Significance level (default: 0.05).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed (default: \code{TRUE}).}

\item{plotit}{Logical. If \code{TRUE}, plots distribution (default: \code{FALSE}).}

\item{xlab}{X-axis label (default: 'Difference').}

\item{ylab}{Y-axis label (default: 'Density').}

\item{estfun}{Quantile estimator function (default: \code{hdmq}).}

\item{plotfun}{Plotting function (default: \code{kerSORT}).}
}
\value{
A list with components:
\itemize{
\item \code{L}: Estimated P(X-Y < -pts)
\item \code{U}: Estimated P(X-Y > pts)
\item \code{Est.dif}: Difference U - L
\item \code{ci}: Bootstrap confidence interval
\item \code{p.value}: P-value for symmetry test
}
}
\description{
Tests whether the distribution of X-Y is symmetric about zero using
reference points. Can test either at quantiles or specified values.
}
\details{
If X and Y have identical distributions, X-Y is symmetric about zero
and the sum of the q and 1-q quantiles equals zero. Tests this
using bootstrap. Should not be used with tied values.
}
\examples{
\dontrun{
# Test symmetry of difference distribution
set.seed(123)
x <- rnorm(50)
y <- rnorm(50, mean = 0.3)
wmw.ref.dif(x, y, q = 0.25, nboot = 500)
}
}
\seealso{
\code{\link{wmw.ref.mul}}, \code{\link{wmw.det}}
}
