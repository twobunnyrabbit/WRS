% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression.R
\name{LMSreg}
\alias{LMSreg}
\title{Least Median of Squares Regression}
\usage{
LMSreg(x, y, xout = FALSE, outfun = outpro, ...)
}
\arguments{
\item{x}{A numeric matrix of predictor variables (n by p).}

\item{y}{A numeric vector of the dependent variable (length n).}

\item{xout}{Logical. Not used in this function but included to avoid conflicts
when using with other functions like \code{regci} (default: \code{FALSE}).}
}
\value{
A list with components:
\describe{
\item{n}{Original sample size.}
\item{n.keep}{Sample size after outlier removal (if \code{xout=TRUE}).}
\item{coef}{Vector of regression coefficients (intercept, slopes).}
}
}
\description{
Computes the least median of squares (LMS) regression estimator, which has
the highest possible breakdown point (50\%) but lower efficiency than LTS.
The LMS estimator minimizes the median of squared residuals.
}
\details{
\strong{Least Median of Squares (LMS)}:

The LMS estimator finds regression coefficients that minimize:
\deqn{median(r_i^2)}
where \eqn{r_i} are the residuals.

\strong{Properties}:
\itemize{
\item \strong{Breakdown point}: 50\% (highest possible)
\item \strong{Efficiency}: Lower than LTS or MM-regression
\item \strong{Resistance}: Excellent resistance to outliers and leverage points
\item \strong{Computation}: Uses \code{MASS::lmsreg} internally
}

\strong{When to use LMS}:
\itemize{
\item Maximum robustness is paramount
\item Heavy contamination suspected (up to 50\%)
\item Initial screening of data
\item As a starting point for other robust methods
}

\strong{Comparison with LTS}: LTS (least trimmed squares) typically has better
efficiency while maintaining high breakdown. LMS is more extreme in its
resistance to outliers.
}
\examples{
\dontrun{
# Simple LMS regression
x <- matrix(rnorm(50), ncol=1)
y <- 2*x + rnorm(50, sd=0.5)
LMSreg(x, y)

# Heavily contaminated data
x <- matrix(rnorm(100), ncol=2)
y <- x[,1] + 2*x[,2] + rnorm(50, sd=0.3)
y[1:20] <- y[1:20] + 10  # 20\% contamination
fit <- LMSreg(x, y)
print(fit$coef)
}
}
\references{
Rousseeuw, P.J. (1984). Least median of squares regression. Journal of the
American Statistical Association, 79, 871-880.
}
\seealso{
\code{\link{ltsreg}}, \code{\link{MMreg}}

Other robust regression functions: 
\code{\link{MMreg}()},
\code{\link{bireg}()},
\code{\link{bmreg}()},
\code{\link{chreg}()},
\code{\link{depreg}()},
\code{\link{mbmreg}()},
\code{\link{mdepreg}()},
\code{\link{mopreg}()},
\code{\link{snmreg}()},
\code{\link{tsgreg}()},
\code{\link{winreg}()},
\code{\link{wreg}()}
}
\concept{robust regression functions}
