% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{skerd}
\alias{skerd}
\title{Kernel Density Estimation Plot}
\usage{
skerd(x, op = TRUE, kernel = "gaussian", xlab = "X", ylab = "")
}
\arguments{
\item{x}{Numeric vector of data values.}

\item{op}{Logical. If \code{TRUE} (default), uses default bandwidth. If \code{FALSE},
uses Sheather-Jones bandwidth selection with direct plug-in method.}

\item{kernel}{Character string specifying the kernel. Currently not used;
defaults to Gaussian kernel.}

\item{xlab}{Character string for x-axis label. Default is "X".}

\item{ylab}{Character string for y-axis label. Default is empty string.}
}
\value{
NULL (invisibly). The function is called for its side effect of creating a plot.
}
\description{
Computes and plots a kernel density estimate for univariate data.
}
\details{
This function is a wrapper around R's \code{density()} function. When \code{op=TRUE},
it uses the default density estimation. When \code{op=FALSE}, it uses the
Sheather-Jones bandwidth selection method with 256 evaluation points.

The function creates a line plot of the estimated density.
}
\seealso{
\code{\link{density}}, \code{\link{bandwidth.sj}}
}
\keyword{hplot}
