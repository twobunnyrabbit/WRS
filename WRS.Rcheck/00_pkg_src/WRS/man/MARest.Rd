% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{MARest}
\alias{MARest}
\title{Maronna Robust Multivariate Location and Scatter Estimate}
\usage{
MARest(x, kappa = 0.1)
}
\arguments{
\item{x}{Data matrix (n by p).}

\item{kappa}{Proportion of cases to be controlled (breakdown point). Default is 0.1.}
}
\value{
List with components:
\item{center}{Robust center (location estimate) vector of length p}
\item{cov}{Robust covariance matrix (p by p)}
}
\description{
Compute Maronna's robust multivariate measure of location (center) and scatter
(covariance matrix) using an iterative M-estimation procedure with Huber weighting.
}
\details{
This function implements Maronna's (1976) robust estimation method for multivariate
location and scatter. The method uses iterative reweighting based on Mahalanobis
distances with Huber-type weight function.

The algorithm:
\enumerate{
\item Initialize with sample mean and covariance
\item Compute Mahalanobis distances from current estimates
\item Compute Huber weights based on distances
\item Update location and scatter estimates using weights
\item Iterate until convergence (or max 200 iterations)
}

The \code{kappa} parameter controls the breakdown point: smaller values provide
higher breakdown point (more robust) but may reduce efficiency. Typical values
are 0.05 to 0.25.

Convergence criterion is \code{ep = 0.00000001} on the sum of squared differences
in the covariance matrix between iterations.
}
\references{
Maronna, R. A. (1976). Robust M-estimators of multivariate location and scatter.
\emph{The Annals of Statistics}, \emph{4}(1), 51-67.
}
\seealso{
\code{\link{covmcd}}, \code{\link{covmve}}, \code{\link{tbs}}
}
\keyword{multivariate}
\keyword{robust}
