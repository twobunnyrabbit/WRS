% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{corREGorder}
\alias{corREGorder}
\title{Order Predictors by Correlation with Response}
\usage{
corREGorder(
  x,
  y,
  com.p.dist = FALSE,
  corfun = wincor,
  iter = 1000,
  PV = NULL,
  pr = TRUE,
  alpha = 0.05,
  nboot = 500,
  SEED = TRUE,
  MC = FALSE,
  xout = FALSE,
  outfun = outpro,
  method = "hoch",
  ...
)
}
\arguments{
\item{x}{Matrix (n-by-p) of independent variables.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{com.p.dist}{Logical. Compute null distribution for critical p-values
(default: \code{FALSE}). Recommended for p > 6 or n > 350.}

\item{corfun}{Correlation function to use. Common options: \code{pbcor} (percentage
bend), \code{pcor} (Pearson), \code{wincor} (winsorized), \code{spear} (Spearman).}

\item{iter}{Number of iterations for null distribution simulation (default: 1000).}

\item{PV}{Pre-computed null distribution from \code{corREGorder.crit()} (default: \code{NULL}).}

\item{pr}{Logical. Print progress messages (default: \code{TRUE}).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{nboot}{Number of bootstrap samples (default: 500).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{MC}{Logical. Use parallel processing (default: \code{FALSE}).}

\item{xout}{Logical. Remove outliers from x before analysis (default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{method}{Method for controlling FWE (default: 'hoch' for Hochberg).}

\item{...}{Additional arguments passed to \code{corfun}.}
}
\value{
List with components:
\item{Order}{Vector of predictor indices ordered by correlation with y.}
\item{results}{Matrix with columns: IV, Est., Highest.Est, Dif, ci.low, ci.hi, p.value, adj.p.value.}
}
\description{
Compares correlations between y and each predictor in x, testing whether the
highest correlation is significantly different from others, controlling FWE.
}
\details{
Identifies the predictor x_I with highest |cor(y, x_I)| and tests whether
this correlation is significantly different from cor(y, x_k) for all k != I.
Uses Hochberg's method to control familywise error rate.

For p > 6 or n > 350, recommend using \code{com.p.dist = TRUE} for more accurate
critical p-values (can be slow). Pre-compute with \code{corREGorder.crit()} and
supply via \code{PV} to reduce execution time.
}
\examples{
\dontrun{
set.seed(123)
x <- matrix(rnorm(50 * 4), ncol = 4)
y <- 0.7 * x[, 1] + 0.3 * x[, 2] + rnorm(50)
corREGorder(x, y, nboot = 500)
}
}
\seealso{
\code{\link{corREGorder.crit}}, \code{\link{corCOM.DVvsIV}}, \code{\link{corREG.best}}
}
