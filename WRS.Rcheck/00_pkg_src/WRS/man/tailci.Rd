% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{tailci}
\alias{tailci}
\title{Confidence Interval for Tail Probability P(X-Y < threshold)}
\usage{
tailci(
  x,
  y,
  pts = NULL,
  q = 0.25,
  nboot = 1000,
  estfun = hdmq,
  alpha = 0.05,
  SEED = TRUE
)
}
\arguments{
\item{x}{Numeric vector for first group}

\item{y}{Numeric vector for second group}

\item{pts}{Numeric. Threshold value. If NULL, estimated as qth quantile of differences}

\item{q}{Numeric. Quantile to use if \code{pts} is NULL (default: 0.25)}

\item{nboot}{Integer. Number of bootstrap samples (default: 1000)}

\item{estfun}{Function. Quantile estimation function (default: \code{hdmq})}

\item{alpha}{Numeric. Significance level (default: 0.05)}

\item{SEED}{Logical. If TRUE, set random seed to 2 for reproducibility (default: TRUE)}
}
\value{
List with components:
\item{Est}{Estimated probability P(X - Y < threshold)}
\item{ci}{Two-element vector with lower and upper confidence limits}
}
\description{
Computes a bootstrap confidence interval for the probability that the difference
between two independent variables falls below a specified threshold.
}
\details{
This function estimates P(X - Y < threshold) and computes a percentile bootstrap
confidence interval for this probability. If \code{pts} is not specified, it is
estimated as the qth quantile of all pairwise differences.

The bootstrap resamples independently from X and Y, computes all pairwise
differences for each bootstrap sample, and estimates the probability for each.
}
\examples{
x <- rnorm(30)
y <- rnorm(30, mean = 0.5)
tailci(x, y, pts = 0, nboot = 500)

}
\seealso{
\code{\link{tailci.reverse}}, \code{\link{hdmq}}
}
\keyword{htest}
