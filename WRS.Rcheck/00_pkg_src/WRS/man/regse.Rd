% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression-advanced.R
\name{regse}
\alias{regse}
\title{Bootstrap Standard Errors for Regression Parameters}
\usage{
regse(
  x,
  y,
  xout = FALSE,
  regfun = tsreg,
  outfun = outpro,
  nboot = 200,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{x}{A numeric matrix or vector containing predictor values.}

\item{y}{A numeric vector containing the response variable.}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{regfun}{Regression function to use (default: \code{tsreg} for Theil-Sen).
Can be any regression function that returns a \code{coef} component (e.g.,
\code{tsreg}, \code{ltsreg}, \code{opreg}, \code{qreg}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{nboot}{Number of bootstrap samples (default: 200).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
A list with components:
\item{param.estimates}{Vector of regression parameter estimates (intercept
and slopes)}
\item{covar}{Covariance matrix of the parameter estimates. Diagonal elements
are squared standard errors.}
\item{s.e.}{Vector of standard errors for each parameter (square roots of
covariance diagonal)}
}
\description{
Estimates standard errors and the covariance matrix for regression parameter
estimates using bootstrap resampling. Works with any regression estimator
(robust or classical).
}
\details{
This function uses bootstrap resampling to estimate the sampling variability
of regression parameters:
\enumerate{
\item Fits the specified regression to the observed data
\item Generates \code{nboot} bootstrap samples by resampling observations
\item Refits the regression to each bootstrap sample
\item Computes the sample covariance of bootstrap parameter estimates
}

The bootstrap standard errors are appropriate for making inferences about
the regression parameters under general conditions (non-normality,
heteroscedasticity).

If \code{xout = TRUE}, outliers are removed from the predictor space before
analysis using the function specified by \code{outfun}.
}
\examples{
\dontrun{
# Standard errors for Theil-Sen regression
set.seed(123)
x <- matrix(rnorm(100), ncol=2)
y <- 2 + 3*x[,1] - x[,2] + rnorm(50)
regse(x, y)

# Standard errors for LTS regression
regse(x, y, regfun=ltsreg)
}
}
\references{
Wilcox, R.R. (2022). Introduction to Robust Estimation and Hypothesis Testing
(5th ed.). Academic Press.
}
\seealso{
\code{\link{tsreg}}, \code{\link{regbtci}}, \code{\link{regci}}
}
