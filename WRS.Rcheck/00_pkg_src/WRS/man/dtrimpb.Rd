% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/location.R
\name{dtrimpb}
\alias{dtrimpb}
\title{Percentile Bootstrap Multiple Comparisons with Trimmed Means}
\usage{
dtrimpb(
  x,
  y = NULL,
  alpha = 0.05,
  con = 0,
  est = tmean,
  plotit = TRUE,
  dif = TRUE,
  grp = NA,
  hoch = TRUE,
  nboot = NA,
  xlab = "Group 1",
  ylab = "Group 2",
  pr = TRUE,
  SEED = TRUE,
  BA = FALSE,
  PCI = FALSE,
  ylab.ebar = NULL,
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{con}{Contrast matrix or 0 for all pairwise comparisons.}

\item{est}{Estimator function to use for computing location or other
statistics. Common choices include \code{mean}, \code{median}, \code{tmean} (trimmed mean),
\code{mom} (modified one-step M-estimator), \code{mest} (M-estimator).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{dif}{Logical. If TRUE, uses difference scores (dependent groups).}

\item{grp}{A vector indicating group membership. Can be numeric, factor, or
character. Used for grouping observations in multi-group comparisons.}

\item{hoch}{Logical. If TRUE, applies Hochberg p-value adjustment.}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{xlab, ylab, ylab.ebar}{Axis labels.}

\item{pr}{Logical. If \code{TRUE}, prints progress messages (default: \code{TRUE}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{BA}{Logical. For Brunner-Munzel-type adjustment.}

\item{PCI}{Logical. For projection-based CI.}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
Matrix with estimates, CIs, and p-values for each comparison/contrast.
}
\description{
Compares all pairs or tests specified contrasts using trimmed means and percentile bootstrap.
}
\seealso{
\code{\link{lincon}}, \code{\link{btrim}}, \code{\link{mcppb20}}
}
