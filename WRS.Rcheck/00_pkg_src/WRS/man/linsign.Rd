% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{linsign}
\alias{linsign}
\title{Estimate Probability that Linear Contrast is Negative}
\usage{
linsign(x, con, nreps = 200, SEED = TRUE, nmax = 10^8)
}
\arguments{
\item{x}{Data in matrix or list format. For matrix: each column is a group.
For list: x[\link{j}] contains data for group j.}

\item{con}{Numeric vector of contrast coefficients (length J). Must sum to zero.}

\item{nreps}{Number of resampling iterations (default: 200).}

\item{SEED}{Logical. If TRUE (default), sets random seed to 2 for reproducibility.}

\item{nmax}{Maximum product of sample sizes (default: 10^8). If exceeded, reduces
resampling size to min(nmin, 100).}
}
\value{
Scalar probability (0 to 1) that the linear contrast is negative.
}
\description{
Estimates the probability that a linear contrast Y = sum(c_j * X_j) is less than zero
using resampling.
}
\details{
This function:
\itemize{
\item Resamples equal-sized groups (size = min sample size)
\item Computes linear contrasts for each resampled dataset
\item Estimates P(Y < 0) for each iteration
\item Returns average probability across iterations
}

When the product of sample sizes exceeds nmax, the resampling size is reduced
to prevent excessive computation.
}
\examples{
# Estimate P(X1 < X2)
set.seed(123)
x <- list(rnorm(40), rnorm(40, mean = 0.3))
result <- linsign(x, con = c(1, -1), nreps = 500)
result  # Probability that group 1 < group 2
}
\seealso{
\code{\link{linQS}}, \code{\link{linEP}}, \code{\link{linWMWMC.sub}}
}
