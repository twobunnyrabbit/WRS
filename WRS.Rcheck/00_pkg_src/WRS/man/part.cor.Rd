% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{part.cor}
\alias{part.cor}
\title{Robust Partial Correlation}
\usage{
part.cor(
  x,
  y,
  z,
  corfun = wincor,
  regfun = MMreg,
  plotit = FALSE,
  xout = FALSE,
  GEN = TRUE,
  BOOT = TRUE,
  SEED = TRUE,
  nboot = 599,
  XOUT.blp = TRUE,
  plot.out = FALSE,
  outfun = outpro,
  plotfun = plot,
  xlab = "Res 1",
  ylab = "Res 2",
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{z}{Variable(s) to control for (can be vector or matrix).}

\item{corfun}{Correlation function to use. Common options: \code{pbcor} (percentage
bend), \code{pcor} (Pearson), \code{wincor} (winsorized), \code{spear} (Spearman).}

\item{regfun}{Robust regression function (default: \code{MMreg}).}

\item{plotit}{Logical. Create scatterplot of residuals (default: \code{FALSE}).}

\item{xout}{Logical. Remove leverage points (default: \code{FALSE}).}

\item{GEN}{Logical. Remove only bad leverage points (default: \code{TRUE}).}

\item{BOOT}{Logical. Compute bootstrap CI (default: \code{TRUE}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{nboot}{Number of bootstrap samples (default: 599).}

\item{XOUT.blp}{Logical. Remove bad leverage points for x vs z and y vs z
regressions (default: \code{TRUE}).}

\item{plot.out}{Logical. Plot outliers (default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{plotfun}{Plotting function (default: \code{plot}).}

\item{xlab, ylab}{Labels for residual plot.}

\item{...}{Additional arguments passed to \code{corfun}.}
}
\value{
List with components:
\item{cor}{Partial correlation estimate.}
\item{p.value}{P-value for testing zero partial correlation (if BOOT = TRUE).}
\item{ci}{Bootstrap confidence interval (if BOOT = TRUE).}
}
\description{
Computes robust partial correlation between x and y controlling for z,
based on residuals from robust regression.
}
\details{
Computes partial correlation by:
\enumerate{
\item Regressing x on z to get residuals res_x
\item Regressing y on z to get residuals res_y
\item Computing correlation between res_x and res_y
}

Default uses Winsorized correlation between residuals from MM-regression.

\strong{Outlier handling:}
\itemize{
\item XOUT.blp = TRUE: Permanently removes bad leverage points
\item GEN = TRUE: Removes only bad leverage points (not all leverage points)
}
}
\examples{
\dontrun{
set.seed(123)
x <- rnorm(50)
z <- rnorm(50)
y <- 0.5 * x + 0.3 * z + rnorm(50)
part.cor(x, y, z, nboot = 500)
}
}
\seealso{
\code{\link{pcor}}, \code{\link{wincor}}, \code{\link{MMreg}}
}
