% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outliers.R
\name{Depth.class.bag}
\alias{Depth.class.bag}
\title{Bagged Depth-Based Classification}
\usage{
Depth.class.bag(
  train = NULL,
  test = NULL,
  g = NULL,
  x1 = NULL,
  x2 = NULL,
  depthfun = prodepth,
  DIST = FALSE,
  nboot = 100,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{train}{Training data matrix (optional if \code{x1} and \code{x2} provided).}

\item{test}{Test data matrix to classify.}

\item{x1, x2}{Training data for groups 1 and 2 (alternative to \code{train}/\code{g}).}

\item{depthfun}{Depth function to use (default: prodepth).}

\item{DIST}{Logical. If TRUE, uses distance-based classification.}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{...}{Additional arguments passed to \code{depthfun}.}
}
\value{
Vector of predicted class labels (1 or 2) for test observations.
}
\description{
Bootstrap aggregating (bagging) version of depth-based classification.
Reduces bias when sample sizes differ between groups.
}
\details{
Uses bootstrap bagging to reduce classification bias when n1 ≠ n2. For each
bootstrap sample, draws equal-sized samples from both groups and classifies
test data. Final classification is by majority vote across bootstrap samples.
}
\seealso{
\code{\link{discdepth}}, \code{\link{KNNbag}}
}
