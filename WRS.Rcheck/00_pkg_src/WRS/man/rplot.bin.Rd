% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{rplot.bin}
\alias{rplot.bin}
\title{Running Interval Smoother for Binary Outcomes}
\usage{
rplot.bin(
  x,
  y,
  est = mean,
  scat = TRUE,
  fr = NULL,
  plotit = TRUE,
  pyhat = FALSE,
  pts = x,
  LP = FALSE,
  theta = 50,
  phi = 25,
  scale = TRUE,
  expand = 0.5,
  SEED = TRUE,
  nmin = 0,
  xout = FALSE,
  outfun = outpro,
  xlab = NULL,
  ylab = NULL,
  zlab = "P(Y=1)",
  pr = TRUE,
  duplicate = "error",
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix of predictor values.}

\item{y}{Binary response vector (will be converted to 0/1 if needed).}

\item{est}{Measure of location for probability estimation (default: \code{mean}).}

\item{scat}{Logical; if \code{TRUE} (default), shows scatter plot.}

\item{fr}{Span for running interval. If \code{NULL} (default), uses 0.8 for
univariate and 1.2 for multivariate predictors.}

\item{plotit}{Logical; if \code{TRUE} (default), creates a plot.}

\item{pyhat}{Logical; if \code{TRUE}, returns predicted probabilities
(default: \code{FALSE}).}

\item{pts}{Points at which to compute predictions (default: \code{x}).}

\item{LP}{Logical; if \code{TRUE}, uses lowess/loess smoothing (default: \code{FALSE}).}

\item{theta}{Rotation angle for 3D plots (default: 50).}

\item{phi}{Colatitude angle for 3D plots (default: 25).}

\item{scale}{Logical; if \code{TRUE} (default), uses Mahalanobis distance for
multivariate predictors.}

\item{expand}{Expansion factor for 3D plots (default: 0.5).}

\item{SEED}{Logical; if \code{TRUE} (default), sets random seed.}

\item{nmin}{Minimum number of points in neighborhood (default: 0).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{xlab}{Label for x-axis in plots (default: "X" or "VAR 1").}

\item{ylab}{Label for y-axis in plots (default: "Y" or "VAR 2").}

\item{zlab}{Label for z-axis (default: "P(Y=1)").}

\item{pr}{Logical; if \code{TRUE} (default), prints warnings about scaling.}

\item{duplicate}{How to handle duplicate predictor values in 3D plots
(default: "error"). Use "strip" if duplicates cause plotting issues.}

\item{...}{Additional arguments passed to outlier detection function.}
}
\value{
If \code{pyhat=TRUE}, returns predictions from \code{rplot.pred()}.
Otherwise returns "DONE".
}
\description{
Applies a running interval smoother specifically designed for binary
response variables. Estimates and plots P(Y=1|X) as a function of
predictor values. Alternative to logistic regression.
}
\details{
The function:
\enumerate{
\item Converts y to 0/1 coding (max value → 1, min value → 0)
\item Applies running interval smoother with \code{est=mean} to estimate P(Y=1|X)
\item Plots probability surface
}

For multivariate predictors, warns if binary independent variables are
detected (use \code{rplot.binv2()} instead). Prints warnings about scaling
options based on whether independence is expected.
}
\examples{
# Binary logistic-like relationship
x <- rnorm(200)
p <- plogis(2*x)  # True probabilities
y <- rbinom(200, 1, p)
rplot.bin(x, y, fr=0.5)
}
\seealso{
\code{\link{rplot}}, \code{\link{rplot.binCI}},
\code{\link{rplot.pred}}
}
