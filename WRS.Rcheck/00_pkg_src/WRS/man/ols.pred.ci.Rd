% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{ols.pred.ci}
\alias{ols.pred.ci}
\title{OLS Regression Line with Prediction Confidence Interval}
\usage{
ols.pred.ci(
  x,
  y,
  xlab = "X",
  ylab = "Y",
  alpha = 0.05,
  xout = FALSE,
  RETURN = FALSE,
  newx = NULL
)
}
\arguments{
\item{x}{Numeric vector. Predictor variable (single predictor only).}

\item{y}{Numeric vector. Response variable.}

\item{xlab}{Character. X-axis label (default: "X").}

\item{ylab}{Character. Y-axis label (default: "Y").}

\item{alpha}{Numeric. Significance level for confidence interval (default: 0.05 for 95\\% CI).}

\item{xout}{Logical. If TRUE, removes leverage points in \code{x} before fitting (default: FALSE).}

\item{RETURN}{Logical. If TRUE, returns predicted values and CI instead of just plotting (default: FALSE).}

\item{newx}{Numeric vector or NULL. X-values for prediction. If NULL, uses
\code{seq(min(x), max(x), 0.1)} (default: NULL).}
}
\value{
If \code{RETURN=TRUE}: Matrix with columns \code{fit}, \code{lwr}, \code{upr}
for each value in \code{newx}. Otherwise NULL (produces plot only).
}
\description{
Plots OLS regression line with confidence intervals for predicted values.
Optionally removes leverage points before fitting.
}
\details{
\strong{Procedure}:
\enumerate{
\item Removes missing values from \code{(x, y)} pairs
\item If \code{xout=TRUE}, removes leverage points using \code{\link{out}}
\item Fits OLS regression
\item Computes confidence interval for mean predicted value (not prediction interval)
\item Plots data, regression line, and dashed CI bounds
}

\strong{Note}: This computes a \strong{confidence interval for the regression line}
(confidence band for E\link{Y|X}), not a prediction interval for future observations.

Leverage point removal can improve robustness when extreme X-values are present.
}
\seealso{
\code{\link{ols}}, \code{\link{out}} for leverage detection
}
