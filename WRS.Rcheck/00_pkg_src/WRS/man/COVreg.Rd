% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression-advanced.R
\name{COVreg}
\alias{COVreg}
\title{Regression Estimation via Robust Covariance Matrix}
\usage{
COVreg(
  x,
  y,
  cov.fun = MARest,
  loc.fun = MARest,
  xout = FALSE,
  outfun = out,
  ...
)
}
\arguments{
\item{x}{A numeric matrix containing predictor values. Can have multiple columns
for multiple predictors.}

\item{y}{A numeric vector containing the response variable.}

\item{cov.fun}{Covariance function for robust estimation (default: \code{MARest}).
Must return a component \verb{$cov} with the covariance matrix.}

\item{loc.fun}{Location function for robust center estimation (default: \code{MARest}).
Must return a component \verb{$center} with location estimates.}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
A list with components:
\item{coef}{Vector of regression coefficients. First element is intercept,
remaining elements are slopes for each predictor.}
\item{residuals}{Vector of residuals (y - fitted values).}
}
\description{
Performs regression estimation using robust covariance and location estimators
instead of the classical maximum likelihood approach. This provides resistance
to outliers in both predictors and response.
}
\details{
Classical regression estimation via maximum likelihood uses the sample
covariance matrix, which is sensitive to outliers. This function replaces
the classical covariance with a robust estimator:

The regression coefficients are derived from the robust covariance matrix
AC of (X,Y):
\itemize{
\item Slope = solve(AC_XX, AC_XY) where AC_XX is covariance of X and
AC_XY is covariance between X and Y
\item Intercept = center_Y - slopes \%*\% center_X
}

Default uses \code{MARest()} for both covariance and location, but any robust
estimator can be substituted (e.g., \code{rmba}, \code{covmba}, \code{covmcd}).

If \code{xout = TRUE}, outliers in the predictor space are removed before
estimation using the function specified by \code{outfun}.
}
\examples{
\dontrun{
# Robust regression via covariance matrix
set.seed(123)
x <- matrix(rnorm(100), ncol = 2)
y <- 2 + 3*x[,1] - x[,2] + rnorm(50)
result <- COVreg(x, y)
result$coef

# With outlier removal
COVreg(x, y, xout = TRUE)
}
}
\references{
Wilcox, R.R. (2022). Introduction to Robust Estimation and Hypothesis Testing
(5th ed.). Academic Press.
}
\seealso{
\code{\link{mlrreg}}, \code{\link{mulgreg}}, \code{\link{tsreg}},
\code{\link{rmba}}, \code{\link{covmba}}
}
