% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{rexgauss}
\alias{rexgauss}
\title{Generate Random Ex-Gaussian Variables}
\usage{
rexgauss(n, mu = 0, sigma = 1, rate = 1)
}
\arguments{
\item{n}{Number of observations to generate.}

\item{mu}{Mean of the Gaussian component (default: 0).}

\item{sigma}{Standard deviation of the Gaussian component (default: 1).}

\item{rate}{Rate parameter (1/mean) of the exponential component (default: 1).}
}
\value{
A numeric vector of length \code{n} containing random Ex-Gaussian
variates.
}
\description{
Generates random variates from an Ex-Gaussian (exponentially modified
Gaussian) distribution, which is the convolution of a normal and an
exponential distribution.
}
\details{
The Ex-Gaussian distribution is defined as \code{X = Z + E}, where:
\itemize{
\item \code{Z ~ Normal(mu, sigma^2)}
\item \code{E ~ Exponential(rate)}
}

This distribution is commonly used in psychology and cognitive science to
model reaction times, which typically show right-skewed distributions.

The overall mean is \code{mu + 1/rate} and the overall variance is
\code{sigma^2 + 1/rate^2}.
}
\examples{
# Generate 100 Ex-Gaussian random variables
# with mu=500, sigma=50, and exponential mean=100
\dontrun{
rt <- rexgauss(100, mu=500, sigma=50, rate=1/100)
hist(rt, main="Ex-Gaussian Distribution")
}

}
\seealso{
\code{\link{rnorm}}, \code{\link{rexp}}
}
\keyword{datagen}
\keyword{distribution}
