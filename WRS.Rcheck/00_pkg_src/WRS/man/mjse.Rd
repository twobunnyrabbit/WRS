% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{mjse}
\alias{mjse}
\title{Maritz-Jarrett Standard Error Estimate}
\usage{
mjse(x, q = 0.5, na.rm = FALSE)
}
\arguments{
\item{x}{Numeric vector of data values.}

\item{q}{Quantile position. Default is 0.5 (median).}

\item{na.rm}{Logical. If \code{TRUE}, removes missing values. Default is \code{FALSE}.}
}
\value{
Numeric value: the Maritz-Jarrett standard error estimate.
}
\description{
Computes the Maritz-Jarrett estimate of the standard error of the
m-th order statistic, where m = floor(qn + 0.5).
}
\details{
The Maritz-Jarrett estimator provides a standard error estimate for quantiles
based on order statistics. It uses weights derived from the beta distribution
to compute the variance of the order statistic.

The weights are: \code{w_i = pbeta(i/n, m-1, n-m) - pbeta((i-1)/n, m-1, n-m)}

The standard error is then computed as \code{sqrt(sum(w * y^2) - (sum(w * y))^2)},
where y is the sorted data.
}
\references{
Maritz, J.S. and Jarrett, R.G. (1978). A note on estimating the variance of
the sample median. Journal of the American Statistical Association, 73, 194-196.
}
\keyword{robust}
\keyword{univar}
