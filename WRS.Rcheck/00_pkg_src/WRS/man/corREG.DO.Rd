% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{corREG.DO}
\alias{corREG.DO}
\title{Decision-Oriented Predictor Selection by Correlation}
\usage{
corREG.DO(
  x,
  y,
  corfun = wincor,
  alpha = 0.05,
  nboot = 500,
  SEED = TRUE,
  neg.col = NULL,
  LARGEST = TRUE,
  xout = FALSE,
  outfun = outpro,
  ...
)
}
\arguments{
\item{x}{Matrix (n-by-p) of independent variables.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{corfun}{Correlation function to use. Common options: \code{pbcor} (percentage
bend), \code{pcor} (Pearson), \code{wincor} (winsorized), \code{spear} (Spearman).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{nboot}{Number of bootstrap samples (default: 500).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{neg.col}{Vector of column indices to negate before computing correlations
(default: \code{NULL}).}

\item{LARGEST}{Logical. Identify predictor with largest correlation if \code{TRUE}
(default), smallest if \code{FALSE}.}

\item{xout}{Logical. Remove outliers from x before analysis (default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{...}{Additional arguments passed to \code{corfun}.}
}
\value{
List with components:
\item{output}{Matrix with columns: Best.IV, IV, Est.best, Est, dif, ci.low, ci.up.}
\item{Result}{Character string: "Decide IV X is best" or "No Decision".}
}
\description{
Determines the best predictor by correlation and makes a formal decision
based on pairwise comparisons with all other predictors.
}
\details{
Identifies the predictor with the strongest correlation, then performs
pairwise comparisons between this predictor and all others using \code{TWOpov()}.
Makes a decision if all pairwise confidence intervals exclude zero (i.e., all
have the same sign for lower and upper bounds).
}
\examples{
\dontrun{
set.seed(123)
x <- matrix(rnorm(50 * 4), ncol = 4)
y <- 0.8 * x[, 1] + 0.2 * x[, 2] + rnorm(50)
corREG.DO(x, y, nboot = 500)
}
}
\seealso{
\code{\link{corREG.best}}, \code{\link{TWOpov}}, \code{\link{corCOM.DVvsIV}}
}
