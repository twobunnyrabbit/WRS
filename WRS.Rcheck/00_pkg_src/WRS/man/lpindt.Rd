% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{lpindt}
\alias{lpindt}
\title{Test for Association Using LOESS (Cleveland's Method)}
\usage{
lpindt(x, y, nboot = 500, xout = FALSE, outfun = out)
}
\arguments{
\item{x}{Vector, matrix, or data frame of predictor variable(s).}

\item{y}{Vector of response values.}

\item{nboot}{Integer. Number of bootstrap samples (default: 500).}

\item{xout}{Logical. If \code{TRUE}, remove outliers from \code{x} before analysis
using \code{outfun} (default: \code{FALSE}).}

\item{outfun}{Function to detect outliers (default: \code{\link{out}}).
Only used if \code{xout=TRUE}.}
}
\value{
A list with components:
\item{Explanatory.power}{Strength of association from LOESS fit on original data}
\item{p.value}{Bootstrap p-value for testing no association}
}
\description{
Tests the hypothesis of no association between predictor(s) and response using
a bootstrap approach based on Cleveland's LOESS (Locally Weighted Scatterplot
Smoothing) fit. The test compares the explanatory power of the LOESS fit to a
bootstrap null distribution.
}
\details{
The function:
\enumerate{
\item Removes missing values and optionally outliers from the data
\item Fits LOESS regression using \code{\link{lplot}}
\item Generates \code{nboot} bootstrap samples by independently resampling
indices for \code{x} and \code{y} to create data under null hypothesis
\item Computes explanatory power for each bootstrap sample
\item Calculates p-value as proportion of bootstrap values exceeding observed value
}

The test evaluates whether the observed association is stronger than would be
expected by chance under independence.
}
\seealso{
\code{\link{lplot}} for LOESS regression plotting
}
\keyword{htest}
\keyword{nonparametric}
