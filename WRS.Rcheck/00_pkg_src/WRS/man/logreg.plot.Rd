% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{logreg.plot}
\alias{logreg.plot}
\title{Plot Logistic Regression with MLE and/or Robust Estimates}
\usage{
logreg.plot(
  x,
  y,
  MLE = TRUE,
  ROB = FALSE,
  xlab = NULL,
  ylab = NULL,
  zlab = "P(Z=1)",
  xout = FALSE,
  outfun = outpro,
  theta = 50,
  phi = 25,
  duplicate = "error",
  LP = TRUE,
  Lspan = 0.75,
  pyhat = FALSE,
  LABELS = FALSE,
  WARN = FALSE,
  BY = TRUE,
  expand = 0.5,
  scale = TRUE,
  fr = 2,
  ticktype = "simple",
  pr = TRUE,
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix of predictor values. For matrices with
more than one column, only the first 1-2 columns are used.}

\item{y}{Numeric vector of binary outcomes (0 or 1).}

\item{MLE}{Logical; if \code{TRUE}, plots the MLE logistic regression line (solid).}

\item{ROB}{Logical; if \code{TRUE}, plots the robust logistic regression line (dashed).}

\item{xlab, ylab, zlab}{Axis labels. Defaults: NULL (auto-generated), NULL, 'P(Z=1)'.}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro}).}

\item{theta, phi}{Viewing angles for 3D plot (defaults: 50, 25).}

\item{duplicate}{How to handle duplicate points in 3D (default: "error").}

\item{LP}{Logical; if \code{TRUE}, uses LOESS for 3D plotting.}

\item{Lspan}{Span parameter for LOESS (default: 0.75).}

\item{pyhat}{Logical; if \code{TRUE}, returns predicted probabilities.}

\item{LABELS}{Logical; if \code{TRUE}, uses data labels for axis names.}

\item{WARN}{Logical; if \code{FALSE}, suppresses warnings from robust methods.}

\item{BY}{Logical; if \code{TRUE}, uses BY robust estimator; else weighted estimator.}

\item{expand, scale}{3D plot parameters for \code{persp()} (defaults: 0.5, TRUE).}

\item{fr}{Span for LOESS (default: 2).}

\item{ticktype}{Tick type for 3D plot (default: "simple").}

\item{pr}{Logical; if \code{TRUE}, prints messages.}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
If \code{pyhat = TRUE}, returns matrix of predicted probabilities;
otherwise returns "Done".
}
\description{
Plots logistic regression curves for binary outcomes using maximum likelihood
estimation (MLE) and/or robust estimation. Supports one or two predictors,
creating either 2D line plots or 3D surface plots.
}
\details{
For \strong{one predictor}:
\itemize{
\item MLE fit plotted as solid line using \code{\link{logreg}}
\item Robust fit plotted as dashed line using \code{wlogreg} or \code{BYlogreg}
}

For \strong{two predictors}:
\itemize{
\item Creates 3D surface plot of predicted probabilities using LOESS
\item Only works when \code{LP = TRUE}
}

The function requires the \code{robustbase} package for robust estimation.
}
\examples{
\dontrun{
# Binary outcome data
x <- rnorm(100)
p <- exp(0.5 + 1.2*x) / (1 + exp(0.5 + 1.2*x))
y <- rbinom(100, 1, p)

# Plot both MLE and robust fits
logreg.plot(x, y, MLE=TRUE, ROB=TRUE)
}
}
\seealso{
\code{\link{logreg}}, \code{\link{logreg.pred}}, \code{\link{lplot}}
}
