% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{lrdata}
\alias{lrdata}
\title{Generate Simulated Data for Logistic Regression}
\usage{
lrdata(n = 200, type = 3)
}
\arguments{
\item{n}{Integer. Total sample size, must be even (default: 200). Half of
observations have Y=0 and half have Y=1.}

\item{type}{Integer (1, 2, or 3). Specifies the configuration of true coefficients
(default: 3):
\itemize{
\item \code{type=1}: beta\link{1}=1, other betas=0, alpha=-0.5
\item \code{type=2}: all betas=1, alpha=-2.5
\item \code{type=3}: beta\link{1:3}=1, other betas=0, alpha=-1.5
}}
}
\value{
A list with components:
\item{alpha}{True intercept parameter}
\item{beta}{Vector of 5 true slope parameters}
\item{lrcoef}{Estimated coefficients from GLM fit (intercept + 5 slopes)}
\item{x}{n × 5 matrix of predictor values}
\item{y}{Vector of n binary responses (0 or 1)}
}
\description{
Creates simulated data for logistic regression with controlled structure.
Generates binary response and 5 predictors from normal distributions where
X|Y=0 ~ N(0,I) and X|Y=1 ~ N(beta,I), allowing controlled testing of
logistic regression methods.
}
\details{
The function generates data where:
\itemize{
\item First n/2 observations have Y=0, X ~ N(0,I)
\item Second n/2 observations have Y=1, X ~ N(beta,I)
\item I is the 5×5 identity matrix
\item Relationship: alpha = -0.5 ||beta||^2
}

A logistic regression model is fit using \code{glm} with all 5 predictors,
and the estimated coefficients are returned for comparison with true values.
}
\note{
If the number of predictors (q) is changed from 5, the GLM formula
must be updated accordingly.
}
\keyword{datagen}
