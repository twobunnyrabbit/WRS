% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outliers.R
\name{outproMC}
\alias{outproMC}
\title{Projection-Based Outlier Detection (Multicore Version)}
\usage{
outproMC(
  m,
  gval = NA,
  center = NA,
  plotit = TRUE,
  op = TRUE,
  MM = FALSE,
  cop = 3,
  xlab = "VAR 1",
  ylab = "VAR 2",
  STAND = TRUE,
  tr = 0.2,
  q = 0.5,
  pr = TRUE,
  ...
)
}
\arguments{
\item{m}{A numeric matrix where rows represent observations and columns
represent variables.}

\item{gval}{Critical value for outlier detection. Default is
\code{sqrt(qchisq(0.975, p))} for most centers, or \code{sqrt(qchisq(0.95, p))} for
cop=1, where p is the number of columns.}

\item{center}{Optional center point. If NA, computed based on \code{cop} argument.}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{op}{Logical. If \code{TRUE}, plots the 0.5 depth contour based on data with
outliers removed. If \code{FALSE}, plots depth contour without removing outliers.}

\item{MM}{Logical. If \code{TRUE}, uses MAD for dispersion. If \code{FALSE}, uses
interquartile range for outlier detection.}

\item{cop}{Integer (1-7) specifying method to compute center of data cloud:
\itemize{
\item 1: Deepest point (Tukey median)
\item 2: MCD (Minimum Covariance Determinant) center
\item 3: Coordinate-wise median (default)
\item 4: MVE (Minimum Volume Ellipsoid) center
\item 5: TBS center
\item 6: RMBA (Olive's median ball algorithm)
\item 7: Spatial (L1) median
}}

\item{xlab}{Label for x-axis in bivariate plots.}

\item{ylab}{Label for y-axis in bivariate plots.}

\item{STAND}{Logical. If \code{TRUE}, standardizes marginal distributions before
checking for outliers (recommended when variables are on different scales).}

\item{tr}{Trimming proportion (default: 0.2). Proportion of observations to
trim from each tail when computing winsorized correlation.}

\item{q}{Quantile to estimate. For example, \code{q = 0.5} estimates the median,
\code{q = 0.25} estimates the first quartile (default: 0.5).}

\item{pr}{Logical. If \code{TRUE}, prints progress messages (default: \code{TRUE}).}

\item{...}{Additional arguments (not currently used).}
}
\value{
A list with components:
\item{out.id}{Vector of row indices identified as outliers.}
\item{keep}{Vector of row indices for non-outliers.}
}
\description{
Detects outliers using a modification of the Stahel-Donoho projection method
with parallel processing for improved performance. Projects data points onto
lines connecting each point to the center, then uses boxplot-based detection
on the projected distances.
}
\details{
This function is the parallel processing version of \code{outpro()}, using
\code{mclapply()} for improved performance on multicore systems. For bivariate
data with \code{plotit=TRUE}, creates a scatterplot marking outliers with "o" and
non-outliers with "*", and plots the 0.5 depth contour. The Donoho-Gasko
(Tukey) median is marked with "+".

When using STAND=FALSE with variables on different scales, a warning is
printed suggesting STAND=TRUE may be more appropriate.
}
\seealso{
\code{\link{outpro}}, \code{\link{outproad}}, \code{\link{outproadMC}}
}
