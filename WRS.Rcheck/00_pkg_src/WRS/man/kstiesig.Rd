% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{kstiesig}
\alias{kstiesig}
\title{Kolmogorov-Smirnov Test with Tied Values}
\usage{
kstiesig(x, y, val)
}
\arguments{
\item{x}{Numeric vector containing first group data.}

\item{y}{Numeric vector containing second group data.}

\item{val}{Observed value of the K-S test statistic.}
}
\value{
Numeric p-value for the two-sided K-S test, accounting for ties.
}
\description{
Computes the exact significance level (p-value) for the two-sample
Kolmogorov-Smirnov test statistic when the data contain tied values.
}
\details{
This function extends the standard K-S test to handle tied values by
modifying the counting algorithm. When tied values are detected in the
pooled sample, the algorithm adjusts the probability calculation to
account for the reduced information due to ties.

The function uses dynamic programming similar to \code{\link{kssig}}, but
with special handling when \code{z[k] == z[k+1]} (indicating a tie).

For data without ties, use \code{\link{kssig}} for potentially better
numerical stability.
}
\seealso{
\code{\link{kssig}}, \code{\link{ks}}, \code{\link{ksties.sub}}
}
\keyword{htest}
\keyword{internal}
