% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{DanctspbMC}
\alias{DanctspbMC}
\title{Dependent Groups ANCOVA Using Theil-Sen with Bootstrap (Multicore)}
\usage{
DanctspbMC(
  x1,
  y1,
  x2,
  y2,
  pts = NULL,
  regfun = tshdreg,
  fr1 = 1,
  fr2 = 1,
  alpha = 0.05,
  SCAT = TRUE,
  plotit = TRUE,
  xout = FALSE,
  outfun = outpro,
  nboot = 500,
  SEED = TRUE,
  xlab = "X",
  ylab = "Y",
  WARN = FALSE,
  pr = TRUE,
  eout = FALSE,
  ...
)
}
\arguments{
\item{pts}{Design points for comparison. If NULL, automatically selected.}

\item{regfun}{Regression function. Default is tshdreg (Theil-Sen for tied values).}

\item{alpha}{Significance level. Default is 0.05.}

\item{SCAT}{Logical. Show scatter plot. Default is TRUE.}

\item{plotit}{Logical. Create comparison plot. Default is TRUE.}

\item{xout}{Logical. Remove outliers based on x values. Default is FALSE.}

\item{outfun}{Outlier detection function. Default is outpro.}

\item{nboot}{Number of bootstrap samples. Default is 500.}

\item{SEED}{Logical. Set random seed. Default is TRUE.}

\item{xlab, ylab}{Axis labels for plot.}

\item{WARN}{Logical. Show warnings. Default is FALSE.}

\item{pr}{Logical. Print progress messages. Default is TRUE.}

\item{eout}{Logical. Remove error outliers. Default is FALSE.}

\item{...}{Additional arguments passed to regfun.}
}
\value{
List with test results and design points.
}
\description{
Compare regression lines of two dependent groups using robust regression with
percentile bootstrap. Multicore-enabled version using parallel processing.
}
\details{
Similar to Danctspb but uses parallel processing for faster bootstrap computation.
Bootstrap samples are obtained from the full data rather than conditioning on x values,
similar to approach used in regci. For OLS regression, use Dancols instead.
}
\examples{
\dontrun{
  x <- rnorm(30)
  y1 <- 2*x + rnorm(30)
  y2 <- 2*x + 0.5 + rnorm(30)
  result <- DanctspbMC(x, y1, x, y2, nboot = 1000)
}
}
\seealso{
\code{\link{Danctspb}}, \code{\link{Dancts}}, \code{\link{tshdreg}}
}
\keyword{ancova}
\keyword{bootstrap}
\keyword{dependent}
\keyword{parallel}
\keyword{robust}
