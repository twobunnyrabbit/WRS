% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression.R
\name{taureg}
\alias{taureg}
\title{Correlation-Based Regression using Kendall's Tau}
\usage{
taureg(m, y, corfun = tau, ...)
}
\arguments{
\item{m}{Numeric matrix of predictor variables (n by p).}

\item{y}{Numeric vector of the response variable (length n).}

\item{corfun}{Correlation function to use (default: \code{tau} for Kendall's tau).
Must return a list with components \code{$cor} and \code{$p.value}.
Alternatives: \code{pbcor}, \code{wincor}, \code{scor}.}

\item{...}{Additional arguments passed to \code{corfun}.}
}
\value{
A list with components:
\describe{
\item{cor}{Vector of correlations between y and each column of m}
\item{p.value}{Vector of two-sided p-values for testing each correlation = 0}
}
}
\description{
Computes correlations between a response variable and each predictor using
Kendall's tau (or another specified correlation function). Useful for screening
predictors and identifying potentially important variables for regression.
}
\details{
This function is useful for exploring relationships between a response
variable and multiple predictors using robust correlation measures. It
computes the correlation between y and each column of m separately.

\strong{Kendall's Tau} (default):
\itemize{
\item Robust to outliers and heavy-tailed distributions
\item Based on concordance/discordance of pairs
\item Range: \link{-1, 1}
\item Interpretation similar to Pearson correlation but more robust
}

\strong{Alternative correlation functions}:
\itemize{
\item \code{pbcor}: Percentage bend correlation (robust alternative to Pearson)
\item \code{wincor}: Winsorized correlation
\item \code{scor}: Spearman's rho (rank-based)
\item Any function returning \code{$cor} and \code{$p.value}
}

\strong{Use cases}:
\itemize{
\item Variable screening in high-dimensional regression
\item Identifying important predictors before model building
\item Exploratory data analysis
\item When relationship may be monotonic but not linear
}

\strong{Note}: This is not a regression fit but rather a correlation analysis.
For actual regression using correlation-based methods, see \code{\link{correg}}
or \code{\link{scorreg}}.
}
\examples{
# Correlation analysis with multiple predictors
set.seed(123)
x1 <- rnorm(100)
x2 <- rnorm(100)
x3 <- rnorm(100)
y <- 2*x1 - x2 + rnorm(100)
m <- cbind(x1, x2, x3)

# Kendall's tau for each predictor
result <- taureg(m, y)
print(result$cor)
print(result$p.value)

# Using percentage bend correlation
taureg(m, y, corfun=pbcor)

}
\references{
Kendall, M.G. (1938). A new measure of rank correlation. \emph{Biometrika},
30, 81-93.

Wilcox, R.R. (2022). \emph{Introduction to Robust Estimation and Hypothesis Testing}
(5th ed.). Academic Press.
}
\seealso{
\code{\link{tau}} for Kendall's tau,
\code{\link{pbcor}} for percentage bend correlation,
\code{\link{correg}} for correlation-based regression fitting
}
