% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{comvar2.astig}
\alias{comvar2.astig}
\title{Compare Variances for Astigmatism Data}
\usage{
comvar2.astig(m1, m2, method = "holm")
}
\arguments{
\item{m1}{Matrix or data frame with two columns (two variables for group 1).}

\item{m2}{Matrix or data frame with two columns (two variables for group 2).}

\item{method}{Method for p-value adjustment (default: "holm").
See \code{\link[stats]{p.adjust}} for options.}
}
\value{
A matrix with two rows (one per variable comparison) and columns:
\itemize{
\item \verb{VAR 1}: Variance estimate for group 1
\item \verb{VAR 2}: Variance estimate for group 2
\item \code{Ratio}: Ratio of variances (VAR 1 / VAR 2)
\item \code{p.value}: Unadjusted p-value
\item \code{p.adjusted}: Adjusted p-value
}
}
\description{
Compares variances between two groups for astigmatism data (or any paired
bivariate data). Specifically designed to compare variances of the first
variable between groups and the second variable between groups.
}
\details{
This function is specifically designed for astigmatism data where each
subject/group has two measurements. It compares:
\itemize{
\item Row 1: Variances of first variable (m1\link{,1} vs m2\link{,1})
\item Row 2: Variances of second variable (m1\link{,2} vs m2\link{,2})
}

The Morgan-Pitman test (\code{varcom.IND.MP}) is used for each comparison,
and p-values are adjusted for multiple testing.
}
\examples{
\dontrun{
# Astigmatism data: two measurements per eye
set.seed(123)
m1 <- matrix(rnorm(60, sd = 1), ncol = 2)   # Group 1
m2 <- matrix(rnorm(60, sd = 1.5), ncol = 2) # Group 2

# Compare variances for both measurements
result <- comvar2.astig(m1, m2)
print(result)
}
}
\seealso{
\code{\link{varcom.IND.MP}}, \code{\link{comvar.mcp}}
}
