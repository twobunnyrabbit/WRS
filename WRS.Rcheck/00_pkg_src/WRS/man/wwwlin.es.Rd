% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/effect-size.R
\name{wwwlin.es}
\alias{wwwlin.es}
\title{Within-Within-Within Design: Linear Contrast Effect Sizes}
\usage{
wwwlin.es(J, K, L, x, tr = 0.2, REL.MAG = NULL, SEED = TRUE, nboot = 1000)
}
\arguments{
\item{J}{Number of levels for Factor A (first within-subjects factor).}

\item{K}{Number of levels for Factor B (second within-subjects factor).}

\item{L}{Number of levels for Factor C (third within-subjects factor).}

\item{x}{Data in matrix or list mode. Expected to contain J\emph{K}L repeated measures.}

\item{tr}{Proportion of trimming for AKP and QStr (default: 0.2).}

\item{REL.MAG}{Optional vector of length 3 for custom small/medium/large effect size
calibration (default: \code{NULL}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducibility (default: \code{TRUE}).}

\item{nboot}{Number of bootstrap samples (default: 50).}
}
\value{
A list with components:
\item{Factor.A, Factor.B, Factor.C}{Main effect sizes for each factor}
\item{Inter.AB, Inte.AC, Inter.BC}{Two-way interaction effect sizes}
\item{Inter.ABC}{Three-way interaction effect sizes}
}
\description{
For a within-within-within (fully repeated measures) three-way factorial design,
computes effect sizes for linear contrasts with bootstrap confidence intervals for
all main effects, two-way, and three-way interactions.
}
\details{
This function handles three-way fully repeated measures designs by computing effect
sizes for:
\itemize{
\item Three main effects (A, B, C)
\item Three two-way interactions (A×B, A×C, B×C)
\item One three-way interaction (A×B×C)
}

Uses \code{\link{con3way}} to generate appropriate contrast matrices, then applies
\code{\link{deplin.ES.summary.CI}} for each set of contrasts.
}
\examples{
# 2x2x2 within design
set.seed(42)
n <- 12
baseline <- rnorm(n)
x <- lapply(1:8, function(i) baseline + rnorm(n, mean = i/4, sd = 0.5))
result <- wwwlin.es(J = 2, K = 2, L = 2, x, nboot = 500)
# Access main effects
result$Factor.A
# Access interactions
result$Inter.AB
}
\references{
Wilcox, R.R. (2022). Introduction to Robust Estimation and Hypothesis Testing (5th ed.).
}
\seealso{
\code{\link{wwlin.es}}, \code{\link{deplin.ES.summary.CI}}, \code{\link{con3way}}
}
