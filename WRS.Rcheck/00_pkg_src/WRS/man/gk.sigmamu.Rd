% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{gk.sigmamu}
\alias{gk.sigmamu}
\title{Robust Scale and Location Estimator for Gnanadesikan-Kettenring Method}
\usage{
gk.sigmamu(x, c1 = 4.5, c2 = 3, mu.too = FALSE, ...)
}
\arguments{
\item{x}{Numeric vector.}

\item{c1}{Numeric. Tuning constant for location estimation (default: 4.5).
Controls the downweighting threshold for outliers in computing the mean.}

\item{c2}{Numeric. Tuning constant for scale estimation (default: 3.0).
Controls the maximum squared deviation used in variance computation.}

\item{mu.too}{Logical. If \code{TRUE}, returns both location and scale estimates.
If \code{FALSE} (default), returns only the scale estimate.}

\item{...}{Additional arguments (currently unused).}
}
\value{
If \code{mu.too = FALSE}: Numeric value, the robust scale estimate \eqn{\sigma}.

If \code{mu.too = TRUE}: Numeric vector of length 2 containing \code{c(mu, sigma)}.
}
\description{
Computes robust scale (and optionally location) estimates using a weighted
median-based approach. This is an internal function used by the
Gnanadesikan-Kettenring covariance estimator.
}
\details{
The estimator proceeds in two stages:

\strong{Stage 1 - Location:}
\enumerate{
\item Compute initial location \eqn{\tilde{\mu}} = median(\code{x})
\item Compute initial scale \eqn{\tilde{\sigma}} = median(|\code{x} - \eqn{\tilde{\mu}}|)
\item Compute weights: \eqn{w_i = (1 - (|x_i - \tilde{\mu}|/c_1 \tilde{\sigma})^2)^2}
for \eqn{|x_i - \tilde{\mu}|/\tilde{\sigma} \leq c_1}, else 0
\item Refined location: \eqn{\mu = \sum w_i x_i / \sum w_i}
}

\strong{Stage 2 - Scale:}
\enumerate{
\item Standardize: \eqn{z_i = (x_i - \mu) / \tilde{\sigma}}
\item Truncate: \eqn{\rho_i = \min(z_i^2, c_2^2)}
\item Scale estimate: \eqn{\sigma^2 = \tilde{\sigma}^2 / n \sum \rho_i}
}
}
\references{
Maronna, R. A., & Zamar, R. H. (2002). Robust estimates of location and dispersion
for high-dimensional datasets. Technometrics, 44(4), 307-317.
}
\seealso{
\code{\link{gk}}, \code{\link{gkcor}}
}
\keyword{internal}
\keyword{robust}
