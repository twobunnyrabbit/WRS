% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classification.R
\name{ridge.est.k}
\alias{ridge.est.k}
\title{Estimate Ridge Parameter Using Kibria's Method}
\usage{
ridge.est.k(
  x,
  y,
  STAND = TRUE,
  locfun = median,
  scat = madsq,
  regfun = ols,
  ...
)
}
\arguments{
\item{x}{Predictor matrix (n x p). Must have at least 2 predictors.}

\item{y}{Response vector of length n.}

\item{STAND}{Logical. If TRUE (default), standardize predictors and center response
using robust location and scale estimators.}

\item{locfun}{Location estimator for standardization (default: median).}

\item{scat}{Scale estimator for standardization (default: madsq - median absolute
deviation squared).}

\item{regfun}{Regression function to use for initial fit (default: ols). Can be
any robust regression function.}

\item{...}{Additional arguments passed to the regression function.}
}
\value{
A numeric value: the estimated ridge parameter k.
}
\description{
Estimates the ridge regression bias parameter k using the method recommended
by Kibria (2003).
}
\details{
The ridge parameter k controls the amount of shrinkage in ridge regression. Larger
k values lead to more shrinkage (more bias, less variance). This function implements
Kibria's recommended estimator:

k = sigma^2 / (product of alpha^2)^(1/p)

where alpha are coefficients in the transformed space and sigma^2 is the residual
variance estimate.

Missing values are automatically removed.
}
\note{
Requires at least 2 predictors (p ≥ 2).
}
\examples{
\dontrun{
set.seed(123)
x <- matrix(rnorm(100 * 3), ncol = 3)
y <- x[,1] + 2*x[,2] + rnorm(100)

# Estimate ridge parameter
k <- ridge.est.k(x, y)
k
}

}
\references{
Kibria, B. M. G. (2003). Performance of some new ridge regression estimators.
Communications in Statistics-Simulation and Computation, 32(2), 419-435.
}
\seealso{
\code{\link{ridge.test}} for ridge regression tests,
\code{\link{ridge.Gtest}} for global ridge tests
}
\keyword{internal}
