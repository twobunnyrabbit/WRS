% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{PMD.PB.PCD}
\alias{PMD.PB.PCD}
\title{Probability of Making a Decision and Correct Decision (Bootstrap Version)}
\usage{
PMD.PB.PCD(
  n = NULL,
  delta = 0.5,
  x = NULL,
  est = tmean,
  SIG = NULL,
  alpha = 0.05,
  iter = 1000,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{n}{Numeric vector of sample sizes (one per group), or \code{NULL} if \code{x} is provided.}

\item{delta}{Numeric. Indifference zone parameter - shift in location for simulation (default: 0.5).}

\item{x}{Data in matrix, data frame, or list format, or \code{NULL} if \code{n} is provided.}

\item{est}{Function. Measure of location to estimate (default: \code{\link{tmean}}).}

\item{SIG}{Numeric vector of population standard deviations, or \code{NULL} to estimate from data.}

\item{alpha}{Numeric. Significance level for decision rule (default: 0.05).}

\item{iter}{Numeric. Number of simulation iterations (default: 1000).}

\item{SEED}{Logical. If \code{TRUE}, set random seed for reproducibility (default: \code{TRUE}).}

\item{...}{Additional arguments passed to \code{est} and \code{\link{anc.best.PB}}.}
}
\value{
List with components:
\itemize{
\item \code{PMD}: Probability of making a decision
\item \code{PMD.ci}: Confidence interval for PMD
\item \code{PCD}: Probability of correct decision (given a decision was made)
\item \code{PCD.ci}: Confidence interval for PCD
}
}
\description{
Determines which group has the largest measure of location using an indifference zone
approach with bootstrap testing. Computes probabilities of making a decision (PMD) and
making a correct decision given that a decision was made (PCD).
}
\details{
Simulates data from J groups, with group 1 shifted by \code{delta} standard deviations.
Uses \code{\link{anc.best.PB}} with bootstrap to test which group has the largest location.
PMD is the proportion of simulations where a decision is made (p-value ≤ alpha).
PCD is the proportion of correct decisions among those simulations where a decision was made.
}
\examples{
\dontrun{
# Simulate with specific sample sizes
result <- PMD.PB.PCD(n = c(20, 20, 20), delta = 0.5, est = tmean, iter = 500)

# Use real data
result2 <- PMD.PB.PCD(x = your_data, delta = 0.5, iter = 500)
}

}
\seealso{
\code{\link{PMD.PCD}}, \code{\link{anc.best.PB}}, \code{\link{RM.PMD.PCD}}
}
\keyword{htest}
