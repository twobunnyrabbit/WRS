% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outliers.R
\name{outmgvf}
\alias{outmgvf}
\title{Fast MGV Outlier Detection (Inward Method)}
\usage{
outmgvf(
  x,
  y = NA,
  plotit = TRUE,
  outfun = outbox,
  se = TRUE,
  ndir = 1000,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{se}{Logical. If TRUE, standardizes variables before analysis.}

\item{ndir}{Number of random projections for depth computation in plots
(default: 1000).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{...}{Additional arguments passed to \code{outfun}.}
}
\value{
A list with components:
\item{n}{Sample size.}
\item{out.id}{Indices of outliers.}
\item{keep}{Indices of non-outliers.}
\item{out.val}{Values of outliers.}
\item{depth.values}{Deletion diagnostic values for all points.}
}
\description{
Faster version of \code{outmgv()} using the inward MGV method. Computes generalized
variance after removing each point, then applies outlier detection to these
deletion diagnostics.
}
\details{
Computes generalized variance with each point deleted (via \code{gvar()}), then
identifies outliers as points whose removal substantially changes the
generalized variance. Faster than \code{outmgv()} but based on different diagnostic.
}
\seealso{
\code{\link{outmgv}}, \code{\link{gvar}}
}
