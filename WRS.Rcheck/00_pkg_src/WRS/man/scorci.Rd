% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{scorci}
\alias{scorci}
\title{Skipped Correlation with Bootstrap Confidence Interval}
\usage{
scorci(
  x,
  y,
  nboot = 1000,
  alpha = 0.05,
  V2 = TRUE,
  SEED = TRUE,
  plotit = TRUE,
  STAND = TRUE,
  corfun = pcor,
  pr = TRUE,
  cop = 3,
  RAN = FALSE,
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{V2}{Logical. If \code{TRUE} (default), uses improved version for n < 120
(recommended as of Sept 2019). Set to \code{FALSE} to use original version.}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{STAND}{Logical. If \code{TRUE}, standardizes data before analysis
(default: \code{TRUE}).}

\item{corfun}{Correlation function to use. Common options: \code{pbcor} (percentage
bend), \code{pcor} (Pearson), \code{wincor} (winsorized), \code{spear} (Spearman).}

\item{pr}{Logical. If \code{TRUE}, prints progress messages (default: \code{TRUE}).}

\item{cop}{Copula type for skipped correlation (default: 3). Options: 1-5.}

\item{RAN}{Logical. If \code{TRUE}, uses random projections for faster outlier
detection (default: \code{FALSE}).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
List with components:
\item{cor.ci}{Bootstrap confidence interval for skipped correlation.}
\item{p.value}{Two-sided p-value for H0: rho = 0.}
\item{cor.est}{Point estimate of skipped correlation.}
}
\description{
Computes a bootstrap confidence interval for the skipped correlation
(correlation after removing bivariate outliers).
}
\details{
This function computes a skipped correlation (outliers removed via \code{scor()})
with bootstrap confidence interval. By default, Pearson's correlation is
computed after outliers are removed, but this can be changed via \code{corfun}
(e.g., \code{corfun = spear} for Spearman).

The V2 algorithm (default) uses an improved method for n < 120 that provides
better coverage. For larger samples, both versions perform similarly.

Outliers are detected using projection-based methods (see \code{scor()} and
\code{outpro()}). The \code{cop} parameter controls the center estimation method.
}
\examples{
# Skipped correlation with bootstrap CI
set.seed(123)
x <- c(rnorm(45), 5, -5, 6)  # Some outliers
y <- c(0.7 * rnorm(45), -5, 5, -6)
result <- scorci(x, y, nboot = 500)
result$cor.est  # Robust to outliers
result$cor.ci   # Bootstrap CI
result$p.value

# Using Spearman after outlier removal
scorci(x, y, corfun = spear, nboot = 500)

# Original version (pre-2019)
scorci(x, y, V2 = FALSE, nboot = 500)
}
\seealso{
\code{\link{scor}}, \code{\link{scorciMC}}, \code{\link{mscor}},
\code{\link{pbcor}}
}
