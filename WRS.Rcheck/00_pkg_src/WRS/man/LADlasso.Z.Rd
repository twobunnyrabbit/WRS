% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{LADlasso.Z}
\alias{LADlasso.Z}
\alias{RA.lasso}
\title{Robust Adaptive Lasso for Variable Selection (Zheng Method)}
\usage{
LADlasso.Z(
  x,
  y,
  STAND = TRUE,
  grid = seq(log(0.01), log(1400), length.out = 100),
  xout = FALSE,
  outfun = outpro,
  ...
)

RA.lasso(
  x,
  y,
  STAND = TRUE,
  grid = seq(log(0.01), log(1400), length.out = 100),
  xout = FALSE,
  outfun = outpro,
  ...
)
}
\arguments{
\item{x}{Matrix, list, or data frame of predictor values.}

\item{y}{Vector or single-dimension matrix/list/data frame of outcome values.}

\item{STAND}{Logical. If \code{TRUE}, standardizes predictors before analysis.
Default is \code{TRUE}.}

\item{grid}{Grid of lambda (tuning parameter) values to check. Default is
\code{seq(log(0.01), log(1400), length.out=100)}.}

\item{xout}{Logical. If \code{TRUE}, removes outliers from predictors using
\code{outfun}. Default is \code{FALSE}.}

\item{outfun}{Function for outlier detection (default: \code{outpro}).}

\item{...}{Additional arguments passed to outlier detection function.}
}
\value{
A list with components:
\item{coef}{Estimated coefficients including intercept (first element) and slopes}
\item{residuals}{Regression residuals}
}
\description{
Performs robust adaptive lasso for variable selection using the method of
Zheng, Gallagher, and Kulasekera (2016). Uses BIC for tuning parameter selection.
}
\details{
This function implements the robust adaptive lasso using LAD regression with
adaptive weights. The optimal tuning parameter is selected via BIC. Small
coefficients (< 1e-8) are set to exactly zero. Requires \code{lars} and
\code{quantreg} packages.
}
\note{
Code is a modification of code supplied by Qi Zheng. \code{RA.lasso} is an
alias for this function.
}
\references{
Zheng, Q., Gallagher, C., and Kulasekera, K. (2016). Robust Adaptive Lasso
for Variable Selection. Communications in Statistics - Theory and Methods,
46(9):4642-4659.
}
\seealso{
\code{\link{LAD.lasso}}, \code{\link{RA.lasso}}
}
\keyword{regression}
\keyword{robust}
