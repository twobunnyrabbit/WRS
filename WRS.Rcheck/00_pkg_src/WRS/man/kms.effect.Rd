% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{kms.effect}
\alias{kms.effect}
\title{KMS Robust Effect Size}
\usage{
kms.effect(x, y, tr = 0.2, DIF = NULL)
}
\arguments{
\item{x}{Numeric vector. First group data.}

\item{y}{Numeric vector. Second group data.}

\item{tr}{Numeric. Trimming proportion (default: 0.2).}

\item{DIF}{Numeric or NULL. If specified, uses this value as the mean difference
instead of the observed difference (useful for power analysis; default: NULL).}
}
\value{
List with components:
\describe{
\item{effect.size}{KMS robust effect size δ}
\item{Cohen.d.equiv}{Approximate Cohen's d equivalent (= 2×δ)}
}
}
\description{
Computes robust effect size using the KMS (Kulinskaya-Morgenthaler-Staudte)
method based on trimmed means and winsorized variances.
}
\details{
The KMS effect size is a robust standardized mean difference that:
\enumerate{
\item Uses trimmed means for location
\item Uses winsorized variances for scale
\item Pools variances accounting for group sizes
\item Adjusts for trimming to provide normal-equivalent effect size
}

Correspondence with Cohen's d (KMS p. 180):
\itemize{
\item KMS δ = 0.1 ≈ Cohen's d = 0.2 (small)
\item KMS δ = 0.25 ≈ Cohen's d = 0.5 (medium)
\item KMS δ = 0.4 ≈ Cohen's d = 0.8 (large)
}

The \code{DIF} parameter allows specifying a hypothetical difference for
power analysis with \code{\link{Stein2g}}.

Reference: Kulinskaya, E., Morgenthaler, S. & Staudte, R. (2008).
Meta Analysis: A guide to calibrating and combining statistical evidence, p. 177.
}
\examples{
set.seed(123)
x <- rnorm(30)
y <- rnorm(30, mean = 0.5)
result <- kms.effect(x, y, tr = 0.2)
result$effect.size
result$Cohen.d.equiv
}
\seealso{
\code{\link{KMS.ci}} for confidence intervals,
\code{\link{Stein2g}} for power analysis,
\code{\link{akp.effect}} for alternative effect sizes
}
