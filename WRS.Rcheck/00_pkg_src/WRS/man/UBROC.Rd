% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{UBROC}
\alias{UBROC}
\title{ROC Curve for Unbiased Classification Methods}
\usage{
UBROC(
  train = NULL,
  g = NULL,
  x1 = NULL,
  x2 = NULL,
  method = "KNN",
  reps = 10,
  pro.p = 0.8,
  SEED = TRUE,
  POS = TRUE,
  EN = TRUE,
  ...
)
}
\arguments{
\item{train}{Training data matrix, or specify groups separately via \code{x1}/\code{x2}.}

\item{g}{Group membership vector for training data.}

\item{x1}{Training data for group 1 (alternative to \code{train}/\code{g}).}

\item{x2}{Training data for group 2 (alternative to \code{train}/\code{g}).}

\item{method}{Classification method to use. Options: \code{'KNN'} (nearest neighbor
using robust depths), \code{'DIS'}, \code{'DEP'}, \code{'SVM'}, \code{'RF'},
\code{'NN'}, \code{'ADA'}, \code{'PRO'} (default: \code{'KNN'}).}

\item{reps}{Number of resamples for ROC estimation (default: 10).}

\item{pro.p}{Proportion used for training; remainder used for testing (default: 0.8).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducibility.}

\item{POS}{Logical. If \code{TRUE}, plots TPR vs FPR; if \code{FALSE}, plots TNR vs FNR.}

\item{EN}{Logical. If \code{TRUE}, uses equal sample sizes for unbiased estimates;
if \code{FALSE}, allows potential bias from unequal sizes.}

\item{...}{Additional arguments passed to classification function.}
}
\value{
Plots ROC curve. No value returned.
}
\description{
Computes and plots ROC (Receiver Operating Characteristic) curve based on
various classification methods. Uses resampling to create unbiased estimates
when \code{EN=TRUE}.
}
\details{
Computes ROC curve using resampling approach:
\enumerate{
\item Split data into training/test sets
\item Fit classifier on training data
\item Predict on test data
\item Aggregate predictions across resamples
\item Plot ROC curve using ROCR package
}

When \code{EN=TRUE}, ensures equal training sample sizes from each group to
avoid bias in error rate estimation.

Requires \code{ROCR} package for plotting.
}
\seealso{
\code{\link{UB.class}}, \code{\link{UB.class.error}}
}
\keyword{classification}
