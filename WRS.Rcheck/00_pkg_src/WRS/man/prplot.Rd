% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{prplot}
\alias{prplot}
\title{Partial Residual Plot for Checking Curvature}
\usage{
prplot(
  x,
  y,
  pval = ncol(x),
  regfun = tsreg,
  fr = 0.8,
  est = tmean,
  op = 1,
  xlab = "X",
  ylab = "Residuals",
  xout = FALSE,
  outfun = out,
  ...
)
}
\arguments{
\item{x}{Matrix of predictor variables (must have 2+ columns).}

\item{y}{Response variable vector.}

\item{pval}{Column index of the predictor to check for curvature (default: last column).}

\item{regfun}{Regression function to use (default: \code{tsreg} for Theil-Sen regression).}

\item{fr}{Span parameter for the running interval smoother (default: 0.8).}

\item{est}{Location estimator for running smoother (default: \code{tmean}).}

\item{op}{Operation mode: 1 for LOWESS (\code{lplot}), other values for running interval
smoother (\code{rungen}) (default: 1).}

\item{xlab}{Label for X-axis (default: "X").}

\item{ylab}{Label for Y-axis (default: "Residuals").}

\item{xout}{Logical; if TRUE, removes outliers based on predictor variables (default: FALSE).}

\item{outfun}{Outlier detection function (default: \code{out}).}

\item{...}{Additional arguments passed to the outlier detection function.}
}
\value{
Invisibly returns NULL. Creates a plot as a side effect.
}
\description{
Creates a partial residual plot to check for curvature (nonlinearity) associated
with a specific predictor variable in a multiple regression context. Subtracts out
the linear predictions from other predictors and plots a smooth against the focal predictor.
}
\details{
A \strong{partial residual plot} is useful for detecting curvature in the relationship
between a specific predictor and the response after accounting for other predictors.

The function:
\enumerate{
\item Fits a regression using all predictors EXCEPT the one indicated by \code{pval}
\item Computes residuals from this reduced model
\item Plots these residuals against the excluded predictor with a smooth curve
}

If there is curvature (nonlinearity) in the smooth, this suggests that the linear
relationship may not be adequate for that predictor.

The smoother used depends on the \code{op} parameter:
\itemize{
\item \code{op=1}: Uses LOWESS via \code{lplot}
\item Otherwise: Uses a running interval smoother via \code{rungen}
}
}
\examples{
# Check for curvature in second predictor
x1 <- rnorm(100)
x2 <- rnorm(100)
y <- 2*x1 + 0.5*x2^2 + rnorm(100)
X <- cbind(x1, x2)
prplot(X, y, pval = 2)  # Should show curvature for x2
}
\seealso{
\code{\link{lplot}}, \code{\link{rungen}}, \code{\link{tsreg}}
}
