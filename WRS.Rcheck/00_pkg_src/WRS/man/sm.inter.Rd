% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{sm.inter}
\alias{sm.inter}
\title{Interaction Tests with Two-Variable Splitting}
\usage{
sm.inter(
  x,
  y,
  IV = c(1, 2),
  Qsplit1 = 0.5,
  Qsplit2 = 0.5,
  PB = FALSE,
  est = tmean,
  tr = 0.2,
  nboot = 1000,
  pr = TRUE,
  con = NULL,
  xout = FALSE,
  outfun = outpro,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{x}{Matrix or data frame of predictor variables}

\item{y}{Response variable vector}

\item{IV}{Vector of length 2 specifying which columns of x to use for splitting.
Default is \code{c(1,2)} (first two columns).}

\item{Qsplit1}{Quantile(s) for splitting first independent variable. Default is 0.5.
Example: \code{c(.25, .5, .75)} splits at quartiles.}

\item{Qsplit2}{Quantile(s) for splitting second independent variable. Default is 0.5.}

\item{PB}{Logical. If \code{TRUE}, use percentile bootstrap via \code{\link{linconpb}}.
If \code{FALSE} (default), use \code{\link{lincon}} with trimmed means.}

\item{est}{Location estimator function. Default is \code{\link{tmean}}.
Setting \code{est=median} or \code{est=hd} forces \code{PB=TRUE}.}

\item{tr}{Trimming proportion for non-bootstrap method (default: 0.2).
For bootstrap method, specify via ... (e.g., \code{tr=0.25}).}

\item{nboot}{Number of bootstrap samples (if \code{PB=TRUE}). Default is 1000.}

\item{pr}{Logical. If \code{TRUE} (default), print warnings for small sample sizes.}

\item{con}{Contrast matrix for linear combinations. If \code{NULL} (default),
uses \code{\link{con2way}}$conAB for all pairwise interaction contrasts.}

\item{xout}{Logical. If \code{TRUE}, remove outliers from x before analysis.
Default is \code{FALSE}.}

\item{outfun}{Outlier detection function. Default is \code{\link{outpro}}.}

\item{SEED}{Logical or numeric. Controls random seed. Default is \code{TRUE}.}

\item{...}{Additional arguments passed to estimator function}
}
\value{
A list with components:
\item{Group.summary}{List of summary statistics for each cell (cells with n > 6)}
\item{loc.est}{Matrix of location estimates (rows=IV1 groups, cols=IV2 groups)}
\item{test}{Test statistics (from \code{\link{lincon}} when \code{PB=FALSE})}
\item{psihat}{Matrix of contrast estimates, standard errors, CIs, and p-values}
\item{con}{Contrast matrix used}
\item{Effect.Sizes}{Vector of effect sizes for all pairwise comparisons}
}
\description{
Splits data based on quantiles of two independent variables and performs
pairwise comparisons or linear contrasts to test for interaction effects.
}
\details{
The function:
\enumerate{
\item Splits data into J × K groups based on quantiles of two variables
\item Computes location estimates for each cell
\item Performs linear contrasts (default: all pairwise interaction comparisons)
\item Computes effect sizes for all pairwise comparisons
}

Groups with sample size ≤ 5 trigger warnings. For trimmed means, use \code{PB=FALSE}
(calls \code{\link{lincon}}). For other estimators like median or Harrell-Davis,
\code{PB} is automatically set to \code{TRUE} (calls \code{\link{linconpb}}).
}
\seealso{
\code{\link{smgrid}}, \code{\link{lincon}}, \code{\link{linconpb}},
\code{\link{con2way}}, \code{\link{IND.PAIR.ES}}
}
\keyword{htest}
\keyword{nonparametric}
