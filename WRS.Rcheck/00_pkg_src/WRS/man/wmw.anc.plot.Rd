% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{wmw.anc.plot}
\alias{wmw.anc.plot}
\title{Plot WMW ANCOVA: P(Y1 < Y2 | X) vs X}
\usage{
wmw.anc.plot(
  x1,
  y1,
  x2,
  y2,
  q1 = c(0.1, 0.9),
  q2 = c(0.1, 0.9),
  npts = 20,
  pts = NULL,
  xout = FALSE,
  outfun = outpro,
  xlab = "X",
  ylab = "P(Y1<Y2)",
  ...
)
}
\arguments{
\item{x1}{Covariate for group 1.}

\item{y1}{Response for group 1.}

\item{x2}{Covariate for group 2.}

\item{y2}{Response for group 2.}

\item{q1}{Quantile range for group 1 covariate (default: c(0.1, 0.9)).}

\item{q2}{Quantile range for group 2 covariate (default: c(0.1, 0.9)).}

\item{npts}{Number of design points (default: 20).}

\item{pts}{Optional vector of design points (default: NULL for automatic).}

\item{xout}{Logical. If \code{TRUE}, removes outliers (default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro}).}

\item{xlab}{X-axis label (default: 'X').}

\item{ylab}{Y-axis label (default: 'P(Y1<Y2)').}

\item{...}{Additional arguments passed to outlier function.}
}
\value{
A list with the covariate range used.
}
\description{
Plots WMW-based conditional probability estimates P(Y1 < Y2 | X=x)
as a function of the covariate X.
}
\details{
Single covariate only. Estimates the conditional WMW effect size at
multiple covariate values and creates a plot. Design points are chosen
within the overlapping quantile ranges of both groups.
}
\examples{
\dontrun{
# Plot conditional WMW effect
set.seed(123)
x1 <- rnorm(50); y1 <- x1 + rnorm(50)
x2 <- rnorm(50); y2 <- x2 + 0.5 + rnorm(50)
wmw.anc.plot(x1, y1, x2, y2, npts = 15)
}
}
\seealso{
\code{\link{wmw.anc}}, \code{\link{wmw.ancp2}}
}
