% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{mat2list}
\alias{mat2list}
\alias{MAT2list}
\title{Convert Matrix to Grouped List (Exclude Grouping Column)}
\usage{
mat2list(m, grp.dat)

MAT2list(x, J = NULL, p = NULL)
}
\arguments{
\item{m}{Data matrix or data frame containing variables to be grouped.}

\item{grp.dat}{Vector containing group identifiers (same length as \code{nrow(m)}).}

\item{x}{Data matrix or data frame with \code{ncol(x) = J*p}.}

\item{J}{Number of groups. Either \code{J} or \code{p} must be specified. Default is \code{NULL}.}

\item{p}{Number of variables per group. Either \code{J} or \code{p} must be specified. Default is \code{NULL}.}
}
\value{
List where each element contains a matrix of data from \code{m} for one
group level (grouping column excluded).

List of length J, where each element is an n by p matrix containing
data for one group.
}
\description{
Split matrix data into groups based on a grouping variable, excluding the
grouping column from the output. Similar to \code{\link{fac2list}} but for matrices.

Convert a matrix or data frame in wide format (where columns represent multiple
groups' measurements on the same variables) to list mode. Columns are divided
into J groups of p variables each.
}
\details{
This function divides rows of \code{m} into groups based on values in \code{grp.dat}.
Unlike \code{\link{mat2grp}}, the grouping variable is not included in the output
list elements.

Rows where \code{grp.dat} is \code{NA} are automatically removed.

The function assumes \code{ncol(x) = J * p} and reshapes the data:
\itemize{
\item Columns 1 to p are stored in \code{y[[1]]}
\item Columns p+1 to 2p are stored in \code{y[[2]]}
\item And so on for J groups
}

This is useful for multivariate repeated measures or matched groups data where
each subject has measurements on p variables for each of J conditions, stored
in wide format.
}
\examples{
\dontrun{
# Divide columns 2-5 into groups based on group IDs in column 9
z <- mat2list(m[,2:5], m[,9])
# z[[1]] contains data from first group
# z[[2]] contains data from second group, etc.
}

\dontrun{
# Data with 4 groups, 3 variables each (12 columns total)
y <- MAT2list(x, J=4, p=3)
# Or equivalently:
y <- MAT2list(x, p=3)  # J computed automatically
}

}
\seealso{
\code{\link{mat2grp}}, \code{\link{fac2list}}, \code{\link{MAT2list}}

\code{\link{mat2list}}, \code{\link{mat2grp}}, \code{\link{linconMpb}}
}
\keyword{manip}
