% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{lplotN}
\alias{lplotN}
\title{LOESS Smoother Plot for Large Datasets}
\usage{
lplotN(
  x,
  y,
  nsub = 5000,
  est = tmean,
  fr = 1,
  xout = FALSE,
  xlab = "X",
  ylab = "Y",
  zlab = "",
  ticktype = "simple",
  theta = 50,
  phi = 25,
  scale = TRUE,
  pc = " ",
  expand = 0.5,
  SEED = TRUE,
  frame = TRUE
)
}
\arguments{
\item{x}{Predictor variable(s). Can be a vector (univariate) or matrix (multivariate).}

\item{y}{Response variable (vector).}

\item{nsub}{Size of random subsample used for plotting (default: 5000). The function
uses all data for prediction but plots only a subsample.}

\item{est}{Measure of location for running interval smoother (default: tmean, 20\% trimmed mean).}

\item{fr}{Span for LOESS smoother (default: 1).}

\item{xout}{Logical. Remove outliers in predictors using projection method if TRUE (default: FALSE).}

\item{xlab}{Label for X-axis (default: "X").}

\item{ylab}{Label for Y-axis (default: "Y").}

\item{zlab}{Label for Z-axis (default: "").}

\item{ticktype}{Type of tick marks (default: "simple").}

\item{theta}{Azimuthal angle for 3D plot viewing (default: 50).}

\item{phi}{Colatitude angle for 3D plot viewing (default: 25).}

\item{scale}{Logical. Scale axes to same range if TRUE (default: TRUE).}

\item{pc}{Point character for plotting (default: " ").}

\item{expand}{Expansion factor for bounding box (default: 0.5).}

\item{SEED}{Logical. Set random seed for reproducibility if TRUE (default: TRUE).}

\item{frame}{Logical. Draw frame around plot if TRUE (default: TRUE).}
}
\value{
None. Creates a plot.
}
\description{
Creates a 3D plot of a LOESS regression surface for large datasets by subsampling
points for visualization. Good for large sample sizes or plotting regression
surfaces without scatterplots.
}
\details{
This function is designed for large datasets where plotting all points would be
computationally expensive or visually cluttered. It works by:
\enumerate{
\item Randomly subsampling nsub points from the data for visualization
\item Computing LOESS predictions using all data
\item Plotting the subsampled points with predicted values
}

The function uses \code{lplot.pred} to compute predictions and \code{lplot} for
3D visualization. Missing values are automatically removed.

\strong{Outlier removal} (xout = TRUE): Uses \code{outpro.depth} projection method
to remove outliers before plotting.
}
\examples{
# Large dataset example
set.seed(123)
n <- 10000
x <- rnorm(n)
y <- 2 * x + rnorm(n)
lplotN(x, y, nsub = 1000)
}
\seealso{
\code{\link{lplot}}, \code{\link{lplot.pred}}, \code{\link{rplotN}}
}
