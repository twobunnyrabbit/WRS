% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{ancovampG}
\alias{ancovampG}
\title{Generalized ANCOVA for Multiple Covariates with Custom Tests}
\usage{
ancovampG(
  x1,
  y1,
  x2,
  y2,
  fr1 = 1,
  fr2 = 1,
  tr = 0.2,
  alpha = 0.05,
  pts = NULL,
  SEED = TRUE,
  test = yuen,
  DH = FALSE,
  FRAC = 0.5,
  cov.fun = skip.cov,
  ZLIM = TRUE,
  pr = FALSE,
  q = 0.5,
  plotit = FALSE,
  LP = FALSE,
  theta = 50,
  xlab = " X1",
  ylab = "X2 ",
  SCAT = FALSE,
  zlab = "p.value ",
  ticktype = "detail",
  ...
)
}
\arguments{
\item{x1}{Matrix of covariate values for group 1 (can have multiple columns)}

\item{y1}{Response values for group 1}

\item{x2}{Matrix of covariate values for group 2}

\item{y2}{Response values for group 2}

\item{fr1}{Span parameter for group 1 smoother (default: 1)}

\item{fr2}{Span parameter for group 2 smoother (default: 1)}

\item{tr}{Trimming proportion (default: 0.2). Proportion of observations to
trim from each tail when computing winsorized correlation.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{pts}{Optional matrix of design points. If NULL, uses points with deepest
depth in x1 (controlled by DH and FRAC)}

\item{SEED}{Logical; if TRUE, set seed for reproducibility with bootstrap/MVE (default: TRUE)}

\item{test}{Hypothesis testing function to use: yuen, qcomhd, qcomhdMC, or
binom2g (default: yuen)}

\item{DH}{Logical; if TRUE, use only deepest (1-FRAC) design points (default: FALSE)}

\item{FRAC}{Fraction of deepest points to use when DH=TRUE (default: .5)}

\item{cov.fun}{Covariance function for depth computation (default: skip.cov)}

\item{ZLIM}{Logical; if TRUE, set z-axis limits in plot (default: TRUE)}

\item{pr}{Logical; if TRUE, print progress (default: FALSE)}

\item{q}{Quantile for qcomhd tests (default: .5 for median)}

\item{plotit}{Logical; if TRUE and 2 covariates, create 3D plot of p-values (default: FALSE)}

\item{LP}{Logical; if TRUE, use lplot for 3D visualization; if FALSE, use scatterplot3d (default: FALSE)}

\item{theta}{Rotation angle for 3D plot (default: 50)}

\item{xlab}{Label for first covariate (default: ' X1')}

\item{ylab}{Label for second covariate (default: 'X2 ')}

\item{SCAT}{Logical; not currently used (default: FALSE)}

\item{zlab}{Label for z-axis in 3D plot (default: 'p.value ')}

\item{ticktype}{Type of tick marks for 3D plot (default: 'detail')}

\item{...}{Additional arguments passed to test function}
}
\value{
List with components:
\item{points}{Matrix of design points used}
\item{results}{Matrix with columns: n1, n2, p.value, crit.p.value (Hochberg),
Sig (1 if significant by Hochberg, 0 otherwise), est.dif (estimated difference)}
\item{num.sig}{Number of significant comparisons}
}
\description{
Generalize ancovamp to allow any hypothesis testing method for comparing two
independent groups with multiple covariates. Uses running interval smoothers
without parametric assumptions. Design points chosen based on data depth.
Controls familywise error rate using Hochberg's method.
}
\note{
\itemize{
\item Design points with fewer than 10 observations in either group are excluded
\item P-values adjusted using Hochberg's sequentially rejective method
\item Requires scatterplot3d package if plotit=TRUE and LP=FALSE
}
}
\examples{
\dontrun{
# Generate data with two covariates
set.seed(890)
n1 <- 70; n2 <- 70
x1 <- matrix(rnorm(n1*2), ncol=2)
y1 <- 2 + 0.5*x1[,1] + 0.3*x1[,2] + rnorm(n1)
x2 <- matrix(rnorm(n2*2), ncol=2)
y2 <- 3 + 0.5*x2[,1] + 0.3*x2[,2] + rnorm(n2)

# Compare using trimmed means (default)
result <- ancovampG(x1, y1, x2, y2)
print(result$results)

# Compare medians using quantile comparison
result2 <- ancovampG(x1, y1, x2, y2, test = qcomhd, q = 0.5)
}

}
