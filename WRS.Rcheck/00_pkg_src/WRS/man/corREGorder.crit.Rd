% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{corREGorder.crit}
\alias{corREGorder.crit}
\title{Critical Values for corREGorder}
\usage{
corREGorder.crit(
  p,
  n,
  corfun = wincor,
  iter = 1000,
  nboot = 1000,
  SEED = TRUE,
  MC = FALSE,
  pr = TRUE,
  ...
)
}
\arguments{
\item{p}{Number of predictors.}

\item{n}{Sample size.}

\item{corfun}{Correlation function to use. Common options: \code{pbcor} (percentage
bend), \code{pcor} (Pearson), \code{wincor} (winsorized), \code{spear} (Spearman).}

\item{iter}{Number of iterations for null distribution simulation (default: 1000).}

\item{nboot}{Number of bootstrap samples per iteration (default: 1000).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{MC}{Logical. Use parallel processing (default: \code{FALSE}).}

\item{pr}{Logical. Print progress messages (default: \code{TRUE}).}

\item{...}{Additional arguments passed to \code{corfun}.}
}
\value{
Matrix of null p-value distributions.
}
\description{
Estimates the null distribution of p-values for \code{corREGorder()} via simulation.
}
\details{
Estimates the null distribution when all correlations are zero. Pre-computing
these values can significantly reduce execution time when repeatedly calling
\code{corREGorder()} with the same n and p.
}
\seealso{
\code{\link{corREGorder}}
}
\keyword{internal}
