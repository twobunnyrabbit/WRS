% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{lplotv2}
\alias{lplotv2}
\title{Plot Regression Surface Using LOESS (Version 2)}
\usage{
lplotv2(
  x,
  y,
  span = 0.75,
  pyhat = FALSE,
  eout = FALSE,
  xout = FALSE,
  outfun = out,
  plotit = TRUE,
  expand = 0.5,
  low.span = 2/3,
  varfun = pbvar,
  cor.op = FALSE,
  cor.fun = pbcor,
  ADJ = FALSE,
  nboot = 20,
  scale = TRUE,
  xlab = "X",
  ylab = "Y",
  zlab = "",
  theta = 50,
  phi = 25,
  family = "gaussian",
  duplicate = "error",
  pr = TRUE,
  SEED = TRUE,
  ticktype = "simple"
)
}
\arguments{
\item{x}{Predictor variable(s). Can be a vector (single predictor) or matrix (multiple predictors).
Maximum of 4 predictors allowed.}

\item{y}{Response variable (vector).}

\item{span}{Span parameter for LOESS when using 2+ predictors (default: 0.75).
Controls the degree of smoothing.}

\item{pyhat}{Logical; if TRUE, returns fitted Y values (default: FALSE).}

\item{eout}{Logical; if TRUE, eliminates multivariate outliers before fitting (default: FALSE).}

\item{xout}{Logical; if TRUE, eliminates outliers based on predictor variables only (default: FALSE).}

\item{outfun}{Outlier detection function (default: \code{out}).}

\item{plotit}{Logical; if TRUE, creates the plot (default: TRUE).}

\item{expand}{Expansion factor for 3D plot (default: 0.5).}

\item{low.span}{Span parameter for LOWESS when using 1 predictor (default: 2/3).}

\item{varfun}{Variance function for computing explanatory power (default: \code{pbvar}).}

\item{cor.op}{Logical; if TRUE, uses correlation-based explanatory power (default: FALSE).}

\item{cor.fun}{Correlation function when cor.op=TRUE (default: \code{pbcor}).}

\item{ADJ}{Logical; if TRUE, computes adjusted strength/explanatory measures (default: FALSE).}

\item{nboot}{Number of bootstrap samples for adjusted measures (default: 20).}

\item{scale}{Logical; if TRUE, scales the 3D plot (default: TRUE).}

\item{xlab}{Label for X-axis (default: "X").}

\item{ylab}{Label for Y-axis (default: "Y").}

\item{zlab}{Label for Z-axis in 3D plots (default: "").}

\item{theta}{Rotation angle for 3D plot (default: 50).}

\item{phi}{Colatitude angle for 3D plot (default: 25).}

\item{family}{Family parameter for LOESS: "gaussian" or "symmetric" (default: "gaussian").}

\item{duplicate}{How to handle duplicate x values in 3D plots: "error" or "strip" (default: "error").}

\item{pr}{Logical; if TRUE, prints warnings (default: TRUE).}

\item{SEED}{Logical; if TRUE, sets random seed for reproducibility (default: TRUE).}

\item{ticktype}{Type of tick marks for 3D plot (default: "simple").}
}
\value{
A list with components:
\item{Strength.Assoc}{Strength of association measure (signed square root of explanatory power).}
\item{Explanatory.power}{Proportion of variance explained by the smoother.}
\item{Strength.Adj}{Adjusted strength of association (if ADJ=TRUE).}
\item{Explanatory.Adj}{Adjusted explanatory power (if ADJ=TRUE).}
\item{yhat.values}{Fitted values (if pyhat=TRUE).}
}
\description{
Creates a regression surface plot using LOESS (locally weighted scatterplot smoothing)
with options for outlier removal, strength of association estimation, and 3D visualization
for multiple predictors. This version includes robust strength and explanatory power measures.
}
\details{
This function fits a LOESS regression surface and provides measures of association strength.

\strong{For 1 predictor}: Uses LOWESS with the specified low.span parameter.

\strong{For 2-4 predictors}: Uses LOESS with interaction terms and creates a 3D surface plot
(for 2 predictors only).

The \strong{strength of association} is computed as the signed square root of the explanatory
power, where the sign indicates the direction of association (increasing vs decreasing).

\strong{Explanatory power} is computed as the ratio of variances or as squared correlation,
depending on the cor.op parameter.

When ADJ=TRUE, adjusted measures account for overfitting by comparing to bootstrap
samples where x and y are independent.

\strong{Note on duplicates}: When plotting 3D surfaces with 2 predictors, duplicate x values
may cause errors. Set duplicate="strip" to remove duplicates.
}
\examples{
# Single predictor with LOWESS
x <- rnorm(100)
y <- x + 0.5 * x^2 + rnorm(100, sd = 0.5)
lplotv2(x, y, low.span = 0.5)

# Two predictors with LOESS 3D surface
\dontrun{
x1 <- rnorm(100)
x2 <- rnorm(100)
y <- x1 + x2 + 0.5 * x1 * x2 + rnorm(100)
X <- cbind(x1, x2)
lplotv2(X, y, span = 0.6)
}
}
\seealso{
\code{\link{lplot}}, \code{\link{lplotCI}}, \code{\link{lplotPV}}
}
