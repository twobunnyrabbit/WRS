% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{ancdetwmw}
\alias{ancdetwmw}
\title{ANCOVA Using Wilcoxon-Mann-Whitney Method with Design Points}
\usage{
ancdetwmw(
  x1,
  y1,
  x2,
  y2,
  fr1 = 1,
  fr2 = 1,
  nmin = 8,
  Ycrit = FALSE,
  alpha = 0.05,
  plotit = TRUE,
  pts = NA,
  span = 2/3,
  sm = TRUE,
  BOTH = TRUE,
  pr = TRUE,
  xout = FALSE,
  outfun = out,
  MC = FALSE,
  npts = 25,
  p.crit = NULL,
  EST = FALSE,
  SCAT = TRUE,
  xlab = "X",
  ylab = "P.hat",
  pc = ".",
  ...
)
}
\arguments{
\item{x1}{Vector of covariate values for group 1.}

\item{y1}{Vector of outcome values for group 1.}

\item{x2}{Vector of covariate values for group 2.}

\item{y2}{Vector of outcome values for group 2.}

\item{fr1}{Span parameter for determining neighborhoods in group 1 (default: 1).}

\item{fr2}{Span parameter for determining neighborhoods in group 2 (default: 1).}

\item{nmin}{Minimum number of observations needed in each neighborhood (default: 8).}

\item{Ycrit}{Logical. If \code{TRUE}, uses Y-based critical values (default: \code{FALSE}).}

\item{alpha}{Significance level for simultaneous confidence band (default: 0.05).}

\item{plotit}{Logical. If \code{TRUE}, creates a plot with confidence band (default: \code{TRUE}).}

\item{pts}{Vector of covariate points for evaluation. If \code{NA}, automatically determined
(default: \code{NA}).}

\item{span}{Span parameter for loess smoothing when plotting (default: 2/3).}

\item{sm}{Logical. If \code{TRUE}, smooths the plot using lowess (default: \code{TRUE}).}

\item{BOTH}{Logical. If \code{TRUE}, considers both groups when determining range
(default: \code{TRUE}).}

\item{pr}{Logical. If \code{TRUE}, prints additional information (default: \code{TRUE}).}

\item{xout}{Logical. If \code{TRUE}, removes outliers from covariates (default: \code{FALSE}).}

\item{outfun}{Function for detecting outliers (default: \code{out}).}

\item{MC}{Logical. If \code{TRUE}, uses multicore processing (default: \code{FALSE}).}

\item{npts}{Number of covariate points to evaluate (default: 25).}

\item{p.crit}{Critical p-value for simultaneous testing. If \code{NULL}, determined
automatically (default: \code{NULL}).}

\item{EST}{Logical. If \code{TRUE}, estimates critical values (default: \code{FALSE}).}

\item{SCAT}{Logical. If \code{TRUE}, includes scatter plot (default: \code{TRUE}).}

\item{xlab}{Label for x-axis (default: "X").}

\item{ylab}{Label for y-axis (default: "P.hat").}

\item{pc}{Plotting character (default: ".").}

\item{...}{Additional arguments passed to outlier detection function.}
}
\value{
A list with components:
\itemize{
\item \code{output}: Matrix with columns for covariate values, estimates, and confidence bounds
\item \code{p.crit}: Critical p-value used for simultaneous inference
\item Other components from \code{ancovaWMW}
}
}
\description{
Performs ANCOVA comparing two groups using the Wilcoxon-Mann-Whitney analog.
Estimates P(Y1 < Y2 | X) at multiple covariate values with simultaneous
confidence bands. Uses Method S for selecting design points based on minimum
sample size.
}
\details{
This function extends ANCOVA to use a robust WMW-based approach. At each
covariate value, it estimates P(Y1 < Y2) - the probability that an observation
from group 1 is less than an observation from group 2, given the covariate value.

\strong{Method S} (used by this function) chooses covariate points based on having
at least \code{nmin} observations in the neighborhood. This differs from \strong{Method Q}
(see \code{ancdetwmwQ}) which selects points between quantiles.

The function creates a simultaneous confidence band across all covariate values,
controlling the familywise error rate at level \code{alpha}. Critical p-values are
either specified via \code{p.crit} or determined automatically based on simulation
results for different sample sizes.

When \code{plotit = TRUE}, the function creates a plot showing:
\itemize{
\item Point estimates of P(Y1 < Y2) across covariate values
\item Simultaneous confidence band
\item Optional smoothing via lowess
}
}
\note{
This function only allows one covariate. For multiple covariates, use other
ANCOVA methods. Missing values are automatically removed.
}
\examples{
\dontrun{
set.seed(123)
n <- 50
x1 <- rnorm(n)
y1 <- 2 + 0.5*x1 + rnorm(n)
x2 <- rnorm(n)
y2 <- 3 + 0.5*x2 + rnorm(n)

# ANCOVA with WMW method
result <- ancdetwmw(x1, y1, x2, y2)

# With fewer design points
ancdetwmw(x1, y1, x2, y2, npts = 15)

# Remove outliers in covariates
ancdetwmw(x1, y1, x2, y2, xout = TRUE)
}
}
\seealso{
\code{\link{ancdetwmwQ}} for Method Q (quantile-based points),
\code{\link{ancovaWMW}} for the underlying WMW ANCOVA computation,
\code{\link{ancdet}} for ANCOVA with trimmed means
}
