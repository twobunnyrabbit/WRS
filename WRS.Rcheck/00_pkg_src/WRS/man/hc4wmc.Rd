% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{hc4wmc}
\alias{hc4wmc}
\title{Compare Slopes Across J Independent Groups Using HC4 Wild Bootstrap}
\usage{
hc4wmc(
  x,
  y,
  nboot = 599,
  k = 2,
  grp = NA,
  con = 0,
  SEED = TRUE,
  STOP = TRUE,
  ...
)
}
\arguments{
\item{x}{Predictors. Either a list (x[\link{1}], x[\link{2}], ..., x[\link{J}]) or matrix
with columns corresponding to groups.}

\item{y}{Response variables. Either a list (y[\link{1}], y[\link{2}], ..., y[\link{J}]) or
matrix with columns corresponding to groups.}

\item{nboot}{Number of bootstrap samples (default: 599).}

\item{k}{Index of the coefficient to compare across groups (default: 2,
typically the slope in simple regression).}

\item{grp}{Vector specifying which groups to compare. If NA (default),
compares all groups. Example: \code{grp = c(1,3,5)} compares groups 1, 3, 5.}

\item{con}{Contrast matrix for custom comparisons (default: 0 = all pairwise).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed to 2 for reproducibility.}

\item{STOP}{Logical. If \code{TRUE} (default), stops with error suggesting
\code{ols1way} instead (this function assumes equal n).}

\item{...}{Additional arguments (not currently used).}
}
\value{
A list with component:
\item{p.value}{Bootstrap p-value for testing equal slopes across groups}
}
\description{
Tests the hypothesis that J independent groups have identical regression
slopes using OLS with HC4 heteroscedasticity-consistent standard errors
and wild bootstrap multiple comparisons.
}
\details{
\strong{WARNING}: This function assumes equal sample sizes across groups. For
unequal sample sizes, use \code{\link{ols1way}} instead. By default, the
function stops with an error; set \code{STOP = FALSE} to bypass this check.

\strong{Test Procedure}:
\enumerate{
\item Fit OLS regression for each group
\item Compute HC4 standard errors for coefficient k
\item Form test statistics for contrasts (default: all pairwise)
\item Wild bootstrap: resample residuals with discrete random signs
\item Compute maximum absolute test statistic for each bootstrap sample
\item P-value is proportion of bootstrap max statistics ≥ observed max
}

The wild bootstrap preserves heteroscedasticity structure while testing
under the null hypothesis of equal slopes.
}
\examples{
\dontrun{
# Three groups with equal sample sizes
n <- 30
x <- list(rnorm(n), rnorm(n), rnorm(n))
y <- list(2*x[[1]] + rnorm(n),
          3*x[[2]] + rnorm(n),
          2*x[[3]] + rnorm(n))

# Test if slopes differ (requires STOP = FALSE due to warning)
hc4wmc(x, y, nboot = 1000, STOP = FALSE)
}

}
\seealso{
\code{\link{ols1way}}, \code{\link{hc4test}}, \code{\link{hc4wtest}}
}
\keyword{htest}
\keyword{regression}
