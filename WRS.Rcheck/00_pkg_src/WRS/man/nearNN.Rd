% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{nearNN}
\alias{nearNN}
\title{Find K Nearest Neighbors Using Mahalanobis Distance}
\usage{
nearNN(x, pt = x, K = 10, mcov, ...)
}
\arguments{
\item{x}{Numeric matrix (n×p) or data frame of observations.}

\item{pt}{Numeric vector of length p specifying the target point (default: \code{x}
returns nearest neighbors for all points).}

\item{K}{Integer. Number of nearest neighbors to find (default: 10).}

\item{mcov}{Covariance matrix (p×p) for computing Mahalanobis distance.}

\item{...}{Additional arguments (currently unused).}
}
\value{
Integer vector of length K containing row indices of the K nearest neighbors.
}
\description{
Identifies the K observations in a dataset that are closest to a target point
using Mahalanobis distance. Returns row indices of nearest neighbors.
}
\details{
\strong{Distance metric}: Mahalanobis distance using provided covariance matrix:
\deqn{d = \sqrt{(x - pt)^T \Sigma^{-1} (x - pt)}}

If duplicate distances occur (tied nearest neighbors), adds small jitter to
break ties.

Missing values are removed from \code{x} before computation.
}
\seealso{
\code{\link{near}} for fixed-radius neighborhoods,
\code{\link{nearl}}, \code{\link{nearr}} for directed neighborhoods
}
\keyword{internal}
