% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{two.dep.pb}
\alias{two.dep.pb}
\title{Compare Two Dependent Groups via Percentile Bootstrap}
\usage{
two.dep.pb(
  x,
  y = NULL,
  alpha = 0.05,
  est = tmean,
  plotit = FALSE,
  dif = TRUE,
  nboot = NA,
  xlab = "Group 1",
  ylab = "Group 2",
  pr = TRUE,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{x}{Vector of observations for group 1, or a two-column matrix/data frame.}

\item{y}{Vector of observations for group 2 (optional if \code{x} is a matrix).}

\item{alpha}{Significance level (default: 0.05).}

\item{est}{Estimator function for location (default: \code{tmean}).}

\item{plotit}{Logical. If \code{TRUE}, creates a plot (default: \code{FALSE}).}

\item{dif}{Logical. If \code{TRUE}, analyzes difference scores; if \code{FALSE}, compares
marginal trimmed means (default: \code{TRUE}).}

\item{nboot}{Number of bootstrap samples (default: \code{NA}, which uses 1000).}

\item{xlab}{Label for x-axis (default: "Group 1").}

\item{ylab}{Label for y-axis (default: "Group 2").}

\item{pr}{Logical. If \code{TRUE}, prints whether difference scores were used (default: \code{TRUE}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducibility (default: \code{TRUE}).}

\item{...}{Additional arguments passed to the estimator function.}
}
\value{
A matrix with one row containing:
\itemize{
\item If \code{dif=FALSE}: \code{Est.1}, \code{Est.2}, \code{Est.dif}, \code{p.value}, \code{ci.lower}, \code{ci.upper}
\item If \code{dif=TRUE}: \code{Est.typical.dif}, \code{p.value}, \code{ci.lower}, \code{ci.upper}
}
}
\description{
Compares measures of location for two dependent (paired) groups using a
percentile bootstrap method. By default, uses trimmed means as the measure
of location.
}
\details{
This is a wrapper function that calls \code{rmmcppb} for convenience. It provides
a simpler interface for the common case of comparing two dependent groups.

When \code{dif=TRUE}, the function analyzes the distribution of difference scores
(X - Y). When \code{dif=FALSE}, it compares the marginal distributions of X and Y.
}
\examples{
\dontrun{
# Generate paired data
set.seed(123)
pre <- rnorm(30, mean = 100)
post <- pre + rnorm(30, mean = 5, sd = 10)

# Compare using difference scores
two.dep.pb(pre, post, nboot = 1000)

# Compare marginal means
two.dep.pb(pre, post, dif = FALSE, nboot = 1000)
}
}
\seealso{
\code{\link{rmmcppb}}, \code{\link{yuend}}, \code{\link{tmean}}
}
