% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{TM}
\alias{TM}
\title{TM Test for Comparing One-Step M-Estimators}
\usage{
TM(x, bend = 1.28)
}
\arguments{
\item{x}{List. Each element contains data for one group}

\item{bend}{Numeric. Bending constant for Huber's psi function (default: 1.28)}
}
\value{
List with component:
\item{TM}{Test statistic value}
}
\description{
Computes the TM test statistic for comparing one-step M-estimators across
multiple groups using MAD for scale estimation.
}
\details{
The TM test provides a heteroscedastic test for comparing robust location
estimators across groups. It:
\itemize{
\item Uses one-step M-estimators for location (via \code{onestep})
\item Uses \code{mestse} for standard errors
\item Uses MAD (median absolute deviation) for scale
\item Downweights observations more than \code{bend} MADs from the median
}

The test statistic has an approximate chi-squared distribution under the
null hypothesis of equal locations.
}
\examples{
x <- list(
  rnorm(20),
  rnorm(20, mean = 0.5),
  rnorm(20, mean = 1)
)
result <- TM(x)

}
\seealso{
\code{\link{onestep}}, \code{\link{mestse}}, \code{\link{t1way}}
}
\keyword{htest}
\keyword{robust}
