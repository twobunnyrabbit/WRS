% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{wmw.det}
\alias{wmw.det}
\title{Compare Distribution Tails Based on Differences}
\usage{
wmw.det(
  x,
  y,
  refp = NULL,
  plotit = FALSE,
  xlab = "Difference",
  ylab = "Density",
  plotfun = kerSORT
)
}
\arguments{
\item{x}{Numeric vector for first group.}

\item{y}{Numeric vector for second group.}

\item{refp}{Reference point for tail comparison (required). The absolute value
is used, so sign doesn't matter.}

\item{plotit}{Logical; if \code{TRUE}, plots the distribution of differences
using \code{plotfun} (default: \code{FALSE}).}

\item{xlab}{Label for x-axis when plotting (default: 'Difference').}

\item{ylab}{Label for y-axis when plotting (default: 'Density').}

\item{plotfun}{Function used for plotting (default: \code{kerSORT}). For faster
execution, try \code{plotfun=skerd}.}
}
\value{
A list with components:
\item{L}{Lower tail probability: \code{P(X - Y <= -refp)}}
\item{U}{Upper tail probability: \code{P(X - Y >= refp)}}
\item{dif}{Difference between upper and lower tail: \code{U - L}}
}
\description{
Compares the tail probabilities of the distribution of pairwise differences
\code{X - Y} relative to a specified reference point. Specifically, compares
\code{P(X - Y < -refp)} versus \code{P(X - Y > refp)}.
}
\details{
This function examines whether the distribution of pairwise differences is
symmetric around a reference point. It computes all pairwise differences
between \code{x} and \code{y}, then calculates the proportion in each tail.

When \code{refp=0}, it compares \code{P(X < Y)} versus \code{P(X > Y)},
which is related to the Wilcoxon-Mann-Whitney test.
}
\seealso{
\code{\link{wmw}}, \code{\link{kerSORT}}, \code{\link{skerd}}
}
\keyword{distribution}
\keyword{htest}
