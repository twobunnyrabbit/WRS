% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{mscorpbMC}
\alias{mscorpbMC}
\title{Multiple Skipped Correlation Bootstrap (Parallel)}
\usage{
mscorpbMC(
  x,
  corfun = pcor,
  nboot = 500,
  alpha = 0.05,
  SEED = TRUE,
  WARN = FALSE,
  outfun = outpro,
  pr = TRUE
)
}
\arguments{
\item{x}{Numeric matrix (n-by-p).}

\item{corfun}{Correlation function to use. Common options: \code{pbcor} (percentage
bend), \code{pcor} (Pearson), \code{wincor} (winsorized), \code{spear} (Spearman).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{WARN}{Logical. If \code{FALSE} (default), suppresses warnings.}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{pr}{Logical. If \code{TRUE}, prints progress messages (default: \code{TRUE}).}
}
\value{
List with components (same as \code{mscorpb()}):
\item{n}{Sample size.}
\item{cor}{Correlation matrix.}
\item{test.stats}{Test statistics.}
\item{crit.val}{Critical value.}
\item{num.sig}{Number of significant correlations.}
\item{p.value}{Overall p-value.}
}
\description{
Parallel version of \code{mscorpb()} using \code{mclapply()} for faster computation.
}
\details{
This is the parallel version of \code{mscorpb()}. Uses \code{mclapply()} for faster
bootstrap computation on multi-core systems.

\strong{Note}: Does not work on Windows. Use \code{mscorpb()} on Windows.
}
\examples{
\dontrun{
# Fast bootstrap on multi-core (not Windows)
set.seed(123)
m <- matrix(rnorm(100 * 5), ncol = 5)
result <- mscorpbMC(m, nboot = 2000)
result$num.sig
}
}
\seealso{
\code{\link{mscorpb}}, \code{\link{mscor}}, \code{\link{scorciMC}}
}
