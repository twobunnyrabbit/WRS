% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{mcd.cor}
\alias{mcd.cor}
\title{MCD Robust Correlation}
\usage{
mcd.cor(x, y)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}
}
\value{
List with component:
\item{cor}{MCD correlation coefficient.}
}
\description{
Computes correlation using Minimum Covariance Determinant (MCD) estimator.
}
\details{
Computes robust correlation based on the MCD covariance estimator, which
has high breakdown point (can handle up to ~50\% contamination).
}
\examples{
\dontrun{
set.seed(123)
x <- rnorm(50)
y <- 0.5 * x + rnorm(50)
mcd.cor(x, y)
}
}
\seealso{
\code{\link{ogkcor}}, \code{\link{pbcor}}, \code{\link{wincor}}
}
