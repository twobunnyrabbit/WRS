% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression-advanced.R
\name{regbin.hyp.split}
\alias{regbin.hyp.split}
\title{Binary Response Hypothesis Testing via Hyperplane Split}
\usage{
regbin.hyp.split(
  x,
  y,
  split.reg = Qreg,
  alpha = 0.05,
  nboot = 1000,
  method = "SK",
  xout = FALSE,
  outfun = outpro,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{x}{Matrix of independent variables (must have p ≥ 2 columns).}

\item{y}{Binary response vector (0/1 or logical).}

\item{split.reg}{Regression function used to define splitting hyperplanes
(default: \code{Qreg} for median regression). Must return coefficients in \verb{$coef}.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{nboot}{Number of bootstrap samples if \code{PB=TRUE} (default: 1000).}

\item{method}{Comparison method for binary data: "SK" (Storer-Kim), "BH"
(Benjamini-Hochberg), or "BY" (Benjamini-Yekutieli) (default: "SK").}

\item{xout}{Logical; if TRUE, remove outliers from predictor space before
splitting (default: FALSE).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{...}{Additional arguments passed to \code{split.reg}.}
}
\value{
A list with components:
\itemize{
\item Independent.variables.summary: Summary statistics for predictors
in each of the 4 regions
\item output: Results from \code{binmcp()} with pairwise comparisons of
proportions across the 4 regions
}
}
\description{
Tests for association between independent variables and a binary response by
recursively splitting the design space based on regression hyperplanes, then
comparing proportions across the resulting regions using methods appropriate
for binary data.
}
\details{
Similar to \code{reg.hyp.split()} but designed specifically for binary responses.
The recursive splitting procedure:
\enumerate{
\item Fits regression hyperplane to binary response data
\item Splits data based on residuals (above/below hyperplane)
\item Recursively splits each group again
\item Compares success proportions across 4 regions using \code{binmcp()}
}

Under the null hypothesis of no association, the 4 regions should have
similar success probabilities. Significant differences indicate association.

The Storer-Kim (SK) method controls family-wise error rate for binary
comparisons and is appropriate when comparing multiple proportions.
}
\note{
Requires at least 2 independent variables (p ≥ 2). For continuous
responses, use \code{reg.hyp.split()} instead.
}
\examples{
\dontrun{
# Test association with binary response
set.seed(123)
x1 <- rnorm(100)
x2 <- rnorm(100)
# Binary response influenced by predictors
p <- plogis(x1 + 2*x2)
y <- rbinom(100, 1, p)
regbin.hyp.split(cbind(x1, x2), y)

# Using Benjamini-Hochberg FDR control
regbin.hyp.split(cbind(x1, x2), y, method="BH", nboot=500)
}
}
\references{
Wilcox, R.R. (2022). Introduction to Robust Estimation and Hypothesis Testing
(5th ed.). Academic Press.

Storer, B.E., & Kim, C. (1990). Exact properties of some exact test statistics
for comparing two binomial proportions. Journal of the American Statistical
Association, 85, 146-155.
}
\seealso{
\code{\link{reg.hyp.split}}, \code{\link{binmcp}}, \code{\link{Qreg}}
}
