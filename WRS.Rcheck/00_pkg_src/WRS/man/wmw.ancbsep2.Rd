% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{wmw.ancbsep2}
\alias{wmw.ancbsep2}
\title{WMW-Based ANCOVA with Bootstrap for Multiple Covariates}
\usage{
wmw.ancbsep2(
  x1,
  y1,
  x2,
  y2,
  pts = NULL,
  nboot = 100,
  alpha = 0.05,
  SEED = TRUE,
  MC = FALSE,
  npts = 30,
  profun = prodepth,
  BOTH = TRUE,
  plotit = TRUE,
  xlab = "X1",
  ylab = "X2",
  method = "hoch",
  xout = FALSE,
  outfun = outpro
)
}
\arguments{
\item{x1}{Covariate matrix for group 1.}

\item{y1}{Response vector for group 1.}

\item{x2}{Covariate matrix for group 2.}

\item{y2}{Response vector for group 2.}

\item{pts}{Design points matrix (default: NULL for automatic selection).}

\item{nboot}{Number of bootstrap samples (default: 100).}

\item{alpha}{Significance level (default: 0.05).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed (default: \code{TRUE}).}

\item{MC}{Logical. If \code{TRUE}, uses parallel processing (default: \code{FALSE}).}

\item{npts}{Number of design points if pts=NULL (default: 30).}

\item{profun}{Projection depth function (default: \code{prodepth}).}

\item{BOTH}{Logical. If \code{TRUE}, selects points from pooled data (default: \code{TRUE}).}

\item{plotit}{Logical. If \code{TRUE}, creates plot (default: \code{TRUE}).}

\item{xlab}{X-axis label (default: 'X1').}

\item{ylab}{Y-axis label (default: 'X2').}

\item{method}{Multiple testing adjustment method (default: 'hoch').}

\item{xout}{Logical. If \code{TRUE}, removes outliers (default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro}).}
}
\value{
Matrix with WMW estimates, standard errors, and p-values at design points.
}
\description{
Performs ANCOVA using a heteroscedastic Wilcoxon-Mann-Whitney analog
with bootstrap standard errors. Supports multiple covariates.
}
\details{
Assumes a linear model is reasonable. Uses projection depth to select
design points and bootstrap to estimate standard errors. Tests are
adjusted for multiple comparisons.
}
\examples{
\dontrun{
# Two-covariate ANCOVA
set.seed(123)
x1 <- matrix(rnorm(100), ncol = 2)
y1 <- rowSums(x1) + rnorm(50)
x2 <- matrix(rnorm(100), ncol = 2)
y2 <- rowSums(x2) + 0.5 + rnorm(50)
wmw.ancbsep2(x1, y1, x2, y2, nboot = 200)
}
}
\seealso{
\code{\link{wmw.ancp2}}, \code{\link{wmw.ancbsep2.sub}}
}
