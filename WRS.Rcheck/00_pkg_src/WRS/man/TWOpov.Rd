% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{TWOpov}
\alias{TWOpov}
\title{Compare Two Dependent Pearson Correlations (Overlapping)}
\usage{
TWOpov(
  x,
  y,
  alpha = 0.05,
  CN = FALSE,
  BOOT = TRUE,
  nboot = 499,
  SEED = TRUE,
  ZCI = FALSE
)
}
\arguments{
\item{x}{Matrix or data frame with 2 columns.}

\item{y}{Numeric vector.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{CN}{Logical; if TRUE, uses Chen-Nadarajah method.}

\item{BOOT}{Logical; if TRUE, uses bootstrap method.}

\item{nboot}{Number of bootstrap samples (default 499).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{ZCI}{Logical; internal parameter for handling NA values in CI.}
}
\value{
A list with components:
\item{est.1}{Correlation between x\link{,1} and y}
\item{est.2}{Correlation between x\link{,2} and y}
\item{ci.lower}{Lower confidence limit for the difference}
\item{ci.upper}{Upper confidence limit for the difference}
\item{p.value}{P-value for testing equality (if BOOT=TRUE)}
}
\description{
Computes a confidence interval for the difference between two dependent Pearson
correlations in the overlapping case, where both correlations share a common variable.
}
\details{
This function compares the correlation of x\link{,1} with y to the correlation of x\link{,2}
with y. This is the "overlapping" case because both correlations share the common
variable y.

The default method uses a bootstrap approach (BOOT=TRUE). Alternatively, CN=TRUE
uses the Chen-Nadarajah method. The method deals with heteroscedasticity and
non-normality.
}
\examples{
set.seed(123)
n <- 50
x <- cbind(rnorm(n), rnorm(n))
y <- rnorm(n)

result <- TWOpov(x, y)
result$ci.lower
result$ci.upper
}
\references{
Wilcox, R.R. (2009). Comparing Pearson correlations: Dealing with heteroscedasticity
and non-normality. Communications in Statistics - Simulation and Computation, 38,
2220-2234.
}
\seealso{
\code{\link{TWOpNOV}}, \code{\link{twoDcorR}}, \code{\link{tworhobt}}
}
