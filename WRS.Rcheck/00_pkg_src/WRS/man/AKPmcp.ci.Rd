% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{AKPmcp.ci}
\alias{AKPmcp.ci}
\title{AKP Effect Size for All Pairwise Comparisons}
\usage{
AKPmcp.ci(
  x,
  tr = 0.2,
  alpha = 0.05,
  SEED = TRUE,
  nboot = 500,
  CI = TRUE,
  method = "hoch"
)
}
\arguments{
\item{x}{Data as matrix, data frame, or list. Each column (matrix/data frame) or
element (list) represents a group.}

\item{tr}{Trimming proportion (default: 0.2). Proportion to trim from each tail.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{SEED}{Logical. If \code{TRUE} (default), sets random seed for reproducibility.}

\item{nboot}{Number of bootstrap samples for confidence intervals (default: 500).}

\item{CI}{Logical. If \code{TRUE} (default), computes confidence intervals; if
\code{FALSE}, computes point estimates only.}

\item{method}{P-value adjustment method (default: "hoch" for Hochberg). See
\code{\link[stats]{p.adjust}} for options.}
}
\value{
Matrix with columns:
\item{Group}{First group number in comparison}
\item{Group}{Second group number in comparison}
\item{Effect.Size}{AKP effect size estimate}
\item{low.ci}{Lower confidence limit (if \code{CI=TRUE})}
\item{up.ci}{Upper confidence limit (if \code{CI=TRUE})}
\item{p.value}{Unadjusted p-value (if \code{CI=TRUE})}
\item{p.adjust}{Adjusted p-value using specified method}
}
\description{
Estimates AKP (Algina-Keselman-Penfield) effect size for all pairwise comparisons
in a one-way independent groups design. Optionally computes bootstrap confidence
intervals.
}
\details{
The AKP effect size is a robust standardized measure based on trimmed means and
Winsorized variances. For each pair of groups, computes:
\itemize{
\item Point estimate of AKP effect size
\item Bootstrap confidence interval (if \code{CI=TRUE})
\item P-value for the comparison
\item Adjusted p-value for multiple comparisons
}

When \code{CI=FALSE}, only point estimates are computed (faster).
}
\references{
Algina, J., Keselman, H. J., & Penfield, R. D. (2005). An alternative to Cohen's
standardized mean difference effect size: A robust parameter and confidence
interval in the two independent groups case. Psychological Methods, 10, 317-328.
}
\seealso{
\code{\link{akp.effect}}, \code{\link{akp.effect.ci}}, \code{\link[stats]{p.adjust}}
}
\keyword{htest}
\keyword{robust}
