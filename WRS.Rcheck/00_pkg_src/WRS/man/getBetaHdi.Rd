% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{getBetaHdi}
\alias{getBetaHdi}
\title{Compute Highest Density Interval for Beta Distribution}
\usage{
getBetaHdi(a, b, width)
}
\arguments{
\item{a}{Alpha parameter (first shape parameter) of beta distribution}

\item{b}{Beta parameter (second shape parameter) of beta distribution}

\item{width}{Width (coverage probability) of the interval}
}
\value{
Numeric vector of length 2: \code{c(lower, upper)} bounds of the HDI.
Returns \code{c(NA, NA)} for degenerate case (a ≈ b ≈ 1).
}
\description{
Computes the highest density interval (HDI) of specified width for a
beta distribution with shape parameters a and b.
}
\details{
The highest density interval (HDI) is the narrowest interval containing
the specified probability mass. For unimodal distributions like the beta,
it's the interval where all points inside have higher density than points outside.

\strong{Special cases handled}:
\itemize{
\item Degenerate (a ≈ 1, b ≈ 1): Returns c(NA, NA)
\item Left boundary (a ≈ 1, b > 1): HDI is \link{0, width}
\item Right boundary (a > 1, b ≈ 1): HDI is \link{1-width, 1}
\item Full range (width ≈ 1): Returns \link{0, 1}
\item General case: Uses numerical root finding around the mode
}
}
\keyword{distribution}
