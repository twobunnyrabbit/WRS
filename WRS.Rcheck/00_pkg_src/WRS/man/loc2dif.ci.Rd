% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{loc2dif.ci}
\alias{loc2dif.ci}
\title{Confidence Interval for Location Measure of Difference Between Independent Groups}
\usage{
loc2dif.ci(x, y, est = median, alpha = 0.05, nboot = 2000, SEED = TRUE)
}
\arguments{
\item{x}{Numeric vector for group 1.}

\item{y}{Numeric vector for group 2.}

\item{est}{Function for location measure (default: \code{median}).}

\item{alpha}{Numeric. Significance level (default: 0.05).}

\item{nboot}{Integer. Number of bootstrap samples for fallback method (default: 2000).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}
}
\value{
List with components:
\describe{
\item{n1}{Sample size of group 1}
\item{n2}{Sample size of group 2}
\item{est}{Point estimate of location measure for all pairwise differences}
\item{conf.int}{Confidence interval for the location measure}
}
}
\description{
Computes a confidence interval for a location measure (default: median) of the
difference D = X - Y for two independent groups. Uses Cliff's delta-based method
with fallback to percentile bootstrap if needed.
}
\details{
\strong{Method}:
\enumerate{
\item Computes Cliff's delta CI using \code{\link{cid}}
\item Converts Cliff's delta bounds to shift values via \code{\link{WMW2med}}
\item Applies shifts to data and computes location measure
\item If method fails (e.g., degenerate CI), falls back to \code{\link{wmwpb}}
}

The approach provides a CI for the location of the distribution of all pairwise
differences X_i - Y_j, not just the difference of medians.
}
\examples{
set.seed(123)
x <- rnorm(25, mean = 0)
y <- rnorm(30, mean = 0.5)
loc2dif.ci(x, y, est = median)
}
\seealso{
\code{\link{cid}} for Cliff's delta,
\code{\link{WMW2med}} for converting Cliff's delta to shift,
\code{\link{wmwpb}} for percentile bootstrap alternative,
\code{\link{loc2dif}} for point estimate
}
\keyword{htest}
\keyword{robust}
