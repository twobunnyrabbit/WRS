% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression.R
\name{MMreg}
\alias{MMreg}
\title{MM-Regression Estimator}
\usage{
MMreg(
  x,
  y,
  RES = TRUE,
  xout = FALSE,
  outfun = outpro,
  STAND = TRUE,
  varfun = pbvar,
  corfun = pbcor,
  WARN = FALSE,
  ...
)
}
\arguments{
\item{x}{A numeric matrix of predictor variables (n by p).}

\item{y}{A numeric vector of the dependent variable (length n).}

\item{RES}{Logical; if TRUE (default), returns residuals.}

\item{xout}{Logical. Not used in this function but included to avoid conflicts
when using with other functions like \code{regci} (default: \code{FALSE}).}

\item{STAND}{Logical; if TRUE (default), standardizes predictors for outlier detection.}

\item{varfun}{Function for computing variance (default: \code{pbvar}), used for
strength of association.}

\item{corfun}{Function for computing correlation (default: \code{pbcor}), used as
fallback for strength of association.}

\item{WARN}{Logical; if FALSE (default), suppresses warnings from \code{lmrob}.}
}
\value{
A list with components:
\describe{
\item{coef}{Vector of regression coefficients (intercept, slopes).}
\item{residuals}{Residuals from the fit (if \code{RES=TRUE}).}
\item{Strength.Assoc}{Measure of association strength (explanatory power).}
}
}
\description{
Computes Yohai's (1987) MM-regression estimator, which combines high breakdown
point with high efficiency. This is a wrapper around \code{robustbase::lmrob}
with optional outlier removal and association strength assessment.
}
\details{
MM-regression achieves:
\itemize{
\item \strong{High breakdown point} (50\%) from the initial S-estimate
\item \strong{High efficiency} (95\% under normality) from the final M-estimate
\item \strong{Equivariance} under affine transformations
}

The method works in three stages:
\enumerate{
\item Compute a high-breakdown S-estimate of regression
\item Compute M-estimate of scale using S-estimate residuals
\item Compute final M-estimate of regression using this scale
}

\strong{Association Strength}: The function computes an explanatory power measure
as the ratio of variance of fitted values to variance of observed values,
using robust variance estimators. If this exceeds 1 (possible with robust methods),
it falls back to the squared robust correlation.

\strong{Requires}: The \code{robustbase} package must be installed.
}
\examples{
\dontrun{
# Simple MM-regression
x <- matrix(rnorm(50), ncol=1)
y <- 2*x + rnorm(50, sd=0.5)
MMreg(x, y)

# Multiple regression with outlier removal
x <- matrix(rnorm(100), ncol=2)
y <- x[,1] + 2*x[,2] + rnorm(50, sd=0.3)
y[1:5] <- y[1:5] + 10  # add outliers
fit <- MMreg(x, y, xout=TRUE)
print(fit$Strength.Assoc)
}
}
\references{
Yohai, V.J. (1987). High breakdown-point and high efficiency robust estimates
for regression. The Annals of Statistics, 15, 642-656.
}
\seealso{
\code{\link{ltsreg}}, \code{\link{LMSreg}}

Other robust regression functions: 
\code{\link{LMSreg}()},
\code{\link{bireg}()},
\code{\link{bmreg}()},
\code{\link{chreg}()},
\code{\link{depreg}()},
\code{\link{mbmreg}()},
\code{\link{mdepreg}()},
\code{\link{mopreg}()},
\code{\link{snmreg}()},
\code{\link{tsgreg}()},
\code{\link{winreg}()},
\code{\link{wreg}()}
}
\concept{robust regression functions}
