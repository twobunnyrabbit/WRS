% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{relfun}
\alias{relfun}
\alias{relplot}
\title{Bivariate Boxplot Estimators (Goldberg-Iglewicz)}
\usage{
relfun(
  xv,
  yv,
  C = 36,
  epsilon = 1e-04,
  plotit = TRUE,
  pch = "*",
  xlab = "X",
  ylab = "Y"
)

relplot(
  xv,
  yv,
  C = 36,
  epsilon = 1e-04,
  plotit = TRUE,
  pch = "*",
  xlab = "X",
  ylab = "Y"
)
}
\arguments{
\item{xv}{Numeric vector for first variable.}

\item{yv}{Numeric vector for second variable.}

\item{C}{Tuning constant controlling weight function (default: 36).}

\item{epsilon}{Convergence criterion for iterative algorithm (default: 0.0001).}

\item{plotit}{Logical; if \code{TRUE}, plots the bivariate boxplot
(default: \code{TRUE}).}

\item{pch}{Plotting character for data points (default: '*').}

\item{xlab, ylab}{Axis labels (defaults: 'X', 'Y').}
}
\value{
A list with components:
\item{mest}{Robust bivariate location estimate: \code{c(x_location, y_location)}}
\item{mvar}{Robust bivariate scale estimates: \code{c(x_variance, y_variance)}}
\item{mrho}{Robust correlation estimate}
}
\description{
Computes robust measures of location, scale, and correlation for bivariate
data using the method of Goldberg and Iglewicz (1992). Optionally plots
the bivariate boxplot with inner and outer fences.
}
\details{
This function implements the bivariate boxplot method from:

Goldberg, K. M., & Iglewicz, B. (1992). Bivariate extensions of the
boxplot. \emph{Technometrics}, 34(3), 307-320.

The algorithm iteratively computes weighted estimates based on Mahalanobis-like
distances. Points far from the center receive less weight. When \code{plotit=TRUE},
the plot shows:
\itemize{
\item Data points (marked with \code{pch})
\item Inner fence (50\\% region based on median distance)
\item Outer fence (outer boundary based on maximum non-outlying distance)
}
}
\references{
Goldberg, K. M., & Iglewicz, B. (1992). Bivariate extensions of the boxplot.
\emph{Technometrics}, 34(3), 307-320. doi:10.2307/1268655
}
\seealso{
\code{\link{biloc}}, \code{\link{bivar}}, \code{\link{relplot}}
}
\keyword{multivariate}
\keyword{robust}
