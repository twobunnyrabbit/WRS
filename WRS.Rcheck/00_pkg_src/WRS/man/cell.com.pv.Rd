% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{cell.com.pv}
\alias{cell.com.pv}
\title{P-Value for Difference Between Multinomial Cell Probabilities}
\usage{
cell.com.pv(x, i = 1, j = 2, method = "AC")

cell.com.pv(x, i = 1, j = 2, method = "AC")
}
\arguments{
\item{x}{Numeric vector of cell frequencies.}

\item{i}{Numeric. Index of first cell to compare (default: 1).}

\item{j}{Numeric. Index of second cell to compare (default: 2).}

\item{method}{Character. Method for binomial confidence intervals (default: "AC").}

\item{data}{Optional data frame; if provided, cell frequencies computed via \code{splot}}
}
\value{
Numeric p-value for testing equality of the two cell probabilities

Numeric. P-value for testing H0: p_i = p_j.
}
\description{
For a multinomial distribution, computes a p-value for testing H0: p_i = p_j,
where p_i and p_j are the probabilities associated with cells i and j.

Computes a p-value for testing whether two cells in a multinomial distribution
have different probabilities, based on observed cell frequencies. Uses confidence
intervals for the difference in proportions.
}
\details{
Searches over alpha values to find the smallest level at which the confidence interval
for p_i - p_j excludes zero. Uses refined search for small samples (n ≤ 35).

Computes confidence intervals for each cell probability p_i and p_j, accounting
for their negative correlation. Searches for the smallest alpha level where
the confidence interval for p_i - p_j excludes zero. For small samples (n ≤ 35),
refines the p-value using \code{\link{binom.conf}}.

The test accounts for the dependency between cell proportions through a correlation
correction term.
}
\examples{
\dontrun{
# Multinomial with 4 cells
cell_freq <- c(15, 25, 18, 12)
# Test if cells 1 and 2 have different probabilities
cell.com.pv(cell_freq, i = 1, j = 2)
}

}
\seealso{
\code{\link{cell.com}} for CI version, \code{\link{acbinomci}}

\code{\link{acbinomci}}, \code{\link{binom.conf}}, \code{\link{best.cell.sub}}
}
\keyword{htest}
\keyword{robust}
