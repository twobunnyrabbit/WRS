% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outliers.R
\name{zoudepth}
\alias{zoudepth}
\title{Compute Projection Depth Using Zuo's Method}
\usage{
zoudepth(x, pts = x, zloc = median, zscale = mad, SEED = TRUE)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{pts}{Points for which to compute depth (default: \code{m}).}

\item{zloc}{Location estimator for projections (default: median).}

\item{zscale}{Scale estimator for projections (default: mad).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}
}
\value{
Vector of projection depth values (higher = more central).
}
\description{
Wrapper for \code{zdepth()} that returns depth values (rather than inverse depth).
Uses Nelder-Mead optimization to find projection depth.
}
\details{
Computes 1/(1 + zdepth()), converting Zuo's outlyingness measure to a depth
measure. More computationally intensive than \code{prodepth()} but exact.
}
\seealso{
\code{\link{zdepth}}, \code{\link{prodepth}}
}
