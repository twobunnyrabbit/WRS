% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{lplot2g}
\alias{lplot2g}
\title{Plot Running Interval Smoother for Two Groups}
\usage{
lplot2g(
  x1,
  y1,
  x2,
  y2,
  fr = 0.8,
  est = tmean,
  xlab = "X",
  ylab = "Y",
  xout = FALSE,
  eout = FALSE,
  pr = TRUE,
  pch1 = "*",
  pch2 = "o",
  outfun = outpro,
  ...
)
}
\arguments{
\item{x1}{Predictor values for group 1 (vector).}

\item{y1}{Response values for group 1 (vector).}

\item{x2}{Predictor values for group 2 (vector).}

\item{y2}{Response values for group 2 (vector).}

\item{fr}{Span for LOWESS smoother (default: 0.8). Controls amount of smoothing.}

\item{est}{Not used in current implementation (retained for compatibility).}

\item{xlab}{Label for X-axis (default: "X").}

\item{ylab}{Label for Y-axis (default: "Y").}

\item{xout}{Logical; if TRUE, removes outliers based on X values only (default: FALSE).}

\item{eout}{Logical; if TRUE, removes bivariate outliers (default: FALSE).}

\item{pr}{Logical; if TRUE, prints suggestion about using xout=TRUE (default: TRUE).}

\item{pch1}{Plotting character for group 1 points (default: '*').}

\item{pch2}{Plotting character for group 2 points (default: 'o').}

\item{outfun}{Outlier detection function (default: \code{outpro}).}

\item{...}{Additional arguments passed to outfun.}
}
\value{
None. Creates a plot.
}
\description{
Creates a scatterplot with running interval smoothers for two independent groups,
allowing visual comparison of regression patterns between groups. Uses LOWESS smoothing.
}
\details{
This function creates a scatterplot showing data from two groups with separate
LOWESS smoothed curves overlaid. The curves allow visual comparison of the
regression relationships in the two groups.

\strong{Group 1} is plotted with solid line and pch1 symbol.
\strong{Group 2} is plotted with dashed line (lty=2) and pch2 symbol.

Missing values are automatically removed. Outlier removal (if requested) is
performed separately for each group before smoothing.

\strong{Note}: The function suggests also examining the plot with xout=TRUE to assess
the impact of outliers in the predictor space.
}
\examples{
# Compare regression patterns in two groups
set.seed(123)
x1 <- rnorm(50)
y1 <- 2 * x1 + rnorm(50)
x2 <- rnorm(50)
y2 <- -1.5 * x2 + rnorm(50)
lplot2g(x1, y1, x2, y2)

# With outlier removal
lplot2g(x1, y1, x2, y2, xout = TRUE)
}
\seealso{
\code{\link{lplot}}, \code{\link{rplot2g}}, \code{\link{reg2plot}}
}
