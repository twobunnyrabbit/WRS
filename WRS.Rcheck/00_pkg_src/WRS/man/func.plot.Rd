% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{func.plot}
\alias{func.plot}
\title{Functional Boxplot (Matrix Input)}
\usage{
func.plot(
  fit,
  x = NULL,
  method = "MBD",
  depth = NULL,
  plotit = TRUE,
  prob = 0.5,
  color = 6,
  outliercol = 2,
  barcol = 4,
  fullout = FALSE,
  factor = 1.5,
  xlim = c(1, nrow(fit)),
  ylim = c(min(fit) - 0.5 * diff(range(fit)), max(fit) + 0.5 * diff(range(fit))),
  xlab = "Time",
  ylab = "Y",
  ...
)
}
\arguments{
\item{fit}{Matrix (n × p) where n = number of curves, p = number of time points.
Rows with missing values are removed.}

\item{x}{Evaluation points (default: NULL, uses column indices).}

\item{method}{Depth measure (default: "MBD"). See \code{\link{fbplot}} for options.}

\item{depth}{Pre-computed depth values (default: NULL).}

\item{plotit}{Logical. Create plot if TRUE (default: TRUE).}

\item{prob}{Probability for central region (default: 0.5).}

\item{color}{Color for central region (default: 6).}

\item{outliercol}{Color for outliers (default: 2).}

\item{barcol}{Color for whiskers (default: 4).}

\item{fullout}{Logical. Show all outliers if TRUE (default: FALSE).}

\item{factor}{Whisker extension factor (default: 1.5).}

\item{xlim}{X-axis limits (default: based on data range).}

\item{ylim}{Y-axis limits (default: based on data range with padding).}

\item{xlab}{Label for X-axis (default: "Time").}

\item{ylab}{Label for Y-axis (default: "Y").}

\item{...}{Additional graphical parameters.}
}
\value{
List with components:
\item{depth}{Depth values for all curves.}
\item{outpoint}{Indices of curves identified as outliers.}
}
\description{
Creates a functional boxplot for matrix data using the Sun & Genton method.
This is a wrapper for \code{fbplot} that transposes the matrix to the expected format.
}
\details{
This function is a convenience wrapper for \code{fbplot} that accepts matrix
input in the natural row-wise format (each row = one curve). The matrix is
transposed before passing to \code{fbplot}.

\strong{Input format}: Provide data as an n × p matrix where:
\itemize{
\item \strong{Rows}: Individual curves (subjects)
\item \strong{Columns}: Time points or evaluation points
}

This is the opposite of \code{fbplot}, which expects columns to be curves.

Requires the \code{fda} package. See \code{\link{fbplot}} for full details
on the functional boxplot methodology.
}
\examples{
# Functional boxplot for matrix data
set.seed(123)
t <- seq(0, 1, length = 50)
# Create 20 curves
x <- matrix(0, nrow = 20, ncol = 50)
for(i in 1:20) {
  x[i,] <- sin(2*pi*t) + rnorm(50, 0, 0.1)
}
# Add outlier
x[1,] <- sin(2*pi*t) + 1.5

func.plot(x)
}
\seealso{
\code{\link{fbplot}}, \code{\link{Flplot}}, \code{\link{spag.plot}}
}
