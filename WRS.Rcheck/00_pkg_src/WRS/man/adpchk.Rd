% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{adpchk}
\alias{adpchk}
\title{Compare Additive and General Model Fits}
\usage{
adpchk(
  x,
  y,
  adfun = adrun,
  gfun = runm3d,
  xlab = "Additive Fit",
  ylab = "Gen. Fit",
  plotfun = lplot,
  xout = FALSE,
  outfun = out,
  ...
)
}
\arguments{
\item{x}{Matrix of predictor variables}

\item{y}{Response vector}

\item{adfun}{Function for fitting additive model (default: \code{\link{adrun}})}

\item{gfun}{Function for fitting general model (default: \code{\link{runm3d}})}

\item{xlab}{Label for x-axis (additive model fits) (default: "Additive Fit")}

\item{ylab}{Label for y-axis (general model fits) (default: "Gen. Fit")}

\item{plotfun}{Plotting function (default: \code{\link{lplot}})}

\item{xout}{Logical. If \code{TRUE}, removes outliers before fitting (default: \code{FALSE})}

\item{outfun}{Outlier detection function (default: \code{\link{out}})}

\item{...}{Additional arguments passed to fitting functions}
}
\value{
Creates a plot invisibly. Points should fall on the identity line (y=x) if the
additive model adequately captures the general model's fit.
}
\description{
Compares fitted values from an additive model (e.g., \code{\link{adrun}}) to
fitted values from a more general model (e.g., \code{\link{runm3d}}). Creates
a scatter plot to assess whether the additive assumption is reasonable.
}
\details{
The function:
\enumerate{
\item Fits both additive and general models to the data
\item Extracts fitted values from each model
\item Creates scatter plot with identity line (y=x)
}

Substantial deviations from the identity line suggest the additive model is
inadequate and interactions or higher-order terms may be needed.
}
\seealso{
\code{\link{adrun}}, \code{\link{runm3d}}, \code{\link{adtest}}
}
\keyword{models}
\keyword{regression}
