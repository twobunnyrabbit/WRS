% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outliers.R
\name{outproad}
\alias{outproad}
\title{Projection Outlier Detection with Adjusted Critical Value}
\usage{
outproad(
  m,
  center = NA,
  plotit = TRUE,
  op = TRUE,
  MM = TRUE,
  cop = 3,
  xlab = "VAR 1",
  ylab = "VAR 2",
  rate = 0.05,
  iter = 100,
  ip = 6,
  pr = TRUE,
  SEED = TRUE,
  STAND = TRUE
)
}
\arguments{
\item{m}{A numeric matrix where rows are observations and columns are variables.}

\item{center}{Optional center point. If NA, computed based on \code{cop}.}

\item{plotit}{Logical. If \code{TRUE}, produces a scatterplot (default: \code{FALSE}).}

\item{op}{Logical. If TRUE, plots depth contour with outliers removed.}

\item{MM}{Logical. If TRUE, uses MAD; if FALSE, uses IQR.}

\item{cop}{Integer (1-7) specifying center computation method (see outproMC).}

\item{xlab}{Label for x-axis.}

\item{ylab}{Label for y-axis.}

\item{rate}{Target false positive rate under normality (default: 0.05).}

\item{iter}{Number of iterations for simulation to adjust critical value
(default: 100).}

\item{ip}{Number of quantile values to try when adjusting (default: 6).}

\item{pr}{Logical. If \code{TRUE}, prints progress messages (default: \code{TRUE}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{STAND}{Logical. If TRUE, standardizes marginal distributions.}
}
\value{
A list with components:
\item{n}{Sample size.}
\item{n.out}{Number of outliers detected.}
\item{out.id}{Indices of outliers.}
\item{keep}{Indices of non-outliers.}
\item{used.gval}{Adjusted critical value used.}
}
\description{
Adjusts the critical value used by \code{outpro()} so that the expected proportion
of points declared outliers under multivariate normality equals a specified
rate. This adjustment is particularly crucial for high-dimensional data (p>9).
}
\details{
The function simulates multivariate normal data and iteratively adjusts the
critical value \code{gval} until the empirical outlier rate matches the target
\code{rate}. This ensures appropriate Type I error control under normality.
}
\seealso{
\code{\link{outpro}}, \code{\link{outproMC}}, \code{\link{outproadMC}}
}
