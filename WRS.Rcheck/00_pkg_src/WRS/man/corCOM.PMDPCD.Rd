% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{corCOM.PMDPCD}
\alias{corCOM.PMDPCD}
\title{Probability of Making and Correctly Deciding on Largest Correlation}
\usage{
corCOM.PMDPCD(
  n,
  p,
  rho = 0,
  delta = 0.3,
  corfun = wincor,
  LARGEST = TRUE,
  alpha = 0.05,
  x = NULL,
  y = NULL,
  iter = 500,
  pr = TRUE,
  SEED = TRUE,
  MC = TRUE,
  FUN = mean,
  ...
)
}
\arguments{
\item{n}{Sample size.}

\item{p}{Number of explanatory variables.}

\item{rho}{Correlation for non-focal predictors (default: 0).}

\item{delta}{Correlation difference for focal predictor (default: 0.3).
Focal predictor has correlation rho + delta.}

\item{corfun}{Correlation function to use. Common options: \code{pbcor} (percentage
bend), \code{pcor} (Pearson), \code{wincor} (winsorized), \code{spear} (Spearman).}

\item{LARGEST}{Logical. Test for largest correlation if \code{TRUE} (default),
smallest if \code{FALSE}.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{x}{Optional data matrix to estimate correlations from (default: \code{NULL}).}

\item{y}{Optional response vector to estimate correlations from (default: \code{NULL}).}

\item{iter}{Number of simulation iterations (default: 500).}

\item{pr}{Logical. Print warnings about execution time (default: \code{TRUE}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{MC}{Logical. Use parallel processing (default: \code{TRUE}).}

\item{FUN}{Function to summarize correlation matrix if x and y are provided
(default: \code{mean}).}

\item{...}{Additional arguments passed to \code{corfun}.}
}
\value{
List with components:
\item{PMD}{Probability of making a decision (proportion of iterations where decision was made).}
\item{PMD.CI}{Confidence interval for PMD.}
\item{PCD}{Probability of correct decision (proportion of decisions that correctly identified focal predictor).}
\item{PCD.CI}{Confidence interval for PCD.}
}
\description{
Estimates PMD (Probability of Making a Decision) and PCD (Probability of
Correct Decision) for identifying the predictor with largest correlation to y.
}
\details{
Determines the probability of making a decision about which predictor has the
largest correlation with y in an indifference zone context. By default, all
predictors have correlation rho with y except the first, which has rho + delta.

Cohen's suggestions for small, medium, and large correlations are 0.1, 0.3, 0.5.

If x and y are provided, estimates correlation matrix from data instead of
using indifference zone.
}
\examples{
\dontrun{
# Estimate PMD and PCD for n=50, p=4, delta=0.3
corCOM.PMDPCD(n = 50, p = 4, delta = 0.3, iter = 100)
}
}
\seealso{
\code{\link{corCOM.DVvsIV}}, \code{\link{corREG.best}}
}
