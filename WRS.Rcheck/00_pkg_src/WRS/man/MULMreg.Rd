% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression-advanced.R
\name{MULMreg}
\alias{MULMreg}
\title{Multivariate Multiple Regression}
\usage{
MULMreg(x, y, regfun = MMreg, xout = FALSE, outfun = outpro, ...)
}
\arguments{
\item{x}{A numeric matrix containing predictor values (must have 2 or more columns).}

\item{y}{A numeric matrix containing response variables (must have 2 or more
columns, one for each response).}

\item{regfun}{Regression function to use for each response variable
(default: \code{MMreg} for MM-regression). Can be any regression function that
returns a \code{coef} component (e.g., \code{tsreg}, \code{ltsreg}, \code{opreg}).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{...}{Additional arguments passed to correlation or estimation functions.}
}
\value{
A list with component:
\item{coef}{Matrix of regression coefficients with rows corresponding to
parameters (intercept, slopes) and columns corresponding to response
variables. Row names indicate parameter type.}
}
\description{
Performs multivariate multiple regression where the response consists of
multiple dependent variables. Estimates regression parameters for each
response variable separately using a specified regression estimator.
}
\details{
This function fits separate regression models for each column of \code{y}:
\itemize{
\item Each response variable is regressed on all predictors in \code{x}
\item The same regression estimator is used for all responses
\item Results are organized in a coefficient matrix
}

This is different from truly multivariate regression (which models the
covariance structure among responses). Here, each response is modeled
independently.

If \code{xout = TRUE}, outliers are removed from the predictor space before
analysis using the function specified by \code{outfun}. The same observations
are removed for all response variables.
}
\note{
Both \code{x} and \code{y} must be matrices with at least 2 columns each.
}
\examples{
\dontrun{
# Multivariate regression with 2 predictors and 3 responses
set.seed(123)
n <- 100
x <- matrix(rnorm(n*2), ncol=2)
y <- matrix(rnorm(n*3), ncol=3)
y[,1] <- 2 + 3*x[,1] - x[,2] + rnorm(n)
y[,2] <- -1 + x[,1] + 2*x[,2] + rnorm(n)
y[,3] <- 5 - 2*x[,1] + rnorm(n)

# Fit using MM-regression
result <- MULMreg(x, y)
result$coef

# Use Theil-Sen instead
result2 <- MULMreg(x, y, regfun=tsreg)
}
}
\references{
Wilcox, R.R. (2022). Introduction to Robust Estimation and Hypothesis Testing
(5th ed.). Academic Press.
}
\seealso{
\code{\link{mlrreg}}, \code{\link{MMreg}}, \code{\link{tsreg}}
}
