% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcp.R
\name{bwmcppb.sub}
\alias{bwmcppb.sub}
\title{Percentile Bootstrap MCP for Between-Within Designs (Internal Helper)}
\usage{
bwmcppb.sub(
  J,
  K,
  x,
  est = tmean,
  JK = J * K,
  con = 0,
  method = "hoch",
  alpha = 0.05,
  grp = c(1:JK),
  nboot = 500,
  bhop = TRUE,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{J}{Number of levels of Factor A (between-subjects factor)}

\item{K}{Number of levels of Factor B (within-subjects factor)}

\item{x}{Data in list mode or matrix. For list mode: x[\link{1}] = level (1,1),
x[\link{2}] = level (1,2), ..., x[\link{K}] = level (1,K), x[\link{K+1}] = level (2,1), etc.}

\item{est}{Estimator function (default: \code{tmean} for trimmed mean)}

\item{JK}{Total number of groups (J*K)}

\item{con}{Contrast matrix for the comparisons of interest}

\item{method}{P-value adjustment method (default: 'hoch' for Hochberg)}

\item{alpha}{Family-wise error rate (default: 0.05)}

\item{grp}{Group indices to analyze (default: all groups 1:JK)}

\item{nboot}{Number of bootstrap samples (default: 500)}

\item{bhop}{Logical; if TRUE use Benjamini-Hochberg; if FALSE use specified method
(default: TRUE)}

\item{SEED}{Logical; if TRUE sets random seed for reproducibility (default: TRUE)}

\item{...}{Additional arguments passed to estimator function}
}
\value{
Matrix with columns: con.num, psihat (contrast estimate), p.value,
adj.p.value, ci.lower, ci.upper
}
\description{
Internal helper function for performing percentile bootstrap multiple comparisons
in between-within (mixed) factorial designs. Called by \code{bwmcppb} to analyze
specific contrasts.
}
\details{
This is an internal subroutine used by \code{bwmcppb} to handle the bootstrap
analysis for a specific set of contrasts (main effects or interactions).

The bootstrap procedure:
\itemize{
\item Resamples subjects within each level of the between-subjects factor
\item Computes the contrast estimates for each bootstrap sample
\item Determines p-values and confidence intervals based on the bootstrap
distribution
}
}
\seealso{
\code{\link{bwmcppb}} for the main user function
}
\keyword{internal}
