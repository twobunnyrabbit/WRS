% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{b1way}
\alias{b1way}
\title{One-Way Bootstrap Test for Location Measures}
\usage{
b1way(x, est = onestep, nboot = 599, SEED = TRUE, ...)
}
\arguments{
\item{x}{Data in list mode, matrix, or data frame. If a list, \code{x[[j]]} contains
data for group j. If a matrix or data frame, columns represent groups.}

\item{est}{Location estimator to use. Default is \code{\link{onestep}} (one-step M-estimator).}

\item{nboot}{Number of bootstrap samples. Default is 599.}

\item{SEED}{Logical. If \code{TRUE} (default), sets random seed to 2 for reproducibility.}

\item{...}{Additional arguments passed to the estimator function.}
}
\value{
A list with components:
\item{teststat}{Observed test statistic (weighted sum of squares).}
\item{p.value}{Bootstrap p-value.}
}
\description{
Tests the hypothesis that J measures of location are equal using the percentile
bootstrap method. By default, one-step M-estimators are compared.
}
\details{
This function tests \eqn{H_0: \theta_1 = \theta_2 = ... = \theta_J}{H0: θ1 = θ2 = ... = θJ}
where \eqn{\theta_j}{θj} is the location measure for group j.

The test procedure:
\enumerate{
\item For each group j, center the data by subtracting the estimated location
\item Generate bootstrap samples from the centered data
\item Compute the test statistic (weighted sum of squares) for each bootstrap sample
\item Compare the observed test statistic to the bootstrap distribution
}

The test statistic is based on \code{\link{wsumsq}} (weighted sum of squares).

Missing values are automatically removed.
}
\examples{
# Compare medians across three groups
x <- list(rnorm(20), rnorm(20, 0.5), rnorm(20, 1))
b1way(x, est = median)

}
\seealso{
\code{\link{onestep}}, \code{\link{wsumsq}}, \code{\link{t1way}}
}
\keyword{htest}
\keyword{robust}
