% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression-advanced.R
\name{regvarp}
\alias{regvarp}
\title{Measure Importance of Predictors in Regression}
\usage{
regvarp(x, y, p = 1, locfun = lloc, scat = var, est = mean, cov.fun = cov.mba)
}
\arguments{
\item{x}{A numeric matrix containing predictor values (must have multiple columns).}

\item{y}{A numeric vector containing the response variable.}

\item{p}{Integer specifying the number of predictors to consider simultaneously
(default: 1 for individual predictors). If p > 1, evaluates all p-way
predictor combinations.}

\item{locfun}{Location function for standardizing predictors (default: \code{lloc}).}

\item{scat}{Scatter function for standardizing predictors (default: \code{var}).}

\item{est}{Estimator function for standardizing predictors (default: \code{mean}).}

\item{cov.fun}{Covariance function to use for generalized variance computation
(default: \code{cov.mba} for Median Ball Algorithm covariance).}
}
\value{
A numeric vector containing generalized variance values:
\itemize{
\item If \code{p=1}: One value per predictor
\item If \code{p>1}: One value per p-way predictor combination
}
Higher values indicate stronger association with the response.
}
\description{
Assesses the importance of individual predictors or predictor combinations
in a regression problem by computing generalized variance for each predictor
or p-predictor subset.
}
\details{
This function evaluates predictor importance by:
\enumerate{
\item Standardizing all predictors using specified location, scatter, and
estimator functions
\item For each predictor or predictor combination, computing the generalized
variance (determinant of robust covariance matrix) of the response with
the predictor(s)
\item Returning these variance measures as importance metrics
}

The generalized variance provides a multivariate measure of association that
accounts for the covariance structure. Lower generalized variance indicates
stronger association (less unexplained variability).

Missing values are automatically removed before analysis.
}
\note{
Used internally by \code{regpord()} and \code{regpord.sub()} for comparing
predictor strengths.
}
\examples{
\dontrun{
# Measure importance of individual predictors
set.seed(123)
x <- matrix(rnorm(100*3), ncol=3)
y <- 2*x[,1] + 0.5*x[,2] + rnorm(100)
regvarp(x, y, p=1)  # Evaluate each predictor

# Evaluate all 2-way combinations
regvarp(x, y, p=2)
}
}
\references{
Wilcox, R.R. (2022). Introduction to Robust Estimation and Hypothesis Testing
(5th ed.). Academic Press.
}
\seealso{
\code{\link{regpord}}, \code{\link{regIVstr}}, \code{\link{gvarg}}
}
