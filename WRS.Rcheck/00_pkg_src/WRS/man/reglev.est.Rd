% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{reglev.est}
\alias{reglev.est}
\title{Regression Estimation with Bad Leverage Point Removal}
\usage{
reglev.est(x, y, regfun = tsreg, ...)
}
\arguments{
\item{x}{Matrix of predictors (n × p).}

\item{y}{Response vector.}

\item{regfun}{Regression function (default: \code{tsreg} for Theil-Sen).}

\item{...}{Additional arguments passed to \code{regfun} and \code{reglev.gen}.}
}
\value{
A numeric vector containing the estimated regression coefficients
(intercept and slopes) computed on the data with leverage points removed.
}
\description{
Estimates regression parameters after identifying and removing bad leverage
points using a homoscedastic detection method.
}
\details{
This function:
\enumerate{
\item Uses \code{\link{reglev.gen}} to identify bad leverage points
\item Removes flagged observations
\item Estimates regression parameters using \code{regfun} on clean data
}

The homoscedastic leverage point detection is appropriate when errors
are assumed to have constant variance. This is a simpler approach than
\code{\link{regci.het.blp}} that only returns point estimates without
confidence intervals.
}
\seealso{
\code{\link{reglev.gen}}, \code{\link{regci.het.blp}}, \code{\link{regciHH}}, \code{\link{tsreg}}
}
\keyword{regression}
