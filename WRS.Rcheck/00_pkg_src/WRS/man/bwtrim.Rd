% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/location.R
\name{bwtrim}
\alias{bwtrim}
\title{Bootstrap Two-Way Repeated Measures ANOVA with Trimmed Means}
\usage{
bwtrim(
  J,
  K,
  data,
  tr = 0.2,
  grp = c(1:p),
  p = J * K,
  MAT = FALSE,
  grpc = 1,
  coln = c(2:3)
)
}
\arguments{
\item{J}{Number of levels for the between-subjects factor.}

\item{K}{Number of levels for the within-subjects factor.}

\item{data}{Data in list mode, matrix, or data frame.}

\item{tr}{Trimming proportion (default: 0.2). Proportion of observations to
trim from each tail when computing winsorized correlation.}

\item{grp}{A vector indicating group membership. Can be numeric, factor, or
character. Used for grouping observations in multi-group comparisons.}

\item{MAT}{Logical. If \code{TRUE}, data is assumed to be in matrix format
(default: \code{FALSE}).}

\item{grpc}{Column number containing group labels if MAT=\code{FALSE}.}

\item{coln}{Column numbers containing the data if MAT=\code{FALSE}.}
}
\value{
A list with components containing test statistics and p-values for
main effects and interaction.
}
\description{
Performs J x K split-plot ANOVA: first factor is between-subjects (independent
groups), second factor is within-subjects (repeated measures).
}
\details{
This function implements a split-plot (mixed) design where one factor varies
between subjects and the other within subjects. The bootstrap-t method is
used for inference with trimmed means.
}
\examples{
# 2x3 split-plot design
set.seed(123)
data_list <- list()
for(i in 1:6) data_list[[i]] <- rnorm(20)
bwtrim(2, 3, data_list, nboot = 500)

}
\seealso{
\code{\link{bbtrim}} for fully between-subjects design,
\code{\link{bbwtrim}} for three-way mixed design,
\code{\link{rmanova}} for repeated measures ANOVA
}
