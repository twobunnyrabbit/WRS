% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{tworegwb}
\alias{tworegwb}
\title{Test Equality of Regression Slopes for Two Independent Groups (Bootstrap)}
\usage{
tworegwb(
  x1,
  y1,
  x2,
  y2,
  nboot = 599,
  RAD = FALSE,
  alpha = 0.05,
  SEED = TRUE,
  xout = FALSE,
  outfun = out
)
}
\arguments{
\item{x1}{Predictor values for group 1.}

\item{y1}{Response values for group 1.}

\item{x2}{Predictor values for group 2.}

\item{y2}{Response values for group 2.}

\item{nboot}{Number of bootstrap samples (default: 599).}

\item{RAD}{Logical. If \code{TRUE}, uses robust analog of distance (default: \code{FALSE}).}

\item{alpha}{Significance level (default: 0.05).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducibility (default: \code{TRUE}).}

\item{xout}{Logical. If \code{TRUE}, removes outliers using \code{outfun} (default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{out}).}
}
\value{
The p-value for testing equality of slopes.
}
\description{
Tests whether two independent groups have equal regression slopes using
a bootstrap method. Supports outlier detection and robust estimation.
}
\details{
This function tests H0: the two groups have equal regression slopes using
an OLS-based bootstrap test. The test is performed by comparing the interaction
term in a combined regression model. Outliers in the predictor can optionally
be removed before analysis.
}
\examples{
\dontrun{
# Generate data with different slopes
set.seed(123)
x1 <- rnorm(30)
y1 <- 2 * x1 + rnorm(30)
x2 <- rnorm(30)
y2 <- 0.5 * x2 + rnorm(30)

# Test for equal slopes
tworegwb(x1, y1, x2, y2, nboot = 500)
}
}
\seealso{
\code{\link{olswbtest}}, \code{\link{reg2ci}}
}
