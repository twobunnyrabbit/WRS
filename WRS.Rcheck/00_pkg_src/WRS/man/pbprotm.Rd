% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{pbprotm}
\alias{pbprotm}
\title{Percentile Bootstrap Test for Linear Contrasts Using Projection-Based Depth}
\usage{
pbprotm(
  x,
  est = tmean,
  con = 0,
  alpha = 0.05,
  nboot = 2000,
  grp = NA,
  op = 3,
  allp = TRUE,
  MM = FALSE,
  MC = FALSE,
  cop = 3,
  SEED = TRUE,
  na.rm = FALSE,
  ...
)
}
\arguments{
\item{x}{Data in list mode, matrix, or data frame. If list, \code{x[[j]]} contains
data for group j. If matrix/data frame, columns correspond to groups.}

\item{est}{Measure of location. Default is \code{\link{tmean}} (20\% trimmed mean).}

\item{con}{Contrast matrix with J rows (number of groups) and C columns (number of contrasts).
Default is 0 (all pairwise comparisons). Each column specifies one contrast.}

\item{alpha}{Significance level. Default is 0.05.}

\item{nboot}{Number of bootstrap samples. Default is 2000.}

\item{grp}{Optional vector to select/reorder specific groups. Default is \code{NA} (use all groups).}

\item{op}{Method for measuring depth:
\itemize{
\item \code{1}: Mahalanobis distance
\item \code{2}: Mahalanobis distance using MCD covariance matrix
\item \code{3} (default): Projection distance
}}

\item{allp}{Logical. If \code{TRUE} (default), test all pairwise comparisons when \code{con=0}.}

\item{MM}{Logical. If \code{TRUE}, use marginal medians in projection distance. Default is \code{FALSE}.}

\item{MC}{Logical. If \code{TRUE}, use multicore processing when \code{op=3}. Default is \code{FALSE}.}

\item{cop}{Additional control parameter for \code{\link{pdis}}. Default is 3.}

\item{SEED}{Logical. If \code{TRUE}, set random seed for reproducibility. Default is \code{TRUE}.}

\item{na.rm}{Logical. If \code{TRUE}, remove missing values. Default is \code{FALSE}.}

\item{...}{Additional arguments passed to the location estimator.}
}
\value{
List with components:
\item{test}{Test statistic and p-value}
\item{con}{Contrast matrix used}
\item{psihat}{Estimated contrast values}
(Additional components depend on bootstrap results)
}
\description{
Tests the hypothesis that C linear contrasts all equal zero using
percentile bootstrap and projection-based depth. A robust multivariate
approach for comparing multiple independent groups.
}
\details{
The function tests whether the specified linear contrasts equal zero.
For example:
\itemize{
\item \code{con[,1] = c(1,1,-1,-1,0,0)}: Tests if sum of first two locations
equals sum of next two
\item \code{con[,2] = c(1,-1,0,0,1,-1)}: Tests if difference between groups 1-2
equals difference between groups 5-6
}

Uses projection-based depth by default (\code{op=3}) with percentile bootstrap
to account for non-normality and heteroscedasticity.
}
\seealso{
\code{\link{pbadepth}}, \code{\link{lincon}}, \code{\link{pdis}}
}
\keyword{htest}
\keyword{multivariate}
\keyword{robust}
