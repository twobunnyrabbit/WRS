% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{Dancts}
\alias{Dancts}
\title{Dependent Groups ANCOVA Using Theil-Sen Regression}
\usage{
Dancts(
  x1,
  y1,
  x2,
  y2,
  pts = NULL,
  regfun = tsreg,
  fr1 = 1,
  fr2 = 1,
  alpha = 0.05,
  plotit = TRUE,
  xout = FALSE,
  outfun = out,
  nboot = 100,
  SEED = TRUE,
  xlab = "X",
  ylab = "Y",
  pr = TRUE,
  ...
)
}
\arguments{
\item{pts}{Design points for comparison. If NULL, automatically selected. Default is NULL.}

\item{regfun}{Regression function. Default is tsreg (modified Theil-Sen).
Can also use tshdreg for tied values.}

\item{alpha}{Significance level. Default is 0.05.}

\item{plotit}{Logical. Create comparison plot. Default is TRUE.}

\item{xout}{Logical. Remove outliers. Default is FALSE.}

\item{outfun}{Outlier detection function. Default is out.}

\item{nboot}{Number of bootstrap samples. Default is 100.}

\item{SEED}{Logical. Set random seed. Default is TRUE.}

\item{xlab, ylab}{Axis labels for plot.}

\item{pr}{Logical. Print warning messages. Default is TRUE.}

\item{...}{Additional arguments passed to regfun.}
}
\value{
List with test results and design points.
}
\description{
Compare regression lines of two dependent groups using robust regression (Theil-Sen by default).
A robust Johnson-Neyman method for dependent groups.
}
\details{
For OLS regression, use Dancols instead. This function uses robust regression
to compare dependent groups at specified design points. The default Theil-Sen
estimator is resistant to outliers. For data with tied values, consider regfun=tshdreg.
}
\examples{
\dontrun{
  x <- rnorm(30)
  y1 <- 2*x + rnorm(30)
  y2 <- 2*x + 0.5 + rnorm(30)  # Parallel shift
  result <- Dancts(x, y1, x, y2)
}
}
\seealso{
\code{\link{Dancols}}, \code{\link{tsreg}}, \code{\link{tshdreg}}
}
\keyword{ancova}
\keyword{dependent}
\keyword{regression}
\keyword{robust}
