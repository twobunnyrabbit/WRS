% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{Rpca}
\alias{Rpca}
\title{Robust Principal Component Analysis}
\usage{
Rpca(
  x,
  p = ncol(x) - 1,
  locfun = llocv2,
  loc.val = NULL,
  iter = 100,
  SCORES = FALSE,
  gvar.fun = cov.mba,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{x}{Matrix or data frame of multivariate data (n x m).}

\item{p}{Integer. Number of principal components to extract (default: ncol(x)-1).}

\item{locfun}{Function for computing robust center. Default is \code{\link{llocv2}}
(marginal medians). Alternatives include \code{\link{mcd}}, \code{\link{tauloc}},
\code{\link{spat}}, etc.}

\item{loc.val}{Optional pre-computed location vector. If \code{NULL}, computed
using \code{locfun}.}

\item{iter}{Integer. Number of random orthogonal matrices to try (default: 100).
The matrix yielding minimum robust generalized variance is selected.}

\item{SCORES}{Logical. If \code{TRUE}, compute and return principal component
scores. Default is \code{FALSE}.}

\item{gvar.fun}{Function for computing generalized variance. Default is
\code{\link{cov.mba}} (projection-based covariance).}

\item{SEED}{Logical. If \code{TRUE} (default), set random seed to 2 for
reproducibility.}

\item{...}{Additional arguments passed to \code{locfun}.}
}
\value{
List with components:
\item{B}{Matrix (p x m) of principal component loadings (rows are components)}
\item{gen.var}{Minimum generalized variance achieved}
\item{scores}{If \code{SCORES=TRUE}, matrix (n x p) of principal component scores;
otherwise \code{NULL}}
}
\description{
Performs robust principal component analysis using random orthogonal matrices
and a robust generalized variance method to find the optimal principal component
subspace.
}
\details{
This function provides a robust alternative to classical PCA that is resistant
to outliers. The algorithm:
\enumerate{
\item Centers data using robust location estimator
\item Generates \code{iter} random orthogonal projection matrices
\item Projects data onto each p-dimensional subspace
\item Computes robust generalized variance for each projection
\item Selects projection with minimum generalized variance (most concentrated)
}

The optimal projection matrix defines the robust principal component subspace.
}
\seealso{
\code{\link{llocv2}}, \code{\link{cov.mba}}, \code{\link{gvarg}},
\code{\link{ortho}}
}
\keyword{multivariate}
\keyword{robust}
