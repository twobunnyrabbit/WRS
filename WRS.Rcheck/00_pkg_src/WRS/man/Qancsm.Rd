% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{Qancsm}
\alias{Qancsm}
\title{ANCOVA Using Depths of Quantile Regression Smooths}
\usage{
Qancsm(
  x1,
  y1,
  x2,
  y2,
  crit.mat = NULL,
  nboot = 200,
  SEED = TRUE,
  REP.CRIT = FALSE,
  qval = 0.5,
  q = NULL,
  xlab = "X",
  ylab = "Y",
  plotit = TRUE,
  pr = TRUE,
  xout = FALSE,
  outfun = out,
  ...
)
}
\arguments{
\item{x1}{Covariate values for group 1}

\item{y1}{Response values for group 1}

\item{x2}{Covariate values for group 2}

\item{y2}{Response values for group 2}

\item{crit.mat}{Matrix of critical values from previous bootstrap (optional)}

\item{nboot}{Number of bootstrap samples (default: 200)}

\item{SEED}{Logical; if TRUE, set random seed (default: TRUE)}

\item{REP.CRIT}{Logical; if TRUE, return critical value matrix (default: FALSE)}

\item{qval}{Quantile to use for regression smooth (default: 0.5 for median)}

\item{q}{Alternative way to specify quantile (overrides qval if not NULL)}

\item{xlab}{Label for x-axis (default: "X")}

\item{ylab}{Label for y-axis (default: "Y")}

\item{plotit}{Logical; if TRUE, create plot (default: TRUE)}

\item{pr}{Logical; if TRUE, print progress messages (default: TRUE)}

\item{xout}{Logical; if TRUE, remove outliers (default: FALSE)}

\item{outfun}{Function to detect outliers (default: out)}

\item{...}{Additional arguments}
}
\value{
List with p.value, crit.mat (if REP.CRIT=TRUE), and test.depth
}
\description{
Compare two nonparametric regression lines for independent groups using the
depths of quantile regression smooths. Tests the null hypothesis that regression
lines are identical in terms of the median (or other quantile) of Y given X for
all X. Only one covariate is allowed.
}
\keyword{internal}
