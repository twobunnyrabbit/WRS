% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{acbinomciv2}
\alias{acbinomciv2}
\title{Agresti-Coull Binomial Test with P-Value}
\usage{
acbinomciv2(
  x = sum(y),
  nn = length(y),
  y = NULL,
  n = NA,
  alpha = 0.05,
  nullval = 0.5
)
}
\arguments{
\item{x}{Number of successes. Default uses \code{sum(y)}}

\item{nn}{Sample size. Default uses \code{length(y)}}

\item{y}{Optional vector of 1s (successes) and 0s (failures). If provided, \code{x}
and \code{nn} are computed from \code{y}}

\item{n}{Deprecated. Use \code{nn} instead}

\item{alpha}{Type I error rate (default: 0.05 for 95\\% confidence interval)}

\item{nullval}{Null hypothesis value for probability of success (default: 0.5)}
}
\value{
List with components:
\describe{
\item{\code{n}}{Sample size used}
\item{\code{phat}}{Estimated probability of success (x/n)}
\item{\code{se}}{Standard error of the estimate}
\item{\code{ci}}{Two-element vector containing confidence limits at level \code{alpha}}
\item{\code{p.value}}{Two-sided p-value for test of H0: p = nullval}
}
}
\description{
Tests the hypothesis that the probability of success equals a specified null value
using the Agresti-Coull method. Computes both confidence interval and p-value by
inverting the confidence interval.
}
\details{
The p-value is computed by inverting the Agresti-Coull confidence interval:
finds the smallest alpha level at which the CI excludes the null value.
This provides a more accurate p-value than the standard Wald test, especially
for extreme values of p or small samples.

The algorithm searches alpha values:
\itemize{
\item Initially: 0.01 to 0.99 by 0.01
\item If p < 0.1: refines to 0.001 increments
\item If p < 0.001: refines to 0.0001 increments
}
}
\seealso{
\code{\link{acbinomci}}, \code{\link{binomcipv}}
}
\keyword{htest}
