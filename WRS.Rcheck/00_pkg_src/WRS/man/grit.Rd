% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{grit}
\alias{grit}
\title{Test Variable Contribution Using Running Interval Smoother}
\usage{
grit(
  x,
  y,
  itest = 1,
  sm.fun = rplot,
  nboot = 500,
  alpha = 0.05,
  SEED = TRUE,
  fr = 1,
  plot.fun = rplot,
  plotit = TRUE,
  ...
)
}
\arguments{
\item{x}{Matrix of predictor variables (must have 2 or more columns).}

\item{y}{Numeric vector. Response variable.}

\item{itest}{Integer. Index of the predictor variable to test (default: 1).
Tests hypothesis that variable \code{itest} does not contribute to the model.}

\item{sm.fun}{Function. Smoother function to use (default: \code{\link{rplot}}).
Alternative: \code{\link{lplot}}; for p > 2: \code{\link{runpd}}.}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{fr}{Numeric. Span parameter for the smoother (default: 1).}

\item{plot.fun}{Function. Plotting function to use (default: \code{\link{rplot}}).}

\item{plotit}{Logical. If \code{TRUE} (default), creates a plot of the tested
variable vs. residuals.}

\item{...}{Additional arguments passed to \code{plot.fun}.}
}
\value{
Test result object from \code{\link{indt}}, typically containing:
\itemize{
\item \code{test}: Test statistic value
\item \code{p.value}: P-value for the independence test
\item Additional test-specific information
}
}
\description{
Tests whether a predictor variable contributes to a regression model by
fitting a running interval smoother on all other predictors and examining
the association between the tested variable and residuals.
}
\details{
The GRIT (Generalized Regression with Interaction Test) procedure:

\enumerate{
\item Fits a running interval smoother using all predictors \emph{except} variable \code{itest}
\item Computes residuals from this fit
\item Tests for independence between variable \code{itest} and the residuals
using \code{\link{indt}}
\item If \code{itest} is independent of residuals, it doesn't contribute
beyond the other predictors
}

This approach can detect:
\itemize{
\item Whether a variable adds predictive value
\item Non-linear relationships
\item Interaction effects
}

The test uses projection distances and bootstrap methods for robustness.
}
\examples{
\dontrun{
# Test whether first predictor contributes to model
x <- matrix(rnorm(300), 100, 3)
y <- x[,2] + x[,3] + rnorm(100)  # y depends on x2 and x3, not x1
result <- grit(x, y, itest=1)  # Should show x1 doesn't contribute
}

}
\seealso{
\code{\link{rplot}}, \code{\link{lplot}}, \code{\link{runpd}},
\code{\link{indt}}
}
\keyword{regression}
\keyword{robust}
