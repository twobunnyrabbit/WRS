% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{Flplot}
\alias{Flplot}
\title{Plot Average of Multiple Curves}
\usage{
Flplot(x, est = mean, xlab = "Time", ylab = "Y", plotit = TRUE)
}
\arguments{
\item{x}{Matrix (n × p) where rows are curves and columns are time points.}

\item{est}{Function to compute measure of location at each time point (default: mean).
Can be any function like median, tmean, etc.}

\item{xlab}{Label for X-axis (default: "Time").}

\item{ylab}{Label for Y-axis (default: "Y").}

\item{plotit}{Logical. Create plot if TRUE (default: TRUE).}
}
\value{
Vector of estimated values at each time point.
}
\description{
Computes and plots the pointwise average (or other measure of central tendency)
of multiple functional curves evaluated at the same time points.
}
\details{
This function computes a summary curve from multiple functional observations by
applying a location estimator (default: mean) at each time point. Useful for
visualizing the average trend across multiple curves.

\strong{Procedure}:
\enumerate{
\item At each time point (column), compute the specified estimator across all curves (rows)
\item Plot the resulting summary curve
}

\strong{Common estimators}:
\itemize{
\item \strong{mean}: Arithmetic mean (sensitive to outliers)
\item \strong{median}: Median (robust to outliers)
\item \strong{tmean}: Trimmed mean (robust, compromise between mean and median)
\item \strong{onestep}: One-step M-estimator
}
}
\examples{
# Average of multiple curves
set.seed(123)
t <- seq(0, 1, length = 50)
# Create 20 noisy sine curves
x <- matrix(0, nrow = 20, ncol = 50)
for(i in 1:20) {
  x[i,] <- sin(2*pi*t) + rnorm(50, 0, 0.2)
}
Flplot(x)

# Use median instead of mean
Flplot(x, est = median)
}
\seealso{
\code{\link{FQplot}}, \code{\link{Flplot2g}}, \code{\link{fbplot}}
}
