% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R, R/special.R
\name{linplot}
\alias{linplot}
\title{Plot Distribution of Linear Contrast}
\usage{
linplot(x, con, nreps = 200, SEED = TRUE, xlab = "DV", ylab = "")

linplot(x, con, nreps = 200, SEED = TRUE, xlab = "DV", ylab = "")
}
\arguments{
\item{x}{Data in matrix or list format. For matrix: each column is a group.
For list: x[\link{j}] contains data for group j.}

\item{con}{Numeric vector of contrast coefficients (length J). Must sum to zero.}

\item{nreps}{Number of resampling iterations to estimate the distribution (default: 200).}

\item{SEED}{Logical. If TRUE (default), sets random seed to 2 for reproducibility.}

\item{xlab}{X-axis label (default: 'DV').}

\item{ylab}{Y-axis label (default: empty string).}

\item{plotfun}{Density estimation function for plotting (default: akerd, adaptive kernel estimator).}

\item{nboot}{Number of bootstrap samples (default: 800).}

\item{plotit}{Logical. Create plot if TRUE (default: TRUE).}

\item{pyhat}{Logical. Passed to plotfun for additional output (default: FALSE).}

\item{...}{Additional arguments passed to plotfun.}
}
\value{
Output from the density estimation function specified by plotfun.

None (creates a plot).
}
\description{
Plots the bootstrap distribution of a linear contrast c1\emph{X1 + c2}X2 + ... + cJ*XJ
from multiple groups, useful for visualizing the distribution of linear combinations
of group estimates.

Estimates and plots the distribution of a linear contrast Y = sum(c_j * X_j)
using adaptive kernel density estimation.
}
\details{
This function visualizes the distribution of a linear contrast across multiple groups
by bootstrap resampling. It's useful for examining the sampling distribution of
linear combinations such as:
\itemize{
\item Sum of group means: c = (1, 1, ..., 1)
\item Difference: c = (1, -1)
\item Custom contrasts: any vector of coefficients
}

\strong{Procedure}:
\enumerate{
\item For each of nboot iterations, sample with replacement from each group
\item Compute the linear contrast for each bootstrap sample
\item Plot the resulting distribution using the specified plotfun
}

Missing values are automatically removed from each group.

This function:
\itemize{
\item Estimates the distribution of the linear contrast via resampling
\item For each iteration, randomly samples one value from each group
\item Computes the weighted sum using contrast coefficients
\item Plots the resulting distribution using \code{\link{akerd}}
}
}
\examples{
# Plot distribution of sum of three groups
set.seed(123)
x1 <- rnorm(30, mean = 0)
x2 <- rnorm(30, mean = 1)
x3 <- rnorm(30, mean = 2)
x <- list(x1, x2, x3)
linplot(x)  # Default: sum of all groups

# Plot distribution of difference (Group 1 - Group 2)
linplot(list(x1, x2), con = c(1, -1))
# Plot distribution of difference between two groups
set.seed(123)
x <- list(rnorm(50), rnorm(50, mean = 0.5))
linplot(x, con = c(1, -1), nreps = 500)
}
\seealso{
\code{\link{lin2plot}}, \code{\link{akerd}}, \code{\link{lincon}}

\code{\link{linQS}} (quantile effect size), \code{\link{linEP}} (explanatory power),
\code{\link{akerd}} (plotting function)
}
