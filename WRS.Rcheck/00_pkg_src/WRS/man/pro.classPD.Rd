% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{pro.classPD}
\alias{pro.classPD}
\title{Projection-Based Classification Using Probability Density Functions}
\usage{
pro.classPD(
  train = NULL,
  test = NULL,
  g = NULL,
  x1 = NULL,
  x2 = NULL,
  nonpar = TRUE,
  rule = 1,
  SEED = NULL
)
}
\arguments{
\item{train}{Numeric matrix of training data, or \code{NULL} if \code{x1} and \code{x2} provided.}

\item{test}{Numeric matrix of test data to classify.}

\item{g}{Numeric vector of group labels (0 or 1) for training data.}

\item{x1}{Numeric matrix for group 1 training data (alternative to \code{train} + \code{g}).}

\item{x2}{Numeric matrix for group 2 training data (alternative to \code{train} + \code{g}).}

\item{nonpar}{Logical. If \code{TRUE}, use smoother; if \code{FALSE}, use logistic regression
(default: \code{TRUE}).}

\item{rule}{Numeric. Classification threshold for likelihood ratio (default: 1).
Classify as group 1 if f_1/f_2 > rule, where f_1 and f_2 are estimated PDFs.}

\item{SEED}{Numeric or \code{NULL}. Random seed value (default: \code{NULL}).}
}
\value{
Numeric vector of classifications (1 or 2) for test observations.
}
\description{
Projects multivariate data onto a line between group centers, then classifies
based on estimated likelihood ratio of probability density functions (PDFs).
}
\details{
Projects data onto a line between group centers using \code{\link{mulwmw.dist.new}}.
For each test observation, estimates PDFs for both groups at the projected location
using adaptive kernel density estimation (\code{\link{akerd}}). Classifies as
group 1 if the likelihood ratio f_1/f_2 exceeds \code{rule}.

\strong{Example}: With \code{rule=0.5}, classify as group 1 if f_1/f_2 > 0.5.
}
\examples{
\dontrun{
# Classify using default rule (1)
result <- pro.classPD(x1 = group1, x2 = group2, test = test_data)

# More lenient classification threshold
result2 <- pro.classPD(x1 = group1, x2 = group2, test = test_data, rule = 0.5)
}

}
\seealso{
\code{\link{pro.class}}, \code{\link{pro.class.probs}}, \code{\link{akerd}},
\code{\link{mulwmw.dist.new}}
}
\keyword{classif}
\keyword{multivariate}
