% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression-advanced.R
\name{reghet.blp.ci}
\alias{reghet.blp.ci}
\title{Confidence Intervals After Removing Bad Leverage Points (Heteroscedastic)}
\usage{
reghet.blp.ci(
  x,
  y,
  regfun = tsreg,
  nboot = 999,
  HH = TRUE,
  SEED = TRUE,
  BCA = FALSE,
  pr = TRUE,
  ...
)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{regfun}{Regression function to use after removing outliers (default:
\code{tsreg} for Theil-Sen regression).}

\item{nboot}{Number of bootstrap samples (default: 999).}

\item{HH}{Logical; if TRUE, use the HH (Hung-Huggins) method for detecting
bad leverage points; if FALSE, use \code{regcon.out()} (default: TRUE).}

\item{BCA}{Logical; if TRUE, use bias-corrected and accelerated (BCa)
bootstrap; if FALSE, use percentile bootstrap (default: FALSE).}

\item{pr}{Logical; if TRUE, print informational messages (default: TRUE).}

\item{...}{Additional arguments passed to \code{regfun}.}
}
\value{
If \code{BCA=FALSE}: Result from \code{regci()} containing confidence intervals for
both intercept and slope.

If \code{BCA=TRUE}: Result from \code{reg.bca()} containing confidence interval for
slope only (intercept not provided with BCa method).
}
\description{
Computes bootstrap confidence intervals for regression parameters after
identifying and removing bad leverage points using a heteroscedastic-robust
method. Provides inference that accounts for both heteroscedasticity and
outlying observations.
}
\details{
This function provides robust inference by:
\enumerate{
\item Detecting bad leverage points using heteroscedastic-robust method
\item Removing identified bad leverage points
\item Computing bootstrap confidence intervals on cleaned data
}

\strong{Bootstrap methods:}
\itemize{
\item Percentile bootstrap (BCA=FALSE): Simpler, provides CIs for all parameters
\item BCa bootstrap (BCA=TRUE): More accurate for small samples, slope only
}

\strong{When to use BCa:}
\itemize{
\item Small sample sizes (n < 50) - more accurate coverage
\item Skewed sampling distributions
\item Only need slope inference (intercept not returned)
}

The function will print a warning when n < 50 and BCA=FALSE, suggesting
BCA=TRUE may be safer.
}
\note{
Currently limited to single independent variable (p=1).
}
\examples{
\dontrun{
# Bootstrap CI with heteroscedastic bad leverage point removal
set.seed(123)
x <- seq(0, 10, length.out=100)
y <- 2*x + rnorm(100, sd=0.5*x)
x[50] <- 12; y[50] <- -5  # Bad leverage point

# Percentile bootstrap (both parameters)
ci.perc <- reghet.blp.ci(x, y, nboot=500)

# BCa bootstrap (slope only, better for small n)
ci.bca <- reghet.blp.ci(x, y, BCA=TRUE, nboot=500)
}
}
\references{
Wilcox, R.R. (2022). Introduction to Robust Estimation and Hypothesis Testing
(5th ed.). Academic Press.

Efron, B., & Tibshirani, R.J. (1993). An Introduction to the Bootstrap.
Chapman & Hall/CRC.
}
\seealso{
\code{\link{reghet.blp}}, \code{\link{regci}}, \code{\link{reg.bca}}
}
