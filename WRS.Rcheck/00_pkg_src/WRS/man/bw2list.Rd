% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{bw2list}
\alias{bw2list}
\title{Convert Between-Within Design Data to List Format}
\usage{
bw2list(
  x,
  between.col,
  within.col,
  grp.col = between.col,
  lev.col = within.col,
  pr = TRUE
)
}
\arguments{
\item{x}{Data frame or matrix containing the data.}

\item{between.col}{Column number indicating levels of the between-subjects factor.}

\item{within.col}{Vector of column numbers containing the repeated measures data.}

\item{grp.col}{Column indicating group levels (default: same as \code{between.col}).}

\item{lev.col}{Columns where repeated measures are stored (default: same as \code{within.col}).}

\item{pr}{Logical; if \code{TRUE} (default), prints the levels of the between-subjects factor.}
}
\value{
A list where each element contains data for one group/condition combination,
organized for use with ANOVA functions.
}
\description{
Converts data from a between-by-within (split-plot) design stored in a data
frame or matrix into list format suitable for use with \code{\link{rmanova}}
and related functions.
}
\details{
This function uses \code{\link{selbybw}} to reorganize the data into list mode,
where each element of the list contains data for one combination of between
and within factor levels.
}
\examples{
\dontrun{
# For a 3x2 design where:
# - Column 2 contains between-subject group information
# - Columns 3 and 7 contain time 1 and time 2 data
data_list <- bw2list(mydata, 2, c(3,7))
}

}
\seealso{
\code{\link{selbybw}}, \code{\link{rmanova}}, \code{\link{bwimcp}}
}
\keyword{manip}
