% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{correg}
\alias{correg}
\title{Correlation-Based Regression}
\usage{
correg(x, y, corfun = tau, loc.fun = median)
}
\arguments{
\item{x}{Numeric vector or matrix. For bivariate functions, the first variable.
For matrix functions, an n-by-p data matrix.}

\item{y}{Numeric vector. The second variable for bivariate correlations.
Can be \code{NULL} for some functions that accept a two-column matrix as \code{x}.}

\item{corfun}{Correlation function to use. Common options: \code{pbcor} (percentage
bend), \code{pcor} (Pearson), \code{wincor} (winsorized), \code{spear} (Spearman).}

\item{loc.fun}{Location estimator for computing intercept (default: \code{median}).}
}
\value{
List with components:
\item{coef}{Regression coefficients (intercept and slopes).}
\item{residuals}{Regression residuals.}
}
\description{
A generalization of the Theil-Sen estimator that minimizes correlations
between predictors and residuals.
}
\details{
This function generalizes the Theil-Sen estimator by allowing alternative
correlation measures via the \code{corfun} argument. The regression coefficients
are chosen to minimize the sum of absolute correlations between predictors
and residuals.

The Nelder-Mead optimization method is used. The intercept is computed using
\code{loc.fun} (default: median) applied to the centered residuals.
}
\examples{
# Robust regression using correlation criterion
set.seed(123)
x <- matrix(rnorm(50 * 2), ncol = 2)
y <- 2 + 3 * x[,1] - 2 * x[,2] + rnorm(50)
correg(x, y)
}
\seealso{
\code{\link{scorreg}}, \code{\link{tsreg}}
}
