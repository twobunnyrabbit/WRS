% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outliers.R
\name{SVMbag}
\alias{SVMbag}
\title{Bagged Support Vector Machine Classification}
\usage{
SVMbag(
  train = NULL,
  test = NULL,
  g = NULL,
  x1 = NULL,
  x2 = NULL,
  depthfun = prodepth,
  kernel = "radial",
  nboot = 100,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{train}{Training data matrix (optional if \code{x1} and \code{x2} provided).}

\item{test}{Test data matrix to classify.}

\item{x1, x2}{Training data for groups 1 and 2 (alternative to \code{train}/\code{g}).}

\item{depthfun}{Depth function to use (default: \code{prodepth}).}

\item{kernel}{Kernel type for SVM (default: 'radial').}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{...}{Additional arguments.}
}
\value{
Vector of predicted class labels (1 or 2) for test observations.
}
\description{
Bootstrap aggregating version of Support Vector Machine (SVM) classification.
Addresses misclassification bias when group sizes differ substantially.
}
\details{
Performs SVM classification via \code{SVM()} with bootstrap bagging. Unlike standard
SVM, this function addresses a critical bias issue: when n1 != n2 and n2/n1 is
small, standard SVM has a misclassification probability of approximately
n2/(n1+n2) when there is no true association. This bagged version maintains
a probability of 0.5 by using balanced bootstrap samples (min(n1,n2)).
}
\seealso{
\code{\link{SVM}}, \code{\link{Depth.class.bag}}
}
