% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{rmanog}
\alias{rmanog}
\title{Test that All Pairwise Differences Equal Zero (Repeated Measures)}
\usage{
rmanog(x, alpha = 0.05, est = onestep, grp = NA, nboot = NA, ...)
}
\arguments{
\item{x}{Data in matrix format (rows = subjects, columns = groups) or list mode.}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{est}{Location estimator function (default: \code{\link{onestep}}).}

\item{grp}{Optional vector to specify subset of groups for analysis.}

\item{nboot}{Number of bootstrap samples. If \code{NA}, chosen based on number
of groups (1000 for ≤4 groups, 5000 otherwise).}

\item{...}{Additional arguments passed to \code{est}.}
}
\value{
A list with component \code{output}: a matrix with columns:
\describe{
\item{\code{con.num}}{Contrast number (difference j vs j+1)}
\item{\code{psihat}}{Estimated location of difference}
\item{\code{sig}}{P-value}
\item{\code{crit.sig}}{Critical significance level (Rom's method)}
\item{\code{ci.lower}}{Lower CI bound}
\item{\code{ci.upper}}{Upper CI bound}
}
}
\description{
Tests the hypothesis that all successive pairwise differences among J
dependent groups have measure of location equal to zero using percentile
bootstrap.
}
\details{
For J groups, this function computes J-1 successive differences:
D\link{i,j} = X\link{i,j} - X\link{i,j+1} for j = 1, ..., J-1

Tests the omnibus hypothesis:
H0: location(D\link{,1}) = location(D\link{,2}) = ... = location(D\link{,J-1}) = 0

Uses Rom's sequentially rejective method to control family-wise error rate.
Bootstrap resampling provides p-values for each difference.

Rows with missing values are removed.
}
\seealso{
\code{\link{rmaseq}}, \code{\link{rmmcp}}, \code{\link{onestep}}
}
\keyword{htest}
\keyword{robust}
