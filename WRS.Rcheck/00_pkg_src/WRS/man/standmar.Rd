% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{standmar}
\alias{standmar}
\title{Standardize Matrix Columns}
\usage{
standmar(x, locfun = lloc, est = mean, scat = var, ...)
}
\arguments{
\item{x}{Numeric matrix.}

\item{locfun}{Function for location estimation (currently not used; \code{est} is used instead).}

\item{est}{Function for computing column-wise location. Default is \code{\link{mean}}.}

\item{scat}{Function for computing column-wise scale (variance). Default is \code{\link{var}}.}

\item{...}{Additional arguments (not currently used).}
}
\value{
Standardized matrix with same dimensions as \code{x}.
}
\description{
Standardizes each column of a matrix by subtracting a location estimate and
dividing by the square root of a scale estimate.
}
\details{
For each column j of matrix \code{x}, computes:
(x\link{,j} - est(x\link{,j})) / sqrt(scat(x\link{,j}))

This standardizes columns to have the specified location and scale. With default
settings (mean and variance), produces z-scores with mean 0 and standard deviation 1.

For robust standardization, use robust location and scale estimators:
\itemize{
\item Location: \code{est=median}, \code{est=tmean}, etc.
\item Scale: \code{scat=mad}, \code{scat=winvar}, etc.
}

\strong{Note}: The \code{locfun} parameter is not currently used in the implementation.
}
\seealso{
\code{\link{scale}}
}
\keyword{array}
