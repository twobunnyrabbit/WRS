% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{yuenQS}
\alias{yuenQS}
\title{Yuen's Test with Quantile Shift Effect Size}
\usage{
yuenQS(
  x,
  y = NULL,
  tr = 0.2,
  alpha = 0.05,
  plotit = FALSE,
  op = TRUE,
  cor.op = FALSE,
  loc.fun = median,
  pr = TRUE,
  xlab = "X",
  ylab = " "
)
}
\arguments{
\item{x}{Vector of data for group 1, or a matrix/data frame with two columns,
or a list with two elements.}

\item{y}{Vector of data for group 2. Not needed if \code{x} is a matrix/data frame/list.}

\item{tr}{Proportion of trimming (default: 0.2 for 20\% trimming from each tail).}

\item{alpha}{Significance level for confidence interval (default: 0.05).}

\item{plotit}{Logical. If \code{TRUE}, creates a plot comparing the two groups (default: \code{FALSE}).}

\item{op}{Logical. If \code{TRUE}, includes output (default: \code{TRUE}).}

\item{cor.op}{Logical. For correlation-related operations (default: \code{FALSE}).}

\item{loc.fun}{Function for computing location measure (default: \code{median}).}

\item{pr}{Logical. If \code{TRUE}, prints interpretation guidance (default: \code{TRUE}).}

\item{xlab}{Label for x-axis when plotting (default: "X").}

\item{ylab}{Label for y-axis when plotting (default: " ").}
}
\value{
A list with components:
\itemize{
\item \code{ci}: Confidence interval for the difference between trimmed means
\item \code{n1}, \code{n2}: Sample sizes for groups 1 and 2
\item \code{p.value}: P-value for Yuen's test
\item \code{dif}: Difference between trimmed means (group 1 - group 2)
\item \code{se}: Standard error of the difference
\item \code{teststat}: Test statistic value
\item \code{crit}: Critical value from t-distribution
\item \code{df}: Degrees of freedom
\item \code{Q.Effect}: Quantile shift effect size measure
}
}
\description{
Performs Yuen's test for comparing trimmed means between two independent
groups, with an additional robust quantile shift (QS) measure of effect size.
}
\details{
This function extends the basic Yuen test (see \code{yuen}) by adding a robust
quantile shift (QS) effect size measure. The quantile shift provides an
alternative to Cohen's d that is more robust to outliers and non-normality.

The function prints an interpretation guide relating Cohen's d values to
approximate Q.Effect values:
\itemize{
\item Cohen's d = 0 corresponds to Q.Effect ≈ 0.5 (no effect)
\item Cohen's d = 0.2 corresponds to Q.Effect ≈ 0.55 (small effect)
\item Cohen's d = 0.5 corresponds to Q.Effect ≈ 0.65 (medium effect)
\item Cohen's d = 0.8 corresponds to Q.Effect ≈ 0.70 (large effect)
}

These correspondences assume normality and homoscedasticity. The QS effect
size is often more interpretable and robust than standardized mean differences.
}
\note{
Use \code{medpb} to compare medians (when \code{tr = 0.5}). Values of \code{tr > 0.5} are
not allowed. Missing values are automatically removed.
}
\examples{
\dontrun{
set.seed(123)
x <- rnorm(40, mean = 0, sd = 1)
y <- rnorm(50, mean = 0.5, sd = 1.2)

# Yuen's test with quantile shift effect size
result <- yuenQS(x, y)
print(result)

# With plot
yuenQS(x, y, plotit = TRUE)

# Less trimming
yuenQS(x, y, tr = 0.1)

# Using matrix input
data <- cbind(x[1:40], y[1:40])
yuenQS(data)
}
}
\seealso{
\code{\link{yuen}} for basic Yuen test,
\code{\link{yuend}} for dependent groups,
\code{\link{yuend.QS.ci}} for bootstrap CI with QS effect size,
\code{\link{shiftQS}} for computing quantile shift
}
