% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_robpca}
\alias{plot_robpca}
\title{Diagnostic Plot for Robust PCA}
\usage{
plot_robpca(robpca.obj, classic = 0, labod = 3, labsd = 3)
}
\arguments{
\item{robpca.obj}{Object returned by a robust PCA function (e.g., from
the \code{rrcov} or \code{robustbase} package).}

\item{classic}{Numeric; if 1, also plots classical PCA diagnostics for
comparison (default: 0).}

\item{labod}{Number of observations with largest orthogonal distances to
label (default: 3).}

\item{labsd}{Number of observations with largest score distances to label
(default: 3).}
}
\value{
Invisibly returns the input \code{robpca.obj}.
}
\description{
Creates diagnostic plots for robust principal component analysis (ROBPCA),
showing score distances and orthogonal distances to identify outliers.
Optionally includes classical PCA diagnostics for comparison.
}
\details{
The function creates diagnostic plots showing:
\itemize{
\item \strong{Score Distance (SD)}: Distance from the robust PCA center in the
principal component space
\item \strong{Orthogonal Distance (OD)}: Distance from the PCA subspace
}

\strong{Two plot types}:
\enumerate{
\item If OD values vary, creates a 2D plot (SD vs OD) with cutoff lines
\item If OD values are constant/tiny, creates an index plot of SD only
}

Outliers appear as points beyond the cutoff lines. The most extreme
observations are labeled.

If \code{classic = 1}, creates corresponding plots for classical (non-robust)
PCA titled "CPCA".
}
\examples{
\dontrun{
# Requires rrcov package
library(rrcov)
data(hbk)
res <- PcaHubert(hbk)
plot_robpca(res, classic=1)
}
}
\references{
Hubert, M., Rousseeuw, P.J., and Vanden Branden, K. (2005). ROBPCA: A new
approach to robust principal component analysis. \emph{Technometrics}, 47, 64-79.
}
\seealso{
Functions in \code{rrcov} or \code{robustbase} packages for computing ROBPCA
}
