% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{anctsmp}
\alias{anctsmp}
\title{Theil-Sen ANCOVA for Multiple Covariates}
\usage{
anctsmp(
  x1,
  y1,
  x2,
  y2,
  regfun = tsreg,
  alpha = 0.05,
  pts = NULL,
  SEED = TRUE,
  nboot = 100,
  xout = FALSE,
  outfun = out,
  ...
)
}
\arguments{
\item{x1}{A matrix containing covariate values for group 1 (one covariate per column).}

\item{y1}{A numeric vector of outcome values for group 1.}

\item{x2}{A matrix containing covariate values for group 2 (one covariate per column).}

\item{y2}{A numeric vector of outcome values for group 2.}

\item{regfun}{Regression function to use (default: \code{tsreg} for Theil-Sen).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{pts}{A matrix specifying design points (covariate combinations) where
regression surfaces are compared. If \code{NULL}, points are chosen based on depth
using \code{\link{ancdes}}.}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{nboot}{Number of bootstrap samples (default varies by function).}

\item{xout}{Logical. If \code{TRUE}, removes outliers before computing correlation
(default: \code{FALSE}).}

\item{outfun}{Outlier detection function (default: \code{outpro} or \code{outmgvf}).}

\item{...}{Additional arguments passed to the regression function.}
}
\value{
A list with components:
\describe{
\item{n}{Vector of sample sizes for the two groups.}
\item{points}{Matrix of design points where comparisons were made.}
\item{output}{Matrix with columns: Est 1 (group 1 estimate), Est 2
(group 2 estimate), DIF (difference), TEST (test statistic), se (standard error),
ci.low (lower CI), ci.hi (upper CI), p.value.}
}
}
\description{
Compares regression lines of two independent groups using robust regression
(default: Theil-Sen) when there are multiple covariates. Generalizes
\code{\link{ancts}} to handle more than one covariate.
}
\details{
This function extends \code{\link{ancts}} to multiple covariates. Design points
are selected based on data depth when \code{pts = NULL}, ensuring comparisons are made
at representative covariate combinations.

Standard errors are estimated via bootstrap. Critical values are adjusted for
multiple comparisons using either Rom's method (for ≤28 tests) or Simes' method
(for >28 tests).

The Theil-Sen estimator provides robust regression that is resistant to outliers
and does not require normality assumptions.
}
\examples{
\dontrun{
# Compare two groups with two covariates
set.seed(123)
x1 <- matrix(rnorm(60), ncol = 2)
y1 <- 2 + 3*x1[,1] + 2*x1[,2] + rnorm(30)
x2 <- matrix(rnorm(60), ncol = 2)
y2 <- 1 + 3*x2[,1] + 2*x2[,2] + rnorm(30)

result <- anctsmp(x1, y1, x2, y2, nboot = 500)
print(result$output)
}
}
\seealso{
\code{\link{ancts}} for single covariate, \code{\link{anctsmcp}}
for multiple groups
}
