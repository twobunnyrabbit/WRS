% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{logistic.LR}
\alias{logistic.LR}
\title{Likelihood Ratio Test for Logistic Regression}
\usage{
logistic.LR(
  x,
  y,
  xout = FALSE,
  outfun = outpro,
  ROB = FALSE,
  ADJ = TRUE,
  reps = 5000,
  SEED = TRUE
)
}
\arguments{
\item{x}{Matrix or data frame of predictor variables.}

\item{y}{Numeric vector of binary outcomes (0/1).}

\item{xout}{Logical. If TRUE, removes outliers from predictors (ignored if \code{ROB=TRUE}).
Default: FALSE.}

\item{outfun}{Function for multivariate outlier detection (default: \code{\link{outpro}}).}

\item{ROB}{Logical. If TRUE, uses Bianco and Yohai (1996) robust estimator for initial
estimates and automatically handles leverage points (default: FALSE).}

\item{ADJ}{Logical. If TRUE, adjusts p-value via simulation to improve small-sample
performance (default: TRUE).}

\item{reps}{Integer. Number of simulation replications for p-value adjustment (default: 5000).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}
}
\value{
Numeric p-value for the likelihood ratio test. If \code{ADJ=TRUE}, returns the
simulation-adjusted p-value.
}
\description{
Performs a likelihood ratio test that all slope parameters in a logistic regression
model equal zero (i.e., tests overall model significance). Includes option for
adjusted p-value via simulation and robust initial estimates.
}
\details{
\strong{Test statistic}: LR = -2(log-likelihood under null - log-likelihood under full model)

\strong{Null model}: Intercept only (all slopes = 0)

\strong{Full model}: Intercept + all predictors

\strong{P-value adjustment} (if \code{ADJ=TRUE}):
\enumerate{
\item Simulates data under null hypothesis (independent X, Y ~ Binomial(0.5))
\item Computes LR test statistic for each simulation
\item Reports proportion of simulated LR values ≥ observed LR
}

This adjustment can improve small-sample performance and accounts for the
discrete nature of binary responses.
}
\examples{
set.seed(123)
x <- matrix(rnorm(100), ncol = 5)
p <- plogis(x[,1] + 0.5*x[,2])  # Only 2 predictors matter
y <- rbinom(20, 1, p)
logistic.LR(x, y, ADJ = FALSE)  # Test all slopes = 0
}
\references{
Bianco, A. M., & Yohai, V. J. (1996). Robust estimation in the logistic regression model.
In \emph{Robust Statistics, Data Analysis, and Computer Intensive Methods} (pp. 17-34). Springer.
}
\seealso{
\code{\link{logistic.lasso}} for lasso regularization,
\code{\link{logreg.pred}} for logistic regression predictions,
\code{\link{wlogreg}} for robust logistic regression
}
\keyword{htest}
\keyword{regression}
\keyword{robust}
