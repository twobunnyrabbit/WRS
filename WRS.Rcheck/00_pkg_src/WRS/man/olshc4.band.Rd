% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{olshc4.band}
\alias{olshc4.band}
\title{Heteroscedastic OLS Confidence Band with HC4 Estimator}
\usage{
olshc4.band(
  x,
  y,
  alpha = 0.05,
  xout = FALSE,
  outfun = outpro,
  plotit = TRUE,
  pr = TRUE,
  xlab = "X",
  ylab = "Y",
  nreps = 5000,
  pch = ".",
  CI = FALSE,
  ADJ = TRUE,
  SEED = TRUE
)
}
\arguments{
\item{x}{Predictor variable (must be univariate).}

\item{y}{Response variable.}

\item{alpha}{Significance level. Default is 0.05.}

\item{xout}{Logical. If \code{TRUE}, eliminate outliers. Default is \code{FALSE}.}

\item{outfun}{Outlier detection function. Default is \code{\link{outpro}}.}

\item{plotit}{Logical. Create plot with confidence band. Default is \code{TRUE}.}

\item{pr}{Logical. Print messages. Default is \code{TRUE}.}

\item{xlab}{X-axis label. Default is 'X'.}

\item{ylab}{Y-axis label. Default is 'Y'.}

\item{nreps}{Number of replications for critical value estimation. Default is 5000.}

\item{pch}{Plotting character for points. Default is '.'.}

\item{CI}{Logical. If \code{TRUE}, return confidence intervals. If \code{FALSE},
only create plot. Default is \code{FALSE}.}

\item{ADJ}{Logical. Adjust critical value for simultaneous coverage. Default is \code{TRUE}.}

\item{SEED}{Logical. Set random seed for reproducibility. Default is \code{TRUE}.}
}
\value{
A list with component:
\item{conf.intervals}{Matrix with columns X, Yhat, ci.low, ci.up, or \code{NA}
if \code{CI=FALSE}.}
}
\description{
Computes heteroscedastic confidence band for OLS regression with simultaneous
probability coverage 1-alpha using HC4 standard errors.
}
\details{
Only one independent variable is allowed. The confidence band has simultaneous
probability coverage, meaning the entire band covers the regression line with
probability 1-alpha, not just pointwise coverage.
}
\seealso{
\code{\link{olshc4}}, \code{\link{olshc4.bandCV}}, \code{\link{ols}}
}
\keyword{regression}
