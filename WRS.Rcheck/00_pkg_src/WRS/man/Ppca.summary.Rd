% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{Ppca.summary}
\alias{Ppca.summary}
\title{Summary of Robust PCA Across Multiple Dimensions}
\usage{
Ppca.summary(x, MC = FALSE, SCALE = TRUE, p = NULL)
}
\arguments{
\item{x}{Numeric matrix with observations in rows.}

\item{MC}{Logical. If \code{TRUE}, use parallel computation via \code{mclapply};
if \code{FALSE}, use sequential computation (default: \code{FALSE}).}

\item{SCALE}{Logical. Scale variables using robust scatter (default: \code{TRUE}).}

\item{p}{Numeric. Maximum number of components to evaluate, or \code{NULL} to use ncol(x).}
}
\value{
List with component:
\itemize{
\item \code{summary}: 3 × p matrix with rows:
\itemize{
\item "Num. of Comp.": Number of components (1 to p)
\item "Gen.Stand.Dev": Robust generalized standard deviation
\item "Relative Size": Proportion relative to maximum value
}
}
}
\description{
Computes robust PCA (\code{\link{Ppca}}) for dimension reductions from 1 to p components.
Reports generalized standard deviations and their relative sizes.
}
\details{
For each dimension reduction from 1 to p components, computes the robust generalized
standard deviation using \code{\link{Ppca}}. The relative size row helps identify
how much variance is captured by each number of components, analogous to a scree plot.

When \code{MC=TRUE}, uses parallel processing for faster computation on multicore systems.
}
\examples{
\dontrun{
# Evaluate robust PCA for 1 to 5 components
x <- matrix(rnorm(100 * 5), ncol = 5)
summary_result <- Ppca.summary(x)
print(summary_result$summary)
}

}
\seealso{
\code{\link{Ppca}}, \code{\link{Ppca.sum.sub}}, \code{\link{robpca}}
}
\keyword{multivariate}
\keyword{robust}
