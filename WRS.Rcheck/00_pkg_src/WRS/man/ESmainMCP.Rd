% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{ESmainMCP}
\alias{ESmainMCP}
\title{Main Effect Sizes for All Pairwise Comparisons in Two-Way Design}
\usage{
ESmainMCP(J, K, x, tr = 0.2, nboot = 100, SEED = TRUE)
}
\arguments{
\item{J}{Number of levels for Factor A}

\item{K}{Number of levels for Factor B}

\item{x}{Data in matrix/data frame format (will be converted to list) or
as a list of length J*K}

\item{tr}{Trimming proportion for \code{\link{yuenv2}} (default: 0.2)}

\item{nboot}{Number of bootstrap samples for effect size computation (default: 100)}

\item{SEED}{Logical or numeric. Controls random seed. Default is \code{TRUE}.}
}
\value{
A list with components:
\item{Factor.A}{Matrix with columns: Level, Level, Effect.Size (for Factor A comparisons)}
\item{Factor.B}{Matrix with columns: Level, Level, Effect.Size (for Factor B comparisons)}
}
\description{
Computes explanatory effect sizes for all pairwise comparisons of main
effects in a two-way ANOVA design (J × K factorial).
}
\details{
For each factor, computes effect sizes for all pairwise level comparisons:
\itemize{
\item Factor A: All j < j' comparisons (pooling across Factor B levels)
\item Factor B: All k < k' comparisons (pooling across Factor A levels)
}

Effect sizes are computed using \code{\link{yuenv2}} with the explanatory
measure based on trimmed means. The function uses contrast matrices from
\code{\link{con2way}} to identify pairwise comparisons, then pools data
across the other factor before computing effect sizes.
}
\seealso{
\code{\link{yuenv2}}, \code{\link{con2way}}, \code{\link{bw.es.main}},
\code{\link{bb.es.main}}
}
\keyword{htest}
