% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{logadr}
\alias{logadr}
\title{Additive Model for Binary Response Using Robust Smoothing}
\usage{
logadr(
  x,
  y,
  est = mean,
  iter = 10,
  pyhat = FALSE,
  plotit = TRUE,
  fr = 0.8,
  xout = FALSE,
  eout = xout,
  outfun = out,
  theta = 50,
  phi = 25,
  expand = 0.5,
  STAND = TRUE,
  ticktype = "simple",
  scale = FALSE,
  ...
)
}
\arguments{
\item{x}{Matrix or data frame of predictor variables.}

\item{y}{Numeric vector of binary outcomes (0/1).}

\item{est}{Function for location estimation (default: \code{mean}).}

\item{iter}{Integer. Maximum iterations for backfitting (default: 10).}

\item{pyhat}{Logical. If TRUE, returns fitted probabilities; if FALSE, returns "Done" (default: FALSE).}

\item{plotit}{Logical. If TRUE and p=2, creates 3D surface plot (default: TRUE).}

\item{fr}{Numeric. Span parameter for smoother (default: 0.8).}

\item{xout}{Logical. If TRUE, removes outliers from predictors (default: FALSE).}

\item{eout}{Logical. If TRUE, removes outliers (defaults to \code{xout}).}

\item{outfun}{Function for outlier detection (default: \code{\link{out}}).}

\item{theta}{Numeric. Viewing angle for 3D plot (default: 50).}

\item{phi}{Numeric. Colatitude angle for 3D plot (default: 25).}

\item{expand}{Numeric. Expansion factor for 3D plot (default: 0.5).}

\item{STAND}{Logical. If TRUE, standardizes predictors before fitting (default: TRUE).}

\item{ticktype}{Character. Tick type for 3D plot (default: "simple").}

\item{scale}{Logical. Scale parameter for 3D plot (default: FALSE).}

\item{...}{Additional arguments passed to \code{est} and smoother functions.}
}
\value{
If \code{pyhat=TRUE}, returns numeric vector of fitted probabilities (constrained to \link{0,1}).
If \code{pyhat=FALSE}, returns character string "Done".
}
\description{
Fits an additive model for binary outcomes using a variation of Copas' (1983)
smooth via a backfitting algorithm. Handles multiple predictors with optional
outlier removal and 3D visualization for two predictors.
}
\details{
\strong{Algorithm} (backfitting):
\enumerate{
\item Standardizes predictors if \code{STAND=TRUE}
\item Initializes additive components using \code{\link{logrsm}} for each predictor
\item Iterates: for each predictor, smooth residuals from other components
\item Continues until convergence (change < 0.01) or max iterations reached
\item Sums components and adjusts to ensure probabilities in \link{0,1}
}

For two predictors, creates perspective plot of fitted surface using \code{\link{interp}}.
}
\examples{
\dontrun{
# Two-predictor logistic additive model
set.seed(123)
x <- matrix(rnorm(100), ncol = 2)
p <- plogis(x[,1] + 0.5*x[,2])
y <- rbinom(50, 1, p)
logadr(x, y, plotit = TRUE)
}
}
\references{
Copas, J. B. (1983). Plotting p against x. \emph{Journal of the Royal Statistical Society: Series C}, \emph{32}(1), 25-31.
}
\seealso{
\code{\link{logrsm}} for univariate logistic smoother,
\code{\link{adcom}} for additive model with continuous response,
\code{\link{logSM}} for alternative logistic smooth
}
\keyword{nonparametric}
\keyword{regression}
\keyword{smooth}
