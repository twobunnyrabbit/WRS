% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{r.gauss.pro}
\alias{r.gauss.pro}
\title{Generate Data from a Gaussian Process}
\usage{
r.gauss.pro(n, C, M, t)
}
\arguments{
\item{n}{Integer. The sample size (number of GP realizations).}

\item{C}{A covariance function taking two arguments (x, y) and returning
the covariance between points x and y.}

\item{M}{A mean function taking one argument (x) and returning the mean
at point x.}

\item{t}{A numeric vector of time points at which the GP will be sampled.}
}
\value{
A matrix with \code{n} rows and \code{length(t)} columns, where each
row is a realization of the Gaussian Process.
}
\description{
Generates random samples from a Gaussian Process with specified mean and
covariance functions evaluated at given time points.
}
\details{
This function generates data from a Gaussian Process by:
\enumerate{
\item Computing the mean vector M(t)
\item Constructing the covariance matrix S where S\link{i,j} = C(t\link{i}, t\link{j})
\item Drawing n samples from the multivariate normal distribution with
mean m and covariance S using \code{\link[MASS]{mvrnorm}}
}
}
\examples{
\dontrun{
# Define covariance and mean functions
C <- function(x, y) 0.01 * exp(-10000 * (x - y)^2)
M <- function(x) sin(x)
t <- seq(0, 1, by = 0.01)

# Generate 100 GP realizations
gp_data <- r.gauss.pro(n=100, C=C, M=M, t=t)
}

}
\seealso{
\code{\link[MASS]{mvrnorm}}
}
\keyword{datagen}
