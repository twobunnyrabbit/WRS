% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcp.R
\name{bwmcppb.adj}
\alias{bwmcppb.adj}
\title{Percentile Bootstrap MCP for Between-Within Designs with Custom Adjustment (Internal)}
\usage{
bwmcppb.adj(
  J,
  K,
  x,
  est = tmean,
  JK = J * K,
  method = "hoch",
  alpha = 0.05,
  grp = c(1:JK),
  nboot = 500,
  bhop = TRUE,
  SEED = TRUE,
  ...
)
}
\arguments{
\item{J}{Number of levels of Factor A (between-subjects factor)}

\item{K}{Number of levels of Factor B (within-subjects factor)}

\item{x}{Data in list mode or matrix. For list mode: x[\link{1}] = level (1,1),
x[\link{2}] = level (1,2), ..., x[\link{K}] = level (1,K), x[\link{K+1}] = level (2,1), etc.}

\item{est}{Estimator function (default: \code{tmean} for trimmed mean)}

\item{JK}{Total number of groups (J*K)}

\item{method}{P-value adjustment method applied globally (default: 'hoch')}

\item{alpha}{Family-wise error rate (default: 0.05)}

\item{grp}{Group indices to analyze (default: all groups 1:JK)}

\item{nboot}{Number of bootstrap samples (default: 500)}

\item{bhop}{Logical; if TRUE use Benjamini-Hochberg; if FALSE use specified method
(default: TRUE)}

\item{SEED}{Logical; if TRUE sets random seed for reproducibility (default: TRUE)}

\item{...}{Additional arguments passed to estimator function}
}
\value{
List with components Fac.A, Fac.B, Fac.AB, each containing a matrix
of results with adjusted p-values
}
\description{
Internal helper that performs bootstrap MCP for between-within designs with
custom p-value adjustment applied to all contrasts together. Similar to
\code{bwmcppb.sub} but adjusts p-values across main effects and interactions.
}
\details{
Differs from \code{bwmcppb.sub} by applying the p-value adjustment method
(e.g., Hochberg) to all contrasts together rather than separately for each
effect type.
}
\seealso{
\code{\link{bwmcppb.sub}}, \code{\link{bwmcppb}}
}
\keyword{internal}
