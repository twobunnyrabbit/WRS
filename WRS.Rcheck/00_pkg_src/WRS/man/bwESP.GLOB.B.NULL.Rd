% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{bwESP.GLOB.B.NULL}
\alias{bwESP.GLOB.B.NULL}
\title{Compute Null Distribution for bwESP.GLOB.B}
\usage{
bwESP.GLOB.B.NULL(
  n1,
  n2,
  K,
  tr = 0.2,
  MM = FALSE,
  SEED = TRUE,
  iter = 1000,
  g = 0,
  rho = 0
)
}
\arguments{
\item{n1}{Integer. Sample size for group 1.}

\item{n2}{Integer. Sample size for group 2.}

\item{K}{Integer. Number of repeated measures (columns).}

\item{tr}{Numeric. Trimming proportion for robust estimation (default: 0.2).}

\item{MM}{Logical. If TRUE, uses marginal medians; if FALSE, uses trimmed means (default: FALSE).}

\item{SEED}{Logical. If TRUE, sets random seed to 2 for reproducibility (default: TRUE).}

\item{iter}{Integer. Number of Monte Carlo iterations (default: 1000).}

\item{g}{Numeric. g parameter for g-and-h distribution (default: 0 for normal).}

\item{rho}{Numeric. Common correlation for simulated data (default: 0).}
}
\value{
Numeric vector of length \code{iter} containing null distribution of
effect size differences.
}
\description{
Generates null distribution for comparing global projection effect sizes in
a 2×K between-within design. Used to calibrate p-values in \code{\link{bwESP.GLOB.B}}.
}
\details{
Generates null distribution under H0: Equal projection effect sizes:
\enumerate{
\item Simulates data from multivariate g-and-h distributions with specified correlation
\item Computes projection effect size for each group via \code{\link{rmESPRO.est}}
\item Computes difference in effect sizes
\item Repeats for \code{iter} iterations
}

The null distribution can be pre-computed and passed to \code{\link{bwESP.GLOB.B}}
via the \code{ND} parameter to avoid re-computation.
}
\examples{
# Generate null distribution for 2x3 design
ND <- bwESP.GLOB.B.NULL(n1 = 20, n2 = 20, K = 3, iter = 500)
hist(ND)
}
\seealso{
\code{\link{bwESP.GLOB.B}} for using the null distribution,
\code{\link{rmESPRO.est}} for projection effect size computation
}
