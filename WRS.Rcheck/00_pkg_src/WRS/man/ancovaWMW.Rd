% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two-sample.R
\name{ancovaWMW}
\alias{ancovaWMW}
\title{ANCOVA Using WMW Method for Two Independent Groups}
\usage{
ancovaWMW(
  x1,
  y1,
  x2,
  y2,
  fr1 = 1,
  fr2 = 1,
  alpha = 0.05,
  sm = FALSE,
  est = tmean,
  plotit = TRUE,
  pts = NA,
  xout = FALSE,
  outfun = out,
  LP = TRUE,
  ...
)
}
\arguments{
\item{x1}{Covariate values for group 1.}

\item{y1}{Response values for group 1.}

\item{x2}{Covariate values for group 2.}

\item{y2}{Response values for group 2.}

\item{fr1}{Span for the running interval smoother for group 1 (default 1).}

\item{fr2}{Span for the running interval smoother for group 2 (default 1).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{sm}{Logical; deprecated parameter (was for bootstrap bagging).}

\item{est}{Estimator function for location (default \code{tmean}).}

\item{plotit}{Logical; if TRUE, creates plots of the regression lines.}

\item{pts}{Optional vector of covariate values at which to compare groups.}

\item{xout}{Logical; if TRUE, removes outliers in the covariate.}

\item{outfun}{Outlier detection function (default \code{out}).}

\item{LP}{Logical; if TRUE, uses running interval smoother followed by LOESS.}

\item{...}{Additional arguments passed to the outlier detection function.}
}
\value{
A matrix with columns:
\item{X}{Covariate value}
\item{n1}{Sample size for group 1 at this X}
\item{n2}{Sample size for group 2 at this X}
\item{p.hat}{Estimated P(Y1 < Y2) at this X}
\item{ci.low}{Lower confidence limit}
\item{ci.hi}{Upper confidence limit}
\item{p.value}{P-value for testing P(Y1 < Y2) = 0.5}
\item{p.crit}{Critical p-value after adjustment}
}
\description{
Compares two independent groups using an ANCOVA-type method in conjunction with
Cliff's improvement on the Wilcoxon-Mann-Whitney test. Makes no parametric assumptions
about the form of regression lines; uses a running interval smoother instead.
}
\details{
This function compares two groups while adjusting for a covariate using a robust
nonparametric approach. At specified covariate values (either automatically chosen
or user-specified via \code{pts}), the function estimates P(Y1 < Y2) using Cliff's
method within neighborhoods defined by the running interval smoother.

The function allows only one covariate. For multiple covariates, see related functions.
}
\examples{
\dontrun{
# Example with simulated data
set.seed(123)
n <- 50
x1 <- rnorm(n)
y1 <- x1 + rnorm(n)
x2 <- rnorm(n)
y2 <- x2 + 0.5 + rnorm(n)

result <- ancovaWMW(x1, y1, x2, y2)
}
}
\seealso{
\code{\link{cid}}, \code{\link{cidv2}}, \code{\link{wmw}}
}
