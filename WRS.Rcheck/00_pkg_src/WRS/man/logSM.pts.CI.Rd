% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/special.R
\name{logSM.pts.CI}
\alias{logSM.pts.CI}
\title{Compare Success Probabilities at Multiple Covariate Values (Two Groups)}
\usage{
logSM.pts.CI(
  x1,
  y1,
  x2,
  y2,
  pts = NULL,
  alpha = 0.05,
  method = "BH",
  fr = 2,
  npts = 5,
  xlab = "X",
  ylab = "Est. Dif",
  xout = FALSE,
  SEED = TRUE,
  outfun = out,
  plotit = TRUE,
  nboot = 1000,
  ...
)
}
\arguments{
\item{x1}{Numeric vector of covariate values for group 1.}

\item{y1}{Binary response vector for group 1 (0/1).}

\item{x2}{Numeric vector of covariate values for group 2.}

\item{y2}{Binary response vector for group 2 (0/1).}

\item{pts}{Numeric vector of covariate values at which to compare groups.
If \code{NULL} (default), automatically selects \code{npts} evenly-spaced
values where both groups have adequate sample sizes.}

\item{alpha}{Numeric. Significance level for confidence intervals (default: 0.05).}

\item{method}{Character. Method for p-value adjustment to control familywise
error rate. Options: \code{"BH"} (Benjamini-Hochberg, default), \code{"hoch"}
(Hochberg), or other methods accepted by \code{\link{p.adjust}}.}

\item{fr}{Numeric. Bandwidth/span for running-interval smoother (default: 2).}

\item{npts}{Integer. Number of evaluation points if \code{pts=NULL} (default: 5).}

\item{xlab}{Character. X-axis label for plot (default: "X").}

\item{ylab}{Character. Y-axis label for plot (default: "Est. Dif").}

\item{xout}{Logical. If \code{TRUE}, removes outliers before smoothing (default: \code{FALSE}).}

\item{SEED}{Logical. If \code{TRUE}, sets random seed for reproducible results
(default: \code{TRUE}).}

\item{outfun}{Function for outlier detection (default: \code{\link{out}}).}

\item{plotit}{Logical. If \code{TRUE}, creates diagnostic plot (default: \code{TRUE}).}

\item{nboot}{Integer. Number of bootstrap samples (default: 1000).}

\item{...}{Additional arguments passed to \code{outfun}.}
}
\value{
Matrix with \code{npts} rows and 8 columns:
\describe{
\item{pts}{Covariate value}
\item{p1}{Estimated P(success|X) for group 1}
\item{p2}{Estimated P(success|X) for group 2}
\item{est.dif}{Estimated difference p1 - p2}
\item{ci.lower}{Lower (1-alpha) confidence bound for difference}
\item{ci.upper}{Upper (1-alpha) confidence bound for difference}
\item{p.value}{Unadjusted p-value for H0: p1 = p2}
\item{p.adjusted}{Multiplicity-adjusted p-value}
}
}
\description{
Compares probability of success between two independent groups across multiple
covariate values using running-interval smoothing. Provides confidence intervals
and multiplicity-adjusted p-values at each evaluation point.
}
\details{
For each evaluation point in \code{pts}:
\enumerate{
\item Uses running-interval smoother via \code{\link{logSMpred}} to estimate
P(Y=1|X) for each group
\item Bootstraps difference in probabilities
\item Constructs confidence interval from bootstrap distribution
\item Computes p-value and adjusts for multiple comparisons
}

If \code{plotit=TRUE}, creates scatter plot showing estimated difference
(points) and confidence bounds ('+' symbols) at each evaluation point.
}
\examples{
\dontrun{
set.seed(123)
# Group 1: lower success probability
x1 <- rnorm(50)
p1 <- plogis(0.2*x1)
y1 <- rbinom(50, 1, p1)

# Group 2: higher success probability
x2 <- rnorm(60)
p2 <- plogis(0.2*x2 + 0.5)
y2 <- rbinom(60, 1, p2)

# Compare at 5 covariate values
logSM.pts.CI(x1, y1, x2, y2, npts = 5, nboot = 500)
}
}
\seealso{
\code{\link{logSMpred}}, \code{\link{logSM}}, \code{\link{logrsm}},
\code{\link{p.adjust}}
}
\keyword{htest}
\keyword{regression}
\keyword{smooth}
